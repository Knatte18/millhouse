# Tasks

<!--
Tasks appear below as `## <slug>` headings, one per task.
Add new tasks with `/mill-add <slug> --description "..."`.
List active tasks with `/mill-list`.

Archived (completed) tasks live under `archive/<slug>/` in this wiki.
Abandoned tasks return here with a note.
-->
