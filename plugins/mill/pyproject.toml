[project]
name = "mill"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pyyaml>=6.0"]

[tool.pytest.ini_options]
pythonpath = ["scripts"]

[tool.ruff.lint.per-file-ignores]
"scripts/*.py" = ["E402"]
