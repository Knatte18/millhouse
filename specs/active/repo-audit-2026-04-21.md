```yaml
date: 2026-04-21
author: Claude Sonnet 4.6 (audit agent)
scope: hub + wiki repos; excludes plugins/mill/integration_tests/ (concurrent edits)
canonical-spec: specs/active/layer-02/discussion.md
```

# Repo audit — 2026-04-21

---

## 1. Directory-level overview

### `specs/`

| Directory | Purpose | Status |
|---|---|---|
| `specs/active/layer-02/` | In-flight Layer 02 design: canonical spec, review rounds, implementation plan | **CANONICAL** |
| `specs/roadmap/` | High-level layer-status table; simplified 2026-04-21 to lean status index | **CANONICAL** |
| `specs/_legacy/` | Pre-discussion drafts superseded by converged design; `README.md` inside marks them as non-authoritative | **ARCHIVE** |
| `specs/_starter/` | Session handoff written at Layer 01 completion; describes state that is now many commits stale | **STALE** |

### `plugins/mill/`

| Directory | Purpose | Status |
|---|---|---|
| `plugins/mill/scripts/` | All production Python: API scripts, review backends, reviewers, LLM-provider, shared helpers | **CANONICAL** |
| `plugins/mill/scripts/__pycache__/` | Python bytecode cache; gitignored in practice | GENERATED (ignored) |
| `plugins/mill/skills/` | One `SKILL.md` per named skill; all loaded by Claude Code at session start | **CANONICAL** (see per-file notes) |
| `plugins/mill/templates/` | Review prompt templates + output schema + config/vscode seed files | **CANONICAL** |
| `plugins/mill/integration_tests/` | Local-dev-only integration tests (out of audit scope; under concurrent edit) | — |

### `plugins/` (non-mill)

| Plugin | Status |
|---|---|
| `plugins/codeguide/` | Canonical; ships 4 skills + scripts + templates for doc generation |
| `plugins/csharp/` | Canonical; 3 language skills |
| `plugins/python/` | Canonical; 3 language skills |
| `plugins/weblens/` | Canonical; web-fetch skill |

### Hub root

| Item | Status |
|---|---|
| `.claude-plugin/marketplace.json` | **CANONICAL** — registers all 5 plugins |
| `.gitignore` | **CANONICAL** — covers `**/.millhouse/`, `**/.env`, `**/worktrees/`, `__pycache__/` |
| `.millhouse/` | Local runtime state; gitignored. Contains `config.local.yaml`, `wiki` junction, `scratch/` |
| `.vscode/settings.json` | Generated by `mill-setup` (green title bar). Gitignored via `**/.vscode/` |
| `LICENSE` | Standard file |
| `SKILLS.md` | **MISSING** — referenced by `workflow/SKILL.md` but never generated |

### Wiki repo (`c:/Code/millhouse/wiki/`)

| File | Status |
|---|---|
| `Home.md` | **CANONICAL** — task registry; 3 tasks: `skills-index-rebuild`, `m1-4-mill-list`, `handler-template-cluster-support` |
| `_Sidebar.md` | **GENERATED** — regenerated by `_sidebar.py` via `mill-add`, `mill-setup` |
| `config.yaml` | **CANONICAL** — shared Layer 01+02 config; paths + review sections |
| `proposal-skills-index-rebuild.md` | **CANONICAL** — background proposal for skills-index task |

---

## 2. Per-file status

### `specs/_starter/handoff.md` — STALE

Written at Layer 01 completion (2026-04-20). Describes a state that is entirely obsolete:

- Refers to five files that no longer exist at their stated paths: `specs/00-overview.md`, `specs/roadmap/M2-review.md`, `specs/layer-02-review.md`, `specs/ref-formats.md`, `specs/ref-v1-reuse.md`. All five moved to `specs/_legacy/` (or never existed at the root level).
- Its "Start sequence" points to `_legacy/` content without knowing it.
- Reports `layer-01-done` as the current tag; hub is now well past that.
- Instructs reader not to implement anything — irrelevant; implementation is done.

This file serves no guidance function. It is pure noise for any agent reading the repo.

### `specs/roadmap/README.md` — CANONICAL

Simplified today (`db528ae` — "collapse to lean status index; drop per-M files"). Now a single status table + deviation protocol. Accurately reflects current state: Layer 01 done, Layer 02 impl done (awaiting integration-test run), Layers 03–04 not started. No issues.

### `specs/active/layer-02/discussion.md` — CANONICAL

Ground truth for Layer 02. Accurate: 4-layer architecture, config contract, CLI contract, template list, round-discovery rules, all 35 decisions. One known divergence from implementation: the discussion says `run_tool_use` allows `Read,Grep,Write`; the implementation (and `plan/00-overview.md`) uses `Read,Grep,Glob`. This is a documented override (Decision in `plan/00-overview.md` under "Decision: run_tool_use exposes Read,Grep,Glob"). Not a bug; the plan supersedes the discussion on this point. The discussion itself is not wrong — it is slightly outdated on this one field.

### `specs/active/layer-02/plan/00-overview.md` — CANONICAL (with format violation)

Uses `---`-style YAML frontmatter, as do all six plan batch files (`01-foundation.md` through `06-integration-tests.md`). The markdown skill explicitly reserves `---` frontmatter for `SKILL.md` and plugin manifests. Plan files are generated artefacts in the spec tree — they should use fenced ```yaml blocks. This is a format-rule violation but does not break any tooling currently (no script reads plan frontmatter except the human-readable `approved:` field).

### `specs/active/layer-02/plan/06-integration-tests.md` — STALE

Describes tests as `.ps1` PowerShell scripts. The actual tests (`test-review-discussion.py`, `test-review-plan.py`, `test-review-code.py`) are Python, per commit `070e126` ("add Python integration tests"). The fixture list also refers to `.ps1` filenames. The plan batch file was never updated after the migration.

`plan/00-overview.md`'s "All Files Touched" list also still references `.ps1` filenames and `fixtures/sample-*.md` fixture files; the actual implementation uses `.millhouse/scratch/` for fixtures and inline test data instead.

### `plugins/mill/skills/workflow/SKILL.md` — STALE (broken reference)

Line 14: "Read `SKILLS.md` (repo root) — generated by `mill-skills-index`." `SKILLS.md` does not exist at the hub root. `mill-skills-index` has not been implemented. The reference is aspirational but will confuse any agent that tries to act on it. The wiki task `skills-index-rebuild` tracks this.

### `plugins/mill/skills/markdown/SKILL.md` — CANONICAL (broken internal reference)

Line: "Full format reference: `plugins/mill/doc/formats/markdown.md`" — this path does not exist. `plugins/mill/doc/` does not exist. The SKILL.md is self-contained enough to be usable, but the pointer is dead.

### `plugins/mill/skills/linting/SKILL.md` — WORK-IN-PROGRESS

Contains placeholder comment `<!-- Project-specific linting rules go here -->` with no actual rules. Functional as a stub; signals the spot for future project-specific rules.

### `plugins/mill/skills/millhouse-issue/SKILL.md` — CANONICAL

References `Knatte18/millhouse-legacy` as the issue target repo. Verify this is still the intended target (the hub repo is `Knatte18/millhouse`). If the issue tracker moved, this is stale.

### `plugins/mill/templates/config.local.yaml` — STALE (stale example key)

The `examples:` block includes `pipeline: code-review-model: sonnet-max`. The `pipeline:` key was superseded by the `review:` structure in Layer 02 (`discussion.md` decision 22). No script reads `pipeline:` from `config.local.yaml` anymore. The example misleads future users.

### `plugins/mill/templates/Home.md` — CANONICAL

Seed template for new wikis. Used by `mill-setup` Phase 6.

### `plugins/mill/templates/review-output.schema.md` — CANONICAL

Defines verdict vocabulary, metadata block, finding severity (`BLOCKING`/`NIT`), canonical filenames. Matches implementation. No issues.

### `plugins/mill/templates/review-discussion.md` / `review-plan-batch.md` / `review-plan-holistic.md` / `review-code-single.md` / `review-code-multi.md` — CANONICAL

Five prompt templates in production use. Not read here in full, but all five exist as expected by `discussion.md`.

### `plugins/mill/scripts/` — summary

| Script | Role | Status |
|---|---|---|
| `mill-add.py` | Task logging to wiki | CANONICAL |
| `mill-list.py` | List tasks from wiki | CANONICAL |
| `mill-review-discussion.py` | API entry point | CANONICAL |
| `mill-review-plan.py` | API entry point | CANONICAL |
| `mill-review-code.py` | API entry point | CANONICAL |
| `_review_common.py` | Shared helpers + exceptions | CANONICAL |
| `_review_discussion.py` | Discussion backend | CANONICAL |
| `_review_plan.py` | Plan backend | CANONICAL |
| `_review_code.py` | Code backend | CANONICAL |
| `_reviewer_sonnetmax.py` | Bulk reviewer (MODE=bulk) | CANONICAL |
| `_reviewer_sonnetmax_tool.py` | Tool-use reviewer (MODE=tool-use) | CANONICAL |
| `_llm_claude.py` | Claude LLM-provider | CANONICAL |
| `_render.py` | Token substitution for templates | CANONICAL |
| `_sidebar.py` | Wiki `_Sidebar.md` generator | CANONICAL |
| `_wiki.py` | Wiki lock + commit + push helper | CANONICAL |
| `_junction.py` | Cross-platform directory junction | CANONICAL |
| `_vscode.py` | VS Code settings helper | CANONICAL |
| `_subprocess_util.py` | Subprocess wrapper | CANONICAL |

### `.millhouse/config.local.yaml` — STALE

The live local config (gitignored, present on disk) still uses the `pipeline:` example comment from the template. The file is a user-local override and is never committed, so this is a runtime issue only, not a repo issue. Noted for completeness.

### `plugins/mill/integration_tests/test-bootstrap.ps1` — STALE

Uses `$env:TEMP` for fixtures. The `conversation/SKILL.md` explicitly prohibits writing to system temp directories and mandates `.millhouse/scratch/` instead. The Python review tests (which replaced the PS1 review tests) use `.millhouse/scratch/`. `test-bootstrap.ps1` (Layer 01 test) still uses `$env:TEMP`.

### `wiki/proposal-skills-index-rebuild.md` — CANONICAL (encoding issue)

File starts with a UTF-8 BOM (`EF BB BF`) and contains Windows-1252-style curly quotes encoded as multi-byte UTF-8 sequences (visible in the xxd dump). The rendered content is readable, but the BOM and encoding artefacts will cause issues if scripts read this file and do string comparisons. The text content is correct.

---

## 3. Cross-cutting issues

### Broken references

| Source | Broken reference | Actual location |
|---|---|---|
| `specs/_starter/handoff.md` | `../00-overview.md` | `specs/_legacy/00-overview.md` |
| `specs/_starter/handoff.md` | `../roadmap/M2-review.md` | does not exist (was never created at that path) |
| `specs/_starter/handoff.md` | `../layer-02-review.md` | `specs/_legacy/layer-02-review.md` |
| `specs/_starter/handoff.md` | `../ref-formats.md` | `specs/_legacy/ref-formats.md` |
| `specs/_starter/handoff.md` | `../ref-v1-reuse.md` | `specs/_legacy/ref-v1-reuse.md` |
| `plugins/mill/skills/workflow/SKILL.md` | `SKILLS.md` (repo root) | does not exist |
| `plugins/mill/skills/markdown/SKILL.md` | `plugins/mill/doc/formats/markdown.md` | does not exist |
| `plugins/mill/skills/millhouse-issue/SKILL.md` | target repo `Knatte18/millhouse-legacy` | verify this is still current |
| `specs/active/layer-02/plan/06-integration-tests.md` | `test-review-*.ps1` | now `test-review-*.py` |
| `specs/active/layer-02/plan/00-overview.md` ("All Files Touched") | `integration_tests/test-review-*.ps1` | `integration_tests/test-review-*.py` |
| `specs/active/layer-02/discussion.md` (supersession note) | `specs/ref-formats.md`, `specs/00-overview.md`, `specs/layer-02-review.md` | all in `specs/_legacy/` now |
| `plugins/mill/scripts/_sidebar.py` (docstring) | `ref-formats.md` | `specs/_legacy/ref-formats.md` |

### Naming inconsistency

- `discussion.md` (line 576, task flow diagram): `run_tool_use` allows `Read,Grep,Write`. Plan `00-overview.md` and implementation: `Read,Grep,Glob`. These refer to the same function with different tool lists. The plan deliberately overrides the discussion, but there is no supersession note in `discussion.md` pointing to this change.
- `config.local.yaml` template uses the stale `pipeline:` key; `wiki/config.yaml` uses the canonical `review:` key.

### Duplicated content with drift risk

- `discussion.md` § "Config contract" and `wiki/config.yaml` are the same config schema in two places. The wiki file is authoritative (it is the actual config); the discussion is the spec. They currently match. If the schema evolves, both must be updated.
- `discussion.md` § "File structure (flat)" lists the exact same file set as `plan/00-overview.md` § "All Files Touched". The plan list has drifted (`.ps1` → `.py` for integration tests, fixture files differ). Two canonical file lists in the same repo with drift is a maintenance burden.

### Format-rule violations

The markdown skill says: use fenced ```yaml for metadata in generated `.md` files; reserve `---` frontmatter for `SKILL.md` and plugin manifests.

Violations found:
- All 7 plan files under `specs/active/layer-02/plan/` use `---` YAML frontmatter.
- `specs/active/layer-02/discussion-review-r5.md` opens with `---` then switches to fenced yaml for the review metadata. The `---` block is a section separator, not frontmatter, so this is borderline — not clearly a violation.

### Orphaned / unreachable

- `specs/_starter/handoff.md` — not referenced by any other file; its role (session continuity) was superseded by the spec tree itself. Zero utility.
- `plugins/mill/skills/linting/SKILL.md` — functional stub but contains no rules. An agent invoking it gets nothing actionable.
- `plugins/mill/integration_tests/test-bootstrap.ps1` — Layer 01 integration test; not referenced by any plan batch or wiki task. Its status relative to the review tests (which were migrated to Python) is unclear — it is still PS1.

---

## 4. Git-state snapshot

### Hub repo — recent history

```
d2cd69f skill(conversation): update File Writing for v2 layout
34bcf20 test(review/code): inline modified-file content; drop brittle patch fixture
b5eb093 test(review): use production parse_verdict instead of duplicate parser
2de2547 review(discussion): split severity to v1 GAP/NOTE + GAPS_FOUND verdict
e084c3f review(common): parse verdict from fenced yaml block
be6d01e review: switch templates to fenced yaml block per markdown skill
02427a1 review: tighten templates to v1-style strict format + copy receiving-review skill
46eb702 fix(review): resolve claude via shutil.which, add required --verbose flag
db528ae roadmap: collapse to lean status index; drop per-M files
5286380 test(review): remove superseded PowerShell integration tests
070e126 test(review): add Python integration tests for discussion/plan/code
c58597b specs: restore roadmap/ to specs/ root
7901b3c specs: move legacy ideas to _legacy/, add Layer 02 active work
2ab5225 test(review): integration test for code review
275ed26 test(review): integration test for plan review
c21d182 test(review): integration test for discussion review
dc0fd30 feat(review): add mill-review-discussion/-plan/-code API scripts
0a5bc8a feat(review): add _review_code.py backend
b1a2c0e feat(review): add _review_plan.py backend with parallel batches
12833ad feat(review): add _review_discussion.py backend
```

### Hub repo — current status

```
 M plugins/mill/integration_tests/smoke-llm-claude.py
 M plugins/mill/integration_tests/test-review-code.py
 M plugins/mill/integration_tests/test-review-discussion.py
 M plugins/mill/integration_tests/test-review-plan.py
```

Four integration test files modified (under concurrent edit by another agent — out of audit scope).

### Wiki repo — recent history

```
1bb0053 task: close review-severity-vocab; add handler-template-cluster-support
28a2c4f task: complete roadmap-sync-layer-02
1c39dbe task: complete integration-tests-ps1-to-py
15587a8 add task: review-severity-vocab (revisit BLOCKING/NIT unification)
b58e46d add tasks: roadmap-sync-layer-02, integration-tests-ps1-to-py
c90b27d feat(config): add paths + review sections for Layer 02
7355e2c add task: m1-4-mill-list
11c66e1 sidebar: Navigation section first, Tasks below
02a64f2 cleanup: drop format-preview demos; adopt flat proposals + sidebar
43f8bc2 preview: flat-namespace variants (A: slug-as-filename, B: prefixed)
```

### Wiki repo — current status

Clean (no uncommitted changes).

---

## Recommended actions

### P1 (blocking confusion — fix next session)

- **Delete `specs/_starter/handoff.md`.**  
  All five file references are broken (targets moved to `_legacy/`). The session-continuity role it served is obsolete. Any agent reading it will follow dead links.

- **Update `plugins/mill/skills/workflow/SKILL.md` to remove the `SKILLS.md` reference.**  
  `SKILLS.md` does not exist. The line "Read `SKILLS.md` (repo root)" actively misleads agents into looking for a file that is not there. Remove the sentence or replace with "No index yet — browse `plugins/*/skills/*/SKILL.md` directly."

- **Update `plugins/mill/templates/config.local.yaml` to drop the `pipeline:` example.**  
  The `pipeline:` key is dead. Replace the example block with `review:` override syntax matching the current `wiki/config.yaml` shape, or remove examples entirely.

### P2 (nice to have)

- **Update `specs/active/layer-02/plan/06-integration-tests.md` to reflect the PS1 → Python migration.**  
  The plan batch is the spec for what was built. It currently describes `.ps1` files that were deleted. Update the file list and the "PowerShell is primary" note to reflect Python tests. This matters if anyone re-reads the plan as a build record.

- **Add a supersession note in `discussion.md` for the `run_tool_use` tool-list change.**  
  One sentence: "Decision amended by plan/00-overview.md: `run_tool_use` uses `Read,Grep,Glob` (not `Read,Grep,Write`)." The plan has the rationale; the discussion should acknowledge the delta.

- **Fix `plugins/mill/skills/markdown/SKILL.md` dead reference.**  
  `plugins/mill/doc/formats/markdown.md` does not exist. Either create the file (a format guide would be useful) or remove the pointer.

- **Fix encoding of `wiki/proposal-skills-index-rebuild.md`.**  
  Remove the UTF-8 BOM and replace curly-quote sequences with straight ASCII or proper UTF-8. Small risk currently; becomes a problem if scripts do string matching on this file.

- **Verify `millhouse-issue/SKILL.md` target repo.**  
  It points at `Knatte18/millhouse-legacy`. Confirm this is still where issues should land, not `Knatte18/millhouse`.

### P3 (defer)

- **Convert plan-batch frontmatter from `---` to fenced ```yaml.**  
  Technically violates the markdown skill. Low practical impact because no script reads plan frontmatter programmatically. Worth doing if plan files are ever templated or machine-read.

- **Fix `test-bootstrap.ps1` to use `.millhouse/scratch/` instead of `$env:TEMP`.**  
  Violates `conversation/SKILL.md` conventions. The test is Layer 01 and works in practice; fixing it is correct but not urgent.

- **Add project-specific rules to `plugins/mill/skills/linting/SKILL.md`.**  
  Currently an empty stub. Any style conventions discovered during Layer 02 work should be recorded here.

- **Remove `wiki/active/` reference from `roadmap/README.md`.**  
  The roadmap mentions `specs/active/<layer>/` but also `wiki/active/<slug>/` (for task state). There is no `active/` in the wiki currently — task state lives in `wiki/active/<slug>/` only after mill-spawn (Layer 03) creates it. The reference is forward-looking but may confuse auditors; a clarifying note would help.
