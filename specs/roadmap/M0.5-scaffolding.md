# M0.5 — Scaffolding pass (before M1.1)

```yaml
depends-on: M0
delivers: marketplace + mill plugin skeleton + lifted support skills
status: complete
```

Before lifting code in M1.1, set up plugin structure and carry over the language-agnostic support skills so later layers have them available.

## M0.5.1 — Marketplace + mill plugin skeleton

- [x] `.claude-plugin/marketplace.json` at hub root listing `mill`, `codeguide`, `python`, `csharp`, `weblens`
- [x] `plugins/mill/.claude-plugin/plugin.json`
- [x] `plugins/mill/{skills,scripts,templates}/` directories created (empty)

## M0.5.2 — Lift support skills as-is from legacy

Carry from `C:\Code\millhouse-legacy\plugins\mill\skills\` into `plugins/mill/skills/`, **unmodified**:

- [x] `conversation`, `workflow`
- [x] `code-quality`, `cli`, `testing`, `linting`, `markdown`
- [x] `git-clone`, `git-commit`, `git-issue`, `git-log`, `git-pr`, `git-workflow`
- [x] `millhouse-issue`

`mill-skills-index` is **NOT** carried — it will be rebuilt fresh when needed.

## Exit criteria

- [x] `.claude-plugin/marketplace.json` exists and lists all five plugins
- [x] `plugins/mill/skills/` contains the 14 carried skills above
- [x] Hub root contains no stray `conversation/` or `workflow/` directories
- [x] All four already-carried plugins (`codeguide`, `csharp`, `python`, `weblens`) have `.claude-plugin/plugin.json`

⛔ **Gate:** don't start M1.1 until every exit criterion is ticked.
