# Review: 39 (A) — mill-start question-format UX — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/ + source
date: 2026-05-09
```

## Findings

### [NIT] `_subprocess_util` import placement in two helpers
**Location:** `plugins/mill/scripts/_review_common.py:7`, `plugins/mill/scripts/_implementer_common.py:3`
**Issue:** `import _subprocess_util` is interleaved with stdlib imports (`importlib`, `re`, `sys` in `_review_common.py`; `json`, `re` in `_implementer_common.py`) rather than grouped with the other local imports at the bottom.
**Fix:** Move `import _subprocess_util` to the local-imports block alongside `import _active`, `import _paths`, etc.

## Verdict

APPROVE
All three batches land correctly; contracts, tests, and exemptions align with the plan.