# Discussion Fix — round 1

```yaml
source_review: task/reviews/20260512-140312-discussion-review-r1.md
```

## Fixed

- **[GAP] mill-go fallback breaks after conversion** — Decisions section updated: body calls converted via `MILL_PYTHON` variable set in Step 0; explicit note added that source-tree venv must exist when fallback fires. Scope section updated to include mill-go Step 0 block change. Technical context updated with the new `MILL_PYTHON` Step 0 block.
- **[GAP] Testing step 1 gives false positive for mill-go** — Testing section updated: step 1 grep narrowed to `${CLAUDE_PLUGIN_ROOT}` forms only; new step 2 added greping `uv run --project.*PLUGIN_ROOT` in mill-go SKILL.md (must be zero).
- **[NOTE] mill-setup "unique inline-prefix form" prose becomes misleading** — Scope section updated to include mill-setup SKILL.md lines ~57–69 prose update; testing step 5 added to verify the prose is updated.

## Pushed Back

(none)
