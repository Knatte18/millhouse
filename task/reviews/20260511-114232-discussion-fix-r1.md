# Discussion fix — round 1

```yaml
slug: fix-wiki-cwd-cascade
round: 1
source_review: task/reviews/20260511-114150-discussion-review-r1.md
verdict: APPROVE with NOTEs → auto-resolved
```

## Fixed

- `20260511-114150-discussion-review-r1.md` → `[NOTE] wiki-path-guard Decision self-contradicts on equality timing`. Rewrote the `wiki-path-guard` Decision in `task/discussion.md` to make the split responsibility explicit: `resolve_wiki_path` does the name check only (the path-equality check would be circular inside the very function that resolves the wiki path); `resolve_git_root` owns the full belt-and-suspenders (name + path equality) since that's the call site where both `git_root` and the resolved wiki path are available without recursion. Updated Rationale and Rejected lines to match.
- `20260511-114150-discussion-review-r1.md` → `[NOTE] Gotchas mixes two test-allowlist approaches with a misleading label`. Rewrote the gotcha paragraph in `## Technical context` to drop the "(preferred)" qualifier and the OR clause. Final wording instructs a single approach: use the four regexes from the `test-design` Decision and an explicit allowlist for `CLAUDE.md` plus the eight documented SKILL.md files.

## Pushed Back

_(none)_
