# Plan Fix — Round 1

```yaml
source_review: 20260512-093514-plan-review-r1.md
```

## Fixed

- `[BLOCKING] Mill-go Entry Phase Gate done row still references /mill-merge` — Added Change E to Card 4 in batch 03: update the Entry Phase Gate `done` row to replace `/mill-merge` with `/mill-finalize`.
- `[BLOCKING] Null dereference in mill-merge Home.md fallback (task not found)` — Updated Card 3 Change A in batch 02: added explicit `if task is None: halt(...)` guard before `task.phase` access.
- `[NIT] Mill-go frontmatter description not updated; SKILLS.md regen will be stale` — Added Change A to Card 4 in batch 03: update mill-go frontmatter `description:` field to replace "Hand off to mill-merge." with "Hand off to mill-finalize."
- `[NIT] Card 4 "Step 19" label references a non-existent step number` — Corrected label in Card 4 Change D from "Step 19" to "Entry Step 3" (now Change D under the updated card structure; located by text match guidance).

## Pushed Back

*(none)*
