# Plan fix — round 5

```yaml
review_file: task/reviews/20260512-060218-plan-review-r5.md
date: 2026-05-12
```

## Fixed

- **[BLOCKING] Card 20 archive-tag push: hub has no origin remote** — Card 20 now creates only the local archive tag (`git -C <hub> tag archive/<slug> <child_branch>`) and drops the `git push origin archive/<slug>` line entirely. The test fixture's `_setup_trio` initialises `hub` without an `origin` remote, so a push would fail; the assertion checks local-tag existence only, so the push is not needed.
- **[NIT] Card 4 introduces duplicate coverage for phase=done** — Card 4 now explicitly states the final post-card test set (five tests, named without `_phase_` prefix for new phases) and instructs the implementer to keep only ONE test per phase, preferring `test_slug_from_branch_done` over `test_slug_from_branch_phase_done` when both would otherwise exist.
- **[NIT] Card 12 leaves `log_has_commits` as dead parameter in `_mock_branch_run`** — Card 12 now explicitly directs the implementer to remove the `log_has_commits: bool = False` parameter from `_mock_branch_run`'s signature when removing the `"log" in argv` branch.

## Pushed Back

(none)
