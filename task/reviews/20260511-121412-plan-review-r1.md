# Review: 31 (A) — Simple Gemini Flash reviewer — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-11
```

## Findings

### [BLOCKING] Integration smoke test excluded from batch 3 verify
**Location:** Batch 3, `verify:` field / Card 6
**Issue:** `smoke-llm-gemini.py` is a new integration test created in Card 6 but excluded from the batch 3 `verify:` command. The plan-mandated skip-on-missing guard (`shutil.which("gemini") is None → return 0`) makes it unconditionally CI-safe to add.
**Fix:** Extend the batch 3 `verify:` to chain both commands: `uv run --project plugins/mill python plugins/mill/integration_tests/smoke-llm-gemini.py && uv run --project plugins/mill python plugins/mill/unit_tests/test-reviewers.py`.

### [BLOCKING] `_reviewer_single.py` unaddressed — provider dispatch gap
**Location:** `## All Files Touched`, Batch 3
**Issue:** All three `_review_*.py` backends call `_reviewer_single.run(spec, ...)` where `spec["provider"] == "gemini"`. `_reviewer_single.py` is absent from the plan and from "All Files Touched". If it currently routes all specs to `_llm_claude`, gemini specs will mis-dispatch silently and the feature is non-functional end-to-end.
**Fix:** Either add a Shared Decision explicitly stating that `_reviewer_single.py` already reads `spec["provider"]` to select the LLM module with no edit needed — or add a card updating it to route `provider: gemini` → `_llm_gemini`.

## Verdict

REQUEST_CHANGES
Integration smoke excluded from verify; `_reviewer_single.py` provider dispatch unaddressed in plan.