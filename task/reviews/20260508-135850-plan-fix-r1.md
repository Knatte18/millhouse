# Plan-fix report — round 1

```yaml
review: 20260508-135643-plan-review-r1.md
verdict_in: REQUEST_CHANGES
blocking_in: 2
date: 2026-05-08T13:58:50Z
```

## Fixed

- `20260508-135643-plan-review-r1.md` "[BLOCKING] Card 4: test fixture branch mismatch causes push to fail" — Updated Card 4 in `task/plan/01-script-fixes.md` to add `git -C <repo> checkout -b hanf/task-one` to the test fixture between `_make_git_repo` and `write_initial_status`. This mirrors production flow where `_worktree.create` puts the worktree on the task branch before `write_initial_status` runs, making `git push --set-upstream origin hanf/task-one` succeed.
- `20260508-135643-plan-review-r1.md` "[BLOCKING] Cards 6 & 7: lazy import incompatible with specified patch target" — Updated Card 7 in `task/plan/02-gh-issues.md` to use a top-level `import _paths` (alongside the existing top-level `import _subprocess_util`) instead of a lazy function-body import. This makes `_gh_issues._paths` a real module attribute so Card 6's `unittest.mock.patch('_gh_issues._paths.resolve_git_root')` resolves correctly. Updated Card 6's parenthetical guidance to drop the inaccurate "NOT `_paths.resolve_git_root` directly" caveat and explain why the explicit-attribute target works under top-level import.

## Pushed Back

(none)
