# Plan-fix r3

```yaml
round: 3
verdict: REQUEST_CHANGES
review_file: task/reviews/20260509-162353-plan-review-r3.md
date: 2026-05-09
```

## Fixed

- **[BLOCKING] Two-worktree test fires real subprocess via unguarded `find_open_vscode_paths`** (review r3, holistic) — Card 4 of batch 2 amended to add the two-worktree picker test (the very first test in `test-millpy-vscode.py`, around line 54) to the patch-chain-update list. It needs only the `find_open_vscode_paths` patch (it already mocks `input`). Card 4's "do not modify" prohibition was narrowed to "do not change assertion logic, fixture setup, or expected paths" — patch-chain extensions are now permitted (and required) when the code under test gains a new external call. Added a paragraph that explains why the other pre-existing tests don't need the patch (they either short-circuit before the filter or have empty `discover_active_worktrees` so the filter loop never iterates).
- **[NIT] `_path_matches_cmdline` loop initialization left implicit in requirements** (review r3, holistic) — Card 1 of batch 1 expanded to spell out the full loop body: `idx = hay.find(needle)` BEFORE `while idx != -1:`, with the boundary check inside and the `idx = hay.find(needle, idx + 1)` increment at the bottom. References the discussion's "Path matching semantics" pseudo-code as canonical.

## Pushed Back

(None.)
