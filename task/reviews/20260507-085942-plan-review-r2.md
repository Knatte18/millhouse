# Review: 28 (A) — review-plan robustness — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-07
```

## Findings

### [BLOCKING] Card 11 Location 1 find-text does not match SKILL.md
**Location:** Batch 4 / Card 11
**Issue:** Location 1's search string specifies hardcoded machine paths (`c:/Code/millhouse/wts/millhouse/plugins/mill`) but the actual `SKILL.md` step 1.5 text reads `${CLAUDE_PLUGIN_ROOT}`. An Edit-tool call using the specified find-text will fail to locate the target, silently leaving the Location 1 change unapplied.
**Fix:** Change the Location 1 find-string to match the actual SKILL.md text: `` re-runs `uv run --project "${CLAUDE_PLUGIN_ROOT}" "${CLAUDE_PLUGIN_ROOT}/scripts/millpy-review-plan.py"` (still no round consumed — the validator gate is pre-LLM). ``

## Verdict

REQUEST_CHANGES
One blocking find-text mismatch in Card 11; all other batch/card logic is sound.