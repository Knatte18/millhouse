# Plan-fix report — round 2

```yaml
task: "35 (A) — Centralize path resolution across all three modes"
slug: mill-path-resolution-audit
round: 2
review_file: 20260509-105637-plan-review-r2.md
verdict_in: REQUEST_CHANGES
blocking_in: 1
nits_in: 0
```

## Fixed

- **[BLOCKING] resolve_active_hub M2+sub design gap: stub not at git_root/.millhouse/** (review r2). Card 2's `resolve_active_hub` implementation now resolves `hub_relative_path` two-tier: (1) `cfg.get("hub_relative_path", ".")` as the default, (2) worktree-root stub overrides when present. This patches the M2+sub gap (mill-claim writes the stub only at the hub, never at `git_root/.millhouse/`) while keeping cross-worktree correctness intact (mill-spawn-bootstrapped stubs still win). Card 1 tests for `resolve_active_hub` are restructured: M1+sub gains a stub-override assertion (cfg says ".", stub says "src/Models" → stub wins); M2 and M2+sub no longer scaffold a fictional stub at `<git_root>/.millhouse/`; M2+sub now relies on the caller's cfg alone, matching what `_review_common.resolve_path` (Batch 2 Card 4) actually produces. Shared Decision in `00-overview.md` rewritten to document the two-tier resolution and the consumer-profile rationale (cross-worktree vs same-cwd).

## Pushed Back

(none)
