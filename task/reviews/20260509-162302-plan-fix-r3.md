# Plan Fix Report — round 3

```yaml
slug: config-schema-refactor
round: 3
review_file: 20260509-162001-plan-review-r3.md
date: 2026-05-09
```

## Fixed

- 20260509-162001-plan-review-r3.md "[BLOCKING] Flow test fixtures will crash — backends call `_reviewers.load()` but fixtures create no `reviewers.yaml`". Verified accurate: card 1's `_reviewers.load` spec raises `ReviewerError` when the file is absent, and the `test_stub` carve-out lives in `_reviewers.resolve` (not in `load`), so every existing flow test would crash at the load call before any stub interaction. Three coordinated fixes:
  1. Card 3 extended: `_test_registry.py` now also exposes `write_to(wiki_root: Path, **overrides) -> Path` which serialises `make_minimal_registry(**overrides)` to YAML at `wiki_root / "reviewers.yaml"` and returns the written path.
  2. Card 8 extended: `_review_discussion.run` signature gains a `wiki_root` parameter (positional after `mill_dir`), mirroring `_review_plan.run` and `_review_code.run`. The backend now uses `_reviewers.load(wiki_root)` directly instead of `_reviewers.load(_paths.resolve_wiki_path(_paths.resolve_git_root()))`. Tests no longer need to fake a git toplevel.
  3. Card 12 extended: `millpy-review-discussion.py` passes `wiki_root` (already resolved at the CLI's line 41) into `_review_discussion.run`. The new call shape is `run(cfg, slug, mill_dir, wiki_root, project_root, max_rounds=args.max_rounds)`.
  4. Card 13 extended: every flow-test fixture (the shared `_make_fixture` helpers in `test-review-discussion-flow.py`, `test-review-plan-flow.py`, `test-review-code-flow.py`) plus every ad-hoc inline worktree setup that calls `code_run` calls `_test_registry.write_to(wiki_root)` so a valid `reviewers.yaml` is in place before the backend's `_reviewers.load(wiki_root)` runs. The instruction names the test groups identified by the reviewer (tests 3, 4, 7, 12 in `test-review-code-flow.py` plus the 14c/14d/14e cluster).

## Pushed Back

(none — every finding fixed)
