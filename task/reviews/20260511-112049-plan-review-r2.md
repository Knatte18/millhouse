# Review: 44 (A) — Bug-fix batch 4 — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-11
```

## Findings

### [NIT] Batch 3 test count says "four" but Card 6 lists five functions
**Location:** batch 3 (03-review-common-divergence-warning.md) — batch scope, Card 6 header, Batch Tests section  
**Issue:** The batch scope paragraph, Card 6's opening line, and the Batch Tests footer all say "four new test functions," but Card 6 defines five: the four BLOCKING-severity tests plus `test_parse_blocking_count_warns_for_gap_severity`.  
**Fix:** Change "four" to "five" in all three places.

### [NIT] D3 and Card 3 give inconsistent session_id handling on retry
**Location:** discussion.md §D3 vs batch 2 (02-llm-claude-fast-fail-retry.md) Card 3 requirement 3  
**Issue:** D3 says "retry once (fresh session, no `session_id` parameter on the second call)"; Card 3 says "use the same `argv` as the first call (same model, same flags, same session_id)." For the common `session_id=None` case the two are equivalent, but for the `session_id`-set / `resume=False` case they diverge.  
**Fix:** Card 3's "same `argv`" rationale is self-consistent ("we are not re-rolling the session") and is the correct intent; add one sentence to Card 3 acknowledging the D3 wording discrepancy and confirming same-argv wins.

## Verdict

APPROVE — both findings are cosmetic and do not block implementation.