and update mill-plan SKILL.md's escape hatch to edit the config directly instead of using `--max-rounds`.

---

### [BLOCKING] Card 15 misses mill-go holistic Guard expression

**Location:** `task/plan/03-skills-docs.md`, card 15 / `plugins/mill/skills/mill-go/SKILL.md` lines ~164–166

**Problem:** Card 15 lists `holistic_rounds` as the only config key to update in mill-go/SKILL.md. But the holistic Guard expression on those same lines reads `cfg.get("review", {}).get("code", {}).get("holistic", True)` — it references the old `review:` key, not `roles:`. After the atomic flip in card 5, that expression silently defaults to `True` (because `review:` no longer exists), always enabling holistic mode regardless of the `roles.code-review.holistic` setting.

**Fix:** Add a Requirements item to card 15 updating the Guard expression from `cfg.get("review", {}).get("code", {}).get("holistic", True)` to `cfg.get("roles", {}).get("code-review", {}).get("holistic", True)` (or the equivalent resolved-config path, if `load_config` already flattens this).

---

### [BLOCKING] Card 15 omits null-reviewer skip in mill-start

**Location:** `task/plan/03-skills-docs.md`, card 15 / `plugins/mill/skills/mill-start/SKILL.md`

**Problem:** The discussion (skip semantics section) defines two skip conditions: `rounds: 0` OR `reviewer: null`. Card 15 says to update `review.discussion.rounds` references in mill-start/SKILL.md, but the current skip guard reads only `if max_review_rounds == 0: skip to Handoff`. The `reviewer: null` skip path is a new condition introduced by this task — it needs a SKILL.md instruction and the guard must be extended. Card 15 does not cover this.

**Fix:** Add a Requirements item to card 15 for mill-start/SKILL.md: after resolving `roles.discussion.reviewer`, if the value is `null`, treat as skip (same Handoff branch as `rounds: 0`). Update the guard text accordingly.

---

### [BLOCKING] Card 13 test 14a rewrite is unimplementable as specified

**Location:** `task/plan/02-flip.md`, card 13, Requirements item for `test-review-code-flow.py` test 14a

**Problem:** Test 14a currently asserts `effort="medium"` propagation by inspecting the stub's captured kwargs. After migration, `_reviewer_single.run` calls the stub as `stub_mod.run(prompt_text, session_id=session_id, resume=resume, timeout=timeout)` — it does NOT forward `effort` or `model`. Card 13 says "migrate test fixtures to new schema" but gives no instruction for what test 14a should assert now that `effort` is no longer passed through. Migrated as-is, the test fails because `kwargs` contains no `effort`. Silently dropping the assertion means there is no coverage of the effort-forwarding path that `_reviewer_single` is supposed to own.

**Fix:** Card 13 must explicitly specify the test 14a rewrite. Two viable options: (a) rewrite it as a `_reviewer_single` unit test that mocks `_llm_claude.run_bulk`/`run_tool_use` and asserts `effort=` is forwarded to the LLM call — real coverage of the right boundary; or (b) delete the test and add a note in card 1's test plan to cover effort forwarding from within `test-reviewer-single.py`. Either choice must be stated in the plan.

---

### [BLOCKING] No card assigns the migration-warning test

**Location:** `task/plan/02-flip.md`, card 7

**Problem:** Card 7 specifies adding a stderr warning in `load_config` when a stale `review:` key is found in a local overlay (`config.local.yaml`). The discussion explicitly calls for a test covering this warning path. No card in the plan assigns that test — not card 7 (backend change), not card 13 (fixture migration), not card 1 (new test files). The warning can silently break or be omitted without CI catching it.

**Fix:** Add a test requirement to card 7 (or card 13): a test in `test-review-common.py` (creating the file if it does not exist, which must be listed in the file-changes section) that constructs a `config.local.yaml` with a `review:` key, calls `load_config`, and asserts the warning was emitted to stderr.

---

### [NIT] Card 5 lacks invocation-mechanism guidance for unknown types

**Location:** `task/plan/02-flip.md`, card 5

**Problem:** Card 5 specifies the `wiki/reviewers.yaml` schema with `type: single | cluster` but gives no guidance on what `_reviewers.resolve()` does when `type` is absent or unrecognised. The discussion mentions `single` as the only live type, but the schema is extensible. An operator who adds a future type gets a silent `AttributeError` or wrong-path fallthrough instead of a clean error.

**Fix:** Add a Requirements line: `_reviewers.resolve()` raises `ValueError(f"Unknown reviewer type: {spec['type']!r}")` for any type not in `{"single", "cluster"}`.

---

### [NIT] Tests 5/6 effort=None assertion becomes vacuous after migration

**Location:** `task/plan/01-foundations.md`, card 1 (planned `test-reviewers.py` tests 5/6)

**Problem:** The planned tests verify that the stub path sets `effort=None`. After migration, `_reviewer_single.run` does not pass `effort` to the stub at all. A test asserting `stub.kwargs["effort"] is None` will either raise `KeyError` (key absent) or pass vacuously if the fixture initialises the dict with a default — neither gives useful signal.

**Fix:** Replace the `effort=None` assertion with an assertion that `"effort"` is NOT a key in the captured kwargs dict (confirming the interface was correctly narrowed), and add a separate assertion that the mocked LLM provider receives `effort=` with the correct value from the spec.

---

## Verdict

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-09
round: 1
```

Five blocking issues prevent safe implementation as written: max_rounds clamping semantics contradict existing tests and the mill-plan SKILL.md escape hatch pattern; card 15 leaves the mill-go holistic Guard and the mill-start null-reviewer skip both unpatched; test 14a has no implementable rewrite path in the plan; and the migration-warning test is unassigned to any card. Resolve all five before proceeding to batch 1.