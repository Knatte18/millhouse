# Review: 38 (A) — Drop active.slug.md marker — holistic

```yaml
verdict: APPROVE
reviewer_model: opusmid
reviewed_file: plan/ + source
date: 2026-05-11
```

## Findings

### [NIT] Stale docstring reference to `.millhouse/active.slug.md`
**Location:** `plugins/mill/scripts/millpy-color.py:1-11` (module docstring)
**Issue:** The module docstring still says "the slug found in `.millhouse/active.slug.md`". The code itself now uses `_marker.slug_from_branch`. Card 26's "documentation review" pass was scoped to a fixed file list and missed this script.
**Fix:** Replace the trailing clause with "the slug derived from the current branch via `_marker.slug_from_branch`".

## Verdict

APPROVE — the migration is atomic, every `_active` consumer is moved to `_marker`, the new signatures (`is_inplace`, `discover_active_worktrees`, `find_active_slug`, `load_task_title`) are propagated end-to-end, the unit + integration test fixtures all use real branch + Home.md state, and SKILL.md prose is in sync. Only a stray docstring remains.