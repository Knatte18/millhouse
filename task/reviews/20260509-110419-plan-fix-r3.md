# Plan-fix report — round 3

```yaml
task: "35 (A) — Centralize path resolution across all three modes"
slug: mill-path-resolution-audit
round: 3
review_file: 20260509-110243-plan-review-r3.md
verdict_in: REQUEST_CHANGES
blocking_in: 1
nits_in: 0
```

## Fixed

- **[BLOCKING] Existing M1 tests in test-review-common.py will fail after Card 4** (review r3). Card 3 now includes a pre-step that extends `_make_worktree_fixture` (at `test-review-common.py:22-62`) to scaffold `<container>/wiki/config.yaml` with minimal content (`paths: {discussion_file: task/discussion.md}`). Container-form sibling resolution lands the wiki at `<container>/wiki/`, matching what `_paths.resolve_wiki_path` returns. After Card 4's `load_config` prerequisite lands, the four existing M1 `resolve_path` tests stay green: `load_config` finds the shared config, the local config is optional, and `cfg["hub_relative_path"]` defaults to `"."` via `.get(...)`. The new M2 test gets a separate fixture (M2 has no `<container>/wts/<slug>/` directory, so `_make_worktree_fixture` does not apply). The Batch Tests section is updated to describe the fixture extension explicitly.

## Pushed Back

(none)
