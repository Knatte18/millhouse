# Plan fix — round 3

```yaml
review_file: task/reviews/20260511-184210-plan-review-r3.md
date: 2026-05-11
```

## Fixed

- **[NIT] Card 20 leaves wiki-active teardown in integration test** — Card 20 now explicitly directs the implementer to remove the `shutil.rmtree(wiki / "active" / slug)` + `_wiki.write_commit_push(wiki, [f"active/{slug}/"], ...)` simulation block from `test-merge.py` and add the inverse assertion `(wiki / "active" / slug).exists()` for legacy-layout fixtures.
- **[NIT] Card 20 says "keep" archive tag assertion that does not exist** — Card 20 now says "add (not keep)" with explicit `subprocess.run(["git", "-C", str(hub), "tag", "-l", f"archive/{slug}"], capture_output=True, text=True).stdout.strip() != ""` assertion, since mill-merge Step 6 still creates the tag.
- **[NIT] Card 18 missing `gh pr list` failure-path test** — Card 18 now has a fourth test block covering `returncode=1` from `gh pr list`: no `git tag`, no worktree-remove, Home.md unchanged, stderr line emitted with slug.

## Pushed Back

(none)
