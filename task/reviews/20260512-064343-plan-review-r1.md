# Review: 50 (A) — Bug-fix batch 5 (post-44 triage) — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-12
```

## Findings

### [BLOCKING] Card 3 omits updates to two existing test assertions broken by Card 2

**Location:** Batch 1 / Card 2 + Card 3

**Issue:** Card 2 changes `append_phase` to emit `phase  'timestamp'` (quoted). Two existing assertions in `test-status.py` check the unquoted form and will fail after Card 2 lands: (1) `assert "discussed  2026-04-22T15:00:00Z" in contents` (the `in`-check fails because the quoted form is `discussed  '2026-04-22T15:00:00Z'` — the single quote comes before `2026`, so the search string is not a substring); (2) `assert r["last_timeline_entry"] == f"discussed  {ts2}"` (equality check against the unquoted string). Card 3's Requirements add four new tests but say nothing about updating these two existing assertions. The batch `verify:` runs the full suite, so both failures would be caught at gate time, leaving the batch broken.

**Fix:** Card 3's Requirements must also update those two assertions to the quoted form: `"discussed  '2026-04-22T15:00:00Z'"` for the `in`-check and `f"discussed  '{ts2}'"` for the equality check.

---

### [BLOCKING] Card 13 test Case 3 uses an untracked file invisible to `compute_new_dirt`

**Location:** Batch 4 / Card 13

**Issue:** `compute_new_dirt` runs `git status --porcelain --untracked-files=no` — untracked files are excluded by design. Case 3 simulates a "dirty worktree" by writing an untracked `dirty.txt` (`path.write_text("x")`), but this file never appears in `git status --untracked-files=no`. `compute_new_dirt` returns `[]`, the three guards all pass, inference succeeds, and the test asserts `stuck_type: logic` — but the actual output is `status: success`. The test would fail in the opposite direction from its intent.

**Fix:** Case 3 must use a modified tracked file. Concretely: after `capture_snapshot` and the new commit, modify a file that was already committed (e.g. rewrite `README.md`) but do NOT commit it. That produces ` M README.md` in `--untracked-files=no` output, making `compute_new_dirt` return a non-empty list and correctly skipping inference.

---

## Verdict

REQUEST_CHANGES — two blocking test correctness errors; both are straightforward to fix in their respective cards.