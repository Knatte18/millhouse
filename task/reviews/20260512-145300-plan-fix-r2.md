# Plan Fix — round 2

```yaml
source_review: task/reviews/20260512-145111-plan-review-r2.md
```

## Fixed

- **[NIT] Multi-line uv-run patterns in mill-go Holistic section not explicitly addressed by Card 3** — `02-mill-go-conversion.md` Card 3 Requirements: added a "Multi-line continuation forms" paragraph with a worked example showing how `uv run --project "${PLUGIN_ROOT}" \` + continuation-line script path collapses to a single converted line, preserving downstream arg-continuation backslashes.
- **[NIT] Batch 3 verification check 2 will false-alarm after Card 5** — `03-mill-setup-and-claudemd.md` Batch Tests item 2: changed expectation to "equal the pre-edit count plus one" (Card 5's rewritten prose contains one new mention of `uv run --project plugins/mill` inside the new exception sentence); added a sub-clause clarifying the per-card expectation if Card 4 is verified before Card 5.
- **[NIT] No global post-edit verification across all SKILL.md files** — `00-overview.md`: added a new "Global post-edit verification" section listing three plan-wide greps (overall `${CLAUDE_PLUGIN_ROOT}` form, mill-go `$PLUGIN_ROOT` form, and the source-tree-form per-file count expectation). Catches inadvertent omissions of SKILL.md files.

## Pushed Back

(none)
