# Discussion Fix Report — round 1

Source review: `task/reviews/20260511-175606-discussion-review-r1.md`

## Fixed

- **[NOTE] Technical Context misstates Entry step for config loading** — replaced "already called in Entry (step 3 of mill-merge-in)" with accurate description: mill-merge-in Entry has no explicit config-load step; plan must add `cfg = _config.load_config(...)` at the top of Step 4 before the verify loop.
- **[NOTE] Report section (Step 6) not addressed for skipped verifies** — added Step 6 Report update to in-scope: when K ≥ 1 verifies are skipped, report reads `<M> batch tests ran, <K> skipped (allowlisted as known-broken)`; also added Step 6 to the Files-to-change list.

## Pushed Back

(none)
