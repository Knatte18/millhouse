# Plan fix — round 2

## Fixed

- `20260512-065014-plan-review-r2.md` [NIT] "`_forward_output` inference path missing try/except around `compute_new_dirt`" — wrapped the inference path in `try: ... except Exception: pass` so any unhandled exception (e.g. `CalledProcessError` from `compute_new_dirt`'s `check=True` git call) falls through to the stuck-logic sentinel. The "always emit JSON" invariant is preserved unconditionally.
- `20260512-065014-plan-review-r2.md` [NIT] "Redundant `read_batches` call in fix-cycle resume path" — simplified Card 12's fix-cycle requirements to reuse the already-computed `batch_state` local from line 198 instead of calling `_status.read_batches` a second time.
- `20260512-065014-plan-review-r2.md` [NIT] "Card 9 instructions contradict their own code template on arrow representation" — clarified that the SELF guard is the primary protection; the implementer may use the literal `→` (as shown in the code template) or the escape sequence — both are equally correct.

## Pushed Back

(none)
