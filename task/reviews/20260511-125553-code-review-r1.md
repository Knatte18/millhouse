# Review: 31 (A) — Simple Gemini Flash reviewer — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmedium
reviewed_file: plan/ + source
date: 2026-05-11
```

## Builder override (mill-go round 1, 2026-05-11)

The reviewer's [BLOCKING] finding about registry entry names is **OVERRIDDEN**: the operator gave an explicit mid-execution directive to use shortname `g25flash` (and `g25flash_tool` for the tool-use variant). The plan card 5 spec is superseded by that user directive. Do NOT rename back to `gemini_flash`. Apply only findings 2 and 3 below.

## Findings

### [BLOCKING — OVERRIDDEN, DO NOT APPLY] Registry entry names deviate from plan spec
**Location:** `wiki/reviewers.yaml` (last two entries)
**Issue:** Plan card 5 explicitly states the entries must be named `gemini_flash` and `gemini_flash_tool`. The implementation used `g25flash` and `g25flash_tool`. Any operator config or documentation referencing `reviewer: gemini_flash` will fail with "reviewer not found".
**Fix:** Rename `g25flash` → `gemini_flash` and `g25flash_tool` → `gemini_flash_tool` in `wiki/reviewers.yaml` and push via `@mill-wiki-push`.
**Builder note:** overridden — see "Builder override" above.

### [NIT] `_build_argv` emits `"-p", ""` — test validates wrong plan spec
**Location:** `_llm_gemini.py:56-57`, `test-llm-gemini.py:99`
**Issue:** Plan specifies `[..., "-p", "-o", "stream-json", ...]`; implementation has `"-p", ""` (empty string injected after `-p`). The unit test was written to match the implementation, so the circular validation masks the deviation. Integration smoke worked in practice, but the test does not catch a regression back to the plan-correct argv.
**Fix:** Either remove the `""` and update the test assertion, or add an explanatory comment in the test asserting the empty-string behaviour is intentional and tested.

### [NIT] `SCRATCH` is dead code in smoke test
**Location:** `smoke-llm-gemini.py:24` (`SCRATCH = HUB / ".scratch"`)
**Issue:** `SCRATCH` is defined at module level but never referenced; the tool-use test correctly uses `INTEGRATION_TESTS` for the temp dir (gemini ignores gitignored paths). The dead variable adds confusion.
**Fix:** Remove `SCRATCH = HUB / ".scratch"` from `smoke-llm-gemini.py`.

## Verdict

REQUEST_CHANGES
Registry entry names must match the plan spec; two NITs are clean-up only.