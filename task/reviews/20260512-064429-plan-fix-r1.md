# Plan fix — round 1

## Fixed

- `20260512-064343-plan-review-r1.md` [BLOCKING] "Card 3 omits updates to two existing test assertions broken by Card 2" — updated Card 3 Requirements to call out three existing test-status.py assertions that compare against unquoted-timestamp form (lines 129, 184, 229) and must be migrated to the quoted form alongside Card 2's `append_phase` change. Added a "Section A / Section B" split to the card so the implementer covers both existing-assertion migration and the four new test blocks.
- `20260512-064343-plan-review-r1.md` [BLOCKING] "Card 13 test Case 3 uses an untracked file invisible to `compute_new_dirt`" — rewrote Case 3 in Card 13 to modify an already-tracked file (`README.md`) instead of creating an untracked `dirty.txt`. Added a fixture instruction to commit `README.md` as part of the base commit, plus an explanatory note about why `--untracked-files=no` makes the original wording wrong.

## Pushed Back

(none)
