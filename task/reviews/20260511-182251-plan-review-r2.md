# Review: 47 (A) — mill-merge-in: allowlist for known-broken pre-existing test failures — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-11
```

## Findings

### [BLOCKING] Card 3 false precondition: `merge:` block absent from production wiki config
**Location:** Batch 1, Card 3
**Issue:** Card 3 requires: "Locate the existing `merge:` block at the bottom (this hub already mirrors the template's `merge:` block — confirm before editing)" and "Append the byte-for-byte identical commented `verify:` section AFTER the production file's `merge:` block." But the provided `wiki/config.yaml` ends at the `groom:` section — there is no `merge:` block. The instruction references a non-existent anchor and gives no fallback, leaving the implementer unable to satisfy the stated precondition.
**Fix:** Remove the false assumption. Instruct the implementer to append the new `verify:` section after the last existing block (`groom:`), and remove "this hub already mirrors the template's `merge:` block — confirm before editing."

## Verdict

REQUEST_CHANGES
Card 3 has an unexecutable precondition; fix the append anchor.