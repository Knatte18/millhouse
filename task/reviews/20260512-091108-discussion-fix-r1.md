# Discussion Fix — Round 1

```yaml
source_review: 20260512-091010-discussion-review-r1.md
```

## Fixed

- `[GAP] test-mill-merge-inplace.py has no PR-path tests to remove` — Testing section updated: removed false "Remove PR-path tests" instruction; confirmed existing file has no PR-path tests; noted phase-gate fallback is SKILL.md agent logic with no Python helper, so no unit test entry needed for it.
- `[NOTE] git.parent-branch is also kebab-case but not in rename scope` — config-key-rename decision and Technical context updated: clarified `git.parent-branch` is intentionally excluded (different purpose, per-user local config placement, deferred scope); corrected rationale from "only the git: block used kebab-case" to "most of the git: block used kebab-case".

## Pushed Back

*(none)*
