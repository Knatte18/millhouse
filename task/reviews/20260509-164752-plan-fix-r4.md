# Plan Fix Report — round 4

```yaml
slug: config-schema-refactor
round: 4
review_file: 20260509-164522-plan-review-r4.md
date: 2026-05-09
```

## Fixed

- 20260509-164522-plan-review-r4.md "[BLOCKING] `_test_registry.write_to` spec omits directory creation; fixtures never mkdir `wiki_root`". Card 3 spec for `write_to` extended: now starts with `wiki_root.mkdir(parents=True, exist_ok=True)` before serialising and writing the registry. Fixtures that do `wiki_root = tmp_path / "wiki"` no longer need to mkdir explicitly.
- 20260509-164522-plan-review-r4.md "[NIT] Card 13 omits updating `discussion_run` call sites after Card 8 inserts `wiki_root` as positional arg". Card 13 extended with an explicit "Discussion-run call-site updates" bullet: every existing `discussion_run(cfg, SLUG, mill_dir, project_root)` call inside `test-review-discussion-flow.py` is rewritten to `discussion_run(cfg, SLUG, mill_dir, wiki_root, project_root)`. Without this every discussion flow test would raise `TypeError`.
- 20260509-164522-plan-review-r4.md "[NIT] Card 13 Edits list includes `test-llm-claude.py` and `test-review-cli.py` with no identifiable schema changes". Verified accurate: grep on both files shows zero `cfg["review"]` references. Two coordinated fixes:
  1. `test-llm-claude.py` removed from card 13's Edits list and from the overview's All Files Touched.
  2. `test-review-cli.py` retained in card 13's Edits with an explicit purpose: add the cross-validation test from discussion.md's Testing section ("registry missing a name referenced by `cfg.roles.*.<scope>.reviewer` causes the API script to exit non-zero with a clear stderr message"). Targets `millpy-review-discussion.py` as the simplest CLI victim.
- 20260509-164522-plan-review-r4.md "[NIT] `_reviewer_test_stub.MODE = 'bulk'` becomes unreferenced dead code after Card 14". Verified accurate: `test-reviewer-modules.py` is the only asserter on `_reviewer_test_stub.MODE`, and card 14 deletes that test file. New dispatch path in `_reviewer_single.run` reads `spec["tooluse"]`, not module-level `MODE`. Card 14 now adds `_reviewer_test_stub.py` to its Edits list with a one-line instruction: delete the `MODE = "bulk"` line. Overview All Files Touched updated to include `_reviewer_test_stub.py`.
- 20260509-164522-plan-review-r4.md "[NIT] Card 1 `resolve_role` spec unguarded against missing `cfg['roles']` or `cfg['roles'][role]` parent keys". Card 1 spec for `resolve_role` rewritten to use a defensive `.get()`-chain guard: `if role not in cfg.get("roles", {}) or scope not in cfg["roles"][role]: raise ReviewerError(...)`. Stale or partial overlays no longer leak `KeyError` past the API scripts' `except ReviewerError` handler.

## Pushed Back

(none — every finding fixed)
