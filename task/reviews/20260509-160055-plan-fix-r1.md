# Plan-fix r1

```yaml
round: 1
verdict: REQUEST_CHANGES
review_file: task/reviews/20260509-155826-plan-review-r1.md
date: 2026-05-09
```

## Fixed

- **[BLOCKING] `path_match_helper_*` tests fail cross-platform** (review r1, holistic) — Card 2 of batch 1 rewritten to build path-match test inputs from `tempfile.TemporaryDirectory()` and `str(real_path.resolve())`, so the haystack and the launch_path's resolved string agree on the actual platform. Drops the `os.name="posix"` patches that didn't change `Path.resolve()`'s behavior. The Windows-case-insensitive case is now skipped on POSIX rather than fabricated.
- **[BLOCKING] `--slug` + empty active list breaks existing test** (review r1, holistic) — Card 3 of batch 2 amended to add the explicit guard `if not active and args.slug is not None: print("No active worktrees found.", file=sys.stderr); return 0` BEFORE the `--new` branch (and a sibling `--list` empty guard before that). Restores today's behavior; keeps the existing test unchanged.
- **[BLOCKING] Mock target for `find_open_vscode_paths` semantically ineffective** (review r1, holistic) — Card 3 of batch 2 changed to `import _vscode_processes` only (no `from _vscode_processes import …`). Every call site now references via the module attribute (`_vscode_processes.find_open_vscode_paths()`, `_vscode_processes._path_matches_cmdline(...)`), so the existing `mill_vscode._vscode_processes.find_open_vscode_paths` patch target rebinds the call. Card 4's test description updated to note this is now an effective patch target.

## Pushed Back

(None.)
