# Plan fix — round 1

```yaml
slug: discussion-review-gap-batching
review: 20260511-105345-plan-review-r1.md
date: 2026-05-11
```

## Fixed

- `[NIT] Step 4a instructs path construction; JSON file field already provides the path` — card 1 step 4a previously read "read the review file at `task/reviews/<YYYYMMDD-HHMMSS>-discussion-r<N>.md` (filename pattern from `_review_common.write_review_file`)". Verified against the rendered SKILL step 2 prose, which delivers an absolute path via `reviews[0].file` in the JSON envelope. Rewrote 4a to read from `reviews[0].file` instead of naming a template pattern. The existing step 2's `<worktree_root>/reviews/` prose is unchanged — out of this plan's scope per `task/discussion.md` "Scope → Out" ("No changes to `mill-plan/SKILL.md` or `mill-receiving-review/SKILL.md`" and the implicit "no scope expansion to unrelated bugs in step 2").

## Pushed Back

(none)
