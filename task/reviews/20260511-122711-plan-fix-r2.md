# Plan fix — round 2

```yaml
slug: fix-wiki-cwd-cascade
round: 2
source_review: task/reviews/20260511-122606-plan-review-r2.md
verdict: APPROVE with 2 NITs → both resolved
```

## Fixed

- `20260511-122606-plan-review-r2.md` → `[NIT] "Equivalent" exception-handler claim is technically inaccurate`. Card 2 Requirements now mandate the explicit `except (Exception, SystemExit):` tuple form and explain why `except BaseException:` is rejected (catches `KeyboardInterrupt` and `GeneratorExit`, which should still propagate during the brief `resolve_wiki_path` call). The "equivalently" wording is removed.
- `20260511-122606-plan-review-r2.md` → `[NIT] Test case 4 phrasing misdirects the implementer`. Card 5 case 4 retitled to "Name check fires before nested-halt from `resolve_wiki_path` can propagate" — the new title matches the actual fire order in card 2 (name check first, path-equality second). The case now spells out explicit `_subprocess_util.run` mock values for both sub-cases (non-wiki path returns `/tmp/not-wiki`, wiki path returns `C:/Code/millhouse/wiki`) so the implementer doesn't infer.

## Pushed Back

_(none)_
