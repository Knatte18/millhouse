# Discussion fix — round 2

## Fixed

- `20260512-062509-discussion-review-r2.md` [NOTE] "D5 mis-attributes snapshot_path source" — corrected D5's Technical Context paragraph and Scope #243 fallback paragraph to state that `snapshot_path` is NOT stored in batch yaml but derived inline at `millpy-implement.py` line 130 as `project_root / "task" / f".cleanliness-snapshot-{batch_name}.txt"`. `start_sha` continues to come from `_status.read_batches(status_path)`.

## Pushed Back

(none)
