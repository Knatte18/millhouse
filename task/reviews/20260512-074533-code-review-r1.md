# Review: 50 (A) — Bug-fix batch 5 (post-44 triage) — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: opushigh
reviewed_file: plan/ + source
date: 2026-05-12
```

## Findings

### [BLOCKING] mill-plan SKILL.md skip-review block missing
**Location:** `plugins/mill/skills/mill-plan/SKILL.md:111` (`### Phase: Plan Review`)
**Issue:** Card 5.1 (#249) requires inserting a new paragraph at the top of `### Phase: Plan Review` describing the two skip conditions (`rounds: 0` OR `reviewer: null`). The heading is immediately followed by `Loop up to \`max_review_rounds\` rounds.` with no skip block in between.
**Fix:** Insert the skip-review paragraph between the heading and the "Loop up to..." line per Card 5.1 Requirements, mirroring mill-start SKILL.md's line-98 placement.

### [BLOCKING] mill-plan SKILL.md APPROVE+NIT substep 4b not added
**Location:** `plugins/mill/skills/mill-plan/SKILL.md:153-154`
**Issue:** Card 5.2 (#247) requires splitting step 4a into 4a (APPROVE with zero NITs) and 4b (APPROVE with NITs), and renumbering existing 4b→4c, 4c→4d. The current file has only one step 4a (which still references existing-style "4d message shape" prematurely) followed directly by step 4.5, then steps 4c/4d. The split into a separate 4b for NIT-fix is not present — the original 4a was modified instead of being split.
**Fix:** Replace the single step 4a with the two-step split per Card 5.2: 4a handles zero-NIT APPROVE only; new 4b handles APPROVE+NITs with fixer-report writing. The current single 4a conflates both cases and breaks the renumbering chain.

### [BLOCKING] mill-plan SKILL.md set_blocked call sites not migrated
**Location:** `plugins/mill/skills/mill-plan/SKILL.md:182` (step 5 non-progress) and step 6 max-rounds branch
**Issue:** Cards 5.3 and 5.4 (#238) require replacing chained `_status.append_phase(status_path, "blocked", ts)` + `_status.update_field(status_path, "blocked_reason", ...)` calls with single `_status.set_blocked(status_path, ..., timestamp=ts)` calls in step 5 (non-progress) and step 6 (max-rounds). Step 5's autonomous-mode branch shows `_status.set_blocked(status_path, f"non-progress round {N}", timestamp=ts)` is present. However, the file content from line 182 onward needs verification for step 6's max-rounds branch as well.
**Fix:** Verify both step 5 and step 6 autonomous-mode branches use `_status.set_blocked(...)` rather than the chained pair. Step 5 appears done; step 6 must be confirmed/updated per Card 5.4.

### [NIT] Plan overview claims slug-only step 5 set_blocked but Cards 5.3/5.4 are present
**Location:** `task/plan/02-skill-docs.md` Card 5 Commit message
**Issue:** Commit message `fix(mill-plan): add skip-review block, APPROVE+NIT substep, set_blocked calls` describes work that is only partially complete in the SKILL.md (skip-review block missing; 4a/4b split missing). The commit message implies all three sub-requirements landed.
**Fix:** Complete Cards 5.1 and 5.2 before considering Card 5 done; the commit message is accurate to the plan but the implementation is incomplete.

## Verdict

REQUEST_CHANGES

Card 5 (mill-plan SKILL.md) is materially incomplete: skip-review block and APPROVE+NIT 4a/4b split are both missing.