# Review: (A) — Add /mill-fold skill with active-task guard — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmedium
reviewed_file: plan/ + source
date: 2026-05-13
```

## Findings

### [NIT] Module docstring for `_tasks_md` not updated with `append_to_body`
**Location:** `plugins/mill/scripts/_tasks_md.py:1-27`
**Issue:** `append_to_body` is added as a public function but is not listed in the module-level `Public API:` block, unlike `_gh_issues.py` which was correctly updated.
**Fix:** Add `append_to_body(text, slug, line) -> str` to the `Public API:` section in the module docstring.

### [NIT] Minor inconsistency in `mill-ghissues-to-tasks` rules bullet
**Location:** `plugins/mill/skills/mill-ghissues-to-tasks/SKILL.md` (Rules section, bullet 4)
**Issue:** The pre-existing "Pointer comment is the invariant" bullet still reads `Consolidated into wiki task: <slug>` as universal, while the new close-comment-strings bullet (correctly) differentiates New-task vs Fold-in. These are technically contradictory.
**Fix:** Update the "Pointer comment" bullet to say "every closed issue gets a pointer comment (see Close-comment strings below)" — but this was intentionally out of scope per Card 7's "do not change any other section" constraint. No-action acceptable.

## Verdict

APPROVE — implementation is complete, all 17 tests and shared decisions are faithfully realised.