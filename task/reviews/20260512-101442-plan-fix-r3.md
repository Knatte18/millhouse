# Plan Fix — Round 3 (NIT-only, APPROVE path)

```yaml
source_review: 20260512-101347-plan-review-r3.md
```

## Fixed

- `[NIT] Config load module name inconsistent with mill-go` — Added `signature: _config.load_config(wiki_path: Path, worktree_root: Path) -> dict` note in batch 01 Card 1 Entry Step 3. Did not change to `_review_common.load_config` — `_config.load_config` is the correct skill-level config loader (matches mill-merge, mill-merge-in SKILL.md usage); `_review_common` is a review-subsystem internal. Reviewer's alternative suggestion (add signature note) applied.
- `[NIT] Change D mislabels config-key bullet location` — Updated Change D prose in batch 03 Card 4 from "at the top of the Handoff section" to "in mill-go's Entry section, Step 3 or the config-key bullet list near it."

## Pushed Back

*(none)*
