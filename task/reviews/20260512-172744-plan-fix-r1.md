# Plan Fix — Round 1

```yaml
review: 20260512-172653-plan-review-r1.md
verdict: APPROVE
blocking_count: 0
nit_count: 3
fixed: 3
pushed_back: 0
```

## Fixed

- `20260512-172653-plan-review-r1.md` — [NIT] "`import re` unused given instruction to reuse `_tasks_md._SLUG_RE`" — dropped `re` from `02-cli-script.md` card 4 imports list; made the `_SLUG_RE` reuse explicit ("does not define its own copy, and `import re` is therefore NOT needed").
- `20260512-172653-plan-review-r1.md` — [NIT] "`close_with_comment` failure: 'surfaced but exits 0' — try/except not shown" — added explicit `try: close_with_comment_fn(...) except _gh_issues.GhError as exc: print(...)` skeleton to `02-cli-script.md` card 4 step 6, with "fall through — do not re-raise" plus a one-sentence rationale.
- `20260512-172653-plan-review-r1.md` — [NIT] "`_patch_resolve_paths` closure arity not specified" — added the two distinct closure signatures (`lambda: wiki_path` for `resolve_git_root`, `lambda _git_top: wiki_path` for `resolve_wiki_path`) to `02-cli-script.md` card 5 helpers section; cited the production signatures and the `TypeError` the test would otherwise raise.

## Pushed Back

None — all three findings VERIFY as accurate, none cause demonstrable harm under the mill-receiving-review decision tree.
