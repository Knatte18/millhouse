# Review: Replace uv-run-project with direct venv Python in SKILL.md invocations — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmedium
reviewed_file: plan/ + source
date: 2026-05-12
```

## Findings

### [NIT] Stale prose in mill-go Step 0 references `uv run` commands
**Location:** `plugins/mill/skills/mill-go/SKILL.md` — prose line immediately after the Step 0 bash block
**Issue:** The line reads "Use `$PLUGIN_ROOT` in place of `$CLAUDE_PLUGIN_ROOT` for all subsequent `uv run` commands in this skill." — after the conversion there are no `uv run` commands left in this skill, making the instruction misleading.
**Fix:** Change "uv run commands" to "Python invocations" (or drop the sentence; `MILL_PYTHON` makes it self-evident).

## Verdict

APPROVE
All three batches are complete and correct; one stale prose NIT harmless at runtime.