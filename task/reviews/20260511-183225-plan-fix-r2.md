# Plan fix — round 2

```yaml
review_file: task/reviews/20260511-183021-plan-review-r2.md
date: 2026-05-11
```

## Fixed

- **[BLOCKING] `to_reap_pr` field without default breaks all existing test constructions** — Card 11 now declares `to_reap_pr: list[SlugRecord] = field(default_factory=list)` and imports `field` from `dataclasses`. Existing 7+ `CleanupPlan(...)` constructions in `test-cleanup.py` continue to work via the default.
- **[BLOCKING] Old git-log guard tests become incompatible after Card 12** — Card 12's `Edits:` now includes `test-cleanup.py`. The requirement explicitly directs the implementer to delete the four `guard-slug-1` through `guard-slug-4` blocks (Card 17's new tests supersede their coverage), and to update `_mock_branch_run` so `tag -l` is mocked instead of `log --oneline`.
- **[NIT] mill-merge Step 1 still says "or not [active]"** — Card 8 now updates Entry Step 1's MarkerError parenthetical and the halt message ("This worktree has no active task branch" → "This worktree has no registered task branch") to reflect batch 1 card 3's `slug_from_branch` relaxation.
- **[NIT] mill-merge frontmatter description not updated for teardown removal** — Card 8 now updates the SKILL.md `description:` field on line 3, removing the "worktree+branch+portal removal, optional legacy wiki cleanup" phrase and pointing to `/mill-cleanup`.
- **[NIT] Card 19 assumes existing state table in mill-status SKILL.md** — Card 19 now instructs the implementer to CREATE a new `## Phase reference` section after `## Run it`, with the full table spelled out, instead of asking them to find a structure that doesn't exist.
- **[NIT] Card 4 removes abandoned/None phase failure tests without adding success equivalents** — Card 4 now adds `test_slug_from_branch_phase_abandoned` and `test_slug_from_branch_phase_none` success variants (asserting the helper succeeds for `phase="abandoned"` and `phase=None`) alongside the three new-phase tests.

## Pushed Back

(none)
