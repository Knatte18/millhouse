# Review: 35 (A) — Centralize path resolution across all three modes — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-09
```

## Findings

### [BLOCKING] Existing M1 tests in test-review-common.py will fail after Card 4
**Location:** Batch 2, Cards 3 and 4
**Issue:** Card 4's new `resolve_path` body calls `load_config(wiki_root, hub_dir / ".millhouse")` before resolving any path. The existing four M1 test cases (`discussion.md → worktree root`, `plan/ and reviews/ templates`, `stale <SLUG>`, `slug-mismatch`) use `_make_worktree_fixture`, which creates only a git repo and active marker — no `wiki/config.yaml`. After Card 4 lands, `load_config` raises `ReviewError: Missing config at <tmp>/container/wiki/config.yaml` and all four tests fail. The slug-mismatch test expects `ActiveWorktreeSlugMismatch` but receives `ReviewError` instead. The Batch 2 Tests section's claim that "existing tests for the worktree-mode behavior (M1) remain green" is incorrect.
**Fix:** Card 3 must also update `_make_worktree_fixture` to write a minimal `wiki/config.yaml` (e.g. `paths: {}`) at `<tmp>/container/wiki/config.yaml`, or add wiki-root setup and `_paths.resolve_wiki_path` patching to each existing `resolve_path` test case.

## Verdict

REQUEST_CHANGES
One existing test suite breaks on the new `load_config` prerequisite; fix is localized to Card 3.