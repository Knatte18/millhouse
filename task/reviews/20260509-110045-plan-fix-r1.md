# Plan Fix — Round 1

```yaml
task: 39 (A) — mill-start question-format UX
slug: mill-start-question-ux
round: 1
review_file: 20260509-105656-plan-review-r1.md
```

## Fixed

- **[BLOCKING] `test-subprocess-util.py` has no `tests = [...]` list** (Batch 3, Card 8) — Card 8 instructions rewritten. The new tests are added as inline `(f)`–`(l)` `try/except` blocks following the existing `(c)`/`(a)`/`(b)`/`(d)`/`(e)` pattern; the `tests = [...]` reference is removed. Import line update (`popen_detached` added) is included to also resolve [NIT] "popen_detached import omitted from card 8 requirements". Added `import os` and `import unittest.mock` to the test file's imports per the new tests' needs.

- **[BLOCKING] Test (d) `creationflags` assertion breaks after patch-target switch** (Batch 3, Card 9) — Card 9 instructions for tests (d) and (e) now explicitly remove the `creationflags` and `start_new_session` assertions from `test-millpy-bg.py`. Replaced with assertions that the launcher invokes `popen_detached` exactly once and forwards `pid` correctly. The deeper flag-coverage now lives in `_test_subprocess_util` blocks `(k)` and `(l)`. Patch target inside `popen_detached` is `_launcher_mod._subprocess_util.subprocess.Popen` so the underlying Popen is captured.

- **[BLOCKING] `millpy-merge-in-subagent.py` line 133 uses `shell=True`** (Batch 3, Card 10) — Card 10 instructions now explicitly exempt line 133 from the refactor. The call (`subprocess.run(args.cmd, shell=True, ...)`) stays as bare `subprocess.run` with a one-line code comment marking why. Lines 142 and 154 (both list-form git calls) receive the standard refactor. Test instructions for `test-millpy-merge-in-subagent.py` updated to keep the line-133 mock targeting `subprocess.run` while the line-142/154 mocks target `_subprocess_util.run`; if a single existing mock drives both code paths via `side_effect`, the mock is split.

- **[BLOCKING] `TestForwardOutput` mock targets unaddressed after `_implementer_common.py` refactor** (Batch 3, Card 10) — Card 10 test-update instructions now enumerate three patch sites in `test-millpy-implement.py`: line 340 (`_call()` helper in `TestForwardOutput`), line 400 (`test_fo_7_sha_normalized`), and line 415 (`test_fo_8_sha_git_failure`). All three `patch.object(_implementer_common.subprocess, "run", ...)` are updated to `patch.object(_implementer_common._subprocess_util, "run", ...)`.

- **[NIT] `popen_detached` import omitted from card 8 requirements** (Batch 3, Card 8) — Card 8 now instructs the implementer to update the import line at `test-subprocess-util.py:18` from `from _subprocess_util import _GRACE_SECONDS, run` to `from _subprocess_util import _GRACE_SECONDS, run, popen_detached`. NITs are not optional under `mill-receiving-review`.

## Pushed Back

- **[NIT] Worker-path `import _subprocess_util` placement unspecified** (Batch 3, Card 9) — The reviewer's diagnosis (instruction was ambiguous about which `import subprocess` block the new import lives near) is **correct**. The reviewer's proposed fix — adding `import _subprocess_util` to BOTH the worker section AND the launcher section — is **rejected** because it conflicts with the documented design at [`millpy-bg.py:22`](plugins/mill/scripts/millpy-bg.py#L22): the worker fast-path is explicitly `stdlib only, no mill imports`. Adding `import _subprocess_util` to the worker section would violate that invariant, which exists so the worker can be loaded as a fast-path subprocess without paying the cost of mill helper imports.

  **Fixed differently:** Card 9 now explicitly states the worker section keeps its stdlib-only invariant; the worker's `subprocess.run` call gets `creationflags=getattr(subprocess, "CREATE_NO_WINDOW", 0)` inline (no new import needed; `getattr(...)` returns 0 on POSIX where the constant doesn't exist; `subprocess.run` forwards `creationflags` directly to `subprocess.Popen`). Only the launcher section adds `import _subprocess_util`. This addresses the underlying ambiguity (which the reviewer correctly identified) without violating the design constraint the reviewer overlooked.

  Cited evidence: [`millpy-bg.py:22`](plugins/mill/scripts/millpy-bg.py#L22) — `# ── worker fast-path — stdlib only, no mill imports ──────────────────────────`.
