# Review: 35 (A) — Centralize path resolution across all three modes — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/ + source
date: 2026-05-09
```

## Findings

No blocking or NIT findings. Observations below are confirmatory.

**Batch 1 — `_paths.py` / `test-paths.py`:** `resolve_active_worktree` correctly reads the hub marker via `resolve_hub_relative_path(git_root, cfg.get("hub_relative_path", "."))` before falling through to the worktree-dir lookup. M1/M1+sub return the worktree root (not the sub-dir hub), M2/M2+sub return `git_root`, as specified. `resolve_active_hub` two-tier resolution (cfg default, worktree-root stub override) is correct; the stub override path is tested both when cfg and stub agree and when they diverge. Error propagation via `ActiveWorktreeNotFound` / `ActiveWorktreeSlugMismatch` is clean. `patch("_inplace.resolve_worktrees_dir", ...)` correctly targets the imported name in `_inplace`'s namespace, matching the `test-inplace.py` pattern.

**Batch 2 — `_review_common.resolve_path`:** The switch from `resolve_active_worktree` to `resolve_active_hub` is complete. `hub_dir = _paths.resolve_hub_path()` sources cfg from the actual hub `.millhouse/`, not from `git_root/.millhouse/` — correct for M2+sub where only the hub carries `hub_relative_path`. Existing M1 tests stay green because `resolve_active_hub` with `hub_relative_path == "."` returns the worktree root, preserving prior assertions. The M2+sub fixture correctly places the active marker and `config.local.yaml` only at `<git_root>/src/Models/.millhouse/`, with no stub at `git_root/.millhouse/`, so `resolve_active_hub`'s stub lookup produces no override and cfg's "src/Models" remains authoritative.

**Batch 3 — `millpy-abandon.py` / `test-abandon.py`:** `status_path = active_hub / "task" / "status.md"` is correct. Git operations target `active_hub` via `-C str(active_hub)`, keeping `"task/status.md"` correct as a hub-relative path under sub-dir hub configs. The trampoline's lambda `lambda container, slug, *, cfg, git_root: Path(r'...')` uses the correct keyword-only signature. All seven test cases updated to assert task-branch git commands instead of wiki commits.

**Batch 4 — `millpy-cleanup.py`:** The substitution is mechanical and exact — stub-read block preserved, ternary replaced by `_paths.resolve_hub_relative_path(wt_path, hub_subpath) / ".millhouse"`. Behavior is byte-equivalent for both `"."` and sub-dir cases.

**Batch 5 — docs:** CLAUDE.md `## Path invariants` gains the new bullet mandating `resolve_active_worktree` / `resolve_active_hub` and banning inline `container / "wts" / slug` constructions. `mill-claim/SKILL.md` documents the in-place contract clearly. `mill-spawn/SKILL.md` cross-reference is present.

**Cross-batch contract:** `resolve_active_hub`'s signature `(container, slug, *, cfg, git_root)` is consistently used across all three callers (`_review_common.resolve_path`, `millpy-abandon.py`, `test-*.py`). No two batches reimplement the same helper. `__all__` updated in `_paths.py`.

## Verdict

APPROVE — implementation is complete, correct, and fully aligned with the approved plan across all five batches.