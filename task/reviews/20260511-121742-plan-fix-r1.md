# Plan fix — round 1

```yaml
slug: fix-wiki-cwd-cascade
round: 1
source_review: task/reviews/20260511-121611-plan-review-r1.md
verdict: REQUEST_CHANGES (1 BLOCKING + 4 NITs) → fully resolved
```

## Fixed

- `20260511-121611-plan-review-r1.md` → `[BLOCKING] Card 2 swallow clause must catch BaseException, not Exception`. Card 2 Requirements now explicitly call out `except BaseException:` (or `except (Exception, SystemExit):`) and cite the inheritance fact + the card 5 test case 4 dependency. An implementer writing `except Exception:` would now have to actively ignore the requirement text.
- `20260511-121611-plan-review-r1.md` → `[NIT] Card 5 test case 6 missing mock for resolve_main_worktree_root`. Card 5 case 6 now wraps the call in `with patch("_paths.resolve_main_worktree_root", return_value=main_root):`, matching the existing pattern used by other `resolve_wiki_path` tests in the same file.
- `20260511-121611-plan-review-r1.md` → `[NIT] Dead CLAUDE.md entry in walker allowlist`. Card 7 Requirements now omit `CLAUDE.md` from the allowlist and add a parenthetical explaining it is intentionally excluded because the walker scope does not reach the repo root.
- `20260511-121611-plan-review-r1.md` → `[NIT] Batch 3 smoke-check grep count is 8, not 9`. Batch 3 Batch Tests now state the expected count is 8 (one per SKILL.md note) and clarify that CLAUDE.md uses the heading `## Wiki access` (no trailing colon) and therefore does not match `"Wiki access:"`.
- `20260511-121611-plan-review-r1.md` → `[NIT] Card 10 uses source-tree path instead of ${CLAUDE_PLUGIN_ROOT}`. Card 10 Requirements now lead with the cache-resolved `PYTHONPATH="${CLAUDE_PLUGIN_ROOT}/scripts" uv run --project "${CLAUDE_PLUGIN_ROOT}"` form and document the documented millhouse-self source-tree fallback (only when `${CLAUDE_PLUGIN_ROOT}` is empty in the current shell, per CLAUDE.md's existing exception).

## Pushed Back

_(none)_
