# Review: 46 (A) — Home.md state machine + split mill-merge teardown into mill-cleanup — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-11
```

## Findings

### [BLOCKING] Card 20: old worktree/branch simulation code not removed
**Location:** Batch 4 / Card 20
**Issue:** The integration test currently executes `git worktree remove --force` and `git branch -D` (simulating old Steps 8–9) before the assertions. Card 20 says to replace `_assert(not worktree.exists(), ...)` and `_assert(not branches, ...)` with positive inverse assertions, but never instructs removing the git commands that delete the worktree and branch beforehand. An implementer following the card literally produces a test where the worktree is already gone when the positive assertion runs.
**Fix:** Add an explicit instruction to remove the `git worktree remove --force`, `git branch -D`, and junction-removal calls from the test body (the old Steps 8–9 simulation), and note that a trailing `mill-cleanup --apply` invocation can handle fixture teardown for the temp dir instead.

### [BLOCKING] Card 20: archive-tag assertion has no corresponding creation step
**Location:** Batch 4 / Card 20
**Issue:** Card 20 adds a new assertion `git tag -l archive/<slug>` must return non-empty, but the test never creates the archive tag — it simulates `git merge --squash`, the Home.md flip, and (currently) the wiki active-dir deletion, but has no `git tag archive/<slug>` step. The new assertion would always fail.
**Fix:** Add an explicit instruction to insert `git -C <hub> tag archive/<slug> test/<slug> && git -C <hub> push origin archive/<slug>` between the squash commit (step 5) and the Home.md flip (step 7) — matching the sequence in the updated mill-merge SKILL.md.

### [NIT] Card 18 says "three" but describes four test blocks
**Location:** Batch 3 / Card 18
**Issue:** Opening sentence reads "Add three new test blocks" but proceeds to describe four (MERGED, OPEN, CLOSED, gh-pr-list-failed). An implementer may implement only three.
**Fix:** Change "three" to "four".

### [NIT] Card 4 omits `test_slug_from_branch_phase_done` from rewrite list
**Location:** Batch 1 / Card 4
**Issue:** The card says to rewrite tests "(likely named `test_slug_from_branch_phase_abandoned` / `test_slug_from_branch_phase_none`)" but does not mention `test_slug_from_branch_phase_done`, which also asserts `MarkerError` for a non-active phase and will fail after card 3 removes the phase check.
**Fix:** Add `test_slug_from_branch_phase_done` to the list of tests that must be rewritten as success-case variants.

## Verdict

REQUEST_CHANGES — two blocking gaps in card 20 produce a broken integration test.