# Plan Fix Report — round 1

```yaml
slug: config-schema-refactor
round: 1
review_file: 20260509-155948-plan-review-r1.md
date: 2026-05-09
```

## Fixed

- 20260509-155948-plan-review-r1.md "max_rounds clamp semantics conflict with mill-plan SKILL.md escape hatch" (truncated heading; verdict text mentioned 5 BLOCKINGs but only 4 had headings; reconstructed from line 1 + verdict prose). Cards 9, 10, 12 and the overview Shared Decision rewritten from `min(scope_rounds, max_rounds)` clamp semantics to OVERRIDE semantics — when `--max-rounds N` is set, both scopes' effective max becomes `N` (replacing the configured value). Preserves the existing `mill-plan/SKILL.md:147` escape-hatch where `--max-rounds {N+1}` extends past the configured cap.
- 20260509-155948-plan-review-r1.md "Card 15 misses mill-go holistic Guard expression". Card 15 extended with an instruction to update `mill-go/SKILL.md:162` Guard expression from `cfg.get("review", {}).get("code", {}).get("holistic", True)` to `cfg.get("roles", {}).get("code-review", {}).get("holistic", {}).get("reviewer") is not None`. Default behaviour intentionally flips from "enabled by default" to "skip if no reviewer set" — matches the schema's skip semantics (null reviewer = skip).
- 20260509-155948-plan-review-r1.md "Card 15 omits null-reviewer skip in mill-start". Card 15 extended with an instruction to update `mill-start/SKILL.md:66` skip guard from `If max_review_rounds == 0:` to `If max_review_rounds == 0 OR roles.discussion-review.holistic.reviewer is None:`. Both schema-level skip conditions (`rounds: 0` OR `reviewer: null`) are now covered.
- 20260509-155948-plan-review-r1.md "Card 13 test 14a rewrite is unimplementable as specified". Card 13 instruction rewritten: tests 14a and 14b in `test-review-code-flow.py` are now DELETED entirely (not migrated). The effort/model forwarding contract they covered is owned by `test-reviewer-single.py` subtests 4 and 5 (introduced in card 4), which mock `_llm_claude.run_bulk` / `run_tool_use` and assert `effort=spec["effort"]` is forwarded to the LLM provider. Coverage preserved at the correct boundary (provider call site, not stub kwargs).
- 20260509-155948-plan-review-r1.md "No card assigns the migration-warning test". Card 13 extended with explicit instructions to add a test in `test-review-common.py` that constructs a `config.local.yaml` overlay containing a top-level `review:` key, calls `_review_common.load_config`, and asserts the warning is emitted to stderr (captured via `contextlib.redirect_stderr`). Test fixture-dir setup uses `tempfile.TemporaryDirectory`.
- 20260509-155948-plan-review-r1.md "[NIT] Card 5 lacks invocation-mechanism guidance for unknown types". Card 1 extended with a defensive type guard in `_reviewers.resolve`: raises `ReviewerError(f"Unknown reviewer type: {spec['type']!r}")` when `spec['type']` is not in `{"single", "cluster"}`. Unreachable if `load` validated correctly, but protects direct callers that bypass `load`.
- 20260509-155948-plan-review-r1.md "[NIT] Tests 5/6 effort=None assertion becomes vacuous after migration". The reviewer's NIT is based on a misreading of the plan — my card 4 subtest 3 does NOT assert `effort=None` on the stub kwargs. Defensively added a clarifying note to card 4 subtest 3 stating that `effort`/`model` MUST NOT be asserted against the captured stub kwargs (because `_reviewer_single.run` deliberately does not forward those to the test stub) and pointing readers to subtests 4/5 for the effort-forwarding contract.

## Pushed Back

(none — every finding fixed)
