# Plan Fix Report — round 2

```yaml
slug: config-schema-refactor
round: 2
review_file: 20260509-161147-plan-review-r2.md
date: 2026-05-09
```

## Fixed

- 20260509-161147-plan-review-r2.md "[BLOCKING] Card 13 migration-warning test asserts contradictory behavior". Removed the contradictory "Also assert the merged cfg returned does NOT contain `cfg['review']` after the merge" sentence from card 13's migration-warning test instructions. Replaced with: "The merged cfg returned by `load_config` is otherwise usable for downstream code; do NOT assert anything about the presence/absence of `cfg['review']` — the deep-merge leaves `cfg['review']` as a harmless orphan branch and that is intended behaviour per card 7's spec."
- 20260509-161147-plan-review-r2.md "[NIT] Cards 4 and 14 list `run-all.py` edits that are no-ops". Verified `run-all.py:19` uses `HERE.glob("test-*.py")` for auto-discovery. Removed `plugins/mill/unit_tests/run-all.py` from card 4's Edits list and from the overview's All Files Touched. Card 4 Requirements clarified: "`run-all.py` requires no source change: it discovers tests via `HERE.glob('test-*.py')` so the new files are picked up automatically." Card 14 Requirements clarified: "`run-all.py` requires no edit — it uses `HERE.glob('test-*.py')` so the deleted test file disappears from the discovery set automatically."
- 20260509-161147-plan-review-r2.md "[NIT] Card 9 batch skip `rounds == 0` not spelled out in rewiring details for `run()`". Card 9's bullet on skip semantics extended with concrete rewiring instructions: at line ~315 replace `if batch_reviewer_name is None:` with `if batch_reviewer_name is None or cfg["roles"]["plan-review"]["batch"]["rounds"] == 0:`; apply symmetric check at lines ~328–329 for the holistic scope.
- 20260509-161147-plan-review-r2.md "[NIT] Card 13 does not explicitly address removal of `load_reviewer` test in `test-review-common.py`". Verified `test-review-common.py:98` imports `load_reviewer` and `lines 476–482` test `load_reviewer("nonexistent_xyz_abc")`. Card 13 extended with explicit instruction: "delete the `load_reviewer` import line at line ~98 and the test block at lines ~476–482". Coverage is preserved by `test-reviewers.py` subtest 11.

## Pushed Back

(none — every finding fixed)
