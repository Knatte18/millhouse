# Review: 47 (A) — mill-merge-in: allowlist for known-broken pre-existing test failures — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-11
```

## Findings

### [NIT] Card 2 Context field is literally "none"
**Location:** Batch 1 / Card 2
**Issue:** Criteria require "non-empty per card"; `Context: none` violates the letter of the rule even though the only needed file (`wiki-config.yaml`) is listed in `Edits:` and implicitly read.
**Fix:** Set `Context: plugins/mill/templates/wiki-config.yaml` (same as the file in `Edits:`) to satisfy the rule explicitly, or annotate the "none" with a comment explaining the file is in `Edits:`.

## Verdict

APPROVE — one nit on Context field; no blocking issues; all decisions faithfully implemented.