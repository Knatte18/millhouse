# Review: 36 (A) — Bug-fix batch 3 — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/ + source
date: 2026-05-09
```

## Findings

No findings. All three batches are correctly implemented.

**Batch 1 — cleanliness-helper**

`_cleanliness.py` matches every requirement: fully-qualified `_subprocess_util.run(...)` calls, `snapshot_path.parent.mkdir(parents=True, exist_ok=True)` before write, `str.splitlines()` normalization, sorted set-diff return. `test-cleanliness.py` covers all nine plan-specified cases with correct patch targets (`"_subprocess_util.run"`), correct CRLF/LF write_bytes in case 8, and correct `sys.stderr` patch in case 7.

**Batch 2 — cleanliness-wireup**

`import _cleanliness` lands alphabetically between `_active` and `_implementer_sonnet`. Snapshot capture is inserted after `start_sha` and before `set_batch_fields` as specified. `git add` is updated to stage the snapshot path via `str(snapshot_path.relative_to(project_root))` — consistent with the existing `review_file_arg` pattern in the resume branch. `test-millpy-implement.py` adds `self.mock_capture_snapshot` in `setUp` to prevent real git calls; per the plan, asserting the staged path shape is explicitly out of scope.

**Batch 3 — git-pr-task-contract**

`### 1.5 Detect task branch` is inserted in the correct position (after `### 1.` and before `### 2.`). The bash snippet, the blockquote redirect message, and the "proceed to step 2" close all match the plan's required wording verbatim. No downstream sections were renumbered or modified.

**Cross-batch contracts:** `capture_snapshot(worktree, snapshot_path) -> None` and `compute_new_dirt(worktree, snapshot_path) -> list[str]` produced in batch 1 are consumed correctly in both batch 2 call sites. Inline `signature:` line in mill-go SKILL.md uses the correct backtick-wrapped one-line form.

## Verdict

APPROVE
All cards fully implemented; plan alignment exact; no regressions in existing test surface.