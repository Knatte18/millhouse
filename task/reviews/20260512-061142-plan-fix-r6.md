# Plan fix — round 6

```yaml
review_file: task/reviews/20260512-061059-plan-review-r6.md
date: 2026-05-12
```

## Fixed

- **[BLOCKING] Dataclass field ordering TypeError — Card 11** — Card 11 now places `to_reap_pr` as the LAST field in `CleanupPlan` (after `to_report`), with an explicit rationale: a defaulted field placed before non-defaulted fields raises `TypeError: non-default argument follows default argument` at class-definition time. All existing callers use keyword arguments so the position change is backward-compatible.
- **[NIT] Card 20 references wrong container sub-path** — Card 20's worktree-intact assertion now uses the existing local variable `worktree` (defined by `_setup_trio`) instead of constructing `container / "wts" / slug`. The test fixture uses `container / "worktrees" / slug`, not the production `container / "wts" / slug` path.

## Pushed Back

(none)
