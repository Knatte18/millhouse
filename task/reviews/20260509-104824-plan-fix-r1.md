# Plan-fix report — round 1

```yaml
task: "35 (A) — Centralize path resolution across all three modes"
slug: mill-path-resolution-audit
round: 1
review_file: 20260509-104410-plan-review-r1.md
verdict_in: REQUEST_CHANGES
blocking_in: 2
nits_in: 2
```

## Fixed

- **[BLOCKING] M2+sub in-place + sub-dir hub remains broken post-implementation** (review r1). `_review_common.resolve_path` now sources cfg from `_paths.resolve_hub_path() / ".millhouse"` (the cwd, which equals the hub for review scripts) instead of `git_root / ".millhouse"`. mill-claim writes `hub_relative_path` only at the hub, never bootstrapping a stub at `git_root/.millhouse/`, so the previous source picked up an empty cfg and `resolve_active_worktree` looked for the active marker at the wrong location. Card 4's pseudocode + docstring updated. Card 3's M2+sub test scaffolding now mirrors what mill-claim actually creates (active marker + cfg at `<hub>/.millhouse/` only; no bootstrap stub).

- **[BLOCKING] Git relative path hardcoded wrong for sub-dir hub in millpy-abandon** (review r1). Card 6 now runs `git -C <active_hub> add task/status.md` (and the matching commit/push) instead of `git -C <active_worktree>`. Using the hub as the `-C` target keeps `"task/status.md"` correct as a hub-relative path under sub-dir hub configs (where the git_root is several directory levels above the hub). The Card 5 assertions were also updated to expect `git -C <active_hub> ...`. The unused `active_worktree` resolution was dropped from the abandon flow; Card 5's trampoline mock keeps `resolve_active_worktree` only for compatibility (no caller in the refactored abandon).

- **[NIT] M2 test mock covers branch call but not worktrees-dir subprocess call** (review r1). Card 1's Requirements now mandate `unittest.mock.patch("_inplace.resolve_worktrees_dir", return_value=<tmp-dir-without-slug>)` alongside the `_subprocess_util.run` patch in M2 and M2+sub cases. The `test-inplace.py` reference is preserved as the canonical pattern.

- **[NIT] test-cleanup.py not visible in review materials** (review r1). Card 7 Context now lists `plugins/mill/unit_tests/test-cleanup.py` so the reviewer can see it next round. The Batch Tests section was rewritten honestly: `test-cleanup.py` covers only `hub_relative_path == "."`; the sub-dir branch's correctness rests on `resolve_hub_relative_path`'s own unit tests in `test-paths.py`. The earlier false claim that "existing tests cover both branches" was removed.

## Pushed Back

(none)
