All four findings addressed and committed:

- **BLOCKING Card 16**: Added `(worktree_path / "_mill").mkdir(parents=True, exist_ok=True)` before the portal `_junction.create` in spawn, with a note explaining why (Windows junction target must pre-exist; `write_initial_status` fires after this block).
- **BLOCKING Card 17**: Same mkdir guard added before the portal `_junction.create` in claim, referencing `resolve_hub_path() / "_mill"`.
- **BLOCKING Card 6**: Added explicit "Root-level fallback drop (intentional)" paragraph — shim covers `_mill/` → `task/` but not `task/` → root; pre-task-32 worktrees intentionally unsupported; Card 6 now includes deleting `test_build_plan_falls_back_to_root_status_md`.
- **NIT Card 10**: Added requirement (4) to update both the per-function docstring and the module-level Public API entry from `task/status.md` to `_mill/status.md`.
- **NIT Card 20**: Added a note that operators must manually remove stale `wiki/active/` subdirectories post-upgrade; explains why batch 4's orphan scan doesn't cover this.