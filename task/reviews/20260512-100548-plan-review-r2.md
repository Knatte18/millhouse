# Review: 40 (B) — mill-finalize: lift PR decision out of mill-merge — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-12
```

## Findings

### [BLOCKING] `_status.read_status` undocumented; likely wrong function name
**Location:** Batch 1, Card 1 — Entry Step 6
**Issue:** Plan specifies `data = _status.read_status(status_path)` with flat access `data["phase"]`. Mill-go (in Context for batch 3) uses `_status.read_full(status_path)` returning `{"yaml": dict, "timeline": list[str]}` with access via `data["yaml"]["phase"]`. No other SKILL.md references `_status.read_status`; no `signature:` line is provided, violating the inline-signature convention all other mill helpers follow.
**Fix:** Verify `_status.read_status` exists; if not, replace with `_status.read_full(status_path)` and `data["yaml"]["phase"]`. Either way, add the required `signature:` comment.

### [BLOCKING] `_tasks_md.parse` undocumented; function absent from all provided SKILL.md files
**Location:** Batch 2, Card 3 — Change A (Phase Gate)
**Issue:** Plan specifies `tasks = _tasks_md.parse(home_text)` returning objects with `.slug` and `.phase` attributes. No provided SKILL.md (mill-merge, mill-go, mill-merge-in) references `_tasks_md.parse`; only `set_phase` (takes text → text) and `set_phase_at` (takes Path → None) appear. If the function does not exist, the pr-pending Home.md fallback in the refactored phase gate cannot run. No `signature:` provided.
**Fix:** Confirm `_tasks_md.parse` exists with the stated return type; if not, use `f"[pr-pending] {slug}"` string-search on `home_text` or rewrite using `set_phase` to probe current phase.

### [NIT] Phase gate pseudocode contains invalid `find task = …` syntax
**Location:** Batch 2, Card 3 — Change A (Phase Gate)
**Issue:** `find task = next((t for t in tasks if t.slug == slug), None)` — "find task" is not valid Python; an implementer may copy it literally or interpret it inconsistently.
**Fix:** Replace with `task = next((t for t in tasks if t.slug == slug), None)`.

## Verdict

REQUEST_CHANGES
Two undocumented helpers (`_status.read_status`, `_tasks_md.parse`) must be verified or replaced before the generated SKILL.md files are correct.