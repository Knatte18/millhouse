# Review: 46 (A) — Home.md state machine + split mill-merge teardown into mill-cleanup — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-11
```

## Findings

### [NIT] Card 20 leaves wiki-active teardown in integration test
**Location:** Batch 4 / Card 20
**Issue:** Card 8 removes Step 10 (Remove wiki active directory) from mill-merge, but Card 20's requirements say nothing about removing the corresponding `shutil.rmtree(wiki / "active" / slug)` + `_wiki.write_commit_push(wiki, [f"active/{slug}/"], ...)` block from `test-merge.py`, leaving the test simulating behaviour mill-merge no longer owns.
**Fix:** Add to Card 20's requirements: remove that block and assert `(wiki / "active" / slug).exists()` post-merge to confirm mill-cleanup's responsibility.

### [NIT] Card 20 says "keep" archive tag assertion that does not exist
**Location:** Batch 4 / Card 20
**Issue:** "Keep all assertions that mill-merge DOES still perform: … archive tag `archive/<slug>` exists on the local repo" — the current `test-merge.py` has no archive tag creation step and no such assertion.
**Fix:** Say "add" rather than "keep"; include an explicit `git tag archive/<slug> <child_branch>` step and a non-empty `git tag -l archive/<slug>` assertion, since Step 6 still runs in mill-merge after this split.

### [NIT] Card 18 missing `gh pr list` failure-path test
**Location:** Batch 3 / Card 18
**Issue:** Card 15 specifies: "On non-zero exit or empty stdout, print to stderr and return early without touching state." Card 18 has no test for this branch.
**Fix:** Add a fourth test block: mock `gh pr list` returning `returncode=1`; assert no `git tag`, no worktree-remove, no Home.md change, and that a stderr line containing the slug is emitted.

## Verdict

APPROVE — three NITs confined to the integration-test card; all production-code logic, batch sequencing, decision alignment, and unit-test coverage are sound.