# Plan fix — round 4

```yaml
review_file: task/reviews/20260511-184854-plan-review-r4.md
date: 2026-05-11
```

## Fixed

- **[BLOCKING] Card 20: old worktree/branch simulation code not removed** — Card 20 now explicitly instructs the implementer to delete every `git worktree remove --force`, `git branch -D`, `_junction.remove`, `shutil.rmtree(wiki / "active" / slug)`, and `_wiki.write_commit_push(...)` simulation call from the test body before flipping the assertions. Without the removal step, the inverse assertions would fail because the simulation already removed the worktree.
- **[BLOCKING] Card 20: archive-tag assertion has no corresponding creation step** — Card 20 now adds explicit `git tag archive/<slug> <child_branch>` + `git push origin archive/<slug>` calls between the squash commit (step 5) and the Home.md flip (step 7) in the test, mirroring mill-merge Step 6. The new assertion `git tag -l archive/<slug>` non-empty now has a creation step to match.
- **[NIT] Card 18 says "three" but describes four test blocks** — opening sentence in Card 18 changed from "Add three new test blocks" to "Add four new test blocks".
- **[NIT] Card 4 omits `test_slug_from_branch_phase_done` from rewrite list** — Card 4's rewrite list now explicitly names `test_slug_from_branch_phase_abandoned`, `test_slug_from_branch_phase_none`, AND `test_slug_from_branch_phase_done` as candidates for rewriting (any of them currently asserting `MarkerError` must become a success variant).

## Pushed Back

(none)

## Status note

Plan is NOT approved yet — operator paused review at end-of-session. Next session: re-run sonnetmax plan review (round 5; pass `--max-rounds 5`) to verify the four fixes above. Continue until no BLOCKINGs remain, per operator instruction. Sonnetmax-round count so far: 3 (rounds 2, 3, 4 of overall counter; round 1 was Flash and does not count).
