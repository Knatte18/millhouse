# Plan Fix Report — round 3

Source review: `task/reviews/20260511-182629-plan-review-r3.md`

## Fixed

- **[NIT] Card 2 Context field is literally "none"** — applied the reviewer's alternative-B suggestion ("annotate the 'none' with a comment explaining the file is in Edits:"). Card 2 Context now reads: `none — the sole file the implementer reads is plugins/mill/templates/wiki-config.yaml, listed in Edits: and therefore implicitly read; no additional reference files are needed.` This satisfies the holistic criterion's "non-empty per card" guidance while preserving the literal "none" sentinel that `plugins/mill/templates/plan-batch.md` line 50 prescribes for empty fields.

## Pushed Back

- **[NIT] (partial: alternative A)** — the reviewer's primary fix ("Set `Context: plugins/mill/templates/wiki-config.yaml` (same as the file in `Edits:`)") was not applied. Reason: it conflicts with the template's documented rule at `plugins/mill/templates/plan-batch.md` line 43 — "Files listed in Edits: are implicitly read — do not repeat them in Context:" — which is a documented design decision (mill-receiving-review HARM CHECK category b). The reviewer's alternative-B (annotation) was applied instead and is fully compatible with both the holistic criterion and the template rule.
