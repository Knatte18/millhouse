```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: task/plan/
date: 2026-05-09
```

## Findings

### [NIT] `_test_cfg.py` timeout defaults diverge from live wiki values
**Location:** Batch 1, Card 3
**Issue:** Card 3 specifies `_test_cfg.make_minimal_cfg` with `bulk_timeout: 600` and `implementer_timeout: 1800`, but Card 5's live wiki flip sets `bulk_timeout: 900` and `implementer_timeout: 3600`, and the existing template already has `implementer_timeout: 3600`. The fixture baseline will silently differ from production values.
**Fix:** Use `bulk_timeout: 900` and `implementer_timeout: 3600` to match the post-flip live wiki. Tests that need different values can override via `**overrides`.

### [NIT] `_make_fixture` return-value gap in discussion flow tests
**Location:** Batch 2, Card 13
**Issue:** Card 13 directs `_test_registry.write_to(wiki_root)` inside `_make_fixture` of `test-review-discussion-flow.py` and requires updating every call site to `discussion_run(cfg, SLUG, mill_dir, wiki_root, project_root)`, but does not explicitly state that `_make_fixture`'s return tuple must grow to include `wiki_root` (currently `(mill_dir, project_root)`). An implementer who misses this will compute `wiki_root` independently in each test body, risking inconsistency.
**Fix:** Add explicit instruction: update `_make_fixture` to return `(mill_dir, wiki_root, project_root)` where `wiki_root = tmp / "container" / "wiki"`, and update every unpacking call site accordingly.

### [NIT] `_reviewer_test_stub.py` `effort` parameter becomes dead code after refactor
**Location:** Batch 2, Card 14
**Issue:** Card 14 deletes `MODE = "bulk"` but leaves `effort=None` in the stub's `run` signature and kwargs dict. After `_reviewer_single.run` is wired in, `effort` is never passed to the stub (dispatched as `stub.run(prompt_text, session_id=..., resume=..., timeout=...)`). The captured-kwargs dict will include `effort: None` silently via the default.
**Fix:** Either remove `effort` from the stub signature in Card 14 or add a note explaining the dead parameter is retained intentionally for caller-supplied override tests. The existing flow-test assertions `retry_kwargs == {"session_id": ..., "resume": True, "timeout": None, "effort": None}` still pass because the default fills it, so this is cosmetic.

### [NIT] Error message in `_review_plan.py` "both null/zero-rounds" still references old config path style
**Location:** Batch 2, Card 9
**Issue:** Card 9 rewires the batch-null guard but does not explicitly update the `ReviewError` message (`"review.plan.batch is null and review.plan.holistic is also null — at least one must be set"`) to reference the new `roles.plan-review.batch/holistic` paths. A stale error message is confusing during operator debugging.
**Fix:** Add explicit instruction to update the error string to `"roles.plan-review.batch.reviewer is null/zero-rounds and roles.plan-review.holistic.reviewer is also null/zero-rounds — at least one must be configured"`.

### [NIT] `_review_discussion.py` `tool_use_timeout` not forwarded to `_reviewer_single.run`
**Location:** Batch 2, Card 8
**Issue:** Card 8 does not wire `cfg["llm"]["tool_use_timeout"]` through to `_reviewer_single.run`. The discussion review silently uses `_llm_claude.run_tool_use`'s hardcoded default (900 s). This is a pre-existing gap — the current code calls `reviewer.run(prompt_text)` with no timeout — but the refactor is a natural opportunity to close it.
**Fix:** Add `timeout = cfg["llm"].get("tool_use_timeout")` in `_review_discussion.run` and pass it to `_reviewer_single.run(spec, prompt_text, timeout=timeout)`. One line; low-risk.

## Verdict

APPROVE — all findings are NITs; no blocking defect prevents correct implementation. The plan is internally consistent and the `test_stub` carve-out, atomic batch-2 lockstep, and skip-semantics alignment are correctly specified throughout.