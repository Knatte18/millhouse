# Plan fix — round 7 (final round, per operator)

```yaml
review_file: task/reviews/20260512-063653-plan-review-r7.md
date: 2026-05-12
```

## Fixed

- **[BLOCKING] Card 18 patches wrong worktree removal function** — Card 18 test 1 now patches `mill_cleanup._worktree.remove_safe` (not `_worktree.remove`), matching the `_apply_worktree_record` call target from card 15. Assertion (c) now reads `_worktree.remove_safe was called with the worktree path`.
- **[NIT] Card 20 portal assertion references non-existent fixture state** — Card 20 drops the `assert (container / "portals" / slug).exists()` assertion. The `_setup_trio` fixture never creates the portal junction, so the assertion would always fail. Added an explicit note: "Do NOT add a portal-junction-exists assertion."
- **[NIT] Card 17 test 1 / Card 12 — double-implementation of `_mock_branch_run` tag branch** — Card 17 test 1 now reads "uses the default tag behavior already installed by card 12 — no extension to `_mock_branch_run` needed for this test", removing the duplicate "extend `_mock_branch_run`" instruction.
- **[NIT] Card 4 inconsistent final test name convention** — Card 4 now specifies a uniform naming scheme (no `phase_` prefix on any of the five rewritten tests): `test_slug_from_branch_ready_to_merge`, `test_slug_from_branch_pr_pending`, `test_slug_from_branch_done`, `test_slug_from_branch_abandoned`, `test_slug_from_branch_none`. Explicit instruction added: "do NOT leave any `test_slug_from_branch_phase_*` test in the file after this card."

## Pushed Back

(none)

## Final state

Operator marked this the last review round ("Dete blir siste review"). Plan approved post-fixes; no further review rounds. Ready for `/mill-go`.
