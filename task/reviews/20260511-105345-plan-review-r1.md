# Review: 43 (A) — Discussion-review gaps in batches + NOTE-finding handling — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-11
```

## Findings

### [NIT] Step 4a instructs path construction; JSON `file` field already provides the path
**Location:** Batch 1, Card 1 — step 4a requirement text
**Issue:** The plan tells the implementer to write "read the review file at `task/reviews/<YYYYMMDD-HHMMSS>-discussion-r<N>.md` (filename pattern from `_review_common.write_review_file`)" — implying the orchestrator should construct the path. The existing step 2 (unchanged) already delivers the review file's absolute path in `reviews[0].file`. Additionally, step 2 describes the review file location as `<worktree_root>/reviews/` (no `task/` prefix), so the template path and step 2's prose will conflict inside the same rendered SKILL.
**Fix:** Rewrite step 4a to say the orchestrator reads from the `file` path in the JSON envelope rather than naming a template pattern. The write target for the fixer report in step 4b is correctly specified as `task/reviews/...` — only the read target in 4a needs this change.

## Verdict

REQUEST_CHANGES
One NIT: step 4a path construction conflicts with the absolute `file` path step 2 already provides.