# Plan Fix — Round 1

Source review: `task/reviews/20260512-140636-plan-review-r2.md`

## Fixed

- `20260512-140636-plan-review-r2.md` — "[NIT] Card 1 -- resolve_task_path shim: cfg_relative_path argument contract is implicit": added exact shim body (Python code block) and a worked call-site example clarifying that callers always pass the `_mill/`-prefixed config path, the shim replaces `_mill/` → `task/` (not prepends), so `task/_mill/discussion.md` is impossible.
- `20260512-140636-plan-review-r2.md` — "[NIT] Card 20 -- pre-existing wiki/active/ directories: no operator guidance": added an upgrade note to card 33 (`mill-setup/SKILL.md` Phase 8) instructing operators to manually remove stale `wiki/active/<slug>/` subdirectories on pre-task-33 hubs, with the exact commands and a note that mill-cleanup's orphan scan does not cover this path.

## Pushed Back

- `20260512-140636-plan-review-r2.md` — "[NIT] Batch 4 -- portal removal timing gap for normally-cleaned-up tasks": **finding is factually inaccurate**. The existing `millpy-cleanup.py._apply_worktree_record` (lines 405-408) already calls `_junction.remove(container_path / "portals" / record.slug)` in the normal worktree-removal path. `_apply_inplace_record` (lines 366-369) does the same. Both functions were pre-existing before any batch in this plan and are not modified by batches 3 or 4. The timing gap described does not exist — portals are removed during normal cleanup, not only by the orphan scan.
