# Review: 54 (A) — Bug-fix batch 6 (post-46/50 triage) — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-12
```

## Findings

### [BLOCKING] Card 3 omits `${CLAUDE_PLUGIN_ROOT}` braced form in Holistic section
**Location:** Batch 1, Card 3
**Issue:** The Holistic code-review section of `mill-go/SKILL.md` (steps 3, 3.5, and 5) exclusively uses the braced form `${CLAUDE_PLUGIN_ROOT}` in its bash blocks — a syntactically distinct string from `$CLAUDE_PLUGIN_ROOT` — but Card 3's replacement spec only covers `$CLAUDE_PLUGIN_ROOT` → `$PLUGIN_ROOT` and claims "25 such occurrences"; a literal implementation leaves approximately 10 Holistic bash-block references unexpanded to `$PLUGIN_ROOT`, so when `$CLAUDE_PLUGIN_ROOT` is unset those lines silently expand to empty string and fail at runtime.
**Fix:** Extend the requirements to also replace `${CLAUDE_PLUGIN_ROOT}` → `${PLUGIN_ROOT}` in all Holistic-section bash blocks, and correct the stated count (observed ~36 unbraced + ~10 braced ≈ 46 total references in code blocks, not 25).

## Verdict

REQUEST_CHANGES
Card 3's replacement spec is incomplete — the braced `${CLAUDE_PLUGIN_ROOT}` form in the Holistic section is unaddressed.