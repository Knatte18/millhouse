# Plan-fix report: round 1

Reviewed file: [task/reviews/20260511-110930-plan-review-r1.md](20260511-110930-plan-review-r1.md)
Verdict in: REQUEST_CHANGES (1 BLOCKING + 3 NIT)
Verdict out: applied via Decision Tree (default FIX); no PUSH BACK.

## Fixed

- **[BLOCKING] Batch 5 leaves existing ERROR-path tests contradicting the new code behavior** — Updated Card 11 to UPDATE existing tests in `test-review-code-flow.py` (three fixtures around lines 597/629/664 and assertions at 602/634/669) and `test-review-plan-flow.py` (line ~883) instead of ADDING new test functions. Mixed APPROVE+ERROR tests (lines 354, 1040 in plan-flow) explicitly left alone per reviewer's "Test 4 unaffected" note.
- **[NIT] Batch 1 / Card 1 — `.gitignore` placement risks wrong block** — Removed the "inside the mill-managed block" option from Card 1; placement is now unambiguously "directly BEFORE the `# === mill-managed` block" with a one-line justification (ownership: Claude Code harness, not mill-regenerated).
- **[NIT] Batch 3 / Card 5 — `_warn_if_prose_diverges` regex ignores `severity`** — Changed Card 5 regex from hardcoded `(blocking|BLOCKING)` to `re.escape(severity)` with `re.IGNORECASE`. Added Card 6 test #5 (`test_parse_blocking_count_warns_for_gap_severity`) to prove the GAP-severity path fires (which the old hardcoded regex would have silently dropped).
- **[NIT] Batch 7 / Card 14 — described fix may already be present** — Re-scoped Card 14: verified the existing code (`_HARDLINK_DEFAULTS = {}` plus the `if not raw: return dict(_HARDLINK_DEFAULTS)` early-return) already handles missing/null `hardlinks:` blocks. Card 14 now does ONLY a docstring update (no runtime code change). `_setup.py` dropped from the Edits list and from the overview's All Files Touched. Card 15 regression test stays as-is to backstop the behavior.

## Pushed Back

(empty)
