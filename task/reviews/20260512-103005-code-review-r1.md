# Review: 40 (B) — mill-finalize: lift PR decision out of mill-merge — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmedium
reviewed_file: plan/ + source
date: 2026-05-12
```

## Findings

### [NIT] mill-finalize sync_pull uses skill name, not task slug

**Location:** `plugins/mill/skills/mill-finalize/SKILL.md` Entry Step 2
**Issue:** `_wiki.sync_pull(wiki_path, slug="mill-finalize")` uses the skill name as the slug before the actual task slug is resolved (Step 4–5). This is spec-compliant but inconsistent with every other skill that passes the real task slug.
**Fix:** Not strictly needed — spec is explicit here. Consider documenting the intent with a comment `# slug not yet resolved; use skill name for tracing`.

### [NIT] Branch-protection fallback references absent task/status.md

**Location:** `plugins/mill/skills/mill-merge/SKILL.md` Step 5 fallback sub-step 6
**Issue:** Sub-step 6 tries `git add task/status.md` after Step 4 already ran `git rm -r task/` and committed the cleanup. This would fail on the task branch. Pre-existing issue, not introduced by this task (plan says "keep fallback intact").
**Fix:** Out-of-scope for this task, but worth a follow-up issue.

## Verdict

APPROVE
All batches aligned with plan; cross-batch contracts consistent; two pre-existing / spec-prescribed nits, nothing blocking.