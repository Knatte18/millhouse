# Discussion fix — round 1

```yaml
slug: discussion-review-gap-batching
review: 20260511-104105-discussion-review-r1.md
date: 2026-05-11
```

## Fixed

- `[NOTE] append_phase regex-validation claim is incorrect` — discussion.md "Technical context → Status timeline phase names" said `_status.append_phase` validates phase names against a regex. Verified against `plugins/mill/scripts/_status.py` lines 220–235: `append_phase` only passes the phase string through `quote_scalar`. The `re.compile` / `re.match` calls in the file target YAML-key matching (`key:` rows in `update_field`) and the `phase:` row lookup, not phase-input validation. Rewrote the bullet to state the actual behaviour ("passed through `_yaml_writer.quote_scalar` only — no input validation").

## Pushed Back

(none)
