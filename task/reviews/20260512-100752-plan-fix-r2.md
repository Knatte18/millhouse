# Plan Fix — Round 2

```yaml
source_review: 20260512-100548-plan-review-r2.md
```

## Fixed

- `[NIT] Phase gate pseudocode contains invalid find task = … syntax` — Fixed in batch 02 Card 3 Change A: removed "find" prose prefix; now reads "`task = next((t for t in tasks if t.slug == slug), None)`".
- `[BLOCKING] _tasks_md.parse undocumented; function absent` (partial fix) — Added `signature: _tasks_md.parse(text: str) -> list[Task]` with Task attribute documentation in batch 02 Card 3 Change A. (Pushed back on "absent" claim — see below.)
- `[BLOCKING] _status.read_status undocumented; likely wrong function name` (partial fix) — Added `signature: _status.read_status(status_path: Path) -> dict` with return-shape documentation in batch 01 Card 1 Entry Step 6. (Pushed back on "replace with read_full" — see below.)

## Pushed Back

- `[BLOCKING] _status.read_status — replace with read_full and data["yaml"]["phase"]`: Finding is factually wrong. `_status.read_status` exists in `_status.py` and returns a flat dict with key `phase` at the top level: `{'phase': ..., 'task': ..., 'current_batch': ..., ...}`. Access via `data["phase"]` is correct. `read_full` returns `{"yaml": dict, "timeline": list}` — using it with `data["phase"]` would raise `KeyError`. Replacement rejected; signature line added instead.
- `[BLOCKING] _tasks_md.parse — function absent from all provided SKILL.md files`: Finding is factually wrong. `_tasks_md.parse(text: str) -> list[Task]` exists and is documented in the module docstring of `_tasks_md.py` as `parse(text) -> list[Task] — parse Home.md body text`. The Task dataclass has `.slug` and `.phase` attributes (confirmed from source). The function is not referenced in other SKILL.md files because those skills use only the write-path helpers (`set_phase`, `set_phase_at`); the read-path (`parse`) is the correct choice for the mill-merge fallback. Replacement with string-search rejected; signature line added instead.
