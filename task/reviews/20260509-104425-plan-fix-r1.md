# Plan-fix report — round 1

```yaml
slug: codeguide-fixes-1
round: 1
review_file: 20260509-104232-plan-review-r1.md
date: 2026-05-09
```

## Fixed

- **[BLOCKING] `## Resolution` callout breaks `codeguide-setup` first-time flow** (review file: `20260509-104232-plan-review-r1.md`). VERIFIED accurate: the verbatim "halt if `found == false`" is exactly the case `codeguide-setup` is designed to handle (first-time setup), and "Before doing anything else..." conflicts with Steps 1–2 (flag parsing + git-toplevel detection) which must run first. Fixed by splitting the Resolution callout into two variants in [task/plan/02-skill-edits.md](task/plan/02-skill-edits.md): the verbatim halt-on-false text remains for `codeguide-generate` / `codeguide-maintain` / `codeguide-update`, and `codeguide-setup` gets a tailored variant ("After parsing flags (Step 1) and detecting the git toplevel (Step 2), run resolve.py … Do NOT halt on `found == false`; that is the expected result for first-time setup."). The batch-local decision in `## Batch Scope` was updated to reflect the split — verbatim across three skills, custom variant for setup.
- **[NIT] `resolve_scope.py` exit-code spec references a `--json` flag that doesn't exist** (review file: `20260509-104232-plan-review-r1.md`). VERIFIED accurate: argparse spec says `nargs='*'` positional only, no flags; mentioning "malformed `--json` flag" was leftover copy from `resolve.py`. Fixed in [task/plan/01-scope-helper.md](task/plan/01-scope-helper.md) Card 1's exit-code bullet — now reads "Non-zero only when not in a git repo at all (the `git rev-parse --show-toplevel` probe fails)."
- **[NIT] `_make_repo` fixture: `set-head` call placement is ambiguous** (review file: `20260509-104232-plan-review-r1.md`). VERIFIED accurate: the helper signature didn't expose how scenarios 5–6 toggle `origin/HEAD` registration. Fixed in [task/plan/01-scope-helper.md](task/plan/01-scope-helper.md) Card 2 — `_make_repo` signature now includes `set_remote_head: bool = True`; scenarios 5 and 6 pass `set_remote_head=False` explicitly; the `origin/HEAD` simulation paragraph was rewritten to describe the parameter contract clearly.

## Pushed Back

(none)
