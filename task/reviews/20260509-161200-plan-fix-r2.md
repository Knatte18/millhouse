# Plan-fix r2

```yaml
round: 2
verdict: REQUEST_CHANGES
review_file: task/reviews/20260509-161059-plan-review-r2.md
date: 2026-05-09
```

## Fixed

- **[BLOCKING] Existing single-worktree tests break after auto-select removal** (review r2, holistic) — Card 4 of batch 2 amended to explicitly call out three pre-existing tests that need their `with patch(...)` chains extended with `patch("mill_vscode._vscode_processes.find_open_vscode_paths", return_value=set())` and `patch("mill_vscode.input", return_value="1", create=True)`. The three tests are: `hub_relative_path set in per-worktree config`, `hub_relative_path = "."`, and the regression test for hub-vs-worktree config precedence. Card 4's blanket "preserve every existing test exactly as-is" was relaxed to "do not modify any *other* pre-existing test" so the implementer knows the touch is bounded.

## Pushed Back

(None.)
