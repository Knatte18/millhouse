# Batch: gitignore-and-claudemd

```yaml
task: 44 (A) — Bug-fix batch 4
batch: gitignore-and-claudemd
number: 1
cards: 2
verify: null
depends-on: []
```

## Batch Scope

Two independent doc-only adds: a single line in `.gitignore` for `.claude/scheduled_tasks.lock` (#217), and a new "Template-mirror rule" paragraph in `CLAUDE.md` documenting that `plugins/mill/templates/wiki-config.yaml` must stay in sync with production wiki/config.yaml. Both are config-text edits with no runtime surface — `verify: null`. Bundled together because each is too small to justify its own batch.

## Cards

### Card 1: Add `.claude/scheduled_tasks.lock` to `.gitignore`

- **Context:** none
- **Edits:**
  - `.gitignore`
- **Creates:** none
- **Deletes:** none
- **Requirements:** Append the line `.claude/scheduled_tasks.lock` to `.gitignore`. Place it directly BEFORE the `# === mill-managed (regenerated by mill-setup) ===` block — that block's comment describes files mill-setup regenerates, and `.claude/scheduled_tasks.lock` is a Claude Code harness file, not mill-managed. Inserting inside that block would misrepresent ownership and mislead future maintainers. The mill-managed block currently lists `**/.scratch/`, `**/.portals/`, `**/.wiki/`, `**/.active/`; the new entry sits one line above the `# === mill-managed` header. After the edit, verify with `git status --porcelain` that the file (if present) no longer appears as untracked. Do not add wildcards (`.claude/*.lock`) — the scheduled-tasks lock is the only file under `.claude/` we currently ignore, and broadening would mask other Claude Code state.
- **Commit:** `chore(gitignore): ignore .claude/scheduled_tasks.lock`

### Card 2: Add "Template-mirror rule" to CLAUDE.md

- **Context:** none
- **Edits:**
  - `CLAUDE.md`
- **Creates:** none
- **Deletes:** none
- **Requirements:** Add a new bullet to `CLAUDE.md`'s `## Conventions worth carrying` section (the bulleted block beginning with "Plugin scripts reference `${CLAUDE_PLUGIN_ROOT}`..."). Insert the new bullet AT THE END of that bulleted list. Bullet text: `**Template `wiki-config.yaml` mirrors production `wiki/config.yaml`.** When changing a config key in production wiki/config.yaml, mirror the change in `plugins/mill/templates/wiki-config.yaml`. The template ships to new hubs via mill-setup; drift means new hubs are seeded with a stale schema. Production is the source of truth for valid schema; the template is the source of truth for documentation comments inside the file.` Use the same `- **Title.** Body.` style as surrounding bullets. Do not edit any other section of CLAUDE.md.
- **Commit:** `docs(claude-md): document template-mirror rule for wiki-config.yaml`

## Batch Tests

No runnable tests. `verify: null`. Sanity check: `git diff` shows exactly two files changed; the gitignore addition is on a single line and the CLAUDE.md addition is a single bullet.
