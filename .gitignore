# Node
**/node_modules/
**/ca-bundle.pem
**/npm-debug.log*
**/yarn-debug.log*
**/yarn-error.log*

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
env/

# VS Code
**/.vscode/

# Convenience hardlink to wiki/Home.md (created by mill-setup)
/tasks.md

# Mill local state (per-worktree, never tracked)
**/.millhouse/
**/.scratch/

# Per-worktree shortcut junction to wiki/active/<slug>/ (created by mill-spawn, or manually)
/.active

# Mill task worktrees (live at container level, never inside a clone)
**/worktrees/

# Secrets (at repo root of each clone)
**/.env
**/.env.local

# OS
.DS_Store
Thumbs.db
desktop.ini

# Misc local overrides
*.local.*
# Plugin templates ARE tracked (e.g. config.local.yaml — used by mill-setup)
!plugins/*/templates/*.local.*
nul

/tasks.md.bak
