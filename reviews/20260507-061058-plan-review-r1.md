# Review: 24 (A) — mill-misc-fixes — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-07
```

## Findings

### [NIT] Codeguide SKILL.md files in Card 8 Edits likely have no bash-block occurrences
**Location:** Batch 2, Card 8 (Edits list and overview "All Files Touched")
**Issue:** The four `plugins/codeguide/skills/*/SKILL.md` files contain `${CLAUDE_PLUGIN_ROOT}` only in inline backtick text or in untagged (tagless ` ``` `) fenced blocks — not in ` ```bash ` blocks. The plan note "each have at least one occurrence per the count grep done at planning time" is true (any-occurrence grep) but misleading (no bash-block occurrences). The fence-tracking implementer correctly makes no changes to these files, but listing them in Edits creates a predictable code-review question ("why is codeguide-generate/SKILL.md in Edits but unchanged?").
**Fix:** Remove the four codeguide SKILL.md files from Card 8's Edits and from the overview's "All Files Touched"; the post-card git grep verification already handles confirming no bash-block occurrences remain.

### [NIT] Card 6 test style inconsistent with the file's existing mock pattern
**Location:** Batch 2, Card 6 (Requirements)
**Issue:** The existing `test-llm-claude.py` mocks subprocess via direct assignment (`_subprocess_util_mod.run = _fake_fn`) with `try/finally` cleanup. Card 6 requires `mock.patch.object`, introducing a pattern inconsistency within the same file.
**Fix:** Allow direct assignment + try/finally as an equivalent alternative; it matches the style already in place in this file.

## Verdict

APPROVE
Plan correctly diagnoses and fixes bugs A–D; the two NITs are implementation notes, not blockers.