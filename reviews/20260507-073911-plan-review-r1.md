# Review: 27 (A) — Prune unused skills and scripts — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-07
```

## Findings

### [BLOCKING] Card 5 omits iter_target_scripts section of test-skill-writer.py
**Location:** Batch 1 / Card 5
**Issue:** Card 5 addresses only lines 155–173 (`write_skill_file` tests) in `test-skill-writer.py`. Lines 99–130 test `iter_target_scripts` by creating one `.py` stub per `SHORTCUT_SCRIPTS` entry and checking `if len(result) != 13`. After Card 3 removes 3 entries, `SHORTCUT_SCRIPTS` shrinks from 14→11; `iter_target_scripts` returns 10 (minus `mill-add`). The `if len(result) != 13` check will fail, and `expected_stems` still contains `"millpy-list"`, `"millpy-worktree"`, `"millpy-fetch-issues"` so the stem-comparison assert also fails.
**Fix:** Card 5 must additionally update `test-skill-writer.py` lines 99–130: remove `"millpy-list"`, `"millpy-worktree"`, `"millpy-fetch-issues"` from `expected_stems`; change `if len(result) != 13:` to `if len(result) != 10:`; update the comment `# Touch one file per SHORTCUT_SCRIPTS entry (14 total)` to `(11 total)`.

### [NIT] Card 8 uses source-tree path instead of CLAUDE_PLUGIN_ROOT
**Location:** Batch 1 / Card 8
**Issue:** Requirements say `python plugins/mill/scripts/millpy-skills-index.py`, which violates the CLAUDE.md rule: "never reference `plugins/mill/…` source-tree paths — use `${CLAUDE_PLUGIN_ROOT}`." The test-runner exception covers only `unit_tests/`; this is an operational invocation.
**Fix:** Change to `uv run --project "${CLAUDE_PLUGIN_ROOT}" "${CLAUDE_PLUGIN_ROOT}/scripts/millpy-skills-index.py"`.

### [NIT] Plan claims 7 render-body tests but lists and copies 8
**Location:** Plan overview, Card 1, Batch Tests section
**Issue:** All three locations say "7 `_render_body_with_comments` tests", but the explicit list — empty-comments, single, ordering, exact-10, 11-comments, 15-comments, deleted-author, empty-body — is 8 cases, matching the source file.
**Fix:** Correct all three occurrences to "8" to avoid implementer confusion about whether one case was intentionally omitted.

## Verdict

REQUEST_CHANGES — one blocking gap: Card 5 must also update the `iter_target_scripts` section of `test-skill-writer.py`.