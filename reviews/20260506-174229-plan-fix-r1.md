# Plan fixer report — round 1 — rename-hub-junctions

```yaml
date: 2026-05-06T17:42:29Z
round: 1
```

## Fixed

### From 03-teardown-migration-r1

- **[BLOCKING] Dangling junction detection misses Windows NTFS junctions** (Card 10 Req 3; Card 12 Req 3)
  Changed `(active_link.exists() or active_link.is_symlink()) and not active_link.is_dir()` to `os.path.lexists(str(active_link)) and not active_link.is_dir()`. Updated Card 12 Req 3 test to patch `os.path.lexists` instead of `Path.exists`.

- **[BLOCKING] `_config.load_config` called with config file path** (Card 11 Req 4b)
  Simplified to `cfg = _config.load_config(wiki_path, hub_root)` unconditionally (correct signature is `(wiki_path, worktree_root)`).

- **[BLOCKING] `_gitignore.upsert` does not exist** (Card 11 Req 4f)
  Reviewer suggested reverting to `upsert_split` — pushed back (that undoes batch 01's design). Root issue is a missing DAG edge: updated batch 03 `depends-on: [2]` → `depends-on: [1, 2]`. The `upsert` call is correct for post-batch-01 state. Also updated `00-overview.md` batch index entry for batch 03.

- **[NIT] Card 11 Req 4e: N commits for git mv** (Card 11 Req 4e)
  Updated requirement to stage all four `git mv` operations first, then issue a single commit with message `"migrate: move working state to task/ for {slug}"`.

### From 02-spawn-wiki-config-r1

- **[BLOCKING] wiki/config.yaml push leaves old junctions unstripped** (Card 3)
  Added Req 4 to Card 3: before committing the new config, strip `.millhouse/wiki`, `.others`, `.active` from every active task worktree (best-effort, using `os.path.lexists` to detect dangling junctions). Added `_junction.py` and `_spawn_core.py` to Card 3 Context.

- **[NIT] `_wiki.py` `_JUNCTION_DEFAULTS` not updated** (Card 3 new Req 5)
  Added `_wiki.py` to Card 3 Edits. Added requirement to update `_JUNCTION_DEFAULTS` from `{".millhouse/wiki": ..., ".active": ...}` to `{".wiki": "<WIKI_PATH>"}`. Updated All Files Touched in overview to include `_wiki.py`.

- **[NIT] Card 9 stub justification incorrect** (Card 9 Req 1)
  Corrected comment: "prevents AttributeError in `main()` when the stub map replaces `_spawn_core` with a bare `MagicMock` that lacks the attribute."

### From holistic-r1

- **[BLOCKING] Hardlink gitignore management silently dropped** (Card 14 Req 1)
  Restored `_wiki.read_hardlinks` in the mill-setup snippet. Updated call to `_gitignore.upsert(hub_gi, _gitignore.GLOB_ENTRIES + hardlink_names)` where `hardlink_names = [f"/{name}" for name in _wiki.read_hardlinks(wiki).keys()]`. Removed the plan note saying to remove the wiki import and hardlink_names logic.

- **[BLOCKING] `test_main_dry_run_prints_worktree_status_path` not updated** (Card 9 Req 2)
  Added requirement to Card 9 for test-millpy-spawn.py: update `test_main_dry_run_prints_worktree_status_path` to assert `Path("/fake/worktrees") / "my-task" / "task" / "status.md"`.

- **[NIT] Card 11 req 4e lacks commit message** — handled in the batch 03 fix above.

- **[NIT] Batch 04 missing depends-on [1]** (Card 14 NIT also from 04-skills-docs-r1)
  Updated batch 04 `depends-on: [2]` → `depends-on: [1, 2]` in both `04-skills-docs.md` and `00-overview.md`.

### From 04-skills-docs-r1

- **[BLOCKING] Card 13 Req 1: wrong path and missing portals update** (Card 13 Req 1)
  Replaced "remove `.others -> ../wts/millhouse`" (which doesn't exist) with correct instruction: change `<slug> -> ../wts/<slug>` to `<slug> -> ../wiki/active/<slug>/` in portals section.

- **[NIT] Card 17 Req 4: always-remove leaves rmtree guard unaddressed** (Card 17 Req 4)
  Updated requirement to preserve the existence guard (`if (wiki_path / "active" / slug).exists():`), noting it is a defensive guard against racing/partially-migrated states, not conditional-legacy logic.

## Pushed Back

None — all findings were factually accurate.
