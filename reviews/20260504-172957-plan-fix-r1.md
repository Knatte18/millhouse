# Plan-fix r1 — script-invocation-hygiene

```yaml
round: 1
date: 2026-05-04
findings_processed: 8
fixed: 8
pushed_back: 0
```

## Fixed

- `20260504-171801-plan-review-02-simple-fixes-r1.md` — "[BLOCKING] Card 5 omits resolve_hub_path from test stubs" — Added explicit `paths_mod.resolve_hub_path = MagicMock(return_value=Path("/fake/repo"))` requirement to Card 5 for both `_make_stub_map` and the `test_smoke_import` paths-stub block. Card 5 retitled to reflect expanded scope.
- `20260504-171801-plan-review-02-simple-fixes-r1.md` — "[BLOCKING] Card 6 omits resolve_hub_path patch in existing color tests" — Added requirement that every existing `with patch("mill_color.resolve_git_root", ...)` block must also patch `mill_color.resolve_hub_path` to the same `repo` value, plus a new test asserting cwd-vs-git-root divergence.
- `20260504-171614-plan-review-03-spawn-worktree-dst-r1.md` — "[BLOCKING] Card 12 imports missing `resolve_container_path`" — Added `resolve_container_path` and `resolve_main_worktree_root` to Card 12's import list.
- `20260504-172431-plan-review-r1.md` — "[BLOCKING] millpy-claim.py calls write_initial_status with wrong kwarg and wrong value" — Card 5 now also fixes the `wiki_path=wiki_path` kwarg at millpy-claim.py:310-313 to `worktree_path=resolve_hub_path()`. Card 5 test additions include `assert "wiki_path" not in status_call.kwargs` and `assert status_call.kwargs.get("worktree_path") == Path("/fake/repo")`. File is in scope (Card 5 modifies it); no destabilization.
- `20260504-171614-plan-review-03-spawn-worktree-dst-r1.md` — "[NIT] Card 11 Test 2 assertion (e) uses wrong call_args accessor" — Test 2 (e) reworded to assert `create_hub_links_mock.call_args.args[0] == worktree_path / "src/Models"` (positional accessor) with explicit warning that `kwargs.get("target_root")` would silently return `None`.
- `20260504-172431-plan-review-r1.md` — "[NIT] Cards 9 and 12 set REPO token to hub's dir name, not repo name" — Both cards now use `resolve_main_worktree_root(...).name` for the `REPO` token. `resolve_main_worktree_root` added to Card 9's `_paths` import list (Card 12 already gets it via the resolve_container_path fix).
- `20260504-172431-plan-review-r1.md` — "[NIT] Card 12 omits `resolve_container_path` from import additions" — Duplicate of the BLOCKING fix above; subsumed by the import-list update in Card 12.
- `20260504-172431-plan-review-r1.md` — "[NIT] Card 14 leaves mill-skills-index/SKILL.md modification ambiguous" — Verified: line 23 (operational invocation) already uses `${CLAUDE_PLUGIN_ROOT}`; line 20 is descriptive prose. Removed `mill-skills-index/SKILL.md` from Card 14 Modifies and from overview's All Files Touched. Updated Card 14 Requirements to confirm the file is already correct and needs no edit.

## Pushed Back

(none)
