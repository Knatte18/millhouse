# Plan fixer report — round 2 — rename-hub-junctions

```yaml
date: 2026-05-06T17:51:51Z
round: 2
```

## Fixed

### From holistic-r2 (20260506-175012)

- **[BLOCKING] Card 11: git mv fails without pre-creating task/ directory** (Card 11 Req 4e)
  Added `(wt_path / "task").mkdir(exist_ok=True)` step before the git mv loop. `git mv` does not create parent directories.

- **[NIT] Card 3 Req 4: junction-stripping code has no designated implementation file** (Card 3 Req 4)
  Removed the inline stripping code (which had no file to land in). Replaced with a deployment note: "Ensure Card 11 (`--step rename-junctions`) is run before the first mill-cleanup or mill-merge post-deploy." Card 11 already strips old junctions atomically as part of the migration step. Removed `_spawn_core.py` from Card 3's Context (it was added alongside the inline code).

- **[NIT] Card 11: resolve_container_path and resolve_wiki_path absent from imports** (Card 11 Req 1)
  Updated to also import `resolve_container_path, resolve_wiki_path` from `_paths`.

- **[NIT] Card 11 Step 4e: wiki active dir creation duplicates write_wiki_active_task_md** (Card 11 Req 4e)
  Replaced inline wiki active dir creation block with `_spawn_core.write_wiki_active_task_md(wiki_path, slug, title, ts)`. The helper is available at batch 03 execution time (batch 02 introduces it; batch 03 depends on [1, 2]).

## Pushed Back

None.
