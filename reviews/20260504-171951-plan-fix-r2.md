# Fixer Report: Plan Review Round 2

```yaml
round: 2
timestamp: 2026-05-04T17:19:51Z
verdict_received: APPROVE (blocking_count=0, 3 NITs)
```

## Fixed

**Holistic [NIT]: Card 3/Card 4 out-of-order execution within B01** (`20260504-171737-plan-review-r2.md`) — Swapped the bodies of B01 Card 3 and Card 4 so numbered order matches execution order. Card 3 is now the caller-migration card; Card 4 is now the public-symbol-removal card. Updated Card 2's cross-reference (`Card 3 does that after Card 4` → `Card 4 does that after Card 3`). Updated Card 4's grep-confirmation prose to reference Card 3 instead of Cards 4–8. The "⚠️ Execute this card AFTER Card 4" warning is removed because the natural sequential order is now correct.

**Holistic [NIT]: Card 23 leaves line-176 rewording vague** (`20260504-171737-plan-review-r2.md`) — Replaced the vague "may need rewording" prose with a prescriptive replacement: change the failure-mode table cell from `wiki.sync_pull fails` to `` `_wiki.sync_pull` raises `WikiPushError` `` (matching the exception vocabulary established in B01).

## Pushed Back

**Holistic [NIT]: Card 6 silent on `expected_wt` assertion change in test-millpy-claim.py** (`20260504-171737-plan-review-r2.md`) — Finding is factually inaccurate. The reviewer claims `test_main_happy_path_calls_spawn_core_helpers` "asserts `worktree_path == Path("/fake/worktrees") / "my-task"`". Verified against the actual test code at `plugins/mill/unit_tests/test-millpy-claim.py:254-260`: the test only asserts `slug`, `parent_branch`, and `branch` kwargs on the `write_initial_status` mock call. There is NO `expected_wt` variable in the file (`grep -n expected_wt test-millpy-claim.py` returns no matches), and there is no assertion on `worktree_path` or `wiki_path`. After Card 3 (was Card 4) changes the millpy-claim.py call from `wiki_path=wiki_path` to `worktree_path=git_root`, the existing test continues to pass unchanged because it does not check that kwarg. No Card 6 update is needed.
