# Review: 19 (A) — mill-go + scripts infra fixes — 05-tests

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: 05-tests
date: 2026-05-06
```

## Findings

### [NIT] Missing git-log failure path in guard tests
**Step:** Card 9 — cleanup guard scenarios
**Issue:** No test covers `_subprocess_util.run` returning `returncode != 0` from the git log call; the guard's error-handling path (fall back to `to_remove_done` or `to_report`) is untested.
**Fix:** Add a fifth scenario: mock returns `returncode=1, stdout=""` and assert slug lands in `to_remove_done` (or whichever safe-default Batch 03 implements).

### [NIT] Scenario 3 status.md construction underdefined
**Step:** Card 9 — scenario 3 (read_parent_branch returns None)
**Issue:** `_make_status_md()` always emits `parent: <value>`, so scenario 3 requires manual file construction; the plan says "(or without a parent key for scenario 3)" without showing how.
**Fix:** Add one sentence: "Construct `status.md` inline without a `parent:` key, following the `no_parent_status` pattern already in the file."

## Verdict

APPROVE — two NITs; no blockers. Both test classes are correctly structured, mock targets are correct, and all six `_forward_output` scenarios and four guard scenarios align with the post-Batch-02/03 implementations.