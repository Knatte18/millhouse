# Plan-fix report — round 3 (max-rounds escape, option B then C)

```yaml
slug: migrate-to-uv
round: 3
date: 2026-04-30
escape: max-rounds (user chose to fix and proceed without round 4)
```

## Fixed

- `20260430-183947-plan-review-04-skills-sweep-r3.md` — [BLOCKING] Card 15: `_junction.create` argument reversal not flagged — extended Card 15 requirement (2) to call out the reversed call at `mill-resume/SKILL.md:135`. Verified signature: `_junction.create(target: Path, link_path: Path)` — `target` is what the junction points AT; `link_path` is where the junction is created. The current call `junction_create(new_worktree / ".millhouse" / "wiki", wiki_clone_path)` has these reversed. Card 15 now explicitly instructs the implementer to rewrite as `junction_create(wiki_clone_path, new_worktree / ".millhouse" / "wiki")`.
- `20260430-183947-plan-review-04-skills-sweep-r3.md` — [NIT] Card 17: `sys.path.insert` in mill-skills-from-scripts helper block not addressed — added requirement (2) to Card 17 to remove the `sys.path.insert(0, '${CLAUDE_PLUGIN_ROOT}/scripts')` line from any "How to invoke the helper" Python block.

## Pushed Back

(none)

## Notes

User invoked the max-rounds escape after round 3 (`/mill-plan` rounds = 3) with this single BLOCKING remaining. The reviewer showed clean convergence: round 1 had 7 blocking, round 2 had 1, round 3 had 1. Both prior fix-rounds had empty Pushed Back. User chose to apply the fix and proceed to handoff (effectively option C with the fix applied), avoiding a 4th round given clear single-fix convergence and trivial remediation.

The plan is now `approved: true` in `plan/00-overview.md` frontmatter.
