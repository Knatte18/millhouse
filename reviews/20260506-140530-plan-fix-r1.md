# Plan Fix Report — round 1

```yaml
slug: plan-template-quality
round: 1
review_file: 20260506-140421-plan-review-r1.md
```

## Fixed

- [BLOCKING] "Integer deps serialized as strings in `_make_overview`" — added explicit instruction to card 6 to update `deps_yaml` so integer deps emit unquoted (`str(d) if isinstance(d, int) else f'"{d}"'`). Without this, the integer-dep code path in `_check_depends_on_unknown` (card 2) would never be exercised by the new test in card 6. (Verdict ACCURATE; no harm to fix.)
- [NIT] "`_check_reads_not_backtick_path` name inconsistent after rename" — added card 8 instruction to rename to `_check_ref_not_backtick_path` and update its single call site in `run()`. (Verdict ACCURATE; small, mechanical, consistent with the rest of the rename.)
- [NIT] "Explore targets criterion retains old field name" — added explicit instruction to card 9 to replace `Reads:` with `Context:` in the "Explore targets" bullet of both review-plan-batch.md and review-plan-holistic.md. (Verdict ACCURATE; the catch-all "same field-name replacements" is too easy to miss.)

## Pushed Back

(none)
