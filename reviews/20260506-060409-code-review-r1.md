# Review: 3 (A) — codeguide improvements: sibling placement + --branch flag — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/ + source
date: 2026-05-06
```

## Findings

No findings. All four cards are fully realised and the implementation is faithful to the plan.

Brief confirmation of each card:

- **Card 1** — `codeguide-generate` Step 1 now calls `resolve.py --json`, parses `{mode, cg_root, sibling_anchor, found}`, binds `git_toplevel`, and preserves the stop-on-error / `found == false` gate. Matches the plan requirement exactly.

- **Card 2** — Step 9 opens with the placement rule branching on `mode`, uses `<placement_root>` throughout all downstream sub-bullets, cites `_sibling_walk` once in passing, includes the fenced multi-project worked example with the explicit `NOT at:` anti-pattern, and leaves all other Step 9 sub-bullets and all surrounding step numbers unchanged.

- **Card 3** — `codeguide-setup` front-matter `argument-hint` matches `"[--sibling [<url>]] [--branch <name>] [.cs .py .ts]"`. Step 1 rewrites to `--sibling`/`--sibling <url>` bullet + `--branch <name>` bullet with the required error message for the no-URL case. Zero `--from-url` references remain anywhere in the file.

- **Card 4** — Step 4 sibling-mode block replaces the old single sentence with the five-way decision tree: no-URL → `git init`; URL no branch → `git clone`; URL + branch → `git ls-remote --heads` dispatch (clone existing, orphan init+remote+checkout, network-error stop); existing-anchor → use as-is with `--branch` ignored. `<rel-path>` computation, inline-mode bullet, and `.codeguide-root` override are all preserved verbatim.

`resolve.py` is untouched (shared-decision: SKILL.md prose only). `--from-url` is fully purged (shared-decision: no backwards compat). `resolve.py --json` output contains `sibling_anchor` and `found`, confirming the JSON contract the SKILL.md depends on is already implemented.

## Verdict

APPROVE — implementation realises every plan requirement cleanly with no deviations.