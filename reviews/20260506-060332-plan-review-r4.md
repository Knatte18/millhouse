# Review: 1 — Implementer dispatch-CLI + Agent-resume fix (conflicts with 8) — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-06
```

## Findings

### [BLOCKING] Card 3 module-level import list incomplete for test patching
**Location:** Batch 01 Card 3 / Batch 02 Card 4
**Issue:** Card 3 specifies module-level imports only for `json`, `sys`, `argparse`, `os`, `subprocess`, `uuid`, `pathlib.Path`. Card 4's setUp applies `patch.object(millpy_implement._paths, ...)`, `patch.object(millpy_implement._status, ...)`, `patch.object(millpy_implement._active, ...)`, `patch.object(millpy_implement._review_common, ...)`, `patch.object(millpy_implement._implementer_sonnet, ...)` — all requiring those names to be module-level attributes of `millpy_implement` at setUp time (before `main()` is ever called). An implementer reading Card 3's explicit list and following "most imports inside main" for everything else will produce tests that blow up with `AttributeError: module 'millpy_implement' has no attribute '_paths'` before any assertion runs.
**Fix:** Add `import _paths`, `import _status`, `import _active`, `import _review_common`, `import _implementer_sonnet`, `import _render`, `import _plan_dag`, `import _timestamp`, `import _llm_claude` to the Card 3 module-level imports requirement block, with the same rationale already given for `subprocess`/`uuid` — test mocks require module-level bindings on these names.

### [NIT] Card 5 edit numbers appear out of order in the text
**Location:** Batch 02 Card 5
**Issue:** The edits are labelled 1, 2, 3, 4, 6, 5 in the document (Edit 6 text precedes Edit 5 text), which will read as confusing even though the two affected sections are independent.
**Fix:** Renumber or reorder so the numerals appear in ascending order in the text.

## Verdict

REQUEST_CHANGES — one blocking spec gap that will silently break all tests if the implementer follows the stated import pattern.