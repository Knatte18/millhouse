# Plan fixer report — round 3 — rename-hub-junctions

```yaml
date: 2026-05-06T18:09:33Z
round: 3
```

## Fixed

### From 03-teardown-migration-r2

- **[BLOCKING] `build_plan` read_parent_branch also needs fallback** (Card 10 Req 1)
  Added the `_task_status if _task_status.exists() else _legacy_status` fallback pattern to the `read_parent_branch` call in `build_plan`'s `if phase == "done":` block, using the same variables already computed for the `_read_phase` call.

- **[BLOCKING] `_timestamp` used but not imported in Card 11** (Card 11 Req 1)
  Added `_timestamp` to the imports list in Req 1.

- **[NIT] Redundant pre-check before `_junction.remove`** (Card 11 Req 4e)
  Removed the `if path.exists() or path.is_symlink():` guard. `_junction.remove` handles absent junctions internally; calling it directly is correct and avoids the Windows lexists issue.

- **[NIT] `import os` inline in apply_plan** (Card 10 Req 3, from holistic)
  Updated Req 3 instruction to add `import os` at the module-level imports section, not inline.

### From 02-spawn-wiki-config-r2

- **[BLOCKING] Card 6 passes `git_root` not `resolve_hub_path()`** (Card 6 Req 4)
  Changed to `resolve_hub_path()`. Added explicit note that `git_root` must NOT be used (subfolder-install regression risk).

- **[NIT] Card 9 `test_main_happy_path` assertion can't catch subfolder regression** (Card 9 Req 2)
  Updated instruction to note that if both `resolve_hub_path` and `resolve_git_root` mocks return the same value, add a distinct hub path to `resolve_hub_path`'s mock so the test catches the `git_root` vs `resolve_hub_path()` distinction.

- **[NIT] Card 3 Context omits `_paths.py`** (Card 3 Context)
  Added `plugins/mill/scripts/_paths.py` to Card 3 Context.

### From 04-skills-docs-r2

- **[BLOCKING] Card 14 req 3 scope too narrow** (Card 14 Req 3)
  Changed "any junction diagram" to "any reference" and added explicit note to update the "When to invoke" prose bullet `".millhouse/wiki junction"` → `".wiki junction"`.

- **[NIT] Card 14 req 5 scope misses description paragraph** (Card 14 Req 5)
  Broadened to "any reference" and added explicit note about the description paragraph ending in "writes the initial `status.md`".

- **[NIT] Card 17 req 6 omits discussion.md** (Card 17 Req 6)
  Added `discussion.md` → `task/discussion.md` to the Board discipline update list.

### From holistic-r2 (20260506-180632)

- **[BLOCKING] `_timestamp` import** — already handled above.

- **[NIT] Card 1: `**/plugins/*/uv.lock` removal unjustified** (Card 1 Req 1 in batch 01)
  Added justification: `**/wts/` and `**/portals/` were hub-layout artefacts; `**/plugins/*/uv.lock` is dead coverage (tracked files aren't affected by gitignore). Added instruction to verify via `git ls-files`.

- **[NIT] Card 11: Token dict underspecified** (Card 11 Req 4e)
  Added full token dict for both task-worktree and hub-worktree calls. Added `millpy-spawn.py` to Card 11 Context as reference for `_build_tokens` pattern.

## Pushed Back

None.
