# Review: 23 (A) — mill infra bugfix-batch — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-06
```

## Findings

### [NIT] Card 4: "sequential-letter naming" misdescribes test-status.py
**Location:** Batch 01-bugfix-batch / Card 4
**Issue:** Plan says "continuing from the last letter in use" but `test-status.py` uses no letter designations — only `PASS: <message>` print statements; letter naming belongs to `test-millpy-bg.py`.
**Fix:** Implementer should use the `PASS: ...` style already shown in the requirements text; the specified print strings are correct as written, so the discrepancy is cosmetic only.

### [NIT] Card 5: `_call` helper not mentioned; existing tests will TypeError
**Location:** Batch 01-bugfix-batch / Card 5
**Issue:** The plan says "update all existing `test_fo_*` test invocations that call `_forward_output` directly" but every existing call goes through `self._call(output)`, not directly — zero direct calls exist. The `_call` helper itself is never mentioned, so a literal reading leaves it unchanged, causing a `TypeError` on all six existing `TestForwardOutput` tests after the signature change. Additionally, the mock return value for subprocess.run in the existing tests is unspecified; using `returncode=0` with any SHA string would break `test_fo_1`–`test_fo_4`, which assert `commit_sha` round-trips exactly from the fixture JSON.
**Fix:** Requirements should say "update `_call` to pass `Path('/fake')` as the second argument and patch `millpy_implement.subprocess.run` with `returncode=1` so existing assertions on commit_sha remain valid."

## Verdict

APPROVE — both findings are self-correcting during the verify run; plan is otherwise tight and complete.