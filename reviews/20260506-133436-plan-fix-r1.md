# Plan Fix Report — round 1

Source review: `reviews/20260506-133134-plan-review-01-wiki-enhance-fixes-r1.md`

## Fixed

- **[BLOCKING] Wrong function name `render` vs `render_sidebar` in `_sidebar.py`** — updated Card 2 Requirements to reference `render_sidebar()` and Card 3 Requirements to import `_sidebar.render_sidebar` and call `render_sidebar()`.
- **[NIT] Pipeline comment lines for removed keys not mentioned** — added instruction in Card 1 Requirements to also remove the `# builder —` and `# implementer —` inline comment lines from the `pipeline:` block.

## Pushed Back

_(none)_
