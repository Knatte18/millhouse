# Plan fixer report — round 4 — rename-hub-junctions

```yaml
date: 2026-05-07T05:36:10Z
round: 4
```

## Fixed

### From 03-teardown-migration-r3 (20260506-181510)

- **[BLOCKING] `_timestamp` missing from Card 11 Context** (Card 11 Context)
  Added `plugins/mill/scripts/_timestamp.py` to Card 11 Context list.

- **[NIT] Token dict incomplete vs `_build_tokens` pattern** (Card 11 Req 4e and 4f)
  Extended both task-worktree and hub-worktree token dicts to include `CWD_PATH` and `REPO`, matching the full `_build_tokens` pattern in `millpy-spawn.py`.

- **[NIT] No test for `_apply_inplace_record` status path fallback** (Card 12 Req 4)
  Added new test `test_apply_inplace_record_reads_task_status_md` to Card 12, covering the fallback path added in Card 10 Req 2. Renumbered the worktree.py test from 4 → 5.

- **[NIT] `_gitignore.upsert` not in current API** — already covered: Card 11 Req 4f calls `_gitignore.upsert` which is introduced by batch 01 (Card 1). Batch 03 `depends-on: [1, 2]` enforces the ordering; the contract is documented in batch 01's Card 1.

### From 02-spawn-wiki-config-r3 (20260506-181517)

- **[BLOCKING] `write_wiki_active_task_md` ships with zero test coverage** (Card 8 Req 6)
  Added new test requirement (Card 8 Req 6): `test_write_wiki_active_task_md` using the existing `_make_wiki` fixture. Asserts directory creation, task.md content, and wiki commit.

- **[NIT] Card 9 stub in `test_smoke_import` is unnecessary** (Card 9 Req 1, 2)
  Removed the stub addition. Replaced with documentation explaining why no stub is needed (test does not call `main()`; `_spawn_core` is loaded as `types.ModuleType` not `MagicMock`).

## Pushed Back

None.
