# Plan-fix report — round 3

```yaml
round: 3
date: 2026-05-05
review_files:
  - 20260505-131515-plan-review-02-tests-and-skill-r3.md
  - 20260505-132036-plan-review-r3.md
```

## Fixed

- `20260505-131515-plan-review-02-tests-and-skill-r3.md` — "[BLOCKING] Tests 5 and 5b omit --round from --resume invocation": Test 5 and 5b now include `"--round", "1"` in their `main()` argv. The plan spec already defines `--round` as optional with default 1 (Card 3), so this finding's premise that `--round` would be required is hypothetical; however, adding the flag is a harmless robustness improvement that produces clearer test failure messages if the implementer accidentally makes `--round` required.
- `20260505-131515-plan-review-02-tests-and-skill-r3.md` — "[NIT] `### 2. Parse implementer report` not updated to match CLI retry": Card 5 expanded with Edit 6 — replaces the transient retry bullet in `### 2.` with the CLI-based re-invocation pattern.
- `20260505-132036-plan-review-r3.md` — "[BLOCKING] Card 4 setUp provides no reference to subprocess mock for Test 4 inspection": Card 4 setUp spec now requires saving every `patcher.start()` return value to a `self.mock_*` attribute. Each patch entry is annotated with its `self.mock_*` name. Test 4 references `self.mock_subprocess_run.call_args_list` directly.
- `20260505-132036-plan-review-r3.md` — "[NIT] Card 3 silent about module-level uuid/subprocess imports": Card 3 Requirements preamble now explicitly states that `json`, `sys`, `argparse`, `os`, `subprocess`, `uuid`, and `pathlib.Path` MUST be imported at module level so unit tests can patch them.
- `20260505-132036-plan-review-r3.md` — "[NIT] Card 4 subprocess mock qualifier implies conditional side_effect": Card 4 setUp now reads "All `subprocess.run` calls in the CLI are git commands; use `return_value`" — no conditional phrasing.
- `20260505-132036-plan-review-r3.md` — "[NIT] Card 1 fix template leaves session_id retrieval ambiguous on resume": Card 1 Requirements now requires the fix template to include an explicit note that the implementer's `session_id` is the UUID from the original spawn (retained in session context), and that re-reading `--session-id` from argv on `--resume` is wrong (it is absent on `--resume`).

## Pushed Back

(none — every finding was a verified gap or a harmless robustness improvement; all were fixed.)
