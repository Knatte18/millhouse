# Plan-fix report — round 3

```yaml
slug: review-subsystem-fixes
round: 3
date: 2026-05-04T17:45:25Z
```

## Fixed

- **[BLOCKING] B01 Card 7 breaks B01's own verify** (review file `20260504-174339-plan-review-r1.md`) — Card 7's Reads / Modifies lists now include `test-review-plan-flow.py` and `test-review-code-flow.py`; Requirements now state that the existing exact-equality `retry_kwargs` assertions (test-review-plan-flow.py tests 6/7 + test-review-code-flow.py test 5) must be updated within Card 7 to expect `"timeout": None`, so B01's verify stays green.
- **[BLOCKING] B07 Card 26 Modifies list excludes the test file it must update** (same review file) — Card 26's Reads and Modifies lists now both include `plugins/mill/unit_tests/test-plan-validate.py`. The Requirements describing the fixture-helper updates were already in place from round 2; this completes the file-list ↔ requirement match.
- **[NIT] B06 Card 23 — double-newline risk in `print_error`** (same review file) — Card 23 Requirements now specify `print(f"ERROR: {exc}", file=sys.stderr)` (no `\n` in the format string) so `print` adds the single trailing newline that Card 25's tests assert. Same correction applied to the hint line.
- **[NIT] B04 Card 18 test (d) — per-batch prompt index non-deterministic with multiple batches** (same review file) — Card 18 test (d) now requires a single-batch fixture so `captured_prompts()[0]` is the per-batch call and `captured_prompts()[1]` is the holistic call deterministically.
- **[NIT] B02 Card 9 — `_GRACE_SECONDS` import not stated** (same review file) — Card 9 Requirements now state `from _subprocess_util import run, _GRACE_SECONDS` so the constant is in scope for test (a)'s wall-time bound.

## Pushed Back

(none)

## Non-progress check

Skipped: round 2's `## Pushed Back` was empty AND round 3's `## Pushed Back` is empty. Convergence, not non-progress.

## Notes

The two BLOCKING findings are both about the difference between "the prose says we touch this file" and "the file is on the Modifies list." Round 2 introduced both gaps when it added test-file updates to the Requirements text without expanding the Reads/Modifies lists. Round 3 closes them in-place.

After this round the loop has consumed `max_review_rounds=3`. Per SKILL.md step 6, present the user with the max-rounds escape options.
