# Plan-fix report — round 1

```yaml
slug: mill-setup-wiki-url
round: 1
date: 2026-05-06
```

## Fixed

- **`reviews/20260506-060858-plan-review-02-skill-and-template-r1.md`** — `[BLOCKING] Phase 3.2 writes to .millhouse/ before it exists`. The factual claim "fails with FileNotFoundError on every fresh install" is inaccurate (Card 3 already requires `cfg_path.parent.mkdir(parents=True, exist_ok=True)` in the helper), but the underlying clarity concern is fair. Added an explicit cross-reference paragraph in Card 5's Phase 3.2 prose stating that `.millhouse/` is absent on fresh install at this point and that the helper from Card 3 handles parent creation, so no separate mkdir is needed in the SKILL.md prose.
- **`reviews/20260506-060858-plan-review-02-skill-and-template-r1.md`** — `[NIT] argument-hint position instruction contradicts reference file`. Card 5 frontmatter instructions changed from "between `name:` and `description:`" to "after `description:`" to match codeguide-setup's actual ordering.
- **`reviews/20260506-060858-plan-review-02-skill-and-template-r1.md`** — `[NIT] WikiPushError from pull path described too narrowly`. Card 5 Phase 3 documentation updated: `WikiPushError` from the pull path now described as "any `git pull --ff-only` failure — network failure, credentials, non-fast-forward / local divergence" rather than the narrower "unmerged local commits" framing.

## Pushed Back

(none)
