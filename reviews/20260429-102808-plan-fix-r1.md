# Plan-fix report — round 1

```yaml
round: 1
date: 2026-04-29
fixed_count: 11
pushed_back_count: 0
new_cards_added: 1
batch_02_review: missing (timeout in r1; will re-run in r2)
```

## Fixed

### Holistic findings

- **[BLOCKING] "Batch 01 verify gate fails: hub-form wiki-path tests in test-paths.py not updated"** — `20260429-101723-plan-review-r1.md`. Fixed in Card 4. Card 4's requirements now explicitly migrate every `tmp_path / "hub"` fixture in `test-paths.py` (covering both `resolve_wiki_path` and `resolve_worktrees_dir` tests, including the walk-up child-worktree variants) to container-form (`tmp_path / "wts" / "millhouse"`). Added a new test asserting the OLD hub-form fixture now resolves to prefix-form, documenting the intentional regression from discussion.md `## Decisions → hub-form-detection`.

- **[BLOCKING] "`upsert` removed in batch 01 but mill-setup SKILL.md invokes it until batch 04"** — `20260429-101723-plan-review-r1.md`. Fixed in Cards 5 and 19. Card 5 no longer removes `upsert` or the legacy single-arg `render_block` overload — both stay alongside the new `upsert_split` and the new two-arg `render_block`. Card 19 (mill-setup SKILL.md update, formerly Card 18 before renumbering) now atomically rewrites the SKILL.md callsite from `upsert(...)` to `upsert_split(...)` AND removes the legacy `upsert` + single-arg `render_block` from `_gitignore.py`. The `Modifies:` list of Card 19 was extended to include `_gitignore.py` and `test-gitignore-phase.py`. This eliminates the regression window where `/mill-setup` would crash with `AttributeError`.

- **[BLOCKING] "Card 12 tests invoke `_paths.resolve_main_worktree_root` via subprocess — no mock planned"** — `20260429-101723-plan-review-r1.md`. Fixed in Cards 12 and 13. Both cards' test specifications now require a real-git-repo fixture (matching the `_make_git_repo` pattern already used in `test-spawn-core.py`) — initialise git in the tempdir, populate `<container>/wts/<slug>/.millhouse/active.slug.md` via `_active.write` inside the fixture, and use `os.chdir` (with try/finally to restore) so `Path.cwd()` resolves inside the fixture. The reviewer suggested `unittest.mock.patch` as an alternative; we chose the real-git-repo fixture instead because it exercises the same code path the live CLI uses, but the BLOCKING gap (tests would error on the subprocess call) is fully closed by either approach.

- **[NIT] "Migration script wiki resolution incorrect when run from new-code branch against old layout"** — `20260429-101723-plan-review-r1.md`. Fixed in Card 23 (Card 22 before renumbering) — same finding as Batch 05's BLOCKING #1 below; one fix covers both.

### Batch 03 (state-on-worktree) findings

- **[BLOCKING] "`resolve_path` test fixture cannot work without git worktree setup"** — `20260429-100653-plan-review-03-state-on-worktree-r1.md`. Same fix as the holistic Card 12 blocker: real-git-repo fixture + `os.chdir` in Cards 12 and 13. We chose the fixture-based approach over the reviewer's `_cwd` parameter alternative because it preserves the live CLI's calling shape (no test-only escape hatch in the production signature).

- **[NIT] "Smoke-check in batch scope implies false end-to-end correctness"** — `20260429-100653-plan-review-03-state-on-worktree-r1.md`. Fixed by addressing the underlying gap rather than the wording: added new Card 15 (`mill-start` + `mill-plan` SKILL.md prose: state files to worktree on task branch), so after batch 03 lands, /mill-start writes `discussion.md` to the worktree directly and the smoke-check actually does work end-to-end without depending on a later batch. The Batch Scope and Batch Tests sections of `03-state-on-worktree.md` were rewritten to reflect this.

- **[NIT] "Backward-compat description overstates protection"** — `20260429-100653-plan-review-03-state-on-worktree-r1.md`. Fixed in Card 12. Reworded the strip rationale to "prevents a path-segment like `<SLUG>` from appearing literally in the joined result if a stale config still has the old templates; paths still fail at file-open time until Card 14 deploys the new `paths:` block, so this is purely a 'don't crash on the token' guard, not a backward-compat correctness bridge."

- **[NIT] "Dry-run status path not explicitly called out"** — `20260429-100653-plan-review-03-state-on-worktree-r1.md`. Fixed in Card 11. Added explicit requirement: "update `millpy-spawn.py`'s `--dry-run` branch (currently prints `wiki_path / 'active' / slug / 'status.md'`): change the printed path to `worktree_path / 'status.md'` so dry-run output matches what the live run would do." Tests in `test-millpy-spawn.py` now assert dry-run prints the worktree-rooted path.

- **[NIT] "Git subprocess error handling omitted from new commit step"** — `20260429-100653-plan-review-03-state-on-worktree-r1.md`. Fixed in Card 11. Added: "Both subprocess calls go through `_subprocess_util.run`; the function checks `returncode != 0` after each and raises `RuntimeError` with `result.stderr.strip()` included, mirroring the existing discipline in `_spawn_core.capture_parent_branch`." Tests cover a forced-failure case.

### Batch 04 (consumers-and-skills) findings

- **[BLOCKING] "Card 17: `active_dir` semantic split not specified — will delete worktrees"** — `20260429-100300-plan-review-04-consumers-and-skills-r1.md`. Fixed in Card 18 (Card 17 before renumbering). Restructured `SlugRecord` to carry separate `worktree_path` (worktree root, used for status reads and `_read_phase`) and `wiki_active_dir` (legacy wiki dir, optional, used only for conditional rmtree). `apply_plan`'s `shutil.rmtree` now targets `wiki_active_dir` only when present and is_dir; `worktree_path` is NEVER rmtree'd (the worktree directory is removed by `_apply_worktree_record._worktree.remove`). `_apply_inplace_record`'s `parent_branch` read now sources from `worktree_path / "status.md"`. The Card now spells out all six structural changes (a)–(g) and the corresponding test fixture variants ("fresh layout, no wiki/active/<slug>/" vs "legacy clone with leftover wiki/active/<slug>/").

- **[NIT] "Card 15: `_worktree.list_worktrees` / `worktree_map` not addressed"** — `20260429-100300-plan-review-04-consumers-and-skills-r1.md`. Fixed in Card 16 (Card 15 before renumbering). Added requirement: "REMOVE `_worktree.list_worktrees` calls in `millpy-status.py` and `millpy-inspect.py`. The `discover_active_worktrees` scan already returns `(path, slug, title)` triples; populate the WORKTREE column / map directly from that result. Drop the now-unused `import _worktree`."

- **[NIT] "Card 16: `load_config` call site must use chosen-worktree path"** — `20260429-100300-plan-review-04-consumers-and-skills-r1.md`. Fixed in Card 17 (Card 16 before renumbering). Added explicit requirement: "after the user picks a worktree, call `_load_config(wiki_path, selected_path)` (passing the chosen worktree root, NOT `git_root`/the hub) so the per-worktree `.millhouse/config.local.yaml` is read." Added a regression test where the HUB's config differs from the SELECTED worktree's, asserting the SELECTED value wins.

### Batch 05 (migration-and-docs) findings

- **[BLOCKING] "Wiki path resolution claim is factually wrong"** — `20260429-100114-plan-review-05-migration-and-docs-r1.md`. Fixed in Card 23 (Card 22 before renumbering). The pre-flight wiki path now derives directly as `main_root.parent / "wiki"` — the OLD layout's structural invariant — without going through `_paths.resolve_wiki_path` (which is calibrated for the new layout and would silently fall through to prefix-form, returning a non-existent path and causing the in-flight check to pass vacuously). Added a clear halt with error message if `<wiki_path>/active/` does not exist.

- **[NIT] "`portals/` mkdir idempotency not annotated"** — `20260429-100114-plan-review-05-migration-and-docs-r1.md`. Fixed in Card 23 Step 5. Added "(idempotent: skip if exists, matching Step 1's discipline so a re-run after partial failure does not crash)" to the `mkdir <container>/portals` requirement.

## New cards added

- **Card 15** (Batch 03): `mill-start` + `mill-plan` SKILL.md prose — write state to worktree on task branch. Closes the gap implicitly noted by Batch 03 NIT #2: without this card, mill-start would still write `discussion.md` to the wiki after batch 03 lands, leaving `_review_common.resolve_path` looking at a non-existent worktree path. Combining mill-start and mill-plan SKILL.md updates into a single card avoids two near-identical prose cards. Cards 15–22 in batches 03–04 and cards 23–24 in batch 05 were renumbered to keep card numbering globally unique.

## Pushed Back

(none — every finding fixed)

## Notes

- **Batch 02 (create-hub-links) per-batch review timed out at round 1** (600s limit; Claude CLI exit=timeout). Holistic review covered the batch implicitly. Round 2 will re-run the per-batch review and surface any findings — applying the same protocol there.
- The 11 fixed findings span 4 batches and the holistic review; 1 NIT was a duplicate (#9 and #14 are the same wiki-resolution issue, fixed once in Card 23).
- The new Card 15 was not requested by any reviewer — it closes a scope gap that surfaced through NIT-level review feedback. Adding it keeps the task delivering on discussion.md `## Scope → In:` ("Working state moves out of wiki. status.md, discussion.md, plan/, reviews/ live at the root of the task worktree on the task branch").
