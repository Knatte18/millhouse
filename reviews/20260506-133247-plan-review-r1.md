# Review: 9 (B) — Wiki-enhance: small wiki cleanups — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-06
```

## Findings

### [NIT] Card 3 names wrong function for test-sidebar.py
**Location:** batch wiki-enhance-fixes / Card 3
**Issue:** Requirements say "imports `_sidebar.render` directly" and "calls `render()`", but `_sidebar.py`'s public API exposes `render_sidebar`, not `render`; importing the name as written causes an `ImportError`.
**Fix:** Change the requirement to `from _sidebar import render_sidebar` and call `render_sidebar(tasks)`.

## Verdict

APPROVE
One wrong function name in Card 3's instructions; source file is in Reads so implementer will catch it.