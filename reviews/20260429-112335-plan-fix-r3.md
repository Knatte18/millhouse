# Plan-fix report — round 3

```yaml
round: 3
date: 2026-04-29
fixed_count: 10
pushed_back_count: 1
batches_approved_in_r3: [01-foundation, 04-consumers-and-skills]
holistic_review: incomplete (network drop killed the holistic subprocess; per-batch coverage stands)
```

## Fixed

### Batch 02 (create-hub-links) findings — `20260429-105620-plan-review-02-create-hub-links-r2.md`

- **[BLOCKING] "Card 7 note contradicts Card 9 on `container_path` expression"** — Card 7's "Note" paragraph still pointed to `_paths.resolve_main_worktree_root(git_root).parent` (the wrong-by-one expression) while Card 9 explicitly forbids it. Rewrote Card 7's Note to require `_paths.resolve_container_path(git_root)` from Card 4. Both cards now prescribe the same derivation; the only correct one.

- **[NIT] "Card 6 hardlink creation omits 'link does not exist' path"** — Card 6's hardlink logic now explicitly handles the first-run case: "If `link_path` does not exist, skip the inode check and proceed directly to `Path.hardlink_to`." Prevents `FileNotFoundError` from `Path.stat()` when the hardlink hasn't been created yet.

- **[NIT] "Card 7 docstring guidance is self-contradictory"** — Changed the docstring instruction from "drop the line" (which would remove a still-correct comment) to "REWRITE the line" with the new rationale ("mill-spawn does NOT call this helper directly — it routes through `_setup.create_hub_links` which iterates the full `junctions:` block including `.active`"). Keeps the operator-facing fact while updating its reasoning.

- **[NIT] "Test mock maps don't account for new imports added by Cards 8–9"** — Card 8 now states that the `_run_main_with_mocks` stub map MUST add a `_setup` entry (with `create_hub_links` mocked to return `{"junctions": [], "hardlinks": []}` by default) so the import-time mock satisfies the new dependency, AND that the `_paths` mock must expose `resolve_container_path` returning a concrete `Path` from the test fixture. Card 9 mirrors this for `test-millpy-claim.py`'s `_make_stub_map`, with explicit guidance that without a concrete `Path` return value the call-order assertions would compare against `MagicMock` objects and fail spuriously.

### Batch 03 (state-on-worktree) findings — `20260429-105842-plan-review-03-state-on-worktree-r3.md`

- **[BLOCKING] "Card 15 omits Phase: Active path update"** — Card 15's requirements now explicitly enumerate every mill-start phase and every mill-plan phase that needs updating: mill-start Phase: Active (verifies status.md → updated to verify `<worktree_root>/status.md`), Phase: Discussion File, Phase: Discussion Review, Phase: Handoff; mill-plan Phase: Plan, Phase: Plan Review, Phase: Handoff. The catch-all "drop wiki/active/<slug>/ references" instruction stays as a safety net but every phase is now named individually so an implementer working through the enumerated list can't miss Phase: Active.

- **[NIT] "Card 11 forced-failure test lacks corruption technique"** — Card 11's test requirements now name a concrete technique: "delete `<repo>/.git/index` (or set the file to read-only on POSIX) just before calling `write_initial_status`, which produces a real non-zero exit from the `git add` subprocess." Explicitly forbids patching `_subprocess_util.run` for this test, so the real subprocess error path is exercised.

- **[NIT] "Module docstring not in Card 11 scope"** — Card 11 now includes the `_spawn_core.py` module-level docstring API summary in its scope: rename the documented parameter from `wiki_path` to `worktree_path` and update the summary line from "Render + write `active/<slug>/status.md`; lock + commit+push" to "Render + write `status.md` at worktree root; stage + commit on task branch."

### Batch 05 (migration-and-docs) findings — `20260429-105609-plan-review-05-migration-and-docs-r3.md`

- **[BLOCKING] "`_junction.create` behavior mischaracterized in Step 5"** — Card 23 Step 5 now spells out the explicit pre-check: `_junction.create` raises `ValueError` for ANY existing `link_path` (per `_junction.py`'s `if link_path.exists() or link_path.is_symlink()` guard), not just mismatched-target ones. The migration script must check each `portals/<dirname>` entry first via `os.path.lexists` + `Path.resolve()`: if missing → create; if correct target → skip silently with log; if wrong target → halt with a clear error message naming the existing target and instructing the operator to remove the wrong junction manually before re-running. Matches the pattern Card 9 already uses for portal handling in mill-claim.

- **[BLOCKING] "Card 24 references non-existent discussion section"** — Card 24 no longer refers to `discussion.md ## Target layout` (which doesn't exist; discussion.md has `## Scope`, `## Decisions`, etc., but no `## Target layout`). Replaced the reference with an INLINE concrete layout diagram in Card 24's requirements — the diagram is derived from `wiki/proposal-container-restructure.md ## Target layout` (where it does exist) and the new junction shape from Card 10 / Card 7's spec. Implementer now has an authoritative diagram source in the card itself, no cross-file lookup required.

- **[NIT] "Reads list contains modules with no evident use"** — Removed `_active.py` and `_tasks_md.py` from Card 23's Reads list. They were vestiges from an earlier draft of the script that touched per-worktree active markers and Home.md; the current spec uses `_status.read_phase` directly.

- **[NIT] "`.scratch/` directory not created before log open"** — Card 23 Step (b) now prepends `Path(".scratch").mkdir(exist_ok=True)` before opening the log file. `.scratch/` does not exist in a fresh checkout.

## Pushed Back

- **[BLOCKING] "`resolve_container_path` absent from plan"** — `20260429-105842-plan-review-03-state-on-worktree-r3.md`. The reviewer claimed `resolve_container_path` "appears nowhere in any batch plan or in the current source." This is factually wrong. Card 4 in batch 01-foundation explicitly adds the helper to `_paths.py` (see Card 4 requirements (b): `Add a NEW helper resolve_container_path(git_root: Path) -> Path to _paths.py and to __all__. Body: compute main_root = resolve_main_worktree_root(git_root); if main_root.parent.name == "wts" return main_root.parent.parent; else return main_root.parent.`). The reviewer's batch-03 review was scoped to batch 03's file alone and they did not have visibility into batch 01's contents — that is the cause of the false negative. Card 12's Requirements text already credits "the helper added in Card 4"; no edit is required to the plan. The reviewer's underlying CONCERN (the helper must exist before Card 12 uses it) is satisfied by the existing batch dependency `state-on-worktree` depends-on `create-hub-links` depends-on `foundation`, so Card 4 lands strictly before Card 12 runs. This finding is filed as factually-incorrect under the mill-receiving-review protocol's "PUSH BACK with evidence (cite actual code)" branch.

## Notes

- Two batches reached APPROVE in r3: 01-foundation and 04-consumers-and-skills. The other three (02, 03, 05) had legitimate detail-level findings, all fixed.
- Holistic review for r3 was killed by a network drop (subprocess hung on the live Claude API call). Re-running just the holistic isn't supported by the current millpy-review-plan.py CLI; per-batch coverage from r3 stands.
- Round 3 was the last allowed round per `review.plan.rounds: 3`. After this round and these fixes, the reviewer-loop is exhausted. Per the SKILL escape-hatch protocol, the user must now choose: A) deep problems → revise discussion; B) shallow → one more review round; C) override → set `approved: true` and proceed to mill-go. See the plan-handoff message for the recommendation.
- Non-progress check: r2 had 0 Pushed Back, r3 has 1 Pushed Back (factually-incorrect finding). Sets are NOT identical (size differs); non-progress does not fire.
