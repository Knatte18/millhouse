# Plan Fix Report — round 3

```yaml
slug: plan-template-quality
round: 3
review_file: 20260506-142102-plan-review-r3.md
```

## Fixed

- [BLOCKING] "Code review de-blinded after batch-02 rename" — added a new card 12 to batch 02 that relabels `**Reads:**` → `**Context:**` and `**Modifies:**` → `**Edits:**` in `plan/02-field-rename.md` and `plan/03-guidance.md`. Card 12 runs LAST in batch 02 so the renamed regex (committed in card 7) matches the now-renamed plan-file headers when mill-go invokes the post-batch code reviewer. Updated batch frontmatter `cards: 5` → `cards: 6`. Renumbered batch 03 cards from 12/13/14 to 13/14/15 (global card uniqueness). Updated batch 02 scope text to reflect six cards and explain card 12's purpose. (Verdict ACCURATE; verified `parse_batch_refs` reads the batch file at code-review time and would return `[]` for old field names after the regex rename.)
- [NIT] "Card 11 misidentifies one test as using hardcoded fixture text" — removed `test_check_reads_not_backtick_path_none_exempt` from the hardcoded-fixture list in card 11. Verified via `Read plugins/mill/unit_tests/test-plan-validate.py:501-523` that this test calls `_make_batch_file("alpha")` rather than building a raw string. Added an explicit note that its docstring should still be updated for consistency, but the test body needs no changes (relies on the `_make_batch_file` parameter rename in card 11). (Verdict ACCURATE; pure description fix.)

## Pushed Back

(none)
