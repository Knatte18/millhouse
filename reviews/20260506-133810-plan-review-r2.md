# Review: 9 (B) — Wiki-enhance: small wiki cleanups — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-06
```

## Findings

No findings. Plan is clean across all three criteria dimensions.

**Spot-checks performed:**

- **Regex compatibility**: `_HEADING_RE` in `_tasks_md.py` and both `_TASK_HEADING_RE` patterns use `(?P<proposal>proposal-[^)]+)` / `(?:\([^)]+\))?` — both already match `proposal-slug.md`; `set_phase` reconstructs verbatim from the captured group. No parse-path changes needed. ✓
- **Test breakage scan**: `test-millpy-add.py` asserts on proposal file content and CLI mutual-exclusion only — no Home.md slug-line assertion, so Card 2's rename doesn't break it. `test-spawn-core.py` `test_multi_select_groom_then_claim_with_proposal` asserts on proposal file existence and content, not Home.md format. ✓
- **Config removals**: `_config.py` uses generic `yaml.safe_load` + `deep_merge`; no key-specific handling exists. `test-config.py` has no assertions on `implementers`, `pipeline.builder`, or `pipeline.implementer`. Both checks in Card 1 requirements hold. ✓
- **run-all.py discovery**: `HERE.glob("test-*.py")` auto-discovers `test-sidebar.py` — no registration step needed. ✓
- **Decision alignment**: All three shared decisions (single-batch, no tombstones, `.md` scope limited to proposal links) faithfully reflected in card requirements. ✓

## Verdict

APPROVE
Plan is complete, consistent, and low-risk.