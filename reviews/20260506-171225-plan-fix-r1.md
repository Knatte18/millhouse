# Plan Fix Report — round 1

## Fixed

- `20260506-171123-plan-review-r1.md` / "subprocess mock returncode unspecified for existing test_fo_* tests" — Card 5 updated: added instruction to mock `subprocess.run` with `returncode=1` for all updated existing `test_fo_*` tests so the "forward original JSON unmodified" path is taken, preserving current equality assertions.
- `20260506-171123-plan-review-r1.md` / "Import style inconsistency in Card 2 requirements" — Card 2 updated: changed bare `set_batch_fields(...)` call syntax to `_status.set_batch_fields(...)` matching the module-style import already in `millpy-implement.py`; removed the ambiguous "match existing import style" qualifier.
- `20260506-171123-plan-review-r1.md` / "Sequential-letter naming convention misapplied to test-status.py" — Card 4 updated: removed letter-naming reference; changed to "append to the existing sequential `print('PASS: ...')` block in `main()`."

## Pushed Back

(none)
