# Plan-fix report — round 2

```yaml
round: 2
date: 2026-04-29
fixed_count: 11
pushed_back_count: 0
new_helpers_added: 1
batches_approved_in_r2: [03-state-on-worktree, 05-migration-and-docs]
```

## Fixed

### Holistic findings

- **[BLOCKING] "container_path off by one `.parent` — affects Cards 3, 9, 12, 16, 18, `_build_tokens`"** — `20260429-104409-plan-review-r2.md`. Fixed by introducing a NEW helper `_paths.resolve_container_path(git_root)` in Card 4 (alongside the existing scope of that card). Helper body: `main_root = resolve_main_worktree_root(git_root); return main_root.parent.parent if main_root.parent.name == "wts" else main_root.parent`. Handles both container-form (returns `<container>`) and prefix-form (returns parent of repo dir) without forcing every caller to know the layout. Updated the following sites to use the helper instead of the wrong-by-one `.parent` derivation: Card 8 `_build_tokens` (CONTAINER_PATH token); Card 9 (mill-claim portal entry creation); Card 12 (`_review_common.resolve_path` body); Card 16 (state-readers discovery); Card 18 (mill-cleanup portal removal). Card 3's body (`container_path / "wts" / slug`) was already correct given proper input — fixing the DERIVATION at every callsite makes Card 3's spec hold. The reviewer also flagged this issue on the per-batch review of 01-foundation; the fix is the same helper.

- **[NIT] "Card 5: render_block 'overload' lacks a Python implementation strategy"** — `20260429-104409-plan-review-r2.md`. Fixed in Card 5. Specified the signature as `render_block(glob_entries: list[str], anchored_entries: list[str] | None = None) -> str` with `anchored_entries is None` triggering legacy single-list mode and a non-None list triggering combined-block mode. Both `upsert` (legacy) and `upsert_split` (new) call the same function with different arities. Eliminates the impossible "two same-name functions" reading.

### Batch 01 (foundation) findings

- **[BLOCKING] "Prefix-form `resolve_worktrees_dir` test contradicts fallback change"** — `20260429-103548-plan-review-01-foundation-r2.md`. Fixed in Card 4. Removed the contradictory "Prefix-form tests unchanged" wording. The new requirement is explicit: prefix-form `resolve_worktrees_dir` test's expectation becomes `tmp_path` (`main_root.parent` for `tmp_path / "foo"`), or the test is dropped with a comment that prefix-form fallback is intentionally minimal and prefix-form users must configure `spawn.worktrees_dir:` for a sensible default. Card 4's docstring now reflects "prefix-form requires `spawn.worktrees_dir:` override".

- **[NIT] "STANDARD_ENTRIES removal not called out in test requirements"** — `20260429-103548-plan-review-01-foundation-r2.md`. Fixed in Card 5. Test-modification requirements now explicitly say to "replace every `from _gitignore import STANDARD_ENTRIES` (and any references to `STANDARD_ENTRIES` symbol) in `test-gitignore-phase.py` with imports/references to `GLOB_ENTRIES + ANCHORED_ENTRIES` as appropriate for the test's intent."

- **[NIT] "render_block 'overload' term is unimplementable as written"** — `20260429-103548-plan-review-01-foundation-r2.md`. Same as the holistic NIT above — fixed once with the explicit `anchored_entries: list[str] | None = None` signature.

- **[NIT] "container_path derivation in discussion.md is off by one `.parent`"** — `20260429-103548-plan-review-01-foundation-r2.md`. Fixed structurally — instead of patching every callsite manually, all callsites now route through the new `_paths.resolve_container_path` helper added in Card 4. The discussion.md prose itself is not edited (discussion is frozen post-handoff); the plan cards override the wrong derivation by referencing the helper.

### Batch 02 (create-hub-links) findings

- **[BLOCKING] "`portals/` must exist before `.others` junction is created"** — `20260429-103605-plan-review-02-create-hub-links-r1.md`. Fixed in Card 8. The card now spells out a strict 3-step ordering: (1) `mkdir <container>/portals/` with `parents=True, exist_ok=True`; (2) create the portal entry junction `<container>/portals/<slug>` → worktree; (3) THEN call `_setup.create_hub_links`. With portals/ present and portals/<slug> already pointing at the worktree, the `.others` and `.active` targets exist when `create_hub_links` runs and Windows `mklink /J` succeeds. The internal `target.mkdir(parents=True, exist_ok=True)` in `create_hub_links` becomes a no-op on the existing junction.

- **[BLOCKING] "Card 9 portal-creation ordering is underconstrained — 'after' causes unrecoverable failure"** — `20260429-103605-plan-review-02-create-hub-links-r1.md`. Fixed in Card 9. The "before or after" wording is replaced with strict "BEFORE `recreate_active_junction`". Card 9 now spells out the 3-step ordering matching Card 8 (mkdir portals/, create portal entry, then `recreate_active_junction`) and explicitly explains why the reverse order produces an unrecoverable `_junction.remove(directory)` ValueError.

- **[NIT] "Card 8 test assertions incompatible with mock-based `test-millpy-spawn.py`"** — `20260429-103605-plan-review-02-create-hub-links-r1.md`. Fixed by splitting the test surface. Card 8's extension to `test-millpy-spawn.py` is now restricted to call-order verification (consistent with the file's existing mock pattern). Real-disk filesystem assertions (`.others` junction resolves to portals dir, `.active` resolves to `portals/<slug>`, `tasks.md` hardlink shares inode with `wiki/Home.md`, `.millhouse/wiki` resolves to wiki path) move into Card 6's `test-setup-hub-links.py` — that file already uses `tempfile.TemporaryDirectory()` and exercises the real filesystem, and was already specified to cover `_setup.create_hub_links` end-to-end. Card 6's test list was extended explicitly to include these integration assertions in a fixture that mirrors the live mill-spawn flow (mkdir portals → create portals/<slug> junction → call create_hub_links).

### Batch 04 (consumers-and-skills) findings

- **[BLOCKING] "Card 21 — mill-resume Phase 6 old worktrees path unaddressed"** — `20260429-103658-plan-review-04-consumers-and-skills-r2.md`. Fixed in Card 21. The card now explicitly requires updating mill-resume Phase 6 (`git -C <git-root> worktree add <container>/wts/<slug> <branch_name>`) and the `<worktrees-dir>` definition to `<container>/wts/`, where `<container>` resolves through `_paths.resolve_container_path(<git-root>)`. The Phase 4 precondition check is updated symmetrically. Verified by reading the live SKILL.md text via grep — the issue is real and the fix is mechanical.

- **[NIT] "Card 17 — two config loads needed but plan phrases it as replacement"** — `20260429-103658-plan-review-04-consumers-and-skills-r2.md`. Fixed in Card 17. Clarified that the existing pre-pick `_load_config(wiki_path, git_root)` call STAYS (used to resolve `worktrees_dir` and other hub-side keys before discovery) and a SECOND post-pick `_load_config(wiki_path, selected_path)` is ADDED to read the per-worktree `hub_relative_path`. The regression test ensures the SELECTED worktree's value wins over the HUB's.

- **[NIT] "Card 18 — `container_path` derivation unstated for portal removal"** — `20260429-103658-plan-review-04-consumers-and-skills-r2.md`. Fixed in Card 18 requirement (e). Both `_apply_worktree_record` and `_apply_inplace_record` now compute `container_path = _paths.resolve_container_path(hub_root)` inline at the top of their bodies, then call `_junction.remove(container_path / "portals" / slug)`. The new helper from Card 4 handles both container-form and prefix-form.

- **[NIT] "Card 16 — 'call stays' wording is vacuously true"** — `20260429-103658-plan-review-04-consumers-and-skills-r2.md`. Fixed in Card 16. Replaced the misleading "call stays" prose with explicit derivation: each script ADDS `container_path = _paths.resolve_container_path(git_root)` and uses `container_path / "wts"` as the discovery argument to `discover_active_worktrees`. Removed the false claim that the scripts already call `resolve_worktrees_dir`.

## Pushed Back

(none — every finding fixed)

## Notes

- Two batches reached APPROVE in r2: 03-state-on-worktree (the heavily-edited batch from r1) and 05-migration-and-docs.
- Batch 02 was reviewed for the FIRST time in r2 (round 1 of THAT batch); the timeout in r1 meant no per-batch coverage. Both Windows-junction-ordering blockers were caught immediately on first read.
- The new `_paths.resolve_container_path` helper is the single highest-leverage fix this round: it replaces 5 separate ad-hoc derivations with one canonical answer, reduces error surface for batches 02–04, and makes the prefix-form vs container-form distinction local to one function.
- Round 3 will re-review every batch. Two are already at APPROVE; the remaining three should land at APPROVE if the helper-based derivation propagates correctly.
- Non-progress check: r1 had 0 Pushed Back, r2 has 0 Pushed Back. Per the SKILL note (folded #66 in the wiki backlog), two empty Pushed Back sets do NOT trigger non-progress when the fixer is making forward progress on a non-empty Fixed set. Skip the check.
