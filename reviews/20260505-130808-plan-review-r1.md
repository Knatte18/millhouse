# Review: 3 (A) — codeguide improvements: sibling placement + --branch flag — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-05
```

## Findings

### [NIT] Card 3 updates a Step 4 string that Card 4 replaces wholesale
**Location:** Batch codeguide-setup-skill / Cards 3 & 4
**Issue:** Card 3 instructs the implementer to update the `--from-url` reference inside Step 4, but Card 4 then replaces that entire "If not exist" sentence with the five-way decision tree. Card 4's requirements quote the pre-Card-3 string ("when `--from-url <url>` was given") as the old_string to match; after Card 3 runs, that string no longer exists in the file.
**Fix:** Remove Card 3's "notably the existing reference inside Step 4" instruction — Card 4 supersedes it. Alternatively, update Card 4's quoted old_string to reflect the post-Card-3 form (`--sibling <url>`).

## Verdict

APPROVE
One minor sequencing ambiguity in Card 3/4 quoted strings; no blocking issues found.