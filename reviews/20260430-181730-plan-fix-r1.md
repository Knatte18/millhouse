# Plan-fix report — round 1

```yaml
slug: migrate-to-uv
round: 1
date: 2026-04-30
```

## Fixed

- `20260430-180638-plan-review-03-mill-setup-skill-r1.md` — [BLOCKING] `pwsh` vs `powershell` inconsistency in Req 3 — replaced `pwsh -Command` with `powershell -Command` in card 7 Req 3 to match discussion.md and PS5 availability on the target machine.
- `20260430-180638-plan-review-03-mill-setup-skill-r1.md` — [BLOCKING] Bash/PS escaping bug — `$env:USERPROFILE` not escaped — rewrote the snippet using `\$env:USERPROFILE` and double-escaped backslashes so bash passes the literal string through to PowerShell.
- `20260430-180638-plan-review-03-mill-setup-skill-r1.md` — [BLOCKING] Req 6 omits updating existing Phase 8 `.py` wrapper invariant — Req 6 now explicitly says REPLACE the existing `.py` invariant (not just add a `.ps1` line).
- `20260430-180638-plan-review-03-mill-setup-skill-r1.md` — [NIT] Redundant `sys.path.insert` in Phase 4.7 snippet after migration — added instruction in Req 4 to remove the `sys.path.insert(0, ...)` line from the Phase 4.7 Python body.
- `20260430-180741-plan-review-04-skills-sweep-r1.md` — [BLOCKING] Card 9 perpetuates wrong `_tasks_md.set_phase` signature — corrected the expected signature to `_tasks_md.set_phase(text: str, slug: str, phase: str | None) -> str` (verified via `inspect.signature`) and added an explicit instruction to fix the existing wrong call in mill-go SKILL.md (read text → mutate → write back).
- `20260430-180741-plan-review-04-skills-sweep-r1.md` — [BLOCKING] Card 15 misses second broken `millpy` import in mill-resume Phase 8 — extended card 15 requirements to also fix `from millpy.core.junction import create as junction_create` at line 134 (verified by grep). Replaced with `from _junction import create as junction_create`.
- `20260430-180741-plan-review-04-skills-sweep-r1.md` — [NIT] Card 8 "exactly one invocation" claim is factually wrong for mill-worktree — changed wording to "one or more invocations"; explicitly noted mill-worktree has three; preserved the original "Replace each invocation" instruction.
- `20260430-181409-plan-review-r1.md` — [BLOCKING] path-fix verify requires pyproject.toml but omits foundation dependency — changed `02-path-fix.md` frontmatter and overview Batch Index entry from `depends-on: []` to `depends-on: [foundation]`.
- `20260430-181409-plan-review-r1.md` — [BLOCKING] mill-resume Phase 8 broken `millpy.core.junction` import not addressed — same fix as 04-skills-sweep BLOCKING #2 above; also added `Grep from\s+millpy\.` to card 18's audit sweep so the regression check catches Python imports (not just CLI invocations).
- `20260430-181409-plan-review-r1.md` — [NIT] Card 3 smoke-check uses bare `python`, not `uv run` — changed the smoke-check command to `PYTHONPATH=plugins/mill/scripts uv run --project plugins/mill python -c "..."` (inline PYTHONPATH retained because the dev-checkout scripts are not yet on the global env path during this batch).
- `20260430-181409-plan-review-r1.md` — [NIT] Card 20 env-kwarg removal instruction is ambiguous for review tests — clarified: remove ONLY the `env["PYTHONPATH"] = str(SCRIPTS)` line; keep `env["PYTHONIOENCODING"] = "utf-8"` and the `env=` kwarg intact (verified by grep that `PYTHONIOENCODING` is set in 9 test files).

## Pushed Back

(none)
