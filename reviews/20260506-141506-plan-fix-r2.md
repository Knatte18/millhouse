# Plan Fix Report — round 2

```yaml
slug: plan-template-quality
round: 2
review_file: 20260506-141313-plan-review-r2.md
```

## Fixed

- [BLOCKING] "Card 4 drops string-dep fix guidance from SKILL.md table" — extended card 4's `depends-on-unknown` row to cover both formats: integer deps (compare against `number:`) AND string deps (compare against `name:`, legacy). Backward-compat decision applies. (Verdict ACCURATE; no harm to fix.)
- [BLOCKING] "Card 11 misses hardcoded `**Reads:**`/`**Modifies:**` strings in two tests" — added explicit instruction to card 11 to update raw-string fixtures in `test_check_reads_not_backtick_path_clean`, `test_check_reads_not_backtick_path_none_exempt`, and `test_check_reads_not_backtick_path_dirty`. Without this, batch 02's verify breaks. (Verdict ACCURATE; verified all three tests use raw strings, not `_make_batch_file`.)
- [NIT] "Card 6 test name is inverted" — renamed `test_check_depends_on_unknown_dirty_integer` to `test_check_depends_on_unknown_dirty_legacy_string` (body tests legacy string deps, not integer deps). (Verdict ACCURATE; pure naming clarity fix.)
- [NIT] "`plan-overview.md` HTML comment retains old field names after batch 02" — added `plan-overview.md` to card 9's scope. Updated both the HTML comment AND the visible `## All Files Touched` description (the latter also says `Modifies:`). (Verdict ACCURATE; verified via grep.)
- [NIT] "`resolve_deps_as_names` unspecified for unresolved integer deps" — specified in card 1 that unresolved integer entries are silently dropped, with `_check_deps` as the authoritative dangling-dep check. Prevents `_compute_transitive_ancestors` KeyError when called after a failed `_check_depends_on_unknown` (which produces errors but does not raise). (Verdict ACCURATE; verified `_plan_validate.run` runs all checks even when earlier ones produced errors.)

## Pushed Back

(none)
