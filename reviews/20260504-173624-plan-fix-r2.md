# Plan-fix report — round 2

```yaml
slug: review-subsystem-fixes
round: 2
date: 2026-05-04T17:36:24Z
```

## Fixed

- **[BLOCKING] `check` parameter silently dropped in Popen rewrite** (review file `20260504-171426-plan-review-02-subprocess-tree-kill-r2.md`, Card 8) — Card 8 requirements now include: after constructing the normal-completion `CompletedProcess`, raise `subprocess.CalledProcessError(proc.returncode, argv, output=stdout, stderr=stderr)` when `check and proc.returncode != 0`. Card 9 gains regression tests (d) `check=True` raises and (e) `check=False` returns CompletedProcess with the non-zero returncode.
- **[NIT] `collected_stdout`/`collected_stderr` origin unspecified in re-raise** (same review file, Card 8) — Card 8 now states explicitly: assign `collected_stdout = exc.stdout` and `collected_stderr = exc.stderr` from the caught `subprocess.TimeoutExpired`, preserving `None`/`bytes` form to match `subprocess.run`.
- **[BLOCKING] Card 18 omits `cfg["llm"]` fixture update** (review file `20260504-171619-plan-review-04-review-plan-integration-r1.md`, Card 18) — Card 18 now requires extending `_make_plan_fixture` so the returned cfg includes `"llm": {"bulk_timeout": None, "holistic_timeout": None}`, matching the unconditional reads added by Card 14.
- **[NIT] Tests 6/7 break when B01 adds `timeout` to stub kwargs** (same review file, Card 18) — Card 18 now requires updating tests 6 and 7's exact-equality `retry_kwargs` assertions to include `"timeout": None` per B01 Card 7's stub-capture extension.
- **[NIT] Test (a) mechanism for LLMError not specified** (same review file, Card 18) — Card 18 now specifies the mechanism: monkey-patch `stub.run` to a function that raises `LLMError`, restoring the original in `finally`. The accompanying B01 Card 7 docstring now documents that monkey-patching is the supported pattern (no exception-seeding queue).
- **[BLOCKING] `holistic_timeout` absent from `wiki/config.yaml` and unowned** (review file `20260504-171417-plan-review-05-review-code-integration-r2.md`, Card 21) — `wiki/config.yaml` is owned by B04 Card 13 (which already adds `holistic_timeout: 1800`). Fix applied at the dependency layer instead of duplicating the config write: B05's `depends-on:` now includes `review-plan-integration` in both the batch yaml and the overview's Batch Index DAG. The Batch Scope prose was updated to explain the dependency edge.
- **[NIT] Stub exception-seeding mechanism undocumented for Card 22(a)/(b)** (same review file, Card 22) — Card 22 now specifies the same monkey-patch pattern; B01 Card 7's docstring update (above) is the canonical source of truth.
- **[BLOCKING] Fixture helpers break all existing tests after Card 26** (review file `20260504-171256-plan-review-07-plan-validate-deletes-r2.md`, Cards 26/28) — Card 26 now requires updating `_make_batch_file`, `_make_batch_file_cards`, and the two custom `batch_text` literals in `test_check_reads_not_backtick_path_*` tests inside `unit_tests/test-plan-validate.py`, so the existing tests stay green after the new required-field check lands.
- **[NIT] Missing import not mentioned in Card 27** (same review file, Card 27) — Card 27 now requires adding `compute_deletes_union` to the existing `from _review_common import (...)` block in `_plan_validate.py`.

## Pushed Back

(none)

## Notes

The holistic plan review timed out at 600s (`Claude CLI timed out after 600s`). That's the exact bug B04 Card 13 + B05 Card 21 fix: bumping `holistic_timeout` to 1800 plus plumbing it through the reviewer modules. No retry attempted in this round — the per-batch findings already cover what the holistic would have surfaced for these batches, and the rest of the suite was already approved (`03-llm-rate-limit`, `06-cli-error-prefix`, `08-mill-plan-skill-step45`).

Per user instruction (`Hvis du trenger flere review-runder: kjør kun holistic.`), round 3 (if required) will run with `--holistic-only`.
