# Plan-fix report — round 4

```yaml
slug: review-subsystem-fixes
round: 4
date: 2026-05-04T17:57:38Z
```

## Fixed

- **[BLOCKING] cfg["llm"] key read before fixture update breaks per-card verify** (review file `20260504-175356-plan-review-r2.md`) — Moved fixture updates from Card 18 to Card 14 (B04) and from Card 22 to Card 21 (B05). Card 14's Modifies now includes `test-review-plan-flow.py`; Card 21's Modifies now includes `test-review-code-flow.py`. Each card landed-and-verified is now self-consistent: the unconditional `cfg["llm"]` read AND the matching fixture key live in the same commit, so `run-all.py` stays green between cards. Card 18 / Card 22 were trimmed to remove the duplicated fixture-update text and now reference the earlier card as the owner.
- **[BLOCKING] Step 4.5 placement bypassed for all-ERROR aggregate** (same review file) — Moved Step 4.5 in B08 Card 29 from "between 4c and 5" to "between 4a and 4b". Rationale captured in the card body: an all-ERROR run yields `verdict=REQUEST_CHANGES, blocking_count=0`, which step 4b would catch first if 4.5 sat later, flipping `approved: true` against a vacuous finding set. Placing 4.5 ahead of 4b makes the ERROR-only check fire first.
- **[NIT] Missing import instructions for new helpers in B04** (same review file) — Card 15 Requirements now state: "Add `compute_deletes_union` and `build_deletes_section` to the existing `from _review_common import (...)` block in `_review_plan.py`." Card 16 Requirements now state: "Add `detect_resume_round` to the existing `from _review_common import (...)` block in `_review_plan.py`." Brings B04 in line with B07 Card 27's explicit-import convention.
- **[NIT] "bytes" comment inaccurate for text-mode Popen in Card 8** (same review file) — Card 8 timeout-handler description changed from "may be `None` or `bytes` — preserve as-is to match `subprocess.run`'s behaviour on timeout" to "may be `None` or `str` since the Popen runs in `text=True` mode."
- **[NIT] B05 Card 22 test (b) stub-wrapping for retry LLMError underspecified** (same review file) — Card 22 test (b) now includes a 12-line code sketch using a `call_count` closure showing exactly how to drive "first call returns seeded NEED_CONTEXT, second call raises LLMError" with `try/finally` restoring the original `stub.run`.

## Pushed Back

(none)

## Non-progress check

Skipped: round 3's `## Pushed Back` was empty AND round 4's `## Pushed Back` is empty. Convergence; not non-progress.

## Notes

The two BLOCKINGs in this round were both structural-sequencing problems that previous rounds' fixes happened to expose: round 2's "add `cfg["llm"]` block to the fixture in Card 18" was correct *for the test card* but missed that Cards 15/16/17 verify in between — Card 14 must own the fixture update for those intermediate cards to stay green. Likewise, B08 Card 29's original step-4.5 placement was a copy-paste of the existing step-1.5 pattern that didn't account for step 4b's catch-all guard. Round 4 closes both with their proper structural fixes.

After this round all five findings are addressed cleanly. The convergence pattern continues to be a strong signal — every round so far has reduced the open BLOCKING set and every fix-pass `## Pushed Back` has been empty.
