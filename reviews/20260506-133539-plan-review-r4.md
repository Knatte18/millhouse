# Review: 11 (B) — Review-code: configurable holistic effort + diff-scoping via start_sha — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-06
```

## Findings

### [NIT] Test A/per-batch-start_sha test: overwrite step implicit
**Location:** Batch 04 / Card 8
**Issue:** `_make_fixture` creates `src/a.py` as `# placeholder` (13 bytes). Both Card 7 Test A and Card 8's "per-batch with start_sha" test need 2000 lines in that file before the initial commit, but the plan says "add and commit `src/a.py` with 2000 lines" without explicitly noting the prior overwrite of the placeholder.
**Fix:** Add "overwrite `src/a.py` with `'x\n' * 2000` before staging" to the step description.

### [NIT] `p.relative_to(project_root)` fallback not shown in code
**Location:** Batch 03 / Card 5
**Issue:** The plan says "if the path is not under `project_root`, fall back to the absolute path's string" but does not show the `try/except ValueError` wrapper that makes `p.relative_to()` safe.
**Fix:** Add the one-liner: `try: rel_path = p.relative_to(project_root).as_posix() \nexcept ValueError: rel_path = str(p)` to the requirements.

## Verdict

APPROVE — plan is sound, decisions correctly threaded, backwards-compatibility preserved, and all error paths are covered by the specified tests.