# Review: 4 (A) — mill-setup: --from-url for separate wiki repo — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-06
```

## Findings

### [NIT] Stale error-conditions table row for `git ls-remote` failure
**Location:** Batch skill-and-template / Card 5
**Issue:** Card 5 adds two new rows to `## Error conditions` but does not update the existing row "`git ls-remote <wiki-url>` fails → Halt with GitHub URL + instruction to create Home page" — which is now only true when `<effective-from-url-source> == 'derived'`.
**Fix:** Instruct the implementer to update that row to say the halt message is conditional per Phase 2's branching logic.

### [NIT] No test for clone failure after a successful `ls-remote`
**Location:** Batch helpers / Card 2
**Issue:** Test 8 covers `ls-remote` returning rc≠0; no test covers the case where `ls-remote` returns non-empty stdout (branch exists) but `git clone -b` subsequently fails. The analogous Path-C case (`git clone` without branch failing) is also untested.
**Fix:** Add `test_halt_clone_failure_after_ls_remote`: ls-remote returns non-empty, clone returns rc≠0; assert `WikiSetupError` is raised.

## Verdict

APPROVE — two minor test/doc gaps; no blocking issues.