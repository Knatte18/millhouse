# Review: 6 — mill-go SKILL.md prose + lock-API + lock-coverage + Builder-oppförsel — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-04
```

## Findings

### [NIT] Card 6 silent on `expected_wt` assertion change in test-millpy-claim.py
**Location:** B01 / Card 6
**Issue:** `test_main_happy_path_calls_spawn_core_helpers` asserts `worktree_path == Path("/fake/worktrees") / "my-task"`, but after Card 4 fixes the call to `write_initial_status(worktree_path=git_root, ...)`, the actual value delivered is `Path("/fake/repo")` (what the mock returns for `resolve_git_root`). The test fails; verify catches it, but Card 6's description says nothing about updating this assertion, so the implementer must debug a surprise failure.
**Fix:** Add one bullet to Card 6: "In `test_main_happy_path_calls_spawn_core_helpers`, update `expected_wt` to match `Path("/fake/repo")` (the `resolve_git_root` mock's return value) since mill-claim's worktree IS the current git root."

### [NIT] Card 3/Card 4 out-of-order execution within B01
**Location:** B01 / Cards 3–4
**Issue:** Card 3 carries a `⚠️ Execute AFTER Card 4` gate, requiring the implementer to jump ahead then loop back. This non-sequential execution is unusual and increases the chance of a misordering mistake, especially since the card numbers suggest 3 precedes 4.
**Fix:** Swap the card bodies so the numbered order matches execution order (current Card 4 body becomes Card 3, current Card 3 body becomes Card 4). Global card numbers shift by one from Card 4 onward, which is preferable to an out-of-order instruction.

### [NIT] Card 23 leaves line-176 rewording vague
**Location:** B05 / Card 23
**Issue:** "The third reference at line 176 is in a failure-mode table and may need rewording" is non-prescriptive. The implementer has no guidance on what the rewritten table cell should say.
**Fix:** Specify the replacement: change `wiki.sync_pull fails` to `` `_wiki.sync_pull` raises `WikiPushError` `` (matching the exception vocabulary established in B01).

## Verdict

APPROVE
Three implementation-detail NITs; no structural or correctness blockers.