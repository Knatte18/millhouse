# Batch: wrapper

```yaml
task: Wrap claude -p via psmux to use subscription instead of API credits
batch: wrapper
number: 3
cards: 2
verify: python plugins/mill/unit_tests/run-all.py && python -m py_compile plugins/mill/scripts/millpy-claude-sub.py
depends-on: []
```

## Batch Scope

This batch delivers the standalone CLI script
`millpy-claude-sub.py` -- the drop-in replacement for `claude -p` that
mill callers will eventually invoke. The wrapper composes the parser
(batch 01) and the driver (batch 02) into the full prompt-in /
response-out flow over a per-call psmux session. No new unit tests are
introduced here -- the wrapper itself is integration glue, covered by
batch 04's integration test.

**External interface (used by batch 04 integration test and any future
caller):** the script accepts `--mode {bulk,tool-use,implementer}`,
`--model NAME`, optional `--effort LEVEL`, optional `--session-id UUID`;
reads the prompt body on stdin; writes the extracted Claude response on
stdout; writes one JSON line `{"session_id": ..., "duration_s": ...,
"mode": ...}` on stderr; exits 0 on success, non-zero on any error
(timeout, missing claude binary, marker-not-found-after-timeout,
psmux failure).

**Batch-local decisions:**
- Constants live at module top, not in a config file (spike scope).
- All side-effecting ops (psmux session create, temp file write) live
  inside one try/finally; the finally block kills the session and
  unlinks the temp file unconditionally.
- The wrapper does NOT modify `_llm_claude.py`. Wiring is follow-up
  work.


## Cards

### Card 8: wrapper skeleton (constants, argparse, mode tables)

- **Context:**
  - `_mill/discussion.md`
  - `plugins/mill/scripts/_llm_claude.py`
  - `plugins/mill/scripts/_paths.py`
- **Edits:** none
- **Creates:**
  - `plugins/mill/scripts/millpy-claude-sub.py`
- **Deletes:** none
- **Requirements:** Module docstring matching the `_llm_claude.py` style:
  one-paragraph summary stating this is the `claude -p` replacement that
  routes through interactive `claude` via psmux for subscription billing,
  with a four-mode table (bulk / tool-use / implementer) and pointers to
  `_psmux.py` (driver) and `_psmux_capture.py` (parser). Use
  `from __future__ import annotations`. Top-level constants:
  - `BOOT_READY_TIMEOUT_S = 20`
  - `PSMUX_COMMAND_TIMEOUT_S = 30` (with comment noting this is also
    defined in `_psmux.py` and the values must stay in sync; the local
    reference is kept for explicitness in the wrapper)
  - `POLL_INTERVAL_S = 1.0`
  - `RESPONSE_POLL_TIMEOUT_S: dict[str, int] = {"bulk": 300, "tool-use":
    600, "implementer": 1800}`
  - `MODE_TOOL_FLAGS: dict[str, list[str]] = {"bulk": ["--tools", ""],
    "tool-use": ["--allowedTools", "Read,Grep,Glob"], "implementer":
    ["--allowedTools", "Read,Edit,Write,Bash,Grep,Glob,Skill"]}`
  Build an `argparse.ArgumentParser` with arguments:
  - `--mode` (required, choices=["bulk", "tool-use", "implementer"])
  - `--model` (required)
  - `--effort` (optional, choices=["low", "medium", "high", "xhigh", "max"])
  - `--session-id` (optional; default generated by
    `str(uuid.uuid4())` at parse time)
  Define stub `def main() -> int:` body `raise NotImplementedError(
  "implemented in card 9")`. Footer
  `if __name__ == "__main__": sys.exit(main())`. ASCII only. The script
  must accept `--help` and exit 0 (argparse autodocumentation suffices).
- **Commit:** `feat(mill): add millpy-claude-sub wrapper skeleton`

### Card 9: wrapper main flow with startup check, helpers, and idle-poll

- **Context:**
  - `_mill/discussion.md`
  - `plugins/mill/scripts/_psmux.py`
  - `plugins/mill/scripts/_psmux_capture.py`
  - `plugins/mill/scripts/_paths.py`
  - `plugins/mill/scripts/_subprocess_util.py`
  - `plugins/mill/scripts/_llm_claude.py`
- **Edits:**
  - `plugins/mill/scripts/millpy-claude-sub.py`
- **Creates:** none
- **Deletes:** none
- **Requirements:** Implement `main()`. Steps in order:
  1. Parse argv. Read prompt body via `sys.stdin.read()`. Verify it is
     non-empty; if empty, print `[millpy-claude-sub] empty prompt on
     stdin` to stderr and return 2.
  2. Generate `session_name = f"mill-{uuid.uuid4().hex[:8]}"`. Generate
     `begin_marker = f"MILL_BEGIN_{secrets.token_hex(4)}"` and
     `end_marker = f"MILL_END_{secrets.token_hex(4)}"` (independent
     calls to `secrets.token_hex(4)` so suffixes differ).
  3. Append the dual-marker reply-protocol footer to the prompt body
     verbatim per the block in `_mill/discussion.md` `### Dual marker
     protocol` decision (substituting `<rand1>`/`<rand2>` with the
     actual marker tokens above).
  4. Resolve scratch dir via `_paths.resolve_git_root() / ".scratch"`.
     Create directory if absent. Write the (footer-appended) prompt
     body to `prompt_path = scratch / f"wrapper-{session_name}-prompt.txt"`
     using UTF-8.
  5. Open a `try:` block; nest a single `finally:` containing
     `_psmux.kill_session(session_name)` and
     `prompt_path.unlink(missing_ok=True)` -- both run unconditionally.
     All work below sits inside the try.
  6. `_psmux.new_session(session_name, shell_argv=["pwsh", "-NoLogo",
     "-NoProfile"])`. Wait briefly (sleep `POLL_INTERVAL_S`) for the
     pane to stabilise.
  7. STARTUP CHECK: `_psmux.send_keys(session_name, "Get-Command claude
     -ErrorAction Stop; Write-Host CLAUDE_READY", enter=True)`. Call
     helper `_wait_for_marker_in_pane(session_name, "CLAUDE_READY",
     BOOT_READY_TIMEOUT_S)`. If the helper returns False, capture the
     pane once for the error message, raise
     `RuntimeError(f"claude not found in psmux pane PATH (expected at
     ~/.local/bin/claude.exe). Pane tail: {tail}")`.
  8. Build the claude launch command string: start with `["claude",
     "--model", args.model, *MODE_TOOL_FLAGS[args.mode], "--session-id",
     args.session_id]`; if `args.effort`, append `["--effort",
     args.effort]`. Join the argv with spaces using `shlex.join` so
     embedded quotes are preserved correctly (the resulting string is
     what pwsh receives, NOT a Python argv).
  9. `_psmux.send_keys(session_name, claude_cmd_str, enter=True)`. Call
     helper `_wait_for_idle_prompt(session_name, BOOT_READY_TIMEOUT_S)`;
     on False, raise `RuntimeError("claude TUI did not reach idle
     prompt within boot timeout")`.
  10. `_psmux.load_buffer(session_name, "p", prompt_path)` then
      `_psmux.paste_buffer(session_name, "p")`. Sleep `POLL_INTERVAL_S`
      (let the TUI render the pasted content). Then
      `_psmux.send_keys(session_name, "Enter", enter=False)` to submit.
      (Note: passing `keys="Enter"` triggers the Enter token; the
      `enter=False` flag avoids appending a duplicate Enter.)
  11. Record `start = time.monotonic()`. Loop: capture =
      `_psmux.capture_pane(session_name)`; try
      `_psmux_capture.extract_response(capture, begin_marker,
      end_marker)`. On success: `print(response)` to stdout (no
      trailing newline beyond what the slice contains); compute
      `duration = round(time.monotonic() - start, 2)`; print
      `json.dumps({"session_id": args.session_id, "duration_s":
      duration, "mode": args.mode})` to stderr followed by newline;
      `return 0`. On `MarkerNotFoundError`: if
      `time.monotonic() - start > RESPONSE_POLL_TIMEOUT_S[args.mode]`,
      raise `RuntimeError(f"response-poll timeout: mode={args.mode}
      elapsed={duration:.1f}s")`. Else `time.sleep(POLL_INTERVAL_S)`
      and continue.
  12. Top-level: wrap the whole try/finally in an outer try/except
      that catches `Exception`, prints `[millpy-claude-sub] {type}:
      {msg}` to stderr, and returns 1. The finally still runs.
  Add two private helpers:
  - `def _wait_for_marker_in_pane(session_name: str, marker: str,
    timeout_s: float) -> bool`: poll `_psmux.capture_pane` every
    `POLL_INTERVAL_S` looking for any line whose stripped value contains
    `marker`. Return True on first match, False on timeout. Catches
    `PsmuxError` and returns False on persistent failure.
  - `def _wait_for_idle_prompt(session_name: str, timeout_s: float) ->
    bool`: poll `_psmux.capture_pane` every `POLL_INTERVAL_S` looking
    for ANY line in the most recent 10 lines whose stripped value is
    exactly the box-prompt prefix character (use the literal Unicode
    character claude renders for the input prompt, observed during PoC
    -- the implementer should grep for it in
    `_mill/discussion.md` `## Technical context` for the verbatim
    char). Treat "the prompt input box rendered with no in-progress
    text" as the idle signal. Return True on first match, False on
    timeout.
  Note: the idle-prompt helper exists for boot detection only; the
  RESPONSE-poll loop in step 11 does NOT use it (per discussion's
  poll-loop termination rule -- markers are sufficient, idle-prompt
  is not a load-bearing gate).
  Imports needed beyond the skeleton: `import json`, `import secrets`,
  `import shlex`, `import sys`, `import time`, `import uuid`, plus
  `import _psmux`, `import _psmux_capture`, `import _paths`. ASCII only
  in stderr/print except for the literal box-prompt character (which
  lives in source code, not stdout). The script must continue to
  accept `--help` and exit 0.
- **Commit:** `feat(mill): implement millpy-claude-sub main flow with startup check`

## Batch Tests

No unit tests are introduced in this batch -- the
wrapper is integration glue and is covered end-to-end by batch 04's
integration test. The per-batch verify runs `python
plugins/mill/unit_tests/run-all.py` (which still exercises the parser
from batch 01 and the driver from batch 02) AND
`python -m py_compile plugins/mill/scripts/millpy-claude-sub.py` to
catch syntax errors at commit time. The wrapper's `--help` output is
exercised in the final batch's integration test.
