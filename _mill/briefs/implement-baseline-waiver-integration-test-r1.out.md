Summary: 1 of 1 card committed (Card 25 — "Integration test for per-batch baseline capture and waiver"). The new integration test at `/home/knatte/Code/millhouse/wts/mill-validate-verify-diagnostics-gaps/plugins/mill/integration_tests/test-baseline-waiver.py` builds a real-git fixture (bare wiki + hub + linked task worktree, task record seeded via `wiki._client.upsert_task` since the wiki daemon's actual source of truth is TinyDB's `tasks.json`, not Home.md), then drives `millpy-implement.py --stage baseline` and `--stage finalize` as real subprocesses with `PYTHONPATH` pinned to this worktree's `plugins/mill/scripts` and `CLAUDE_PLUGIN_ROOT` stripped from the child env. It confirms: (1) baseline capture of a pre-existing failure signature, (2) a genuinely new failure alongside the pre-existing one is not waived (`stuck_type: verify`), and (3) a pre-existing-only failure set is waived (`status: success`). Verify command ran clean (exit 0), working tree is clean, and the commit is pushed to `hanf/mill-validate-verify-diagnostics-gaps`.

{"status":"success","commit_sha":"01193e60957eb3ddddb81666e82c7116e5a4944c","session_id":"f9ffc399-138e-4cea-b6e2-a6298abec834","cards_done":[25]}
