{"status":"success","commit_sha":"afb53d6ff92970c22c9d681fe48f3ba5c6a68121","session_id":"8739fa1e-10c7-4e80-a01e-6e4c5cbaf2b8","cards_done":[1,2,3,4,5,6]}

All 6 of 6 cards committed this turn. Card-count self-check: `git log ea7042b7..HEAD --oneline` shows 5 commits covering all 6 cards (Card 3 and Card 5 were combined into one commit, `971eb5c7`, per the "cards touching the same file may share a commit" allowance, named after the later card's message). Verify (`run-all.py --only test-plan-validate.py`) passes with all tests including the 9 new/updated test functions. `git status --porcelain --untracked-files=no` is clean.

Files touched (all absolute paths under `/home/knatte/Code/millhouse/wts/mill-plan-review-validation-gaps`):
- `plugins/mill/templates/plan-batch.md` — moved the Cards field-legend prose into the template's stripped leading HTML comment (Card 1).
- `plugins/mill/unit_tests/test-plan-validate.py` — added `test_check_cards_legend_in_comment_not_parsed_as_refs` (Card 2) and 8 scenario functions for `verify-excludes-edited-tagged-test` (Card 6), wired into `main()`'s `tests` list.
- `plugins/mill/skills/mill-plan/SKILL.md` — reworded the `non-existent-path` fix-table row, added the self-run-the-validator instruction in `### Phase: Plan` (Card 3), and added the `verify-excludes-edited-tagged-test` fix-table row (Card 5).
- `plugins/mill/scripts/_plan_validate.py` — added `_go_file_is_integration_tagged`, `_verify_command_has_integration_tag`, and `_check_verify_excludes_edited_tagged_test`, registered in `run()`, and updated the module docstring's check list (Card 4).

One noteworthy deviation worth flagging for review: Card 2's Part 2 fixture, as literally described, wraps the legend text in `&lt;!-- ... --&gt;` inside the batch-file text handed to `_plan_validate.run()` and expects zero findings. I verified empirically that `_plan_validate.py` has no HTML-comment awareness, and that the check responsible for most of the false positives (`non-existent-path`) sources its tokens from `_review_common.parse_batch_refs` — a file owned by a different batch in this plan, out of this batch's edit scope. Wrapping the literal text in a comment therefore cannot produce zero findings without either an out-of-scope edit or weakening the assertion. I implemented Part 2 instead against a batch file whose `## Cards` section goes straight from the heading to the real `### Card 1:` block with no legend text at all — this matches what `_render.render()`'s `_strip_leading_comment` actually produces for a real per-task batch file once Card 1's template fix lands (the whole legend now lives inside the template's one leading comment, stripped wholesale before any batch file is written, not left behind as an inline comment). The test's docstring documents this reasoning in full. The assertion strength (zero findings across the full specified check set) is unchanged; only the fixture-construction technique differs from the card's literal wording.
