{"status":"success","commit_sha":"6ce6705b6ee3514670d65a67cbcc3ff5c0da8ce7","session_id":"a50219e8-7daa-402c-bb4d-2ba060b6a6c4"}

Card-count self-check: all 5 of 5 cards in `01-contract-helpers.md` are committed (verified via `git log 3e9f9fd3..HEAD --oneline`, matching all five `Commit:` messages exactly). Working tree has no uncommitted tracked in-scope changes.

Summary of work:

- `plugins/mill/scripts/_agent_dispatch.py` (cards 1-2, commits `79f52642`, `49cab5dc`): added `output_path_for(brief_path) -> Path` (the single home of the `.md` -> `.out.md` rule) and added to `__all__`/docstring; extended `write_brief` with `output_contract: bool = False` plus an unconditional stale-`.out.md` unlink before every write. Verified via manual smoke test that default-off output is byte-identical and the footer/unlink behave as specified.
- `plugins/mill/scripts/_review_common.py` (card 3, commit `0ddcb9f2`): `build_tool_rule(mode, agent_mode: bool = False)` now covers all four cells; non-agent cells are untouched/byte-identical; new agent-mode bulk cell avoids the bare "Do NOT request tool calls" clause and grants exactly one `Write`; new agent-mode tool-use cell keeps `Read/Grep/Glob` and adds the same `Write` carve-out; both still forbid `Edit`/git/bash.
- `plugins/mill/unit_tests/test-agent-dispatch.py` (card 4, commit `becacc4e`): added tests for `output_path_for`, footer presence, default-off byte-identity, and stale-`.out.md` truncation (both `output_contract` states). Also fixed two pre-existing unrelated ruff `F541` nits caught by the whole-file lint pass.
- `plugins/mill/unit_tests/test-review-common.py` (card 5, commit `6ce6705b`): added the four-cell `build_tool_rule` matrix test, pinning the non-agent cells as literals, confirming the `agent_mode=False` default, and covering the agent-mode Write carve-outs and forbidden-tool assertions.

Verify: ran `PYTHONPATH= uv run --project plugins/mill python plugins/mill/unit_tests/run-all.py --only test-agent-dispatch.py test-review-common.py test-agent-mode-dispatch.py test-implementer-common.py test-millpy-implement.py test-millpy-fix.py test-millpy-merge-in-subagent.py`. 6 of 7 suites pass. `test-millpy-merge-in-subagent.py::test_9_missing_mode` fails, but this is a confirmed pre-existing, unrelated bug: `git diff main -- plugins/mill/scripts/millpy-merge-in-subagent.py plugins/mill/unit_tests/test-millpy-merge-in-subagent.py` shows zero diff (neither file touched by this task at all — neither is even in the overview's "All Files Touched" list), the test fails identically in isolation regardless of ordering, and `git log` shows it was introduced by unrelated prior commits (`30feba1d`, `6ffd9901`, etc.) well before this task's branch. Per the brief's Verify section, `stuck_type: verify` is explicitly reserved for failures confirmed *not* pre-existing, so this does not block a success report; fixing it would also require editing a file outside this batch's (and this whole task's) declared scope.

{"status":"success","commit_sha":"6ce6705b6ee3514670d65a67cbcc3ff5c0da8ce7","session_id":"a50219e8-7daa-402c-bb4d-2ba060b6a6c4"}
