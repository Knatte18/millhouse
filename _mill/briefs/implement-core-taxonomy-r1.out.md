All 8 of 8 cards in the batch are committed (cards 1 and 6 combined into one commit since both touched the same dataclass section of `_review_common.py`; every other card has its own commit). The batch's `verify:` command passes cleanly. No uncommitted tracked changes remain.

Summary of work:

- `plugins/mill/scripts/_review_common.py` — added `BLOCKING_SEVERITY`/`NIT_SEVERITY`/`RECOGNIZED_CLASSES` constants, the `Finding` dataclass, `extract_findings` (single-pass dual-mechanism extraction), `resolve_blocking_classes` + `DEFAULT_BLOCKING_CLASSES`, `apply_blocking_ceiling`, `rewrite_demoted_findings` (plus its heading/yaml rewrite helpers), widened `parse_blocking_count`/`count_unrecognized_severity_findings` regexes to accept an optional `:<class>` suffix, `parse_verdict` now normalises historical `GAPS_FOUND` to `REQUEST_CHANGES` at both the fenced and unfenced check sites, `ReviewResult.findings`, and rewired `finalize_scope` (new keyword-only `blocking_classes` param, ceiling + rewrite before write, verdict recomputed from surviving blocking count, `findings` key added to the returned dict).
- `plugins/mill/unit_tests/test-review-common.py` — updated the one assertion that depended on `parse_verdict` returning `GAPS_FOUND` verbatim.
- `plugins/mill/unit_tests/test-review-finalize.py` — verified unaffected; no edits needed (confirmed by running it both before and after the `_review_common.py` changes).
- `plugins/mill/unit_tests/test-review-class-taxonomy.py` — new file, 20 tests covering every Requirements bullet in card 8 (ceiling table per stage, demote-only, dual-mechanism demotion rewrite on disk, re-read round trip, verdict derivation, unknown-class handling, no-double-count regression, mixed-format documents, cross- and same-mechanism dedup in both directions, `resolve_blocking_classes` defaults, `GAPS_FOUND` normalisation).

`{"status":"success","commit_sha":"3f01fe69f9f363cc95877f8f75d75844903e1f81","session_id":"e30c1a64-fbe4-4f97-97f3-ad539e2fd640","cards_done":[1,2,3,4,5,6,7,8]}`
