# Plan fix — round 1

```yaml
slug: mill-misc-fixes-7
round: 1
source_review: _mill/reviews/20260512-172836-plan-review-r1.md
date: 2026-05-12T17:30:45Z
```

## Fixed

- `_mill/reviews/20260512-172836-plan-review-r1.md` — `[BLOCKING] Broken Windows junction bypasses idempotency guard in Card 5` — replaced the outer guard `link_path.exists() or link_path.is_symlink()` with `os.path.lexists(str(link_path))` in Card 5's prescribed code block (mirrors `_junction.remove`'s own lexists-based guard at line 162). Added an explicit `import os` instruction to the Edits section's import-block guidance for `_setup.py`. Updated the Observable-Behaviour bullet for "broken junction" to spell out why `lexists` fires where `exists()/is_symlink()` would silently bypass.
- `_mill/reviews/20260512-172836-plan-review-r1.md` — `[NIT] Card 2 references non-existent test-wiki.py "invocation list"` — rewrote Card 2's test-extension instructions to match the actual `test-wiki.py` pattern (inline `try / except` blocks inside `main()`, lettered cases `# --- (X) ... ---`, `ok(name)` / `fail(name, exc)` helpers — NO function-list dispatch). Removed every reference to a non-existent "test list" / "peers" / "registered in main()'s invocation list" wording. Made the call-site requirement explicit: use `ok(...)` / `fail(...)` helpers, not raw `print("PASS: ...")`.
- `_mill/reviews/20260512-172836-plan-review-r1.md` — `[NIT] Card 8 describes wrong test-status.py counter pattern` — rewrote Card 8's test-extension instructions to match the actual `test-status.py` pattern (single top-level `try:` block, raw `assert` + `print("PASS: ...")`, NO `passed += 1` / `failed += 1` counters, NO `ok` / `fail` helpers). Updated both the introductory paragraph and the trailing "counters" paragraph; pointed the implementer at the exact line range (148–207) of the surrounding Tests 1–3 as the idiom to mirror.

## Pushed Back

(none)
