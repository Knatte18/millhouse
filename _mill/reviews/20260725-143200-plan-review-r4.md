MILL_REVIEW_BEGIN
# Review: mill-go CLI dispatch robustness, wiki-RPC stalls, and briefs_dir path-resolution gaps — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/
date: 2026-07-25
```

## Findings

### [BLOCKING] Card 7's skip_slug_validation=True in-place tests reuse a non-git M2 fixture
**Location:** batch `paths-skip-slug-validation`, Card 7, requirements 1 and 4
**Issue:** Card 6's `skip_slug_validation=True` branch confirms in-place mode via `_pygit2_util.current_branch(git_root)` with no daemon fallback (unlike the unconfirmed-daemon-fails path). Card 7 instructs reusing the existing M2 fixture (`git_root = tmp_path / "hub"; git_root.mkdir()` — a bare directory, no `git init`) for both the `resolve_active_worktree` and `resolve_active_hub` skip_slug_validation=True tests. Against a non-git `git_root`, `current_branch` raises `GitOpsError` (caught → `branch=""`), `marker_slug` never equals `slug`, the in-place check is skipped, and the function falls through to check `container_path/"wts"/slug` — which this fixture never creates — raising `ActiveWorktreeNotFound` instead of returning `git_root`. The test as specified will not pass.
**Fix:** Have Card 7 tests 1 and 4 build a real git repo on the confirming branch (e.g. via `_test_helpers.init_minimal_git_repo` + `checkout_new_branch`, as `_make_active_marker` already does for the M1 tests) at `git_root`, not the plain-mkdir M2 fixture used by the daemon-mocked (skip_slug_validation=False) tests.

### [NIT] `_pygit2_util` import note in Card 6 slightly overstates uncertainty
**Location:** batch `paths-skip-slug-validation`, Card 6
**Issue:** The card hedges "add `import _pygit2_util` at module scope if not already imported" — it is already imported at `_paths.py` line 98, so this is a no-op instruction, not a real ambiguity.
**Fix:** Minor wording only; no functional risk since the hedge already covers the actual (already-imported) case correctly.

## Verdict

REQUEST_CHANGES
Card 7's two skip_slug_validation in-place tests will fail as specified; the M2 fixture lacks the real git checkout the new code path requires.
MILL_REVIEW_END
