# Discussion-fix round 7

## Fixed

- `20260813-080517-discussion-review-r7.md` — "Testing section overclaims drift-guard catches arity mismatches" (demoted from BLOCKING): reworded the Testing section to state `test-skill-helper-drift.py` only catches typo'd/renamed/unresolvable helper references, not argument-count or argument-order drift; explicitly flagged `_config.load_config(hub_root, worktree_root)`'s 2-argument order as still needing manual verification.
- `20260813-080517-discussion-review-r7.md` — "`blocked_reason` example list mismatches source": corrected the #832 Decision's illustrative `blocked_reason` value list — `"plan-validate non-progress"` was never actually written to `status.md` (that halt is ephemeral, never routed through `_status.set_blocked`); replaced with the actual two other persisted values (`"non-progress round {N}"`, `"plan review usage error: <message>"`) and noted why the ephemeral halts structurally can't reach the new `blocked` row.

## Pushed Back

(none)
