# Plan fix: round 3

```yaml
slug: mill-go2-fork-fixer
round: 3
source: _mill/reviews/20260811-142806-plan-review-r3.md
```

## Fixed

- `20260811-142806-plan-review-r3.md` — "[BLOCKING:design] Fork-fallback log doesn't reconstruct
  fork_attempted for the common crash-resume case": took the reviewer's first offered fix and
  narrowed the claim. Shared Decision `fork-fallback-log-is-control-flow-state` now carries an
  explicit "Scope of the guarantee" clause stating that the reader keeps an already-recorded fallback
  cold across a resume and does not make forking idempotent across every crash;
  that a crash while a fork is still in flight legitimately re-forks, because no terminal failure was
  classified and neither the fallback contract nor the one-retry budget has been consumed;
  and that discussion Decision `fork-attempted-must-survive-resume` accepts one narrow true gap of a
  single commit's width. Batch 1 card 2's docstring requirement was narrowed to match.
  The reviewer's second offered fix — a pre-dispatch "fork dispatched" marker — is not taken:
  discussion Decision `fork-attempted-must-survive-resume` rejects exactly that remedy, because a
  pre-dispatch write "would log fallbacks that never happened and corrupt the very measurement
  `record-the-fallback` exists to produce", and fork-death rate is the experiment's primary output.
- `20260811-142806-plan-review-r3.md` — "[BLOCKING:design] `_check_fork_override`'s assertions don't
  distinguish scenario 2 of the 5 it claims to catch": added a fifth assertion to batch 2 card 3
  requiring that no line of `mill-go2`'s extracted body is exactly `(none)` after stripping, with its
  own distinct `FAIL:` string, and spelled out why the existing not-exactly-`"(none)"` equality does
  not cover an override appended below a leftover placeholder.

## Pushed Back

(none)
