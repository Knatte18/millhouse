# Discussion-fix round 2

Source review: `_mill/reviews/20260821-090207-discussion-review-r2.md` (verdict APPROVE, 1 NIT finding, not demoted, class `consistency`).

## Fixed

- **de-briefing literal text doesn't reconcile with existing two-bullet structure** — added an explicit "Implementation note" to the "de-briefing text" Decision stating that `mill-go2/SKILL.md`'s current two-bullet structure ("Fork every fresh attempt" + separate "De-briefing (prompt opening)") has no slot for post-brief-pointer text, and that applying the bookended decision requires collapsing both bullets into one prompt-construction step emitting the full literal string. The reviewer itself suggested no fix was needed beyond the existing Testing-section cross-check instruction, but the decision tree's default is to fix rather than rely on an indirect cross-check catching it — the one-line clarification removes the ambiguity at the point of decision instead.

## Pushed Back

(none)
