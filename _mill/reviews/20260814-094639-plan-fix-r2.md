# Plan-fix round 2

Source review: `20260814-094441-plan-review-r2.md` (REQUEST_CHANGES, 1 BLOCKING, 3 NIT).

## Fixed

- [BLOCKING:design] "\"Inline comments\" section left byte-identically duplicated across golang and csharp" — added the "If the code needs a 'what' comment, the code itself is unclear — refactor instead" tail to `code-comments/SKILL.md`'s Purpose-not-mechanism section (card 1), and removed the fully-redundant `## Inline comments` section from both `golang-comments/SKILL.md` (card 2) and `csharp-comments/SKILL.md` (card 4) — python's version was already language-specific and untouched. Added a card 5 check confirming both sections are absent.
- [NIT:consistency] "Python's sub-function-decomposition bullet overlaps the shared 'many comments needed' corollary" — shortened python's bullet (card 3) to reference `code-comments`' corollary and state only the fallback delta (single inline comment as a last-resort exception).
- [NIT:consistency] "Card 5's grep-based verification for python omits literal grep commands" — spelled out the three literal `grep -n` invocations for python's removed content, matching the golang example's specificity.
- [NIT:scope] "Csharp compile-check edge case has no cleanup instruction" — card 4's optional CS1587 compile check now names an explicit scratch path (`.scratch/csharp-header-check/Program.cs`) and instructs deleting the directory immediately after the check.

Also fixed a validator-flagged `context-completeness` finding introduced by the grep fix above (`_mill/discussion.md` referenced in card 5's Requirements but not listed in its Context:) — added it to card 5's Context: list.

## Pushed Back

(none)
