# Plan-fix report — round 2

```yaml
round: 2
fixer: mill-plan
source_review: 20260525-134127-plan-review-r2.md
date: 2026-05-25
```

## Fixed

- `20260525-134127-plan-review-r2.md` `[BLOCKING] Card 4 puts merged_body in wrong V3 field` — Card 4's `multi_select_groom_then_claim` rewrite now maps V2 `merged_body` → V3 `brief` (Home.md inline text) and V2 `proposal_body` → V3 `body` (proposal-file content; daemon renders to `proposal-<slug>.md`). The upsert dict is built conditionally: `"brief": merged_body` always, plus `"body": proposal_body` only when `has_proposal and proposal_body is not None`. Removed the explicit `proposal_path.write_text(proposal_body, ...)` block at `_spawn_core.py:520-523` (would race with daemon write). Added grep guards `grep -n '"body": merged_body' _spawn_core.py` and the V2→V3 field semantics paragraph cited from the discussion's API table.
- `20260525-134127-plan-review-r2.md` `[NIT] Card 4 doesn't enumerate _task_to_dict call-site deletion` — Added explicit call-site deletion step (5 call sites verified by grep at `_spawn_core.py:183, 304, 433, 535, 572`). Each call-site is a list-comprehension reassignment that becomes redundant because `wiki.list_tasks_brief` already returns `list[dict]`. Added `grep -n '_task_to_dict' plugins/mill/scripts/_spawn_core.py` to the zero-match guards.
- `20260525-134127-plan-review-r2.md` `[NIT] Card 8 references wrong helper for wiki fixtures` — Replaced the `_test_helpers._make_task_worktree` reference with `_setup_tempfile_wiki(home_md_content)` at `test-fold.py:43` (verified body: it sets up a bare-clone `origin` remote, exactly the daemon-mediated `upsert_task` precondition). Noted that the card-11 origin fix does not apply here.
- `20260525-134127-plan-review-r2.md` `[NIT] Card 9 doesn't name [s] fast-path tests for deletion` — Enumerated the two functions explicitly: `test_pick_task_single_fast_path_s` (`test-spawn-core.py:230`) and `test_pick_task_single_or_multi_fast_path_s` (`test-spawn-core.py:712`). Both must also be removed from the `tests = [...]` runner list. Clarified that `test_pick_task_single_slug_matching_s` (line 197) stays because it exercises `--slug` mode, not the implicit `[s]`-fast-path.

## Pushed Back

_(none)_
