# Discussion fix report -- round 3

```yaml
slug: claude-p-wrapper
review_round: 3
review_file: 20260515-110826-discussion-review-r3.md
verdict: APPROVE (with NOTE)
```

## Fixed

- "Poll-loop termination condition ambiguous" (NOTE, `### Dual marker
  protocol` decision in 20260515-110826-discussion-review-r3.md). The
  decision section now states explicitly that the parser is the loop's
  termination authority -- success returns the slice, `MarkerNotFoundError`
  means continue-polling, wall-clock exhaustion of
  `RESPONSE_POLL_TIMEOUT_S[mode]` is the failure exit. The idle-prompt
  signal is documented as NOT a load-bearing gate, since a status line
  ("Cogitated for Ns") may render after the end marker before the input
  prompt clears.

## Pushed Back

(none)
