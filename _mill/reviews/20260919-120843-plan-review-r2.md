MILL_REVIEW_BEGIN
# Review: _plan_validate.py context-completeness: path-token exemption list gaps — holistic

```yaml
duration_s: 376.0
verdict: REQUEST_CHANGES
reviewer_model: sonnethigh
reviewer_self_id: claude-sonnet-5 (Sonnet 5)
reviewed_file: plan/
date: 2026-09-19
```

## Findings

### [NIT:consistency] New exemption guards mis-indented by 2 spaces
**Demoted-from:** BLOCKING
**Location:** batch 1, cards 1, 2, 3 (`01-context-completeness-exemptions.md`).
**Issue:** Verified against `_plan_validate.py` lines 2356-2361: the anchor block (`_is_contrast_citation_exempt` guard) and `if is_path_shaped:` both sit at 20-space (comment/`if`) / 24-space (`continue`) indent. Each card's own new guard block is specified at 22-space (comment/`if`) / 26-space (`continue`) indent — 2 spaces deeper than every existing per-token guard in that `for match in backtick_re.finditer(line):` body. Inserted verbatim, 22 does not match any open indentation level (20 for the loop body, 24 from the just-closed `if`), so Python raises `IndentationError` in all three cards.
**Fix:** Re-indent all three new guard blocks to 20 spaces (comment/`if`) and 24 spaces (`continue`), matching the surrounding exemption chain.

### [BLOCKING:scope] Cards 4/6 cite `_plan_validate.py` constants outside Context/Edits
**Location:** batch 2, card 4 ("...actually exists in `_OWNERSHIP_VERB_FORMS`") and card 6 ("a second/third `_OUTPUT_VERB_FORMS` entry", "...actually exists in `_OUTPUT_VERB_FORMS`").
**Issue:** Both constants are defined in `_plan_validate.py` (batch 1, cards 1/3) and are ALL-CAPS-with-underscore tokens that qualify under `_symbol_candidate_shape` (verified at `_plan_validate.py` line ~1926-1939: underscore/uppercase qualifies a bare identifier). Neither card's `Context:` (none) nor `Edits:` (`test-plan-validate.py` only) lists `_plan_validate.py`. None of the listed exemptions (quoting, citation/contrast/escape-marker, forward-Creates, gitignored, out-of-repo, trailing-slash) covers a bare parenthetical rationale mention. It happens to escape `_plan_validate.py`'s own live check today only because these constants don't exist in the repo until batch 1 lands (zero-match resolution, per the docstring at lines 2210-2213) — a coincidence of ordering, not a stated exemption.
**Fix:** Drop the constant name from the parenthetical, or add `plugins/mill/scripts/_plan_validate.py` to these cards' `Context:`.

### [NIT:consistency] Card 4's "last context-completeness test" claim is wrong
**Location:** batch 2, card 4 (`02-context-completeness-exemptions-tests.md`).
**Issue:** Verified in `test-plan-validate.py`: `test_check_context_completeness_dirty_quoted_material_prose_after_fence_closed` (line 5062) is not the last `context-completeness` test. `test_check_context_completeness_clean_fence_aware_extraction_fake_header_inside_fence` (5109), its "genuine_header_after_fence" pair (5156), and two `escape_marker` tests (5218, 5257) — all filtering on `check == "context-completeness"` — appear later, ending at the `main()` registration line just before the `# requirements-quote-indent-drift check` comment (~line 10338). The literal anchor instruction is still executable (the named function exists exactly once), so this doesn't block implementation, but the new ownership tests land mid-block rather than at the block's true end as the card's rationale claims.
**Fix:** Correct the "last existing context-completeness test" claim, or retarget the anchor to `test_check_context_completeness_dirty_escape_marker_phrase_removed` if end-of-block placement is actually intended.

## Verdict

REQUEST_CHANGES
Mis-indented guard blocks (batch 1) break the module; two cards cite ungranted constants (batch 2).
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 1._
MILL_REVIEW_END
