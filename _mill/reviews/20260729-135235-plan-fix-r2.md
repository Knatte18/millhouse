## Fixed

- `_mill/reviews/20260729-135151-plan-review-r2.md`: "[NIT] Card 4 omits MagicMock import despite needing DirEntry stand-ins" — Card 4's Requirements now import `from unittest.mock import MagicMock, patch` (was `patch` only), matching Card 3's existing import pair, with a note that `MagicMock` is needed to construct the fake `DirEntry`-shaped objects for cases 1-2.
- `_mill/reviews/20260729-135151-plan-review-r2.md`: "[NIT] Inner per-entry log message in `_junction.py` reuses directory-scan wording" — Card 2's per-entry guard message changed from `[junction] WARNING: vanished entry scanning {ep}; skipping` to `[junction] WARNING: vanished entry: {ep}; skipping`, distinct from the directory-level guard's "scanning" wording since `ep` is a single entry, not a directory being scanned. The overview's matching Shared Decision quote was updated in step with this.

## Pushed Back

(none)
