# Plan fix report — round 2

Source review: `_mill/reviews/20260904-094249-plan-review-r2.md`

## Fixed

- `[BLOCKING:scope]` "New tests never registered in test-plan-validate.py's `main()` list" — added an explicit Requirements step to card 2 (step 7) and card 3 instructing the implementer to append every new `test_check_context_completeness_symbol_*` function reference to `main()`'s hard-coded `tests = [...]` dispatch list, grouped under a comment placed right after the existing context-completeness block.
- `[NIT:consistency]` "Test file's top-of-file 'Check coverage' docstring not updated" — added card 2 step 8, extending `test-plan-validate.py`'s own module docstring `context-completeness` summary line to mention symbol-shaped tokens, mirroring card 1's equivalent `_plan_validate.py` docstring update.

## Pushed Back

(none)
