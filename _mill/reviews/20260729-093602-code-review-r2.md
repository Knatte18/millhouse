MILL_REVIEW_BEGIN
# Review: mill-start: tracked _mill/ files disappear from the working tree mid-review-loop; existing safeguard covers only status.md — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-07-29
```

All five batches were checked end-to-end against `00-overview.md`'s Shared Decisions and each batch file's cards.

- `_treeguard.check_and_restore` (`plugins/mill/scripts/_treeguard.py`) implements the exact signature, `hub_prefix` rebase, deletion-status-code partition (`" D"`/`"D "` only), single-shot `git checkout HEAD --`, per-path disk-existence verification, partial-vs-total-failure reporting, and unconditional still-missing stderr diagnostic that Card 1 specifies — matches `_cleanliness.revert_out_of_scope_drift`'s established primitives (`_pygit2_util.status_porcelain` + `_subprocess_util.run`) without importing the private `_cleanliness._rebase_onto_hub` closure.
- `plugins/mill/unit_tests/test-treeguard.py` covers all 10 scenarios from the batch (no-deletion, single/multi delete, staged deletion, untracked-alongside-deletion, uncommitted-modification-alongside-deletion, total-failure, nested-hub rebase, `git_root=None`, partial restore) using the correct single-try/except runner shape (per `test-status.py`'s pattern, explicitly not `test-cleanliness.py`'s accumulate-and-report shape).
- `_status.append_recovery_log` / `_find_recovery_log_block` (`_status.py:1034-1141`) mirror `_write_batches`'s lazy-insert pattern but append-only into the fenced block, exactly as Card 3 specifies; `status-discussing.md` template is correctly left untouched. `test-status.py` adds the 5 required scenarios (lazy creation, append-only, multi-path comma-join, non-disturbance of yaml/Timeline, malformed-section ValueError x2).
- All three wiring batches (`mill-start`, `mill-plan`, `mill-go` SKILL.md) insert the tree-guard paragraph/checkpoints at every location the cards specify, always passing `git_root=git_root` (both locals are bound in each skill's own Path Setup before use), never inside the shared `## Agent-mode dispatch` section, and never touching the subprocess/psmux branches or the out-of-scope fixer/Implement/merge-in dispatches. mill-go's per-batch loop gets the specified commit-ordering fix (`reviewing-{batch_name}-r{N}` append_phase followed by an immediate commit) while the Holistic loop (already committing immediately) correctly receives only the tree-guard checkpoint, no ordering change.
- The prior non-blocking "duplicate `read` entry" item (`test-status.py:17`/`:24`) is no longer present — the import block now lists `read` exactly once (line 23). No escalation needed; not re-flagged.
- No out-of-plan files, no duplicated helper logic beyond the deliberate, plan-sanctioned reimplementation of `_rebase_onto_hub` (justified by `_cleanliness`'s closure being private), no mutable-default/import-side-effect/path-separator pitfalls found — `as_posix()` is used consistently for the hub-relative rebase, matching the existing convention.

## Verdict

APPROVE
Implementation faithfully realizes every batch and shared decision; tests are thorough; no blocking issues found.
MILL_REVIEW_END
