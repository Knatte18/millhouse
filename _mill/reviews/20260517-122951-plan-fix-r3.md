# plan-fix round 3 — mill-branch-slug-fixes

Source review: `_mill/reviews/20260517-122846-plan-review-r3.md` (verdict: APPROVE, 3 [NIT] findings)

## Fixed

- [NIT] Test registration wording ambiguous for monolithic main() files (Batch 1 Card 3, Batch 2 Card 6) — replaced "Register the new test in `main()`" with "Call the new test function from `main()`" in both cards, with explicit notes that `test-status.py` and `test-review-cli.py` use monolithic inline `main()` patterns (not the list-based runner from `test-marker.py`).
- [NIT] Unused `print_error` import after D2 wiring (Batch 2 Card 5) — added an explicit "Drop the `print_error` import" sub-paragraph instructing the implementer to remove `print_error` from each CLI's `from _review_cli import ...` line. `print_error` itself stays defined in `_review_cli.py`.
- [NIT] mill-plan SKILL step 1.5 implicit millpy-bg site (Batch 5 Card 10) — clarified the Card 10 instruction to handle both fenced bash blocks AND prose-only `millpy-bg` invocation sites. Search must cover both `PYTHONPATH=.*millpy-bg.py` patterns and prose mentions that trigger an invocation.

## Pushed Back

(none)
