# Plan-fix report — round 6

```yaml
round: 6
fixer: mill-plan
source_review: 20260525-143001-plan-review-r6.md
date: 2026-05-25
```

## Fixed

- `20260525-143001-plan-review-r6.md` `[NIT] Card 6 rename instructions redundant for terminal/vscode CLIs` — Card 6's "Local-variable naming collision" block now scopes the rename to `millpy-inspect.py:45` and `millpy-status.py:24` ONLY (verified those two have `wiki = _paths.resolve_wiki_path(...)`). `millpy-terminal.py` and `millpy-vscode.py` already use `wiki_path` (verified at terminal.py:52, 55, 57, 105 and vscode.py:68, 77, 95, 116) — explicit "no rename needed" note added for those two files. Per-file specifics block updated to drop the rename step from terminal and vscode entries.
- `20260525-143001-plan-review-r6.md` `[NIT] Card 8 wiki-failure test patches global module state via direct assignment` — Card 8's failure-path patching section rewritten to prescribe `unittest.mock.patch.object(mill_fold.wiki, "upsert_task", side_effect=WikiPushError(...))` as a context manager — NOT the manual try/finally + direct attribute assignment shape from the earlier draft. Concrete code block included. The old `orig_wcp`/`_failing_write`/manual-restore scaffolding is explicitly deleted; `_failing_upsert` helper is gone (`side_effect=WikiPushError(...)` does the job directly).

## Pushed Back

_(none)_
