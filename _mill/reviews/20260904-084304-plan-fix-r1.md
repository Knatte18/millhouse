```yaml
round: 1
source_review: _mill/reviews/20260904-084144-plan-review-r1.md
```

## Fixed

- `20260904-084144-plan-review-r1.md` — "[NIT:consistency] \"Do NOT set phase: done\" sentence has conflicting placement across Cards 3/4": Card 3 now deletes the sentence from its original single-occurrence position instead of keeping it there; Card 4 explicitly duplicates it into each of its two new halt branches, stating Card 3's placement is superseded.
- `20260904-084144-plan-review-r1.md` — "[NIT:consistency] Card 5's stated \"four total call sites\" doesn't match its own 1+3+1=5 breakdown": corrected "four" to "five" to match the enumerated 1+3+1 breakdown.
- `20260904-084144-plan-review-r1.md` — "[NIT:consistency] Card 7's \"reuse that same insertion point\" is ambiguous for a relocated halt": clarified Card 7 writes a fresh notify/lock-release call-pair at the new (earlier) halt site, reusing only the shape/pattern from batch 2's Card 5, not a single shared physical code block.
- `20260904-084144-plan-review-r1.md` — "[NIT:scope] Card 8's `Context:` lists `handoff.md`, which nothing in its Requirements references": dropped the stray `handoff.md` Context entry — Card 8's Requirements only reference `SKILL.md` sections (step 3(b)/(c) is itself in `SKILL.md`, not `handoff.md`).

## Pushed Back

(none)
