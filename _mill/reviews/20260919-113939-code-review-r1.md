MILL_REVIEW_BEGIN
# Review: mill-finalize/mill-merge-in citation-scan pathspec `:!_mill` fails on git 2.53+ — holistic

```yaml
duration_s: 57.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-19
```

Both cards' substitutions are present and byte-exact: `plugins/mill/skills/mill-finalize/SKILL.md:87` and `plugins/mill/skills/mill-merge/SKILL.md:287` both read `':(exclude)<task_dir>' ':(exclude)plugins/**/SKILL.md' ':(exclude)plugins/**/unit_tests/**' ':(exclude)plugins/**/integration_tests/**'`, matching the plan's required post-fix text verbatim, with the preceding `git -C <worktree> grep -InE ...` line and surrounding prose left untouched as required.

A repo-wide grep for the short-form `:!` pathspec turned up zero remaining matches in either edited file or in `mill-merge-in/SKILL.md`, confirming the Shared Decision "scope excludes mill-merge-in" holds (no citation-scan pathspec exists there to begin with) and that the fix was applied completely with no stray short-form instances left behind in either edited file.

`## All Files Touched` in the overview lists exactly the two files present in the manifest; no out-of-plan files, no cross-batch contracts (single batch, two independent one-line edits), and no shared-state or interface concerns apply to this doc-only fix. The plan's stated rationale for omitting unit/integration tests (external git-binary behavior, not mill code) is unchanged from the discussion and not re-litigated here per the out-of-scope note.

## Verdict

APPROVE
Both citation-scan snippets correctly switched to long-form `:(exclude)` pathspecs; no stray short-form instances or out-of-plan files found.
MILL_REVIEW_END
