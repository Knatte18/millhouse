MILL_REVIEW_BEGIN
# Review: mill-merge: run the deterministic path as one script — holistic

```yaml
duration_s: 356.6
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/
date: 2026-09-26
```

Checked against source: `_subprocess_util.run`, `_marker.task_data`/`MarkerError`, `_inplace.is_inplace`, `_parent_branch.resolve`/`check_liveness`/`resolve_dead_parent`, `_status.read_full`/`append_phase`/`set_parent_branch`/`set_blocked`, `_pr_state.resolve_pr_state`, `_archive_tag.create_or_resolve`, `_notify.notify`, `_timestamp.now_utc_iso`, `_paths.resolve_active_hub`/`resolve_task_path`/`resolve_worktrees_dir`/`resolve_hub_path`/`resolve_git_root`/`resolve_container_path`/`resolve_wiki_path`, `_config.load_config`, `wiki/_client.get_task`/`set_phase`. Every `Ops` method signature and return-shape referenced across cards 1-7 matches the real function it wraps.

Card 9's four target files were grep-verified against the batch-5 gate pattern (`mill-merge[^-].{0,25}Step [0-9]|mill-merge/SKILL\.md.{0,40}Step [0-9]|Card 1 fix`): every current match in `mill-merge-in/SKILL.md`, `mill-status/SKILL.md`, `mill-go-base/SKILL.md`, and `CLAUDE.md` is covered by a card 9 bullet, and none of the specified replacement texts reintroduce a match — the gate will pass post-edit.

DAG, global step numbering (cards 1-10), `## All Files Touched` (matches the Edits/Creates union across all cards), `Ctx`/`MergeOptions`/`STEPS` field usage across cards, and Context/Edits completeness for every card's named functions all check out. No `Moves:` anywhere, so the rename-mechanic rule doesn't apply. No vague `Requirements:` prose — every card names the specific function, dataclass, or CLI flag being added.

No findings.

## Verdict

APPROVE
Source-verified signatures, cross-batch references, and the batch-5 grep gate all check out; no blocking issues found.
MILL_REVIEW_END
