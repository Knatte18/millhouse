# Review: Replace claude -p with psmux-routed LLM dispatch — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-05-31
```

## Findings

### [NIT] Scenario (b) uses 1 `esc to interrupt` capture vs plan's 2
**Location:** `plugins/mill/unit_tests/test-claude-sub.py` (Scenario b, ~line 740)
**Issue:** Plan spec says "first two captures return 'esc to interrupt'" but test uses `side_effect=["esc to interrupt\n", "? for shortcuts\n", "? for shortcuts\n"]` — one, not two. Phase 1 breaks on the first match so the second would never be consumed; the test is correct but diverges from the plan description.
**Fix:** Either update the plan description to match (preferred), or add a second `"esc to interrupt\n"` element and a fifth monotonic value to prove Phase 1 iterates correctly; the behaviour is identical either way.

## Verdict

APPROVE
All four batches implemented correctly; shared decisions applied consistently; cross-batch contracts and test coverage are sound.