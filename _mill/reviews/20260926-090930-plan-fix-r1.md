# Plan fix r1

## Fixed

- r1 review, "Card 4 grep gate is prose-only, unenforced": card 4 now names the explicit `grep -n -i notification` command.

## Pushed Back

- r1 review, "Cards 9-11 Context lists a doc the edits don't need": the plan-batch template requires a non-empty `Context:`, and the contract doc is the reference for the reworded comments; no other file is a better candidate.
