MILL_REVIEW_BEGIN
# Review: pipeline.autonomous_mode warns as unknown config key on every mill invocation — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 4.5
reviewed_file: plan/
date: 2026-08-02
```

## Findings

No findings. Verified against source:

- `warn_unknown_keys` at `_config.py:113`, `deprecated_keys` set at `_config.py:122` — line references in Card 1 are exact; the prescribed edit (`{"llm.claude.psmux.via_psmux", "pipeline.autonomous_mode"}`) is a correct, minimal, isolated change.
- Card 2's inline template text is byte-identical to `_setup_plugin_template`'s content (`test-config.py:56-84`) plus the added `pipeline:\n  auto_merge: true\n` block; the stated rationale (recursion needs a matching top-level dict key to descend rather than flag the whole `pipeline` path) is correct per `walk_unknown_keys`'s logic (`_config.py:89-110`).
- Insertion point is exact: `test_via_psmux_does_not_trigger_unknown_key_warning` ends at line 1347 (`print(...)`), followed by a blank line then the next test at 1350 — new test slots in cleanly.
- `tests` list registration point verified: line 1556 is exactly `test_via_psmux_does_not_trigger_unknown_key_warning,`.
- All helpers referenced (`tempfile`, `Path`, `_git_init`, `_write_yaml`, `_paths`, `_config`, `io`, `patch`) are already imported/defined in `test-config.py`; no new imports needed.
- Single-batch DAG is well-formed (no cycle, file present, no forward deps); `All Files Touched` matches the union of `Edits:` across both cards; `verify:` prefix matches the mandated `PYTHONPATH= ` convention for Python projects.
- Decision alignment: the one `### Decision:` (suppress via `deprecated_keys`, no shim) is faithfully and exclusively implemented — no stray migration/shim logic introduced.

## Verdict

APPROVE
Minimal, source-grounded fix; all line references, template contents, and insertion points verified exactly against current files.
MILL_REVIEW_END
