# Plan Fix Report — round 2

```yaml
source_review: 20260514-092138-plan-review-r2.md
round: 2
```

## Fixed

- `20260514-092138-plan-review-r2.md` / "[BLOCKING] Fixture path resolution unspecified in Card 3" — updated Card 3 artefact construction section to use `FIXTURES = Path(__file__).parent / "fixtures"` and resolve all fixture paths relative to it.
- `20260514-092138-plan-review-r2.md` / "[NIT] `render_prompt` loop-placement note missing for PLAN and CODE artefacts" — added "Note: render inside the reviewer loop — `reviewer_model` changes per reviewer" to the PLAN and CODE artefact bullets in Card 3; restructured the section header to distinguish pre-loop fixture reads from per-loop `render_prompt` calls.
- `20260514-092138-plan-review-r2.md` / "[NIT] Batch 2 verify uses bare `python`; inconsistent with batch 3" — updated batch 2 `verify:` in both `00-overview.md` and `02-bench-script.md` to `uv run --project plugins/mill python ...`.
- `20260514-092138-plan-review-r2.md` / "[NIT] Batch Tests description references junction path `.wiki`" — updated Batch 1 Batch Tests prose to reference `resolve_wiki_path(resolve_git_root())` instead of `Path('.wiki')`.

## Pushed Back

(none)
