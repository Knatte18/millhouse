## Fixed

- `20260729-141606-plan-review-r1.md`: "Batch 1's verify: command can never pass" — `run-all.py` hardcodes `SKIP = frozenset({"test-wiki-sync.py"})`, filtered out of `discovered` before `--only` resolves against it, so `--only test-wiki-sync.py ...` always exits 1 with "unknown test file(s)". Dropped `test-wiki-sync.py` from Batch 1's `verify:` (both `00-overview.md` and `01-wiki-health-check-and-messaging.md`); Card 1's Requirements and Batch Tests now note it is verified standalone (`PYTHONPATH= uv run --project plugins/mill python plugins/mill/unit_tests/test-wiki-sync.py`), matching the file's pre-existing exclusion from `run-all.py`.
- `20260729-141606-plan-review-r1.md`: "Phase 1b Step 1 snippet uses `_paths` without importing it" — verified the literal snippet against the plan file; `_paths` was indeed never imported in that `-c` invocation. Added `import _paths` to Phase 1b Step 1's snippet in Card 10.
- `20260729-141606-plan-review-r1.md`: "Card 9's test file needs `_pygit2_util.py`, not in Context" — verified Card 9's Requirements reference `_pygit2_util.status_porcelain`'s output format without the file in Context. Added `plugins/mill/scripts/_pygit2_util.py` to Card 9's `Context:`.
- `20260729-141606-plan-review-r1.md`: "`_handle_health`'s pull skips the store close/reopen dance" — verified `_render_and_commit_all` (`wiki/_server.py:421-433`) closes/reopens `self._store` around its own `pull()` call specifically for a documented Windows TinyDB-handle lock issue. Card 2's Requirements now specify the identical `self._store.close()` / `finally: self._store.reload()` wrapping around the new debounced `pull()` call inside `_handle_health`.
- `20260729-141606-plan-review-r1.md`: "`hub_root` can resolve to the broken worktree itself" — verified: Phase 1 branches into Phase 1b when *either* `.millhouse/config.local.yaml` *or* `.wiki` is missing, so the `.wiki`-only-missing case leaves a local `.millhouse/config.local.yaml` in place; `_paths.resolve_hub_path()`'s cwd-walk would then return the broken worktree itself as `hub_root`, producing `cwd == old_worktree` in the subsequent `_worktree.move()` call — exactly what Card 7's own docstring warns against. Changed Phase 1b Step 4's `hub_root` resolution from `_paths.resolve_hub_path()` to `_paths.resolve_main_worktree_root(git_root)` (pure git-metadata resolution, never consults cwd's own `.millhouse/`), added an explanatory note in Card 10, and added a docstring requirement to Card 8's `relocate_and_scaffold` clarifying `hub_root` must be the true main-worktree root.

## Pushed Back

(none — all five findings verified accurate against source; all fixed.)
