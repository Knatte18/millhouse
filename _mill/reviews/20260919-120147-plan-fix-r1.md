# Plan fix round 1

Source review: `_mill/reviews/20260919-120026-plan-review-r1.md`

## Fixed

- "`_OWNERSHIP_RE` cannot match its own possessive test case" (BLOCKING) — changed the regex's
  middle group from `\d+\s+(?:'s\s+)?` to `\d+(?:'s)?\s+` in Batch 1, Card 1, so the apostrophe-s
  attaches directly to the digits before the required whitespace, matching "batch 8's fix".
- "Dirty ownership/output tests never make their token resolvable" (NIT, demoted from BLOCKING) —
  added an explicit "create a real `x.py`/`README.md` under `project_root`" instruction to Batch 2's
  three under-specified dirty tests (`test_check_context_completeness_dirty_ownership_no_number_not_exempted`,
  `..._separate_line_not_exempted`, `test_check_context_completeness_dirty_illustrative_output_no_verb_not_exempted`),
  mirroring Card 5's existing phrasing for its own dirty tests.
- "Card 2's stated rationale for `_BACKTICK_RE`'s placement is inaccurate" (NIT) — dropped the false
  "both Card 1's future callers and this function need it" claim; reworded to state the placement is
  purely for a stable location among the batch's new constants, and that only Card 2's own
  `_is_literal_enumeration_exempt` calls `_BACKTICK_RE`.

## Pushed Back

(none)
