# Plan fix — round 2

## Fixed

- `20260904-083115-plan-review-r2.md`: "Card 3's anchor-test description mismatches the actual anchor tests" — reworded Card 3's Requirements to say the anchor tests are matched for control-flow shape only (the anchors hand-inline batch markdown rather than calling `_make_verify_only_batch_text`, verified at source lines ~4548-4564), so using that helper for the new tests' batch-text construction is called out as an intentional simplification, not a literal copy of the anchors' body.
- `20260904-083115-plan-review-r2.md`: "done_gate exemption gets no overview-level test" — added a 6th test, `test_check_verify_full_suite_done_gate_exact_match_overview_level_is_ok`, asserting the done_gate exemption also applies to the overview-level call path (`batch: None`), built via the same `_make_overview(..., overview_verify=...)` + `_make_batch_file(...)` combination `test_check_verify_full_suite_overview_level_dirty` already uses. Updated the card's test count (5 -> 6) and registration instructions accordingly.

## Pushed Back

(none)
