## Fixed

- `20260814-085327-discussion-review-r2.md` — "Retry-shutdown wording vs. Testing D1 call count": reworded the `verify-gate-retry-one-shot-no-sleep` decision to state explicitly that no new/second `dotnet build-server shutdown` call is added for the retry — it reuses the shutdown `_run_verify_gate` already runs unconditionally after every dotnet verify subprocess (#554/#556), matching Testing scenario D1's 3-call sequence (initial run, existing shutdown, retried run). Also updated the corresponding Q&A log entry for consistency.

## Pushed Back

(none)
