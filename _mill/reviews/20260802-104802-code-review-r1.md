MILL_REVIEW_BEGIN
# Review: Verify/build gates leak shell state and ignore nested Go modules — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-02
```

Both batches are fully realized and independently verifiable, matching their cards precisely.

Batch 1 (`plugins/mill/scripts/millpy-fix.py:119`): `_resolve_holistic_verify` wraps each joined command in its own `(...)` subshell exactly as specified, the docstring's `Returns:` clause is updated to describe the new wrap, both call sites (lines 454, 577) are untouched, and the two new tests (`test-millpy-fix.py:2045`, `:2058`) are placed exactly where the card requires and assert the documented output strings verbatim. A pre-existing test at `test-millpy-fix.py:1884` correctly stays unwrapped because it exercises `--scope batch`, which never goes through `_resolve_holistic_verify`.

Batch 2 (`plugins/mill/scripts/_implementer_common.py:985`): `_nested_go_module_root_and_pattern` is placed exactly between `_go_build_pattern` and `_is_qualifying_custom_tag` as required, the walk-up loop checks `affected_dir` before ever advancing to `.parent`, stops at either a found `go.mod` or `project_root`, and returns the byte-identical fallback `(project_root, _go_build_pattern(dir_str))` when no nested module is found — verified against case 66l. Both `go build` call sites (added-tag and removed-tag) are correctly rewired to use `build_cwd`/`build_pattern`, with no other line in `_go_build_tag_retiering_stuck` touched. `_go_gate_mock` in `test-implementer-common.py:46` was widened to a 3-tuple exactly as specified, all 13 unpacking call sites (10 pre-existing across 66a–66i, including the two 66e sub-cases, plus the 3 new ones) were updated mechanically with assertion bodies left untouched. Cases 66j/66k/66l (`test-implementer-common.py:4475-4626`) are inserted immediately after 66i and before `# Case 67`, and correctly cover the nested-root, nested-subpath, and no-nested-module-fallback scenarios with matching `cwd_calls` assertions.

Shared Decisions are honored: neither fix's new tests shell out to a real Go toolchain (bug 1's tests call the pure function directly; bug 2's tests continue using the `go`-prefixed `_subprocess_util.run` mock with real git/filesystem fixtures), and bug 2's fallback path is byte-identical to pre-fix behavior. No out-of-plan files are present; `_plan_validate.py` is included only as read-only Context and was not modified, consistent with the plan. No cross-batch contract issues — the two batches touch disjoint files and have no shared interface.

## Verdict

APPROVE
Both batches match their plan cards precisely; tests are correctly placed and cover the documented scenarios.
MILL_REVIEW_END
