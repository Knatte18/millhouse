# Plan fix report — round 1

## Fixed

- `20260729-073858-plan-review-r1.md` — "[BLOCKING] effort field never forwarded to test_stub provider": confirmed against `_reviewer_single.py`'s `test_stub` dispatch branch (line 51) — it calls `stub.run(prompt_text, session_id=session_id, resume=resume, timeout=timeout)` with no `effort` kwarg, so the two large-prompt-switch-skip test checks (originally Cards 14/16, now Cards 15/17) would always observe `effort: None` regardless of which spec dispatched. Added a new Card 2 to batch `reviewer-override-helper` that forwards `effort=spec.get("effort")` to the `test_stub` branch (mirroring the forwarding every other provider branch in the same function already does), and updated batches `unit-tests-discussion`/`unit-tests-plan` to depend on it, with their Batch Scope sections explaining the dependency. This required renumbering every card after the original Card 1 by one (2→3, 3→4, ..., 17→18) to keep each batch's card numbering internally contiguous with no cross-batch duplicates.
- `20260729-073858-plan-review-r1.md` — "[BLOCKING] Context completeness: cross-file helper references missing from Context": confirmed Cards 13/14 (now 14/15 for discussion) and Card 15 (now 16 for plan) named `test-large-prompt-switch.py`'s and `test-reviewers.py`'s fixture shapes in `Requirements:` without listing those files in `Context:`. Added `plugins/mill/unit_tests/test-large-prompt-switch.py` to the cluster-fixture checks' Context (now Cards 14 and 16) and `plugins/mill/unit_tests/test-reviewers.py` to the gemini-save/restore checks' Context (now Cards 15 and 17); reworded Card 17's check 3 to cite `test-reviewers.py` directly instead of cross-referencing "Card 14 check 3" in a different batch.
- `20260729-073858-plan-review-r1.md` — "[NIT] Card 1 misnames the exception raised on `None.startswith(...)`": corrected Card 1's rationale from `TypeError` to `AttributeError`.
- `20260729-073858-plan-review-r1.md` — "[NIT] Overview's rounds=0 rationale overstates `_review_plan.py::prepare()`'s existing behavior": reworded the `--reviewer bypasses a reviewer: null disablement, not a rounds: 0 disablement` Decision's rationale to correctly scope the `rounds == 0` check per backend — `_review_discussion.py::prepare()` has its own early-raise; both backends' `run()` functions have their own early-return stub; `_review_plan.py::prepare()`'s holistic branch has no such check at all.

## Pushed Back

(none)
