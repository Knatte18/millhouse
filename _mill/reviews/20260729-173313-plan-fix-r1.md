## Fixed

- `20260729-173239-plan-review-r1.md` — "[NIT] Card 2 mischaracterizes existing SKILL.md style": reworded Batch 01 / Card 2's `.get()` rationale to state it introduces `.get()` usage (unlike the existing snippet's direct-subscript reads) rather than falsely claiming it matches existing style, and explained why `.get()` is required here (new fields, avoid `KeyError` against a stale `_archive_tag.py`).
- `20260729-173239-plan-review-r1.md` — "[NIT] Card 8 insertion point isn't actually grouped with other test_load_* entries": corrected Batch 03 / Card 8's registration instruction to insert after `test_load_raises_cluster_use_referencing_cluster` (~line 1162, the true last entry in the main `test_load_*` cluster) instead of after `test_load_falls_back_to_reviewers_yaml` (~line 1172, which sits among unrelated `test_validate_role_refs_*` entries).

## Pushed Back

(none)
