# Discussion fix report — round 5

## Fixed

- `20260729-065805-discussion-review-r5.md` — "`--reviewer` bypass covers `reviewer: null` but not `rounds: 0`": added a clarifying `Note:` to the `reviewer-flag-bypasses-null-disablement` Decision stating that `--reviewer` alone does not bypass a `rounds: 0` disablement (a separate, independent check from `reviewer: null`) — an operator wanting to force a round on a `rounds: 0` scope must also pass `--max-rounds`.

## Pushed Back

(none)
