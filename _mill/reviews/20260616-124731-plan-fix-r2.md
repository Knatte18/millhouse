# Plan fixer report — round 2

Source review: `_mill/reviews/20260616-124526-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 2 NIT).

## Fixed

- [BLOCKING] "Batch 3 verify diverges between overview and batch file" — VERIFIED accurate (introduced by the round-1 fix, which added `test-millpy-fix.py` to the batch file's `verify:` but not the overview Batch Index). Synced the overview batch-3 `verify:` to include `test-millpy-fix.py`; both declarations now match.
- [NIT] "Card 7 does not assert commit_sha enrichment on gated success" — VERIFIED accurate (the success path enriches `commit_sha` at `_implementer_common.py:246`). Firmed up Card 5 to require the gate stuck dict to carry `commit_sha` (current HEAD when `git rev-parse` succeeds) and added a Card 7 assertion that the emitted stuck dict includes `commit_sha`.
- [NIT] "Card 6 verify resolution untested in the CLI test files" — VERIFIED accurate. Took the threading-test option: extended Card 7 to add a `test-millpy-implement.py` case (the file is a `unittest.TestCase` with a mocking `setUp`) that patches `_implementer_common.finalize_from_output` and asserts the `--stage finalize` branch passes the batch's resolved `verify:` as `verify_cmd`. Added `test-millpy-implement.py` to Card 7 Edits and the overview's All Files Touched. The `millpy-fix.py` threading uses the identical `.get("verify")` + pass-through pattern and is covered by the shared gate tests plus its `test-millpy-fix.py` regression run (the reviewer's explicitly-offered acceptance for the fix-path CLI), so no separate fix-path threading case is added.

## Pushed Back

- (none)
