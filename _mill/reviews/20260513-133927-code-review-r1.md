# Review: (A) — Add status_md to paths config + refactor 14 callsites — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmedium
reviewed_file: plan/ + source
date: 2026-05-13
```

## Findings

No findings. The implementation is complete and correct across both batches.

**Batch 1 verification:**
- `wiki/config.yaml` and `wiki-config.yaml` both have `status_md: _mill/status.md` under `paths:` with matching alignment. Template mirrors production. ✅
- `test-paths-status.py` covers all five cases with correct `contextlib.redirect_stderr` usage, PASS lines matching `test-paths.py` style, and auto-discovery by `run-all.py`. ✅
- `_paths.status_path` has correct `KeyError` guard for both missing `paths` key and missing `status_md` subkey, one-line docstring, `__all__` entry adjacent to `resolve_task_path`, and module-docstring Public API entry. ✅

**Batch 2 verification:**
- `write_initial_status` signature correctly appends `*, cfg: dict`; body uses `_paths.status_path`; git-add path is computed via `relative_to().as_posix()`. ✅
- Both callers (`millpy-spawn.py`, `millpy-claim.py`) pass `cfg=cfg`; dry-run prints use `_paths.status_path(worktree_path, cfg)`. ✅
- `millpy-inspect.py` and `millpy-status.py` bugs fixed: `wt_path / "status.md"` → `_paths.status_path(wt_path, cfg)`. ✅
- `_review_code.py`: `import _paths` added; broken `resolve_path("status.md", slug)` → `_paths.status_path(project_root, cfg)` wrapped in the existing exception handler (correctly handles test fixtures without `status_md` key). ✅
- `build_plan` in `millpy-cleanup.py` retains `resolve_task_path` (documented exception); `_apply_inplace_record` and `_apply_pr_reap_record` both thread `cfg` correctly. ✅
- All test fixtures updated: `test-spawn-core.py` passes `cfg={"paths":{"status_md":"_mill/status.md"}}` to `write_initial_status`; `test-cleanup.py` passes `cfg=` to `apply_plan`; implement/holistic mocks return configs with `status_md`. ✅

## Verdict

APPROVE — implementation fully realises the plan with no deviations.