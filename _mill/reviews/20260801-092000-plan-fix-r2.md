## Fixed

- Review `20260801-091855-plan-review-r2.md` — "[NIT] Inconsistent `_config.load_config` argument pattern": Entry step 1's `_config.load_config` call now uses `(worktree_root, git_root)`, matching the `(hub_root, git_root)` call shape used elsewhere (e.g. `mill-go/SKILL.md`'s "0.55" block).
- Review `20260801-091855-plan-review-r2.md` — "[NIT] Board discipline section omits the wiki-mutation bullet": added a bullet to Card 1's `## Board discipline` requirements noting the `_client.set_phase(wiki_path, slug, "ready-to-merge")` wiki mutation on the success path.

## Pushed Back

(none)
