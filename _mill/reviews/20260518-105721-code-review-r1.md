# Review: 58 (D) — Activate psmux-based claude subprocess routing — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-05-18
```

## Findings

No findings. All four cards land correctly:

**Card 1** — `via_psmux: false` added under a new `claude:` sub-block in both `mill-config.yaml` and `plugins/mill/templates/mill-config.yaml`, correctly positioned after the four timeout scalars and before the `pipeline:` separator. Comment text is byte-identical between the two files. ✓

**Card 2** — `shutil` and `uuid` imported at module level. `_MODE_BY_ALLOWED_TOOLS` constant placed after `_MUTATING_TOOLS`. `_get_via_psmux_flag()` uses deferred imports, dual-class `except (Exception, SystemExit)`, correct `_config.load_config(git_root, git_root)` call. `_build_psmux_argv()` placed after `_build_argv`, mode lookup raises `LLMError` on unknown tools, appends `--effort` conditionally and `--session-id` unconditionally. ✓

**Card 3** — Psmux branch inserted immediately after the entry log and before `start = time.monotonic()`. All 12 steps from the plan implemented in order: resume guard → PATH guard → UUID generation → start timer → `_build_psmux_argv` → try/except with timeout/spawn classification → dt → non-zero exit as plain `LLMError` (no `_scan_rate_limit`, no fast-fail retry, no `LLMSessionError`) → `rstrip()` → sid_log → log print → `return text, session_id`. Direct path body (else-branch via early return) is preserved byte-for-byte. ✓

**Card 4** — All 11 plan-specified test cases are present. `os`, `re`, `uuid` imported at top. `_get_via_psmux_flag` is patched to `True`/`False` per case on every psmux test. Test 11 imports `_paths` before the patch to guarantee it's in `sys.modules`, correctly routing through the deferred import inside the helper. `time.monotonic` side-effect lists are sized to match the exact call count in each path. Existing retry/resume/rate-limit tests are guarded with `_get_via_psmux_flag` → `False` patches. ✓

## Verdict

APPROVE — implementation matches plan exactly; no integration gaps found.