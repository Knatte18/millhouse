# Plan Fix Report — round 1

```yaml
source_review: 20260518-065054-plan-review-r1.md
round: 1
```

## Fixed

- `20260518-065054-plan-review-r1.md` / "[BLOCKING] Card 9 test-structure instruction contradicts existing file layout" — Removed the contradictory "inline call within main()" clause. Requirements now correctly specify: define named functions matching the existing `test_deep_merge_…` / `test_load_config_…` style, place before `if __name__ == "__main__":`, append names to `tests = [...]` list in `main()`.

- `20260518-065054-plan-review-r1.md` / "[NIT] Card 10 Test A primary patching target won't intercept _review_common calls" — Updated Card 10 Test A to specify `patch("_review_common.resolve_plugin_template_path", ...)` as the primary instruction, with an explicit note explaining why patching `_config` directly won't work (local binding from `from _config import ...`).

- `20260518-065054-plan-review-r1.md` / "[NIT] Card 8 find-text uses double-hyphen; actual SKILL.md contains em-dash" — Updated Card 8 to instruct the implementer to locate the echo line by surrounding context and to use em-dash `—` in the find-text (`[mill-go] HALT: wiki appears missing or corrupted — re-run mill-setup to restore it`), then replace it with the ASCII double-hyphen form.

## Pushed Back

(none)
