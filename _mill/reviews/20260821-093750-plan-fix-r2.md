# Plan Fix — round 2

Source review: `_mill/reviews/20260821-093553-plan-review-r2.md` (verdict: REQUEST_CHANGES, 1 BLOCKING, 2 NIT demoted from BLOCKING).

## Fixed

- `[BLOCKING:scope]` `_bg.py` omitted from Card 6's Context despite naming `check_bg_status` — added `plugins/mill/scripts/_bg.py` to Card 6's `Context:` list, matching Card 3's own precedent of listing `_verify_baseline.py` for its equivalently-literal reused-function reference.
- `[NIT:consistency]` "0.6" inner dispatch drops the pre-edit PYTHONPATH worktree override — this was a genuine functional bug, not cosmetic: `millpy-bg.py`'s worker runs the `--`-payload as a literal no-shell argv list (`subprocess.run(cmd, ...)` with no `env=` override), so the pre-edit `PYTHONPATH="<git_root>/..." "$MILL_PYTHON" ...` shell-prefix idiom silently does nothing inside that payload — the inner command would have inherited the outer `millpy-bg.py` call's own cache-form `PYTHONPATH`, defeating "0.6"'s entire purpose. Fixed by wrapping the inner command with `env PYTHONPATH="<git_root>/plugins/mill/scripts"` (a plain executable that sets env vars for one child process without needing a shell), and updated 6e's cache-form-exception prose to name both the `PYTHONPATH` value and the script-path argument as the two things that get the worktree-form exception.
- `[NIT:consistency]` "0.5" halts on `dead` liveness while "0.6" treats identical case as no-op — changed 6a's `"dead"` branch to log-and-continue instead of halt, matching "0.5"'s own retained "never blocks the task" principle (and matching "0.6"'s own Failure handling, which already treats `dead` as non-fatal).

## Pushed Back

(none)
