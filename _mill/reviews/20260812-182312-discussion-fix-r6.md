# Discussion Fix — round 6

Source review: `_mill/reviews/20260812-182248-discussion-review-r6.md` (APPROVE, 1 NIT, not demoted).

## Fixed

- "git-pr/SKILL.md:93 citation doesn't support `--exit-code`" — dropped the incorrect `git-pr/SKILL.md:93` citation from both the `817-dead-parent-detection` Decision and the round-5 Q&A log entry; `git-pr/SKILL.md:93` uses `git ls-remote --heads origin <base>` with no `--exit-code` flag, so it doesn't actually establish the cited pattern. `mill-resume/SKILL.md:222` alone is the accurate precedent and remains cited.

## Pushed Back

(none — finding verified accurate, no harm from fixing)
