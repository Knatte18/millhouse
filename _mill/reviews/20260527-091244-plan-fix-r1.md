# Plan fix report — round 1

Source: `_mill/reviews/20260527-091104-plan-review-r1.md`

## Fixed

- `20260527-091104-plan-review-r1.md` — "[BLOCKING] Card 8 breaks existing test-safe-rmtree assertion" — Card 8 expanded to also edit `test-safe-rmtree.py`: the `# --- ignore_errors passes through to shutil.rmtree ---` block at lines 261-282 is rewritten as `# --- ignore_errors contract honoured via outer try/except ---` and now asserts the presence of `onexc` (Python 3.12+) or `onerror` (3.11 and earlier) in the call kwargs instead of `ignore_errors`. The pre-existing `# --- ignore_errors=True swallows OSError from rmtree ---` block at line 242 is kept unchanged because the outer try/except wrapper preserves its semantics.
- `20260527-091104-plan-review-r1.md` — "[BLOCKING] Card 5 wrong patch target for commit_push spy" — Card 5's orphan-deletion case (Test 3) updated to patch `wiki._server.commit_push` (the locally bound name in the importing module) instead of `wiki._sync.commit_push`. Rationale cited inline in the card so future readers don't re-introduce the bug.
- `20260527-091104-plan-review-r1.md` — "[NIT] Card 2 contradictory cleanup instructions" — Card 2's cleanup instruction rewritten: explicitly drop `_test_helpers.safe_temp_dir` (it is a context manager handling its own cleanup) and use the existing `tempfile.mkdtemp()` + `try/finally + shutil.rmtree(tmp, ignore_errors=True)` pattern that tests (a)-(f) in `test-wiki-daemon.py` already use. Batch 3 converts every `shutil.rmtree` in this file in one pass.

## Pushed Back

(none)
