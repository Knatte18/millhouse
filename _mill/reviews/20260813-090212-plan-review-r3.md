MILL_REVIEW_BEGIN
# Review: mill-plan SKILL.md: entry, phase-gate, finalize, and re-entry path gaps — holistic

```yaml
duration_s: 376.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (self-assessed)
reviewed_file: plan/
date: 2026-08-13
```

## Findings

### [BLOCKING:design] Card 3's widened trigger is unreachable — phase table not widened
**Location:** Batch 01, Card 3 (`### Entry-gate wait for upstream mill-start`)
**Issue:** The subsection this card edits only runs "Whenever the phase-table lookup above lands on the `phase: discussing` row" (unchanged prose, Entry step 4). Entry step 4's phase table still has a bare `phase: discussing` row — no card in this batch widens it to also match `discussion-fix-r{N}`. So when `phase == "discussion-fix-r1"`, the table lookup falls to the catch-all "any other phase" row and halts before `matches_wait_trigger` (with the new regex) is ever called — the #821 fix is dead code.
**Fix:** Widen the Entry step 4 phase table's `phase: discussing` row itself to also match `^discussion-fix-r\d+$` (mirroring `mill-go-base/SKILL.md`'s own widened `discussed`/`discussing`/`planning`, or matching `^plan-review-r\d+$`/`^plan-fix-r\d+$` row at line 119), so the table lookup actually routes into this subsection before the regex match fires.

### [NIT:consistency] Card 5 cites a nonexistent "Card 4's 'Dispatch-mode scoping'"
**Demoted-from:** BLOCKING
**Location:** Batch 01, Card 5, "Subprocess/psmux CLI-flag threading" paragraph
**Issue:** Card 5 opens with "(Agent-mode is unaffected — see Card 4's 'Dispatch-mode scoping')." Card 4's own Requirements text contains zero mentions of "agent" or any such subsection/heading — there is nothing in Card 4 to see. (The underlying claim is independently true — `_review_plan.prepare()` has no `max_rounds` parameter or round-cap check, and `millpy-review-plan.py`'s `--stage prepare`/`--stage finalize` branches never read `args.max_rounds` — but the citation itself is dangling.)
**Fix:** Either add the "Dispatch-mode scoping" explanation to Card 4, or inline the Agent-mode-unaffected rationale directly in Card 5 and drop the cross-reference.

### [NIT:consistency] Card 6's `verify-mixed-cwd` row overstates a single message's contents
**Location:** Batch 01, Card 6, new `verify-mixed-cwd` fix-table row
**Issue:** The row says "The payload's `message:` field names every conflicting batch (sorted name list) and each one's resolved cwd." In `_check_verify_mixed_cwd` (`_plan_validate.py`), each individual error dict's `message` states only *that* batch's own resolved cwd plus a bare name list of the others (no per-name cwd values inline); the full per-batch cwd picture only emerges by reading all emitted error dicts for the check together.
**Fix:** Reword to "each error dict states its own batch's resolved cwd plus the sorted list of conflicting batch names; read all `verify-mixed-cwd` error dicts together to see every batch's cwd."

## Verdict

REQUEST_CHANGES
Card 3's fix is inert without a phase-table widening, and Card 5 cites a nonexistent Card 4 subsection.
MILL_REVIEW_END
