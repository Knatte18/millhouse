# Plan fixer report — round 2

Source review: `_mill/reviews/20260713-084539-plan-review-r2.md`
(verdict REQUEST_CHANGES, 1 BLOCKING, 1 NIT).

## Fixed

- `20260713-084539-plan-review-r2.md` — "[BLOCKING] Card 5 preserves a non-existent `.ps1` wrapper mention": verified — `_shortcuts.py` renders `.cmd` wrappers and `unlink`s any legacy `.ps1`, so `.millhouse/millpy-wikipush.ps1` exists on no platform (the Windows wrapper is `millpy-wikipush.cmd`). Reworked Card 5 from "add a parenthetical, keep the `.ps1` mention" to "correct the stale `.ps1` reference to the actual `.cmd` wrapper and note POSIX runs `millpy-wikipush.py` directly, leaving no `.ps1` reference." Retitled the card and updated its commit message accordingly.
- `20260713-084539-plan-review-r2.md` — "[NIT] Card 2 leaves the \"Four checks\" docstring prose stale": added a requirement to Card 2 to update the `test-guards.py` module-docstring header sentence "Four checks bundled into one test file" to "Five checks bundled into one test file", alongside the existing `Checks:` bullet addition.

## Pushed Back

(none)
