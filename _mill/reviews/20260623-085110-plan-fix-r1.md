# Plan-fix report — round 1

Source review: `_mill/reviews/20260623-084859-plan-review-r1.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 2 NIT).

## Fixed

- [BLOCKING] "Completeness gate crashes existing success tests on int parse" (Card 6) — `_batch_completeness_stuck` now guards the `int()` parse: returns `None` on non-zero rev-list returncode or non-numeric stdout (mocked git in `test-millpy-implement.py` returns a non-numeric sha for all git calls). Requirements updated in `02-implementer-finalize-contract.md` Card 6.
- [BLOCKING] "card_count=0 from heading-less batch files mis-feeds the gate" (Card 8 + Card 6) — Card 6's helper now early-returns `None` when `card_count <= 0` (falsy/zero = no `### Card` headings), stated explicitly in Requirements.
- [NIT] "Card 4 rationale misstates `_review_code` failure mode" (Card 4) — guard scoped to the per-batch path only (`if args.batch is not None:`) so holistic reviews gain no new hard-fail; rationale corrected; noted no batch `verify:` exercises `millpy-review-code.py`.
- [NIT] "Test cards under-specify run-all discovery for main()-style file" (Cards 10, 5, 12) — Card 10 now says add inline numbered cases inside `main()` (no standalone `test_*`); Card 5 says add inline assertions in `test-paths.py`'s `main()` try block; Card 12 says append the new `test_*` function to `test-config.py`'s `tests = [...]` list.

## Pushed Back

(none)
