# Plan Fix Report — round 3

## Fixed

- `20260522-113741-plan-review-r3.md` — "[BLOCKING] test-wiki-sync.py auto-included by run-all.py glob": Updated Card 11 requirements to add `SKIP: frozenset[str] = frozenset({"test-wiki-sync.py"})` constant and filter it from the discovery glob. The three in-memory tests are auto-discovered; `test-wiki-sync.py` is excluded by name.
- `20260522-113741-plan-review-r3.md` — "[BLOCKING] Store.content_hash must be @staticmethod": Updated Card 2 requirements to declare `content_hash` as `@staticmethod`, callable as `Store.content_hash(content)` without an instance. Updated call-site description to `Store.content_hash(content)`.

## Pushed Back

(none)
