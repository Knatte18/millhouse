# Plan-fix round 1

## Fixed

- `_mill/reviews/20260802-102409-plan-review-r1.md` — "[NIT] `_resolve_holistic_verify` docstring not updated for subshell wrap": Card 1 (Batch 1) Requirements now also directs updating the function's `Returns:` docstring clause to note each command is individually wrapped in its own subshell before joining.
- `_mill/reviews/20260802-102409-plan-review-r1.md` — "[NIT] Walk-up order in `_nested_go_module_root_and_pattern` is textually ambiguous": Card 3 (Batch 2) Requirements step 2 rewritten to state explicitly that the `go.mod` check happens at the current candidate (starting with `affected_dir` itself) before any `.parent` advance, so the module-root case (66j) isn't skipped past.

## Pushed Back

(none)
