# Plan fix: round 4

## Fixed

- [source: `20260918-185139-plan-review-r4.md`] "Speculative baseline launch writes to the shared worktree while mill-plan may still be committing" — verified accurate: confirmed `mill-plan/SKILL.md` never references `millpy-builder-lock.py` (the builder lock provides no mutual exclusion against a concurrently active mill-plan session), and `SKILL.md`'s own text explicitly documents the entry-gate wait as a window where an upstream mill-plan session may still be actively committing in the same worktree. Reworked the fix's persistence mechanism from a `status.md` yaml field (requiring a `git commit` during the wait — the actual race) to a plain local file at `<worktree_root>/.millhouse/baseline-preflight-log.txt` (gitignored, never `git add`ed or committed — modeled directly on `_builder_lock.py`'s existing `.millhouse/builder.lock` precedent), which needs no git operation at all and therefore cannot race a concurrent commit from any other session, while still surviving a `/mill-go` restart via the ordinary worktree filesystem. This removed the need for batch 2's `_status.py` `baseline_preflight_log` persistence trio (Card 3) entirely — deleted that card, retargeted batch 2 to just the `--module-wide-only` flag (Card 4), dropped its now-unjustified `depends-on: [1]` edge (no file overlap remains between batches 1 and 2), and rewrote batch 3's Card 5 end-to-end (both edits) to use plain filesystem read/write/delete instead of `_status.get/set/clear_baseline_preflight_log`. Updated the overview's Batch Index (verify commands, `depends-on`), the "seven independent robustness fixes" Shared Decision, and batch 3's own verify marker (`baseline-preflight-log.txt` instead of `baseline_preflight_log`) to match throughout.
- [source: `20260918-185139-plan-review-r4.md`] "Card 8 Edit A misdescribes adjacency of the two anchor sentences" — verified accurate: `holistic-review.md` step 4 does have an intervening sentence ("The fixer loads `mill-receiving-review`...") between the dispatch sentence and the "After the NIT-fix completes..." sentence, which Edit A's wording omitted (unlike Edit B, which already names it). Added the same parenthetical to Edit A naming the intervening sentence, and adjusted the closing "do not alter" instruction to name all three sentences explicitly.

## Pushed Back

(none)
