# Plan fix — round 1

Source review: `_mill/reviews/20260918-181245-plan-review-r1.md`

## Fixed

- `Card 4 points at a docstring bullet that does not exist` — batch 1 card 4's Requirements no longer instructs adding/updating a nonexistent `batch-oversized` bullet under `_plan_validate.py`'s top-of-file "Checks performed" docstring list (confirmed absent from that list). Repointed the instruction at the two places `batch-oversized` is actually documented: `run()`'s own docstring checks-summary line, and its `Args:` section's `max_cards_per_batch`/`max_batch_context_tokens` descriptions — the latter two now to be updated to state the context-token cap applies per card.

## Pushed Back

(none)
