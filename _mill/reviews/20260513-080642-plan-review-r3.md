# Review: (A) — Add status_md to paths config + refactor 14 callsites — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-13
```

## Findings

### [NIT] Card 3 module-docstring style reference inaccurate
**Location:** Batch 1, Card 3
**Issue:** Requirements say "match the prose style of the surrounding entries (`resolve_task_path`, `resolve_hub_relative_path`)" but `resolve_task_path` is not in the existing module-level Public API docstring — it is only in `__all__`. The implementer has no entry to copy style from.
**Fix:** Change the example to `resolve_hub_relative_path` only (which is in the docstring), or state explicitly "match the style of `resolve_hub_relative_path`'s docstring entry."

### [NIT] No unit-test coverage for bug fixes in Cards 5 and 6
**Location:** Batch 2, Cards 5–6
**Issue:** The bugs in `millpy-inspect.py:77` and `millpy-status.py:49` (missing `_mill/` segment) are real regressions, but the verify command (`run-all.py`) has no test that exercises status-path resolution through those scripts. If the fix is applied incorrectly the only signal is a manual run.
**Fix:** Either add a thin unit test that mocks `discover_active_worktrees` and confirms the correct `_mill/status.md` path is passed to `_status.read_*`, or call out this gap explicitly in the Batch 2 test notes so a future reviewer knows coverage is absent here.

## Verdict

APPROVE  
Plan is correct and complete; two minor clarity/coverage gaps but nothing blocking.