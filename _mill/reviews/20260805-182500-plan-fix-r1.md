# Plan-fix round 1

## Fixed

- `20260805-182342-plan-review-r1.md`: "[NIT] Card 3 doesn't spell out the `overview_text` read step" — added an explicit `overview_text = overview_path.read_text(encoding="utf-8")` clause (mirroring `millpy-implement.py:670-671`) to Batch 2 / Card 3's trigger-check condition 4, before the `_plan_dag.extract_batch_index(overview_text)` call.

## Pushed Back

None.
