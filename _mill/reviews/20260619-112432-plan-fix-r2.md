# Plan-fix report — round 2

```yaml
round: 2
reviewed_dir: _mill/plan/
source_review: _mill/reviews/20260619-112404-plan-review-r2.md
verdict_in: APPROVE (2 NITs)
date: 2026-06-19T11:24:32Z
```

## Fixed

- r2 "[NIT] Card 4 brief-commit guard double-stages on porcelain check" — added a note to Card 4 that the `git status --porcelain -- _mill/briefs` guard also covers the already-committed case (empty porcelain → step skipped → no empty commit).
- r2 "[NIT] Card 5 assertion `mill-start: discussion-fix` substring count" — tightened the Card 5 backstop count from `>= 3` to `>= 4`, designated the per-message windowed checks as the **primary** form, and noted that `mill-start: discussion-fix` legitimately appears twice (interactive 4b + `--auto` 4b) so the test must not assert single-occurrence.

## Pushed Back

_(none — both NITs were accurate, optional refinements; applied per the receiving-review default.)_
