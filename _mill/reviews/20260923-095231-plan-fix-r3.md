```yaml
round: 3
```

## Fixed

- [NIT:consistency] "Card 19 misattributes which batch deletes baseline_parent_sha" — corrected the test-rewrite card's closing sentence: `get_baseline_parent_sha` still exists at that point in the plan and is only deleted in batch 4, not batch 1.
- [NIT:consistency] "Card numbering starts at 2, no Card 1 exists anywhere in the plan" — renumbered every card across the whole plan down by 1 (1-35, was 2-36), so the global sequence now starts at 1, matching the review template's "unique, sequential, no gaps across batches" criterion literally. Also fixed a pre-existing, previously-unflagged stale cross-reference discovered while re-checking the renumbered result: the overview's `mid-flight-config-key-removal-is-safe` Shared Decision cited "Card 16/18/19" for the config-removal bootstrap justification, which was already wrong before this round's renumbering — corrected to "Cards 14-17."

## Pushed Back

(none)
