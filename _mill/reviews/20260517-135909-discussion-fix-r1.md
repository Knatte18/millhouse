# Discussion fix — round 1

Source review: `20260517-135748-discussion-review-r1.md` (verdict: APPROVE, 3 NOTE findings).

## Fixed

- "[NOTE] parse_verdict already handles non-leading yaml; part 1 is done" — rewrote the `parse_verdict-yaml-search-strategy` decision and the Problem #315 / Scope-In entries to clarify that `parse_verdict` already supports prose preamble (verified at `_review_common.py:984-1026`); remaining #315 work is test coverage + backend error envelope only.
- "[NOTE] _review_plan.py already has the parse_verdict error envelope" — updated the Scope-In and the `parse_verdict-error-envelope` decision to mark `_review_plan.py` as verify-only, citing the existing catch at `_review_plan.py:607-617`.
- "[NOTE] _review_discussion.py backend scope gap" — added `_review_discussion.py` explicitly to both Scope-In and the `parse_verdict-error-envelope` decision target list (cited bare-raise at `_review_discussion.py:122`).

## Pushed Back

(none)
