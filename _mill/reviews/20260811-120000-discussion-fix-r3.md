# Discussion fix report — round 3

Source review: `_mill/reviews/20260811-115805-discussion-review-r3.md` (verdict APPROVE, 0 BLOCKING, 1 NIT)

## Fixed

- `[NIT:scope]` "Generic orchestrator-name lists outside the cross-reference enumeration" — `plugins/mill/skills/cli/SKILL.md:40` and `plugins/mill/skills/conversation/SKILL.md:74` name `mill-go` in prose without citing its file path, so the repoint enumeration (scoped to literal `mill-go/SKILL.md` citations) missed them. Added both to Technical context with an explicit instruction to add `mill-go2` to each list, a note that neither is a behaviour gap, and a rule against adding `mill-go-base` since these lists name operator-invocable orchestrators. Also widened the Scope bullet so the plan cannot read the repoint work as path-citations-only.

## Pushed Back

(none)
