# Plan fix — round 2

```yaml
task: "Agent-tool dispatch discards the effort tier already encoded in mill-agents.yaml (opushigh/opusmedium/opusmax)"
slug: "mill-agent-effort-gap"
round: 2
```

## Fixed

- `_mill/reviews/20260726-081731-plan-review-r2.md` — "[NIT] Card 4's 'alphabetically sorted' claim doesn't match ASCII sort order": reworded `01-tier-agent-definition-files.md` Card 4's Requirements to describe the `plugin.json` `agents` array ordering as "grouped base-file-first per role then tier variants sorted alphabetically within each group," and noted the ordering carries no functional meaning since Card 4's own test compares the array as a set.

## Pushed Back

None.
