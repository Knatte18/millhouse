# Plan Fix Report — round 4

```yaml
slug: mill-infra-and-path-fixes
round: 4
source_review: _mill/reviews/20260615-110841-plan-review-r4.md
verdict_of_source: APPROVE
blocking: 0
nit: 3
```

## Fixed

- `20260615-110841-plan-review-r4.md` — [NIT] "Decision text contradicts batch-2 dependency": amended the overview `six-independent-fixes` decision to state that `config-repo-layer` (batch 2) `depends-on: [1]` for the same shared-file (`_review_common.py`) reason cited for the wiki merge, and that all other batches are `depends-on: []`.
- `20260615-110841-plan-review-r4.md` — [NIT] "Card 12 sets env flag implemented only in later Card 13": added a note to batch 05 Batch Scope that Cards 12 and 13 are interdependent and ship together as one atomic batch (verify + review run after all cards), so the env-flag setter and reader land as a unit with no broken intermediate state.
- `20260615-110841-plan-review-r4.md` — [NIT] "git-pr config-resolution invocation underspecified": Card 13 now specifies the config resolution as a guarded cache-form Python invocation (`PYTHONPATH="${CLAUDE_PLUGIN_ROOT}/scripts" "$MILL_PYTHON" -c '...'`) that runs only when `$MILL_PYTHON` and `$CLAUDE_PLUGIN_ROOT` are set, falling through to the literal `$GIT_ROOT/_mill/status.md` check otherwise.

## Pushed Back

(none)
