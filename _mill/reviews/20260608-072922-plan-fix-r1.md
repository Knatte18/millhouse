# Plan Fix Report — round 1

Source review: `_mill/reviews/20260608-072810-plan-review-r1.md` (holistic, APPROVE, 2 NITs)

## Fixed

- `20260608-072810-plan-review-r1.md` [NIT] "review-output.schema.md table still cites sonnetmax_tool" — broadened Card 2's `review-output.schema.md` requirement to target the line-47 metadata-fields table row `(e.g. sonnetmax, sonnetmax_tool)` (the line-14 example is already `sonnetmax`), dropping `sonnetmax_tool`, with a confirming grep. Verified against the file: line 47 holds the stale reference.
- `20260608-072810-plan-review-r1.md` [NIT] "haiku gets tooluse: true under the convention" — applied option (a): Card 1 now requires a header-comment note that `tooluse` is reviewer-only and harmless on implementer/fixer/merge entries (e.g. `haiku`); Card 5 now requires a matching comment so the convention test's uniform application (including `haiku`) reads as deliberate.

## Pushed Back

(none)
