# Plan Fix Report — round 1

```yaml
slug: mill-infra-and-path-fixes
round: 1
source_review: _mill/reviews/20260615-105003-plan-review-r1.md
verdict_of_source: REQUEST_CHANGES
blocking: 2
nit: 2
```

## Fixed

- `20260615-105003-plan-review-r1.md` — [BLOCKING] "Card 3 omits resolve_main_worktree_root from Context": Card 3 Requirements now specify that `worktree_root` is a real `_git_init`'d clone at `<hub>/wts/<repo>/` so `resolve_main_worktree_root(worktree_root)` (candidate #2) resolves to it, that `load_config` is called as `load_config(hub_root=<container>, worktree_root=<clone>)`, and that no `resolve_wiki_path` stub is needed (load_config never calls it).
- `20260615-105003-plan-review-r1.md` — [BLOCKING] "Card 8 test-mocking contract is underspecified": Card 8 now defines a discrete `_parent_diff_names(worktree, parent_branch)` function for the parent-diff; Card 10 names the exact mock points (`_cleanliness._pygit2_util.status_porcelain` and `_cleanliness._parent_diff_names`) with no real-git invocation.
- `20260615-105003-plan-review-r1.md` — [NIT] "Real-git test added to run-all default set": Card 14 now adds `test-finalize-cleanup.py` to `run-all.py`'s `SKIP` frozenset (matching the `test-wiki-sync.py` precedent); `run-all.py` added to Card 14 Edits and to the overview All Files Touched; batch 05 `verify:` changed to invoke the test directly (in batch file and overview).
- `20260615-105003-plan-review-r1.md` — [NIT] "Card 4 push refspec detached-HEAD path": Card 4 now requires raising `WikiPushError` (ASCII message) when the resolved branch is empty or the literal `HEAD`, rather than pushing a malformed `HEAD:HEAD` refspec.

## Pushed Back

(none)
