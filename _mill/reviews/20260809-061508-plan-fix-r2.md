# Plan fix — round 2

```yaml
slug: verify-baseline-nested-worktree-orphan-risk
round: 2
```

## Fixed

- `20260809-061417-plan-review-r2.md` — "[NIT] Nested worktree parent dir not explicitly created in Card 2's test": Card 2 now explicitly creates `task_wt / ".scratch"` before the `git worktree add --detach` call, mirroring `_checkout_parent_branch`'s own explicit `mkdir(parents=True, exist_ok=True)`.
- `20260809-061417-plan-review-r2.md` — "[NIT] Regression test doesn't pin down which removal branch executed": Card 2 now captures `remove_safe`'s stderr via `contextlib.redirect_stderr`/`io.StringIO` and asserts `"removed via git"` appears in it, confirming the direct-success branch (not the pre-existing fallback branch) is what's under test.

## Pushed Back

None.
