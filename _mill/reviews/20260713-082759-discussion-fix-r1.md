# Discussion fixer report — round 1

Source review: `_mill/reviews/20260713-082700-discussion-review-r1.md` (verdict APPROVE, 2 NOTEs).

## Fixed

- `20260713-082700-discussion-review-r1.md` — "[NOTE] Per-file venv guard heuristic is coarse": reworded the Scope > Regression guard bullet to scope the guard to the venv-existence idiom (matching the `Scripts/python.exe` existence-test text, paired-with-`bin/python`-in-the-same-block), label it an explicit coarse per-file tripwire, and document the simpler per-file fallback rule — so the discussion no longer implies per-block proof.
- `20260713-082700-discussion-review-r1.md` — "[NOTE] test-bootstrap.sh prerequisites unstated": added a Preconditions clause to the Scope > test-bootstrap bullet and the Testing bullet stating the `.sh` port assumes a synced venv + `git` (same preconditions as the `.ps1`) and must fail loudly (nonzero exit naming the missing tool) rather than silently skip or report a false pass.

## Pushed Back

(none)
