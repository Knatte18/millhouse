MILL_REVIEW_BEGIN
# Review: Audit mill-start/mill-plan/mill-go for turn reduction — holistic

```yaml
duration_s: 250.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-19
```

## Findings

### [BLOCKING:scope] `_status.append_phase` call-site counts don't match current source
**Location:** `doc/turn-reduction-audit.md:441-445` (the `## mill-go-base` re-derivation paragraph)
**Issue:** The doc claims re-derived `_status.append_phase` call-site counts of `mill-start` 6 / `mill-plan` 14 / `mill-go-base` 21, stating these "match `_mill/discussion.md`'s own cited numbers exactly, so no drift." A direct grep for `_status\.append_phase\(` against the current worktree source gives `mill-start/SKILL.md` 5 (lines 377×2, 387, 406, 413), `mill-plan/SKILL.md` 9 (lines 59, 60, 134, 299, 551, 571, 577, 599, 628), and `mill-go-base/SKILL.md` 19 including one that is a `signature:` annotation, not a call (line 214, counted once as a real call). All three files disagree with the doc's claimed figures, not just the one file `_mill/discussion.md` itself flagged as one-off — this contradicts Card 3's explicit instruction to re-derive rather than copy `_mill/discussion.md`'s counts on faith, and the doc presents an unverified match as a completed re-verification.
**Fix:** Re-run the count against the current worktree source (excluding `signature:` type-annotation lines and cross-references that only *mention* `append_phase` without calling it) and correct the `## mill-go-base` paragraph's stated counts, or explain the counting methodology if the higher figures include sites this reviewer's grep missed.

## Verdict

REQUEST_CHANGES
Doc's re-derived `append_phase` call-site counts contradict grep evidence across all three files, undermining its own re-verification claim.
MILL_REVIEW_END
