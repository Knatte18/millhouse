# Review: mill-merge / fixer teardown recovery — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-05-28
```

## Findings

### [NIT] test-fixer-env-isolation: benign/unrelated preservation tests only exercise direct branch
**Location:** `plugins/mill/unit_tests/test-fixer-env-isolation.py:tests 3-4`
**Issue:** Tests 3 (`preserves_benign_git_vars`) and 4 (`preserves_unrelated_vars`) only patch `_get_via_psmux_flag=False`. Plan card 10 required all four strip tests to cover both branches explicitly.
**Fix:** Add matching psmux-branch variants for tests 3 and 4, or split each into `_psmux` / `_direct` pair. Logic is identical in both branches so this is purely a coverage gap, not a correctness issue.

### [NIT] `STRIP_VARS` placement diverges from plan spec
**Location:** `plugins/mill/scripts/_llm_claude.py` (line where STRIP_VARS is defined, after `_MODE_BY_ALLOWED_TOOLS`)
**Issue:** Plan card 11 says "immediately after the existing top-of-module imports"; instead it sits after two other module-level dicts.
**Fix:** Move the constant and its comment to immediately below the import block. No functional impact.

### [NIT] Moved-aside stderr message uses colon instead of semicolon
**Location:** `plugins/mill/scripts/_archive_tag.py` (moved-aside print at end of function)
**Issue:** Plan specifies `[archive-tag] moved aside -- archive/<slug>-NN; new tag created`; implementation emits `...-NN: new tag created` (colon instead of semicolon).
**Fix:** Change `:` to `;` in the format string.

## Verdict

APPROVE
All batches correctly implemented; three cosmetic NITs, no correctness or contract issues.