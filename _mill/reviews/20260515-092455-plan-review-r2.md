# Review: Make implementer model configurable via config.yaml — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-15
```

## Findings

### [NIT] Context: none on cards 2, 3, 5
**Location:** Batch 1 / Cards 2–3; Batch 2 / Card 5
**Issue:** `Context: none` technically violates the "Context field — non-empty per card" rule. The Edits files are implicitly readable and provide the needed context, so there is no functional gap.
**Fix:** List the Edits file(s) under `Context:` as well (e.g. Card 2 → `Context: plugins/mill/scripts/_reviewers.py`).

### [NIT] config.yaml string replacement has no failure signal
**Location:** Batch 2 / Card 4
**Issue:** `cfg_text.replace("    self_fix_rounds: 2\n", ..., 1)` is silently a no-op if the live `wiki/config.yaml` uses different indentation or a trailing comment. The script exits 0 and commits without the `model:` key.
**Fix:** After the replacement, assert the result string contains `model: sonnethigh` before writing; raise if not.

## Verdict

APPROVE — plan is well-sequenced, decisions faithfully implemented, tests comprehensive; two cosmetic NITs only.