MILL_REVIEW_BEGIN
# Review: Misc infra/wiki/PR/self-hosting reliability bugs — holistic

```yaml
duration_s: 270.0
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-21
```

## Findings

### [BLOCKING:consistency] Card 17's find-and-replace doubled six paths in mill-start/SKILL.md
**Location:** `plugins/mill/skills/mill-start/SKILL.md:262,265,275,302,318,324`
**Issue:** Verified via `grep -o 'CLAUDE_PLUGIN_ROOT.*mill-go-base/SKILL\.md'`: these six lines (exactly the six the plan itself named as "already qualified with the repo-relative `plugins/mill/skills/mill-go-base/SKILL.md` prefix") now read `` `${CLAUDE_PLUGIN_ROOT}/skills/${CLAUDE_PLUGIN_ROOT}/skills/mill-go-base/SKILL.md` `` — a doubled, malformed path (consistent with a second blanket substitution re-matching the already-fixed lines). The four lines the plan named as originally bare (198, 264, 300, 320) are correctly single-qualified. `mill-plan/SKILL.md` (all 12 occurrences originally bare) shows no doubling.
**Fix:** Collapse the doubled prefix on the six affected lines back to a single `` `${CLAUDE_PLUGIN_ROOT}/skills/mill-go-base/SKILL.md` ``.

### [NIT:scope] Card 17's own verification method would not have caught the doubling
**Location:** `_mill/plan/08-skill-cross-reference-fix.md:59-62`
**Issue:** The batch's stated self-check (`grep -n "mill-go-base/SKILL.md" ...` showing every occurrence prefixed with `` `${CLAUDE_PLUGIN_ROOT}/skills/` ``) is satisfied even by the doubled/malformed form found above, since a doubled prefix still "starts with" the expected prefix — the check as written cannot distinguish correct-once from correct-twice.
**Fix:** For future plans of this shape, phrase the self-check as an exact-match grep (e.g. anchor on the full expected string, not just presence of the prefix substring).

## Verdict

REQUEST_CHANGES
Card 17 left six malformed, doubled `${CLAUDE_PLUGIN_ROOT}` cross-references in mill-start/SKILL.md.
MILL_REVIEW_END
