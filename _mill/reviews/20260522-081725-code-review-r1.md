# Review: Set MILL_PYTHON via mill-setup, use in all skill invocations — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-05-22
```

## Findings

### [NIT] Verify batch expected grep count is off by one
**Location:** `_mill/plan/05-verify.md` — Card 11 requirements
**Issue:** The card states "Expected output: `20`. Batch 1 adds 2 new occurrences…18 + 2 = 20." However the Phase 8 verification bullet added by Card 2 contains *two* occurrences of `.venv/Scripts/python.exe` — one in the descriptive text (`equals \`${CLAUDE_PLUGIN_ROOT}/.venv/Scripts/python.exe\``) and one in the inline verify command. Phase 4.8's bash block adds one more. Actual post-implementation count is 21. The implementation applied the plan's edit text correctly; the stated expected count is wrong.
**Fix:** Change the expected output from `20` to `21` in the verify card, and update the arithmetic note to `18 + 3 = 21`.

## Verdict

APPROVE
All plan edits applied correctly; only the verify step's expected count needs correction.