# Discussion-fix round 3

## Fixed

- `20260813-074257-discussion-review-r3.md` — "#815 guardrail location contradicts itself" (demoted from BLOCKING): corrected the Q&A log's answer for the #815 question to say "`## Principles` section" instead of "Phase: Plan (SKILL.md-only)", matching the Decision's own concrete `## Principles` location and line-number citation.
- `20260813-074257-discussion-review-r3.md` — "Reworded halt message leaves `blocked_reason` string stale": clarified in the #832 Decision that `_status.set_blocked`'s `blocked_reason` argument intentionally keeps its existing wording (it is a machine field, prefix-matched only via `.startswith("max-rounds exhausted")` by the new `blocked` re-entry row) while the reworded text applies to the user-facing halt message an operator actually reads at the time of the original halt.

## Pushed Back

(none)
