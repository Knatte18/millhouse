## Fixed

- `20260728-170843-plan-review-r1.md` — "Card 1 wrongly claims `os` isn't imported in
  test-subprocess-util.py": corrected Card 1's test-case-(r) requirement to note `os`
  and `unittest.mock` are already imported in `test-subprocess-util.py`, dropping the
  false "add `import os`" instruction.

## Pushed Back

(none)
