# Discussion fix report — round 3

Source review: `_mill/reviews/20260904-081716-discussion-review-r3.md`

## Fixed

- `[NIT:consistency]` "Resolvability gate still says dotted text is 'compared against own refs'" — struck the stale clause from the Resolvability gate Decision; it now states only that the full dotted text is shape-matched, and points at "Membership check against the card's own refs" / "Reverse path-canonicalization for symbol matches" as the sole owners of the own-refs comparison.
- `[NIT:consistency]` "Keyword-suppression rationale contradicts the shape gate's own example" — replaced the `nil`/`true`/`false`/`null` example (all of which are actually excluded earlier by the shape gate) with Python's capitalized `None`/`True`/`False`, which do pass the shape gate and are genuinely caught by the exactly-one-file ambiguity gate instead.
- `[NIT:scope]` "No test named for the 'required, not optional' per-search-key cache" — added a Testing bullet asserting the underlying search function is called exactly once per unique search key via a call-counting stub, exercising the memoization requirement directly.

## Pushed Back

(none)
