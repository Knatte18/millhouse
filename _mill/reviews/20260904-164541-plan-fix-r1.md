# Plan fix — round 1

Source review: `_mill/reviews/20260904-164456-plan-review-r1.md`

## Fixed

- r1 BLOCKING:scope "`_build_artefact_section`'s two `bulk_files` call sites, only one named" — verified: the bulk-mode branch has three bulking calls, not two, and two of them are mutually exclusive alternatives on the same `if` (`bulk_files_with_diff(source_files, ...)` + `bulk_files(plan_and_ancestors)` on the `start_sha is not None and project_root is not None` branch; `bulk_files(all_bulked)` on the `else` branch). Card 11's Requirements now enumerate all three explicitly and state why the diff-scoped branch is the common production path rather than an edge case.
- r1 BLOCKING:scope "Card 13's new assertions don't cover the diff-scoped branch" — Card 13's Requirements now mandate that the bulk-mode coverage include a `start_sha`-bearing fixture (either extending an existing one that already asserts on `--- DIFF:` / `--- FILE:` delimiters, or adding a new case), and that the "no occurrence of `str(project_root)`" assertion be made against a prompt provably containing both delimiter families so both bulking calls on that branch are covered.
- r1 NIT:scope "`DisplayRoots.render` test list omits the wiki-vs-project_root priority case" — Card 1's enumerated test list now includes a path under both a `wiki_root` and a deeper `project_root`, asserting the `wiki/` form wins, with a note that this is the case proving rule 1 short-circuits rule 2.

## Pushed Back

(none)
