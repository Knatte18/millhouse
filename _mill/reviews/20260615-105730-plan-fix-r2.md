# Plan Fix Report — round 2

```yaml
slug: mill-infra-and-path-fixes
round: 2
source_review: _mill/reviews/20260615-105605-plan-review-r2.md
verdict_of_source: REQUEST_CHANGES
blocking: 1
nit: 3
```

## Fixed

- `20260615-105605-plan-review-r2.md` — [BLOCKING] "Restored task_dir collides with git-pr guard": Card 12 now updates mill-finalize Step 5 to invoke `/git-pr` with `MILL_FINALIZE_PR_CLEANUP=1` and notes the restore branch leaves `task_dir` present; Card 13 now makes git-pr's Step 1.5 guard skip entirely when `MILL_FINALIZE_PR_CLEANUP` is set, so PR creation proceeds on the stacked-branch restore case while the standalone guard remains.
- `20260615-105605-plan-review-r2.md` — [NIT] "parent_branch not available in mill-go Path Setup": Card 9 now specifies deriving `parent_branch = _parent_branch.resolve(status_path, interactive=False)` in the Handoff gate rather than claiming it is already in scope.
- `20260615-105605-plan-review-r2.md` — [NIT] "Card 3 test does not exercise candidate #2 distinctly": Card 3 now adds a linked worktree (`git worktree add`) as `worktree_root` with the config at the primary clone, so `resolve_main_worktree_root` resolves to the clone — exercising candidate #2 distinctly from #3 (the faithful #470 task-worktree scenario).
- `20260615-105605-plan-review-r2.md` — [NIT] "compute_terminal_dirt path-prefix stripping under-specified": Card 8 now states the pure filter extracts the path via `line[3:]` (status-code prefix strip) and uses the destination path for rename lines before membership testing.

## Pushed Back

(none)
