# Discussion fix — round 1

```yaml
slug: baseline-uses-worktree-not-checkout
round: 1
source_review: 20260922-125928-discussion-review-r1.md
```

## Fixed

- r1 `[NIT:consistency]` (demoted from BLOCKING) "Gotchas section misquotes mill-go-base/SKILL.md" — verified `tail -1` appears nowhere in `plugins/mill/skills/mill-go-base/SKILL.md`; both §0.5's speculative branch (line 583) and its main flow (line 605) extract with `grep '^{' <log-path>`. Rewrote the bullet to drop the fabricated discrepancy and keep the surviving point: both sites describe parsing *the one* JSON line and must select by the `substage` key once a second line is added.

## Pushed Back

_None._
