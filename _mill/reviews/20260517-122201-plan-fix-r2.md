# plan-fix round 2 — mill-branch-slug-fixes

Source review: `_mill/reviews/20260517-122033-plan-review-r2.md` (verdict: REQUEST_CHANGES, 1 BLOCKING + 1 NIT)

## Fixed

- [BLOCKING] Card 3 omits updating existing read_branch test Case B (Batch 1 / Card 3) — Card 3's Requirements now explicitly instructs updating Case B's cfg (line 564 of `test-status.py`) from `cfg={"spawn": {"branch_prefix": "hanf"}}` to `cfg={"spawn": {"branch_prefix": "hanf/"}}` (with canonical trailing-slash separator), keeping the expected return value `"hanf/foo"`. Without this update, Batch 1's `verify:` step would fail because the fixed `f"{prefix}{slug}"` formula returns `"hanffoo"` against the old assertion.
- [NIT] Card 9 container-form fixture doesn't specify config seeding (Batch 4 / Card 9) — Card 9 now mandates writing `tmp/wiki/config.yaml` with `spawn: {branch_prefix: "hanf/"}` inside the test fixture helper. The accept test now uses branch `hanf/<slug>` and exercises D1's normal prefix-strip path (not the legacy bare-`/`-split fallback). Added a "Wiki config seeding" sub-paragraph explaining why an empty-prefix fixture would mask regressions in the post-D1 normal path.

## Pushed Back

(none)
