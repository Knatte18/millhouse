# Plan Fix: Replace claude -p with psmux-routed LLM dispatch — round 2

```yaml
source_review: 20260531-150507-plan-review-r2.md
round: 2
date: 2026-05-31
```

## Fixed

- `20260531-150507-plan-review-r2.md` "[NIT] Phase 2 capture_pane call not stated explicitly":
  Expanded Batch 2 Card 4 Phase 2 description to explicitly state "call
  `capture = _psmux.capture_pane(session_name, alternate=True)` in a try/except
  `PsmuxError` — on error set `capture = ''"` at the top of each iteration.

- `20260531-150507-plan-review-r2.md` "[NIT] No direct unit test for rewritten _wait_for_idle_prompt":
  Expanded Batch 2 Card 3 to also edit `test-claude-sub.py` and add Scenarios (d)
  and (e): (d) capture returns "? for shortcuts" → True; (e) capture never contains
  "for shortcuts", timeout fires → False.

- `20260531-150507-plan-review-r2.md` "[NIT] Integration test prereq check still hardcodes pwsh":
  Updated Batch 4 Card 11 requirements to remove `shutil.which("pwsh")` from
  `_check_prerequisites()` — shell path is now resolved from config and may not
  be on PATH; the test depends on `millpy-claude-sub.py` which handles resolution
  internally.

## Pushed Back

(none)
