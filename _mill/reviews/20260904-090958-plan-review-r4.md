MILL_REVIEW_BEGIN
# Review: millpy-implement/bg: Windows baseline-worktree teardown (WinError 145) and stale liveness reporting — holistic

```yaml
duration_s: 296.0
verdict: REQUEST_CHANGES
reviewer_model: sonnethigh
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-09-04
```

## Findings

### [BLOCKING:scope] Card 3 Requirements cite functions from files absent from Context
**Location:** Batch 2 (`02-cleanup-orphan-baseline-sweep.md`), Card 3.
**Issue:** Requirements name `_junction.strip_all_in_worktree` (behavior claim used to justify passing `junctions_cfg={}`) and `_verify_baseline.compute_baseline`'s call shape as precedent, but neither `_junction.py` nor `_verify_baseline.py` is in Card 3's `Context:` (only `_worktree.py`) or `Edits:` (`millpy-cleanup.py`).
**Fix:** Add `_junction.py` and `_verify_baseline.py` to Card 3's `Context:`, or drop the cited-behavior justification and rely solely on the already-Context'd `_worktree.remove_safe` docstring.

### [BLOCKING:scope] Card 9 Requirements name a new symbol from a file absent from Context
**Location:** Batch 4 (`04-baseline-undercount-corroboration.md`), Card 9.
**Issue:** Card 9 patches `_implementer_common._corroborate_batch_failure` and exercises `_run_verify_gates`'s new `start_sha`/`status_path`/`batch_name` kwargs (both added by Card 7 in `_implementer_common.py`), but `_implementer_common.py` is not in Card 9's `Context:` (`_verify_baseline.py`, `millpy-implement.py`, `_status.py`) or `Edits:` (`test-implementer-common.py`).
**Fix:** Add `_implementer_common.py` to Card 9's `Context:` so the implementer can confirm the new signature/symbol names against Card 7's actual landed code rather than trusting only the plan prose.

## Verdict

REQUEST_CHANGES
Two cards cite functions from files missing from their own Context lists; add those files to Context.
MILL_REVIEW_END
