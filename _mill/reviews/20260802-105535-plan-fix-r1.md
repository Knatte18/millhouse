## Fixed

- `20260802-105509-plan-review-r1.md` — "[NIT] Card 3 cites a nonexistent anchor comment for the quote-indent-drift function insertion": dropped the incorrect "before the `# skip_checks filtering (Card 7 / #188)` section comment" clause from Card 3's function-definition insertion instruction (that comment appears exactly once in the file, only in the later `tests = [...]` registration list, not near the function definitions). Replaced with an accurate anchor: the next function definition after `test_check_requirements_quote_indent_drift_dirty_multiple_edits_tie_break` is `test_skip_checks_filters_wiki_config_mutation`, with no comment line between them. The registration-list paragraph's citation of the same comment was already correct and is unchanged.

## Pushed Back

(none)
