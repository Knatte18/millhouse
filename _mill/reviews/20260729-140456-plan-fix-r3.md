# Plan fix — round 3

```yaml
task: mill-plan/review validation false-positives, hard-fails, and truncated failure reasons
slug: mill-plan-review-validation-gaps
round: 3
source_review: _mill/reviews/20260729-140426-plan-review-r3.md
verdict: APPROVE (1 NIT)
```

## Fixed

- "Batch 2 scope prose mislabels the shared prepare() call site" — `_mill/reviews/20260729-140426-plan-review-r3.md`: `[NIT] Batch 2 scope prose mislabels the shared prepare() call site`. Verified: `prepare()` in `_review_code.py` is the single shared entry point for both holistic (`scope=None`) and per-batch (`scope="<name>"`) code review, and Card 8's Requirements patch the shared ref-collection block unconditionally, with no scope guard — so the fix applies to both scopes, not holistic only. Reworded the Batch 2 Batch Scope paragraph to drop the "holistic" qualifier and state the scope-agnostic reality explicitly.

## Pushed Back

(none)
