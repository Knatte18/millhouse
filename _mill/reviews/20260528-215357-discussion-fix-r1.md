# Discussion Fix — round 1

```yaml
slug: codeguide-jupyter-support
round: 1
source_review: 20260528-215305-discussion-review-r1.md
```

## Fixed

- `20260528-215305-discussion-review-r1.md` — "[NOTE] Non-ASCII cell content:
  normalization mechanism unstated". Accurate catch: the "ASCII-only stdout"
  constraint conflated the helper's own diagnostic/marker text (must stay ASCII
  for Windows cp1252) with the cell-content payload (user-authored, frequently
  non-ASCII). Updated § Constraints to split the two and require non-crashing
  emission of non-ASCII cell content (reconfigure stdout to UTF-8 with
  `errors='replace'`); added a matching § Testing case so the unit suite covers
  non-ASCII cell content round-tripping while marker text stays ASCII.

## Pushed Back

- (none)
