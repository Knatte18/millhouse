# Review: Isolate verify PYTHONPATH so tests validate worktree code — holistic

```yaml
verdict: APPROVE
reviewer_model: opushigh
reviewed_file: plan/
date: 2026-05-25
```

## Findings

### [NIT] Card 4 Requirements names `_render` helpers not in Context
**Location:** Batch verify-isolation, Card 4
**Issue:** Requirements references `_render.render` and `_render._strip_leading_comment` to explain comment-stripping behavior, but `_render.py` is not in Context: or Edits:. The mention is explanatory rather than instructional, but technically tripwires the "Context completeness" rule.
**Fix:** Either reduce to a parenthetical without naming the helpers (e.g., "the leading HTML comment is stripped at render time"), or add `plugins/mill/scripts/_render.py` to Card 4's Context:.

## Verdict

APPROVE
Plan is small, self-consistent, well-sequenced TDD, complies with its own new prefix rule, and uses the canonical 5-key envelope.