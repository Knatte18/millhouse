# Plan-fix round 4

Review: `_mill/reviews/20260801-163057-plan-review-r4.md`

## Fixed

- [BLOCKING] "mill-plan/SKILL.md's opening framing still claims an operator prompt exists" — confirmed lines 10 and 14 both assert an operator-facing prompt at step 6, false after Card 2's collapse. Added new Card (now Card 3, after global renumbering) fixing both, plus a third stale reference found by a self-driven grep sweep (Phase: Plan Review step 2's "AskUserQuestion ... banned here absent ... escape hatches" clause) that the reviewer didn't flag but is the same defect.
- [BLOCKING] "mill-go/SKILL.md's Step 0b still claims two sections contain operator prompts" — confirmed Entry Step 0b's rationale is false after batches 2-4's edits land. Added new Card (now Card 11) to batch 04-mill-go-handoff-gates (last in the mill-go dependency chain, so its accuracy claim holds once implemented) fixing Step 0b's framing.
- [BLOCKING] "Stale 'ask user per Stuck escalation' cross-references left unedited in mill-go/SKILL.md" — confirmed both line 241 and line 419 still say "ask user." Extended Card 3 (now Card 4) with two additional old/new quote pairs replacing both with "route to *Stuck escalation*, which self-resolves once before escalating."

Mechanical follow-up (not a review finding, required by the new cards above): global card renumbering. Adding 2 new cards mid-sequence (batch 1, batch 4) broke the `card-numbering` validator invariant (cards must be sequential within a batch, and batches' ranges must be contiguous in filename order). Renumbered every card reference across all 6 batch files (1,2,15->1,2,3 in batch 1; 3->4 in batch 2; 4,5,6->5,6,7 in batch 3; 7,8,9,16->8,9,10,11 in batch 4; 10,11->12,13 in batch 5; 12,13,14->14,15,16 in batch 6) via a scripted two-pass token substitution, then fixed one resulting `requirements-quote-indent-drift` false-positive (a mid-sentence fenced quote whose trailing-newline artifact didn't byte-match the single-line source paragraph) by switching that one sub-edit to unfenced prose description instead of an old/new quote pair.

## Pushed Back

(none — all three findings were accurate and fixed)
