MILL_REVIEW_BEGIN
# Review: mill-merge/merge-in: squash non-ff rejection, stale-worktree logic, parent-branch detection, conflict resolution — holistic

```yaml
duration_s: 309.0
verdict: REQUEST_CHANGES
reviewer_model: sonnethigh
reviewer_self_id: Claude Sonnet 4.5-class (self-assessment; environment reports "claude-sonnet-5")
reviewed_file: plan/
date: 2026-08-12
```

## Findings

### [BLOCKING:scope] `_status.update_field` used without `_status.py` in Context
**Location:** Batch 2 Card 6, Batch 3 Card 7
**Issue:** Both cards' Requirements introduce a brand-new call, `_status.update_field(status_path, 'parent', ...)`, to rebind the dead-parent branch — verified this call does not already appear anywhere in either `mill-merge/SKILL.md` or `mill-merge-in/SKILL.md` (both files already use `_status.read_full`/`append_phase`/`set_blocked`/`read_batches`, but never `update_field`). Neither card lists `_status.py` under `Context:`.
**Fix:** Add `plugins/mill/scripts/_status.py` to both cards' `Context:` lists.

### [NIT:consistency] New batch-4 fixture containers never registered in `main()`'s finally block
**Demoted-from:** BLOCKING
**Location:** Batch 4, Cards 9–13 (containers `container_ff`, `container_ff_halt`, `container_rb`, `container_dead`)
**Issue:** `test-merge.py`'s established convention (verified in `main()`) is that every standalone fixture container (`container_dirty`, `container_retry`, `container_untracked`, `container_step5_guard`) gets an `if "X" in locals()` entry in both the failure-preserve branch and the success-cleanup (`_safe_rmtree`) branch of the `finally:` block. None of the four new containers this batch introduces are added to either list.
**Fix:** Add `container_ff`, `container_ff_halt`, `container_rb`, `container_dead` to both `finally:` lists (or note this explicitly as part of Cards 9/11's Requirements), otherwise every test run leaks four new directories under `.scratch/` unboundedly.

### [NIT:scope] No test for the "neither status.md layout exists" chain-end sub-case
**Location:** Batch 4 (Cards 11–14) vs. Batch 1 Card 2 step 4
**Issue:** Card 2's step 4 documents two distinct fallback-(b) sub-cases collapsing to `chain-end`: neither `_mill/status.md` nor `task/status.md` exists at the pre-cleanup tree, and a status.md exists but has no `parent:` row. Card 14 tests the legacy-path fallback (`task/status.md` exists) but no card exercises the "neither layout present" branch of step 4.
**Fix:** Add a small sub-case (or fold into an existing card) tagging an archive commit whose tree has no status.md at all under either path, asserting `outcome: fallback, reason: chain-end`.

### [NIT:consistency] Card 3's forward reference to a paragraph title Card 4 hasn't created yet
**Location:** Batch 2, Card 3's "Rollback exemption" note
**Issue:** Card 3 inserts text citing "that section's 'Dirty-parent-worktree halt and parent-fast-forward-failure halt (Step 5)' paragraph" — but at Card 3's own commit, that paragraph is still titled "Dirty-parent-worktree halt (Step 5)" per the current source; Card 4 renames it. Between Card 3's commit and Card 4's commit the cross-reference is briefly inaccurate.
**Fix:** Either reword Card 3's reference to the pre-rename title, or note explicitly that Card 4 must land in the same PR/session before Card 3's commit is considered self-consistent.

## Verdict

REQUEST_CHANGES
Two Context-completeness gaps (Cards 6/7) and an unregistered-fixture-cleanup gap (batch 4) need fixing before implementation.
MILL_REVIEW_END
