# Review: mill-go / mill-plan loop hardening — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-28
```

## Findings

### [BLOCKING] Card 2 omits updating Cases B and F in test-review-guard.py
**Location:** Batch 1 / Card 2
**Issue:** The new `worktree_snapshot_guard` logic tolerates fast-forward commits with no new working-tree dirt. Under the new conditions (a)/(b)/(c), **Case B** ("git commit inside with raises, HEAD differs") and **Case F** ("commit inside expected_paths raises, HEAD changed") both describe exactly the scenario that will now NOT raise — a clean-before, clean-after, straight-line commit. Both tests assert `ReviewerOverstepError` is raised; under the new logic neither fires any of (a)/(b)/(c). The plan says to ADD new tests but is silent on updating or removing B and F. The batch `verify:` command runs `test-review-guard.py`, so both legacy cases will FAIL, blocking the batch from passing.
**Fix:** Add an explicit instruction in Card 2 to remove or update Cases B and F — Case B's scenario becomes a valid fast-forward (no raise expected); Case F's scenario is identical. The new test "a fast-forward commit that only removes prior dirt passes (no raise)" already covers what those cases should now assert.

### [NIT] Card 7 Context includes extraneous millpy-review-code.py
**Location:** Batch 3 / Card 7
**Issue:** `plugins/mill/scripts/millpy-review-code.py` is listed in Card 7's `Context:` but is neither edited nor provides any API consumed by the new `_check_batch_oversized` or the `millpy-review-plan.py` wiring. The two helper APIs used (`parse_batch_refs`, `resolve_existing_paths`) come from `_review_common.py`, which is already listed.
**Fix:** Remove `millpy-review-code.py` from Card 7's `Context:` list.

## Verdict

REQUEST_CHANGES
One blocking gap: Cases B and F in test-review-guard.py contradict the new fast-forward tolerance and will fail verify.