# Plan-fix report — round 2

Source review: `_mill/reviews/20260921-155708-plan-review-r2.md`

## Fixed

- "[NIT:consistency] Card 7's add-pass fence quotes ~20 lines of unchanged existing source" — replaced
  the large fenced "replace the whole add-pass loop with:" block (which reproduced ~20 lines of
  unchanged existing matching logic verbatim) with a three-step prose description naming exactly where
  each new line goes, reserving small fences for only the genuinely new lines (`add_matched = False`;
  the three-line capture inside the match branch; `if add_matched: continue` after the loop) — matching
  how the strip-pass capture and the clean-check refactor in this same card already do it, and complying
  with `00-overview.md`'s own "no fenced quote of existing source as an edit anchor" Decision.
- "[NIT:consistency] Batch 1 scope rationale misstates cs_member_re's position relative to the walk" —
  corrected "a few lines below the walk" to "earlier in the function, before the walk" in Batch 1's
  Batch Scope paragraph, matching `cs_member_re`'s actual line 2149 position (before the per-file walk
  starting at line 2196).
- "[NIT:consistency] Card 8 implies a second copy of the fix table that doesn't exist" — dropped the
  confusing "also present verbatim in `mill-plan/SKILL.md`'s own copy of the same table" parenthetical
  and reworded to name the table's own single location plainly.

## Pushed Back

(none — all three findings were accurate and fixed as described)
