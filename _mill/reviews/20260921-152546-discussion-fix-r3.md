# Discussion-fix report — round 3

Source review: `_mill/reviews/20260921-152502-discussion-review-r3.md`

## Fixed

- "[NIT:consistency] cs-member-regex-tightening's own regression example can't match cs_member_re" —
  replaced the Testing section's flawed regression example
  (`public InvalidOperationException Custom { get; }`, a return-type occurrence that never reaches
  `cs_member_re`'s `\s*[({;=]` tail) with `public int InvalidOperationException { get; set; }`, where
  the symbol itself is the declared member name immediately preceding `{`, so the example actually
  exercises the tightened regex.
- "[NIT:decision] requirements-quote-indent-drift fix-table row content left unspecified" — added the
  intended remedy phrasing for the new fix-table clause (row 391) to the Scope section: compute
  `Y - X` between the anchor's and sibling's indent, add or strip that many leading spaces per line
  accordingly, mirroring the existing strip/add rows' per-line adjustment convention.

## Pushed Back

(none — both findings were accurate and fixed as described)
