# Plan fix — round 1

```yaml
task: Explore and adopt Claude Code fork-agents in mill orchestration
slug: explore-fork-agent-opportunities
round: 1
source_review: 20260712-135518-plan-review-r1.md
verdict_addressed: REQUEST_CHANGES (2 BLOCKING, 4 NIT)
```

## Fixed

- `20260712-135518-plan-review-r1.md` — **[BLOCKING] "Card 21's static-surface sweep is
  unsatisfiable"**. Accurate. Card 21 demanded that no file under `plugins/mill/agents/` state a
  tool permission or an output destination, while card 14 deliberately makes `mill-reviewer.md` do
  exactly that, and `mill-implementer.md` already lists seven tools in a file the reviewers-only
  Shared Decision forbids touching. The test could only have gone green by violating the plan. Card
  21 now splits the assertion: `templates/` keeps the strict no-tool-statement sweep;
  `agents/` gets the narrower invariant card 14 actually produces (no `<OUTPUT_FILE>` token, no
  sole-output-is-final-message claim, no blanket `Write` prohibition, while still forbidding `Edit`,
  `Bash`, `NotebookEdit`).

- `20260712-135518-plan-review-r1.md` — **[BLOCKING] "Cards 12/13 mock the modules whose real
  behaviour is under test"**. Accurate, and the more dangerous of the two: the tests would have
  passed while asserting nothing. `test-review-finalize.py:110-137` replaces `_review_common`,
  `_review_cli` and `_agent_dispatch` with bare `MagicMock`s — under which `print_error_envelope`
  emits no envelope (12b unobservable), `except ReviewError` binds a non-exception (`TypeError` on
  any throw), `write_brief` unlinks nothing (12c's stale-`.out.md` guard proves nothing), and
  `output_path_for` returns a `MagicMock` (card 13's `.md` -> `.out.md` equality cannot hold). Cards
  12 and 13 now carry an explicit **mocking-discipline** requirement: use the real `_agent_dispatch`,
  `_review_cli` and `_review_common`; mock only `_paths`, `_reviewers` and the backend; give the
  backend's `finalize` a `side_effect` delegating to the real `parse_verdict`; assert on stdout
  captured via `contextlib.redirect_stdout`, never on mock call args. The existing round-trip tests
  in card 12(a) keep the current style, which is sound for what they inspect.

- `20260712-135518-plan-review-r1.md` — **[NIT] "`mill-reviewer.md` frontmatter description goes
  stale"**. Accurate — `:3` still says "without modifying files or running commands", and it is the
  string the harness surfaces. Added as card 14(a2).

- `20260712-135518-plan-review-r1.md` — **[NIT] "Cards 9-12 cite `parse_verdict` / `ReviewError`
  without `_review_common.py` in Context"**. Accurate; `Context:` is an allowlist, so the implementer
  could not legally open the module whose behaviour the guard rationale depends on. Added
  `_review_common.py` to cards 9, 10, 11, 12 and 13, plus `_review_cli.py` to card 12. Verified this
  does not trip `batch-oversized`: `_plan_validate.py:1629` unions the refs into a **set** per batch,
  so a file cited by several cards is counted once (batch 2 stays ~56k tokens against the 120k cap).

- `20260712-135518-plan-review-r1.md` — **[NIT] "Agent-mode prompt still says 'wrap your entire
  output'"**. Accurate and materially important: the templates must keep the `MILL_REVIEW_BEGIN` /
  `MILL_REVIEW_END` wrapper (it is the format `finalize` parses), so an agent could satisfy both it
  and the ack by writing the file *and* dumping the block into chat — forfeiting the exact context
  saving this task exists to win. Card 2(b)'s footer must now state explicitly that the wrapped
  report is the **content of the file** and the chat message is the ack **and nothing else**.

- `20260712-135518-plan-review-r1.md` — **[NIT] "Batch 4 `depends-on: [2]` omits the batch-3 Write
  grant"**. Accurate. Card 18(c) stops the orchestrator capturing `.out.md` for reviewers, which is
  only safe once card 14 (batch 3) lets the reviewer write it. On the old edge, batch 4 could land
  with batch 3 outstanding, leaving a window where **nobody** writes `.out.md` and every review round
  returns `ERROR`. Batch 4 is now `depends-on: [2, 3]` in both the Batch Index and the batch
  frontmatter, with the rationale recorded in its Batch Scope.

## Pushed Back

_None. All six findings verified accurate against source; none met a harm-check exception._
