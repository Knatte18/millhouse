# Plan-fix report r1

```yaml
slug: subprocess-fixes
round: 1
source_review: _mill/reviews/20260513-082238-plan-review-r1.md
```

## Fixed

- `20260513-082238-plan-review-r1.md` — `[BLOCKING] Integration tests omitted from verify:`: appended each integration test to its batch's `verify:` via `&&`. Batch 1 verify is now `python plugins/mill/unit_tests/test-subprocess-util.py && python plugins/mill/integration_tests/test-subprocess-tree-kill.py`; batch 2 verify is now `python plugins/mill/unit_tests/test-millpy-bg.py && python plugins/mill/integration_tests/test-millpy-bg-detached.py`. The POSIX skip-guard inside each integration test exits 0 immediately, so POSIX runs stay fast. Rewrote the "Integration tests are local-dev-only" shared decision in `00-overview.md` to "Integration tests are Windows-only and chained into verify:" with the matching rationale. Rewrote the Batch Tests sections + Card 4 / Card 8 requirements lines that asserted the tests were excluded from `verify:`.

- `20260513-082238-plan-review-r1.md` — `[NIT] Card 8 subprocess spawn missing PYTHONPATH`: updated Card 8 requirements to spawn `millpy-bg.py` with `env={**os.environ, "PYTHONPATH": str(HUB / "plugins" / "mill" / "scripts")}` in the `subprocess.Popen` call. Added a one-line note in the requirements explaining the failure mode without the env injection (`ModuleNotFoundError: No module named '_subprocess_util'` before the launcher prints `pid=... log=...`).

## Pushed Back

_(none — both findings accepted under the receiving-review decision tree; no harm category applied.)_
