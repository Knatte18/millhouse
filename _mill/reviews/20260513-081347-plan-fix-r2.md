# Plan-fix report -- round 2

```yaml
slug: safe-rmtree
round: 2
source_review: _mill/reviews/20260513-081216-plan-review-r2.md
```

## Fixed

- `_mill/reviews/20260513-081216-plan-review-r2.md` [BLOCKING] "Card 3 breaks `test-worktree.py` 'path exists + not a working tree' test" -- expanded Card 3's `test-worktree.py` section into two named changes. Change A keeps the original patch-target rename (`_worktree.shutil.rmtree` -> `_safe_rmtree.shutil.rmtree`). Change B adds `with patch("_safe_rmtree._blacklist_for", return_value=[])` wrapping each of the three tests that reach the fallback path (`:185-202`, `:227-242`, `:244-261`). The `_blacklist_for` stub bypasses the new `_paths.resolve_container_path` subprocess call -- cleaner than padding `side_effect` lists with `mock_prune`-shaped entries because the latter would also need `result.stdout` set to a real string to satisfy `_paths.resolve_main_worktree_root`'s `Path(result.stdout.strip())` parse. The path-absent variant at `:263+` is exempted from Change B because its `if path.exists():` guard skips the `safe_rmtree` call.
- `_mill/reviews/20260513-081216-plan-review-r2.md` [NIT] "ALLOWED_FILES description says 'four entries' but lists five" -- changed "four" to "five" in Card 6's `ALLOWED_FILES` introduction.

## Pushed Back

(none)
