MILL_REVIEW_BEGIN
# Review: mill-start: discussion-review timeline gaps and stray orch-review.md scratch file — holistic

```yaml
duration_s: 209.0
verdict: APPROVE
reviewer_model: sonnethigh
reviewer_self_id: claude-sonnet-4.5
reviewed_file: plan/
date: 2026-09-04
```

## Findings

### [NIT:consistency] Card 1's literal replacement text drops `<status_path>` from the git-add pathspec
**Demoted-from:** BLOCKING
**Location:** Batch 01, Card 1, Requirements — the fenced "Replace it with:" block.
**Issue:** The quoted verbatim replacement text is `git -C <worktree> add <discussion_path> <reviews_dir>/ _mill/briefs/ && git commit ...` — `<status_path>` is absent — yet the very next sentence in the same card claims "`<status_path>` added to the existing commit's git-add pathspec". This also contradicts `discussion.md`'s own `gap-fix-commit-pathspec` Decision, which specifies the pathspec ending `... _mill/briefs/ <status_path> && git commit ...`, and that Decision is not carried into `00-overview.md`'s `## Shared Decisions` at all (only `gap-fix-timeline-label`, `orch-review-scratch-location`, and `atomic-write-read-path-change` appear there).
**Why it matters:** An implementer following the literal quoted block (the normal reading of "current exact text / replace it with") will append the phase to `status.md` on disk but never stage it in the same commit as the discussion/review changes — breaking the atomicity the Decision and the card's own prose require. Neither `test-phase-wait.py` (tests the regex in isolation, not SKILL.md text) nor `test-brief-commit.py` (only checks that `_mill/briefs/` sits within 300 chars of the commit-message string, regardless of whether `<status_path>` is present) will catch this — the batch's `verify:` cannot detect the omission.
**Fix:** Correct the fenced replacement block to end `... _mill/briefs/ <status_path> && git commit -m "mill-start: discussion-gap-fix round {N} for {slug}"`, and add `gap-fix-commit-pathspec` to the overview's `## Shared Decisions` so this exact pathspec ordering is visible cross-batch instead of only in prose.

## Verdict

APPROVE
Card 1's literal replacement text omits `<status_path>` from the commit pathspec, contradicting its own prose and discussion.md's Decision.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
