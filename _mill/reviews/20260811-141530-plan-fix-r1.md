# Plan fix report — round 1

```yaml
task: 'mill-go2: fork-based implementer dispatch'
slug: mill-go2-fork-implementer
round: 1
review: 20260811-140618-plan-review-r1.md
```

## Fixed

- r1 `[BLOCKING:design]` "Card 5's \"second site\" claim is factually wrong" — verified: `mill-plan/SKILL.md:118-127` sanctions a live Phase: Plan fork-usage site, so the base's closing sentence was already stale and mill-go2 is not the second site. Card 5 requirement 3 now names all three sites (mill-start Explore, mill-plan Phase: Plan research, mill-go2's implementer override) and forbids asserting any ordinal, since the count is what went stale the first time.
- r1 `[BLOCKING:design]` "Cold-fallback trigger doesn't clearly cover all implementer re-dispatch points" — verified against `mill-go-base/SKILL.md:877-906` (`### Stuck escalation`) and `:921-945` (`## Resume`): both hold implementer re-dispatches outside step 4. Card 4 now enumerates all six implementer `Agent()` call sites with an explicit fork-or-cold assignment, and the override text states the governing rule (fork every fresh attempt at the batch's work; cold at every point that exists to escape a dispatch that already failed). The ambiguous phrase "terminal-failure re-dispatch under the base's step-4 classification" is gone: the one cold fallback per batch is now pinned to the Stuck-escalation already-retried-`transient` fresh re-fire.
- r1 `[NIT:consistency]` "New helper omitted from `_status.py`'s module docstring" — verified: the module's `Public API:` list at `_status.py:19-38` names every public function including both sibling audit-append helpers. Card 2 now requires `append_fork_fallback_log(status_path, batch_name, timestamp) -> None` be added to that list.

## Pushed Back

_None._
