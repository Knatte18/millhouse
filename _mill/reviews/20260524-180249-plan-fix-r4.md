# Plan-fix report — round 4

```yaml
round: 4
source_review: 20260524-180016-plan-review-r4.md
verdict: REQUEST_CHANGES
blocking_count: 2
nit_count: 1
```

## Fixed

- `20260524-180016-plan-review-r4.md` — "[BLOCKING] _spawn_core body conversions absent from card 26" — added an explicit Attribute-to-dict-key-conversion sub-bullet to card 26. The implementer is instructed to grep `_spawn_core.py` end-to-end for `t.<attr>` (and other iteration variable names — `task`, `entry`, `picked`, `chosen`) and convert every `.phase`/`.slug`/`.title`/`.brief`/`.group`/`.status`/`.has_proposal` access to dict-key access (`t["phase"]`, etc.). Function names called out: `pick_task_single`, `_prompt_numbered`, `_prompt_numbered_multi`, `pick_task_single_or_multi`, `prompt_merged_entry`, `multi_select_groom_then_claim`, `discover_active_worktrees`.
- `20260524-180016-plan-review-r4.md` — "[BLOCKING] Migration commit message contradicts daemon handler" — chose reviewer's option (a). Added a keyword-only `message: str | None = None` parameter to `wiki._client.upsert_tasks_batch` (External interface block + card 6 signature). Card 5's `_handle_upsert_tasks_batch` now accepts `message` in its payload; commits as `wiki: <message>` when provided (with the canonical `wiki: ` prefix preserved), or `wiki: batch` when `message` is null/missing. Card 13 step 5d now passes `message="migrate to V3 (TinyDB-backed)"`; the resulting commit message is `wiki: migrate to V3 (TinyDB-backed)` exactly matching card 14's assertion.
- `20260524-180016-plan-review-r4.md` — "[NIT] groom_and_claim_merge naming inconsistency" — confirmed via grep that the actual function in `_spawn_core.py` is `multi_select_groom_then_claim` at line 446 (no `groom_and_claim_merge` exists). Rewrote card 26's citation to use `multi_select_groom_then_claim` as the canonical name (body roughly `:446-525`); flagged the old `:488-514` citation as stale. Similar treatment for `claim_in_wiki` (correct location `:624`, body `:624-644`; stale `:636-641` flagged). The implementer cites the function by name; the line citations are approximate hints.

## Pushed Back

(none — every finding was factually accurate and no fix would cause harm per the `mill-receiving-review` decision tree.)
