# Review: Replace powershell subprocess with winreg in mill-setup — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-05-31
```

## Findings

### [BLOCKING] test-winenv.py has no main() runner — tests never execute
**Location:** `plugins/mill/unit_tests/test-winenv.py` (entire file)
**Issue:** The file defines 8 test functions but has no `main()` runner and no `if __name__ == "__main__": sys.exit(main())` guard. The plan explicitly requires "the pattern from `test-setup-hub-links.py`", which has a full `main()` that collects and runs each function, exits 1 on failure, and prints a pass summary. Without it, `run-all.py --only test-winenv.py` either exits 0 without running anything or raises `AttributeError` on `main`, making the `verify:` command vacuously green.
**Fix:** Add a `main() -> int` that iterates all test functions in a try/except loop (matching `test-setup-hub-links.py`'s structure exactly), and add `if __name__ == "__main__": sys.exit(main())` at the bottom.

## Verdict

REQUEST_CHANGES
One blocking issue: test suite silently does not run without a `main()` entry point.