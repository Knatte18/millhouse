# Plan fix -- round 3

```yaml
review: 20260808-175152-plan-review-r3.md
round: 3
```

## Note on the round-3 envelope

Unchanged from rounds 1 and 2: the envelope reported `blocking_count: 0` / `nit_count: 0` against `REQUEST_CHANGES` because the reviewer emits the classed heading syntax this task introduces and the installed cache's regex cannot match it.
Routed off the review file: 1 BLOCKING, 2 NIT, step 4d.

## Fixed

- `20260808-175152-plan-review-r3.md` -- "[BLOCKING:consistency] extract_findings loses demoted status on re-read paths".
  Accurate.
  Round 1's fix wired the two `_review_plan.py` re-read sites onto `extract_findings`, but card 2 still specified `demoted` as always `False` and never read the `**Demoted-from:**` marker, so those sites would report `demoted: false` for findings that were in fact demoted -- the `findings` list disagreeing with itself depending on whether an entry arrived fresh or via carryforward.
  Took the reviewer's first suggested fix over its second: `extract_findings` now detects the marker itself (the `**Demoted-from:** BLOCKING` field line for the heading mechanism, the `demoted_from` field for the yaml mechanism) rather than each call site post-processing the list.
  Marker detection at the single extraction point cannot drift between call sites, and a third re-read site added later inherits it for free.
  Stated explicitly in card 2 that this is not ceiling application -- it reports a demotion already recorded in the text -- and that the fresh path is unaffected because the marker does not exist yet when `extract_findings` runs there.
  Added a re-read round-trip assertion to card 8 covering both mechanisms, and recorded the marker's dual role in the overview's `demotion-rewritten-into-review-file` Decision.
- `20260808-175152-plan-review-r3.md` -- "[NIT:consistency] review-output.schema.md has two finding-heading examples, Card 23 only names one".
  Accurate and verified: the grammar appears in the `## File format` block (`### [BLOCKING|NIT|GAP|NOTE] ...`) and again in the `### `## Findings`` body section's fenced example (`### [BLOCKING|NIT] ...`, no class).
  Card 23 now names both as separate edit sites and calls out why the second is the easy one to miss -- it carries no retired token, so a vocabulary-driven search passes over it.
  Also added a card 25 assertion that every `### [BLOCKING` occurrence in the schema carries a class suffix, which closes the gap the finding noted in the test coverage rather than only in the card prose.
- `20260808-175152-plan-review-r3.md` -- "[NIT:consistency] review-output.schema.md's raise-condition bullet not updated for the now-3-value grammar".
  Accurate and verified: the bullet reads "The `verdict:` value is not one of the four listed above" and would contradict a three-value grammar line directly above it.
  Added it to card 23's edit list, rewritten to state the asymmetry the `gaps-found-back-compat` Decision creates -- three emitted values, four accepted inputs.

## Pushed Back

(none)
