MILL_REVIEW_BEGIN
# Review: mill-go-base: remove subprocess/psmux dispatch branches — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-12
```

## Findings

### [BLOCKING:design] Card 24's empty-diff gate will fail from pre-existing drift, not this task's edits
**Location:** batch 5, card 24. **Issue:** `SKILLS.md`'s committed `mill-go2` row description ("Behaviourally identical to /mill-go today...") already does not match `mill-go2/SKILL.md`'s current frontmatter `description:` ("Forks the fixer role instead of dispatching it cold..."), verified by direct read of both files — this drift predates the plan and is unrelated to card 23's body-only edits. Regenerating `SKILLS.md` per card 24 will therefore produce a non-empty diff regardless of correct execution, and the card's own diagnosis table ("a companion file grew frontmatter" / "mill-go/mill-go2 frontmatter changed") does not cover this actual cause, so the implementer has no correct remediation path. **Fix:** add a third diagnosis branch for pre-existing frontmatter drift unrelated to this task, or verify current `SKILLS.md`/frontmatter parity before batch 5 starts and scope a fix if needed.

### [BLOCKING:scope] Card 24 references mill-go/mill-go2 SKILL.md revert action outside its Context/Edits
**Location:** batch 5, card 24. **Issue:** Requirements' remediation text ("card 23 edits their bodies only, so revert that too") names `plugins/mill/skills/mill-go/SKILL.md` and `plugins/mill/skills/mill-go2/SKILL.md`, but neither file is listed in card 24's `Context:` (which lists only `SKILLS.md`, `mill-skills-index/SKILL.md`, and the three companion files) and `Edits:` is declared `none`. **Fix:** add both SKILL.md files to `Context:`, and either add them to `Edits:` or explicitly scope card 24 to detection-only with remediation deferred to a follow-up card.

### [NIT:consistency] Named tree-guard block will contain the call substring twice, contradicting card 13's own postcondition
**Demoted-from:** BLOCKING
**Location:** batch 3, cards 12 and 13. **Issue:** Card 12 has the named block state the checkpoint body once (`result = _treeguard.check_and_restore(...)`) and also move in the `signature: _treeguard.check_and_restore(...) -> dict` line verbatim — both contain the literal substring `_treeguard.check_and_restore(`. Card 13 then asserts "After this card, `_treeguard.check_and_restore(` must appear exactly once in the file — inside the named block," which is false by construction once the signature line is included (2 occurrences: the call and the signature). Verified against the source: the file currently has exactly 12 checkpoint-call occurrences plus 1 signature-only occurrence = 13 total matches for the substring, so the "once" postcondition was never achievable once the signature line is folded in. **Fix:** reword card 13's postcondition to "the call occurs exactly once, and the signature line exactly once" (2 total), or have the implementer count only executable call sites.

### [NIT:consistency] History note hardcodes a stale line count
**Location:** batch 4, card 17. **Issue:** The mandated exact `## History` text says "Pre-strip version (1483 lines...)", copied from `_mill/discussion.md`'s pre-written note, but the actual current `mill-go-base/SKILL.md` at commit `356da5e5` is 1484 lines (confirmed by reading the file to its last line). **Fix:** derive the line count from the actual file at execution time, or drop the specific count from the fixed literal.

## Verdict

REQUEST_CHANGES
Card 24's own-diagnosis gate is unreachable-correct given verified pre-existing SKILLS.md drift, plus a context gap and a self-contradicting postcondition in batch 3.
MILL_REVIEW_END
