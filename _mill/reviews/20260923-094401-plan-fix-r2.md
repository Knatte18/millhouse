```yaml
round: 2
```

## Fixed

- [BLOCKING:design] "Batch 1 deletes an accessor while its only unguarded caller survives until Batch 4" — moved the `get_baseline_parent_sha`/`set_baseline_parent_sha` deletion (and its corresponding `test-status.py` coverage deletion) from batch 1 into a new Card 27, appended at the end of batch 4, landing after Card 21 (which deletes the accessor's last, unguarded caller). Batch 1 now only adds the new `module_verify_baseline_signatures` accessor pair; it leaves `get/set_baseline_parent_sha` in place with an explanatory Batch Scope note. This required renumbering every card from the old Card 27 onward (+1) to keep batch 4's own card range contiguous without colliding with batch 5/6/7's existing numbers — the old Cards 27-35 are now Cards 28-36, with two internal self-references ("Card 27" in the old test-recompute card, "Card 31" in the old cleanup card) updated to match. Also added `test-status.py` to batch 4's `verify:` scope (it now edits that file) and updated the overview's Batch Index verify line for batch 4 to match.

## Pushed Back

(none)
