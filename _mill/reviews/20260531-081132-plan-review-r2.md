# Review: Replace manual layer letters with depends_on + isolated flags — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-31
```

## Findings

### [BLOCKING] merge_tasks snapshot wording permits mutation before validation
**Location:** Batch 2 / Card 6
**Issue:** "snapshot = `self._db.all()` after the `remove_slugs` removals" + "Validate before any **insert**" reads naturally as: do removals, build snapshot, validate, insert. Removals are DB mutations; if `_validate_write` then raises, removed slugs are already gone — contradicting the plan's own claim "All three paths already guarantee no partial writes on failure."
**Fix:** Replace the snapshot description with "Build projected snapshot = `[t for t in self._db.all() if t['slug'] not in set(remove_slugs)]` merged with `upsert`, WITHOUT any prior DB mutation. Validate against this snapshot. On success, perform removals then insert." Also change "Validate before any insert" to "Validate before any mutation."

### [NIT] TDD card ordering contradicts tdd-order Decision in every batch
**Location:** All batches — Cards 3, 10, 14, 21, 26 are test cards numbered last
**Issue:** Mill-go executes cards sequentially; test cards numbered last means implementation cards run first, violating the tdd-order Decision. Each test card's inline "(write before implementing X)" instruction directly contradicts the card order a sequential executor sees.
**Fix:** Renumber test cards to appear first in each batch (e.g., Card 3 → Card 1 in batch 1, Card 10 → Card 4 in batch 2, etc.) so the mill-go executor naturally writes failing tests before implementing.

## Verdict

REQUEST_CHANGES
Fix the merge_tasks atomicity spec ambiguity; TDD ordering is a minor sequencing polish.