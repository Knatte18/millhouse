# Plan fix — round 1

```yaml
slug: bg-worker-and-shell-edge-cases
round: 1
source_review: _mill/reviews/20260528-212000-plan-review-r1.md
date: 2026-05-28
```

## Fixed

- `20260528-212000-plan-review-r1.md` [NIT] "assertLogs scope not specified per branch" — Card 1 now specifies `assertLogs` wraps the fresh-mtime call (branch a), since `assertLogs` raises if no record is captured in its scope and branch a is the simplest (no `utime` backdate).
- `20260528-212000-plan-review-r1.md` [NIT] "Card 2 finally block file-handle scope unspecified" — Card 2 now requires the `finally` to write the sentinel via append-mode `open(log_path, "a", ...)` (the `"w"` handle is closed when the `with` block exits on exception), to capture the success `returncode` before the `with` closes, and to write exactly one EXIT line (none inside the `with` block).

## Pushed Back

_(none)_
