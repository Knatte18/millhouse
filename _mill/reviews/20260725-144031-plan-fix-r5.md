```yaml
round: 5
source_review: _mill/reviews/20260725-143830-plan-review-r5.md
```

## Fixed

- `[NIT] Card 12 mis-describes millpy-fix.py/millpy-merge-in-subagent.py setUp mocking` — Card 12 (`04-project-root-rebinding-implement-side.md`) corrected with a precise per-file description of how `project_root` actually resolves to `self.tmp_path` today in each of the 3 test files (`test-millpy-implement.py` via a direct `resolve_hub_path` mock; `test-millpy-fix.py` via the real, unmocked `resolve_hub_path()` succeeding against a real `.millhouse` fixture at `cwd`; `test-millpy-merge-in-subagent.py` via `Path.cwd()` after `os.chdir`) — the prescribed fix (add `resolve_container_path`/`resolve_active_hub` mocks returning `self.tmp_path`) was already correct and unaffected, only the narrative was imprecise.
- `[NIT] Cards 3/4 omit _marker.py from Context despite referencing its API` — added `plugins/mill/scripts/_marker.py` to both cards' `Context:` lists in `02-on-disk-first-resolution.md`.

## Pushed Back

(none — both findings were confirmed accurate and fixed)
