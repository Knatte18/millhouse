# Plan fix — round 3

```yaml
task: Explore and adopt Claude Code fork-agents in mill orchestration
slug: explore-fork-agent-opportunities
round: 3
source_review: 20260712-141551-plan-review-r3.md
verdict_addressed: REQUEST_CHANGES (2 BLOCKING, 3 NIT)
```

## Fixed

- `20260712-141551-plan-review-r3.md` — **[BLOCKING] "Batch 3 verify in overview contradicts the batch
  file"**. Accurate, and my error: round 2 added `test-review-templates.py` to
  `03-prompt-surfaces.md`'s frontmatter but left the **overview's** Batch Index still naming
  `test-render.py`. The overview is the authoritative DAG mill-go reads, so the gate that actually
  runs would never have executed the new file — cards 15 and 16 would have shipped uncovered, which is
  exactly the hole round 2's BLOCKING was raised to close. The overview's batch-3 `verify:` is now
  byte-identical to the batch file's.

- `20260712-141551-plan-review-r3.md` — **[BLOCKING] "Card 12 Context omits the three review backends
  it exercises"**. Accurate. Card 12's most important assertions pin the backends' own
  `except ReviewError -> return verdict "ERROR"` path (exit 0, not `print_error_envelope`), and cite
  `_review_discussion.py:146-164`, `_review_code.py:547-559`, `_review_plan.py:568-575` by line — but
  `Context:` is an allowlist and none of the three was in it, so the implementer could not legally read
  the behaviour it must assert. Added all three to card 12's `Context:`.

- `20260712-141551-plan-review-r3.md` — **[NIT] "Card 21 cross-references the wrong card"**. Accurate;
  fallout from round 2's renumbering, where a `sed` word-boundary missed the parenthesised forms. Card
  21 now cites card **20(b)** (the stale-rationale fix) and card **20(a)** (the Explore fork guidance)
  rather than card 19's mill-go step-4(a) rewording.

- `20260712-141551-plan-review-r3.md` — **[NIT] "Card 19(e) asserts config values with no config in
  Context"**. Accurate on both counts, and the second is a genuine factual error I had inherited from
  discussion.md. Verified against `plugins/mill/templates/mill-config.yaml`: `roles.implementer.model:
  sonnethigh` (`:171`) and `roles.fixer.model: haiku` (`:174`) are real `model:` keys, but
  **reviewer roles have no `model:` key at all** — they name a reviewer from `agents.yaml`
  (`roles.discussion-review.holistic.reviewer: sonnetmax`, `:137`) whose model is resolved from that
  registry and mapped by `_agent_dispatch.model_to_tier`. Card 19(e) now spells out both mechanisms,
  explicitly forbids writing the non-existent `roles.discussion-review.model`, and notes fork breaks
  both. Added the config template to card 19's `Context:`.

- `20260712-141551-plan-review-r3.md` — **[NIT] "Deleting the template header strips the last Edit/git
  prohibition from the non-agent bulk prompt"**. Accurate, and worth recording precisely because the
  obvious "fix" is the wrong one. Verified: `_llm_claude.py:79`'s `_MODE_BY_ALLOWED_TOOLS` maps `bulk`
  to the **empty** allowed-tools string, so a `--stage full` bulk reviewer is granted **zero** tools
  and cannot call `Edit` or `Bash` regardless of what the prompt says — the protection is by
  construction, not by prose. Card 15(a) now records this, and explicitly instructs the implementer
  **not** to restore the prohibitions into the shared template, since doing so would re-introduce the
  cross-channel leak this batch exists to remove.

## Pushed Back

_None. All five findings verified accurate against source; none met a harm-check exception._
