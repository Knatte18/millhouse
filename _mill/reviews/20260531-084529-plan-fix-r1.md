# Plan Fix Report — round 1

Source review: `_mill/reviews/20260531-084357-plan-review-r1.md`

## Fixed

- `20260531-084357-plan-review-r1.md` / "[BLOCKING] elif condition too broad — breaks existing Case 4 test": narrowed `elif start_sha is not None:` to `elif start_sha is not None and snapshot_path is None:` in Card 4 requirements.
- `20260531-084357-plan-review-r1.md` / "[NIT] Wrong function name in call-site instruction": replaced `validate_plan()` with `run()` throughout Card 8 and Card 10 requirements; also updated Card 8's condition to include `"--only "` as a valid scoping flag (preventing false-positive on `--only`-scoped run-all.py calls, which Card 10 noted was needed).
- `20260531-084357-plan-review-r1.md` / "[NIT] Test C dirty-file type is ambiguous": specified that Test C must modify an already-tracked file (`README.md`) so the dirty state appears under `git status --porcelain --untracked-files=no`.

## Pushed Back

(none)
