# Plan fix — round 5

Source: `_mill/reviews/20260904-091448-plan-review-r5.md`

## Fixed

- [BLOCKING:scope] "Card 7 Requirements call `_worktree.remove_safe` but `_worktree.py` is not in
  Context" — added `plugins/mill/scripts/_worktree.py` to Card 7's Context.

## Additional fix (found while re-validating, not in the reviewer's list)

- Adding `_worktree.py` to Card 7's Context pushed batch 4's context-token estimate over the cap
  (`_plan_validate`'s `batch-oversized` check: ~124,451 tokens against the 120,000 cap — the
  estimate unions Context/Edits/Creates across *every* card in the batch file, and
  `test-implementer-common.py` alone is ~238KB). Trimming Card 9's own Context list did not help,
  since cards 7/8 still referenced the same files elsewhere in the same batch file. Split Card 9 out
  into a new downstream batch, `05-baseline-undercount-corroboration-tests.md` (`depends-on: [4]`,
  since it tests symbols Card 7 introduces), leaving batch 4 with only cards 7-8. Updated
  `00-overview.md`'s Batch Index (added batch 5) and the "three independent Windows-only bug fixes"
  Shared Decision to describe the batch-4/5 split accurately. Re-validated: both batches now well
  under the token cap (batch 4 ~49,760 tokens; batch 5 ~102,475 tokens).

## Pushed Back

(none)
