# Plan fix — round 3 (max-rounds escape, option C override + defensive BLOCKING fix)

Source review: `20260517-114346-plan-review-r3.md` (verdict: REQUEST_CHANGES, blocking_count: 1)

Operator chose option **C (override)** at the max-rounds escape and instructed: ALWAYS fix every reviewer finding, never push back, never ask. The BLOCKING was a third consecutive false-positive corruption claim against `review-discussion.md` (file verified clean: 75 lines / 2937 bytes; the alleged appended plan-holistic content does not exist on disk). Per the operator rule, it is fixed defensively rather than pushed back: card 8 now mandates a pre-prepend verification of `review-discussion.md`'s exact line/byte count and last-line contents, plus a truncation step that strips any appended garbage if found. The defensive check is a no-op against the current clean file but forecloses the failure mode in any environment where the file IS corrupted. A separate bug is filed for the underlying reviewer hallucination (cross-file recall failure between adjacent bulked templates).

## Fixed

- `[BLOCKING] review-discussion.md is corrupted; Batch 4 prepends without repair` — Card 8 now has a new step `3a` mandating a defensive pre-prepend verification: `wc -l` + `tail -3` against `review-discussion.md` confirms the file ends at line 75 with `"Omit \`## Findings\` section entirely..."`. If any content exists past that line (a second `## Source-grounding rule`, plan-holistic criteria, a hardcoded output example), the implementer truncates the file at the legitimate closing line before prepending the identity header. The defensive check costs nothing when the file is clean (current state) and forecloses the alleged failure mode entirely.
- `[NIT] _filter_porcelain ordered-list equality susceptible to false positives` — Updated card 1's guard-helper body to compare via `set(before_filtered) != set(after_filtered)` rather than list-equality. Porcelain lines are unique per path entry; set semantics survive any cross-call ordering variance from `git status --porcelain`.
- `[NIT] Card 7 captured_argv closure scope guidance is ambiguous` — Replaced the ambiguous "declare a fresh captured_argv list per new test" wording with an explicit warning about Python closure binding semantics. Card 7 step 4 now mandates a FRESH `_fake_run` closure per test (each closing over its own local `captured_argv: list[str] = []`), explains why the alternative pattern (rebinding the outer name) silently passes vacuously, and offers `captured_argv.clear()` between tests as an acceptable second pattern.

## Pushed Back

(none)
