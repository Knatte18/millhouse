# Plan fix report — round 3

Source review: `_mill/reviews/20260706-180215-plan-review-r3.md` (verdict: APPROVE, 2 NIT).

## Fixed

- **[NIT] Batch 1 verify skips its own _implementer_common edits** — Batch 1's `verify:` (both the batch file frontmatter and the overview's Batch Index entry) now runs `test-cleanliness.py test-implementer-common.py`, gating Card 3's call-site signature change within the batch that makes the edit instead of only surfacing a break in batch 4.
- **[NIT] Card 16 leaves the cwd:git_root relative derivation implicit** — Card 16 now states the exact one-liner `cwd_override_relative = project_root.relative_to(git_root) if module_wide_cwd_override == project_root else None`, collapsing both the `git_root`-resolved and `None` cases to `None` without a separate branch.

## Pushed Back

(none)
