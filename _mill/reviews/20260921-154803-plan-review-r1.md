MILL_REVIEW_BEGIN
# Review: _plan_validate.py: further context-completeness, fence/indent-drift, and tag-exclusion gaps — holistic

```yaml
duration_s: 314.0
verdict: REQUEST_CHANGES
reviewer_model: sonnethigh
reviewed_file: plan/
date: 2026-09-21
```

## Findings

### [BLOCKING:design] `_compute_declared_symbols_union` mis-picks parens over braces for a struct token
**Location:** Batch 1, Card 3 **Issue:** The helper's delimiter-selection `if "(" in token and ")" in token: ... elif "{" in token and "}" in token:` (as literally specified in the card) checks parens first even when a token is brace-delimited; for the card's own worked example `` `type Deps struct { Acquire func() error }` `` the embedded `func()` makes the paren branch fire, so `start`/`end` bind to the empty `()` span instead of the outer `{...}` body — `inner` becomes `""` and neither `Acquire` nor `error` is ever added to `declared_symbols`. **Fix:** Select the delimiter pair by which one actually spans the token as a whole (e.g. prefer `{...}` when present, or pick whichever pair's outermost span is wider), verified by tracing `_compute_declared_symbols_union`'s own pseudocode in `_mill/plan/01-plan-validate-check-fixes.md` Card 3 against the described test-(b) scenario, which fails as specified.

### [NIT:consistency] `requirements-quote-indent-drift` SKILL.md row left self-contradictory
**Demoted-from:** BLOCKING
**Location:** Batch 2, Card 8, row 2 **Issue:** `mill-plan/SKILL.md`'s current row (verified: line 391) closes with "In both cases the goal is identical: the fence body must end up a literal byte-exact substring of the target `Edits:` file" — that goal is false for the new third message shape Card 8 appends (a paired-fence indent mismatch, which is never byte-matched against `Edits:` content, only indent-compared against an anchor fence), and Card 8's Requirements never says to reconcile or generalize that sentence. **Fix:** Either drop/rewrite "In both cases..." to scope it explicitly to the strip/add cases only, or update it to "In the first two cases..." before appending the new sentence.

### [NIT:consistency] Batch Scope's edit-overlap audit omits Cards 4/6 sharing `_resolve_symbol_files`
**Location:** Batch 1, `## Batch Scope` prose **Issue:** The prose calls out cards 1 and 3 as the only pair touching the same function ("no two cards' edits land on overlapping line ranges: cards 1 and 3 both touch `run()`'s check-invocation block..."), but Card 4 (new helper + skip-check in the per-file walk) and Card 6 (`cs_member_re` regex, verified at line ~2149) both edit inside `_resolve_symbol_files` too — the specific anchors don't actually collide, but the overlap-audit claim is incomplete. **Fix:** Extend the Batch Scope paragraph to also name the Card 4/Card 6 non-overlap, matching the same rigor applied to cards 1/3.

## Verdict

REQUEST_CHANGES
Card 3's declared-symbols algorithm fails its own worked struct-literal example; Card 8's doc row is left self-contradictory.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 1._
MILL_REVIEW_END
