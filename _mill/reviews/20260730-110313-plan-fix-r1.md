## Fixed

- `_mill/reviews/20260730-110114-plan-review-r1.md`: "[BLOCKING] Card 3 test-insertion anchor points to the wrong file location" — split Card 3's Requirements into two distinct, correctly-anchored instructions: the nine new function defs go in the gap between line 2046 (end of `test_check_context_completeness_dirty_line_range_suffix_missing`'s body) and line 2049 (next existing `def`); the `tests = [...]` list entries go separately near line 4972-4973, immediately after the existing `test_check_context_completeness_dirty_line_range_suffix_missing,` entry and before the in-list `# skip_checks filtering (Card 7 / #188)` comment.
- `_mill/reviews/20260730-110114-plan-review-r1.md`: "[BLOCKING] Error `message` format disagrees between Card 1 (impl) and Card 2 (docs)" — reworded Card 2's fix-table prose to say the payload's `message` carries a fence index and the strip amount `N`, with no content snippet (matching Card 1's authoritative `f"card {card_num}'s Requirements: fence {fence_idx} matches ... (found N={n})"` format exactly); reworded Card 3 test 6's assertion guidance to assert on `fence_idx == 2` only, dropping the "message's content snippet" alternative phrasing.
- `_mill/reviews/20260730-110114-plan-review-r1.md`: "[NIT] 'Three new module-level pieces' imprecisely describes 4 functions + 1 nested regex" — reworded Card 1's opening Requirements sentence to say "four new pieces (three helper functions plus the check function itself)" and clarified that only item 4's fence regex is a genuine module-level constant; items 1-3 are ordinary function definitions, and item 3's header regex is a local variable inside its function body.

## Pushed Back

(none — all three findings were factually accurate with no harm-check exemption; all three were fixed)
