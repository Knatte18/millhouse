# Plan fix report — round 1

Source review: `_mill/reviews/20260904-083144-plan-review-r1.md`

## Fixed

- `[BLOCKING:scope] Full-validate gate added to 4b/4d but not to 4c` — added new Card 8, applying the identical `_plan_validate.run` full-validate gate (same 7 kwargs, same fix-table-then-retry-once-then-halt mechanics) to step 4c's fix-and-converge path.
- `[NIT:consistency] --approve escape hatch not surfaced in the row operators actually see` — extended Card 2's Requirements to also update the `phase: blocked` row's action text to mention `/mill-plan --approve` as an alternative to `--revise` when `blocked_reason` starts with "max-rounds exhausted".
- `[NIT:consistency] Two independently-toggleable features bundled into one card/commit` — split the former Card 5 into Card 5 (round-cap-raise only) and Card 6 (step-6-waiver + the "Resumed-loop round-cap substitution" sentence append), each with its own commit message; former Card 6 renumbered to Card 7.
- `[NIT:consistency] Step-6-waiver commit instruction omits explicit git pathspec` — Card 6's commit instruction now names `git -C <worktree> add <plan_dir> && ...` explicitly, matching sibling commit instructions elsewhere in this file.

## Pushed Back

(none)
