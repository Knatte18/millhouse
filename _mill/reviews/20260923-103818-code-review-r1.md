MILL_REVIEW_BEGIN
# Review: Monitor tool: persistent:true still referenced in mill-go-base and mill-plan despite no such param — holistic

```yaml
duration_s: 85.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-23
```

## Findings

No findings. All three cards were implemented verbatim against the plan:

- `plugins/mill/skills/mill-go-base/SKILL.md:208` and `:220` — initial `Monitor` call and re-arm branch both use `timeout_ms: 1800000` with the "expected, normal outcome" framing, matching Card 1's replacement text exactly.
- `plugins/mill/skills/mill-plan/SKILL.md:102` and `:115` — same pattern for the mill-start entry-gate wait (including `clean_tree_root`/`clean_tree_paths` preserved in the re-arm), matching Card 2 exactly.
- `plugins/mill/docs/harness-tool-contracts.md:26,28,35` — schema paragraph, poll-script line, and expiry bullet all corrected to match Card 3, and cross-reference the two SKILL.md sections consistently.

Cross-file consistency verified: `1800000` is always a fixed constant (never derived from `remaining_s`) at both initial-call and re-arm sites in both files, per the Shared Decision. All three commits from the batch (`docs(mill-go-base): ...`, `docs(mill-plan): ...`, `docs(harness-tool-contracts): ...`) are present in git history and match the plan's specified commit messages.

Repo-wide grep for `persistent: true` / `persistent:true` under `plugins/mill/` returns zero hits — the batch's own verify claim holds, and no out-of-scope stale reference was missed. A broader grep for the bare word `persistent` turns up unrelated, correct usages elsewhere (`orch-review/SKILL.md`, `orch-wait/SKILL.md`: "persistent background bash poll" describing the poll process itself, not a Monitor param; `mill-setup/SKILL.md`: "persistent global env var" unrelated to Monitor) — none are stale Monitor-schema claims, so no scope gap.

No out-of-plan files present, no duplicated helpers, no language-pitfall or constraint-violation surface (three markdown docs, no runnable code).

## Verdict

APPROVE
All three cards match the plan verbatim; no stale `persistent: true` references remain anywhere in `plugins/mill/`.
MILL_REVIEW_END
