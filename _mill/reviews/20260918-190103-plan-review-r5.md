MILL_REVIEW_BEGIN
# Review: mill-go-base: orchestration robustness gaps — holistic

```yaml
duration_s: 285.0
verdict: REQUEST_CHANGES
reviewer_model: sonnethigh
reviewer_self_id: claude-sonnet-5 (Sonnet 5)
reviewed_file: plan/
date: 2026-09-18
```

## Findings

### [BLOCKING:scope] Card numbering has a gap — Card 3 is missing
**Location:** Global card numbering across all batches. **Issue:** Cards are numbered 1, 2 (batch 1), 4 (batch 2), 5 (batch 3), 6 (batch 4), 7, 8 (batch 5), 9, 10 (batch 6), 11 (batch 7) — `Card 3` does not exist anywhere in the plan (confirmed via grep across `_mill/plan/`). Batch 2's own Batch Scope explains an earlier `status.md`-backed persistence card was dropped after a prior review round changed the approach, which is the likely cause of the gap. **Fix:** Either renumber the remaining cards to close the gap (1,2,3,4,...) or, if global numbering must survive plan-review history, state explicitly in the overview/batch 2 scope that Card 3 was deliberately retired and its number retired with it — per the "Global step numbering: unique, sequential, no gaps" criterion this is a finding regardless of the historical cause.

### [NIT:design] Batch 5's `verify: null` skips a checkable marker for Card 7
**Location:** Batch 5 (`05-review-loop-fixes.md`), frontmatter `verify: null` / Batch Tests. **Issue:** Card 7's fix is a literal, greppable Python keyword change (`latest=True`) — same style of marker batches 3/4/6/7 use — yet the batch-level `verify:` is left fully `null` because Card 8's insertion is prose-only and can't be checked the same way. **Fix:** Set `verify:` to a `python -c` assertion checking `latest=True` landed in `holistic-review.md` (covering Card 7 deterministically), and keep relying on read-through review for Card 8's prose alone, rather than forgoing automated verification for the whole batch.

### [NIT:consistency] Card 8 overstates how "identical" the two edit sites are
**Location:** Batch 5 (`05-review-loop-fixes.md`), Card 8, Edit A vs. Edit B. **Issue:** Edit A quotes `holistic-review.md`'s intervening sentence verbatim ("The fixer loads `mill-receiving-review` and applies the NITs. Do NOT re-review — the NIT fix is trusted. On stuck → escalate...") and calls Edit B's location "the identical shape," but `SKILL.md`'s actual intervening sentence (line 841) is longer and materially different ("...applies the NITs from the APPROVE'd review file. Parse the JSON report the same way as step 2 — including the exit-code-1-with-stuck-JSON behavior... The NIT-fix session commits its own source-file changes atomically..."). The insertion point is still unambiguous (the "After the NIT-fix completes successfully..." anchor sentence is byte-identical in both files), so this doesn't block correctness. **Fix:** Reword Edit B to note the intervening sentence differs in wording between the two files, only the insertion-point anchor is identical, to avoid an implementer expecting literal-identical prose to diff against.

## Verdict

REQUEST_CHANGES
Card-numbering gap (missing Card 3) must be resolved or explicitly justified before approval.
MILL_REVIEW_END
