# Plan-fix round 2

Review: `_mill/reviews/20260801-161639-plan-review-r2.md`

## Fixed

- [BLOCKING] "Card 7 Context omits `_review_common.py`" — confirmed Requirements names `RE_SIMPLE`/`RE_BATCH`, defined in `_review_common.py`, which was absent from Context. Added `plugins/mill/scripts/_review_common.py` to Card 7's Context.
- [BLOCKING] "Card 11 Context omits `_parent_branch.py`" — confirmed the ParentBranchError claim in Card 11's Requirements had no supporting Context entry. Added `plugins/mill/scripts/_parent_branch.py` to Card 11's Context.
- [BLOCKING] "Cards 8 & 9 self-resolve actions skip the audit-trail decision" — confirmed Shared Decision `audit-trail-via-status-timeline` lists `mill-go-handoff-gates` under "Applies to" and Cards 8/9's self-resolve commits had no `_status.append_phase` call. Added `_status.append_phase(status_path, "self-resolved-terminal-dirt", ...)` / `"self-resolved-scope-violation"` calls, folded into each card's existing commit step.
- [NIT] "Shared Decision text overstates batch 4's mechanism" — confirmed Batch 4's own Batch Scope says its gates have no existing autonomous branch, contradicting the overview's "batches 1-4" gate-removal claim. Narrowed the decision to "batches 1-3" and added a sentence noting batch 4 adds new self-resolve logic instead.

## Pushed Back

(none — all four findings were accurate and fixed)
