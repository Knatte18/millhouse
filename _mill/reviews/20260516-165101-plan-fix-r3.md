# Plan-fix round 3

```yaml
round: 3
source_review: 20260516-164521-plan-review-r3.md
date: 2026-05-16
```

## Fixed

- [BLOCKING] "`_wiki.read_junctions` not updated -- post-migration cleanup is junction-unsafe" -- added a new batch 5 `wiki-helpers-post-migration` (depends-on: [1, 2]) with card 23 retargeting `_wiki.read_junctions` AND `_wiki.read_hardlinks` (same bug exists in the latter; preemptively fixed) to read from `hub_root / "mill-config.yaml"` with `try/except SystemExit` fallback to `_paths.resolve_wiki_path(hub_root) / "config.yaml"`. Signature changes from `(wiki_root)` to `(hub_root)`. Call sites in `millpy-cleanup.py:611` (passes `git_root`) and `_setup.create_hub_links` (derives `hub_root = _paths.resolve_git_root()` internally, no SKILL.md changes) are updated. The batch-2-dep on batch 5 serialises the two cleanup-file edits to avoid parallel-modifies-overlap.
- [BLOCKING] "`_wiki.health_check` always raises post-migration -- permanently breaks mill-go" -- batch 5 card 24 retargets `_wiki.health_check` to accept `hub_root: Path` and verify "at least one config source exists" -- success when `hub_root/mill-config.yaml` OR `_paths.resolve_wiki_path(hub_root)/config.yaml` exists. Failure raises `WikiHealthError` with both searched paths in the message. mill-go SKILL.md call sites at approximately lines 120 and 296 are updated to compute `hub_root = _paths.resolve_git_root()` and pass it instead of `wiki_path`. Exception class and HALT recovery message are unchanged.
- [NIT] "Card 10 leaves `resolve_wiki_path` failure mode unspecified" -- card 10's step 2 now uses the same verbatim phrasing as card 11: `try: ... except (Exception, SystemExit): wiki_cfg_path = None`. Card 11's prior wording ("if wiki resolution raises, treat wiki_cfg_path as None") is now also tightened to spell out the same `try/except (Exception, SystemExit)` shape for symmetry.

## Pushed Back

_None._
