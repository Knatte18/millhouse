# Plan fix -- round 1

```yaml
review: 20260808-173652-plan-review-r1.md
round: 1
```

## Note on the round-1 envelope

The round-1 CLI envelope reported `blocking_count: 0` and `nit_count: 0` against a `REQUEST_CHANGES` verdict.
Those scalars are wrong and were not used for routing.
The reviewer read the plan and adopted the syntax this very task introduces, emitting `### [BLOCKING:design]` and `### [NIT:consistency]` headings, which the installed plugin cache's un-widened `parse_blocking_count` pattern (`^###\s+\[BLOCKING\]\s+`) cannot match.
This is the exact defect batch 1 card 5 fixes, observed in this task's own review loop.
The review file was treated as authoritative: 3 BLOCKING and 2 NIT findings, routed as step 4d.

## Fixed

- `20260808-173652-plan-review-r1.md` -- "[BLOCKING:design] Demotion rewrite has no path for YAML-block findings".
  Accurate: `extract_findings` reads both mechanisms but the rewrite only touched headings.
  Renamed `rewrite_demoted_headings` to `rewrite_demoted_findings` and specified both branches -- heading rewrite plus `severity:` / `demoted_from:` rewrite inside the source fenced `findings:` entry -- keyed on the finding's title.
  Chose title-keyed dual rewrite over the reviewer's suggested origin-tagged rewrite: dedup keeps only one `Finding` for a title present in both mechanisms, so an origin tag would leave the other representation stale.
  Added the corresponding assertions to card 8 (YAML-only demotion, and a title present in both mechanisms) and recorded the rule in the `demotion-rewritten-into-review-file` Shared Decision.
- `20260808-173652-plan-review-r1.md` -- "[BLOCKING:design] Plan-review carryforward entries break findings/count consistency".
  Accurate: carryforward entries contributed to the aggregate scalars but not to the aggregate `findings` list.
  Card 14 now has `_scan_approved_batches` derive its counts from `extract_findings` and return a `findings` list.
  Extended the same treatment to `run`'s crash-recovery re-read, which has the identical problem and feeds the same aggregation -- fixing only the site the reviewer named would have left the inconsistency in place one branch over.
  Neither site applies the ceiling, which the `ceiling-applied-once-at-write-time` Shared Decision still forbids; extraction is not ceiling application, and both sites read a file already written in demoted form.
  Added the carryforward-plus-aggregation test to card 16, asserting `len(findings) == blocking_count + nit_count` and membership together.
- `20260808-173652-plan-review-r1.md` -- "[BLOCKING:scope] bench-reviewers.py still hardcodes retired GAP/NOTE severities".
  Accurate: `bench-reviewers.py` counts discussion findings via `severity="GAP"` / `"NOTE"` and appeared in no batch.
  Folded into batch 2 card 12 rather than added as a new card -- card 12 is already the "retire the old vocabulary from non-production scripts and fixtures" card, and a new card would have forced renumbering 17 downstream cards for one file.
  Added the file to the overview's All Files Touched.
- `20260808-173652-plan-review-r1.md` -- "[NIT:consistency] Batch 3 scope prose miscounts finalize_scope call sites".
  Accurate: `_review_plan.py` has five, not four.
  Corrected the count in batch 3's Batch Scope.
- `20260808-173652-plan-review-r1.md` -- "[NIT:consistency] Card 17's blanket findings:[] clause matches no extra site".
  Accurate: the clause said "review-entry dict that currently sets `blocking_count=0`", but it is the enclosing `ReviewResult` that sets that, never the nested dict.
  Reworded rather than dropped, which is the stronger fix the finding's own analysis points to: the six nested `reviews=[{...}]` dicts in `_review_code.py` genuinely lack a `findings` key and should carry it for envelope-shape consistency, while the outer `ReviewResult` calls need no argument because the dataclass default already supplies `[]`.
  Applied the same correction to batch 2 card 9, which had the identical sibling defect in `_review_discussion.py`.

## Pushed Back

(none)
