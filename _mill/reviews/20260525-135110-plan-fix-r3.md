# Plan-fix report — round 3

```yaml
round: 3
fixer: mill-plan
source_review: 20260525-134734-plan-review-r3.md
date: 2026-05-25
```

## Fixed

- `20260525-134734-plan-review-r3.md` `[BLOCKING] Card 8 phase-guard tests silently exercise wrong path` — Added a full "Seed daemon state for EVERY surviving test that invokes `millpy_fold.main(...)`" block to card 8. The block explains why text-only `_setup_tempfile_wiki` is insufficient (V3 daemon reads `tasks.json`, not Home.md text), enumerates the affected tests at lines 234, 259, 284, 306, 330, 352, 372, 395, 416, 443+, 474+, 515+, and prescribes extending `_setup_tempfile_wiki` to accept a `tasks: list[dict]` parameter that seeds via `wiki.upsert_task(wiki_path, **task)`. Includes a per-call-site validation grep.
- `20260525-134734-plan-review-r3.md` `[BLOCKING] Card 12 pytest.skip used on bare-script integration tests` — Replaced the `pytest.skip` instruction in card 12 with a script-friendly pattern: delete the helper outright, or replace function body with `print(f"SKIP: ...; see #NNN"); return 0` plus removal from the file's runner list. Cap restated as "at most 2 functions across the four files". Explicit prohibition on `pytest.skip` for bare scripts added.
- `20260525-134734-plan-review-r3.md` `[BLOCKING] Card 12 wiki local variable collision in test-merge.py` — Added per-file rename instruction for `test-merge.py:90` (`wiki = container / "wiki"` → `wiki_path = container / "wiki"` end-to-end across the file) BEFORE adding `from wiki import _client as wiki`. Same pattern as card 6. Other integration test files also get a conditional rename note depending on whether they call V3 client functions.
- `20260525-134734-plan-review-r3.md` `[NIT] Card 7 leaves V2 wiki/config.yaml reference in _worktree.py docstring` — Added a sub-bullet for line 206 (`wiki/config.yaml` → `mill-config.yaml`) in card 7's `_worktree.py` block. Also added a grep guard `grep -nE "wiki/config\.yaml" plugins/mill/scripts/_paths.py plugins/mill/scripts/_junction.py plugins/mill/scripts/_worktree.py` returning zero matches.
- `20260525-134734-plan-review-r3.md` `[NIT] Card 4's iteration-var grep regex may miss real callsites` — Broadened the regex from `\b(t|task|entry|picked|chosen|c|cand)\.(slug|...)` to `[a-zA-Z_][a-zA-Z0-9_]*\.(slug|...)`, accepting higher false-positive rate in exchange for completeness. Added a sentence explaining that remaining matches require by-hand triage.

## Pushed Back

_(none)_
