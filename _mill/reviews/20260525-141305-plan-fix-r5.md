# Plan-fix report — round 5

```yaml
round: 5
fixer: mill-plan
source_review: 20260525-141024-plan-review-r5.md
date: 2026-05-25
```

## Fixed

- `20260525-141024-plan-review-r5.md` `[BLOCKING] parse_home_md does not produce has_proposal / id keys` — Added a "Shape-compat fix to wiki/_parse.py" sub-section to card 4. Card 4's Edits now includes `plugins/mill/scripts/wiki/_parse.py`. The fix: in the `tasks.append({...})` block at `wiki/_parse.py:104-113`, add `"has_proposal": bool(proposal_match)` so the returned dict shape matches `wiki._client.list_tasks_brief`. `id` is intentionally NOT added (TinyDB doc ids aren't derivable from text). After this, `parse_home_md` is shape-compatible for the 6 keys the picker code reads — picker tests under `test-spawn-core.py` will not `KeyError`.
- `20260525-141024-plan-review-r5.md` `[BLOCKING] Card 13 prescribes pytest.skip for bare-script tests` — Removed every `pytest.skip` reference from batch 6's scope section, card 13's failure-recovery step, and the closing Batch Tests block. Mirrored card 12's bare-script skip pattern: drop the failing test from the per-file `tests = [...]` runner list AND add a top-of-file `# SKIP: see #NNN -- <reason>` comment. Explicit "NEVER `pytest.skip`" prohibition stated since every test in this repo is a bare Python script (not pytest collection).
- `20260525-141024-plan-review-r5.md` `[NIT] millpy-wikipush "post-edit shape" duplicates existing line` — Clarified that the pre-existing `changed = _changed_files(wiki)` line stays put; only the `return _push_inner(...)` body line un-indents from the deleted `with` block. Added explicit "Do NOT duplicate" guard.
- `20260525-141024-plan-review-r5.md` `[NIT] Card 12 dict-literal uses "brief": None` — Changed the replacement dict-literal template from `"brief": None` to `"brief": ""` with a rationale citing both `parse_home_md` and `Store.list_tasks_brief` producing empty-string for unset brief.

## Pushed Back

_(none)_
