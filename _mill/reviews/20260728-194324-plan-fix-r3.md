# Plan fix — round 3

## Fixed

- [NIT] Asymmetric dead-code cleanup between Card 2 and Card 4 — Card 4 already instructed removing the dead `verdict = parse_verdict(raw)` reassignment in the holistic NEED_CONTEXT resume-retry-success branch, but Card 2 left the analogous per-batch reassignment (in `_review_one_batch()`, immediately after `# Second NEED_CONTEXT propagates to caller untouched.`) untouched. Verified the site exists at `_review_plan.py:282`. Added the same removal instruction to Card 2.

## Pushed Back

(none — finding verified accurate with no harm from fixing)
