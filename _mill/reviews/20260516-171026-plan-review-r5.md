All four review findings addressed and committed:

- **[BLOCKING] Card 24** — added explicit `UPDATE two existing health_check tests` section before the append list, specifying exactly how to update test (g) (seed `mill-config.yaml`, assert `None`) and test (h) (monkeypatch `resolve_wiki_path` to raise `SystemExit`, assert `WikiHealthError` with `"mill-config.yaml"` in message), and that test (i) is unchanged.
- **[NIT] Batch 1 scope** — corrected "12 entries" → "14 entries" for `mill-agents.yaml`.
- **[NIT] Card 14 rationale** — changed "causes import errors at runtime once `_machine.py` is deleted in batch 4" → "causes assertion failures at runtime since `_config.load_config` no longer merges the machine layer".
- **[NIT] Card 11 private import** — renamed `_resolve_plugin_template_path` → `resolve_plugin_template_path` throughout all plan files (Cards 10, 11, 16, 18, and the setup-migration batch), with Card 10 now explicitly requiring the public name and `__all__` registration.