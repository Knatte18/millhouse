## Fixed

- `20260728-193445-plan-review-r4.md` — "[BLOCKING] Card 14 missing Context entry for Card 12's file": verified — Card 14's Requirements name a description string introduced in `merge-in-conflict-brief.md` (Card 12's Edits target) but Card 14's Context was `none`. Added `plugins/mill/templates/merge-in-conflict-brief.md` to Card 14's Context.
- `20260728-193445-plan-review-r4.md` — "[NIT] Card 16 fixture weight for status-only checks": verified — only the mid-retry-squash case needs a real worktree/child branch; the dirty-edit and untracked-file cases only need a hub-shaped repo with one tracked file. Rewrote Card 16 to use a lightweight `git init` + one-commit fixture for those two cases, keeping the full `_setup_trio()` spin-up only for the mid-retry case.

## Pushed Back

(none — both findings verified accurate against source and fixed)
