# Plan Fix Report — round 3

```yaml
slug: mill-infra-and-path-fixes
round: 3
source_review: _mill/reviews/20260615-110035-plan-review-r3.md
verdict_of_source: REQUEST_CHANGES
blocking: 1
nit: 3
```

## Fixed

- `20260615-110035-plan-review-r3.md` — [BLOCKING] "Card 11 cat-file path needs posix normalization": Card 11 now requires rendering the worktree-relative `task_dir` with `.as_posix()` before building the `<rev>:<path>` pathspec, with a note that a raw `str(...relative_to...)` emits backslashes on Windows and git would reject it (helper would always return False).
- `20260615-110035-plan-review-r3.md` — [NIT] "#470 fix not applied to the second load_config in _review_common.py": verified `_review_common.load_config` (line ~1391/1419) is a separate implementation with the same single-path bug, and reviewer/model selection flows through it. Card 2 now adds a shared `resolve_repo_config_path(hub_root, worktree_root)` helper to `_config.py` and rewires BOTH `_config.load_config` and `_review_common.load_config` to use it; `_review_common.py` added to Card 2 Edits and to overview All Files Touched; Card 3 gains a parallel `_review_common.load_config` container-layout assertion. (Verified accurate, not pushed back — the symptom would otherwise persist for mill-go.)
- `20260615-110035-plan-review-r3.md` — [NIT] "Card 9 names _parent_branch.resolve but _parent_branch.py not in Context": added `plugins/mill/scripts/_parent_branch.py` to Card 9 Context.
- `20260615-110035-plan-review-r3.md` — [NIT] "Card 8 rename ` -> ` parsing is dead code": verified `_pygit2_util.status_porcelain` docstring (lines 175-176) states index renames return only the new path; Card 8 now drops the rename clause and uses `line[3:]` only.

## Pushed Back

(none)

## Note

Adding `_review_common.py` to batch 2 Edits created a parallel-modifies-overlap
with batch 1 (which also edits `_review_common.py` for #475). Resolved per the
validator fix table by adding a dependency edge: batch 2 (`config-repo-layer`)
now `depends-on: [1]`, serializing the two edits to that file.
