# Discussion-fix report — round 1

```yaml
source_review: _mill/reviews/20260709-130432-discussion-review-r1.md
round: 1
```

## Fixed

- **#610 leaves existing carve-out prose contradictory** — `implementer-liveness-probe` decision now explicitly requires narrowing SKILL.md's line-131 carve-out to the clean-turn-exhaustion sub-case only, and splitting the line-133 "OR stopped/interrupted" clause out of the shared immediate-finalize paragraph into its own probe-gated branch.
- **#610 discriminator between stopped/interrupted and turn-exhaustion unstated** — decision now states the discriminator is the same harness stopped/interrupted notification signal step 4(c) already keys on: presence → probe branch; absence → clean turn-exhaustion, unchanged.
- **#616 card-count self-check range-start undefined for fresh dispatch** — decision now specifies the self-check reuses the brief's existing `git log --grep="^mill-go: start batch" -n 1 --format=%H` fallback when `<START_SHA>` is empty, exactly mirroring "Resume-after-incomplete".
- **#616 regression-test assertion form left as two alternatives** — decision now commits to the allowlist form (`{"sonnet", "sonnethigh", "sonnetmax", "opus", "opushigh"}`, all verified present in `mill-agents.yaml`), dropping the "or simply not haiku" alternative.
- **#619 residual prose-drift risk not acknowledged** — added a "Residual risk (accepted)" note to `nits-only-envelope-threading` acknowledging that the SKILL.md step-6 re-pass instruction remains hand-maintained, untested prose, in the same category as the other four wording-only fixes in this task.

## Pushed Back

None — all five findings were accurate and independent of each other; no harm-check grounds to reject any.
