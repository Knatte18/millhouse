# Plan Fix — round 1

Source review: `20260812-184850-plan-review-r1.md` (verdict: REQUEST_CHANGES, blocking_count: 1, nit_count: 1)

## Fixed

- **[BLOCKING:design]** "Batch 3's 'sole site' premise is false; Card 11's test additions will
  fail" — Verified by direct read: `_review_plan.py::_review_one_batch` and
  `_review_plan.py::run()`'s holistic branch, plus `_review_code.py::run()`'s two inline
  `except ReviewError` blocks, all build their own ERROR dicts independently of `finalize()`.
  Chose the reviewer's second offered remedy over the first: rather than expanding Cards 8/10 to
  also patch `run()`/`_review_one_batch()` (which would (a) go beyond discussion.md's
  five-round-negotiated Scope, which names only the three `finalize()` wrapper functions, and
  (b) mis-tag non-`parse_verdict` `ReviewError`s — e.g. `_review_one_batch()`'s round-guard and
  `resolve_ref_paths` hard-failures share its one `except ReviewError` block with the genuine
  `parse_verdict` failure — as `error_kind: "reviewer"`, which would make Batch 5's SKILL.md retry
  logic pointlessly retry a deterministic, non-reviewer-output error), rewrote Card 11 to call each
  `finalize()` wrapper directly (matching the discussion's own Testing section language: "unit test
  calling each `finalize()` wrapper directly") instead of extending the existing `run()`-based Test
  20 / Test 15 cases. Also rewrote Batch 3's `## Batch Scope` to state this boundary explicitly, so
  a future reader (or reviewer) doesn't rediscover the same false premise. Cards 8, 9, 10 are
  otherwise unchanged.
- **[NIT:consistency]** "Card 7 wrongly claims `ReviewError` is already imported in
  test-review-plan-finalize-round.py" — Confirmed by direct read: the file's only `_review_common`
  import is `from _review_common import discover_round`. Corrected Batch 2 / Card 7 to instruct
  changing that import to `from _review_common import ReviewError, discover_round` instead of
  asserting it already exists.

## Pushed Back

(none)
