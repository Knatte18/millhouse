```yaml
round: 1
source: _mill/reviews/20260820-174649-discussion-review-r1.md
```

## Fixed

- `20260820-174649-discussion-review-r1.md` — "`head` field format contradicts itself across two
  decisions": added a clarifying note to `duplicate-pr-detection` explaining that GitHub's
  list-pulls endpoint requires the owner-prefixed `owner:branch` form for its `head` filter while
  create-pulls (same-repo) does not — the two commands use different, individually-correct
  formats for different endpoints, not an inconsistency. Added a matching forward-pointer in
  `rest-payload-construction`.
- `20260820-174649-discussion-review-r1.md` — "Pattern-matching rejected for trigger, then relied
  on for duplicate detection": added a clause to `duplicate-pr-detection`'s rationale explaining
  that GitHub's "already exists" wording is a stable, canonical 422/GraphQL response for a
  specific condition, unlike the transient-outage error text rejected in `rest-fallback-trigger`
  (which genuinely drifts across `gh` versions/outage types) — the two are not the same kind of
  text match.

## Pushed Back

(none)
