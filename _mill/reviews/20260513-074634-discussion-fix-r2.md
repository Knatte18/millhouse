# Discussion-fix report -- round 2

```yaml
slug: safe-rmtree
round: 2
source_review: _mill/reviews/20260513-074601-discussion-review-r2.md
```

## Fixed

- `_mill/reviews/20260513-074601-discussion-review-r2.md` [NOTE] "POSIX walk described as no-op but symlink test contradicts that" -- reworded `### Platform behaviour` so only `isjunction`/`st_file_attributes` branches are `os.name == "nt"`-guarded; `entry.is_symlink()` runs cross-platform, consistent with `test_strips_symlink_inside_tree (POSIX)`.
- `_mill/reviews/20260513-074601-discussion-review-r2.md` [NOTE] "`test-cleanup.py:481` false positive in callsite analysis" -- removed the spurious `unit_tests/test-cleanup.py:481` bullet from the callsites-to-migrate list; the line is the comment string `worktree NOT rmtree'd` which the regex `shutil\.rmtree` would not match.

## Pushed Back

(none)
