# Plan-fix report — round 3

Source review: `_mill/reviews/20260623-085945-plan-review-r3.md` (holistic, APPROVE, 0 BLOCKING + 3 NIT).

## Fixed

- [NIT] "Decision overstates resolve_hub_path 'never cwd'" (00-overview Decision) — reworded the "cwd-independent hub resolution" Decision to "a cwd walk up to the `.millhouse/` marker, stub-aware ... rather than a raw `Path.cwd()` literal", and noted that `resolve_hub_path()` does seed its walk from `Path.cwd()`. No longer implies a marker-only signature.
- [NIT] "Card 8 card_count regex over-escaped for findall" (Card 8) — clarified that the implemented regex uses single-backslash raw-string form (`r"(?m)^###\s+Card\s+\d+\s*:"`) and that any doubled backslashes in the plan markdown are escaping artifacts only; warned that the doubled form would silently disable the gate.
- [NIT] "Manifest omits test-millpy-implement.py" (Batch 2 / Card 8) — added `plugins/mill/unit_tests/test-millpy-implement.py` to Card 8's `Context:` so the implementer reads the mocked-git fixture environment and the file enters the code-review bulk manifest. (Reviewer marked this informational; the Card 6 int()/exception guards already anticipate the fixture.)

## Pushed Back

(none)
