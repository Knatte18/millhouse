MILL_REVIEW_BEGIN
# Review: mill-go: baseline-stage timeout/cold-build cost and finalize dirty-tree false positive — holistic

```yaml
duration_s: 157.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-21
```

## Findings

No findings. Verified against source, all three batches:

- **Batch 1** (`_implementer_common.py:463-473`): `owned_paths` set-comprehension excludes `_mill/briefs/` exactly as specified; docstring paragraph inserted verbatim in the right location. `test-implementer-common.py` Case 76/77 (lines 5178-5319) match Card 2's fixture steps; both cases correctly add an extra content commit (`marker.txt`) between `batch_start_sha` capture and the dirty rewrite, mirroring the file's own pre-existing Case 74 convention — required to clear the earlier no-content-commit gate (`_implementer_common.py:1780-1819`) so the test actually exercises the dirty-tree gate. Case 77 asserts `README.md` is named and the briefs path is excluded from `reason`, confirming scoped (not blanket) exclusion.
- **Batch 2**: `_run_baseline_stage` signature/docstring/insertion point/call-site threading (`millpy-implement.py:216-223, 285-289, 397-414, 669-673`) match Card 3 verbatim, correctly reusing `_verify_baseline._run_verify_in` with no new wrapper (per the Shared Decision). `cfg` reload ordering (line 594 before 669) satisfies the plan's constraint. Both `mill-config.yaml` and the template gain the identical `baseline_prepare_cmd: null` key/comment (Card 4). `test-millpy-implement.py`'s three new tests (lines 1644-1750) are inserted at the exact specified location, use the established `patch.object`/`_write_two_batch_fixture`/`_run_main` idioms, and correctly assert `{None}`-fragment single-call behavior, no-op-when-unset, and non-fatal-failure-still-completes.
- **Batch 3**: `mill-go-base/SKILL.md` "0.5"/"0.6" dispatch blocks (lines 537-614) replaced with the `millpy-bg.py` background+poll pattern exactly as specified, including the pwd-guard note, the `_bg.check_bg_status` liveness branch (verified against `_bg.py:117-159`, whose `"running"`/`"exit"`/`"dead"` return values match the documented branching), the `env PYTHONPATH=` wrapper rationale for "0.6"'s inner worktree-form command, and the updated failure-handling/parse-source-reference wording. No stray reference to the old 600000ms timeout paragraph remains in either edited section (the two other 600000ms mentions in the file, lines 374/526, belong to untouched, out-of-scope sections).

Cross-batch check: all three batches are file-disjoint as declared (`depends-on: []` each); "All Files Touched" in the overview lists exactly the 7 files edited, no surprise files present.

## Verdict

APPROVE
All three batches match their plan cards precisely; cross-batch contracts, shared decisions, and test coverage are sound.
MILL_REVIEW_END
