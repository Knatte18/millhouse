# Plan fix — round 1

```yaml
round: 1
source_review: _mill/reviews/20260919-113434-plan-review-r1.md
```

## Fixed

- `20260919-113434-plan-review-r1.md` — "[BLOCKING:design] Card 1's new tests can't produce the dotted keys RENAMED_KEY_HINTS needs": verified against `_config.walk_unknown_keys` (`_config.py:88-109`) that `_setup_plugin_template`'s fixture template has no `pipeline:` key, so it can never produce the dotted `pipeline.max_review_rounds`/`pipeline.max_discussion_review_rounds` unknown-key paths the new hint lookup needs. Rewrote Card 1's test-authoring instructions so both new tests build their own local plugin template with an explicit `pipeline: {}` key (mirroring `test_worktree_template_augments_template_cfg`'s local-template pattern, `test-config.py:867-931`) instead of reusing `_setup_plugin_template`.

## Pushed Back

(none)
