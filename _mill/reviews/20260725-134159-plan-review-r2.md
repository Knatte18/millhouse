MILL_REVIEW_BEGIN
# Review: mill-plan review severity counting and validation schema gaps — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/
date: 2026-07-25
```

## Findings

### [NIT] Card 8's Context: omits `_review_common.py` despite naming its functions
**Location:** batch 03-severity-vocabulary-docs / card 8
**Issue:** Requirements names `_review_common.finalize_scope()` and `count_unrecognized_severity_findings()` (both defined in `_review_common.py`), but Context: is `none` and Edits: is only `review-output.schema.md` — literal violation of the Context-completeness rule.
**Fix:** Add `plugins/mill/scripts/_review_common.py` to Card 8's Context:. Low practical risk since the exact paragraph to write is already fully dictated verbatim in Requirements.

### [NIT] Card 10 updates only `run()`'s docstring, not the module-level "Checks performed" list
**Location:** batch 04-commit-none-validator / card 10
**Issue:** `_plan_validate.py`'s top-of-file docstring (lines 8-42) enumerates every check key by name; Card 10 only instructs updating `run()`'s inline docstring summary, leaving the module-level list without the new `commit-none-with-content` entry (that list is already stale for `out-of-worktree-target`/`batch-oversized` pre-existing this plan, but the new check compounds it).
**Fix:** Add an instruction to append `commit-none-with-content` to the module docstring's "Checks performed (check keys):" list, mirroring how cards 1 and 9 explicitly require updating `_review_common.py`'s/`_plan_dag.py`'s "Public API" docstring lists.

### [NIT] Card 15 calls a new parameter "keyword-only" without adding `*` to the signature
**Location:** batch 06-commit-none-backend-gate / card 15
**Issue:** `_reclassify_verify_failure`'s current signature has no bare `*` separator (`cards_done=None` is a regular positional-or-keyword param), so simply appending `commit_none_card_ids: set[int] | None = None` after it does not make it keyword-only in Python semantics, unlike Card 16's target functions (`finalize_from_output`/`_forward_output`), which do use `*`.
**Fix:** Clarify Card 15 either to insert `*,` before `commit_none_card_ids` (making the term accurate) or drop the "keyword-only" wording — functionally harmless either way since every existing call site already passes `cards_done=` by keyword.

## Verdict

APPROVE
Plan is precisely grounded against source (line numbers, function names, existing test patterns all verified); only cosmetic NITs found.
MILL_REVIEW_END
