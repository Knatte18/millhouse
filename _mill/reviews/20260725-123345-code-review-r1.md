MILL_REVIEW_BEGIN
# Review: Wiki daemon error-log leak and stale plugin-cache config validation produce misleading noise — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-07-25
```

## Findings

No findings. Both batches were verified end-to-end against the plan:

- Batch 1 (`_daemon.py`, `wiki/_client.py`, `wiki/_server.py`): the pre-dispatch recv/decode/parse region in `_handle_connection` (`_daemon.py:130-151`) is now one inner `try` catching `(OSError, json.JSONDecodeError, UnicodeDecodeError)`, logging at `debug` and returning without a response; the outer `except Exception` (dispatch-and-beyond) still logs at `error` and attempts a response, confirmed unaffected for both benign (`OSError`) and non-benign (`KeyError`) types raised from `handle_request`. `_spawn_server` (`wiki/_client.py:687-703`) adds `stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL` to both Popen calls with every other kwarg preserved. `WikiServer.__init__` (`wiki/_server.py:58`) passes `"wiki-server"` to `super().__init__`, making `self._logger is self._log` (verified singleton identity via `logging.getLogger`); the dead `logging.basicConfig` block is gone from `DaemonBase.run()`, and grep confirms no other bare root-level `logging.*` call in `wiki/_server.py`. `test-wiki-daemon.py` cases (y)-(ac) cover all five required scenarios with matching assertions on `sendall`/`close`/`debug`/`error` call state.
- Batch 2 (`_review_common.py`): the duplicate `load_config`/`_deep_merge` implementation and its now-unused imports (`copy`, `_apply_dispatch_shim`, `apply_env_overrides`, `warn_unknown_keys`) are fully removed (grep-confirmed zero remaining references). The new `load_config` wrapper (`_review_common.py:1826-1896`) performs the missing-source check independently of the delegate's return value, delegates the core merge to `_core_load_config`, and re-derives the stale-`review:`-key warning via its own read of `config.local.yaml` — matching the plan's required order and wording exactly. `test-review-common.py` adds `import _config` (line 21) and a new delegation test (lines 885-934) that patches both `_config.resolve_plugin_template_path` and `_review_common.resolve_plugin_template_path` to the same fake cache template, writes a worktree-local template + repo config with the new key, and asserts the unknown-key warning is absent while the value merges through — a correct regression test for the cache-lag augmentation, and it does not touch the five pre-existing `load_config` blocks.

Cross-batch check: no file present outside the two batches' `Edits:`/`Creates:` lists; `_config.py` and `wiki/_client.py`'s in-progress line numbers referenced in the plan drifted slightly post-edit (e.g. self-call now at line 373, not 376) purely due to import-count shrinkage — this is expected line drift, not a deviation from the required code.

## Verdict

APPROVE
Both batches match plan requirements precisely; tests correctly exercise every new behavior with no gaps or regressions.
MILL_REVIEW_END
