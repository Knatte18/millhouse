# Plan fix: round 2

```yaml
slug: mill-go2-fork-fixer
round: 2
source: _mill/reviews/20260811-141949-plan-review-r2.md
```

## Fixed

- `20260811-141949-plan-review-r2.md` — "[BLOCKING:scope] Card 4 Context omits _status.py despite
  naming its exact signatures": added `plugins/mill/scripts/_status.py` to card 4's `Context:`, and
  added a requirement that the implementer confirm both `_status` call signatures against the module
  as batch 1 finalized them before writing the override block. Batch 2's context estimate stays well
  under `pipeline.max_batch_context_tokens` with the file added.
- `20260811-141949-plan-review-r2.md` — "[NIT:consistency] mill-go2 frontmatter description becomes
  false after this batch": added an explicit card 4 requirement replacing the `description:` line,
  with the exact replacement text given verbatim. Took the reviewer's first offered fix (update the
  line), not the second (declare it intentionally stale) — the claim is simply false after this card
  and nothing justifies shipping it. The reworded line is 32 bytes longer, projecting the file at
  2364 bytes against the 4096-byte cap;
  the card's byte-accounting paragraph was updated to say the figure includes it.

## Pushed Back

(none)
