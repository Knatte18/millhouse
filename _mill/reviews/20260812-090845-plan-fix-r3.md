# Plan fix report — round 3

Source review: `_mill/reviews/20260812-090302-plan-review-r3.md`

## Fixed

- r3 "`skills_dir.rglob(\"SKILL.md\")` cited but its file not in Context" (BLOCKING:scope) — accurate. Round 2's own fix introduced this: replacing the wrong frontmatter rationale with the real filename-glob mechanism cited an expression that lives in `millpy-skills-index.py:100`, a file neither card could read. `plugins/mill/scripts/millpy-skills-index.py` added to `Context:` on both card 14 and card 24, keeping the specific-expression claim rather than weakening it to the SKILL's generic description.
- r3 "Card 20 misstates the source file's step-6.5 sub-labels" (NIT:consistency) — accurate. `mill-go-base/SKILL.md` writes step 6.5's sub-items as bare `1.`/`2.`/`3.`; the dotted `6.5.1`/`6.5.2` form exists only in `mill-go2/SKILL.md`'s external citations. Card 20's opening description corrected, and it now says explicitly that the dotted form is mill-go2's citation convention handled by card 23.
- r3 "Overview's \"twelve-tree-guard-checkpoints\" rationale arithmetic doesn't add up" (NIT:consistency) — accurate on both counts: 5+7+1 is 13, and the source file's own "5 more" is a pre-existing off-by-one (there are 5 total in that section, so 4 more). Rationale rewritten to lead with the grep count and card 13's file-order enumeration, and to name the source sentence's off-by-one as the reason it is not being relied on. Card 13 already deletes that sentence, so the error does not survive the task.

## Pushed Back

None. All three findings were accurate and no fix caused harm.
