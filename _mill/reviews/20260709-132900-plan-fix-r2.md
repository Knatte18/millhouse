# Plan Fix — Round 2

```yaml
slug: codeguide-scope-resolution-fixes
round: 2
source_review: _mill/reviews/20260709-132842-plan-review-r2.md
```

## Fixed

- Source: `20260709-132842-plan-review-r2.md` — "[NIT] Parent resolution ordered before codeguide-initialized check": moved Card 7's parent-resolution prose to after the existing `found == false` skip check, so mill path resolution only runs once codeguide is confirmed initialized.
- Source: `20260709-132842-plan-review-r2.md` — "[NIT] Card 7 degrade path leans on unstated error wrapping": added an explicit instruction that `_paths.resolve_git_root()`/`resolve_hub_path()`/`resolve_task_path()` failures (not just a missing `parent:` row) must degrade to the no-arg invocation, since `resolve_for_codeguide` only swallows `ParentBranchError`.
- Source: `20260709-132842-plan-review-r2.md` — "[NIT] Scenario 14 setup prose is self-contradictory": rewrote the fixture as a clean numbered sequence (freeze `mill-checkpoint-feature` via `git branch` without checkout, then commit `a.py`/`b.py` on `main`), removing the garbled prior wording.

## Pushed Back

(none — all three findings verified accurate and fixed)
