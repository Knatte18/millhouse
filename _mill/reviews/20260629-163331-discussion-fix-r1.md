# Discussion fix report — round 1

```yaml
slug: mill-scope-and-infra-gaps
round: 1
source_review: _mill/reviews/20260629-163232-discussion-review-r1.md
verdict_resolved: APPROVE (2 NOTEs)
```

## Fixed

- r1 NOTE ".exe blanket rule reintroduces the rejected risk" — made the blanket-vs-gated `.exe` decision explicit in Decisions → go-artifact-allowlist: `.exe` is intentionally a blanket suffix rule (not gated by the `package main` heuristic), with documented rationale for the asymmetry vs. the rejected magic-byte option and an explicit accepted-residual-risk statement.
- r1 NOTE "#567 verification strategy is non-committal" — committed Testing → #567 to prose-only verification (inspection of the edited step 2/Rollback + the manual repro from issue #567), instructed plan writers to set that batch's `verify:` to `null`, and removed the hedged "decide later" language.

## Pushed Back

- (none)
