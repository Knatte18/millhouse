# Review: Accumulated bug fixes — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-19
```

## Findings

### [NIT] test_7 side_effect list size comment clarity
**Location:** Batch 1 / Card 3
**Issue:** Card 3 says "Keep `_subprocess_util.run` side_effect = `[<git diff>, <git rev-parse>]` unchanged" but doesn't explicitly note that the side_effect list must grow from length 1 (`return_value`) to length 2 for `subprocess.run` as well — only the `subprocess.run` change is described. A reader could conflate the two patches.
**Fix:** Add one sentence clarifying that `subprocess.run` is the patch receiving the `side_effect=[<fail>, <fail>]` upgrade, while `_subprocess_util.run` side_effect stays at its current 2-entry list unchanged.

## Verdict

APPROVE
Plan is correct, complete, and internally consistent across both batches.