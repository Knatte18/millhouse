# Plan-fix report: round 4

## Fixed

- `20260904-103738-plan-review-r4.md` — "[BLOCKING:design] Card 2 exposes dead-parent rebind to an absent status.md": Card 2's Requirements now except the dead-parent rebind write (and its commit/push) from running when `status_path.exists()` is `False`, mirroring `mill-merge/SKILL.md` Entry Step 4's own precedent for its `status_path`-absent fallback branch. The read-only liveness check itself still runs unconditionally.

## Pushed Back

(none)
