# Plan Fix Report — round 1

Source review: `_mill/reviews/20260515-091840-plan-review-r1.md`

## Fixed

- [BLOCKING] Card 13 uses class-based test API in a standalone-function file — rewrote both new test samples as standalone functions following the existing pattern (`with tempfile.TemporaryDirectory()`, `assert`, `print("PASS: ...")`); added instruction to register both in the `tests = [...]` list inside `main()`.
- [NIT] `validate_role_refs` called without module prefix in Card 13 — updated sample to use `_reviewers.validate_role_refs(cfg, registry)` matching existing file style; removed instruction to add a bare import.

## Pushed Back

(none)
