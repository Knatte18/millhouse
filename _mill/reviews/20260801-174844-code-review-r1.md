MILL_REVIEW_BEGIN
# Review: Non-interactive pipeline: only mill-start's interview may prompt the operator — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-01
```

## Findings

### [BLOCKING] mill-merge Step 1 uses `status_path` before Path Setup defines it
**Location:** `plugins/mill/skills/mill-merge/SKILL.md:23` (vs. `:35-41`)
**Issue:** The stale-worktree self-resolve text in Entry Step 1 calls `_status.append_phase(status_path, ...)` and `git -C <worktree> commit`, but `status_path` (and `worktree_root`) are not derived until Step 1.5 "Path Setup", which runs after Step 1 completes. An agent following the steps in order hits an undefined variable at the point of use.
**Fix:** Either move the Path Setup derivation earlier (before the stale-worktree edge check in Step 1), or inline the `worktree_root`/`status_path` computation into the stale-worktree edge paragraph itself before it is used.

### [BLOCKING] mill-go Principles still tells the Builder to surface stuck states to the user
**Location:** `plugins/mill/skills/mill-go/SKILL.md:965`
**Issue:** `## Principles` retains "**Never guess when stuck.** Surface to the user with concrete options; don't invent a recovery." This directly contradicts the batch-2/3/4 rewrite of `### Stuck escalation` and `## Holistic code review`, which now self-resolve once before escalating to a clean halt — there is no more "surface to the user with concrete options" path anywhere in this file. Left as-is, this principle actively misleads a future reader (or a future self-resolve implementation) back toward the old interactive behavior this task exists to remove.
**Fix:** Update or remove the bullet to match the new self-resolve-then-halt behavior (e.g. "self-resolve once per the stuck_type rules, then halt cleanly — never invent a recovery beyond the documented one-shot attempt").

### [NIT] Stale "explicitly named escape hatch" phrasing survives in Agent-mode properties
**Location:** `plugins/mill/skills/mill-go/SKILL.md:279`
**Issue:** "`AskUserQuestion` ... is banned here absent an explicitly named escape hatch for the calling phase" is leftover framing from before batches 2-4 removed every escape-hatch prompt in this file. mill-plan's equivalent line (batch 1, Card 3) was explicitly rewritten to state the ban is now unconditional; mill-go's parallel line was not touched by any card in this plan.
**Fix:** Reword to state the ban is unconditional now that no operator-facing escape hatch remains in mill-go, matching mill-plan's Card 3 fix.

## Verdict

REQUEST_CHANGES
Two BLOCKING correctness/consistency defects (undefined-variable ordering in mill-merge; stale contradictory Principle in mill-go).
MILL_REVIEW_END
