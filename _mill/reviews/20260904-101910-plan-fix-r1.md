# Plan-fix report: round 1

## Fixed

- `20260904-101729-plan-review-r1.md` — "[BLOCKING:design] Card 2 reorder drops status_path from the override branch": Card 2's Requirements now explicitly hoist the `status_path = _paths.resolve_task_path(_paths.resolve_hub_path(), "_mill/status.md")` assignment to run unconditionally at the top of Entry, before the branch check, so the retained Liveness-check paragraph's dead-parent rebind can still bind it regardless of which branch fires.
- `20260904-101729-plan-review-r1.md` — "[BLOCKING:design] Batch 2 scan can't reach the citation source its own example names": Card 5's Requirements now add a second scan part that greps the resolved `wiki_path` (already bound at mill-finalize's own Entry step 1) for the same markdown-link-context pattern, covering the wiki-board `Home.md` case the Shared Decision was framed around, which the worktree-only grep structurally cannot reach.
- `20260904-101729-plan-review-r1.md` — "[NIT:consistency] Card 3's unrestricted porcelain check may sweep unrelated dirty state": Card 3's Requirements now add a sentence explaining why the tree can only hold briefs/codeguide-doc changes at Step 5.5, given every earlier step that could leave state dirty already commits or rolls back per the file's own Rollback section.
- `20260904-101729-plan-review-r1.md` — "[NIT:consistency] Card 4's \"every other call site... already carries\" claim is inaccurate": Card 4's Requirements now correctly attribute the cwd-guard callout's precedent to `mill-go-base/SKILL.md`'s 0.5/0.6 sections and note this is the first `millpy-bg` call site in `mill-merge-in/SKILL.md` or `mill-merge/SKILL.md`.

## Pushed Back

(none)
