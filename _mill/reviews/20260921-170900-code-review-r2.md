MILL_REVIEW_BEGIN
# Review: Misc infra/wiki/PR/self-hosting reliability bugs — holistic

```yaml
duration_s: 286.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-21
```

## Findings

No findings.

Verified end-to-end across all 8 batches:

- Batch 1 (`wiki/_client.py`, `_sync.py`, `_server.py`): `_ensure_daemon`'s post-spawn loop now
  probes health via `_connect_send_recv`/`OP_HEALTH` instead of `wait_for_socket_reachable`;
  `commit_push`'s rebase-failure message names `millpy-wiki-shutdown.py`;
  `_render_and_commit_all`'s pre-render pull failure prints to stderr. Tests
  `test-wiki-health-check.py` (g), `test-wiki-sync.py` (u), `test-wiki-daemon.py` (z3) cover exactly
  the plan's specified cases.
- Batch 2 (`_pr_state.py`, `millpy-cleanup.py`, `mill-merge/SKILL.md`, `CLAUDE.md`):
  `resolve_pr_state` resolves `--repo` via `_gh_issues.detect_repo`, splits the combined
  returncode/empty-stdout check into two branches each carrying its own `error`, and the
  `none`-state PR-state gate in `mill-merge/SKILL.md` halts on non-null `error` before the
  phase-based fallback. `CLAUDE.md`'s cache-freshness bullet and the Step 4 citation-scan pointer
  are both present and cross-referenced correctly.
- Batch 3 (`millpy-merge-in-subagent.py`): `_run_recompute_baseline` now routes through
  `_plan_dag.parse_verify_field`, threads `cwd_override_relative`, and wraps the call in the
  established `{"status": "success", "baseline": "error", ...}` pattern.
- Batch 4 (`_status.py`, `_verify_baseline.py`, `_implementer_common.py`, `millpy-implement.py`,
  `mill-go-base/SKILL.md`): `baseline_parent_sha` accessors mirror `module_verify_baseline`'s
  mechanics; `compute_batch_baseline_on_demand` mirrors `compute_baseline`'s checkout/teardown
  shape; `_run_verify_gates`'s on-demand prelude fires only when uncached, persists+commits with
  the same try/except-pass convention as the adjacent corroboration-expansion code; `millpy-implement.py`'s
  `_run_baseline_stage` is reduced to module-wide + cheap SHA pin, `--module-wide-only` fully
  removed; `mill-go-base/SKILL.md`'s `0.6` section is deleted and the `0.5` speculative-launch text
  no longer references a second per-batch call.
- Batch 5 (`_agent_dispatch.py`, three `millpy-review-*.py`): `prepare_ts_path_for`/`derive_duration_s`
  added exactly as specified (including the deliberate, documented deviation from
  `_mill/discussion.md` placing the helper in `_agent_dispatch.py` rather than `_review_common.py`);
  all three CLIs' finalize calls now pass `_agent_dispatch.derive_duration_s(agent_output_path,
  args.duration_s)`.
- Batches 6–8 (doc-only): `handoff/SKILL.md` contains all four required elements distinctly;
  `csharp-build/SKILL.md` adds `-p:UseSharedCompilation=false /nr:false` to both commands with the
  rationale note; `mill-plan/SKILL.md` and `mill-start/SKILL.md` — re-verified via
  `grep -c 'mill-go-base/SKILL\.md'` vs. `grep -c '\${CLAUDE_PLUGIN_ROOT}/skills/mill-go-base/SKILL\.md'`
  — show exactly 12/12 and 10/10 matches respectively, confirming no bare, repo-relative, or doubled
  form remains (Card 17's implementation itself is correct; the prior round's non-blocking item was
  about the batch's *stated self-check* wording, not a doubling defect, and no new evidence of an
  actual doubling was found this round).

`## All Files Touched` in `00-overview.md` matches the reviewed source manifest exactly — no
out-of-plan files. No cross-batch helper duplication (all eight batches are independent DAG roots
per the plan's own Shared Decision, confirmed by disjoint edit sets). Local `import _verify_baseline`
inside `_run_verify_gates` matches this file's own pre-existing local-import convention (see the
adjacent `_corroborate_batch_failure` function, same file) — not a deviation.

## Verdict

APPROVE
All eight batches match their plan cards, tests, and shared decisions with no violations found.
MILL_REVIEW_END
