# Plan-fix round 2

## Fixed

- `20260729-074407-plan-review-r2.md`: "[BLOCKING] `check_and_restore` doesn't rebase paths for nested-hub layouts" — `_treeguard.check_and_restore` now takes an optional `git_root: Path | None = None` keyword and rebases git-root-relative porcelain paths onto `worktree` using the identical technique `_cleanliness.revert_out_of_scope_drift` already uses. Added a new `00-overview.md` Shared Decision documenting the fix, updated Batch 1 Card 1's implementation steps, and added two new Card 2 test scenarios (a mocked nested-hub regression test mirroring `_cleanliness.py`'s `ROOD-5`, and a `git_root=None` flat-layout-equivalence test). Every `check_and_restore(...)` call site added in batches 3/4/5 now passes `git_root=git_root` (each skill already binds `git_root` locally in its own Entry/Path Setup).
- `20260729-074407-plan-review-r2.md`: "[BLOCKING] Holistic loop's rate-limit-fallback redispatch has no tree-guard bracket" — Batch 5 Card 8 now adds an explicit pre/post tree-guard checkpoint pair around step 3.6's "re-run sub-step 3" redispatch, rather than relying on step 3's own checkpoints implicitly re-firing through the cross-reference. Also corrected Card 8's post-dispatch-checkpoint anchor, which incorrectly described the boundary after step 3 as "before step 4" when step 3.5 (not step 4) is what physically follows.
- `20260729-074407-plan-review-r2.md`: "[NIT] Card 2's runner-shape claim misdescribes `test-cleanliness.py`" — Batch 1 Card 2 now says the new file follows `test-status.py`'s single-abort-on-first-failure runner shape, and explicitly notes `test-cleanliness.py`'s own runner shape (per-scenario try/except, accumulate-and-report-all) is NOT the one being followed — that file is cited only for its git/mocking fixture idioms.
- `20260729-074407-plan-review-r2.md`: "[NIT] mill-start Card 5 mischaracterizes where step 2 'closes'" — Batch 3 Card 5's post-dispatch checkpoint instruction now says the anchor is "before step 3" unambiguously, without the incorrect "closes out step 2" framing.

## Pushed Back

(none — every round 2 finding was fixed as described above)
