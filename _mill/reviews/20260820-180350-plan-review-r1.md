MILL_REVIEW_BEGIN
# Review: millpy-implement --stage baseline: Windows verify-baseline worktree teardown fails (WinError 145 / long paths), leaves orphaned artifacts — holistic

```yaml
duration_s: 305.0
verdict: APPROVE
reviewer_model: sonnethigh
reviewer_self_id: claude-sonnet-4.5 (best-effort self-assessment)
reviewed_file: plan/
date: 2026-08-20
```

## Findings

### [NIT:decision] `_safe_rmtree._is_reparse_point` never gets `to_extended` despite Decision naming its calls
**Demoted-from:** BLOCKING
**Location:** 04-safe-rmtree-long-path-safety.md, Card 7
**Issue:** `_is_reparse_point` (`_safe_rmtree.py:48-57`) calls `os.path.isjunction(str(p))` / `os.lstat(str(p))` on a raw path — both functions are explicitly named as in-scope call sites by the overview's `proactive-extended-path-prefix` Shared Decision, yet Card 7's Requirements never mention `_is_reparse_point`. It is called at `_safe_rmtree.py:137` directly on `original` (the top-level, non-walk-derived path passed to `safe_rmtree`, the exact check that decides whether to refuse a junction/reparse-point per the module's wiki-wipe-incident safety contract) and again at `:69` inside the recursive walk.
**Contrast:** `_junction.py`'s structurally parallel function `_is_junction_or_symlink` (same `os.path.isjunction`/`os.lstat` calls) DOES get full `to_extended` treatment via Card 5 — the two walkers are treated asymmetrically with no stated rationale for the omission.
**Fix:** Add a `p = _long_path.to_extended(p)` binding inside `_is_reparse_point` (mirroring Card 5's `_is_junction_or_symlink` treatment) and route its `os.path.isjunction`/`os.lstat` calls through it, or explicitly document in the Decision/card why this call site is excluded.

### [NIT:scope] Batch 03's tests cover only 1 of Card 5's 3 modified call sites
**Location:** 03-junction-walker-long-path-safety.md, Card 6
**Issue:** Card 5 applies `to_extended` in three places in `_junction.py` (`_walk`'s scandir, `_is_junction_or_symlink`, `remove()`), but Card 6's two new test cases (i)/(j) only assert the marker is passed to `_walk`'s `os.scandir` call — `_is_junction_or_symlink` and `remove()`'s new `p` binding are never exercised.
**Contrast:** Batch 04's Card 8 tests both of Card 7's modified call sites (walker `os.scandir` and `safe_rmtree`'s `shutil.rmtree` root), so batch 03 is asymmetrically under-tested relative to batch 04's parallel structure.
**Fix:** Add cases asserting `_junction._long_path.to_extended` is invoked with `link_path` inside `_is_junction_or_symlink` and `remove()` (e.g. via a marker-injection pattern matching cases (i)/(j)).

## Verdict

APPROVE
`_safe_rmtree._is_reparse_point` is left on raw paths despite the Decision naming its OS calls as in-scope.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
