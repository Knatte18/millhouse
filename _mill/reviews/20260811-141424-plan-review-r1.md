MILL_REVIEW_BEGIN
# Review: mill-go2: fork-based fixer (NIT-fix) dispatch — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnethigh
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-11
```

## Findings

### [NIT:decision] Fork override scoped to all fixer dispatch, not just NIT-fix
**Demoted-from:** BLOCKING
**Location:** batch 2 / Card 4 (`### fixer` override block); overview task title
**Issue:** The task's own title is `mill-go2: fork-based fixer (NIT-fix) dispatch`, and "NIT" appears nowhere else in the three plan files, but Card 4's override text says "Governs the **first** fixer dispatch per scope and round only" with no distinction between the post-`APPROVE`, `nit_count > 0` NIT-only fix pass and the `REQUEST_CHANGES` fix pass — `mill-go-base`'s Override point A is consulted identically for both (batch scope lines ~791 and ~813, holistic scope lines ~1239 and ~1261), so the fork applies to `REQUEST_CHANGES` fixer dispatches too, which fix BLOCKING findings, not just NITs. No `## Shared Decisions` entry states this broadening is intentional.
**Fix:** Either add a Shared Decision explicitly stating the fork override is meant to cover all fixer dispatch (batch + holistic, NIT-fix + REQUEST_CHANGES-fix) and reconcile the title, or scope the `### fixer` block's applicability condition to the NIT-only pass only (e.g. gate on the dispatch args carrying `--nits-only`).

## Verdict

APPROVE
Card 4's fork-override scope silently exceeds the task's own NIT-fix title with no Shared Decision covering it.
MILL_REVIEW_END
