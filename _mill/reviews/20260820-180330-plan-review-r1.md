MILL_REVIEW_BEGIN
# Review: mill-plan Entry step 2: _config.load_config called with hub_root/worktree_root swapped vs its own signature — holistic

```yaml
duration_s: 117.0
verdict: APPROVE
reviewer_model: sonnethigh
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-20
```

## Findings

No findings. Verified against source:

- `plugins/mill/skills/mill-plan/SKILL.md:39` currently reads exactly `Call \`cfg = _config.load_config(worktree_root, git_root)\`.` — matches the card's quoted "before" text verbatim; the "after" text (`hub_root=worktree_root, worktree_root=git_root`) correctly reorders the same two values into keyword form against the real signature.
- `plugins/mill/scripts/_config.py:221` confirms `def load_config(hub_root: Path, worktree_root: Path) -> dict:`; `_config.py:178/205/213` and `_paths.py:234` (`resolve_main_worktree_root(git_root: Path)`) confirm the trace in discussion.md — the call's current positional values are already semantically correct, so this is legitimately a naming/legibility fix, not a value swap.
- `_paths.py:583` (`resolve_task_path(worktree_root, ...)`) and `mill-go-base/SKILL.md:73-86` (Path Setup: `worktree_root = _paths.resolve_active_hub(...)` feeding `resolve_task_path(worktree_root, ...)`) confirm the rejected-blanket-rename rationale is accurate, not fabricated.
- `_plan_validate.py:2708-2710` confirms `run(plan_dir, project_root, ...)` — second positional param is `project_root`, supporting the Decision that mill-plan's own self-run-validator call site (SKILL.md:259-261) has no textual-match defect and correctly needs no edit.
- Card 1 has all required fields (Creates/Edits/Deletes/Moves/Requirements/Commit); Moves is bare `none` (valid); no Moves anywhere in the plan so the "Rename mechanic" section is correctly absent.
- Batch Index DAG: single batch, `depends-on: []`, `file:` present in plan dir — no cycle, no dangling reference.
- All Files Touched lists exactly the one Edits target, matching the union.
- `verify: null` at both batch and overview level is justified in `## Batch Tests` (prose-only Markdown edit, no runnable code path) — consistent with the no-op-verify precedent and not contradicted by any `_plan_validate.py` rule requiring non-null verify.
- Requirements are maximally specific (verbatim before/after quote, exact step identification) with no vague "refactor X" prose; no BLOCKING requirements-specificity issue.
- No over-engineering: single-line, single-card, single-batch change with no unrequested scope.

## Verdict

APPROVE
Single-card docs-only fix is accurate, fully grounded in source, minimal, and complete.
MILL_REVIEW_END
