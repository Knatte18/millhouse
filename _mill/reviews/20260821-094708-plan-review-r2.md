MILL_REVIEW_BEGIN
# Review: mill-merge/merge-in: nested-layout config resolution, stale locks, and rollback-target bugs — holistic

```yaml
duration_s: 397.0
verdict: APPROVE
reviewer_model: sonnethigh
reviewer_self_id: claude-sonnet-4.5 (self-assessed; brief-mandated identity is "sonnethigh")
reviewed_file: plan/
date: 2026-08-21
```

## Findings

### [NIT:consistency] Card 1/Card 2 fenced find-text drops the source's 2-space base indent
**Demoted-from:** BLOCKING
**Location:** Batch 1 (`01-mill-merge-push-and-lock.md`), Card 1 (both find and replace fences) and Card 2 Edit B (find and replace fences).
**Issue:** Every line of `mill-merge/SKILL.md`'s "On push failure" numbered list (lines 344-405, e.g. `  1. Check the captured output...`) carries a 2-space base indent because it is nested under the `- **Direct path:**` bullet. Card 1's and Card 2 Edit B's fenced "Find this exact text" blocks reproduce this content flush-left (verified via `grep -n "^\s*1\. Check the captured output"` returning column 0 in the plan file vs. column 2 in the actual source). The card's own instruction ("byte-exact indentation... copy fence contents literally") is violated by its own content.
**Fix:** Re-indent both fences (find and replace) in Card 1 and Card 2 Edit B with the source's leading 2-space indent on every line, matching lines 344-405 of `mill-merge/SKILL.md` exactly, so the Edit tool's literal old_string search succeeds and the inserted sub-step 1a/Post-Step-5-success paragraph lands at the correct nesting depth.

### [NIT:consistency] "~11 other call sites" undercounts by 2
**Location:** Batch 4 (`04-config-caller-alignment.md`), Batch Scope paragraph.
**Issue:** A repo-wide scan finds 14 call sites of `_review_common.load_config` (`millpy-review-summary.py`, `millpy-review-discussion.py`, `millpy-review-code.py`, `_review_common.resolve_path`'s own internal call, `millpy-implement.py` x2, `millpy-abandon.py` x2, `millpy-validate-plan.py`, `millpy-review-plan.py`, `millpy-fix.py` x2, `millpy-merge-in-subagent.py` x2); Card 9 changes one, leaving 13 unchanged, not ~11.
**Fix:** Correct the count to "~13" (or exact) so a future reader auditing the unchanged-call-sites list isn't short by two files.

## Verdict

APPROVE
Batch 1's Card 1/Card 2 fenced text omits the source's 2-space base indent, breaking the mandated literal Edit-tool match.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
