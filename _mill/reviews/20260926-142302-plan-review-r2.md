MILL_REVIEW_BEGIN
# Review: mill-merge: run the deterministic path as one script — holistic

```yaml
duration_s: 414.4
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/
date: 2026-09-26
```

## Findings

### [BLOCKING:design] Card 9's "Caller propagation" retarget can gut load-bearing content
**Location:** 05-cross-references.md, Card 9 (mill-merge-in/SKILL.md edits)
**Issue:** Every other bullet in this card scopes its edit to a "reference" or "sentence" inside a paragraph; this one instead names the whole "Caller propagation (#977 follow-up)" **paragraph** as the replacement target, mapping it to one short sentence. The current paragraph (mill-merge-in/SKILL.md's Entry section) carries substantive mechanism content beyond the old step-number cross-reference — "record substituted_parent_branch = resolved_branch", "Step 6's Report below must surface this value", and the caller-rebind instruction — none of which appears in the proposed replacement text.
**Fix:** Scope this bullet to only the internal cross-reference clause ("When this skill is invoked as mill-merge's own Step 2 ... its own Step 5 onward" / "before continuing past its Step 2"), matching the card's own closing "Keep all other wording" instruction, and preserve the rest of the paragraph verbatim.

### [NIT:consistency] Card 9 misattributes the active-hub status_path derivation to step_parent
**Location:** 05-cross-references.md, Card 9 (mill-merge-in/SKILL.md rebind-safety paragraph)
**Issue:** The card retargets "mill-merge/SKILL.md Entry Step 4's warning" to "`_merge.step_parent`, which resolves `status_path` through the slug-driven active-hub lookup." Per 01-merge-core-entry.md Card 2, the `ops.resolve_active_hub(...)` / `_paths.resolve_task_path(...)` derivation happens in `step_entry`, not `step_parent` — `step_parent` only consumes the already-bound `ctx.status_path`.
**Fix:** Attribute the active-hub derivation to `_merge.step_entry`, and reference `_merge.step_parent` only for the rebind-warning half of the sentence.

### [NIT:scope] Card 1 requires `import _status` but omits `_status.py` from Context
**Location:** 01-merge-core-entry.md, Card 1
**Issue:** The Requirements' top-level-imports list for `_merge.py` includes `_status`, but Card 1's `Context:` list does not include `plugins/mill/scripts/_status.py` (it first appears in Card 2's Context).
**Fix:** Add `_status.py` to Card 1's `Context:` list, or note inline that the import is added in anticipation of Card 2's usage.

### [NIT:consistency] Card 1 cites test-inplace.py's naming convention inaccurately
**Location:** 01-merge-core-entry.md, Card 1
**Issue:** Requirements says to write `test-merge.py` "in the plain-function style of `test-inplace.py`: ... define `test_*` functions." `test-inplace.py`'s own functions are actually named `_test_*` (leading underscore); the `test_*` (no underscore) convention is `test-millpy-spawn.py`'s, cited separately in the same sentence for harness shape only.
**Fix:** Either name `_test_*` explicitly, or drop the naming-convention claim from the test-inplace.py citation.

### [NIT:scope] Card 9's grep gate is not wired into batch 5's `verify:`
**Location:** 05-cross-references.md, Card 9 / Batch Tests
**Issue:** Card 9's acceptance check (`grep -nE "mill-merge...Step [0-9]|Card 1 fix" ...` prints nothing) is described only as inline Requirements text; batch 5's `verify:` only byte-compiles Card 10's file (`py_compile plugins/mill/integration_tests/test-merge.py`), so a stray leftover old-style reference in the four Card 9 files is never automatically re-checked.
**Fix:** Either fold the grep into batch 5's `verify:` command (e.g. `&&`-chained), or note explicitly that this gate is implementer-run-only, not machine-enforced at verify time.

## Verdict

REQUEST_CHANGES
One ambiguous edit-scope instruction in Card 9 risks destructive content loss in mill-merge-in/SKILL.md.
MILL_REVIEW_END
