## Fixed

- `20260729-142554-plan-review-r2.md`: "`_worktree.move()` runs with cwd inside the dir being moved" — verified `_subprocess_util.run`'s docstring: `cwd=None` inherits the caller's OS-level cwd, and `create()`'s existing pattern relies on `-C` alone with no explicit `cwd=` kwarg. Since Phase 1b invokes `move()` from a session whose shell cwd is `old_worktree` itself, this would leave the subprocess's real OS working directory pointed inside the directory being renamed — a Windows lock-failure class `remove_safe`'s `WorktreeLockedError` already exists to catch on `remove()`. Card 7 now requires passing `cwd=cwd` explicitly to `_subprocess_util.run` (not just `-C`), plus lock-pattern detection mirroring `remove_safe`'s existing pattern set, raising `WorktreeLockedError` instead of a bare `WorktreeError` on a lock-shaped failure.
- `20260729-142554-plan-review-r2.md`: "New `verify_git_repo()` hard-fails health_check() for non-git test fixtures" — verified `_test_helpers.py`'s `init_wiki_repo()`: under the suite's own `WIKI_DAEMON_SKIP_GIT=1` default, it only `mkdir`s the wiki dir, no real `.git`. An unconditional `verify_git_repo()` call would hard-fail `OP_HEALTH` across the large majority of existing test fixtures, and would also flip `_ensure_daemon()`'s internal reuse-probe (`wiki/_client.py:643-649`) to always-false, triggering spurious respawns suite-wide. Card 2 now gates the entire new git-validity/staleness logic behind the same `WIKI_DAEMON_SKIP_GIT` check `_render_and_commit_all` already uses, short-circuiting to the unchanged `{FIELD_OK: True}` when set.
- `20260729-142554-plan-review-r2.md`: "Requirements cite files absent from Context (Card 14, and pattern in 12/13/15/16)" — verified Card 14's Requirements cite `_review_common.py:306` and `_plan_validate.py:1455` by line number without either file in Context. Added both to Card 14's `Context:`. Cards 12/13/15/16 were left as-is per the reviewer's own note that they're mitigated by spelling out the full function signature inline rather than citing unread line numbers.
- `20260729-142554-plan-review-r2.md`: "Overview's batch-1 verify command includes a file `run-all.py` always skips" — verified: round 1's fix updated `01-wiki-health-check-and-messaging.md`'s own frontmatter `verify:` but missed the mirrored entry in `00-overview.md`'s Batch Index. Removed `test-wiki-sync.py` from the overview's batch-1 `verify:` string to match.
- `20260729-142554-plan-review-r2.md`: "[NIT] `_paths.py` docstring/`__all__` not updated for the new function" — verified `_paths.py` has both a module-docstring Public API list and an `__all__` list, and `resolve_container_path` appears in both. Added the matching requirement to Card 6.
- `20260729-142554-plan-review-r2.md`: "[NIT] 'existing clone fixture helper' doesn't exist as a separate function" — verified `test-wiki-sync.py` inlines its bare-repo+clone setup in `main()`, no separate helper function. Reworded both Card 1 and Card 4's Requirements to "adapt the inline bare-repo+clone setup pattern" instead of implying a callable helper.
- `20260729-142554-plan-review-r2.md`: "[NIT] Debounce spy patch target is ambiguous and only one option actually works" — verified `wiki/_server.py` does `from wiki._sync import pull`, binding a local name; only patching `wiki._server.pull` intercepts the call inside `_handle_health`. Card 4 now states `wiki._server.pull` as the sole correct patch target, with the binding rationale inline.

## Pushed Back

(none — all seven findings verified accurate against source; all fixed.)
