# Plan-fix round 2

## Fixed

- Source: `20260813-082227-plan-review-r2.md`, `[NIT:consistency] Card 2 insertion-point instructions self-contradict`. Verified accurate: `test-paths.py`'s `main()` continues to line ~1193, far past the Regression-2 block ending at line 386, so "the current end of main()" and "immediately after Regression 2" named two different locations. Removed the ambiguous "end of main()" phrasing; Card 2 now names the exact anchor line (`print("PASS: resolve_task_path flat layout finds _mill/status.md (regression)")`, `test-paths.py:373-386`) directly.
- Source: `20260813-082227-plan-review-r2.md`, `[NIT:consistency] Card 7 docstring instruction references wording not present in source`. Verified accurate: `_parent_diff_names`'s current docstring (`_cleanliness.py:113-118`) never contains the word "empty" — only the stderr log line does. Card 7 point 1 now describes adding a new `Returns:` note (an addition) rather than implying a reword of nonexistent docstring text.

## Pushed Back

(none — both findings were accurate and fixed)
