MILL_REVIEW_BEGIN
# Review: mill-go-base: remove subprocess/psmux dispatch branches — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-12
```

## Verification summary

- Banned literals (`psmux`, `millpy-bg`, `` `dispatch == subprocess` ``) absent from `SKILL.md`, `resume.md`, `holistic-review.md`, `handoff.md` — grep confirms zero occurrences across the directory.
- Tree-guard: exactly 2 occurrences of `_treeguard.check_and_restore(` remain in `SKILL.md` (executable call + signature line), and 13 `Tree-guard checkpoint block` mentions total (1 definition + 12 references: 5 in `### 3. Code Review loop`, 7 in `## Holistic code review`) — matches card 12/13 exactly.
- Agent-mode dispatch steps read 1–6 with sub-step 5.5 and sub-labels 3(a)/3(b)/3(c); no stray `step 7`, `step 6.5`, or `step 4(` anywhere in the tree. Cross-file namespace sweep (`mill-go2/SKILL.md`'s `base step 3`, `step 3(a)`, `5.5.1`/`5.5.2`) is internally consistent with card 20's mapping.
- Companion files (`resume.md`, `holistic-review.md`, `handoff.md`) exist, carry no frontmatter, no wiki-access banner, and `test-mill-go-base-agent-only.py`'s four checks all pass against the current source (repo-relative reference + `` Read `plugins/mill/skills/mill-go-base/<name>` `` directive present at all three `SKILL.md` reference sites).
- Cross-references repaired bidirectionally: companions name `SKILL.md` by path for every "above"/"below" position reference found in source; `SKILL.md`'s Entry phase-table, `### Blocked`, `### 0.55`, and Stuck-escalation sections name the companion files by path.
- `test-skill-helper-drift.py` widens the scan to `mill-go-base/*.md` and re-points the `#496` `reviews_dir` lock to search across the companion set — matches card 19.
- `SKILLS.md` carries no row for the three companion files, unchanged row count, and both `mill-go`/`mill-go2` rows match their current frontmatter — matches card 24's three invariants.
- `mill-go2/SKILL.md`'s "Known limits" no longer references a dispatch-mode distinction; both variant files' base-coverage sentences describe Resume/Holistic/Handoff as reached via mandatory-read pointers without naming companion paths — matches card 23.
- The discussion's own `line-count-is-an-outcome-not-a-gate` Decision explicitly disclaims the ~630-line estimate as non-gating; `SKILL.md`'s actual 829 lines is not treated as a defect given that explicit disclaimer.

No BLOCKING or NIT findings. Implementation realizes all five batches' cards faithfully, shared decisions are applied consistently, and no out-of-plan files or duplicated helpers were found.

## Verdict

APPROVE
All batches align with the plan; regression guard, tree-guard dedup, extraction, and renumbering sweep are internally consistent.
MILL_REVIEW_END
