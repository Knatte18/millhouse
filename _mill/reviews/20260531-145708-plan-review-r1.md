# Review: Replace claude -p with psmux-routed LLM dispatch — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-31
```

## Findings

### [BLOCKING] Batch 2 verify breaks before Batch 4 fixes land
**Location:** Batch 2 / Cards 4 and 5; Batch 4 / Card 9
**Issue:** Batch 2's verify (`test-claude-sub.py`) will fail after Cards 4 and 5 are applied — Card 4 breaks Scenarios a/b/c (mocks return `"❯ idle\n"` but code now checks `"for shortcuts"`), and Card 5 breaks S1 (`m_send_keys.call_count == 3` becomes 4 with the added Escape call). The plan acknowledges the Card 4 impact but mislabels it as "expected for per-card runs"; mill-go runs batch-level verify. The Card 5/S1 failure is not acknowledged in Batch 2 at all. Batch 4 Card 9 contains all the fixes, but it cannot run until Batch 2's verify is green — causing a stuck state.
**Fix:** Move the S1 assertion update (`call_count == 3` → `4`, verify first call is `Escape`) and Scenario (a)/(b)/(c) rewrites from Batch 4 Card 9 into Batch 2 — expand Cards 4 and 5 requirements to also update their respective direct tests, or add a new Card 5.5 that does so. Batch 4 Card 9 then covers only S12/S13/S14 (the new `_resolve_shell_path` and `rows=100` tests).

## Verdict

REQUEST_CHANGES
Batch 2's verify will be stuck: Cards 4 and 5 break existing tests whose fixes are deferred to Batch 4.