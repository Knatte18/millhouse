## Fixed

- `20260728-190336-plan-review-r2.md` — "[BLOCKING] Dirty-parent halt collides with existing generic Rollback section": verified against `mill-merge/SKILL.md:320-336` — the generic `## Rollback (Steps 1-5 only)` rule literally covers "any failure ... Step 5" and would apply `git -C <parent-path> reset --hard mill-checkpoint-<name>` to the new halt, destroying the uncommitted work scenario (a) tells the operator to preserve. Added an explicit carve-out to Card 15, mirroring the doc's existing Step-4-specific rollback exception.
- `20260728-190336-plan-review-r2.md` — "[NIT] Card 4's 'mirror exactly' framing contradicts its own fixture needs": verified against `test_worktree_template_augments_template_cfg` — it calls `_config.load_config(wt_root, wt_root)` with one shared directory. Added an explicit up-front statement to Card 4 that `hub_root` and `worktree_root` must be distinct directories in the new test.
- `20260728-190336-plan-review-r2.md` — "[NIT] Card 5 misstates `_review_common.load_config`'s raise condition": verified against `_review_common.py:2003` — the raise fires on the conjunction `not template_path.exists() and mill_cfg_path is None`, not on `mill_cfg_path is None` alone. Corrected Card 5's citation.
- `20260728-190336-plan-review-r2.md` — "[NIT] Card 10's stuck-skips-gate test doesn't name which call site it covers": split the single test into two, one per call site (`--stage full` and `--stage finalize`), each asserting the gate function is not called.

## Pushed Back

(none — all findings verified accurate against source and fixed)
