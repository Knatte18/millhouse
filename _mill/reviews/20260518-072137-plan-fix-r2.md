# Plan Fix: round 2

## Fixed

- `20260518-072035-plan-review-r2.md` — "NIT — Pre-existing unreachable success path in `_review_code.py` is untracked": added a note to Card 6 Requirements documenting the pre-existing dead code at lines ~361+ (`NEED_CONTEXT` block inside `except ReviewError:` after an unconditional return); noted that the rounds=0 guard fires before that region and the structural fix is out of scope.
- `20260518-072035-plan-review-r2.md` — "NIT — Card 8's implementer deferral should state the guard's return shape explicitly": tightened Card 8 Requirements to specify that both guards must fire at the outermost level of each sub-section and return a synthesized APPROVE ReviewResult directly from `run()`, mirroring the Cards 6-7 stub shape. Removed the vague "implementer must confirm" deferral.

## Pushed Back

(none)
