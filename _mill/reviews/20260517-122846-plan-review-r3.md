# Review: 60 (A) — Branch/slug/claim fixes — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-17
```

## Findings

### [NIT] Test registration wording ambiguous for monolithic main() files
**Location:** Batch 1 Card 3, Batch 2 Card 6
**Issue:** Plan says "Register the new test in `main()`" but `test-status.py` and `test-review-cli.py` use monolithic inline `main()` functions, not the list-based runner pattern of `test-marker.py`. An implementer unfamiliar with both files could misread this as requiring a list.
**Fix:** Wording should say "call the new test function from `main()`" rather than "register" to match the monolithic pattern.

### [NIT] Unused `print_error` import after D2 wiring
**Location:** Batch 2 Card 5
**Issue:** After replacing all handler bodies with `print_error_envelope`, the `from _review_cli import print_error` import in all three review CLIs becomes unused. The plan does not mention removing it.
**Fix:** Implementer should drop the `print_error` import from the `from _review_cli import` line in each CLI (or the plan should note this explicitly).

### [NIT] mill-plan SKILL step 1.5 implicit millpy-bg site
**Location:** Batch 5 Card 10
**Issue:** Step 1.5 says "re-runs the review CLI via millpy-bg (slug `plan-validator-fix`)" but there is no explicit fenced bash block showing the invocation — only prose. The plan's directive "there must be no `millpy-bg` invocation site in mill-plan SKILL that lacks the prelude" correctly captures the requirement, but a grep for `PYTHONPATH=.*millpy-bg.py` may miss this prose-only site if the bash block doesn't exist yet and needs to be added in the same edit.
**Fix:** Card 10 implementer should verify whether step 1.5 already has an explicit bash block or only prose, and add the prelude in whichever form is present.

## Verdict

APPROVE
All three batches with verify commands are self-contained; no blocking defects found.