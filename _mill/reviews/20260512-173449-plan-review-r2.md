# Review: (A) — Small infra fixes batch 7 — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-12
```

## Findings

### [NIT] WikiHealthError not listed in Exceptions docstring section
**Location:** Batch 1 / Card 1
**Issue:** Card 1 specifies updating `_wiki.py`'s module docstring Public API section for `health_check`, but does not mention adding `WikiHealthError` to the `Exceptions:` section alongside `LockBusy`, `WikiPushError`, and `WikiSetupError`.
**Fix:** Add a line to Requirements: "Append `WikiHealthError` to the `Exceptions:` block in the module docstring immediately after `WikiSetupError`."

### [NIT] Confusing test numbering in Card 8
**Location:** Batch 3 / Card 8
**Issue:** Plan refers to "new tests 4/5/6" to be inserted after "existing Test 3", but `test-status.py` already has a `# Test 4: append_phase quoting regression.` comment later in the same `try:` block. The implementer may misread this and introduce a duplicate label.
**Fix:** Change "new tests 4/5/6" wording to "three new test blocks" with the note that they should use descriptive `# Test:` comment labels, not sequential numbers, to avoid collision.

## Verdict

APPROVE — plan is sound; both findings are cosmetic documentation clarifications that do not affect implementation correctness.