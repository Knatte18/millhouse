```yaml
round: 1
source_review: _mill/reviews/20260730-193718-plan-review-r1.md
date: 2026-07-30
```

## Fixed

- `20260730-193718-plan-review-r1.md` — "[BLOCKING] `build_wait_command`'s grep anchors break on CRLF status.md": verified `_status.py`'s writers (`update_field`/`append_phase`/`set_blocked`) call `Path.write_text` with no `newline=` override, confirming a Windows-authored `status.md` would carry CRLF line endings that break GNU/POSIX `grep`'s trailing `$` anchor. Fixed by routing every `grep` read of `status_path` in Batch 1 Card 1's poll-script template through a `tr -d '\r' <` pipe first (localized to `_phase_wait.py`, no change to `_status.py`'s writers, matching this task's existing scope-discipline pattern). Added a real end-to-end CRLF regression test (case 13) to Card 2 that writes a raw-bytes CRLF file and runs the generated script via `subprocess.run`, asserting exit code 0 and stdout `"READY"`.
- `20260730-193718-plan-review-r1.md` — "[BLOCKING] Monitor tool's `persistent`/`task_id`/notification contract is unverified": rather than deferring to a future spike as the finding's suggested fix proposed, ran a live `Monitor` spike directly during this review round. Confirmed empirically: the tool exposes a task identifier usable with `TaskOutput`/`TaskStop`, and a script that echoes N lines before exiting produces N per-line `<event>`-tagged notifications followed by one separate terminal `<status>completed</status>` notification with no `<event>` tag — a materially different shape from the Agent tool's single combined result notification. Recorded this as a new overview Shared Decision ("Monitor's task-identifier and notification-shape contract, empirically verified") and reworded Batch 2 Card 4 / Batch 3 Card 5's wait-handling subsections to branch on the first notification's `<event>` content instead of a generic "single stdout line," with an explicit note that the second, event-less completion notification needs no separate handling.
- `20260730-193718-plan-review-r1.md` — "[NIT] Replacement phase-table rows use 5-space indent, contradicting the 'preserve 3-space' instruction": verified both Batch 2 Card 4 and Batch 3 Card 5's illustrative replacement-row fences used 5 leading spaces against their own "preserve 3-space indentation" instruction text. Re-indented all four replacement rows (Batch 2's one row; Batch 3's two rows) to 3 leading spaces.
- `20260730-193718-plan-review-r1.md` — "[NIT] Card 3's wiki-config-mutation bootstrap claim may not match the Phase: Plan self-validate call": verified against `mill-plan/SKILL.md`'s own text — its Phase: Plan self-validate step hardcodes `skip_checks=frozenset()` with no documented override for that specific call site; only the CLI-driven Step 1.5 gate natively supports `--skip-check`. Reworded Batch 1 Card 3's bootstrap-justification paragraph to state plainly that applying the skip at the direct self-validate call site is an interpretive extension of this fix-table row's intent (not a pre-existing documented mechanism), while the CLI-driven gate's `--skip-check wiki-config-mutation` support is native and needs no such caveat.

## Pushed Back

(none)
