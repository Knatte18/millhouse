# Plan fix report -- round 1

```yaml
slug: claude-p-wrapper
review_round: 1
review_file: 20260515-112707-plan-review-r1.md
verdict: REQUEST_CHANGES (3 BLOCKING + 2 NIT)
```

## Fixed

- "`duration` NameError in response-poll timeout path" (BLOCKING). Card 9
  step 11 in `03-wrapper.md` now binds `elapsed = time.monotonic() -
  start` at the top of each iteration BEFORE the try, so the
  `RuntimeError` message at timeout has a defined value regardless of
  whether `extract_response` raised. The success branch's JSON envelope
  uses `round(elapsed, 2)` instead of recomputing `duration`.
- "`set_history_limit` call absent from wrapper flow" (BLOCKING). Card
  9 step 6 in `03-wrapper.md` now invokes
  `_psmux.set_history_limit(session_name, 50000)` immediately after
  `new_session`. The cross-reference to `_psmux.py`'s built-in fallback
  policy (swallow `PsmuxError`, log default-fallback line) is included
  in the requirement so the implementer does not duplicate fallback
  logic in the wrapper.
- "Batch 3 header `depends-on` inconsistent with overview DAG" (NIT).
  `03-wrapper.md` header now reads `depends-on: [1, 2]`, matching the
  authoritative overview DAG. The same template-default-`[]` issue was
  also present in `04-integration-and-report.md` -- fixed in the same
  commit (header now `depends-on: [3]`, matching the overview).
- "Card 5 stub omits `import _subprocess_util`" (NIT). Card 5 in
  `02-driver.md` now requires the stub to import `_subprocess_util`
  (and `sys`) at module top so card 6's
  `mock.patch("_psmux._subprocess_util.run", ...)` resolves correctly
  against an existing attribute.

## Pushed Back

- "Batch 4 DAG missing `depends-on: [3]`" (BLOCKING). Factually wrong:
  the line in `_mill/plan/00-overview.md` for batch 4 already reads
  `depends-on: [3]`. Verified by `grep -A4 "number: 4"
  _mill/plan/00-overview.md` -- output shows `    depends-on: [3]`
  unchanged since the initial plan write. The reviewer appears to have
  read the per-batch file's stale header (which DID have the
  template-default `[]`); that header inconsistency is the NIT 1 issue
  and is fixed separately. The authoritative DAG was correct from the
  start; no change required to the overview's batch index.
