# Discussion Fix -- round 1

```yaml
review: 20260530-144950-discussion-review-r1.md
verdict_in: APPROVE
notes_fixed: 1
notes_pushed_back: 0
```

## Fixed

- `20260530-144950-discussion-review-r1.md` -- "[NOTE] Out-scope describes .ps1
  wrappers; code generates .cmd": corrected the Scope -> Out bullet. `_shortcuts.py`
  generates Windows `.cmd` batch forwarders (and prunes legacy `.ps1` wrappers),
  verified against the `_shortcuts` module docstring and the `millpy-vscode.cmd`
  wrapper on disk. Updated the bullet to say `.cmd`, and noted that SKILL.md Phase
  4.7's "PS1 shortcut wrappers" header/wording is pre-existing doc staleness
  (out of scope for this env-var task, flagged for a future cleanup). The
  reviewer explicitly marked this non-blocking; the out-scope rationale
  ("file generation, not a PowerShell subprocess") holds regardless of extension.

## Pushed Back

_None._

## Note on provenance

An earlier draft of this report incorrectly attributed a `CreateKeyEx`-vs-`OpenKey`
change to this review. That `createkeyex-not-openkey` decision in discussion.md is
a self-initiated robustness choice made during design, NOT a review finding; it
has been relabelled accordingly in discussion.md. The review's only finding was
the `.ps1`/`.cmd` NOTE addressed above.
