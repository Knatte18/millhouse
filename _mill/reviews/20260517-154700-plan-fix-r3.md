# Plan-fix report -- round 3

Source review: [`20260517-154625-plan-review-r3.md`](20260517-154625-plan-review-r3.md) (verdict: APPROVE with 3 NITs)

## Fixed

- `[NIT] Card 15 intro count contradicts body` -- VERIFY accurate: the intro said "with four cases" while the body enumerated six (1-5 plus `test_log_live_pid_with_stale_mtime`). Fixed Batch 5, Card 15 opening sentence to read "six cases".
- `[NIT] Card 3 docstring gap: post-snapshot capture failure eats inner_exc` -- VERIFY accurate: if `_capture_head_sha` or `_capture_porcelain` raises `ReviewError` after `inner_exc` was set, the post-snapshot exception propagates and `inner_exc` is lost. Fixed Batch 2, Card 3 by extending the docstring directive to two sentences -- the second explicitly documents the post-snapshot-failure path, calls it an intentional trade-off (capture failure means the snapshot is untrustworthy and `ReviewerOverstepError` cannot safely be raised), and notes the inner exception remains visible in traceback frames.
- `[NIT] Card 11 omits importlib guidance for hyphenated CLI filenames` -- VERIFY accurate: `millpy-review-discussion.py` etc. cannot be imported via bare `import` statements because hyphens are invalid in module identifiers. Fixed Batch 3, Card 11 by adding a "Loading the CLI modules" subsection that names `importlib.util.spec_from_file_location` + `spec.loader.exec_module` as the canonical loader pattern, calls out caching the loaded module so patches and `main()` see the same instance, and gives the `scripts_dir = Path(__file__).parent.parent / "scripts"` resolution from the test file.

## Pushed Back

(none -- all three NITs VERIFY-accurate, all fixes are pure documentation clarification)
