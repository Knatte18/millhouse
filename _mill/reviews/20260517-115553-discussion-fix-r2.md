# discussion-fix round 2 — mill-branch-slug-fixes

Source review: `_mill/reviews/20260517-115500-discussion-review-r2.md` (verdict: APPROVE, 2 [NOTE] findings)

## Fixed

- [NOTE] D9: test-claim coverage is confirmed-absent, not "probably" — reworded D9's D5 test bullet to use the correct existing filename `test-millpy-claim.py` and treat "add a new test" as the default action instead of "update probably-existing assertions".
- [NOTE] D7 validation — unhandled exceptions from `resolve_wiki_path`/`load_config` — wrapped the D7 sketch's validation block in an outer `try` that catches `ValueError` / `SystemExit` in addition to `MarkerError`, with a clean one-line stderr message before `return 1`.

## Pushed Back

(none)
