# Plan-fix round 1

Review: `_mill/reviews/20260801-161026-plan-review-r1.md`

## Fixed

- [BLOCKING] "Card 13 leaves a dangling 'Phase 4 (Cleanup)' reference in mill-autofix" — confirmed a third occurrence at `mill-autofix/SKILL.md`'s `### Step 0: Branch guard + killswitch check` ("Proceed to Phase 4 (Cleanup) then Phase 5 (Report)."). Added a new old/new quote pair to `06-cleanup-dead-autonomous-mode.md` Card 13 replacing it with "Proceed to Phase 5 (Report)."
- [BLOCKING] "Card 7's holistic-scope glob description doesn't actually mirror `_find_final_code_review`" — confirmed `_review_common.RE_SIMPLE`/`RE_BATCH` are anchored regexes, and the card's unanchored glob `*-code-review-r*.md` could false-match a per-batch scope name starting with `r` (e.g. `retry-fix`). Rewrote `04-mill-go-handoff-gates.md` Card 7's matching description to name the exact anchored-pattern semantics instead of a glob string.

## Pushed Back

(none — both findings were accurate and fixed)
