```yaml
round: 3
source_review: _mill/reviews/20260725-141706-plan-review-r3.md
```

## Fixed

- `[BLOCKING] Batch 4's rebind breaks all 3 test files' existing tests` — `project-root-rebinding-implement-side`'s test card (now Card 12) corrected: requires adding shared `setUp` mocks for `_paths.resolve_container_path` and `_paths.resolve_active_hub` (returning the fixture's existing `self.tmp_path`, preserving every pre-existing assertion) before the new per-file regression test, which overrides `resolve_active_hub`'s return value specifically to prove the rebind takes effect.
- `[BLOCKING] resolve_path() (reviews_dir) still hits the daemon on every review finalize` — added new Card 8 to `paths-skip-slug-validation` (batch 3): threads `skip_slug_validation=True` into `_review_common.resolve_path`'s own internal `resolve_active_hub` call, plus a new on-disk-fast-path test in `test-review-common.py`. This closes a previously-undiscovered daemon-reintroduction gap distinct from the `briefs_dir`/`project_root` rebind the other batches fix.
- `[BLOCKING] Card 15 assumes CLI-level test scaffolding that only exists in 1 of 3 files` — `project-root-rebinding-review-side`'s test card (now Card 16) corrected: explicitly distinguishes `test-review-discussion-flow.py` (extend its existing `test_brief_path_nested_layout` importlib/`sys.modules`-injection harness) from `test-review-code-flow.py`/`test-review-plan-flow.py` (build the same harness from scratch, with the exact module-mock list each file's `main()` needs), rather than claiming all 3 files "match existing conventions."
- `[NIT] Card 13 undercounts which stages benefit from the rebind` — Card 14 (`millpy-review-plan.py`) reworded to state the rebind applies to all three stage branches (`prepare`/`finalize`/`full`), not just `prepare`.
- `[NIT] Shared Decision's "two early consumers" list is incomplete` — overview's Shared Decision updated to list the `git config --global` subprocess calls as a third (harmless but real) early consumer of the pre-rebind value.

## Incidental fixes required by the above

- Adding Card 8 to batch 3 required renumbering cards 8-16 to 9-17 across batches 4/5/6 to keep card numbering sequential within each batch (validator `card-numbering` check), and adding `paths-skip-slug-validation` → `on-disk-first-resolution` as a new `depends-on: [2]` edge, since Card 8 and `on-disk-first-resolution`'s Cards 3-5 now edit the same two files (`_review_common.py`, `test-review-common.py`) with no prior ordering between the batches (validator `parallel-modifies-overlap` check).

## Pushed Back

(none — all 3 BLOCKING findings and both NITs were confirmed accurate against source and fixed)
