## Fixed

- `20260515-075742-plan-review-r1.md` [BLOCKING] "Card 5 -- MILL_PYTHON override points to non-existent worktree venv" -- added `.venv` existence guard to Step 0 PLUGIN_ROOT override; both PLUGIN_ROOT and MILL_PYTHON are updated only when the worktree venv exists; added skip message when venv is absent.
- `20260515-075742-plan-review-r1.md` [NIT] "Card 3 -- Two equally-valid implementations without mandating one" -- mandated tilde fence (`~~~markdown`) as the sole approach; removed co-equal blockquote alternative.
- `20260515-075742-plan-review-r1.md` [NIT] "Card 9 -- blank when the file should exist condition is semantically wrong" -- removed incorrect blank-output clause; safeguard now keys only on `D` in `git status --short` output; added explanatory note that blank means file is present and unchanged.

## Pushed Back

(none)
