# Plan fix report — round 1

Source review: `_mill/reviews/20260706-174412-plan-review-r1.md` (verdict: REQUEST_CHANGES, 1 BLOCKING, 3 NIT).

## Fixed

- **[BLOCKING] Card 5 omits the 6 CV-* direct compute_scope_violations calls** — added a requirement to Batch 1 / Card 5 to update all 6 CV-1..CV-6 direct-call test cases (lines ~179-245) to pass `(Path(tmp), Path(tmp))`, alongside the already-planned CESV case updates.
- **[NIT] Card 23 requires unstated signature/wiring changes** — Batch 6 / Card 23 now states `_check_verify_full_suite` gains a `project_root` parameter, both functions gain an `overview_path` parameter, and `run()`'s two call sites are updated to pass the new arguments.
- **[NIT] Card 13/16 threading is under-specified vs. actual call counts** — Batch 4 / Card 13 now names all four `_forward_output` gate call sites (lines ~952, ~1139, ~1233, ~1323) explicitly and clarifies `finalize_from_output` only needs pass-through kwargs since it delegates its whole body to `_forward_output`. Card 16 now specifies `_run_baseline_stage` gains a `module_wide_cwd_override` parameter and its line-~318 call site is updated to pass it.
- **[NIT] Cards 23 and 24 both emit malformed-cwd findings** — Batch 6 / Card 23's checks now `continue` silently on `ValueError` instead of emitting a finding; Card 24's dedicated `_check_verify_malformed_cwd` is now explicitly the sole reporter for malformed mappings.

## Pushed Back

(none)
