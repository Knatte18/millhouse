# Plan-fix report: round 2

```yaml
round: 2
review_file: _mill/reviews/20260527-091333-plan-review-r2.md
date: 2026-05-27
```

## Fixed

- `[NIT] Push error handling in _archive_tag tests is implicit` — added explicit policy to Card 2 Requirements: "Do not check the push return code -- push failure is treated as a non-fatal warning (the helper returns the result dict regardless of push outcome) so tests can exercise the tag-resolution branches against tmp repos with no remote." The TDD contract from Card 1 (tmp repos, no remote, no push assertion) is now matched by an explicit non-fatal-push contract in Card 2.
- `[NIT] Card 13 overstates commit sites in millpy-implement.py` — confirmed by direct grep that `millpy-implement.py` has exactly one `git commit` call (`mill-go: start batch {batch_name}`, around line 141). Updated Card 13 Requirements to drop "batch-resume, and potentially others" and state the single commit point with its line anchor.

## Pushed Back

_(none — both NITs fixed.)_
