# Discussion-fix round 2

## Fixed

- `_mill/reviews/20260921-151315-discussion-review-r2.md` — "[NIT:consistency] Scope > Out cites a rejected `verify_module:` field": corrected the Scope > Out bullet to say "#1056 reuses the existing per-plan overview `verify:` field, not a new one" instead of describing the discarded `verify_module:` field.
- `_mill/reviews/20260921-151315-discussion-review-r2.md` — "[NIT:design] mill-descope-batch fit for #1086b left unverified despite being cheap to check now": read `plugins/mill/scripts/millpy-descope-batch.py` in full; confirmed it gates only on the target batch's own `status.md` state (`pending`) and on no surviving dependents, with no task-level `phase:` gate. Updated the Technical context bullet to state this as a confirmed fit rather than deferring verification to implementation time.

## Pushed Back

(none)
