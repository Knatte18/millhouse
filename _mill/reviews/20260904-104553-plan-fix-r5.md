# Plan-fix report: round 5

## Fixed

- `20260904-104448-plan-review-r5.md` — "[NIT:consistency] `<branch>` positional-arg doc goes stale after Card 1" (demoted from BLOCKING): Card 2's Requirements now instruct revising Entry step 3's `<branch>` description to cover both the pre-existing ad-hoc-override use and the new caller-handoff use Card 1 introduces.
- `20260904-104448-plan-review-r5.md` — "[NIT:consistency] Card 2's \"mirrors mill-merge Step 4\" claim overstates the analogy": reworded to state this is a deliberately broader exemption than Step 4's (Step 4 skips its liveness check entirely; Card 2 only exempts the rebind write), not a mirror of it.
- `20260904-104448-plan-review-r5.md` — "[NIT:consistency] Reorder in Card 2 doesn't address Step 4's step-2 cross-reference": Card 2's Requirements now instruct confirming/updating Step 4's "Entry step 2" citation after the reorder.
- `20260904-104448-plan-review-r5.md` — "[NIT:design] Card 5's citation-scan warning text is imprecise for the restore-from-base branch": Card 5's Requirements now split the warning wording between the rm-path branch ("about to go dead") and the restore-path branch ("about to silently start pointing at a different task's content").
- `20260904-104448-plan-review-r5.md` — "[NIT:consistency] `_explicit_scope`'s `toplevel` param becomes dead after the fix": Card 7's Requirements now explicitly replace (not add alongside) the `toplevel` parameter with `cwd_path`.

## Pushed Back

(none)
