```yaml
round: 3
source: 20260923-115647-plan-review-r3.md
```

## Fixed

- [BLOCKING:scope] "Card 5's 'add a second batch' recipe doesn't fit already-two-batch fixtures" —
  added two named exceptions to card 5's generic recipe for
  `test_check_context_completeness_symbol_cache_invoked_once_per_key` and
  `test_check_context_completeness_symbol_qualifier_cache_correctness_different_qualifiers`, each
  specifying a THIRD, citing-only batch (not "a second") naming the exact declaring file(s) each
  fixture already writes to disk, leaving both tested batches' own refs and assertions unchanged.
- [BLOCKING:scope] "Card 3's #1116 regression test is invisible to card 5's migration grep and will
  break" — restructured card 3's `test_check_context_completeness_dirty_literal_enumeration_issue_1116`
  to use a two-batch fixture from the start (tested card + a second, citing-only batch naming the
  `.cs` fixture file), so it resolves under both the pre-card-4 repo-wide walk and the post-card-4
  plan-wide-cited-files walk without ever needing migration; also noted explicitly in card 5 that
  this test is out of scope for its grep-driven audit.
- [BLOCKING:design] "Card 6's line-join elides quoted material, creating a new cross-gap
  backtick-pairing risk" — redesigned card 6 from a single whole-body joined text to per-RUN
  processing (a run = a maximal contiguous span of non-quoted physical lines; a quoted/fenced line
  always starts a new run), so a dangling backtick can never bridge across an elided quoted region —
  each region that would have been elided now structurally ends the run instead. Updated the overview's
  `line-join-refactor` Shared Decision to match. Added a new regression test,
  `test_check_context_completeness_clean_line_join_dangling_backtick_before_fence_not_bridged`, proving
  a dangling backtick immediately before a fence does not swallow a genuine dependency several lines
  after the fence closes.

## Pushed Back

(none)
