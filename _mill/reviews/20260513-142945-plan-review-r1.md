# Review: (B) — Size-based reviewer switch (mechanism + configurable target) — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-13
```

## Findings

### [BLOCKING] Test spec omits `reviewer_name` value; assertion implies wrong default
**Location:** Batch 3 / Card 7 — `test_above_threshold_switches`
**Issue:** The test asserts `stderr contains "sonnetmax"`, but the test setup uses `_make_cfg_with_large_prompt()` which builds on `make_minimal_cfg()` — where `code-review.holistic.reviewer = "test_stub"`. The stderr log prints `{reviewer_name!r}`, so passing the cfg-derived name produces `"switching reviewer 'test_stub' -> ..."`, never touching `"sonnetmax"`. The test spec never states what `reviewer_name` or `spec` to pass directly.
**Fix:** Add one explicit line in the test description: `reviewer_name = "sonnetmax"` (resolved from `make_minimal_registry()`) so the assertion is self-consistent. The fixture and registry already have `"sonnetmax"` — the spec is simply incomplete.

### [NIT] Card 6 cites wrong indentation depth
**Location:** Batch 2 / Card 6 — wiki-config.yaml template comments
**Issue:** The card says "4-space indentation" but `reviewer:` inside the `holistic:` blocks sits at 6 spaces (roles → 2, holistic → 4, reviewer → 6). The immediately-following clause "matching the reviewer: line it follows" is correct, which contradicts the "4-space" description.
**Fix:** Remove "4-space indentation" and keep only "matching the `reviewer:` line it follows" so the instruction is unambiguous.

## Verdict

REQUEST_CHANGES
One test spec is incomplete enough to produce a failing assertion as written.