```yaml
round: 1
source_review: _mill/reviews/20260725-140020-plan-review-r1.md
```

## Fixed

- `[BLOCKING] fail-fast guard breaks the bulk of test-millpy-implement.py` — Card 2 (`01-fail-fast-guard.md`) now requires changing the shared `setUp`'s default cfg to `llm.claude.dispatch: "subprocess"` before adding the new guard-specific tests, so every pre-existing bare/full-stage test keeps exercising the non-agent path unaffected.
- `[BLOCKING] find_active_slug reorder can silently return a stale slug` — Card 3 (`02-on-disk-first-resolution.md`) redesigned: the fast path now only short-circuits when a cheap branch-derived slug (prefix-strip, no daemon) agrees with the single on-disk `.active` marker; any disagreement falls through to the unchanged, fully-validated `slug_from_branch` path. Card 5's tests updated to cover both the agreeing case and a stale-marker/branch-mismatch regression case.
- `[BLOCKING] Card 12/15 misstate millpy-review-code.py's current briefs_dir binding` — Card 12 and Card 15 (`05-project-root-rebinding-review-side.md`) corrected: `briefs_dir` in `millpy-review-code.py` uses `project_root`, not `git_root`, and is fixed by this batch's rebind the same as the other two review-CLI files. The erroneous "already git_root-based, needs no change" claim and its corresponding "unaffected" test carve-out are removed.
- `[NIT] Module docstrings not updated for new keyword-only params` — Card 6 (`03-paths-skip-slug-validation.md`) now also requires updating `_paths.py`'s module-level docstring signatures for `resolve_active_worktree`/`resolve_active_hub` to include `skip_slug_validation`.
- `[NIT] find_active_slug module-summary line goes stale` — Card 3 (`02-on-disk-first-resolution.md`) now also requires updating `_review_common.py`'s top-of-file Public API summary line for `find_active_slug`.

## Pushed Back

(none — all 5 findings were confirmed accurate against source and fixed)
