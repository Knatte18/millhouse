MILL_REVIEW_BEGIN
# Review: Merge-in conflict handling: silent marker-verification gaps, mill-config.yaml chicken-and-egg crash, and undocumented dirty-worktree squash failure — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/
date: 2026-07-28
```

## Findings

### [BLOCKING] Card 14 missing Context entry for Card 12's file
**Location:** Batch 3 / Card 14 (`mill-merge-in/SKILL.md`)
**Issue:** Card 14's Requirements identify the kept-both `discarded` shape "by its `\"kept both sides...\"` description text from Card 12" — that exact string is introduced in `merge-in-conflict-brief.md` (Card 12's Edits target), but Card 14's `Context:` is `none` and `Edits:` is only `mill-merge-in/SKILL.md`. Per this batch's own precedent, Card 16 correctly adds a sibling card's file (`mill-merge/SKILL.md`) to its `Context:` when referencing that card's new content — Card 14 misses the equivalent step.
**Fix:** Add `plugins/mill/templates/merge-in-conflict-brief.md` to Card 14's `Context:` list.

### [NIT] Card 16 fixture weight for status-only checks
**Location:** Batch 4 / Card 16 (`test-merge.py`)
**Issue:** The dirty-edit and untracked-file negative-check sub-scenarios each spin up a full `_setup_trio()` (bare wiki repo + clone, child worktree, junctions) though neither needs anything beyond a hub-shaped git repo with one tracked file — only the mid-retry-squash case needs the worktree/child-branch half.
**Fix:** Consider a lighter fixture (plain `git init` + one commit) for the dirty-edit and untracked-file cases instead of two more full `_setup_trio()` spin-ups.

## Verdict

REQUEST_CHANGES
Card 14 needs Card 12's file added to Context; one minor test-fixture-weight NIT in Card 16.
MILL_REVIEW_END
