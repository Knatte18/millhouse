```yaml
round: 4
source_review: _mill/reviews/20260725-143200-plan-review-r4.md
```

## Fixed

- `[BLOCKING] Card 7's skip_slug_validation=True in-place tests reuse a non-git M2 fixture` — Card 7 (`03-paths-skip-slug-validation.md`), tests 1 and 4, corrected to build a real git repo at `git_root` (via `_test_helpers.init_minimal_git_repo` + `checkout_new_branch`, mirroring `_make_active_marker`'s pattern) instead of reusing the existing bare-directory M2 fixture, since Card 6's `skip_slug_validation=True` branch calls `_pygit2_util.current_branch` against the real filesystem with no daemon fallback.
- `[NIT] _pygit2_util import note in Card 6 slightly overstates uncertainty` — Card 6 reworded to state plainly that `_pygit2_util` is already imported at `_paths.py` line 98, dropping the unnecessary hedge.

## Pushed Back

(none — both findings were confirmed accurate against source and fixed)
