# Plan-fix report — round 1

```yaml
round: 1
fixer: mill-plan
source_review: 20260525-133146-plan-review-r1.md
date: 2026-05-25
```

## Fixed

- `20260525-133146-plan-review-r1.md` `[BLOCKING] import wiki does not expose client API functions` — Every `import wiki` directive across batch 2 card 4, batch 3 card 5, batch 4 card 6, batch 5 card 8, and batch 6 card 12 changed to the canonical `from wiki import _client as wiki` (mirrors `millpy-add.py:36`, `millpy-claim.py:46`, `millpy-cleanup.py:25`, `millpy-fold.py:39`, `_marker.py:22`). Card 8 (`test-fold.py`) uses the extended pattern `from wiki import _client as wiki, LOCKED_FOLD_PHASES, WikiPushError` since `LOCKED_FOLD_PHASES` and `WikiPushError` are top-level exports from `wiki/__init__.py` while functions live in `_client`. Each plan section that adds the import now also documents the rationale and references the canonical existing call sites.
- `20260525-133146-plan-review-r1.md` `[BLOCKING] Card 4 body rewrite references undefined source_tasks` — Card 4's `multi_select_groom_then_claim` body rewrite now uses `remove_slugs=source_slugs` directly (the parameter is `list[str]` per `_spawn_core.py:462–469`). The dict-iteration rewrite (`source_tasks: list[dict]`) is explicitly scoped to `prompt_merged_entry` (line 545) only, with a parenthetical noting it is in a different function.
- `20260525-133146-plan-review-r1.md` `[BLOCKING] Verify gate for batch 2 references file not in context` — Added `plugins/mill/unit_tests/test-millpy-color.py` to card 4's Context. Verified the file exists (`ls plugins/mill/unit_tests/test-millpy-color.py` succeeds; failure with `ModuleNotFoundError: No module named '_sidebar'` reproduced earlier in this session, see test output in `discussion.md`'s "Phase: Explore" section). Also proactively added `test-millpy-terminal.py` + `test-millpy-vscode.py` to batch 4 card 6's Context, and `run-all.py` to batch 6 card 13's Context — every verify-gate file is now in a Context allowlist somewhere in its batch.
- `20260525-133146-plan-review-r1.md` `[NIT] Decision name references nonexistent card 26` — Renamed `Decision: card-26-deletes-_task_to_dict-helper` → `Decision: card-4-deletes-_task_to_dict-helper` in `00-overview.md`, including the `Applies to:` reference.
- `20260525-133146-plan-review-r1.md` `[NIT] Card 7 cites card 15 of the parent plan` — Dropped the parenthetical; the docstring rewrite now reads `as returned by _junction.read_junctions` (the helper lives in `_junction.py` after the V3 adoption).
- `20260525-133146-plan-review-r1.md` `[NIT] Card 8 cites helper-deletion decision for test deletion` — Extended `Decision: card-4-deletes-_task_to_dict-helper` to explicitly cover "V2-only test cases get deleted, not bent" with an `Applies to:` line that names batches 5 and 6. Card 8's citation now matches the (now-broader) decision text.
- `20260525-133146-plan-review-r1.md` `[NIT] Card 12 deep-broken-test escape hatch is broad` — Capped allowable `pytest.skip` marks at 2 in card 12 across the four integration test files; over-cap triggers stuck-policy halt and operator redesign.

## Pushed Back

_(none)_
