# Plan fix report -- round 1

Source review: `20260606-191337-plan-review-r1.md` (verdict REQUEST_CHANGES, 9 BLOCKING + 4 NIT).

## Fixed

- r1 BLOCKING "Batch 1 verify references a non-existent test file" -- added a verify-ordering note to batch 01 (and batches 07, 08) clarifying mill-go runs `verify:` once at batch end after the test-creating card is committed.
- r1 BLOCKING "Card 4 directory-skip fix is incomplete: bulk_files_with_diff" -- Card 4 now requires the `PermissionError` guard in BOTH `bulk_files` and `bulk_files_with_diff`.
- r1 BLOCKING "Card 3 unfenced fallback contradicts review-output.schema.md" -- added `review-output.schema.md` to Card 3 Edits + a requirement to update its "Raises" wording; added it to overview All Files Touched.
- r1 BLOCKING "Card 5 resolve_large_prompt_timeout wiring ambiguous (prepare vs run)" -- Card 5 now names `_review_plan.run()` and both `_reviewer_single.run` call sites (primary + resume-retry) explicitly, excluding `prepare()`.
- r1 BLOCKING "Card 11 Windows process enumeration API unspecified (wmic deprecated)" -- Card 11 now mandates PowerShell `Get-CimInstance Win32_Process` for ProcessId+CommandLine (see Pushed Back for why not `tasklist`).
- r1 BLOCKING "Card 16 formatter-drift detection mechanism unspecified" -- Card 16 now specifies the deterministic whitespace-only heuristic (`git diff` non-empty AND `git diff -w` empty AND no untracked).
- r1 BLOCKING "Card 19 timeout threading path unspecified" -- Card 19 now states `_build_psmux_argv` gains a `timeout` param and the single call site (psmux invoke branch, timeout already in scope) passes it; `_reviewer_single.run` signature unchanged.
- r1 NIT "Batch 7 verify references test-preflight.py" / "Batch 8 verify references test-merge-in-subagent.py" -- ordering notes added to batches 07 and 08.
- r1 NIT "Card 10 orphan detection ignore OR report non-deterministic" -- Card 10 now mandates silent ignore of non-registry dirs (single deterministic behavior).
- r1 NIT "Card 5 DRY divergent helpers" -- Card 5 now requires extracting the shared size/threshold computation into one internal helper used by both functions; public signature of `maybe_switch_spec_for_large_prompt` unchanged.
- r1 NIT (reclassified) Card 22 `CLAUDE_PLUGIN_ROOT`-unset fallback -- added a fallback test case to Card 22.

## Pushed Back

- r1 BLOCKING "Card 15 millpy-implement.py absent from Context" -- FACTUALLY WRONG. `millpy-implement.py` is Card 15's `Edits:` target. Per the plan-batch template (lines 47-48, 56), Edits files are implicitly read by the implementer and the plan-reviewer bulks `Context: union Edits:`, so the file is both read and bulked. Adding it to Context would violate the template rule "Files listed in Edits: are implicitly read -- do not repeat them in Context." No change.
- r1 BLOCKING "Card 23 ... include millpy-merge-in-subagent.py in Context" (Context part only) -- FACTUALLY WRONG for the same reason: it is Card 23's `Edits:` target (implicitly read and bulked). The legitimate part of the finding (ambiguous three-case branching) WAS fixed -- Card 23 now spells out Cases A/B/C explicitly. Added a note in Card 23 that the Edits target is implicitly read.
- r1 BLOCKING "Card 11 ... use tasklist /FO CSV /V /NH" (the specific tool suggestion) -- partial push back on the suggested API: `tasklist` exposes only the window title, NOT the command line, so it cannot match bash poll-loops by worktree path. Fixed the underlying gap with the correct API (`Get-CimInstance Win32_Process`, the supported `wmic` replacement that does expose `CommandLine`). The finding's intent (avoid deprecated `wmic`, specify a working Windows enumerator) is satisfied.
