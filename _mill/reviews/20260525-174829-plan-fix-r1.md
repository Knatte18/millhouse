# Plan fix: round 1

```yaml
round: 1
source_review: 20260525-174704-plan-review-r1.md
date: 2026-05-25
```

## Fixed

- `20260525-174704-plan-review-r1.md` -- "[NIT] test-bg-launcher.py Home.md double-bracket not fixed" -- Card 8 now requires changing the inlined Home.md from `[[{slug}]] [active]` to `[{slug}] [active]` and updating the matching in-file assertion. Aligns the bg-launcher fixture with the V3 `parse_home_md` syntax so future Home.md-parsing call sites do not silently miss the seeded task.
- `20260525-174704-plan-review-r1.md` -- "[NIT] WikiServer logger name collision risk in Card 7 regression test" -- Card 7 now filters handlers defensively by `RotatingFileHandler` instance type AND `baseFilename` matching the test's own `wiki_path`, asserts `len(our_handlers) == 1`, and post-`on_stop` checks `handler not in wiki_server._log.handlers` rather than `handlers == []`. Robust against any stale handler left behind by an earlier `WikiServer` instance on the shared `"wiki-server"` logger in the same runner process.
- `20260525-174704-plan-review-r1.md` -- "[NIT] Batch 2 verify comment understates expected reds" -- Batch 2's "Batch Tests" prose now states explicitly that batch 3 runs in parallel (`depends-on: []`), so the "3 transient reds" only describes the case where mill-go schedules batch 2 before batch 3; the authoritative pass condition is the overview-level verify run after all three batches land (77/77).

## Pushed Back

(none)
