# Plan fix — round 2

Source review: `_mill/reviews/20260919-114314-plan-review-r2.md`

## Fixed

- "Card 1's verbatim insertion is a blockquote; sibling 'Why' text is plain" — verified via grep that no `**Why` callout in `plugins/mill/skills/` is blockquoted; dropped the leading `> ` and added an explicit instruction that the paragraph must be plain, matching its sibling.
- "Card 2's verbatim insertion is a blockquote; its cited precedent is plain" — same fix: dropped the leading `> ` and added an explicit plain-paragraph instruction.
- Consequence of the above two fixes (caught by re-running the full validator gate, not by the reviewer): removing Card 2's blockquote marker removed the `context-completeness` check's blockquote exemption for its `_mill/status.md` mention, so the check now correctly flagged that path as referenced but not listed. Added `_mill/status.md` to Card 2's `Context:` list.

## Pushed Back

(none)
