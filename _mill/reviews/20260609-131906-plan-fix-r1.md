# Plan Fix Report — Round 1

Source review: `20260609-131822-plan-review-r1.md`

## Fixed

- **[NIT] Card 5 (`02-review-cli-finalize.md`)** "discussion-review --max-rounds becomes dead in finalize" — Added explicit note to Card 5 Requirements that `--max-rounds` is intentionally a prepare-only concern in finalize; implementer must not add enforcement logic there.
- **[NIT] Card 8 (`04-unit-tests.md`)** "Card 8 mock list omits read_branch/status_path specifics" — Added `millpy_fix._paths.status_path` and `millpy_fix._paths.resolve_task_path` to the shared mock helper specification, with explanation that these patches are required to reach the finalize early-return cleanly.

## Pushed Back

(none)
