# Plan fix report — round 5

Source review: `_mill/reviews/20260812-092441-plan-review-r5.md`

## Fixed

- r5 "Card 12 bakes an ungrounded \"validator-fix\" concept into the tree-guard block" (BLOCKING:design) — accurate on both counts, verified directly: `grep -c validator plugins/mill/skills/mill-go-base/SKILL.md` returns 0, so the concept does not exist in the file card 12 edits (it belongs to mill-plan's `_plan_validate` gate, which is where the phrasing was carried over from). The structural half of the finding is also right: the ERROR-only-aggregate retries at `### 3. Code Review loop` sub-step 4.5 and `## Holistic code review` sub-step 3.5 are separate dispatch points with their own pre/post checkpoint pairs in card 13's enumeration, not sub-cycles of the preceding dispatch, so describing the post-dispatch form as spanning them would have contradicted card 13. The post-dispatch form now reads "once per dispatch, immediately after that dispatch's prepare-through-finalize sequence returns", with an explicit instruction not to describe it as spanning a retry cycle and a pointer to why.

## Pushed Back

None. The finding was accurate and the fix caused no harm.
