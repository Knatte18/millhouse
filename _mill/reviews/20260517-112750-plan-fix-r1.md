# Plan fix — round 1

Source review: `20260517-112427-plan-review-r1.md` (verdict: REQUEST_CHANGES, blocking_count: 1)

## Fixed

- `[BLOCKING] Batch 2 breaks all existing flow tests: empty-repo git rev-parse HEAD` — Extended cards 3, 4, 5 to also seed an initial commit at every `git init` call site in their corresponding flow-test fixtures (`test-review-plan-flow.py`, `test-review-code-flow.py`, `test-review-discussion-flow.py`). Each card's Edits: now lists its flow-test file alongside the backend module; the requirement text provides the verbatim seed-commit block to insert after each `git init` (+ optional `git checkout -b`). Updated Batch 2's Batch Scope to explain why fixture seeding is needed; updated Batch Tests to call out the new failure-mode signal (`ReviewError: git rev-parse HEAD failed`). Updated overview `All Files Touched` to include the three flow-test files in sorted order. Took option (a) from the reviewer's fix block — fixing fixtures rather than special-casing empty-repo in the guard helper — to keep `_capture_head_sha` strict (any empty-repo state IS suspicious in a real review run; the helper should not silently absorb it).
- `[NIT] Case E fixture missing directory-creation step` — Card 2's Case E requirement now spells out `(tmp / "allowed").mkdir(parents=True, exist_ok=True)` before the `write_text` call, with an inline note explaining why `allowed/` does not pre-exist.
- `[NIT] run-all.py not updated; new test file may be silently excluded` — Verified `plugins/mill/unit_tests/run-all.py:19` uses `HERE.glob("test-*.py")` for auto-discovery. Added a one-line clarification under Batch 1's Batch Tests confirming this so a future reader does not have to re-check.

## Pushed Back

(none)
