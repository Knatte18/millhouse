# Discussion-fix report — round 2

Source review: `_mill/reviews/20260716-133541-discussion-review-r2.md` (verdict: APPROVE, 3 NOTE findings, 0 BLOCKING).

## Fixed

- Source: `20260716-133541-discussion-review-r2.md`, finding "**#651: absent `effort` on haiku not in tier spec**" — specified that a resolved spec with no `effort` key (e.g. shipped `haiku`/`haiku_bulk`) ranks as `0` (same as `low`) for the tiebreaker, in the `fixer-tier-warning-scope-and-callsite (#651)` Decision.
- Source: `20260716-133541-discussion-review-r2.md`, finding "**#640: git_root param optionality vs "ROOD tests unchanged"**" — changed `git_root` to `Path | None = None`, mirroring `compute_scope_violations`'s existing optional-`git_root` pattern, so the existing three-positional-argument `ROOD-*` test call sites need no edit, in the `cleanliness-revert-hub-prefix-fix (#640)` Decision.
- Source: `20260716-133541-discussion-review-r2.md`, finding "**Stale sequential-batch claim in Q&A log**" — annotated the superseded Q&A entry inline as corrected, pointing to the batch-structure Decision and the two later Q&A entries that already fix the claim.

## Pushed Back

(none)
