# Plan-fix round 1

Source review: `_mill/reviews/20260904-101335-plan-review-r1.md`

## Fixed

- Source: `20260904-101335-plan-review-r1.md` — `[BLOCKING:design] commit_sha_field_name override leaves stale commit_sha key in output`. Verified by re-reading `_implementer_common.py` lines 1873-1901 and Batch 03 Card 6's own fixture (`{"status":"success","commit_sha":"xyz"}`): the original Card 3 Requirements only added `parsed[commit_sha_field_name]`, never removing the pre-existing self-reported `parsed["commit_sha"]` key, so both keys would coexist under a non-default override. Updated Card 3's Requirements in `03-commit-sha-field-rename-and-regression-tests.md` to pop `"commit_sha"` from `parsed` before setting the new key whenever `commit_sha_field_name != "commit_sha"`, with the rationale spelled out explicitly so the implementer doesn't drop the guard.
- Source: `20260904-101335-plan-review-r1.md` — `[NIT:consistency] Card 1 quotes an anchor sentence that isn't the paragraph's actual end`. Verified by re-reading `implementer-brief.md` lines 142-148: the Card-count self-check paragraph continues one more sentence ("This applies regardless of which model...") past the quoted anchor. Corrected Card 1's Requirements in `01-brief-instruction-hardening.md` to anchor on the paragraph's actual final sentence.

## Pushed Back

(none — both findings verified accurate, both fixed)
