MILL_REVIEW_BEGIN
# Review: mill-go-base: orchestration robustness gaps — holistic

```yaml
duration_s: 259.0
verdict: APPROVE
reviewer_model: sonnethigh
reviewer_self_id: claude-sonnet-5 (Sonnet 5)
reviewed_file: plan/
date: 2026-09-18
```

## Findings

(none)

Verification performed: read all 8 plan files (overview + 7 batches) and cross-checked every
mechanism claim against the actual source it cites — `_status.py` (`phase_entry_timestamp`
signature at line 820-822, `set_batch_field`'s `None`-pop convention at line 974-975,
`append_phase`'s `blocked_reason` auto-clear at line 478-489, `_BATCH_ALLOWED_KEYS`/`_BATCH_STATES`
sets), `millpy-implement.py`'s `_run_baseline_stage` (lines 216-477, confirming the
`batches_needing_computation`/`cached_batches` population block at lines 295-305 and Case A/Case B
branch structure Card 4 describes), `test-millpy-implement.py`'s `_run_main`/module-attribute
mocking convention and its two named existing tests, `test-status.py`'s actual section-marker
layout (confirmed `# --- set_batch_fields tests ---` at line 683 is immediately followed by
`# --- read() tests ---` at line 747, exactly as Card 2 requires for the new `resume_batch`
section's insertion point), `SKILL.md`'s Entry phase gate/phase table (lines 89-122), the
`Entry-gate wait for upstream mill-plan` bullet block (lines 160-201), `0.5. Baseline pre-flight`
(lines 548-577), all three `TaskOutput(task_id: <agentId>, block: false)` call sites (lines 303,
325, 393) and the exact quoted anchor/intervening sentences in both the per-batch (lines 840-844)
and holistic (`holistic-review.md` lines 167-169) NIT-fix dispatch sites, `resume.md` (confirmed no
existing `blocked` mention), `handoff.md` step 6's exact quoted text (lines 169-173) and
`worktree_root`'s binding at `SKILL.md`'s Path Setup (line 80), `_builder_lock.py`'s
`mkdir(parents=True, exist_ok=True)` precedent, `_bg.check_bg_status`'s three-state return contract,
`_plan_validate.py`'s `_check_parallel_modifies_overlap` (confirmed it uses transitive-ancestor BFS,
validating the plan's claim that the 3→5→4→6 dependency chain covers all pairwise `SKILL.md`-overlap
serialization without needing direct edges), and `mill-plan/SKILL.md` (confirmed zero references to
`millpy-builder-lock.py`, supporting batch 3's race-avoidance rationale).

No BLOCKING or NIT findings. Batch Index DAG is acyclic, every `file:` referenced exists, every
`depends-on` batch number is declared. All Moves are `none` (no Rename mechanic section required,
correctly absent). Context/Edits fields are non-empty and consistent with each card's Requirements;
the two file-pattern citations that don't appear in a card's own `Context:` (`_builder_lock.py` in
batch 3 card 5, referenced for a pattern whose exact code is already inlined in the Requirements
text) fall under the "signature inlined" exemption, not a gap. Global card numbering is unique and
the retirement of card 3 is explicitly and consistently documented. `verify:` commands for all seven
batches are deterministic marker/test-suite checks, correctly scoped per batch.

## Verdict

APPROVE
Every mechanism claim source-verified true; DAG, Context, and Requirements are consistent and complete.
MILL_REVIEW_END
