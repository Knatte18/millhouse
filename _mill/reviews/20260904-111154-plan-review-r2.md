MILL_REVIEW_BEGIN
# Review: millpy-implement/fix.py: stuck-type false positives and session-hygiene gaps — holistic

```yaml
duration_s: 243.0
verdict: APPROVE
reviewer_model: sonnethigh
reviewer_self_id: claude-sonnet-5 (per environment metadata; self-assessment, not independently verifiable)
reviewed_file: plan/
date: 2026-09-04
```

## Findings

No findings.

Cross-checked every quoted code snippet and cited line/signature against the actual source across all five batches:

- Batch 1 Card 1: the corroboration-waiver `try/except`/`batch_result = None` block quoted verbatim matches `_implementer_common.py` lines 1180-1191 exactly; `_subprocess_util.git_commit(cwd, message, *, name, email)` signature matches lines 219-234 exactly.
- Batch 1 Card 2: `finalize_from_output`/`_forward_output` both end their signatures in `batch_name: str | None = None,`; all four `_run_verify_gates` call sites inside `_forward_output` end in `batch_name=batch_name,`; `_in_scope_dirty_stuck` is called exactly once, downstream only of the first (explicit-JSON) call site — confirmed by reading each call site.
- Batch 1 Cards 3-4: git-identity resolution in both `millpy-implement.py` and `millpy-fix.py` occurs before slug resolution, and both finalize-branch `finalize_from_output(...)` calls end in the claimed last kwarg (`module_wide_cwd_override=`/`cwd_override=`) — confirmed.
- Batch 3 Card 6: `overview_path`/`plan_base` already defined before the finalize block in `millpy-fix.py`; `_status.get_module_verify_baseline`, `_status.read_batches`, `_plan_dag.iter_batch_verifies` (returns `(batch_name, verify_cmd, cwd)` triples) all match their actual signatures and usage.
- Batch 4 Cards 8/10: `_BATCH_ALLOWED_KEYS` literal matches exactly; `_status.read_full` returns `{"yaml":..., "timeline":...}` and is already called unguarded elsewhere in `millpy-implement.py`'s `main()` (line 602), so the new unguarded call is consistent with existing convention, not a new risk; the manual `split(None, 1)` / `.strip("'\"")` parsing mirrors `_status.phase_entry_timestamp`'s internal logic exactly; the fresh-mint `set_batch_fields` call being replaced matches verbatim; `self-resolved-dead-parent`'s "fires mid-flow during a cleanliness gate, not before re-dispatch" claim is corroborated by `mill-go-base/SKILL.md` line 708.
- Batch 5 Cards 12-13: the pre-edit `_worker_main` body quoted in Card 12 matches `millpy-bg.py` lines 56-70 verbatim; `_worker_mod`/`_worker_main` module-level test fixtures Card 13 assumes already exist in `test-millpy-bg.py`.

DAG integrity: batches 1-5, deps `[]/[1]/[1]/[1]/[]`, no cycles, all `depends-on` targets declared, all `file:` entries present. Global card numbering 1-13 sequential, no gaps. `## All Files Touched` matches the union of `Edits:` across all batches exactly (no `Creates`, no `Moves`). No card has a non-empty `Moves:`, so no `## Rename mechanic` section is required and none is missing. All three Shared Decisions are faithfully threaded through the batches that touch them, including the None-default-degrades-to-no-op convention on every new parameter/field. `Context:`/`Edits:` completeness checked per card — every function/constant named in a card's `Requirements:` is covered by that card's `Context:` or `Edits:`. All five `verify:` commands are Python-project-correct (`PYTHONPATH= ` prefix) and target the files each batch actually edits.

## Verdict

APPROVE
Every quoted snippet, signature, and cross-file claim verified accurate against source; no constraint, sequencing, or coverage gaps found.
MILL_REVIEW_END
