# Discussion fix report — round 3

Source review: `_mill/reviews/20260904-163053-discussion-review-r3.md`

## Fixed

- r3 NIT:consistency — "card-numbering claim overstates actual enforcement": verified `_check_card_numbering` (`_plan_validate.py:912`-978) enforces only within-batch sequencing and cross-batch uniqueness, never cross-batch monotonicity. Replaced the overstated rationale with a composite ordering key `(batch file-sort index, card number)` for the forward-creates direction test, plus an explicit statement that this task does not add a monotonicity check to `_check_card_numbering`. Added the malformed-numbering test scenario.
- r3 NIT:decision — "`_extract_requirements_text`'s truncation risk vs. new fence tracking left unaddressed": verified the helper's boundary scan is fence-blind and that `_requirements_fence_aware_body` (line 2235) exists for exactly this reason. Recorded the disposition — the step-1 call at line 2057 switches to `_requirements_fence_aware_body`, both helpers otherwise unmodified — in the quoted-material Decision, the Scope In bullet, and the Technical context flow, and added both fence-boundary test directions.

## Pushed Back

(none)
