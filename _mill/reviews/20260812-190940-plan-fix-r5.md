# Plan fix — round 5

## Fixed

- `20260812-190833-plan-review-r5.md` — "[BLOCKING:design] Card 10's rollback-fix repro never checks out `main`, so its assertions don't test what it claims": added an explicit `git -C parent_rb checkout main` after step 3's checkpoint creation, matching Cards 9/12's own convention, so steps 4-6's `reset --hard`/`rev-parse HEAD` assertions actually operate on `main` (the parent worktree's real branch) instead of the throwaway checkpoint-creation branch.
- `20260812-190833-plan-review-r5.md` — "[BLOCKING:scope] Card 6's inserted bash blocks reference `_config`/`_paths` functions not listed in Context": added `plugins/mill/scripts/_config.py` and `plugins/mill/scripts/_paths.py` to Batch 2 Card 6's `Context:` list.

## Pushed Back

(none)
