# Plan-fix round 1

Source review: `_mill/reviews/20260821-091146-plan-review-r1.md` (verdict REQUEST_CHANGES, blocking_count 1, 2 NIT, 1 demoted from BLOCKING).

## Fixed

- **[BLOCKING:scope] Card 2 Requirements cite mill-go-base/SKILL.md content not in its Context** — added `plugins/mill/skills/mill-go-base/SKILL.md` to Card 2's `Context:`, and reworded Card 2's Requirements to point the implementer at that file's Entry section directly, quoting the exact "Override point B" text to confirm before writing the edit, rather than asserting the mechanism secondhand.
- **[NIT:consistency, demoted] Card 1's "(every attempt)" contradicts Card 3's narrowed forking** — changed Card 1's replacement description text from "Forks the implementer (every attempt) ..." to "Forks the implementer (every batch's initial dispatch and transient re-dispatch) ...", matching Card 3's actual narrowed forked set (excludes the Stuck-escalation self-resolve re-fire, which Card 3 moves to cold), and added a clarifying sentence explaining why the original wording would have misrepresented Card 3's own change.
- **[NIT:design] Ambiguous double-logging when both cold-fallback triggers occur in one batch** — added a clarifying sentence to Card 3's "Cold fallback, once per batch" bullet stating the logging fires at most once per batch, because whichever trigger reaches it first switches the batch to cold dispatch, leaving no second fork for the other trigger to fail on and re-log against.

## Pushed Back

(none — all three findings were factually accurate; fixed per the mill-receiving-review default.)
