# Plan Fix Report — round 2 (NIT pass)

## Fixed

- `20260522-073212-plan-review-r2.md` "[NIT] Stale code-block comment in mill-setup 'How to invoke the helpers'" — added Edit C to Card 1 (batch 01) to update the `# RIGHT` comment from "the canonical mill-script form, shared with every other mill SKILL.md" to "mill-setup bootstrapper exception; all other skills use '$MILL_PYTHON'".
- `20260522-073212-plan-review-r2.md` "[NIT] Venv-check semantics degrade when MILL_PYTHON is unset or stale" — updated Card 5 (batch 03) to add two restoration steps after the `replace_all`: Steps 2 and 3 revert the two venv-check blocks in mill-go/SKILL.md to the explicit `"${CLAUDE_PLUGIN_ROOT}/.venv/Scripts/python.exe"` path. Updated batch 03 scope, batch tests, and card 11 (verify) expected grep output to reflect the 2 intentional remaining hits in mill-go/SKILL.md.

## Pushed Back

(none)
