# Plan fix -- round 2

Source review: `_mill/reviews/20260513-080151-plan-review-r2.md`

## Fixed

- [BLOCKING] `build_plan internal config load breaks test-cleanup.py verify` -- applied reviewer's option (a): card 8 now explicitly leaves the `build_plan` line-130 call site unchanged (`_paths.resolve_task_path(wt_path, "_mill/status.md")` stays). Rationale recorded inline in the card: `build_plan` is called directly by `test-cleanup.py` with tempdir wiki paths (no `paths.status_md` key), so `_paths.status_path` would `KeyError`; the discussion's uniformity decision is restricted to call sites that already have `cfg`. Eight `test-cleanup.py` `build_plan` invocations (lines 203, 228, 250, 273, 292, 316, 331, 349) are spared rewrites.
- [NIT] `Card 8 misidentifies _load_config as locally defined` -- moot after the BLOCKING fix; the reference to `_load_config` was removed when the line-130 entry was reduced to "leave unchanged". No remaining text in card 8 mentions `_load_config`.

## Pushed Back

(none)
