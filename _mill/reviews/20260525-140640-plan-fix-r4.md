# Plan-fix report — round 4

```yaml
round: 4
fixer: mill-plan
source_review: 20260525-140426-plan-review-r4.md
date: 2026-05-25
```

## Fixed

- `20260525-140426-plan-review-r4.md` `[BLOCKING] millpy-wikipush leaves orphaned try: block` — Card 6 now explicitly instructs deletion of line 110 (`try:`) in addition to line 111 (`with`) and lines 113-118 (`except` body). Post-edit shape included (the block collapses to two lines: `changed = _changed_files(wiki)` + `return _push_inner(wiki, changed, leave_conflicts=args.leave_conflicts)`). Added `ast.parse` syntax check as a verification step.
- `20260525-140426-plan-review-r4.md` `[NIT] Card 11 fixture-helper scope ambiguity` — Card 11 now points the implementer at the shared `_setup_wiki(wiki_path)` helper at `test-wiki-noop-commit.py:31`. One helper edit fixes all three tests; no per-test patching. Bare-clone setup uses `wiki_path.parent / f"{wiki_path.name}.git"` for the sibling bare.
- `20260525-140426-plan-review-r4.md` `[NIT] Card 8 patching scope leaks to other importers` — Added explicit paragraph noting that `mill_fold.wiki` IS the `wiki._client` module object (alias-binding) and patching its attributes is process-global. Recommended `unittest.mock.patch.object` context manager for guaranteed restoration on assertion failure.
- `20260525-140426-plan-review-r4.md` `[NIT] Card 9 retains redundant slug-matching-s test` — Added `test_pick_task_single_slug_matching_s` (`test-spawn-core.py:197`) to the deletion list in card 9. Rationale: under V3 it is functionally identical to `test_pick_task_single_slug_matching_unmarked` (line 189), so it adds no coverage.
- `20260525-140426-plan-review-r4.md` `[NIT] Card 4 proposal-file rendering claim is unverified` — Added `plugins/mill/scripts/wiki/_render.py` to card 4's `Context:` (line 56 verifies the claim `result[f"proposal-{slug}.md"] = body`). The card body now cites the line explicitly so the implementer can verify the daemon-renders-proposal behaviour from source.

## Pushed Back

_(none)_
