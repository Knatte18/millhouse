# Review: mill-go / mill-merge / plan-validator follow-up bugs (round 2) — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-31
```

## Findings

### [NIT] Card 7 test patching target underspecified
**Location:** Batch 2 / Card 7
**Issue:** The plan says to "intercept the `_forward_output` call" but doesn't name the patching target. Because `millpy-fix.py` uses `from _implementer_common import _forward_output`, the implementer must patch `millpy_fix._forward_output` (the local binding), not `_implementer_common._forward_output`, to intercept the call.
**Fix:** Add one line: "Patch via `unittest.mock.patch.object(millpy_fix, '_forward_output', side_effect=...)` to capture the `start_sha` kwarg."

### [NIT] Card 11 Test B setup is over-described
**Location:** Batch 3 / Card 11
**Issue:** "Same path resolves identically" is harder to construct in a temp fixture than the simpler alternative the plan itself acknowledges. The "simpler alternative" (no augmentation path exercised at all, result unchanged) achieves full coverage of the guard condition.
**Fix:** The plan's "simpler alternative" is the right implementation; the primary description can be dropped to avoid confusion.

## Verdict

APPROVE
Plan is complete, specific, self-consistent, and safe to implement as written.