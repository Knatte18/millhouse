# Plan fix -- round 4

```yaml
review: 20260808-175749-plan-review-r4.md
round: 4
```

## Note on the round-4 envelope

Unchanged from rounds 1-3: `blocking_count: 0` against `REQUEST_CHANGES`, because the reviewer emits the classed heading syntax this task introduces and the installed cache's regex cannot match it.
Routed off the review file: 1 BLOCKING, 0 NIT, step 4d.

## Fixed

- `20260808-175749-plan-review-r4.md` -- "[BLOCKING:consistency] Title-based dedup can silently drop a distinct finding, breaking the file/envelope invariant".
  Accurate.
  Card 2 specified a flat first-occurrence-wins dedup over the concatenated scans, so two distinct same-mechanism findings sharing a title would collapse to one, dropping it from `findings` and both scalars; combined with card 4's "match each heading exactly once", the un-extracted `### [BLOCKING:<cls>]` heading would never be rewritten, leaving a BLOCKING heading on disk the envelope no longer counts.
  Took the reviewer's first suggested fix, not its accepted-risk alternative: scoped the dedup to cross-mechanism collapse only, so same-mechanism title collisions keep both findings.
  This is faithful to the `dual-mechanism-scan-preserved` Decision rather than in tension with it -- that Decision's dedup exists to stop one finding being counted twice for appearing in both representations, not to merge distinct findings within one, so no pushback on documented-design grounds applies.
  Also fixed the half of the defect the finding identified but did not prescribe a fix for: card 4's rewrite now keys on the pair (`title`, `cls`) and rewrites every matching occurrence instead of the first only.
  That pairing is what makes duplicate titles safe without positional bookkeeping -- two out-of-ceiling `[BLOCKING:scope]` headings with one title are both rewritten, while a same-titled `[BLOCKING:design]` heading does not match the pair and is correctly left alone.
  Dropped the now-wrong "match each heading exactly once" clause, and added a card 8 test asserting both same-mechanism findings survive in `findings`, contribute to the scalars, and leave no residual `BLOCKING` heading on disk.
  Recorded the cross-mechanism-only scoping and its reason in the overview's `dual-mechanism-scan-preserved` Decision.

## Pushed Back

(none)
