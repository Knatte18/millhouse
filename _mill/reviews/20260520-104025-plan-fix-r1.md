# Plan Fix Report — round 1

```yaml
source_review: 20260520-103817-plan-review-r1.md
round: 1
date: 2026-05-20
```

## Fixed

- `20260520-103817-plan-review-r1.md` / "[BLOCKING] Batch 3 verify breaks existing test-paths.py mock tests" — added card 11 to batch 03 specifying rewrite of all `patch("_subprocess_util.run", ...)` test blocks for `resolve_main_worktree_root` and `resolve_git_root` wiki-guard tests; mock target changes to `_pygit2_util.discover_workdir` where needed; real-git-repo replacements for subprocess output tests.
- `20260520-103817-plan-review-r1.md` / "[NIT] list_worktrees gitdir path stripping fragile on Windows" — updated card 2 in batch 01 to use `(wt_dir / Path(content.strip())).resolve().parent` instead of string-stripping `"/.git"` suffix; handles backslashes and relative paths on Windows.
- `20260520-103817-plan-review-r1.md` / "[NIT] Rename entries in status_porcelain deviate silently from porcelain v1" — added note to `status_porcelain` requirements in card 2 that `repo.status()` returns only the new path for index renames (no `oldpath -> newpath` format), and that callers using line-set arithmetic are unaffected; directed the implementer to document this in the docstring.

## Pushed Back

(none)
