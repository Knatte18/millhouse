# Plan fix — round 1

Source review: `20260812-180740-plan-review-r1.md`

## Fixed

- `[BLOCKING:scope] Card 3 Requirements name _claude_settings identifiers absent from its Context` — added `plugins/mill/scripts/_claude_settings.py` to Card 3's `Context:`.
- `[NIT:consistency] New helper omitted from __all__ and from the (already-incomplete) Exports docstring convention` — added `resolve_plugin_root_from_syspath` to Card 1's `__all__` instruction; replaced the docstring-update instruction with an explicit note that the module's "Exports -------" docstring block already omits both sibling functions and is not an actively maintained convention to extend.
- `[NIT:consistency] Card 3 commit message covers only one of the two edited snippets` — broadened Card 3's commit message to `fix(mill-setup): resolve plugin root via sys.path scan in Phase 4.8 write + Phase 8 verify`.

## Pushed Back

(none)
