# Discussion fix — round 1

Source review: `_mill/reviews/20260513-074401-discussion-review-r1.md`

## Fixed

- [NOTE] `write_initial_status callers not listed in Technical Context` — added a parenthetical to the `_spawn_core.py:716,720` entry and to the `millpy-spawn.py:165` / `millpy-claim.py:216` entries noting the `write_initial_status` callers at `millpy-spawn.py:240` and `millpy-claim.py:300` also need `cfg` threaded through. Function signature line corrected from `~681` to `672`.
- [NOTE] `_review_code.py needs import _paths — not mentioned` — added explicit "Also add `import _paths` to the import block" note to the `_review_code.py:220` entry, explaining the current imports pull `resolve_path` from `_review_common`, not `_paths`.

## Pushed Back

(none)
