# Plan fix — round 1

```yaml
round: 1
review: 20260812-063529-plan-review-r1.md
```

## Fixed

- r1 `[BLOCKING:scope]` "Summary CLI never matches per-batch code-review filenames" — verified against `write_review_file`'s `review_type in ("plan", "code")` filename branch; card 30 now specifies the batch-scoped pattern for both types (mirroring `RE_BATCH`'s alternation) and card 31 adds a per-batch code-review filename case.
- r1 `[BLOCKING:design]` "`_review_one_batch`'s outer ReviewError handler can read unset locals" — verified: `_review_plan.py:170-195` raises `ReviewError` from the round-cap guard and `resolve_ref_paths` before any reviewer call. Card 23 now requires `duration_s = tool_calls = cost_usd = None` as the first statement inside the try; card 26 gains case (6) as the regression guard.
- r1 `[NIT:consistency]` "Card 33's regen command bypasses the mandated cache-form invocation" — card 33 now uses the `${CLAUDE_PLUGIN_ROOT}` cache form CLAUDE.md mandates for operational calls.
- r1 `[NIT:scope]` "Card 30 omits resolving `wiki_root` needed by `find_active_slug`" — card 30's resolution list now includes `wiki_root` via `_paths.resolve_wiki_path(git_root)` and names it as `find_active_slug`'s second positional argument.
- r1 `[NIT:consistency]` "Shared Decision undercounts the ReviewError sites it describes" — the Shared Decision now says six of seven and enumerates each site, noting where discussion.md's narrower "four of five" count came from.

## Pushed Back

_(none — every finding was accurate and none of the fixes met a harm-check exception.)_
