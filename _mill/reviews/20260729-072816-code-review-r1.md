MILL_REVIEW_BEGIN
# Review: mill-merge misjudges worktree topology and mishandles Step 5 squash-restore checkout — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-07-29
```

Both batches were verified end-to-end against their cards and the two Shared Decisions.

- Batch 1 (`_inplace.py`): `is_inplace` now does `resolve_main_worktree_root(git_root)` + `samefile`/`resolve()` fallback exactly as specified, `slug`/`cfg` retained unused with docstring notes, module/API docstrings updated, `prompt_stale_worktree` untouched. Signature test (`params == ["slug","git_root","cfg"]`) still passes unmodified.
- `test-inplace.py`, `test-paths.py` (4 sites), `test-review-common.py` (2 sites) all migrated from `_inplace.resolve_worktrees_dir` to `_inplace.resolve_main_worktree_root`/dropped per-card instructions; no stale `resolve_worktrees_dir` references remain anywhere touching `_inplace`.
- `test-cleanup.py`'s new `test_resolve_inplace_mode_topology_outcomes` matches Card 5 precisely: patches both `mill_cleanup._paths.resolve_main_worktree_root` and `mill_cleanup._inplace.resolve_main_worktree_root` independently (confirmed both are separate module-level imports in `millpy-cleanup.py`), covers Case A (inplace) and Case B (the previously-unreachable `("worktree", "")` fallback at `millpy-cleanup.py:437`), registered in `main()`.
- Batch 2 (`mill-merge/SKILL.md` Step 5): checkout line now guarded with `2>/dev/null || true`; `Why:` prose corrected to state the true (non-no-op) exit-1 pathspec-match behavior and the guard's rationale; the `reset` line and all other Steps left untouched.
- `test-merge.py`'s new Step 5 guard scenario is a fresh, self-contained fixture (does not reuse `_setup_trio`'s hub/worktree/container), reproduces #736 (bare checkout fails with "did not match any file"), proves the guarded form (`shell=True` for the `||` construct) exits 0, and asserts the squash commit lands `feature.txt` while excluding `_mill` — inserted at the exact specified location in `main()`.

No out-of-plan files, no duplicated helpers, no cross-batch contract issues (the two batches are independent, as declared, and touch disjoint files). No language pitfalls (mutable defaults, import side effects, path-separator issues) found in the diffs reviewed.

## Verdict

APPROVE
All cards across both batches are correctly and completely implemented per plan; no findings.
MILL_REVIEW_END
