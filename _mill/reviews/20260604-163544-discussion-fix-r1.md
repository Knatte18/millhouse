# Discussion Fix — Round 1

Source review: `_mill/reviews/20260604-122632-discussion-review-r1.md`

## Fixed

- **[NOTE] Staged files left uncommitted when commit is skipped** — clarified in `§ Skip duplicate "start batch" commit on re-fire (#412)` that the entire `git add` + commit + push block is skipped together; staged files must not be left when the implementer session starts.
- **[NOTE] start_sha = None unguarded in millpy-fix.py commits_made path** — added guard in `§ commits_made in stuck JSON — Technical context: millpy-fix.py`: when `start_sha is None`, set `commits_made = 0` and skip the `git rev-list` call.

## Pushed Back

(none)
