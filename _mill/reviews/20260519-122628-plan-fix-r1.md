# Plan-fix report — round 1

```yaml
review_file: _mill/reviews/20260519-122452-plan-review-r1.md
round: 1
```

## Fixed

- `20260519-122452-plan-review-r1.md` [BLOCKING] "Card 1 describes mill-config.yaml roles block as commented-out, but it is active" — Card 1 of batch 1 now states the `roles:` block is already active in `plugins/mill/templates/mill-config.yaml` (lines 122-159) and instructs the implementer to add `fixer:` as a sibling of `implementer:` under that existing block. Added an explicit "do NOT create a second top-level `roles:` key" guard with the PyYAML last-wins rationale.
- `20260519-122452-plan-review-r1.md` [NIT] "Batch 3 frontmatter `cards: 5` does not match" — `03-cutover.md` frontmatter changed from `cards: 5` to `cards: 3`.
- `20260519-122452-plan-review-r1.md` [NIT] "Card 5 requirements reference modules absent from Context list" — Card 5 of batch 2 now lists `_plan_dag.py`, `_marker.py`, `_render.py`, `_reviewers.py`, `_timestamp.py` in its Context block.
- `20260519-122452-plan-review-r1.md` [NIT] "`_timestamp` import in millpy-implement.py becomes dead after resume branch removal" — Card 8 of batch 3 Requirements now instructs the implementer to remove the `import _timestamp` line as part of the same edit.

## Pushed Back

- `20260519-122452-plan-review-r1.md` [NIT] "`run-all.py` not listed in files-touched; static test list would fail batch 3 verify" — `plugins/mill/unit_tests/run-all.py` line 19 reads `tests = sorted(HERE.glob("test-*.py"))`. It uses filesystem glob auto-discovery and contains no hardcoded test-file references; deleting `test-millpy-implement-holistic.py` (per card 9) leaves nothing to update. The reviewer's prescribed cross-check would land empty-handed and pollute the implementer's edit list. No fix.
