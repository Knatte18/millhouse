# Plan fix: round 1

```yaml
slug: mill-go2-fork-fixer
round: 1
source: _mill/reviews/20260811-141424-plan-review-r1.md
```

## Fixed

- `20260811-141424-plan-review-r1.md` — "[NIT:decision] Fork override scoped to all fixer dispatch,
  not just NIT-fix": added Shared Decision `fork-covers-all-fixer-dispatch` to `plan/00-overview.md`
  recording that the override is deliberately role-scoped across all four fixer dispatch sites, and
  added a matching rationale bullet to batch 2 card 4 forbidding a `--nits-only` gate or per-site
  enumeration. Took the reviewer's first offered fix (record the decision), not the second (narrow to
  the NIT-only pass), because narrowing contradicts discussion Decision
  `fork-all-four-fixer-dispatch-sites`.

## Pushed Back

(none)
