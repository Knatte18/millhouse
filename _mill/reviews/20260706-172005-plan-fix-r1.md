# Plan fix report — round 1

Source review: `_mill/reviews/20260706-171919-plan-review-r1.md` (verdict: REQUEST_CHANGES, 1 BLOCKING)

## Fixed

- `_mill/reviews/20260706-171919-plan-review-r1.md` — "[BLOCKING] Cards 2-4 unescape tests are structurally vacuous": rewrote Cards 2, 3, and 4's Requirements in `_mill/plan/01-html-unescape-agent-output.md` to require the unescape check be the test function's return value (a boolean comparison against the mock's captured `call_args`), not a bare `assert` inside the model function's `try` block — the model's `except Exception: return True` outer handler swallows any `AssertionError` other than the literal "prepare() must not be called" one, which would have made the prescribed tests unable to fail regardless of whether `html.unescape` was actually applied.

## Pushed Back

(none)
