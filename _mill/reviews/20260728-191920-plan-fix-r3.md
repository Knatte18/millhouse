## Fixed

- `20260728-191806-plan-review-r3.md` — "[BLOCKING] Batch 4's new integration-test scenarios are never auto-run": verified — batch 4's `verify:` (both `04-dirty-parent-worktree-preflight.md` frontmatter and the overview's batch-index entry) was `null`, while batches 1-2 already prove direct-script `verify:` invocation is an accepted shape for non-`run-all.py` test files. Set batch 4's `verify:` to `PYTHONPATH= uv run --project plugins/mill python plugins/mill/integration_tests/test-merge.py` in both places, updated the batch's own "Batch Tests" section, and narrowed the overview's "Documentation-only batches skip verify:" Shared Decision to apply to batch 3 only.
- `20260728-191806-plan-review-r3.md` — "[NIT] Card 16's new `_setup_trio()` calls risk shadowing the primary scenario's locals": added explicit distinct variable-name requirements (`dirty_*`, `retry_*`, `untracked_*` prefixes, matching the file's own `nested_hub`/`verify_hub_root` convention) to Card 16, and updated the rest of its Requirements text to use those names instead of the ambiguous shared `hub`/`worktree`/`child_branch`.
- `20260728-191806-plan-review-r3.md` — "[NIT] Card 12 doesn't call for renumbering the brief's subsequent steps": added an explicit instruction to Card 12 to renumber the existing steps 4-6 to 5-7 after inserting the new step 4.

## Pushed Back

(none — all findings verified accurate against source and fixed)
