```yaml
round: 5
source_review: 20260923-104840-discussion-review-r5.md
```

## Fixed

- `20260923-104840-discussion-review-r5.md`: "[NIT:consistency] Moves-target disposition contradicts
  itself across 3 passages" — Scope's `In` bullet, the `resolution-scope-rework` Decision's set
  definition, and its Rationale all said "`Moves:`-source" only while Technical context pointed the
  plan-writer at `_card_own_reference_set`, which adds both halves of a `Moves:` pair. Resolved by
  standardizing all three passages on `_card_own_reference_set`'s existing (both-halves) behavior — a
  not-yet-existing `Moves:`-target token can never resolve to an existing file under the narrowed
  resolver anyway, so including it is behaviorally a no-op and excluding it would be unneeded extra
  filtering logic.

## Pushed Back

(none)
