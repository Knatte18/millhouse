# Discussion-fix report — round 2

```yaml
slug: wiki-v3-verify-isolation
source_review: _mill/reviews/20260525-104834-discussion-review-r2.md
date: 2026-05-25
```

## Fixed

- From `20260525-104834-discussion-review-r2.md`: **"`_read_batch_frontmatter` guidance misleads implementer"** — dropped the misleading "look for `_read_batch_frontmatter` import" clause in Technical context → Validator changes. Rewrote to direct the implementer to the in-module inline-YAML extraction pattern already in `_plan_validate.py` at `_check_depends_on_batch_mismatch` (lines 534-549).
- From `20260525-104834-discussion-review-r2.md`: **"Coverage scenario '(top-level + batch)' is ambiguous"** — reworded the Testing → Coverage scenarios bullet from "Two unprefixed verify strings (top-level + batch) in one plan" to "Two batch files both with unprefixed `verify:` in one plan", matching the actual validator scope per the `validator-check-shape` decision (per-batch frontmatter only; overview top-level and batch-index mirror are excluded).

## Pushed Back

_(none)_
