# Plan fixer report — round 2

```yaml
slug: mill-spawn-and-workflow-integrity
round: 2
verdict_addressed: APPROVE (3 NIT)
source_review: _mill/reviews/20260625-071822-plan-review-r2.md
```

## Fixed

- `20260625-071822-plan-review-r2.md` — [NIT] "Card 9 miscounts `_run_verify_gate` call sites": corrected the enumeration to four sites, all inside `_forward_output` (~567/~691/~746/~802), and noted `finalize_from_output` delegates to `_forward_output` rather than calling the gate directly.
- `20260625-071822-plan-review-r2.md` — [NIT] "Card 16 may need no code change beyond the test": added a note that the conflicts-mode success path already prints the full parsed dict verbatim (`print(json.dumps(parsed))` ~`_implementer_common.py:632`), so `discarded` likely survives without threading; the regression test is the load-bearing deliverable and the implementer should not invent a needless reshape.
- `20260625-071822-plan-review-r2.md` — [NIT] "Card 4 rollback does not restore multi-mode groomed sources": added an explicit out-of-scope note to Card 4 — multi-mode source restoration is intentionally not attempted; `wiki.set_phase(slug, None)` clears the merged slug and the Card 7 reconcile backstop converges any residual orphaned-active state.

## Pushed Back

(none)
