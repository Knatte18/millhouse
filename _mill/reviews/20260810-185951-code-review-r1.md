MILL_REVIEW_BEGIN
# Review: _review_common/_review_plan: verdict/count consistency and path-suppression gaps — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-10
```

## Findings

No findings.

All three batches were verified end-to-end against source:

- Batch 1 (`review-plan-context-soft-fail`): all four call sites in `_review_plan.py`
  (`_review_one_batch`, `prepare()` per-batch, `prepare()` holistic, `run()` holistic) correctly
  split `Context:` refs from `Edits:`/`Creates:`/`Deletes:` refs, resolve them separately with
  `soft_fail_gitignored=True` only on the Context: group, and concatenate into `reads`/`all_reads`.
  `raw_refs`/`raw_refs_set`/`all_raw_refs` downstream membership checks are preserved correctly at
  every site (no `NameError` regressions). `test-review-plan-flow.py` Tests 40-43 cover per-batch
  soft-fail, holistic soft-fail, the non-ignored-still-hard-fails regression (both branches), and
  the `#808` Edits:-only-still-hard-fails design-boundary case exactly as Card 5 specifies; Tests 9
  and 17 (`#790` resume-mode guards) are present and unchanged.

- Batch 2 (`finalize-verdict-rewrite`): `rewrite_verdict_token` in `_review_common.py` correctly
  reuses `apply_actual_model_override`'s header-fence-scan strategy and rewrites both the fenced
  `verdict:` field and the `## Verdict` section's token line. `finalize_scope` is restructured
  exactly per Card 7's target shape — `demoted_any` (not `blocking_classes is not None`) gates the
  rewrite, combined with `verdict != original_verdict`. `test-review-class-taxonomy.py` adds all
  four required tests (ceiling-flip rewrite, no-op when no demotion, no-op when mismatched without
  demotion, plan/code type coverage), matching the Requirements' exact fixture and assertion shapes.

- Batch 3 (`review-loop-min-rounds`): `min_rounds: 1` is added at all four required sites in both
  `templates/mill-config.yaml` and hub `mill-config.yaml`, correctly omitted under
  `plan-review.batch` and correctly present under `code-review.batch` (mill-go's per-batch loop is
  a real termination site, unlike mill-plan's). All three `SKILL.md` files wire the convergence
  gate into every specified termination branch (mill-start 4a/4b + auto-mode; mill-plan 4a/4b/4c
  with the `current_round_findings` round-filtered exception verbatim at its site; mill-go per-batch
  and holistic `APPROVE` branches), with the implicit-approve-at-cap commit-message note applied
  consistently everywhere.

Cross-batch: no duplicated helpers, no orphaned config keys, no interface mismatches between the
three independently-developed batches (all `depends-on: []`). `finalize_scope`'s new
`demoted`-flag/verdict-rewrite plumbing (batch 2) is exactly what batch 3's convergence gate reads
via `envelope["findings"][*]["demoted"]`, confirming the `no-backend-change-for-798` Shared Decision
holds.

## Verdict

APPROVE
All three batches match their plan cards precisely; test coverage and cross-batch integration are sound.
MILL_REVIEW_END
