# Plan-fix report — round 2

```yaml
source_review: _mill/reviews/20260709-133217-plan-review-r2.md
round: 2
```

## Fixed

- **[NIT] Card 9 leaves step-3 summary (line 125) contradicting new 4(b)** — verified SKILL.md line 125 currently reads "implementer notifications go through the existing clean-mid-work-stop / `incomplete` routing; reviewer and fixer notifications are checked with a one-shot liveness probe" — accurate: this would go stale once card 9's Edit B/C land, since it implies only reviewer/fixer get probed. Added a new "Edit A" to batch 2 / card 9 (renumbering the prior Edit A/B to Edit B/C) with the exact before/after text for line 125, matching the batch's precision-editing contract (every edit site gets exact quoted before/after text, not a general re-read instruction). Note: this fix was initially applied as a live, uncommitted edit directly to the real `plugins/mill/skills/mill-go/SKILL.md` by mistake — caught and reverted (`git checkout --`) before committing, since mill-plan writes plan files only; the actual source edit happens later, during mill-go's Execute phase.

## Pushed Back

None.
