# Discussion fix r1

Source review: `_mill/reviews/20260517-130942-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 2 NOTE).

## Fixed

- `[NOTE] sys.platform vs os.name idiom inconsistency (#305)` -- replaced `sys.platform != "win32"` with `os.name != "nt"` in both the Scope bullet and the Decision body so the new guards match the existing skip idiom at `test-vscode-processes.py:273`.
- `[NOTE] resolve_deps_as_names API description slightly off (#303)` -- clarified the Decision body and Technical context: the overview-side normalisation uses `resolve_deps_as_names(batches)` directly; the per-batch-file side uses an inline `number_to_name` lookup (the function's input is a full batch-dict list, not a raw deps list).

## Pushed Back

_(none)_
