# Review: (A) — Small infra fixes batch 7 — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmedium
reviewed_file: plan/ + source
date: 2026-05-13
```

## Findings

All three batches verified against plan. No blocking issues found.

Brief spot-checks that passed:

- **Batch 1 (`_wiki.py`):** `WikiHealthError.__init__` signature, `health_check` check order (dir before config), both `exists()` checks correct. Module docstring and Exceptions block updated in the right positions. Tests (g)/(h)/(i) use `ok()`/`fail()` helpers, not bare prints, and assert `whe.wiki_path == nonexistent` in (i).

- **Batch 1 (SKILL.md):** Execute loop inserts `### 0.` heading before `### 1. Implement`. Holistic loop inserts plain `0.` entry before `1. **Crash-recovery.**`. Lock-release precedes `exit 1` in both inline scripts. Explanatory prose is present in both locations.

- **Batch 2 (`_junction.py`):** `_is_junction_or_symlink` placed before `create()`; `remove()` refactored to call it without changing any observable output or error message. `points_to` wraps both `resolve()` calls in a single `try/except OSError`. Module docstring updated.

- **Batch 2 (`_setup.py`):** `import os` inserted correctly (alphabetical: `os`, `re`, `sys`). `os.path.lexists` guard fires before `points_to`; silent `continue` on correct target; drift path removes then creates and appends; `ValueError` propagates unchanged on real directory.

- **Batch 3 (`_status.py`):** Deletion block placed after phase-rewrite `break`, before timeline recalculation. `y_end` is stale post-deletion but never used again (timeline code rebuilds from `rewritten`). Docstring updated with fourth sentence.

- **Pre-existing fixes (Card 8):** `mill-finalize/SKILL.md` present in `test-no-wiki-cwd.py` allowlist; `cwd=wt` present in `test-millpy-spawn.py`'s `discover_active_worktrees` call; arrow style (`->`) consistent in `test-mill-finalize-dispatch.py`.

- **Test style consistency:** `test-status.py` new blocks use raw `assert` + `print("PASS: …")` inside the single top-level `try:`, matching Tests 1–3 style. `test-setup-hub-links.py` new tests follow the `raise AssertionError(…)` pattern and are registered in the correct position in `tests = […]`.

## Verdict

APPROVE — implementation is complete, correct, and consistent across all three batches.