# Discussion fix — round 3

```yaml
task: task-deps-and-isolation
round: 3
date: 2026-05-30
```

## Fixed

- [round-3 NOTE] (`20260530-152812-discussion-review-r3.md` — "Gotcha
  contradicts Migration decision on approach") Reworded the Technical context
  Gotcha to use `tinydb.operations.delete("group")` in place and dropped the
  stale "rewrite records wholesale" wording, so it matches the authoritative
  `### migration` decision.
- [round-2 NOTE] (`20260530-151709-discussion-review-r2.md` — "Batch validation
  semantics for intra-batch deps unspecified") Added a decision: `upsert_tasks_batch`
  validates against the projected post-batch state (current store unioned with
  incoming records), so intra-batch deps resolve and batch-internal cycles are
  caught; added a batch-projection test case. Round-3 review classed this as
  optional polish; fixed anyway per default-fix rule.
- [round-2 NOTE] (`20260530-151709-discussion-review-r2.md` — "'Effective
  depends_on' ambiguous in display") Clarified that the `Depends on:` line shows
  the RAW declared dependency list (not the done-filtered `effective_deps` used
  for layer math); added the all-deps-done test case. Round-3 review classed
  this as optional polish; fixed anyway per default-fix rule.

## Pushed Back

(none — all findings accurate and fixed)
