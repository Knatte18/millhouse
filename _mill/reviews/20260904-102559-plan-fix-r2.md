# Plan-fix report: round 2

## Fixed

- `20260904-102436-plan-review-r2.md` — "[BLOCKING:design] Step 5.5 unrestricted porcelain check can fail with nothing staged": Card 3's Requirements now gate the commit on `git diff --cached --name-only` (staged-only) instead of unscoped `git status --porcelain`, with an explanatory sentence on why staged-only avoids both sweeping foreign dirt and the empty-commit failure mode.
- `20260904-102436-plan-review-r2.md` — "[NIT:consistency] Imported millpy-bg halt-guard contradicts Step 3.5's \"never blocks or fails the merge\" contract" (demoted from BLOCKING): Card 4's Requirements now scope the imported cwd-guard's halt-on-mismatch outcome to this call site only, treating a cwd rejection like the existing "dead" branch (log and continue) rather than a skill-level halt, reconciling it with Step 3.5's preserved fail-safe contract.
- `20260904-102436-plan-review-r2.md` — "[NIT:consistency] Grep exclusion syntax left as prose instead of a literal command": Card 5's Requirements now give both scan parts as literal fenced `git grep` bash blocks with pathspec-magic exclusions, matching mill-finalize/SKILL.md's established bash-block convention.

## Pushed Back

(none)
