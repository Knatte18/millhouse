# Plan fix report — round 1

Source review: `_mill/reviews/20260904-093459-plan-review-r1.md`

## Fixed

- `[BLOCKING:scope]` "Card 1 names `resolve_existing_paths` from a file absent from its own Context:/Edits:" — reframed the citation in card 1 step 3 to explicitly state the precedence order is fully restated inline and no read of `_review_common.py` is required, mirroring how card 4 already handles its own discussion-notes citation. Chose this over adding `_review_common.py` to card 1's `Context:`, since that would push the batch's context-token estimate from ~113K to ~144K, over the 120000 cap (`batch-oversized`).
- `[BLOCKING:design]` "No encoding/error-handling policy for the new arbitrary-source-tree read" — card 1 step 3 now specifies `path.read_text(encoding="utf-8", errors="replace")` inside a `try`/`except OSError` block, skipping an unreadable file rather than crashing the whole validator run.
- `[NIT:design]` "Trailing-segment rule can return a non-qualifying search key" — card 1 step 2 now requires the RETURNED segment itself (the trailing segment for a 2-segment token, or the whole token for a 1-segment token) to qualify, not "at least one segment" generally. Added a new test in card 3 (`test_check_context_completeness_symbol_dotted_qualifying_prefix_nonqualifying_trailing`) exercising the exact case the finding described.
- `[NIT:scope]` "C# angle-bracket generics unhandled by the call/generic-suffix stripper" — card 1 step 2(b) now also strips a trailing `<...>` balanced-bracket group, covering C#-style `GetItems<T>()` call sites.

## Pushed Back

(none)
