# Plan fix — round 3

```yaml
slug: mill-go2-scaffold
round: 3
review: 20260811-122517-plan-review-r3.md
```

## Fixed

- `[BLOCKING:design]` "Card 7's description-recovery instruction uses an unanchored `HEAD~N`" — card 7 in `02-thin-variants.md` now recovers the pre-move `description:` from the literal commit `6442a688` (the anchor the rest of the plan already cites for verified counts), with a `git log --follow` fallback for the rebased case and an explicit ban on retyping the value from memory.
- `[NIT:consistency]` "Card 6 misattributes its function-shape convention to the wrong file" — card 6's shape reference changed from `test-guards.py` (whose check functions return `int` and print inline) to `plugins/mill/unit_tests/test-skill-helper-drift.py`, which is the file that actually uses the one-function-per-check-returning-failure-strings shape. Card 6's `Context:` was swapped to match, and the `main()` description now matches that file's stderr-plus-summary-line behaviour.

## Pushed Back

_None._
