# Plan-fix round 3

## Fixed

- `20260729-075336-plan-review-r3.md`: "[BLOCKING] Partial-restore failure in `check_and_restore` is silently reported as full success" — Batch 1 Card 1 steps 8-10 rewritten: an unconditional `still_missing = set(deleted_paths) - set(restored_paths)` diagnostic print fires whenever anything is still missing, regardless of whether `restored_paths` is also non-empty (a partial restore is no longer silently upgraded to indistinguishable-from-full-success). Added Card 2 scenario 10, a mocked partial-restore regression test asserting `triggered: True`, `restored_paths` containing only what was actually restored, and a stderr diagnostic naming the still-missing path.
- `20260729-075336-plan-review-r3.md`: "[BLOCKING] Card 8's step 3.6 bracketing references a checkpoint anchor that does not exist" — removed the orphaned third insertion instruction (a leftover duplicate from an earlier edit, referencing a `"If dispatch == subprocess or psmux:"` line that doesn't exist inside step 3.6). Step 3.6's own pre/post checkpoint pair around "Re-run sub-step 3" is now stated as the complete bracketing for that call site, with an explicit note that no third checkpoint is needed.

## Pushed Back

(none — both round 3 findings were fixed as described above)
