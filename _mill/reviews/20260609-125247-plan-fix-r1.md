# Plan Fix Report — round 1

Source review: `_mill/reviews/20260609-125237-plan-review-r1.md`

## Fixed

(none)

## Pushed Back

- `20260609-125237-plan-review-r1.md` / "[NIT] Verify runs file directly, not via run-all.py": Conflicts with documented design. mill-plan SKILL.md `## Phase: Plan` "Verify command scope" explicitly documents "Single test file: `verify: PYTHONPATH= uv run --project plugins/mill python plugins/mill/unit_tests/test-fold.py`" as the correct pattern for a single test file — direct invocation is the spec, not a deviation from it.
- `20260609-125237-plan-review-r1.md` / "[NIT] hub_root-substring assertion is slightly brittle": Reviewer's own finding text resolves the concern: "the two leaf names differ so the check is sound. No change required." Fixture uses `"my-slug"` vs `"millhouse"` — no substring overlap possible.
