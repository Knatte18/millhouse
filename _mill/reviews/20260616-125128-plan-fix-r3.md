# Plan fixer report — round 3

Source review: `_mill/reviews/20260616-125034-plan-review-r3.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 2 NIT).

## Fixed

- [BLOCKING] "Card 7 threading patch target is wrong binding" — VERIFIED accurate: `test-millpy-implement.py` loads the CLI via importlib as module object `millpy_implement` (line 28-30) and `millpy-implement.py` does `from _implementer_common import ... finalize_from_output` (line 36), so the CLI holds its own binding. Patching `_implementer_common.finalize_from_output` would not intercept the call. Rewrote Card 7 to patch `millpy_implement.finalize_from_output` via the file's existing `_p(millpy_implement, "<attr>", ...)` convention, with an explicit CRITICAL note on why.
- [NIT] "Card 5 gate-on-parsed-emit could re-run verify on a parsed stuck report" — VERIFIED accurate: the parsed branch (`_implementer_common.py:240-253`) prints the parsed dict for both `success` and `stuck`. Tightened Card 5 to require the parsed-emit gate call be guarded by `parsed.get("status") == "success"`, so a self-reported `stuck` never triggers a verify re-run.
- [NIT] "Card 3 should make the no-warning assertion explicit" — VERIFIED accurate (the prose said "emits NO warning" but did not tie it to a stderr assertion). Tightened Card 3 case (a) to assert, against captured stderr, that the divergence-warning string is ABSENT (the load-bearing zero-heading suppression for #489).

## Pushed Back

- (none)
