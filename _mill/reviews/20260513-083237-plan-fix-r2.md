# Plan-fix report r2

```yaml
slug: subprocess-fixes
round: 2
source_review: _mill/reviews/20260513-083050-plan-review-r2.md
```

## Fixed

- `20260513-083050-plan-review-r2.md` — `[NIT] Batch 1 scope prose contradicts verify: field`: rewrote the closing sentence of Batch 1's Batch Scope paragraph in `01-subprocess-util-windows-fixes.md`. The "local-dev only and not in verify:" clause is replaced by "chained into batch 1's verify: and no-ops on POSIX via the skip-guard".

- `20260513-083050-plan-review-r2.md` — `[NIT] Card 3 missing explicit red-state instruction`: added a `**TDD order:**` sentence at the end of Card 3's Requirements paragraph in `01-subprocess-util-windows-fixes.md`. The instruction names test (k)'s new argv assertion as the failing-first marker and instructs the implementer to record the red state before editing `_subprocess_util.py`.

- `20260513-083050-plan-review-r2.md` — `[NIT] Card 10 context missing _implementer_common.py`: added `plugins/mill/scripts/_implementer_common.py` to Card 10's `Context:` block in `03-session-id-propagation.md`. The card edits the unit test and calls `_forward_output`, so the helper file is a legitimate read-only allowlist entry.

- `20260513-083050-plan-review-r2.md` — `[NIT] WORKER ERROR sentinel from discussion scope absent in plan`: extended Card 6 in `02-millpy-bg-start-sentinel.md` to implement the WORKER ERROR sentinel as a paired (b) requirement alongside the START sentinel. Renamed card title to "Worker START + ERROR sentinels in millpy-bg.py". Updated the batch's Batch Scope paragraph to mention both sentinels. The implementation spec wraps the worker fast-path body in an outer `try/except Exception`, writes `[mill-bg] WORKER ERROR <repr>` (the token literal from `discussion.md`) and exits 1 on exception, with a defensive nested-try fallback to stderr if the log-file write itself fails. Discussion.md's in-scope spec is now reflected.

## Pushed Back

_(none — all four NITs accepted under the receiving-review decision tree; no harm category applied.)_
