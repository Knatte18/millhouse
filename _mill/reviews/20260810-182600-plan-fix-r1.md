## Fixed

- `20260810-181747-plan-review-r1.md` — "Card 8's code-stage verdict-rewrite sub-case has no excluded class to test": Card 8's code-type sub-case now builds an explicit custom `blocking_classes` frozenset excluding a class (e.g. `frozenset(RECOGNIZED_CLASSES) - {"consistency"}`) instead of `resolve_blocking_classes({}, "code", "holistic")`, since code review's real default ceiling includes all four `RECOGNIZED_CLASSES` (confirmed by the existing, unchanged `test_ceiling_table_code`), so no ceiling-flip fixture was constructible from the default as originally specified.
- `20260810-181747-plan-review-r1.md` — "Card 7's rewrite guard doesn't actually scope to \"ceiling changed the verdict\"": Card 7's gate is now `demoted_any and verdict != original_verdict` (a new local `demoted_any = any(f.demoted for f in findings)` computed right after `apply_blocking_ceiling`), replacing the `blocking_classes is not None` guard, which never actually excludes anything in production since `resolve_blocking_classes` never returns `None`. Card 8 gained a new test, `test_verdict_token_unchanged_when_mismatched_without_demotion`, exercising the exact gap the finding named — a reviewer-stated `verdict: APPROVE` alongside a genuinely-BLOCKING (non-demoted) finding must recompute `REQUEST_CHANGES` in the returned envelope but leave the persisted file's verdict lines untouched.
- `20260810-181747-plan-review-r1.md` — "Demoted-predicate convergence gate ignores plan-review's approved-batch carryforward": the Batch Scope now documents an explicit exception for mill-plan's site (Card 12): `_review_plan.py`'s `_scan_approved_batches` splices stale `demoted: true` markers from already-approved carried-forward batches into the same top-level `envelope["findings"]` every round, so Card 12 now computes `converged` from a round-filtered `current_round_findings` (`envelope["reviews"]` entries whose `round` equals `envelope["round"]`) instead of the raw `envelope["findings"]` the other two sites (mill-start, mill-go) safely use directly.

## Pushed Back

(none — all three BLOCKING findings were factually accurate and fixable within this batch's existing scope; no harm-check objection applied to any of them)
