# Plan fix report — round 1

Source review: `20260606-185649-plan-review-r1.md` (holistic, REQUEST_CHANGES, 3 BLOCKING + 1 NIT).

## Fixed

- [BLOCKING] "Card 2 invalidates existing is_bg_worker_alive mocks" — verified accurate: `test_check_bg_status_running` (L205), `test_check_bg_status_dead_no_exit` (L221), `test_check_bg_status_race_guard_exit_appears` (L246) all `patch.object(_bg, "is_bg_worker_alive", ...)`, which Card 2's switch to `_probe_liveness` renders inert. Added an explicit "Mock-target migration (required)" directive to Card 4 naming the three tests and the repointing to `patch.object(_bg.os, "kill", ...)` + `os.utime`, with outcomes preserved.
- [BLOCKING] "Card 4 Context missing _bg.py" — added `plugins/mill/scripts/_bg.py` to Card 4 `Context:` (Card 4 references `_bg._STALE_LOG_SECONDS` / `check_bg_status` / `_bg.os.kill` but does not edit `_bg.py`).
- [BLOCKING] "Card 5 Context missing _bg.py" — added `plugins/mill/scripts/_bg.py` to Card 5 `Context:` (Card 5 references `_bg._has_valid_json_result` and has no `Edits:`).
- [NIT] "_probe_liveness exit-code extraction is ambiguous" — added a sentence to Card 2 Requirements: `_probe_liveness` returns the `pid` (not the exit code) in the `"exit"` case; `check_bg_status` extracts the numeric code via `_EXIT_CODE_RE` from the already-read text before returning `("exit", code)`.

## Pushed Back

- none
