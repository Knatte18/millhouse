# Plan-fix round 1

## Fixed

- `20260919-134704-plan-review-r1.md` — "[BLOCKING:design] Card 6 omits Step 3.5 retry from revise_from_blocked --max-rounds fix": extended card 6's Requirements so the `revise_from_blocked` `--max-rounds <round>` threading applies at both step 2's dispatch and step 3.5's ERROR-only-aggregate retry dispatch (Agent-mode `<args>` and subprocess `millpy-review-plan.py` invocation, at both sites), matching card 5's equivalent Step 3.5 coverage for the sibling Entry-blocked-reentry flow.

## Pushed Back

(none)
