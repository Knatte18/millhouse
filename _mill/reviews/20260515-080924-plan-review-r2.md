# Review: 56 (A) — Fix mill-go/start/plan/merge runtime behavioral bugs — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-15
```

## Findings

### [NIT] Card 9 "covers: Handoff" claim conflicts with condensed-form scope
**Location:** Batch 4 / Card 9
**Issue:** Requirements say the safeguard "covers: the `discussed` Handoff append (Phase: Handoff)", but the condensed placement puts the note at the top of `### Phase: Discussion Review` — a sibling SKILL.md section, not the parent of `### Phase: Handoff`. An implementer following the condensed form literally leaves the Handoff append unguarded.
**Fix:** Replace "covers: the `discussed` Handoff append" with a clarifying note that protection is transitive — the guard within the loop ensures `_mill/status.md` is restored before Handoff runs, so no additional guard is needed in the Handoff section itself.

### [NIT] Card 2 Case 3b print message mentions "inferred success" but test label should reflect the new outcome
**Location:** Batch 1 / Card 2
**Issue:** The plan says to update the print message to `"PASS: pre-existing dirt in snapshot, no new dirt -> stuck/logic (inferred-success requires clean tree)"`. The phrase "inferred-success requires clean tree" correctly explains the new behavior, but the test setup comment (`# Case 3b: snapshot has pre-existing dirt; no new dirt added -> inference succeeds`) in the existing test file describes the OLD expected outcome; not updating it leaves a misleading docstring.
**Fix:** Update the Case 3b comment to `# Case 3b: pre-existing dirt survives in full-tree check -> stuck/logic` to match the new assertion.

## Verdict

APPROVE
Plan correctly identifies and fixes all 11 bugs with sound card-level specifications.