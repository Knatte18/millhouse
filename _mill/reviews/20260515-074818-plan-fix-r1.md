# Plan Fix Report — round 1

```yaml
source_review: 20260515-074655-plan-review-r1.md
date: 2026-05-15T07:48:18Z
```

## Fixed

- [NIT] "worktree_root undefined before step 4.5 in mill-go": updated Card 2 requirement 1 to instruct the implementer to check if `worktree_root` and `cfg` are already set by prior entry steps and only assign them if missing.
- [NIT] "Step 4.5 re-loads config already loaded in step 3": same Card 2 fix — requirement now says reuse existing `cfg` if already loaded; add only the path-derivation lines.
- [NIT] "Board Discipline references undefined variables in mill-merge": updated Card 4 step 7 to reference only `task_dir` (the variable mill-merge actually defines) and use descriptive prose for the other paths rather than undefined variable names.
- [NIT] "plan_dir trailing slash changes value written to status.md": updated Card 6 step 3 to call `.rstrip('/')` on `cfg['paths']['plan_dir']` before passing to `_status.update_field`.
- [NIT] "Card 8 required_keys omits junctions": added `'junctions'` to `required_keys` list in Card 8.

## Pushed Back

(none)
