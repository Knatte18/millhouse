# Plan fixer report — round 1

```yaml
slug: mill-skill-and-tooling-gaps
round: 1
source_review: _mill/reviews/20260630-044456-plan-review-r1.md
verdict_addressed: APPROVE (2 NITs)
```

## Fixed

- r1 "[NIT] get_task() key set not grounded in Context" — added
  `plugins/mill/scripts/wiki/_store.py` to Card 1's Context (it assembles the task document
  with the `brief`/`body` defaults and validates `depends_on`/`isolated`/`deferred`), and noted
  in Card 1's Requirements that the documented key set is grounded there, so the implementer can
  verify the field list from provided context.
- r1 "[NIT] Test module docstring left stale after third check group" — extended Card 3's
  Requirements to also refresh the top-of-file module docstring with a line for the new
  extract-unit checks and the mill-start `body`/`brief` regression lock, keeping it in sync with
  `main()`'s check groups.

## Pushed Back

(none — both NITs were factually accurate and harmless to fix)
