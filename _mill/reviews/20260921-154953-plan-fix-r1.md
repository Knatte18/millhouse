# Plan-fix report — round 1

Source review: `_mill/reviews/20260921-154803-plan-review-r1.md`

## Fixed

- "[BLOCKING:design] `_compute_declared_symbols_union` mis-picks parens over braces for a struct
  token" — Card 3's helper pseudocode checked the `(...)` span before `{...}`, so a struct-literal
  token whose field type itself contains `func()` (the card's own worked example) picked the empty
  `()` span and produced an empty `inner`, never adding `Acquire`/`error`. Changed the
  delimiter-selection logic to collect both candidate spans (when present) and pick whichever pair's
  outermost span is wider, so the `{...}` span wins for the struct-literal example. Updated the
  helper's docstring to explain the wider-span tie-break with the same worked example.
- "[NIT:consistency] `requirements-quote-indent-drift` SKILL.md row left self-contradictory" — Card 8's
  instructions now explicitly reword the row's closing "In both cases the goal is identical..."
  sentence to "In the first two cases..." (scoping it to the strip/add remedies only) before appending
  the new third-message-shape sentence, instead of silently leaving a now-false universal claim in
  place.
- "[NIT:consistency] Batch Scope's edit-overlap audit omits Cards 4/6 sharing `_resolve_symbol_files`"
  — extended Batch 1's `## Batch Scope` paragraph to also name Cards 4 and 6 as a second non-overlapping
  pair inside `_resolve_symbol_files`, matching the rigor already applied to cards 1/3.

## Pushed Back

(none — all three findings were accurate and fixed as described)
