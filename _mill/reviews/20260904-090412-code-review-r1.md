MILL_REVIEW_BEGIN
# Review: mill-go: done-gate halt path and cleanliness-gate recovery are under-documented — holistic

```yaml
duration_s: 191.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-04
```

Verified all six batches against `00-overview.md`'s Shared Decisions and each batch file's card requirements, cross-checked against the full source: `_done_gate.py`/`test-done-gate.py` (batch 1), `handoff.md` (batches 2, 3), `SKILL.md` (batches 3, 4), `mill-pause/SKILL.md` (batch 5), `_cleanliness.py`/`test-cleanliness.py` (batch 6), and `_parent_branch.py` (context-only, pre-existing #817 work, unchanged).

- `run_gate()` mirrors `run_preflight`'s never-raise contract, tail-truncation, and Windows-dotnet-cleanup exactly per Card 1; `test-done-gate.py` cases 6-9 cover no-dotnet/Windows/Linux/failure-truncation paths matching Card 2's mocking requirements verbatim.
- `handoff.md`'s "0. Pre-done gate" correctly calls `_done_gate.run_gate`, deletes the superseded sentences, and the fixer-dispatch control flow (Card 4) correctly branches on agent-file existence, re-runs the gate once, and duplicates the "Do NOT set phase: done" sentence at both new halt branches — Card 3's single-occurrence deletion and Card 4's two-copy re-insertion both land correctly.
- All five Card 5 notify+lock-release insertion points (nit-gate ×1, terminal-dirt ×3, scope-violations ×1) are present with correct event names and no unintended prose changes elsewhere.
- `SKILL.md` step 2b (Card 6) and `handoff.md`'s terminal cleanliness gate (Card 7) both correctly wire `check_liveness`/`resolve_dead_parent` with matching signatures, correctly differentiated batch-scoped vs. task-scoped commit messages/halt mechanisms (`set_batch_field`+*Blocked* vs. fresh notify/lock-release pairs), and both fall through correctly to the pre-existing unresolvable-diff halt on a still-`None` retry.
- Step 5.5's warm-resume liveness probe (Card 8) matches step 3(b)/(c)'s existing probe contract exactly, including the unbounded-wait branch and the "probe never fires on clean completion" no-op case.
- `mill-pause/SKILL.md`'s new third bullet (Card 9) is present, unambiguous, and the two existing bullets are untouched.
- `_cleanliness.py`'s `.pyc`/`__pycache__` allowlist clauses (Card 10) and docstring update, plus `test-cleanliness.py`'s CESV-11/12/13 (Card 11), match the plan's exact test scenarios including the non-pycache `.py` regression guard.
- No out-of-plan files present; `All Files Touched` in `00-overview.md` matches exactly the files edited across all six batches; `_parent_branch.py` is correctly Context-only and unmodified.
- Cross-batch contracts hold: batch 2 consumes batch 1's `run_gate` signature correctly; batch 3's Card 7 halt builds on batch 2's Card 5 lock-release/notify pair as directed rather than duplicating it; batch 4's SKILL.md edit and batch 3's SKILL.md edit target disjoint, non-overlapping sections per the declared `depends-on` edge.

## Verdict

APPROVE
Implementation matches the plan precisely across all six batches; no BLOCKING or NIT findings.
MILL_REVIEW_END
