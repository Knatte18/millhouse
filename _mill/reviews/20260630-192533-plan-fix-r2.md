# Plan-fix round 2

Source review: `_mill/reviews/20260630-192527-plan-review-r2.md` (verdict: REQUEST_CHANGES, blocking_count: 1, nit_count: 1)

## Fixed

- `_mill/reviews/20260630-192527-plan-review-r2.md` — "[BLOCKING] Card 4's new test-marker.py functions are never executed": confirmed `test-marker.py:241-258` manually enumerates tests in a `tests = [...]` list (no unittest auto-discovery). Added an explicit Card 4 requirement bullet instructing the implementer to append the three new function names to that list, after the existing `test_task_data_happy_path` entry.

## Pushed Back

- `_mill/reviews/20260630-192527-plan-review-r2.md` — "[NIT] Other unguarded `_marker.slug_from_branch`/`task_data` call sites remain" (millpy-abandon.py, millpy-color.py, millpy-cleanup.py, millpy-merge-in-subagent.py, `_review_common.load_task_title`): pushed back, no plan change. The reviewer's own Fix note frames this as optional and explicitly accepts the existing scoping. `_mill/discussion.md`'s "WikiStartupError retry lives in `_marker.slug_from_branch()`" Decision (and its Scope > In/Out bullets) deliberately scoped the *clean-error-message* half of the fix (Card 3's `except WikiStartupError` clause) to the two call sites GitHub issue #579 actually reported — `millpy-implement.py` and, per plan-review round 1's NIT, `millpy-fix.py`. The *retry* half (Card 2's `_list_tasks_brief_with_retry()` helper) already transparently covers every caller listed in this NIT, including the five named scripts and `_review_common.load_task_title` — only the rare exhausted-retry raw-traceback case remains unguarded for them, which is the documented, deliberate scope boundary, not an oversight. Extending the except-clause pattern to five additional unrelated CLIs is a real scope expansion beyond #579 and beyond this task's approved discussion; out of scope per `mill-receiving-review`'s "conflicts with documented design decision" pushback category.
