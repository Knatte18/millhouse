# Discussion fix report — round 5

Source review: `_mill/reviews/20260904-082503-discussion-review-r5.md`

## Fixed

- `[NIT:consistency]` (demoted from BLOCKING) "'looks like code' signal definition excludes its own motivating examples" — corrected the Symbol-candidate shape signal from "an uppercase letter after position 0" (which wrongly excluded single-capitalized words like `New`/`None`) to "the segment is not entirely lowercase" (uppercase anywhere, including position 0, or an underscore). Updated the Symbol-candidate shape Decision/Rationale and the Resolvability-gate Rationale's `None`/`True`/`False` example to match, and added a Testing bullet confirming a single-capitalized bare word (`New`) is now correctly a candidate.

## Pushed Back

(none)
