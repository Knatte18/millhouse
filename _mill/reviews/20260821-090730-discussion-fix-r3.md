# Discussion Fix — round 3

Source review: `_mill/reviews/20260821-090659-discussion-review-r3.md` (verdict: APPROVE, 1 NIT, demoted from BLOCKING).

## Fixed

- `[NIT:consistency]` baseline-dispatch-background ignores 0.6's cache-path exception — added an explicit "Cache-path exception preserved" bullet to the `baseline-dispatch-background` Decision stating that "0.6. Per-batch baseline recapture (self-hosting only)"'s documented cache-form exception (inner `millpy-implement.py` command runs from `<git_root>/plugins/mill/scripts/...`, not `${CLAUDE_PLUGIN_ROOT}`) must survive the conversion to `millpy-bg.py` unchanged — only the outer `millpy-bg.py` wrapper call is cache-form for both "0.5" and "0.6"; "0.5"'s inner command stays cache-form too, with no exception.

## Pushed Back

(none)
