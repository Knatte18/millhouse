# Plan-fix round 2

## Fixed

- `20260814-092731-plan-review-r2.md` — **[NIT:consistency] Card 2 misdescribes Test C's
  try/except granularity**: removed the inaccurate "Test C1/Test C2" citation and replaced it with
  an explicit note that Test I intentionally uses finer-grained per-sub-case try/except isolation
  than Test C's own single-block structure, with the rationale (Test I's four sub-cases assert
  materially different things, so isolating each keeps one failure from masking the others).
- `20260814-092731-plan-review-r2.md` — **[NIT:consistency] Card 6's compute_batch_baselines mock
  instruction is self-contradicting**: removed the contradictory `side_effect`-function digression
  and clarified that a flat `return_value={"batch-a": [], "batch-b": []}` is correct here, since
  (unlike the failure-isolation test) both batches get the identical outcome in this test.

## Pushed Back

(none)
