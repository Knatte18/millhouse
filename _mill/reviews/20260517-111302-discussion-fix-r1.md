# Discussion fix — round 1

Source review: `20260517-111120-discussion-review-r1.md` (verdict: APPROVE, 2 NOTE findings)

## Fixed

- `[NOTE] _invoke not listed in affected-modules table` — Reframed the sandbox-argv-fix decision so derivation lives entirely inside `_build_argv` (tokenise `allowed_tools` and check set intersection with `{Edit,Write,Bash,NotebookEdit}`). No signature change to `_invoke` or the `run_*` public functions. Updated the **In** scope bullet, the `sandbox-argv-fix` Decisions subsection, the affected-modules table row for `_llm_claude.py`, and the matching Q&A log entry to all describe the same approach.
- `[NOTE] Existing _build_argv test assertion conflicts with new behavior` — Updated the **In** scope bullet to spell out that the assertion at `test-llm-claude.py:152-154` (currently `argv == [..., "--allowedTools", ""]`) must be REPLACED, not augmented, with the new shape (no `--allowedTools`, contains `--disallowedTools Edit,Write,Bash,NotebookEdit`).

## Pushed Back

(none)
