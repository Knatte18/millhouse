# Discussion fix — round 1

## Fixed

- `20260904-080931-discussion-review-r1.md`: "Scope bullet overstates pytest's per-runner message" — corrected `discussion.md`'s Scope bullet and the `fix-table-runner-agnostic-remedy` Decision to state that the bare-pytest `verify-full-suite` message names only `-k`, not `--only` (an option that belongs only to `run-all.py`'s message), instead of implying both flags apply to pytest.

## Pushed Back

(none)
