# Plan-fix report — round 1

```yaml
round: 1
source_review: 20260524-171833-plan-review-r1.md
verdict: REQUEST_CHANGES
blocking_count: 5
nit_count: 3
```

## Fixed

- `20260524-171833-plan-review-r1.md` — "[BLOCKING] V3 client API signatures omit wiki_path" — added `wiki_path: Path` as required first positional argument to every public function in batch 1 card 6 (`upsert_task`, `upsert_tasks_batch`, `set_phase`, `remove_task`, `get_task`, `list_tasks_brief`, `list_tasks_full`, `health_check`). Updated External interface block in batch 1 scope. Updated every consumer in batch 2 card 13 (migration script) and batch 3 cards 21-28 (call-site ports) and card 11 (e2e tests) to pass `wiki_path` as first positional argument.
- `20260524-171833-plan-review-r1.md` — "[BLOCKING] Integration tests still import _wiki after card 30 deletes it" — added new card 36 ("Sweep every remaining test file for `_wiki` / `_tasks_md` / `_sidebar` references") covering all 29 test files that import or reference V2 modules. Rules cover imports, mock.patch targets, direct calls, fixture builders, and dead-test deletion. Updated card 30's grep-guard text to acknowledge card 36 owns test-side cleanup. Updated batch 3 cards count 21 -> 22, overview decision "card-numbering-global" range 15-35 -> 15-36, and All Files Touched list with the 29 test files now in card 36's Edits.
- `20260524-171833-plan-review-r1.md` — "[BLOCKING] unit_tests/test-cleanup.py patches break after card 23" — addressed via card 36 rule 2 (`mock.patch("mill_<cli>._wiki.<fn>")` -> `mill_<cli>.wiki.<fn>`); `unit_tests/test-cleanup.py` is in card 36's Edits list and card 34 now explicitly notes that `_wiki`/`_tasks_md`/`_sidebar` patches are card 36's concern (this card 34 sticks to `wiki/config.yaml` purge only).
- `20260524-171833-plan-review-r1.md` — "[BLOCKING] _spawn_core return-type change not propagated to callers" — added explicit "_spawn_core return-shape propagation" sub-bullets to cards 22 (millpy-claim) and 25 (millpy-spawn) instructing the implementer to grep each file end-to-end for `.slug`/`.title`/`.brief`/`.group`/`.status` attribute access on return values from `_spawn_core.multi_select_groom_then_claim` and `groom_and_claim_merge`, and convert to dict-key access. Card 26 also strengthened to call out the return-signature change explicitly and reference cards 22/25.
- `20260524-171833-plan-review-r1.md` — "[BLOCKING] Migration script does not handle missing/legacy wiki/config.yaml" — added Preconditions block to card 13 (`mill-config.yaml` must exist; script aborts cleanly via `_paths.resolve_wiki_path`'s existing exception); added step 6 "Aborted-prior-run guarantee" stating the script always rebuilds the full task list from current Home.md and idempotently re-runs `upsert_tasks_batch` (no partial-state drift possible).
- `20260524-171833-plan-review-r1.md` — "[NIT] Card 12 commit-message-only deletion comment is confusing" — reworded card 12's instruction to "Mention the deleted test name in the commit message body; do not leave any `# removed:` comment inside the test file itself."
- `20260524-171833-plan-review-r1.md` — "[NIT] Card 13 missing-tasks.json error semantics undefined" — added explicit guarantee in card 13 step 4 (dry-run does not import `wiki._client` from the top level; daemon cannot spawn during dry-run). Strengthened card 3 (`_parse.py`) to state `parse_home_md` is pure string-in/list-out and does not initialise `_client`. Added card 14 dry-run assertion: no `tasks.json`, no `.wiki-daemon.json` state file post dry-run.
- `20260524-171833-plan-review-r1.md` — "[NIT] Card 9 brief-paragraph-collapse rule loses paragraph breaks" — added explicit one-way migration note to card 3 (parser) stating paragraph-collapse is migration-only; new tasks via `wiki.upsert_task` preserve brief verbatim; the renderer never re-splits.

## Pushed Back

(none — every finding was factually accurate and no fix would cause harm per the `mill-receiving-review` decision tree.)
