# Review: Set MILL_PYTHON via mill-setup, use in all skill invocations — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-22
```

## Findings

### [BLOCKING] Batch 5 expected count wrong after Batch 1 adds occurrences
**Location:** Batch 5, Card 11
**Issue:** The count check `grep -c '\.venv/Scripts/python\.exe' plugins/mill/skills/mill-setup/SKILL.md` expects `18` with the comment "no occurrences should have been added or removed from mill-setup," but Batch 1 adds at least two new occurrences: the Phase 4.8 Bash invocation and the Phase 8 verification bullet (`"${CLAUDE_PLUGIN_ROOT}/.venv/Scripts/python.exe" -c "import json...`). The expected count after all batches is the pre-task count plus whatever Batch 1 inserts.
**Fix:** Remove the hardcoded `18` and replace with a comment stating the count should equal the pre-task baseline plus the occurrences explicitly added by Batch 1 Cards 1–2; or compute the expected count by running the grep against the post-Batch-1 file and documenting that result.

### [BLOCKING] Card 4 commit targets a file outside the git repository
**Location:** Batch 2, Card 4
**Issue:** `C:\Users\hanf\.claude\CLAUDE.md` is in the user's home directory, not inside the task worktree or any tracked git repository. The card's `Commit:` field (`docs(user-CLAUDE.md): update mill script path to use $MILL_PYTHON`) instructs the implementer to commit this file, but `git add` will silently skip or reject a path outside the repo root, leaving nothing staged; the subsequent `git commit` will fail with "nothing to commit."
**Fix:** Remove the `Commit:` field from Card 4 and add a note that this edit is a direct file change with no git commit. If tracking the change is desired, fold it into Card 3's commit with a message that acknowledges both files were updated.

## Verdict

REQUEST_CHANGES
Two cards have incorrect instructions that will cause implementer failures.