# Plan fix report — round 6

Source review: `_mill/reviews/20260812-093057-plan-review-r6.md`

## Fixed

- r6 "Card 20 miscounts step 6.5's sub-items as two" (NIT:consistency) — accurate; the prose said "two" while listing three markers, and the source's step 6.5 does have three sub-items. Card 20 now says three and names them (warm `SendMessage` resume, the `--resume-incomplete` fallback, After recovery), so the count and the enumeration agree and the implementer can check itself against the file.
- r6 "Cards 7 and 11 drop the source's arrow in \"verbatim\" preserved quotes" (NIT:consistency) — accurate: the on-disk sentences read "on stuck → escalate", and both cards claimed to quote them verbatim while omitting the U+2192. Took the reviewer's second remedy rather than the first: both excerpts are now marked as locators with elisions, and each card states that the on-disk bytes are authoritative wherever the quote and the file differ. Reproducing the arrow inside the plan would have pulled a U+2192 into plan files for no gain, and "preserve what is on disk" is the instruction the implementer actually needs — the quote's only job is to locate the sentence.

## Pushed Back

None. Both findings were accurate and neither fix caused harm.
