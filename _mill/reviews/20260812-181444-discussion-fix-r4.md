# Discussion Fix — round 4

Source review: `_mill/reviews/20260812-181347-discussion-review-r4.md` (APPROVE, 3 NIT findings, all demoted-from-BLOCKING).

## Fixed

- "819 Scope still lists the dropped is_inplace()-suspect trigger" — struck the `_inplace.is_inplace()`-suspect clause from Scope/In's #819 bullet and the stale "suspect-determination conditions" phrase in Testing, so both match the Decision's single-condition gate.
- "817 fallback trigger (b) is defined two incompatible ways" — reconciled the two definitions in the `817-dead-parent-detection` Decision: a hop is chain-end when it cannot produce a `parent:` value at all, whether because both `_mill/status.md` and `task/status.md` are absent, or because one of them exists with no `parent:` row — both sub-cases now explicitly collapse into the same trigger (b).
- "817 Testing section hasn't kept pace with the Decision's accumulated complexity" — added three more 817 integration-test cases: multi-hop chain resolution, the 10-hop cycle-cap halt, and the `task/` legacy-layout fallback read.

## Pushed Back

(none — all three findings verified accurate, no harm from fixing)
