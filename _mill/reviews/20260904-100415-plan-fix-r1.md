# Plan fix -- round 1

Source review: `_mill/reviews/20260904-100336-plan-review-r1.md`

## Fixed

- [BLOCKING:design] "Card 6's fixture reuse instruction cannot produce required scenarios" -- verified against `_make_overview` (line 51, hardcodes `    verify: null` per entry with no override). Card 6 now explicitly authorizes extending `_make_overview` with a backward-compatible per-entry `verify` override and states that batch-file frontmatter stays hand-rolled; batch 2's Batch Scope records the same.
- [NIT:consistency] "\"ASCII only\" instruction in new-code cards contradicts file's existing em-dash convention" -- verified (the file uses em dashes in comments/docstrings; the house rule targets stdout). Dropped the trailing "ASCII only" sentence from batch 1 cards 1, 3, 4 and 5.

## Pushed Back

(none)
