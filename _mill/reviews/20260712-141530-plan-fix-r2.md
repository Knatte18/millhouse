# Plan fix — round 2

```yaml
task: Explore and adopt Claude Code fork-agents in mill orchestration
slug: explore-fork-agent-opportunities
round: 2
source_review: 20260712-141016-plan-review-r2.md
verdict_addressed: REQUEST_CHANGES (3 BLOCKING, 3 NIT)
```

## Fixed

- `20260712-141016-plan-review-r2.md` — **[BLOCKING] "finalize never raises ReviewError — ERROR-envelope
  rationale is wrong"**. Accurate, and verified against source: `_review_code.py:547-559` (and its
  siblings at `_review_discussion.py:146-164`, `_review_plan.py:568-575`) catch `ReviewError` from
  `finalize_scope` **inside the backend** and **return** an ERROR `ReviewResult`. The CLI prints it
  via `result.to_dict()` and exits **0**; `print_error_envelope` is never reached and the CLI's
  `except ReviewError` never fires on an empty `raw_text`. Cards 9(c), 10(c) and 11(c) now state the
  real mechanism (backend's own ERROR result, exit 0), and card 12(b) now asserts `verdict: "ERROR"`
  with **return code 0** and explicitly forbids the raising-`side_effect` mock and the exit-1
  assertion my round-1 fix had prescribed — both of which pinned behaviour the system does not have.

- `20260712-141016-plan-review-r2.md` — **[BLOCKING] "Card 12/13 mocking discipline is unrunnable as
  written"**. Accurate. My round-1 fix over-corrected: a wholly real `_review_common` makes the CLIs
  call `find_active_slug` (raises in a tempdir with no marker) and `resolve_path`, which reaches the
  real `_paths.resolve_git_root()` (`_review_common.py:353-375`) — env-dependent and against the
  no-real-git convention. Cards 12 and 13 now prescribe the workable middle: pass `--slug` so
  `find_active_slug` is never called, and patch `load_config` / `resolve_path` **as attributes on the
  real `_review_common`**, leaving `ReviewError`, `parse_verdict` and the backend's own exception
  handling genuinely real. Recorded why this works: each CLI does its `from _review_common import ...`
  **inside `main()`**, so pre-patched attributes are picked up.

- `20260712-141016-plan-review-r2.md` — **[BLOCKING] "Batch 3's verify does not cover the five
  template edits"**. Accurate and verified: `test-render.py` renders only `tempfile` fixtures
  (`:16-96`) and never opens `plugins/mill/templates/`, so my claimed "regression net" was fiction —
  cards 15 and 16 would have shipped with **zero** coverage in their own batch, deferring all
  detection of a broken template to batch 5. Added **card 18**, creating
  `plugins/mill/unit_tests/test-review-templates.py`: all five templates still render with their full
  token sets (the real `<UPPERCASE>`-token guard), the deleted prose stays deleted, the
  `MILL_REVIEW_BEGIN`/`END` wrapper stays, no `<OUTPUT_FILE>` token. Batch 3's `verify:` now runs it
  and **drops `test-render.py`**, with the reason recorded so nobody re-adds it. Cards renumbered:
  batch 4 -> 19-21, batch 5 -> 22-23; batch 5's card 23 drops its now-duplicated renderability
  assertion and keeps the broader templates-plus-agents no-token sweep.

- `20260712-141016-plan-review-r2.md` — **[NIT] "`_review_plan.run()` does not call `prepare()`"**.
  Accurate. `run()` is defined at `_review_plan.py:594` and re-renders inline with its **own**
  `build_tool_rule` at `:836`, reaching batch scope via `_review_one_batch` (`:196`) submitted to a
  `ThreadPoolExecutor` (`:752`). Only discussion (`:215`) and code (`:629`) route `run()` through
  `prepare()`. Batch 2's Batch Scope and card 8 now say this explicitly, so the implementer does not
  hunt for a `prepare()` call that is not there — and so the reason `:196`/`:836` are safe is stated
  correctly (separate code, not a default).

- `20260712-141016-plan-review-r2.md` — **[NIT] "Context gaps in batch 5"**. Accurate; `Context:` is
  an allowlist. Added `plugins/mill/agents/mill-implementer.md` to card 22 (its Requirements reason
  about that file's `tools:` line) and the five template paths to card 23.

- `20260712-141016-plan-review-r2.md` — **[NIT] "Card 1 `Context:` lists `_paths.py` with no use"**.
  Accurate as written. Took the reviewer's second option and stated the purpose in Requirements:
  `write_brief` routes the brief filename through `_paths.sanitize_filename_component`
  (`_agent_dispatch.py:117`), and `output_path_for` must stay consistent with the filename that
  produces. Also noted that `output_path_for` itself is pure `pathlib` and adds no `_paths` dependency.

## Pushed Back

_None. All six findings verified accurate against source; none met a harm-check exception._
