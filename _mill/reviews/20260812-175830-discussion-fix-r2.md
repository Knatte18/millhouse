# Discussion Fix — round 2

Source review: `_mill/reviews/20260812-175755-discussion-review-r2.md`

## Fixed

- `[NIT:consistency] Field-report platform left unspecified vs. Windows-only prior doc` — added an
  explicit "platform is unconfirmed for both" note in the Problem section, plus a pointer to the
  Decisions section noting the chosen fix is platform-agnostic regardless.
- `[NIT:design] Scan-collision test case not enumerated` — added TDD candidate (5) to the Testing
  section: two `scripts`-named entries in `sys.path` returns the first match (documented
  first-match-wins semantics), with a note on why this can't realistically occur for Phase 4.8's
  actual invocation.

## Pushed Back

(none)
