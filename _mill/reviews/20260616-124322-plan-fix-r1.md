# Plan-fix report — round 1

Source review: `_mill/reviews/20260616-124207-plan-review-r1.md` (REQUEST_CHANGES, 2 BLOCKING)

## Fixed

- `20260616-124207-plan-review-r1.md` [BLOCKING] "Card 10 step-2b wiring needs parent_branch, not in scope/Context" — Card 10 requirement (3) now derives `parent_branch = _parent_branch.resolve(status_path, interactive=False)` and `task_dir = status_path.parent` within step 2b, and `plugins/mill/scripts/_parent_branch.py` is added to the card Context.
- `20260616-124207-plan-review-r1.md` [BLOCKING] "Card 10 status-path target is wrong line / incomplete" — Card 10 requirement (1) now states the line-151 fix replaces `resolve_git_root()` with `resolve_hub_path()` inside the per-batch cleanup `python -c` snippet (no `worktree_root` in scope there), and requirement (2) extends the card to also fix the crash-recovery helper's `reviews_dir = git_root / '_mill/reviews'` to `resolve_hub_path()`, while explicitly leaving `scratch_dir = git_root / '.scratch'` unchanged (not a `_mill/` path).

## Pushed Back

(none)
