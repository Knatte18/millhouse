# Plan fixer report — round 1

Source review: `_mill/reviews/20260616-123917-plan-review-r1.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 2 NIT).

## Fixed

- [BLOCKING] "Card 7 test pattern won't execute in its host file" — VERIFIED accurate: `test-implementer-common.py` is a `main()` runner (`sys.exit(main())`, helpers `_setup_fixture`/`_capture_stdout`, no `unittest.main()`). Rewrote Card 7 to add cases inside `main()` using `_setup_fixture` + `_capture_stdout` and the file's failure accounting, and to explicitly forbid adding a `unittest.TestCase` class.
- [NIT] "Card 3 misnames the host file's test convention" — VERIFIED accurate: `test-review-common.py` is a `main()`/`PASS`/`FAIL` runner, not `unittest.TestCase`. Corrected Card 3 to name the actual convention (`main()` runner + `contextlib.redirect_stderr`, module-path bootstrap already present).
- [NIT] "Verify gate not threaded into millpy-fix.py" — VERIFIED accurate: `millpy-fix.py` calls the same `finalize_from_output` (line ~198) and `_forward_output` (line ~356) and shares the false-success risk on the review-fix path. Took the threading option (reviewer's first choice): extended Card 6 to also edit `millpy-fix.py`, threading `verify_cmd` for batch-scope finalize/full (resolving `batch_file` from the already-parsed `batches`/`plan_base`, lines 170-177) and passing `None` for holistic scope. Updated batch-03 frontmatter `verify:` and Batch Tests to add `test-millpy-fix.py`, and added `millpy-fix.py` to the overview's All Files Touched.

## Pushed Back

- (none)
