MILL_REVIEW_BEGIN
# Review: mill-go2: fork-based dispatch reliability, shared-skill preloading, and catalog accuracy — holistic

```yaml
duration_s: 118.0
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-08-21
```

Verified against source: `mill-go2/SKILL.md`'s implementer bookended-fork paragraph (three bullets:
"Fork these attempts only", "Dispatch cold to escape a failed dispatch", "Cold fallback, once per
batch") matches Card 1's literal replacement text byte-for-byte, including the narrowed fork scope
(initial dispatch + step 3(a) transient only) and the folded-in stuck-escalation self-resolve cold
fallback. Card 2's `## Driver preamble` body matches the required preload text verbatim, and its
marker-file→skill-trio mapping is confirmed identical to `workflow/SKILL.md`'s Language Detection
table (`pyproject.toml`/`setup.py`/`setup.cfg`→python, `.csproj`/`.sln`→csharp, `go.mod`→golang).
Card 2's quoted `mill-go-base/SKILL.md` Entry/Override-point-B text is confirmed verbatim present at
`mill-go-base/SKILL.md:30-33`, so the "no other wiring needed" premise holds. Card 3's fixer
bookended de-briefing text matches Card 3's replacement exactly, with the pre-existing
`fork_attempted` paragraph and cold-fallback block left untouched as required. Card 4's frontmatter
`description:` matches Card 1's narrowed forked-set wording exactly (no stale "every attempt"
phrasing). `test-mill-go-variants.py`'s scope-extension updates (8192-byte ceiling,
`## Driver preamble` "Before Step 0" assertion, required-substring checks for `not the orchestrator`
/ `fork-fallback` / `unclaimed`) correctly track the new SKILL.md content and give end-to-end
coverage of all three cards' text changes. No out-of-plan files present; both `## Shared Decisions`
(single-file scope, full-suite verify) are honoured with no deviation.

## Verdict

APPROVE
All four cards' edits verified verbatim against source; test updates track the new content; no cross-batch or scope issues found.
MILL_REVIEW_END
