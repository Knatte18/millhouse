# Plan fix: round 1

## Fixed

- `20260725-133403-plan-review-r1.md` — "[BLOCKING] `commit_none_card_ids` not threaded into the `--stage full` dispatch path": extended Batch 06 Card 17's Requirements to also add `commit_none_card_ids=commit_none_card_ids` to the second, symmetric `_forward_output(...)` call site at `millpy-implement.py` lines 672-687 (the `--stage full` synchronous/subprocess dispatch path), mirroring the existing `card_ids` threading pattern already used at both call sites. Also extended Card 18 with a note explaining the gate logic is fully covered by its direct `_forward_output` tests (shared by both call sites), and the call-site wiring itself is covered by code-identity with the pre-existing, already-trusted `card_ids` threading pattern.

## Pushed Back

(none)
