```yaml
round: 1
source: 20260923-113632-plan-review-r1.md
```

## Fixed

- [BLOCKING:design] "Card 2's worked example fails its own function's existing qualification gate" —
  replaced the worked example and new test's symbol token from `` `x = mod.func(args)` `` to
  `` `x = mod.get_value(args)` `` throughout card 2, so the trailing segment (`get_value`, has an
  underscore) satisfies `_symbol_candidate_shape`'s existing, unmodified `qualifies()` gate; added a
  parenthetical to the Requirements explaining why `func` would have failed that same gate.
- [BLOCKING:design] "Card 5's blanket test-migration recipe breaks tests whose asserted outcome
  depends on mechanisms Card 4 deletes outright" — moved deletion of the 6 named tests
  (`..._out_of_scope_{deprecated,legacy,obsolete,archive,pruned_leaves_outside_file_sole_survivor}`
  and `..._first_match_wins_root_precedence`) into card 4 itself (the card that removes the
  mechanisms those tests exercise), and added an explicit exclusion clause to card 5's Requirements
  so its generic recipe is scoped to the remaining tests only.
- [NIT:consistency] "Batch Scope references a non-existent 'Testing decisions' section" — removed the
  dangling cross-reference from the Batch Scope paragraph.
- [NIT:design] "Card 5 bundles migration of ~50+ existing symbol-branch tests into one card" — added
  an explicit acknowledgment to card 5's Requirements stating why the single-card size is intentional
  (identical mechanical rule across all affected tests, shared batch-level `verify:`, no sub-mechanism
  split would change which tests need the edit).

## Pushed Back

(none)
