# Plan fix — round 1

Source: `_mill/reviews/20260904-084403-plan-review-r1.md`

## Fixed

- [BLOCKING:scope] "Corroboration self-healing persist never fires from real callers" — Card 8
  (batch 4) now threads `status_path=status_path,` alongside `batch_name=args.batch_name,` at both
  `millpy-implement.py` call sites (finalize stage and full stage), confirmed against the actual
  current source (`task_dir=status_path.parent,` was present but a bare `status_path=status_path,`
  kwarg was not, at either call site).
- [NIT:consistency] "New corroboration test case labels collide with an existing case 72e" —
  confirmed `test-implementer-common.py` already has a real case 72e ("a stuck dict with no
  'signatures' key must never be waived"); Card 9 (batch 4) now labels its three new cases 72f/72g/72h
  and places them after 72e, before the unrelated Case 73.
- [NIT:consistency] "Card 3 reimplements `git worktree list --porcelain` parsing that
  `_worktree.list_worktrees` already provides" — confirmed `_worktree.list_worktrees` exists and is
  already used the same way in `millpy-cleanup.py` (`_worktree.list_worktrees(hub_root)`); Card 3
  (batch 2) and its Card 4 test requirements now reuse it (wrapped in
  `try/except _worktree.WorktreeError: return []`) instead of hand-rolling porcelain parsing.

## Pushed Back

(none — all three findings verified accurate against the actual source and fixed)
