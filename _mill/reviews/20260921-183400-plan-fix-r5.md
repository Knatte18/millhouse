# Plan fix — round 5

Source review: `20260921-183307-plan-review-r5.md`

## Fixed

- `[BLOCKING:scope] Card 2 Requirements cites two files absent from Context/Edits` — added
  `plugins/mill/scripts/millpy-fix.py` and `plugins/mill/scripts/millpy-merge-in-subagent.py` to
  Card 2's `Context:` (batch 1), since its Requirements make a load-bearing safety claim about both
  files' call sites that the implementer must be able to independently verify.
- `[NIT:consistency] Card 7 names the wrong aggregator function` — corrected Card 7's Requirements
  (batch 3) from "the `validate()` aggregator" to "the `run()` aggregator," matching
  `_plan_validate.py`'s actual function name (`validate()` belongs to `_plan_dag.py`).

## Pushed Back

(none — both findings were accurate and fixed as described above)
