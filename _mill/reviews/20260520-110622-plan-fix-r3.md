# Plan Fix — round 3

```yaml
source_review: 20260520-110448-plan-review-r3.md
round: 3
```

## Fixed

- `20260520-110448-plan-review-r3.md` / "[BLOCKING] Windows path separator mismatch in list_worktrees tests": Updated `test_list_worktrees_single` and `test_list_worktrees_with_linked` specs in batch 02 card 3 to assert `entry["path"] == repo_path.as_posix()` and `entry["path"] == linked_path.as_posix()` respectively. `list_worktrees` returns `.as_posix()` paths; `str(Path(...))` on Windows produces backslashes and would always fail.

- `20260520-110448-plan-review-r3.md` / "[NIT] Card 7 import-removal instruction is wrong": Updated the Imports paragraph in batch 03 card 7 to explicitly require replacing any `_subprocess_util.run(["git", "init", ...])` fixture calls with stdlib `subprocess.run([...], check=True)`. The import-removal instruction is now unconditional (after all rewrites) rather than conditional on unknown surviving call sites.

## Pushed Back

(none)
