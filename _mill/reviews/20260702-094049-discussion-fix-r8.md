# Discussion fix: round 8 (APPROVE with NOTE)

```yaml
task: Fix agent-mode dispatch races and pipeline gaps
slug: agent-mode-gaps
round: 8
source_review: _mill/reviews/20260702-094030-discussion-review-r8.md
```

## Fixed

- `_mill/reviews/20260702-094030-discussion-review-r8.md` — "[NOTE] Baseline stage must bypass batch-index resolution": confirmed `millpy-implement.py:239-242`'s batch-entry lookup hard-fails with `batch_name=None` before the module-wide verify command is even read at `:266`. Updated the Decisions § baseline-aware module-wide verify gate section to require the new `--stage baseline` branch dispatch before the existing `batches`/`batch_entry` resolution block (`:231-242`), reading `overview_frontmatter`/`module_wide_verify_cmd` immediately after the overview file loads (`:225-230`) instead of after batch resolution.

## Pushed Back

(none)
