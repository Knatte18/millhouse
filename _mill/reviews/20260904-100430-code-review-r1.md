MILL_REVIEW_BEGIN
# Review: _plan_validate.py context-completeness check: misses bare symbol/identifier references entirely, only matches path tokens — holistic

```yaml
duration_s: 170.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-04
```

## Findings

None. The implementation matches both batches' plans end-to-end:

- Card 1 helpers (`_SYMBOL_SEARCH_EXTENSIONS`, `_SYMBOL_SEARCH_DENYLIST_DIRS`, `_symbol_candidate_shape`, `_resolve_symbol_files`, `_covered_by_own_refs`) are present at `plugins/mill/scripts/_plan_validate.py:1581-1811`, with exact shape/qualification/suffix-stripping semantics matching the spec (verified trailing `()`/`[...]`/`<...>` repeated-strip loop, `qualifies()` uppercase-or-underscore rule, 1-vs-2-segment dotted handling). The path-branch refactor to `_covered_by_own_refs` (`:1951`) is a clean pure extraction, byte-identical behavior preserved.
- `_resolve_symbol_files`'s candidate-root order (`:1763-1768`) matches `resolve_existing_paths`'s actual first-match-wins precedence (`_review_common.py:1067-1078`), including the subtle detail that the bare-`git_root` candidate is unconditional on whether `root` is set — I traced the real code (not just its docstring, which is misleadingly worded) and confirmed the plan's restated precedence is accurate.
- Card 2's wiring (`_check_context_completeness:1910-1998`) correctly gates shape before falling through to the shared exemption checks, branches on `is_path_shaped`, and constructs the exact fixed `message` format (`:1992-1996`) the batch-2 SKILL.md row depends on. `path` in the error dict is always the original token, never `search_key`/`canonical` — confirmed.
- Card 3's wiring correction (checking `search_cache` before calling `_resolve_symbol_files` at all, `:1970-1975`) is implemented and is what makes `test_check_context_completeness_symbol_cache_invoked_once_per_key` (`test-plan-validate.py:3391`) actually pass with `call_count[0] == 1`.
- All 14 new tests (8 from card 2, 6 from card 3) are present, follow the file's tempfile/`_plan_validate.run()`/filter-by-check convention, and are registered in `main()`'s `tests = [...]` list in the correct grouped position (`:7774-7790`). The one sanctioned exception (monkeypatching `_plan_validate._resolve_symbol_files` with try/finally restore) is implemented correctly and works because the call site resolves the name via module globals at call time.
- Both module docstrings (`_plan_validate.py:47-49` and `test-plan-validate.py:21-22`) and `_check_context_completeness`'s own docstring (`:1826-1891`) were updated as required.
- `mill-plan/SKILL.md`'s Step 1.5 fix-table `context-completeness` row (`:362`) branches on the `"which resolves to '"` substring exactly as specified, preserves the unchanged path-case instruction and the trailing `line`-field sentence, and does not touch row position or any other row.
- No git subprocess dependency in the new production code (`os.walk`/`pathlib` only) — Shared Decision honored. No helper duplication found elsewhere in the plugin tree. No out-of-plan files — only the three files named in `## All Files Touched` were touched.

## Verdict

APPROVE
End-to-end implementation matches both batches' plans precisely; no blocking or nit findings.
MILL_REVIEW_END
