MILL_REVIEW_BEGIN
# Review: mill-merge-in/mill-finalize/codeguide-update: cleanup-ordering and path-resolution bugs — holistic

```yaml
duration_s: 234.0
verdict: APPROVE
reviewer_model: sonnethigh
reviewer_self_id: Sonnet 5 (claude-sonnet-5, per system metadata)
reviewed_file: plan/
date: 2026-09-04
```

## Findings

### [NIT:consistency] resolve_scope.py module docstring goes stale after Card 7's fix
**Location:** batch 3 / card 7 **Issue:** The module docstring's "Scope-resolution chain" item 4 ("Explicit paths ... resolve relative to git toplevel") describes the pre-fix behavior; Card 7's Requirements never ask to update this comment, so it will contradict the new `cwd_path`-anchored behavior after the fix lands. **Fix:** Add one sentence to Card 7's Requirements instructing the implementer to update that docstring line to say "resolve relative to invocation cwd" alongside the code change.

### [NIT:scope] New nested-cwd test scenario risks ref-name collision, unstated precaution
**Location:** batch 3 / card 7 **Issue:** The new TDD scenario picks an arbitrary `<relative-filename>` for the nested-cwd explicit-path case; if that name happens to resolve as a git ref, `enumerate_scope`'s single-token dispatch would misroute it into `_head_rev_scope` instead of `_explicit_scope`, silently invalidating the test — exactly the collision class Scenario 18 was written to guard against. **Fix:** Add a one-clause note in Card 7's Requirements to pick a filename with no ref-name collision (mirroring Scenario 18's own precaution), or use a multi-token call to sidestep the single-token dispatch entirely.

## Verdict

APPROVE
Plan is source-grounded and internally consistent; only two non-blocking doc/test-hygiene NITs remain.
MILL_REVIEW_END
