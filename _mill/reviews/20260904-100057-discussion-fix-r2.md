# Discussion-fix round 2

Source review: `_mill/reviews/20260904-100010-discussion-review-r2.md`

## Fixed

- Source: `20260904-100010-discussion-review-r2.md` — `[NIT:consistency] Scope text under-scopes #953 vs. Technical context/Testing`. Verified by reading Scope > In, the Decision's own summary, Technical context, and Testing side by side: Scope and the Decision named only the finalize-stage call site while Technical context/Testing already required both. Updated Scope > In, the Decision's summary, and the "Out" section's contradictory "Only the conflicts-mode finalize call is renamed" line to explicitly cover both call sites (`--stage finalize` ~397-424 and `_run_conflicts` full-mode ~490), and dropped the "re-check during planning" hedge in Technical context.
- Source: `20260904-100010-discussion-review-r2.md` — `` [NIT:consistency] `_attach_commit_sha` reuse note doesn't match the actual edit site ``. Verified by reading `_implementer_common.py` ~1879-1889: the block being parameterized is a standalone `git rev-parse HEAD` + `_is_valid_commit_sha` check, not a caller of `_attach_commit_sha`. Reworded the Technical context bullet to name only `_is_valid_commit_sha`/`_COMMIT_SHA_RE` as reusable, and stated `_attach_commit_sha` is unrelated and must not be touched.

## Pushed Back

(none — both findings verified accurate, both fixed)
