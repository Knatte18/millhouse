MILL_REVIEW_BEGIN
# Review: _plan_validate.py verify: command validation: false positives, missing escape hatches, and a doc/enforcement mismatch — holistic

```yaml
duration_s: 344.0
verdict: REQUEST_CHANGES
reviewer_model: sonnethigh
reviewer_self_id: Claude Sonnet 4.5 (self-assessed)
reviewed_file: plan/
date: 2026-09-04
```

## Findings

### [NIT:consistency] Card 1's go-test replacement snippet is mis-indented
**Demoted-from:** BLOCKING
**Location:** Batch 1 / Card 1 — the `for segment in _RE_SHELL_OPERATOR.split(command):` replacement block. **Issue:** Verified via anchored grep against the batch file: the replacement's top line sits at column 10, while the sibling `if "run-all.py" in command...` / `if "dotnet test" in command...` checks it sits between (in `_check_frontmatter`) are at column 8 in current source; inserted verbatim this is a Python `IndentationError`, not a stylistic nit. **Fix:** Re-indent the whole `for segment in ...:` block (and every nested line under it) two columns left, to start at column 8.

### [BLOCKING:design] Batch 3 has no depends-on despite a live runtime coupling to batch 1's new kwarg
**Location:** `00-overview.md` Batch Index / Batch 3 `docs-and-reviewer-guardrail`, Card 4. **Issue:** Card 4 edits `mill-plan/SKILL.md`'s self-run call block (verified: this is a `_plan_validate.run(...)` call actually executed by mill-plan, not prose) to pass `done_gate=cfg.get("pipeline", {}).get("done_gate"),` — a kwarg `_plan_validate.run` only accepts once batch 1 lands. Batch 3 declares `depends-on: []`, so the plan's own DAG permits it to be implemented/merged independently of, or before, batch 1; any mill-plan self-run invoked on this self-hosted repo in that window raises `TypeError: run() got an unexpected keyword argument 'done_gate'`. **Fix:** Add `depends-on: [1]` for batch 3 in both `00-overview.md`'s Batch Index and `03-docs-and-reviewer-guardrail.md`'s frontmatter.

### [BLOCKING:scope] Card 6's Context omits `_plan_validate.py`, whose function it names
**Location:** Batch 3 / Card 6. **Issue:** Requirements' "after" text names `_plan_validate._is_python_project` to justify the new comment's accuracy claim (correctly, per source verification), but Card 6's `Context:` lists only `CLAUDE.md` and `Edits:` lists only the config template — `_plan_validate.py` appears in neither, violating the Context-completeness criterion (a function from a file not in Context:/Edits:). **Fix:** Add `plugins/mill/scripts/_plan_validate.py` to Card 6's `Context:`.

## Verdict

REQUEST_CHANGES
Card 1's replacement snippet is mis-indented (syntax break), batch 3 lacks a needed depends-on edge, and Card 6's Context omits a named file.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 2._
MILL_REVIEW_END
