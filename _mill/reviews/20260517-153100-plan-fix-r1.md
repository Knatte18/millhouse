# Plan-fix report -- round 1

Source review: [`20260517-153023-plan-review-r1.md`](20260517-153023-plan-review-r1.md) (verdict: REQUEST_CHANGES, blocking_count=1)

## Fixed

- `[BLOCKING] test_log_dead_pid_no_exit will fail consistently on Windows` -- VERIFY accurate: on Windows, `os.kill(99999999, 0)` raises `OSError(errno=EINVAL)` which is not caught by the typed `except ProcessLookupError` / `except PermissionError` clauses; execution falls through to the mtime branch and returns `(True, pid)` for fresh log fixtures. Fix applied to Batch 5, Card 15 Requirements: case 5 now backdates the log mtime via `os.utime(log_path, (old_ts, old_ts))` so the mtime-fallback branch resolves the dead-PID case on Windows AND the test remains platform-independent. Added a new case 6 (`test_log_live_pid_with_stale_mtime`) as a regression guard that the mtime check is not inside the try-block. Batch-Tests prose updated to reflect six cases.
- `[NIT] Card 11 infrastructure mocking is underspecified` -- VERIFY accurate: the three CLI `main()` bodies call `_paths.resolve_wiki_path`, `_review_common.load_config`, `_reviewers.load`, `_reviewers.validate_role_refs`, and `Path.cwd` before reaching `find_active_slug` or the backend, so a bare temp/test directory short-circuits to a pre-launch envelope. Fix applied to Batch 3, Card 11 Requirements: added an explicit "Pre-launch dependency patches" subsection listing every helper to patch (with the canonical return values), called out the bare-import-binding issue with a `_paths.resolve_wiki_path`-style patch target, and instructed the implementer to patch `_plan_validate.run -> []` for the plan-review CLI so the validator gate is bypassed.
- `[NIT] Dead errno branches in is_bg_worker_alive` -- VERIFY accurate: Python 3.3+ promotes `OSError(errno=ESRCH)` to `ProcessLookupError` and `OSError(errno=EPERM/EACCES)` to `PermissionError`, so the typed `except` clauses already exhaust those cases and the explicit `if exc.errno == EPERM/ESRCH` branches inside `except OSError` are unreachable. Fix applied to Batch 5, Card 14 Requirements: implementation outline now uses a bare `except OSError: pass` with a comment, and a follow-up paragraph documents the Windows-EINVAL motivation and the optional `import errno` removal.

## Pushed Back

(none -- all three findings were VERIFY-accurate and the fixes carry no harm)
