```yaml
type: plan-fix
round: 1
source_review: 20260821-091303-plan-review-r1.md
```

## Fixed

- `20260821-091303-plan-review-r1.md` — "[BLOCKING:design] `test-preflight.py` has no
  test-invocation entrypoint at all": added a requirement to batch 2 card 7 to add a `main()` /
  `if __name__ == "__main__"` dispatcher mirroring `test-marker.py`'s pattern, listing every
  `test_*` function (six pre-existing plus the new ones this card adds).
- `20260821-091303-plan-review-r1.md` — "[BLOCKING:design] `importlib.import_module` won't find
  modules in an arbitrary `scripts_dir`": changed batch 2 card 6's import approach from
  `importlib.import_module(module_name)` to `importlib.util.spec_from_file_location` +
  `module_from_spec` + `exec_module`, which loads directly from the file path regardless of
  `sys.path` contents.
- `20260821-091303-plan-review-r1.md` — "[BLOCKING:scope] New tests in batch 1 are written but
  never invoked": added explicit instructions to batch 1 cards 2 and 5 to append the new test
  function names to `test-pygit2-util.py`'s `run_all()` and `test-marker.py`'s `main()` hard-coded
  `tests = [...]` lists respectively.
- `20260821-091303-plan-review-r1.md` — "[NIT:consistency] New helper omitted from
  `_pygit2_util.py`'s `__all__`": added a requirement to batch 1 card 1 to append
  `"local_branches_at_sha"` to the file's `__all__` list, alphabetically.

## Pushed Back

(none)
