# Plan fixer report — round 5

Source review: `_mill/reviews/20260616-125906-plan-review-r5.md` (holistic, APPROVE, 0 BLOCKING + 2 NIT).

## Fixed

- [NIT] "test-millpy-fix.py in verify but not in Context" — VERIFIED accurate: batch 3's `verify:` runs `test-millpy-fix.py` and card 6 edits `millpy-fix.py`, but the test file appeared in no card's Context. Added `plugins/mill/unit_tests/test-millpy-fix.py` to Card 6's `Context:` so the implementer reads the existing fix-path cases before threading `verify_cmd`. (It is a regression-only run, not a new-code target, so it stays out of Edits and All Files Touched.)
- [NIT] "Card 5 gate-after-drift wording vs emit-point ordering" — VERIFIED accurate (only the post-drift inferred emit has a commit between HEAD-capture and emit; the others run against the already-current HEAD). Added a clarifying parenthetical to Card 5 noting the ordering rule just guarantees no emit precedes its gate, with no behavioural difference for the non-drift emits.

## Pushed Back

- (none)
