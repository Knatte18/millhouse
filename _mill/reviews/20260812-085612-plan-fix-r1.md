# Plan fix report — round 1

Source review: `_mill/reviews/20260812-084840-plan-review-r1.md`

## Fixed

- r1 "Card 24's empty-diff gate will fail from pre-existing drift, not this task's edits" (BLOCKING:design) — confirmed by direct read: `SKILLS.md:56` describes `mill-go2` as "Behaviourally identical to /mill-go today", while that skill's frontmatter now reads "Forks the fixer role instead of dispatching it cold". Card 24 rewritten to regenerate and commit the index under three explicit invariants (no companion-file row, unchanged row count, every changed row matching its skill's current frontmatter) instead of asserting an empty diff. New Shared Decision `skills-index-regenerates-rather-than-asserting-an-empty-diff` records why.
- r1 "Card 24 references mill-go/mill-go2 SKILL.md revert action outside its Context/Edits" (BLOCKING:scope) — accurate; the validator's `context-completeness` check missed it because the reference was prose, not a backticked path token. Both variant SKILL.md paths added to card 24's `Context:`, `SKILLS.md` added to its `Edits:` and to the overview's `All Files Touched`, and the card given a real `Commit:` message.
- r1 "Named tree-guard block will contain the call substring twice, contradicting card 13's own postcondition" (NIT:consistency, demoted from BLOCKING) — accurate. Card 12 moves the `signature: _treeguard.check_and_restore(...)` line into the named block, so the substring necessarily appears twice. Card 13's postcondition now reads "exactly two occurrences, both inside the named block: one executable call site and one signature line", and card 12 states the intent explicitly so the two cards agree.

## Pushed Back

- r1 "History note hardcodes a stale line count" (NIT:consistency) — factually wrong. `wc -l plugins/mill/skills/mill-go-base/SKILL.md` returns 1483, and `od -c` on the file tail shows it ends with a single `\n` after "do.", so line 1483 is the last line and the discussion's "1483 lines" is correct. The reviewer's 1484 comes from the Read tool, which reports a phantom final line for the empty string after the trailing newline. No change; the exact `## History` wording mandated by the discussion's `git-history-is-the-backup` Decision stands.
