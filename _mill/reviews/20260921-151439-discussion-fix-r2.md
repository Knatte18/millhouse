# Discussion-fix round 2

## Fixed

- `_mill/reviews/20260921-151328-discussion-review-r2.md` — "#1092 bare/qualified line breakdown is
  inverted" (demoted from BLOCKING). Verified independently by reading `mill-start/SKILL.md` at every
  cited line: the true split is 5 bare / 5 qualified (not the discussion's original 6/4, and not the
  reviewer's own suggested 4/6 either — that replacement misclassified line 318 as qualified when it
  is bare). Replaced the itemized, fragile line-number list with the verified 5/5 split and an
  instruction to re-derive exact line numbers by reading the file at plan-writing time.
- `_mill/reviews/20260921-151328-discussion-review-r2.md` — "#1097 derivation site lacks a concrete
  function signature". Confirmed neither `apply_cost_metadata` nor `finalize_scope` receives a brief
  path today. Named the concrete insertion point: a new `derive_duration_s(agent_output_path,
  fallback)` helper in `_review_common.py`, called from each of the three review CLIs (which already
  hold `agent_output_path`) before `finalize_scope`, with no signature change to `finalize_scope`
  itself.

## Pushed Back

(none)
