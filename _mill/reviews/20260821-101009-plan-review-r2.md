MILL_REVIEW_BEGIN
# Review: mill-plan SKILL.md: Phase Plan Review gate, convergence, and DAG-validation correctness bugs — holistic

```yaml
duration_s: 331.0
verdict: REQUEST_CHANGES
reviewer_model: sonnethigh
reviewer_self_id: Claude Sonnet 4.5 (Anthropic; self-reported as "Sonnet 5" by the runtime env)
reviewed_file: plan/
date: 2026-08-21
```

## Findings

### [BLOCKING:scope] Batch 2 new unit tests never registered in the runner's explicit list
**Location:** Batch 2, Cards 8, 9 (and possibly 10). **Issue:** `test-plan-validate.py` has no pytest/dynamic discovery — `main()` (line ~6436) only runs functions explicitly listed in its own `tests = [...]` array (verified: `run-all.py` subprocess-execs the file, which gates entirely on that list). Card 8's Requirements say "add three new test functions" but never instruct adding their names to `tests = [...]`; Card 9's "extend whatever existing test function(s)" is ambiguous but if new sibling functions are added (as Card 10 also explicitly permits: "or add new sibling test functions") they hit the same gap. **Fix:** Add an explicit step to each affected card instructing insertion of the new function name(s) into the `tests = [...]` list in `main()`, at the appropriate existing section (e.g. near the `verify-full-suite check` / a new comment block), so `verify:`'s `run-all.py --only test-plan-validate.py` actually exercises the new coverage instead of silently passing with dead test code.

### [NIT:consistency] Card 2 and Card 6 (Batch 1) leave a stale two-of-three skip_checks enumeration
**Location:** Batch 1, Card 2 (`skip_checks` persist paragraph and the Step 1.5 intro-paragraph replacement) vs Card 6 (`out-of-worktree-target` override, same batch). **Issue:** Card 2's new "Persist `skip_checks` for Phase: Plan Review" paragraph says the frozenset is "computed above, after applying the `wiki-config-mutation` and `verify-full-suite` skip-check overrides", and its Step-1.5-intro replacement again names only those same two fix-table rows as the "reactive fallback" pair — both omit Card 6's `out-of-worktree-target` override, which mutates the same `skip_checks` variable earlier in the same Phase: Plan sequence and is likewise persisted/threaded by Card 2's own mechanism. Functionally harmless (same shared variable), but the prose misleads a future reader about which overrides are covered. **Fix:** Have Card 2's two inserted sentences enumerate all three override names (`wiki-config-mutation`, `verify-full-suite`, `out-of-worktree-target`), or have Card 6 append itself to Card 2's enumeration since Card 6 lands after Card 2 in the same batch.

## Verdict

REQUEST_CHANGES
Batch 2's new validator tests are unreachable by `verify:` unless registered in the test file's explicit runner list.
MILL_REVIEW_END
