# Discussion fix — round 1

Source review: `_mill/reviews/20260918-174605-discussion-review-r1.md`

## Fixed

- "Testing section's verify guidance risks tripping its own validator" — reworded the Testing section's verification note to state explicitly that the plan batch must leave `verify:` **absent/null** (not a descriptive string like `"manual read-through"`), matching the pure-docs-batch convention `_plan_validate.py`'s `verify-not-isolated` check already relies on.
- "Decision overstates \"general-purpose\" as already documented in mill-start" — reworded the "Fallback destination" Decision to correctly attribute `Explore` to mill-start's existing "Sub-investigation guidance" bullet and `general-purpose` to mill-plan's "Fork scope guardrail" precedent, rather than implying both were already documented in mill-start.

## Pushed Back

(none — both findings verified accurate, no harm from fixing either)
