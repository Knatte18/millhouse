## Fixed

- `20260729-144313-plan-review-r4.md`: "Debounced health-check pull can exceed `_ensure_daemon`'s 1.0s reuse-probe timeout" — verified: `_ensure_daemon()`'s reuse-probe (`wiki/_client.py:643-649`) uses a hardcoded `timeout=1.0` with `read_timeout=None`, which per `_connect_send_recv`'s implementation leaves the socket's 1.0s connect-timeout in effect for the response read too; `_dispatch()` calls `_ensure_daemon()` before every op, not just `health_check()`. Card 2's debounced pull (up to `_GIT_NETWORK_TIMEOUT_SECONDS`=30.0s, roughly once per 60s TTL window) would reliably blow through that 1.0s budget and cause the reuse-probe to spuriously read the daemon as unreachable, racing a redundant respawn on essentially every dispatched op in the codebase. Fixed by tagging the reuse-probe's payload with `{"liveness_only": True}` (Card 3, new edit to `_ensure_daemon()`) and adding an early-return in `_handle_health` (Card 2, new step -1) that returns today's unconditional `{FIELD_OK: True}` whenever that flag is set, before any of the new git-validity/pull logic runs. `health_check()`'s own dispatch is unaffected (empty payload, flag defaults falsy, gets the full check). Added test (f) to Card 4 covering both the server-side exemption and the client-side payload tagging.
- `20260729-144313-plan-review-r4.md`: "[NIT] `capsys` suggested for a non-pytest test harness" — verified `run-all.py` invokes test files as plain subprocesses (`subprocess.run([sys.executable, str(test)], ...)`), no pytest runner anywhere in `unit_tests/`, and `pytest` is not a declared dependency. Reworded Card 4's scenario (a) to redirect `sys.stderr` to an `io.StringIO()` instead of suggesting `capsys`.

## Pushed Back

(none — both findings verified accurate against source; both fixed.)
