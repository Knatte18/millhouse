MILL_REVIEW_BEGIN
# Review: Classify review GAPs by kind (design/scope/decision/consistency); scope discussion review to what downstream stages cannot catch — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude (Sonnet 4.5 family, best-effort self-assessment; distinct from the dictated reviewer_model)
reviewed_file: plan/
date: 2026-08-08
```

## Findings

### [BLOCKING:consistency] crash-recovery re-read site does not "feed the aggregation"
**Location:** 00-overview.md Shared Decision `ceiling-applied-once-at-write-time`; 03-plan-backend.md Card 14
**Issue:** Both the overview's Decision and Card 14 assert that `run`'s crash-recovery re-read block (the `if resume_round is not None:` branch, `_review_plan.py:759-798`) "feeds the same aggregation as the write-time entries." Source-checked: that branch builds `_disk_reviews` but never appends it to `reviews` (no `reviews.extend`/`reviews.append` anywhere in the branch — confirmed by grepping every `reviews.append` call site in the file). Its entries never reach the returned `ReviewResult`, so the stated rationale for adding `extract_findings`/`findings` there is false for this specific site.
**Fix:** Verify this site's actual behavior before asserting it feeds aggregation; either correct the Decision/Card 14 rationale to acknowledge the dead branch, or scope in fixing the pre-existing bug (`_disk_reviews` never merged into `reviews`) explicitly rather than implying it already works.

### [BLOCKING:consistency] Card 15's findings key breaks an untouched test via KeyError
**Location:** 03-plan-backend.md, Card 15 (`millpy-review-plan.py`, `--stage finalize` branch)
**Issue:** Card 15 adds `"findings": review_entry["findings"]` (direct key access) to the hand-built `result_dict` in `millpy-review-plan.py`'s finalize stage. `plugins/mill/unit_tests/test-review-plan-finalize-round.py` — not in this task's Files Touched/manifest — mocks `_review_plan.finalize` to return a plain dict lacking a `"findings"` key (lines 71-79, 128-136). After this change that code path raises `KeyError: 'findings'`, which is not a `ReviewError` and propagates uncaught, failing 2 of that file's 4 test cases (`--stage finalize` calls for plan). Batch 3's "Batch Tests" note excuses excluding this file on round-discovery grounds, but the actual break is in the finalize-stage dict construction this same file exercises via mocking, unrelated to round discovery, and batch 3's own `verify:` (only `test-review-plan-flow.py`) would not catch it.
**Fix:** Use `review_entry.get("findings", [])` instead of a direct key access, or add `test-review-plan-finalize-round.py` to this task's touched-files/context and update its fixtures to include `"findings"`.

### [NIT:consistency] Card 10 describes nonexistent test assertions and an inaccurate docstring
**Location:** 02-discussion-backend.md, Card 10
**Issue:** Card 10 says to "extend the existing key-set assertions" in `test-review-cli-error-envelope.py`, but that file has no key-set assertions today (only `verdict` checks) and never exercises `print_error_envelope`'s dict shape directly except incidentally via `test_plan_uncaught_exception`. Separately, the card's requested docstring text ("the envelope mirrors `ReviewResult.to_dict()`'s key set with zeroed counts") stays false after the change: `print_error_envelope`'s dict has no `nit_count` key at all (pre-existing, `_review_cli.py:32-38`), unlike `to_dict()`.
**Fix:** Reword to "add" rather than "extend" the assertions, and either add `"nit_count": 0` to the envelope or soften the docstring's key-set-parity claim.

## Verdict

REQUEST_CHANGES
Two source-grounded factual errors in batch 3 (dead-code aggregation claim; untouched test breaks via KeyError) need fixing.
MILL_REVIEW_END
