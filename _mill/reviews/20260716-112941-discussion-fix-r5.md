# Discussion fix report — round 5

```yaml
source_review: _mill/reviews/20260716-112905-discussion-review-r5.md
round: 5
```

## Fixed

- `20260716-112905-discussion-review-r5.md` — "[NOTE] cards_done int-vs-string JSON coercion unspecified": `completeness-recount-cards-done` now specifies `cards_done` entries are coerced to `int` before the `card_ids - set(cards_done)` comparison, and a coercion failure on any entry treats the field as absent (falls back to the count heuristic) rather than raising or partially trusting a malformed list. Testing section extended with a coercion test case.
- `20260716-112905-discussion-review-r5.md` — "[NOTE] \"affected package(s)\" resolution not stated for the retiering gate": `go-build-tag-retiering-check` now states the package-resolution mechanism explicitly — the immediate directory containing each transitioned file, deduplicated to one compile check per unique directory per batch.

## Pushed Back

(none)
