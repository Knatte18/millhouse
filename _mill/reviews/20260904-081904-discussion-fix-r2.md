# Discussion fix report — round 2

Source review: `_mill/reviews/20260904-081814-discussion-review-r2.md`

## Fixed

- `--approve` addition leaves usage-hint text unaddressed" — extended the #948 Decision to also update the frontmatter `argument-hint` and the Step 0.5 usage-halt string alongside the new `--approve` token, so both stay in sync with the accepted token set.
- `"Waive remaining BLOCKINGs" decision lacks its own Decision heading` — split the waive-instruction item out into its own `### #970 — waiving step 6 live` heading with its own Rationale/Rejected, matching the file's structural convention for every other decision.

## Pushed Back

(none)
