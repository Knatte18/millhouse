# Plan fix report — round 2

Source review: `_mill/reviews/20260812-085625-plan-review-r2.md`

## Fixed

- r2 "Batch 5 Card 20's self-reference enumeration is a non-exhaustive example list, not a full sweep" (BLOCKING:scope) — accurate and a genuine coverage hole: card 21 explicitly scopes itself to text *outside* `## Agent-mode dispatch`, so any in-section reference card 20 missed would be caught by no later card. Card 20 now mandates the same enumerate-then-classify method card 21 uses, applied to every `step ` + number occurrence inside the section (list markers, bold titles, and prose alike), states that the named sites are shape examples rather than a bound on the work, adds the one exception (citations into another skill's numbering), and closes with a concrete postcondition: no `step 7`, `step 6.5`, or `step 4(` may remain in the section.
- r2 "Cards 14-16/24's frontmatter rationale for skills-index exclusion is inaccurate but harmless" (NIT:design) — confirmed against the generator: `millpy-skills-index.py:100` collects inputs with `skills_dir.rglob("SKILL.md")`, an exact-filename match, so the companion files are excluded by filename and frontmatter is irrelevant to it. Card 14's rationale now cites the filename glob as the actual mechanism and keeps the no-frontmatter rule on its real basis (a `name:`/`description:` block marks an invocable skill, and these are fragments of one); cards 15 and 16 inherit it by reference. Card 24's invariant 1 reworded from a risk being managed to a confirmation that the generator still collects by filename, with the remedy re-pointed at the generator rather than at frontmatter.

## Pushed Back

None. Both findings were accurate and neither fix caused harm.
