# Discussion-fix round 3

Source review: `_mill/reviews/20260812-181453-discussion-review-r3.md` (verdict APPROVE, 0 BLOCKING, 1 NIT).

## Fixed

- **[NIT:decision] "error_kind top-level mirroring has no named code site"** — verified via
  direct read of `ReviewResult` (`_review_common.py:346-372`, fixed field set, no `error_kind`,
  `to_dict()` hard-codes output keys) and `millpy-review-plan.py`'s finalize-stage `result_dict`
  construction (lines 296-304, copies only specific named keys off `review_entry`, `error_kind`
  not among them). Dropped the ungrounded "mirrored at the top level" claim from the "error_kind
  field, not a new verdict value" Decision — the per-entry field is sufficient since the "Retry
  semantics keyed on error_kind" decision already reads `reviews[]` entries directly and never
  needed a top-level mirror.

## Pushed Back

(none)
