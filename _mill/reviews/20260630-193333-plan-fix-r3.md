# Plan-fix round 3

Source review: `_mill/reviews/20260630-193244-plan-review-r3.md` (verdict: APPROVE, blocking_count: 0, nit_count: 2)

## Fixed

- `_mill/reviews/20260630-193244-plan-review-r3.md` — "[NIT] Card 1 Context omits mill-go/SKILL.md despite citing its dispatch contract": added `plugins/mill/skills/mill-go/SKILL.md` to Batch 1 Card 1's `Context:`, and added a Requirements sentence clarifying that finalize also receives `--max-rounds` in Agent-mode (per mill-go/SKILL.md step 6's "same standard arguments" rule) and that this is harmless/inert since `--stage finalize` never reads the flag — no special-casing needed.
- `_mill/reviews/20260630-193244-plan-review-r3.md` — "[NIT] Card 4's task_data retry test under-specifies side_effect call count": corrected the `test_task_data_retries_on_cold_daemon()` description in Batch 2 Card 4 to account for `task_data()`'s 3 total `list_tasks_brief` calls (2 inside its internal `slug_from_branch()` call + 1 of its own), specifying a 3-item `side_effect` list (or unbounded equivalent) instead of the originally-described 2-item list that would have raised `StopIteration`.
- Also corrected a now-stale overview claim found while applying the above: the "each of the three gaps is an independent batch with no cross-batch dependency" Shared Decision in `00-overview.md` still said "No batch depends on another," which contradicted Batch 3's `depends-on: [2]` (added in plan-fix round 1). Reworded to describe the dependency as a same-file test-coordination artifact, not a logical one.

## Pushed Back

(none)
