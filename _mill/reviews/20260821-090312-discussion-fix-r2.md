# Discussion Fix — round 2

Source review: `_mill/reviews/20260821-090224-discussion-review-r2.md` (verdict: APPROVE, 2 NITs, 1 demoted from BLOCKING).

## Fixed

- `[NIT:consistency]` "Sole precedent" claim for millpy-bg pattern is false — corrected the `baseline-dispatch-background` Decision's "Precedent" bullet and the matching Technical context bullet to name `mill-plan/SKILL.md` as a second (more extensive) precedent alongside `mill-start/SKILL.md`'s subprocess/psmux branch, and to explicitly call for replicating the `pwd`/cwd-guard step that every `mill-plan/SKILL.md` `millpy-bg` call site pairs with the invocation.
- `[NIT:scope]` No verification plan for the SKILL.md dispatch-mechanism edit — added a Testing-section line stating the edit is prose/orchestration-instruction only, has no unit-testable surface of its own (the underlying `millpy-bg.py`/`_bg.check_bg_status` mechanics already have coverage from the call sites that established the pattern), and is verified via manual dry-run on the next real mill-go baseline pre-flight.

## Pushed Back

(none)
