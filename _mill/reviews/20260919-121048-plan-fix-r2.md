# Plan fix round 2

Source review: `_mill/reviews/20260919-120843-plan-review-r2.md`

## Fixed

- "New exemption guards mis-indented by 2 spaces" (NIT, demoted from BLOCKING) — re-indented all
  three new `continue` guard blocks in Batch 1, cards 1-3, from 22/26-space (comment/`if` / `continue`)
  to 20/24-space, matching the surrounding exemption chain's actual indentation
  (`_plan_validate.py` lines 2356-2361).
- "Cards 4/6 cite `_plan_validate.py` constants outside Context/Edits" (BLOCKING) — dropped the
  backtick-wrapped `_OWNERSHIP_VERB_FORMS`/`_OUTPUT_VERB_FORMS` constant-name mentions from Batch 2,
  cards 4 and 6's parentheticals, rewording each to describe the same guard in prose ("Batch 1 Card
  1's ownership verb-form table", "Batch 1 Card 3's output verb-form table") without citing a symbol
  defined in a file these cards neither `Context:` nor `Edits:`.
- "Card 4's 'last context-completeness test' claim is wrong" (NIT) — corrected the anchor from
  `test_check_context_completeness_dirty_quoted_material_prose_after_fence_closed` to the actual last
  such test, `test_check_context_completeness_dirty_escape_marker_phrase_removed`, in both the
  insertion-point and `main()`-registration instructions.

## Pushed Back

(none)
