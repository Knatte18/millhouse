# Plan fix r1

Source review: `_mill/reviews/20260517-132459-plan-review-r1.md` (verdict APPROVE, 0 BLOCKING, 3 NIT).

## Fixed

- `[NIT] Card 3 -- resolve_deps_as_names already imported` -- removed the "Update the imports" instruction in Batch 2 / Card 3; added a note that the import already exists at `_plan_validate.py:35`.
- `[NIT] Card 5 -- _collect_task_intent inline-form path extraction underspecified` -- clarified the regex shape to capture `(?P<inline>...)` and handle inline single-line bullets via `re.findall(r"\`([^\`]+)\`", inline)` before falling through to the sub-bullet scan; cited the existing `_parse_edits_only` pattern at `_plan_validate.py:114-119`.
- `[NIT] Card 9 -- resolve_git_root/resolve_wiki_path signatures placed after step 3 but used in step 1` -- moved the two new signature lines to immediately after the `_wiki.sync_pull` signature in Entry step 1 in both mill-plan and mill-start SKILL.md, where the functions are first invoked.

## Pushed Back

_(none)_
