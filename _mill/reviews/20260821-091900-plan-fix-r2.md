# Plan-fix round 2

Source review: `_mill/reviews/20260821-091715-plan-review-r2.md` (verdict APPROVE, blocking_count 0, 2 NIT, both demoted from BLOCKING).

## Fixed

- **[NIT:consistency, demoted] Card 1's quoted "current value" omits the frontmatter's trailing sentence** — confirmed against the actual on-disk `plugins/mill/skills/mill-go2/SKILL.md` line 3 that the `description:` field ends with "Invoked only by an explicit /mill-go2." Added that sentence verbatim to both Card 1's quoted current value and its replacement text, so the find/replace is an exact match and the sentence is preserved rather than silently dropped.
- **[NIT:consistency, demoted] Card 2 prescribes an unprecedented `Skill(plugin:name)` call syntax** — confirmed via repo-wide grep that no `SKILL.md` file uses a parenthesized `Skill(...)` call form; the established convention (used one line below this same section in `mill-go2/SKILL.md` itself) is prose: "Load the `plugin:skill-name` skill via the Skill tool." Rewrote Card 2's preload text to use that phrasing instead of inventing new call syntax.

## Pushed Back

(none — both findings were factually accurate; fixed per the mill-receiving-review default.)
