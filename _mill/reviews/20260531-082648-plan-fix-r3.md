# Plan Fix Report — Round 3

## Fixed

- `20260531-082448-plan-review-r3.md` "[BLOCKING] _layer_sort_key references row['id'] which is never added to rows" — Added `"id": home_tasks[slug].get("id") if slug in home_tasks else None` alongside `"layer"` in `_build_rows`. Changed `_layer_sort_key` to use `row.get("id") or 0` instead of `row["id"]`.
- `20260531-082448-plan-review-r3.md` "[NIT] Card 11 does not enumerate which existing tests need updates" — Extended the final paragraph of Card 11 Requirements to enumerate each affected test (2, 3, 4, 12, 13) with specific schema replacements and assertion updates.
- `20260531-082448-plan-review-r3.md` "[NIT] Card 15(e) migration test incompatible with post-Batch-2 upsert_task" — Added explicit instruction to insert via `wiki_server._store._db.insert({...})` bypassing `upsert_task` validation.
- `20260531-082448-plan-review-r3.md` "[NIT] render_order(unmarked) vs. server-computed layer field in extended_title" — Changed Card 24 to call `render_order(tasks)` on the full task list and filter to unmarked in the result order, ensuring consistent layer assignment with the server's full-set computation.

## Pushed Back

(none)
