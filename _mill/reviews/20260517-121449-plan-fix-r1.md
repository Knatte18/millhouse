# plan-fix round 1 — mill-branch-slug-fixes

Source review: `_mill/reviews/20260517-121244-plan-review-r1.md` (verdict: REQUEST_CHANGES, 2 BLOCKING + 1 NIT)

## Fixed

- [BLOCKING] Wrong `_config.load_config` call in bg launcher validation (Batch 4 / Card 8) — Card 8's Requirements now explicitly states the correct argument order: `_config.load_config(Path(git_root), Path(git_root))`. Added an argument-order note explaining why passing `wiki_path` as `repo_root` (the discussion's sketch error) would weaken D1's slug fallback and defeat D7's intent.
- [BLOCKING] Card 9 test fixture layout incompatible with `resolve_wiki_path` (Batch 4 / Card 9) — Card 9's Requirements now mandates container-form layout (`tmp/wts/<name>/` + `tmp/wiki/`) for every test. Added a "Critical fixture layout constraint" sub-paragraph explaining why the existing `_test_helpers._make_task_worktree` layout (`tmp/worktree` + `tmp/wiki`) cannot be used as-is, and authorised an inline test-local `_make_container_form_worktree` helper. Tests rewritten to use subprocess invocation with cross-platform no-op (`sys.executable -c "pass"`).
- [NIT] `FileNotFoundError` from `slug_from_branch` not caught in launcher (Batch 4 / Card 8) — Card 8 widened the second `except` clause from `(ValueError, SystemExit)` to `(ValueError, SystemExit, OSError)` and added rationale tying it to D1's Home.md-read-up-front control flow.

## Pushed Back

(none)
