# Plan fix — round 1

```yaml
task: mill-plan/review validation false-positives, hard-fails, and truncated failure reasons
slug: mill-plan-review-validation-gaps
round: 1
source_review: _mill/reviews/20260729-133942-plan-review-r1.md
verdict: REQUEST_CHANGES (5 BLOCKING, 2 NIT)
```

## Fixed

- "Card 7: candidates->tuples refactor leaves the hit-found append broken" — `_mill/reviews/20260729-133942-plan-review-r1.md`: `[BLOCKING] Card 7: candidates->tuples refactor leaves the hit-found append broken`. Added an explicit instruction to Batch 2 / Card 7's Requirements to change `resolved.append(hit)` to `resolved.append(hit[0])` after the candidates-to-tuples refactor, since `hit` is now a `(candidate, source_root)` pair and `resolved` must stay `list[Path]`.
- "Card 8: all_raw_refs removal leaves moves_targets_on_disk dangling" — `_mill/reviews/20260729-133942-plan-review-r1.md`: `[BLOCKING] Card 8: all_raw_refs removal leaves moves_targets_on_disk dangling`. Verified against `_review_code.py`'s `prepare()`: `moves_targets_on_disk`'s list comprehension also references the now-removed `all_raw_refs` name. Rewrote Batch 2 / Card 8's Requirements to instruct updating both remaining `all_raw_refs` reference sites (`moves_targets_on_disk`'s filter and `ancestors_on_disk`'s filter), not just `ancestors_on_disk`.
- "Card 3: transcribed self-run call omits wiki_root/skip_checks, miscounts kwargs" — `_mill/reviews/20260729-133942-plan-review-r1.md`: `[BLOCKING] Card 3: transcribed self-run call omits wiki_root/skip_checks, miscounts kwargs`. Verified: `wiki_root=wiki_root` was in fact already present in Card 3's transcribed call (the finding's claim on that point was inaccurate), but `skip_checks=` was genuinely missing and the "six keyword arguments" count undercounted the real gate's seven. Added `skip_checks=frozenset()` to the call (the self-run has no CLI `--skip-checks` flag to source from, so it passes the same default `_plan_validate.run` itself uses) and corrected the count/kwarg list to seven.
- "Card 4: new check hardcodes root/git_root/wiki_root to None" — `_mill/reviews/20260729-133942-plan-review-r1.md`: `[BLOCKING] Card 4: new check hardcodes root/git_root/wiki_root to None`. Verified against `run()`'s registration pattern for every sibling check: confirmed the new function must accept and thread `root`/`wiki_root`/`git_root` like `_check_non_existent_path`/`_check_move_source_missing`/`_check_batch_oversized` do, not hardcode `None`. Rewrote Card 4's function signature and step 3's `resolve_existing_paths` call to thread the parameters, and its `run()` registration call to pass `effective_root`/`git_root`/`wiki_root`.
- "Cards 2, 6, 10: new regression tests are never wired into their file's runner" — `_mill/reviews/20260729-133942-plan-review-r1.md`: `[BLOCKING] Cards 2, 6, 10: new regression tests are never wired into their file's runner`. Verified: `test-plan-validate.py::main()` gates on an explicit `tests = [...]` list; `test-review-code-flow.py::main()` gates on explicit `errors += test_xxx()` calls. Added an explicit wiring instruction to Cards 2, 6 (append to `test-plan-validate.py`'s `tests` list) and Card 10 (add `errors += test_context_only_gitignored_ref_soft_fails_prepare()` to `test-review-code-flow.py::main()`).
- "Card 9: tempfile.TemporaryDirectory convention claim is wrong for this file" (NIT) — `_mill/reviews/20260729-133942-plan-review-r1.md`: `[NIT] Card 9: "tempfile.TemporaryDirectory" convention claim is wrong for this file`. Verified: `test-review-common.py` exclusively uses `_test_helpers.safe_temp_dir()` (131 uses, zero `tempfile.TemporaryDirectory`). Corrected Card 9's Requirements to specify `_test_helpers.safe_temp_dir()`.
- "Card 10: mischaracterizes _make_fixture's field placement" (NIT) — `_mill/reviews/20260729-133942-plan-review-r1.md`: `[NIT] Card 10: mischaracterizes \`_make_fixture\`'s field placement`. Verified against `_make_batch_file` in `test-review-code-flow.py`: the `reads` parameter maps to `Context:`, and `Edits:` is hardcoded to `"none"`. Corrected Card 10's description from "Edits:-listed" to "Context:-listed".

## Pushed Back

(none)
