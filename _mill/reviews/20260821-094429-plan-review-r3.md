MILL_REVIEW_BEGIN
# Review: mill-go: baseline-stage timeout/cold-build cost and finalize dirty-tree false positive — holistic

```yaml
duration_s: 363.4
verdict: APPROVE
reviewer_model: sonnethigh
reviewer_self_id: claude-sonnet-4-6 (high effort)
reviewed_file: plan/
date: 2026-08-21
```

## Findings

None. All quoted source blocks, line-number citations, function signatures, and helper-reuse claims across all three batches were verified against the actual files and match exactly:

- Batch 1 (Card 1): `_in_scope_dirty_stuck`'s `owned_paths` block at `_implementer_common.py:461-465` matches the quoted "before" text verbatim; the docstring anchor paragraph ("Unlike `_cleanliness.compute_terminal_dirt`...that would reintroduce the false-block this function exists to avoid.") matches exactly.
- Batch 1 (Card 2): insertion point (immediately after Case 75's closing `errors += 1` at line 5176, before `if errors:` at 5178) confirmed exact; `_forward_output`'s keyword signature (`start_sha`, `verify_cmd`, `task_dir`, `parent_branch`) and `_setup_fixture`/`_capture_stdout` fixture shapes confirmed to match Card 2's prescribed usage.
- Batch 2 (Card 3): `_run_baseline_stage` signature (216-223), the Case-B shared-setup try/except boundary (357-390) and the call site at 644-647 all match verbatim; `_verify_baseline._run_verify_in`'s signature and no-checkout/no-teardown contract confirmed; the shared-decision to reuse it directly (no new wrapper) is consistent with the file's own established pattern of calling `_checkout_parent_branch`/`_link_dependency_dirs`/`_run_module_wide_verify_algorithm` directly.
- Batch 2 (Card 4): `pipeline:` block key ordering divergence between the template (124: `done_gate_baseline_preflight` before `max_cards_per_batch`) and this hub's own config (17/23: reversed) is real — the card's instruction to anchor only on `done_gate_baseline_preflight` is correct. The `wiki-config-mutation` validator's condition (a)/(b) text and the "key addition never satisfies (b)" rule were verified against `_plan_validate.py` and `mill-plan/SKILL.md`'s own fix-table row; Card 4's bootstrap justification correctly invokes condition (a).
- Batch 2 (Card 5): insertion point, `_write_two_batch_fixture`/`mock_load_config`/`_run_main` helper shapes, and all patched attribute paths (`millpy_implement._verify_baseline.*`, `._parent_branch.resolve`, `._worktree.remove_safe`) confirmed to exist and match the file's own preceding-test style.
- Batch 3 (Card 6): every quoted "before" bash block and paragraph in `mill-go-base/SKILL.md` (0.5's dispatch block/timeout paragraph, 0.6's dispatch block/cache-form-exception paragraph/failure-handling sentence) matches the live file byte-for-byte at the cited locations. `_bg.py`'s `_worker_main` confirmed to invoke `subprocess.run(cmd, ...)` with no `shell=True` and no `env=` override, which correctly grounds the claim that the inner `env PYTHONPATH=...` wrapper is functionally required (not cosmetic) rather than being a fabricated platform claim. `_bg.check_bg_status`'s three-state return contract (`"running"`/`"exit"`/`"dead"`) and the `mill-start/SKILL.md` precedent for the identical liveness-check idiom (lines 269-271, 316-320) both confirm Card 6 is reusing an established pattern, not inventing one.

Batch Index DAG has no cycles, all `depends-on: []` with zero file overlap across batches, `## All Files Touched` is the exact 7-file union of all `Edits:` lists, and every card carries non-empty `Creates`/`Edits`/`Context`/`Requirements`/`Commit` with `Moves: none` throughout (no Rename mechanic section required).

## Verdict

APPROVE
Plan is fully source-grounded across all three batches; no blocking or nit findings.
MILL_REVIEW_END
