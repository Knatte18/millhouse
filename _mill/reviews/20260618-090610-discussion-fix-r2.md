# Discussion fix — round 2

```yaml
review_file: _mill/reviews/20260618-090534-discussion-review-r2.md
verdict: APPROVE
fixed: 2
pushed_back: 0
```

## Fixed

- `20260618-090534-discussion-review-r2.md` [NOTE] "Drift-guard false-positive surface beyond subpackages" — added a "Matching precision" point to Decisions › drift-guard scope: resolver matches module-level `def`s only; underscore-prefixed locals / dunder method calls are expected non-module matches the implementer curates into the allowlist (not drift failures).
- `20260618-090534-discussion-review-r2.md` [NOTE] "mill-merge in-place mode interacts with hub-resolution change" — added an in-place-mode caveat to Decisions › hub-resolution-helper: `1.5 Path Setup` runs for both modes; in in-place mode there is no `wts/<slug>` worktree, so the plan MUST verify `resolve_active_hub` returns the correct hub or branch to a mode-appropriate resolver.

## Pushed Back

(none — both findings were factually accurate and the fixes carry no harm.)
