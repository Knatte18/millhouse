# Review: Replace manual layer letters with depends_on + isolated flags — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-31
```

## Findings

### [BLOCKING] Test 8 sidebar assertion breaks after Card 14 rename
**Location:** Batch 3, Card 11
**Issue:** After Card 14, active tasks get `[A]` appended to `display_title` (e.g. `**#000:** Has Proposal [A]`), so the sidebar entry becomes `"- [**#000:** Has Proposal [A]](proposal-with-body.md)"`. Test 8's existing assertion `"- [**#000:** Has Proposal](proposal-with-body.md)"` is NOT a substring of this — the `](` sequence becomes ` [A]](` — causing a verify failure in Batch 3.
**Fix:** Card 11 must add test 8 to the "update existing tests" list; change the sidebar assertion to `"- [**#000:** Has Proposal [A]](proposal-with-body.md)"`.

### [NIT] ERR_VALIDATION missing from Card 20 import list
**Location:** Batch 4, Card 20
**Issue:** Card 20's requirements list `OP_SET_DEPS`, `OP_MIGRATE_DEPS`, `WikiValidationError` for import, but `set_deps` also checks `if error_type == ERR_VALIDATION` — requiring `ERR_VALIDATION` in Card 20. Card 21 then says "already added in Card 20" for `ERR_VALIDATION`, contradicting Card 20's omission.
**Fix:** Add `ERR_VALIDATION` to Card 20's import list.

### [NIT] Test 3 update leaves assertions unspecified
**Location:** Batch 3, Card 11
**Issue:** The plan replaces test 3's `group`-based dicts with a three-task chain (A→B→C), making all three layers present. But the original `assert b_pos == -1` assertion (the test's whole premise) is now invalid, and Card 11 doesn't specify what replacement assertions to write. An implementer must guess.
**Fix:** Explicitly state the new assertions for test 3 (e.g. `assert a_pos < b_pos < c_pos`) or redirect to new test (k) which already covers the "empty sections skipped" case.

## Verdict

REQUEST_CHANGES
One blocking failure: test 8 sidebar assertion breaks under the new display-title format.