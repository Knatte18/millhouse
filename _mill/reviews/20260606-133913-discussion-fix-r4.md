# Discussion fixer report -- round 4

```yaml
task: subprocess-to-agents
round: 4
source_review: 20260606-13xxxx-discussion-review-r4.md (APPROVE, 2 NOTEs)
```

## Fixed

- [NOTE] "Large-prompt switch needs rendered prompt first" (review-r4) --
  added a sentence to Decisions/model-and-effort: the
  `maybe_switch_spec_for_large_prompt` switch is computed from the rendered
  brief size and fires for holistic reviews only, so `prepare` must render the
  brief before finalizing the reviewer model.

## Pushed Back

- [NOTE] "Plugin agents declaration unverified in manifest" (review-r4) -- no
  edit needed; the reviewer itself marked it "None required". Decisions/
  subagent-types already instructs the plan to "confirm the exact
  plugin-manifest field/dir name against the installed plugin layout". The NOTE
  records a genuinely-open implementation detail for the plan stage, not a gap
  in the discussion. Adding a guessed manifest field now would be speculation.
