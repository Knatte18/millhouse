# Plan fixer report — round 1

```yaml
slug: mill-review-and-verify-quality
round: 1
source_review: _mill/reviews/20260623-085127-plan-review-r1.md
verdict_addressed: REQUEST_CHANGES (3 BLOCKING, 2 NIT)
```

## Fixed

- `20260623-085127-plan-review-r1.md` — "[BLOCKING] Card 4 nits-fixed marker cannot hook the success print": rewrote Card 4 to thread `nits_only`/`status_path`/`nits_scope` into `_forward_output`/`finalize_from_output` (the single success-emit point), so the marker write + `nits_applied` injection no longer rely on `main()` seeing the status. Card 4 now also edits `_implementer_common.py`.
- `20260623-085127-plan-review-r1.md` — "[BLOCKING] Card 4/Card 8 ignore the --stage finalize (agent) path": Card 4 now passes the nit args in BOTH the `--stage finalize` branch and the full-stage dispatch; Card 8 now derives the holistic `verify_cmd` in the `--stage finalize` branch (lines 192-213) as well as the full-stage holistic path, so both fire under `dispatch == agent`.
- `20260623-085127-plan-review-r1.md` — "[BLOCKING] Cards 11-12 don't thread prior_notes through run()": Card 11 now passes `args.prior_notes` to both the `prepare()` and `run()` calls; Card 12 now adds `prior_notes` to `_review_code.run()` and forwards it to the internal `prepare()` call.
- `20260623-085127-plan-review-r1.md` — "[NIT] Card 4 re-resolves status_path instead of reusing the computed one": Card 4 now reuses the existing `status_path = _paths.status_path(project_root, cfg)` (line 146) instead of a hard-coded `"_mill/status.md"`.
- `20260623-085127-plan-review-r1.md` — "[NIT] Batch-2 verify lists test-nit-gate.py before its creating card runs": added a Batch Tests note that `verify:` runs only at batch completion, after every card, so `test-nit-gate.py` always exists when `run-all.py --only` names it.

## Pushed Back

(none)
