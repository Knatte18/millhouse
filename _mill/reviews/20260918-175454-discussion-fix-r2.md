# Discussion-fix round 2

Source review: `_mill/reviews/20260918-175424-discussion-review-r2.md` (verdict APPROVE, 2 NIT).

## Fixed

- "Go const/var-block tracking contradicts Python's rejected mini-parser" — added an explicit justification to the `declaration-form-regex` Decision's Go bullet: const/var-group tracking is a single bounded open/unclosed-group boolean (one paren-depth counter, incremented/decremented per line), not the general enclosing-scope (class/def) tracking the Python bullet's class-body-attribute residual gap declines to add. The two bullets now rest on the same rule: a bounded single-level state counter is in scope, unbounded enclosing-scope tracking is not.
- "Exemption line-range citation includes code outside the cited range" — widened the Technical context citation from `lines 2340-2359` to `quoted-material at lines 2317-2327, then ... lines 2340-2359`, matching where the quoted-material exemption is actually implemented in `_plan_validate.py`.

## Pushed Back

(none)
