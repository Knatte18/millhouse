# Plan fixer report — round 1

```yaml
slug: mill-merge-pr-state-awareness
round: 1
source_review: _mill/reviews/20260630-144347-plan-review-r1.md
verdict_in: REQUEST_CHANGES (1 BLOCKING, 1 NIT)
```

## Fixed

- r1 [BLOCKING] "closed route from pr-pending lacks commit-message source" —
  Card 3's `closed` route now requires the gate to establish
  `cached_task`/`cached_task_description` before continuing to Step 5: read from
  `status.md` when present, else derive from the wiki via
  `_client.get_task(wiki_path, slug)["title"]` (description falls back to title).
  This defines the squash commit message for the pr-pending+closed visibility
  workflow where `status.md` is absent.
- r1 [NIT] "`_paths` not in Context" — added `plugins/mill/scripts/_paths.py` to
  Card 3 `Context:` since the gate snippet calls `_paths.resolve_git_root()`.

## Pushed Back

- None. Both findings were factually accurate and caused no harm to fix.
