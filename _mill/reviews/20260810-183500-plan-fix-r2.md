## Fixed

- `20260810-182852-plan-review-r2.md` — "Cards 3/4 omit the raw-refs-preservation caveat Cards 1/2 include, for an identical hazard": Cards 3 and 4 now carry the same downstream-reuse caveat Cards 1/2 already had — `all_raw_refs` is read twice more in each holistic branch (`all_creates_on_disk`, `holistic_moves_on_disk` / `run_hol_moves_on_disk` construction), and a literal deletion of the dict without a replacement would raise `NameError` there.
- `20260810-182852-plan-review-r2.md` — "Cards 1/2's own caveat covers `raw_refs_set` but not the bare `raw_refs` list also read downstream": Cards 1 and 2's Requirements now explicitly name both `raw_refs` and `raw_refs_set` in the remedy, citing the exact downstream line (`ancestors_on_disk = ... if raw not in raw_refs`) and noting the two names are interchangeable for that membership check.
- `20260810-182852-plan-review-r2.md` — "Card 5 mischaracterizes test-review-plan-flow.py's existing structure": corrected — the file does have one discrete `def test_*` function (`test_project_root_rebind_uses_resolve_active_hub_not_resolve_hub_path` at line 2546), it is the outlier against the file's dominant inline-block convention, not proof no such function exists. Card 5 now says to follow the dominant convention rather than asserting an absolute rule.
- `20260810-182852-plan-review-r2.md` — "Card 9's \"bootstrap card for both Card 9 and Card 10\" framing overstates Card 9's own exposure to the validator check": confirmed against `_check_wiki_config_mutation`'s exact-string `"mill-config.yaml" in writes` test — Card 9's `Edits:` token is a full path under `templates/` and never matches; only Card 10's bare `mill-config.yaml` token does. Reframed both cards' bootstrap-justification notes accordingly (Card 10 is where the check actually fires; Card 9's note is now explicitly defensive/documentation-only).

## Pushed Back

(none — all four findings were factually accurate; a stray literal path reference introduced while fixing the last one was caught and corrected by a validator re-run before committing, not pushed back)
