# Plan fix — round 4

Source: `_mill/reviews/20260904-090958-plan-review-r4.md`

## Fixed

- [BLOCKING:scope] "Card 3 Requirements cite functions from files absent from Context" — added
  `plugins/mill/scripts/_junction.py` and `plugins/mill/scripts/_verify_baseline.py` to Card 3's
  Context (batch 2), matching the `_junction.strip_all_in_worktree` behavior claim and the
  `_verify_baseline.compute_baseline` call-shape precedent already cited in its Requirements.
- [BLOCKING:scope] "Card 9 Requirements name a new symbol from a file absent from Context" — added
  `plugins/mill/scripts/_implementer_common.py` to Card 9's Context (batch 4), so the implementer can
  confirm Card 7's new `_corroborate_batch_failure` symbol and `_run_verify_gates`'s new
  `start_sha`/`status_path`/`batch_name` kwargs against the actual landed code.

## Pushed Back

(none)
