# Plan fix — round 1

Source review: `_mill/reviews/20260918-180102-plan-review-r1.md` (verdict APPROVE, 2 NITs, 0
BLOCKINGs).

## Fixed

- **"Card 1's 'preceding paragraph' citation is wrong"** — confirmed accurate: the text
  immediately preceding the deleted sentence in `review-discussion.md` is the `Decisions` criteria
  bullet, not `Class governs who decides…` (which lives ~55 lines later). Reworded Card 1's
  Requirements to cite "the `Decisions` criteria bullet" instead.
- **"Card 4's 'directly below reviewer_model' citation is wrong"** — confirmed accurate: in
  `review-output.schema.md`'s `## File format` example, `reviewer_self_id:` sits directly below
  `cost_usd:`, three lines after `reviewer_model:` (duration_s/tool_calls/cost_usd intervene).
  Reworded Card 4 sub-item 1 to cite "directly below `cost_usd: <reported dollar cost of the
  round>`, three lines after `reviewer_model: <reviewer name from config, e.g. sonnetmax>`".

Both findings were documentation-accuracy issues only — the actual line to delete in each case was
already uniquely identified by its own quoted text, so neither fix changes what the implementer
deletes, only the (wrong) landmark description pointing at it.

## Pushed Back

(none)
