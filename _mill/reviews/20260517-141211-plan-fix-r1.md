# Plan fix -- round 1

Source review: `20260517-141111-plan-review-r1.md` (verdict: APPROVE, 3 NIT findings).

## Fixed

- "[NIT] Card 4 claim 'exactly one parse_verdict invocation' is misleading" -- rewrote Card 4 in `02-backend-error-envelope.md` to state TWO invocations (lines `_review_code.py:336` and `_review_code.py:374`) and to instruct the implementer to wrap both with a single outer try/except.
- "[NIT] Card 2 guard test: positional-arg check described incorrectly" -- dropped the broken kwargs-based guard test from Card 2 in `01-review-common.md`. The frozen-clock test is sufficient to lock UTC behaviour.
- "[NIT] Card 10 YAML indentation example off by 2 spaces" -- corrected the indentation example in Card 10 of `04-mill-go-config.md` from 8 spaces to 6 spaces and added a sanity-check instruction (vertical alignment with the existing `reviewer:` key).

## Pushed Back

(none)
