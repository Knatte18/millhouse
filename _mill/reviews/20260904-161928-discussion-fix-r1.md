# Discussion fix — round 1

Source review: `_mill/reviews/20260904-161843-discussion-review-r1.md`

## Fixed

- r1 NIT (demoted-from BLOCKING) "Testing item 7's four-site labels don't match Technical Context's four sites" — Testing item 7 rewritten to name the four canonical `_review_plan.py` sites (batch-mode 232, second batch-mode 505, holistic 614, run-holistic 1045), stating explicitly that they live in two different functions (`_review_one_batch` line 134, `prepare()` 382-661) and are not bulk/tool-use variants of one site; bulk/tool-use mode coverage folded in as an additional axis.
- r1 NIT "`_review_code.py` function is `_build_artefact_section` (private), not `build_artefact_section`" — corrected in Scope and Technical context; added the definition line (136) and call site (349).
- r1 NIT "\"six sites\" count doesn't match the five enumerated `build_manifest_section` call sites" — both occurrences corrected to five, with the 4+1 split spelled out.

## Pushed Back

(none)
