```yaml
type: plan-fix
round: 1
source_review: _mill/reviews/20260716-111441-plan-review-r1.md
date: 2026-07-16
```

## Fixed

- "Card 3 gate `except Exception` misses SystemExit" — changed Card 3's (`01-psmux-cleanup-crash.md`)
  Requirements from `except Exception: pass` to `except (Exception, SystemExit): pass`, mirroring
  `_get_via_psmux_flag`'s own `except (Exception, SystemExit): return False`, since
  `_paths.resolve_git_root()` raises `SystemExit` on cwd-outside-git-worktree — the exact
  resolution-failure scenario the gate must fall through on, and the exact scenario Card 4's
  K5(vii) test exercises.

## Pushed Back

(none)
