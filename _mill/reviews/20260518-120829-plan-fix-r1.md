# Plan-fix report: round 1

```yaml
slug: psmux-session-keepalive
round: 1
source_review: 20260518-120605-plan-review-r1.md
```

## Fixed

- `20260518-120605-plan-review-r1.md`: "[BLOCKING] Card 9 derives psmux_name unconditionally, contradicting Test K2" — Card 9 (c) now reads `psmux_name = f"mill-{session_id[:12]}" if caller_provided_session_id else None`; both `psmux_session_name` and `keep_alive` gate on the same `caller_provided_session_id` boolean. The Shared Decision "psmux-session-name derivation" in 00-overview.md is updated to document the refinement: the deterministic name applies only on the keepalive path (auto-gen path falls back to the wrapper's existing `mill-<uuid8>` auto-name). K2 now matches the implementation as planned.
- `20260518-120605-plan-review-r1.md`: "[NIT] Test S5 omits required `_wait_for_marker_in_pane` mock setup" — S5's description now explicitly states `_wait_for_marker_in_pane` is mocked to return True so the failure fires at the post-claude-launch idle step (where the wrapper owns the session) instead of the CLAUDE_READY step. Card 7's per-test-file setup paragraph was also updated to default both `_wait_for_*` mocks to True unless a test overrides one — this protects S4, S6, and S7 (other tests that go through the create-session happy path) from the same latent issue.

## Pushed Back

(none)
