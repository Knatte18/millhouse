# Plan-fix round 1

Source review: `_mill/reviews/20260918-181042-plan-review-r1.md` (verdict APPROVE, 1 NIT).

## Fixed

- "Card 2's 'one call site' framing conflates two separate code regions" — rewrote batch `resolve-symbol-engine` card 2's Requirements to describe the shape-gate call (`if not is_path_shaped:`, before the shared exemption checks) and the later resolution `else:` arm (`search_cache` lookup / `_resolve_symbol_files` call / `len(matches) != 1` guard / canonicalization) as two distinct edit sites, rather than one contiguous "arm" — matches how they actually sit in `_plan_validate.py` (separated by the path branch's own `if is_path_shaped:` arm in between).

## Pushed Back

(none)
