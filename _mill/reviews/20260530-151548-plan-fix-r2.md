# Plan-fix report — round 2

Source review: `20260530-151453-plan-review-r2.md` (holistic, APPROVE, 0 BLOCKING + 4 NIT)

## Fixed

- [NIT] "Card 8 omits ERROR-only retry poll sites in mill-go" (Batch 3, Card 8)
  — added the per-batch ERROR-only retry (step 4.5) and the holistic ERROR-only
  retry (step 3.5) to Card 8's enumeration of poll sites that receive the
  per-iteration `check_bg_status` liveness check, matching Card 9's coverage of
  mill-start's retry loop.
- [NIT] "Card 3 timeout value unspecified" (Batch 1, Card 3) — specified the
  call as `wait_for_socket_reachable(state["host"], state["port"],
  timeout=deadline - time.monotonic())` (the remaining `SPAWN_TIMEOUT` budget
  from the outer loop's `deadline`), removing the `timeout=...` placeholder.
- [NIT] "Card 4 test (g) no platform guard" (Batch 1, Card 4) — replaced the
  direct assertion with a platform-guarded one: `assert == 20` on win32 else
  `assert == 10`, green on both the Windows target and Linux CI.
- [NIT] "Card 6 missing-log early return not sequenced" (Batch 2, Card 6) —
  added step (0): `if not log_path.exists(): return ("dead", None)` as the
  explicit first step before any `read_text`, mirroring `is_bg_worker_alive`.

## Pushed Back

(none — all NITs accepted and fixed)
