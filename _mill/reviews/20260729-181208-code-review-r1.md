MILL_REVIEW_BEGIN
# Review: Self-discovered mill pipeline bugs: silent archive-tag push failure, ignored --max-rounds override, dead test-registry helper, truncated commit_sha in implementer reports — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-07-29
```

All four independent batches were verified against their cards and cross-checked for integration correctness.

- **Batch 1** — `_archive_tag.py` captures every `git push` result across all four action branches (`created`, `noop`, `force_update`, `moved_aside`) and returns `push_failed`/`push_error` per the Card 1 spec, including the correct partial/combined `moved_aside` error-naming logic. `mill-merge/SKILL.md` Step 6 reads both fields with `.get()` (not subscript) as required, non-halting. `test-archive-tag-conflict.py`'s new `_init_bare_remote` fixture and six new tests match the Card 3 spec exactly (pre-receive hook, per-action coverage, partial/combined failure assertions).
- **Batch 2** — `_review_plan.py:730` now reads `holistic_max_rounds` (computed at line 676) instead of the raw config value, mirroring `_review_discussion.py`'s sibling check; the untouched `reviewer_override` gate (line 718) and `batch`-rounds gate (line 711) are confirmed unchanged. Test 33 in `test-review-plan-flow.py` reproduces the #740 regression scenario and asserts the fix.
- **Batch 3** — `_test_registry.py`'s new `_write_registry_file` is shared correctly: `write_to` builds via `make_minimal_registry` + delegates; `_test_helpers.write_local_overlay` delegates with raw verbatim entries, preserving its 13 existing call sites' behavior (7 in test-review-plan-flow.py, 6 in test-review-discussion-flow.py, confirmed via grep). The stale `plugins/mill/scripts/_test_registry.py` shadow copy is confirmed deleted. `test-reviewers.py`'s new round-trip test is registered in the correct position in `main()`'s list. The 11 no-op `write_to(wiki_root)` call sites (2 + 1 + 7 + 1, matching the plan's breakdown) are confirmed untouched and still use `test_stub`.
- **Batch 4** — `_implementer_common.py` adds `_COMMIT_SHA_RE`/`_is_valid_commit_sha` and guards the success-path override at line 1660 exactly as specified, replacing the unconditional passthrough with a validated stuck envelope on failure. Cases 68–70 in `test-implementer-common.py` cover the abbreviated-SHA correction, the corrective-failure non-passthrough, and direct validator coverage, matching the plan's assertions (including the `"abc" not in captured` leak check). All four brief templates gained the identical tightening sentence at the specified insertion points without disturbing the `<last-HEAD-sha>`/`<HEAD-sha, unchanged...>` placeholder tokens.

No out-of-plan files, no cross-batch contract mismatches, no duplicated utilities, and no deviations from the Shared Decisions were found.

## Verdict

APPROVE
All four batches faithfully implement their plan cards with correct cross-batch consistency and no regressions.
MILL_REVIEW_END
