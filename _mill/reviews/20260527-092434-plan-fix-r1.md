# Plan fix report — round 1

```yaml
slug: hub-relative-path-support
round: 1
source_review: 20260527-092333-plan-review-r1.md
```

## Fixed

- `[NIT] Card 6 may be no-op — verify and document` — refined card 6's Requirements to spell out the exact no-op commit-message wording and to cite the existing test-paths.py lines (633-686 in the M1+sub and M2+sub blocks) that already lock in `resolve_active_hub` sub-project behaviour. The card now produces a deterministic commit message in both the no-op and additive branches.

## Pushed Back

- `[NIT] Line numbers in Cards 9/10 must be re-verified at implementation time` — the reviewer themselves notes "no plan change needed" because the plan already mandates the re-grep + halt-on-drift behaviour ("Re-grep `resolve_ref_paths` and `resolve_existing_paths` in `_review_code.py` and assert ... If counts differ, halt and surface the discrepancy"). No edit applied. This is not a forbidden-dismissal pushback: the existing plan already encodes the requested behaviour; further redundancy would clutter the cards.
