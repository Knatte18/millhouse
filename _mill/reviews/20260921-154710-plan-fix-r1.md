# Plan-fix round 1

## Fixed

- `_mill/reviews/20260921-154502-plan-review-r1.md` — "[BLOCKING:design] `_check_cross_batch_build_break`'s ancestor test is inverted": inverted the exemption condition in `04-cross-batch-build-break.md` card 13 from `if other_name in ancestors.get(renaming_batch, set())` to `if renaming_batch in ancestors.get(other_name, set())`, matching `_check_cross_batch_creates_no_depends_on`'s established direction. Also corrected the function's own docstring paragraph describing this exemption, which described the same wrong direction.
- `_mill/reviews/20260921-154502-plan-review-r1.md` — "[NIT:consistency] Module docstring's check-key list omits the new check": card 13 now also adds a `cross-batch-build-break` bullet to `_plan_validate.py`'s top-of-file "Checks performed (check keys):" docstring list, not just `run()`'s own inline docstring sentence.
- `_mill/reviews/20260921-154502-plan-review-r1.md` — "[NIT:consistency] Card 14's cited `main()` shape doesn't match the file it claims to mirror": corrected card 14's text to state plainly that `test-plan-validate-card-numbering.py`'s actual `main()` does NOT use a `tests`-list iteration shape, and to instruct writing the list-iteration shape directly rather than citing that file as a mirror for it.

## Pushed Back

(none)
