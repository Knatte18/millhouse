# Plan Fix: round 1

## Fixed

- `20260518-070705-plan-review-r1.md` — "[BLOCKING] Card 8 plan-review guard unreachable; Card 11 test will fail": rewrote Card 8 to clarify the guard fires for the `max_rounds kwarg=0` path (not config-rounds=0, which already skips via holistic_spec=None); rewrote Card 11 plan-review test fixture to use `max_rounds=0` kwarg with batch reviewer=None and holistic reviewer=test_stub, eliminating the failing `r.round == 0` assertion.
- `20260518-070705-plan-review-r1.md` — "[BLOCKING] Card 15 Fix 1 passes wrong second argument to `_config.load_config`": changed instruction from `_config.load_config(worktree_root, worktree_root / ".millhouse")` to `_config.load_config(worktree_root, worktree_root)`; added note that the function internally appends `.millhouse/config.local.yaml` to the second arg.
- `20260518-070705-plan-review-r1.md` — "[NIT] Card 1 produces duplicate exit-code-timeout breadcrumb on Windows": changed Windows watchdog except block to emit only `_spawn_msg` (not the exit-code line); noted that `_run_windows_watchdog` already emits it.
- `20260518-070705-plan-review-r1.md` — "[NIT] Card 15 Fix 2/3 error message hardcodes wrong path": replaced literal `plugins/mill` with `${PLUGIN_ROOT}` in the halt message.

## Pushed Back

(none)
