# Plan-fix report — round 1

## Fixed

- `20260811-033633-plan-review-r1.md`: "Test-insertion location instruction contradicts itself" — `01-crash-fix.md` Card 1 now says to insert the new test method immediately after `test_2x_finalize_conflicts_missing_files_flag` (the class's last existing method), before the module-level `_git` helper and `class TestVerifyConflictMarkersGate`, instead of the ambiguous "before the class line" wording.
- `20260811-033633-plan-review-r1.md`: "Docstring's enumerated failure-path list not updated" — `01-crash-fix.md` Card 1 now instructs adding "status.md absent" as a fourth enumerated failure path in `_run_recompute_baseline`'s docstring.

## Pushed Back

(none)
