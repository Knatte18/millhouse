```yaml
round: 5
source: 20260923-122808-plan-review-r5.md
```

## Fixed

- [BLOCKING:scope] "Card 5's audit misses a test tied to a mechanism Card 4 deletes" — verified
  `test_check_context_completeness_symbol_issue_1030_deprecated_directory_property`'s fixture relies
  on `_SYMBOL_SEARCH_OUT_OF_SCOPE_DIRS` pruning; added it as a 7th named deletion to card 4's list
  (its name doesn't match the `..._out_of_scope_...` pattern so it was missed when the list was first
  built) and updated every "6" reference in cards 4/5 to "7".
- [BLOCKING:scope] "Card 5's 'run verify to green' backstop doesn't catch skipped migrations whose
  outcome is already 0 errors" — verified `clean_ambiguous_matches` and several sibling tests place
  their target file(s) fully uncited plan-wide while asserting 0 errors that would stay 0 either way
  post-narrowing, silently defanging their real coverage (ambiguous-match/qualifier-mismatch/
  comment-string-usage-only/test-file-exclusion) if left un-migrated. Added an explicit paragraph to
  card 5 restating the audit criterion as "does resolution need to actually be attempted," named the
  confirmed member tests (`clean_ambiguous_matches`, `qualifier_no_match_still_zero`,
  `qualifier_ambiguous_dirs_still_zero`, `clean_test_file_excluded_all_languages`, and the
  `comment_only_*`/`string_literal_only_*`/`usage_site_only_*` families), and corrected the "verify to
  green is authoritative" claim to state it does not hold for this class.
- [NIT:consistency] "`_compute_plan_wide_cited_files` omits the card_text join step" — added the
  explicit `card_text = "\n".join(card_lines)` step to card 4's Requirements, matching
  `_check_context_completeness`'s own established `_parse_cards` -> join pattern.

## Pushed Back

(none)
