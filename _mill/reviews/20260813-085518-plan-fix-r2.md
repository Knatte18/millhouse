# Plan fix — round 2

## Fixed

- `20260813-085337-plan-review-r2.md` — `[BLOCKING:design] Resumed loop never threads local_max_review_rounds into subprocess/psmux CLI dispatch`. Verified against `_review_plan.py`: `run()` (the default `--stage full` path the Subprocess/psmux branch hits) computes `holistic_max_rounds = max_rounds if max_rounds is not None else cfg[...]["rounds"]` and raises `ReviewError` when `round_n > holistic_max_rounds`; `prepare()`/`finalize()` (Agent-mode's functions) have no such parameter or check, confirming Card 4's existing Agent-mode-is-a-no-op claim was already correct but incomplete for the Subprocess/psmux side. Added a new paragraph to Card 5 instructing a blockquote note (mirroring `mill-start/SKILL.md`'s own analogous extension-round pattern) immediately before both Subprocess/psmux bash blocks in Phase: Plan Review (step 2's dispatch and Step 4.5's retry), conditionally appending `--max-rounds <local_max_review_rounds>` to the inner `millpy-review-plan.py` invocation only during a resumed loop.
- `20260813-085337-plan-review-r2.md` — `[NIT:consistency] Reworded step-6 halt text overclaims "addressed"`. Card 5's step 6 halt-message reword now reads "were acted on (fixed or pushed back) but not yet re-reviewed" instead of "were addressed," since `mill-receiving-review`'s decision tree permits legitimate pushback and "addressed" implied every finding was fixed.
- `20260813-085337-plan-review-r2.md` — `[NIT:scope] Card 1 cites two external files' call-site code without listing them in Context`. Added `plugins/mill/skills/git-commit/SKILL.md` and `plugins/mill/skills/mill-quick/SKILL.md` to Card 1's `Context:` (alongside the already-present `mill-go-base/SKILL.md`), matching Card 3's existing pattern of listing cited-for-convention files in Context. Verified no `batch-oversized` regression (both files are small; `mill-go-base/SKILL.md` was already counted once via Card 3).

## Pushed Back

(none)
