# Plan fix -- round 5

```yaml
review: 20260808-180528-plan-review-r5.md
round: 5
```

## Note on the round-5 envelope

Unchanged from rounds 1-4: `blocking_count: 0` against `REQUEST_CHANGES`, because the reviewer emits the classed heading syntax this task introduces and the installed cache's regex cannot match it.
Routed off the review file: 2 BLOCKING, 1 NIT, step 4d.

## Fixed

- `20260808-180528-plan-review-r5.md` -- "[BLOCKING:consistency] crash-recovery re-read site does not feed the aggregation".
  Accurate, and it invalidates a claim introduced by my own round-1 fix.
  Source-checked: the `if resume_round is not None:` block populates `_disk_reviews`, and the only consumer is the `len(_disk_reviews)` in its own stderr log line -- there is no `reviews.append`/`extend` for it anywhere in the function, so its entries reach no `ReviewResult`.
  Reverted that site to unchanged: it keeps `parse_blocking_count` and `count_unrecognized_severity_findings`, gains no `extract_findings` call and no `findings` key, because all of that would have been dead work resting on a false premise.
  Corrected the rationale in both card 14 and the overview's `ceiling-applied-once-at-write-time` Decision to say only `_scan_approved_batches` feeds aggregation.
  Also corrected the consequential claim round 1 added, that the two helpers would have no remaining caller and could be dropped from the import block -- they still do, so the imports stay.
  Took the reviewer's "correct the rationale" option rather than its "scope in fixing the pre-existing bug" option: repairing the discarded `_disk_reviews` changes what a resumed plan review returns, which is a behavioural change unrelated to the class taxonomy.
  Documented it explicitly in card 14 as an observed, deliberately-unfixed pre-existing bug so it is not silently implied to work, and left it for the Handoff `mill-self-report` pass to file.
- `20260808-180528-plan-review-r5.md` -- "[BLOCKING:consistency] Card 15's findings key breaks an untouched test via KeyError".
  Accurate and verified: `test-review-plan-finalize-round.py` patches `_review_plan.finalize` with a `stub_review_entry` dict carrying no `"findings"` key in both of its `--stage finalize` cases, so card 15's direct access would raise an uncaught `KeyError` in a file batch 3's gate did not run.
  Chose the reviewer's second option over its first: kept the direct `review_entry["findings"]` access and fixed the stale stubs, rather than switching to `.get("findings", [])`.
  Card 14 makes `finalize` return the key on every path, so a missing key is a contract violation that should fail loudly; a `.get()` default would hide exactly the class of bug this finding caught.
  Added the file to card 15's `Edits:`, to the overview's All Files Touched, and to batch 3's `verify:`, and replaced the Batch Tests paragraph that had excused excluding it -- the old justification was about round discovery and missed that the break is on the finalize-stage path those same stubs drive.
- `20260808-180528-plan-review-r5.md` -- "[NIT:consistency] Card 10 describes nonexistent test assertions and an inaccurate docstring".
  Accurate on both halves and verified: `test-review-cli-error-envelope.py` asserts only `verdict` values and exit codes, so there were no key-set assertions to "extend"; and `print_error_envelope`'s dict has no `nit_count` key, so the requested docstring claim of `to_dict()` key-set parity would have stayed false.
  Reworded "extend" to "add" and enumerated the assertions to add.
  For the docstring, took the reviewer's first option rather than softening the claim: added `"nit_count": 0` to the envelope so the parity claim becomes true.
  A consumer that branches on `nit_count` -- mill-go gates its NIT-fix dispatch on `nit_count > 0` -- should read a zero from an ERROR envelope rather than an absent key.

## Pushed Back

(none)
