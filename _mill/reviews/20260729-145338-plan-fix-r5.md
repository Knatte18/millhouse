## Fixed

- `20260729-145128-plan-review-r5.md`: "Phase 1b has no recovery path for a partial `relocate_and_scaffold` failure" — verified: `relocate_and_scaffold` ran `move()` -> `copy_millhouse()` -> `_junction.create()` sequentially with no partial-failure handling, and Step 2's collision check treated any existing `canonical_path` as an unconditional halt, giving a retry after a post-move scaffold failure no way back into the flow. Fixed by making `relocate_and_scaffold` (Card 8) idempotent: skip `move()` if `canonical_path` is already a registered worktree; `copy_millhouse` is already overwrite-safe (unchanged); guard `_junction.create` with an existence check so a second call does not raise on an already-created junction. Fixed Step 2 (Card 10) to distinguish a genuine collision (`canonical` exists, slug in its `_mill/status.md` differs or is absent) from a resumable interrupted repair (`canonical` exists, same slug) — the latter skips Step 3's confirmation and routes straight to Step 4, which now completes idempotently. Updated Step 4's failure message and the Error Conditions table to state retries are safe. Updated Card 11 scenario (d) to specifically exercise the genuine-collision (different slug) classification, and added new scenario (g) exercising the move-succeeded-scaffold-failed retry path end-to-end.

## Pushed Back

(none — the finding verified accurate against source; fixed.)
