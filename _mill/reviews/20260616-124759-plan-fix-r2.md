# Plan-fix report — round 2

Source review: `_mill/reviews/20260616-124657-plan-review-r2.md` (APPROVE, 3 NITs)

## Fixed

- `20260616-124657-plan-review-r2.md` [NIT] "Card 10 mis-states that task_dir is out of scope at 2b" — Card 10 edit (3) now derives only `parent_branch` in step 2b and reuses the existing global `task_dir` from Path Setup (step 4.5, line 47); the erroneous "neither is in scope" claim and the `task_dir` re-derivation are removed.
- `20260616-124657-plan-review-r2.md` [NIT] "worktree arg ambiguity for the drift-revert call" — Card 10 edit (3) now specifies the first arg is the SAME `<worktree>` the adjacent step-2b `compute_new_dirt` call uses, namely `worktree_root` (the hub root, also passed to `compute_terminal_dirt` at line 654), and notes the helper relativizes `task_dir` internally. (Adjustment vs the NIT's prescription: the shipped convention passes `worktree_root`/`<worktree>` — the hub — to both `compute_new_dirt` and `compute_terminal_dirt`, evidenced by the snapshot path `<worktree>/_mill/...`; so the arg is `worktree_root`, not the git checkout root the NIT suggested. The ambiguity the NIT flagged is resolved; consistency with the shipped gate is preserved.)
- `20260616-124657-plan-review-r2.md` [NIT] "Card 1 briefs path applies only to the prepare stage" — Card 1 now states the `briefs_dir` edit targets the line inside the `--stage prepare` branch.

## Pushed Back

(none — NIT 2's underlying ambiguity was fixed; only its specific "git checkout root" framing was corrected to `worktree_root` per the shipped `compute_terminal_dirt`/`compute_new_dirt` convention, cited above.)
