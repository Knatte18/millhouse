# Discussion fix — round 4

Source review: `_mill/reviews/20260904-163027-discussion-review-r4.md`

## Fixed

- r4 BLOCKING:design "tool-use-mode-relative rationale misdescribes `run_tool_use`" — verified accurate: `run_tool_use()` (`_llm_claude.py:481-514`) has no `cwd` parameter and forwards none to `_invoke()` (which defaults `cwd=None`), and `_reviewer_single.run()` (71-78) builds kwargs without `cwd`; only `run_implementer()` (525) takes one. Rather than restate the ambient-cwd invariant as a dependency, the decision was rewritten to remove the dependency: the `## Path roots` header states the absolute root and the tool-use instruction prose now tells the reviewer to resolve each listed path against that stated root, so an absolute path reaches `Read` regardless of cwd. The corrected source facts (including where the ambient invariant does hold in practice — `millpy-bg.py:151-170`, agent-mode inheriting the orchestrator cwd) are recorded in the rationale. Added a Scope bullet for the instruction-prose edit at the five assembly sites, amended the corresponding Q&A entry, and recorded the rejected `cwd`-plumbing alternative as a separate follow-up.

## Pushed Back

(none)
