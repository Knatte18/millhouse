MILL_REVIEW_BEGIN
# Review: mill-spawn, millpy-implement, _cleanliness, discussion-review: small bugs and inconsistencies — holistic

```yaml
duration_s: 295.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 4.5 (self-assessed; exact point version unconfirmed)
reviewed_file: plan/
date: 2026-08-13
```

## Findings

### [NIT:consistency] Card 2 insertion-point instructions self-contradict
**Location:** batch 1 / Card 2 **Issue:** Requirements say to insert "immediately after" the Regression-2 block (ends ~line 386) but also say to find the insertion point by reading "the current end of `main()`... preceding the closing of `main()`'s `try:` block" — these are different locations; `main()` continues for ~250 more lines (resolve_wiki_path/resolve_main_worktree_root/worktree-form tests etc.) after Regression 2. **Fix:** Drop the "end of main()" phrasing; state plainly "insert immediately after the `print("PASS: resolve_task_path flat layout...")` line that closes the Regression-2 block."

### [NIT:consistency] Card 7 docstring instruction references wording not present in source
**Location:** batch 4 / Card 7, point 1 **Issue:** Requirements say to update `_parent_diff_names`'s docstring "to describe the diff as unresolvable... rather than 'empty'" but the current docstring (verified in `_cleanliness.py:113-118`) never uses the word "empty" — only the stderr log line does (which the same point correctly updates). **Fix:** Reword to "add a `Returns:` note that `None` signals an unresolvable diff, distinct from an empty `[]` result" instead of implying existing docstring text needs replacing.

## Verdict

APPROVE
All five batches are precisely grounded in source (line numbers, signatures, docstrings verified accurate); only two cosmetic wording nits found.
MILL_REVIEW_END
