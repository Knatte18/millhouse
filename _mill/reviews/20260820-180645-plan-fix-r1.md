# Plan Fix — round 1

## Fixed

- Review `20260820-180350-plan-review-r1.md`, finding "`_safe_rmtree._is_reparse_point` never gets `to_extended` despite Decision naming its calls" — added a Card 7 requirement in `04-safe-rmtree-long-path-safety.md` binding `p = _long_path.to_extended(p)` inside `_is_reparse_point` and routing its `os.path.isjunction`/`os.lstat` calls through it, mirroring Card 5's `_is_junction_or_symlink` treatment. Added a matching fourth test case to Card 8 exercising this new call site directly.
- Review `20260820-180350-plan-review-r1.md`, finding "Batch 03's tests cover only 1 of Card 5's 3 modified call sites" — added Card 6 cases (k) and (l), covering `_is_junction_or_symlink`'s `os.path.lexists`/`os.path.islink` calls and `remove()`'s `os.unlink` call respectively, bringing batch 03's test coverage up to parity with batch 04's Card 8 (which already covered both of Card 7's original call sites).

## Pushed Back

(none — both findings were accurate and their fixes carried no harm; both fixed in full.)
