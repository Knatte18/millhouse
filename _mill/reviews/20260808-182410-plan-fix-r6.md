# Plan fix -- round 6

```yaml
review: 20260808-181204-plan-review-r6.md
round: 6
```

## Note on the round-6 envelope

Unchanged from rounds 1-5: `blocking_count: 0` against `REQUEST_CHANGES`, because the reviewer emits the classed heading syntax this task introduces and the installed cache's regex cannot match it.
Routed off the review file: 1 BLOCKING, 0 NIT, step 4d.

## Fixed

- `20260808-181204-plan-review-r6.md` -- "[BLOCKING:consistency] Card 2's dedup rule contradicts itself on same-mechanism YAML duplicates".
  Accurate, and self-inflicted: round 4's fix scoped the dedup to cross-mechanism only but left a trailing clause -- "a title produced by the yaml scan is dropped when an earlier yaml entry already produced it" -- which is exactly the same-mechanism dedup the next two sentences forbid.
  For two same-titled findings expressed only in the yaml block, the card asked for both drop-the-second and keep-both.
  Applied the reviewer's fix: deleted the contradicting clause so the rule reads only that a yaml title is dropped when and only when the heading scan already produced it, and stated explicitly that there is no same-mechanism dedup of any kind, heading-vs-heading or yaml-vs-yaml.
  Mirrored the tightened wording into the overview's `dual-mechanism-scan-preserved` Decision, which carried the same loose phrasing.
  Also acted on the finding's Note rather than only its Fix: card 8's duplicate-title test covered the heading mechanism only, so a wrong resolution of this contradiction would have shipped past batch 1's own `verify:` gate.
  Added the yaml-only duplicate-title case -- two fenced `findings:` entries sharing a title with no heading for either -- asserting both survive and both are rewritten when demoted.

## Pushed Back

(none)
