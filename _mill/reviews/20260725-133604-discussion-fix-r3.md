```yaml
round: 3
source_review: _mill/reviews/20260725-133541-discussion-review-r3.md
```

## Fixed

- `[NOTE] load_task_title's git_root parameter name doesn't match its needed on-disk semantics` — added a Technical Context bullet clarifying that `_review_common.py:329`'s `load_task_title(git_root, ...)` parameter is actually passed the hub-resolved `project_root` at every call site, and that the on-disk fast path must read `status.md` relative to that passed-in (hub-resolved) value, not assume it's a raw git checkout root — relevant for M2+sub configs where the two diverge.

## Pushed Back

(none)
