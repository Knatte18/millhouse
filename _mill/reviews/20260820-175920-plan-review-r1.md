MILL_REVIEW_BEGIN
# Review: git-pr: gh pr create fails on GraphQL 5xx with no REST fallback documented — holistic

```yaml
duration_s: 166.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-08-20
```

## Findings

### [NIT:consistency] Mismatched nested code-fence backtick counts corrupt the batch file
**Demoted-from:** BLOCKING
**Location:** 01-rest-fallback.md, Card 1, Requirement 1 (lines 41-85)
**Issue:** The `Content:` block opens with an outer ` ```markdown ` fence (line 41) and later contains a nested ` ```bash ` fence (line 51) using the *same* 3-backtick length as the outer fence. Per CommonMark, the closing fence only needs matching-or-greater backtick count and no info string, so the bare ` ``` ` at line 61 (intended to close the inner `bash` block) actually closes the *outer* `markdown` fence instead. The bare ` ``` ` at line 85 then opens a new, unclosed fence that swallows everything after it — Requirement 2, Requirement 3, the "Do not change steps 1-9" note, the Commit line, and the entire Batch Tests section — as literal code text in any standard markdown parse.
**Fix:** Use a longer outer fence (e.g. 4 backticks ` ```` markdown ` / ` ```` `) so it doesn't collide with the nested 3-backtick `bash` fence, or drop the outer `markdown` fence entirely and rely on indentation to delimit the insertion content.

### [NIT:consistency] Overview's Shared Decisions omits 3 of discussion.md's 5 decisions
**Location:** 00-overview.md, `## Shared Decisions`
**Issue:** Only `unconditional-rest-attempt` and `duplicate-pr-check-after-both-tiers` are surfaced as Shared Decisions; `rest-payload-construction`, `doc-structure`, and `report-wording` from discussion.md are not listed there, though they are faithfully implemented inline in Card 1's Requirements.
**Fix:** Either list all five decisions in the overview for at-a-glance traceability, or note in the overview that batch-local decisions are covered directly in the single batch card.

## Verdict

APPROVE
Fix the colliding code-fence lengths in 01-rest-fallback.md's Requirement 1 insertion block.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
