## Fixed

- `20260728-195046-plan-review-r5.md` — "[NIT] Card 16's new scratch fixtures aren't added to the cleanup `finally:` block": verified against `test-merge.py:945-965` (`container_nested`/`container_verify` pattern). Rewrote Card 16 to name three explicit container variables (`container_dirty`, `container_retry`, `container_untracked`) and require registering each in the `finally:` block's preserved-on-failure and cleanup-on-success branches.
- `20260728-195046-plan-review-r5.md` — "[NIT] Card 16 case 2's fixture is heavier than its own stated rationale requires": agreed — a full `_setup_trio()` was unnecessary for the mid-retry case; it only needs two branches in a plain repo to run a real `merge --squash`. Rewrote all three sub-scenarios in Card 16 to use lightweight `git init` fixtures consistently, removing the `_setup_trio()` dependency entirely from this card.

## Pushed Back

(none — both findings verified accurate and fixed)
