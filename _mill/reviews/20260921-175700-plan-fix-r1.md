# Plan fix — round 1

Source review: `20260921-175511-plan-review-r1.md`

## Fixed

- `[BLOCKING:scope] Card 7 edits mill-plan/SKILL.md but never declares it` — added
  `plugins/mill/skills/mill-plan/SKILL.md` to Card 7's `Edits:` (batch 3) and to the overview's
  `## All Files Touched`.
- `[BLOCKING:scope] Card 3's completeness fallback misses the --stage full call site` — Card 3's
  Requirements (batch 1) now instruct threading `card_commit_messages` into both
  `millpy-implement.py` call sites that already pass `card_ids=card_ids`: the `--stage finalize`
  branch's `finalize_from_output(...)` call and the `--stage full` branch's direct
  `_forward_output(...)` call.
- `[NIT:design] Synthetic signature may not dedupe across baseline corroboration runs` — added a
  "Known limitation, accepted rather than engineered around" paragraph to Card 1's Requirements
  (batch 1) explaining why cross-run normalization is not added (the target class — compiler/linter
  diagnostics — is deterministic across runs), and a matching note in batch 1's `## Batch Tests`.
- `[NIT:consistency] Overview's test-convention Decision misdescribes test-merge-in-subagent.py` —
  reworded the overview's "test convention" Decision to separate the files batches actually edit
  from `test-merge-in-subagent.py`, which batch 4 only re-runs for regression safety.

## Pushed Back

(none — all four findings were accurate and fixed as described above)
