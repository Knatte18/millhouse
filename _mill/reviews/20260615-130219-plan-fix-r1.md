# Plan-fix report — round 1

Source review: `20260615-125915-plan-review-r1.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 2 NIT)

## Fixed

- [BLOCKING] "test-agents-defs.py never runs under run-all.py" — verified (`run-all.py:26-27` uses `subprocess.run([sys.executable, test])`; `test-agents-defs.py` has `test_reviewer_agent_definition`/`test_implementer_agent_definition` but no `__main__`). Card 7 now adds a `__main__` runner to `test-agents-defs.py` and lists it under `Edits:`.
- [BLOCKING] "parse_batch_refs includes Context: files in language detection" — verified (`_review_common.parse_batch_refs` unions Context/Edits/Creates/Deletes). Card 4 now refactors `parse_batch_refs` with a behavior-preserving `fields=` param (default all four) and calls it with `fields=("Edits","Creates")` from the builder; added a test case asserting a `.go` file in `Context:` only does NOT trigger golang skills. Shared Decision `targeted-skill-injection` reworded to "touched (Edits/Creates) files, not Context". `_review_common.py` moved to Card 4 `Edits:`; `test-review-common.py` added to batch-2 `verify:`; both new files added to All Files Touched.
- [NIT] "Card 5/6 token-comment edit not in a stable identifier" — Cards 5/6 now state the `<LANGUAGE_SKILLS>` token expands to the `## Required skills` block emitted by `language_skills_directive` (Card 4 specifies that heading), giving a stable cross-round anchor.
- [NIT] "Card 3 inline-snippet match string must be exact" — Card 3 now cites both occurrence sites (lines ~189 and ~210, one per branch) explicitly.

## Pushed Back

- none
