# Review: Wiki daemon error-log leak and stale plugin-cache config validation produce misleading noise — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-07-25
```

## Findings

### [NIT] Delegation silently inherits `_interpolate_env` and new stderr output
**Location:** Batch 2 / Card 5
**Issue:** `_config.load_config` runs a step 7 `_interpolate_env` (absent from the current `_review_common.load_config`) that can raise `ConfigError`; callers only expect `ReviewError`. It also emits `[_config] note: no repo-layer mill-config.yaml found` to stderr when the template exists but no repo-layer config is present — another behavior the current implementation silences. The plan's "no behavior change beyond documented scope" shared decision doesn't acknowledge either addition.
**Fix:** Add a sentence in the Card 5 scope noting these two inherited side-effects and why they are acceptable (configs don't use `${VAR}` patterns; the note is informational).

## Verdict

APPROVE
Plan is structurally sound and correctly specified; one undocumented behavioral addition warrants a note.