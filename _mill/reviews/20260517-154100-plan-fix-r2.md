# Plan-fix report -- round 2

Source review: [`20260517-153958-plan-review-r2.md`](20260517-153958-plan-review-r2.md) (verdict: REQUEST_CHANGES, blocking_count=1)

## Fixed

- `[BLOCKING] Card 11: resolve_path not patched for millpy-review-plan.py test` -- VERIFY accurate: `millpy-review-plan.py` line 94 calls `resolve_path(cfg["paths"]["plan_dir"], slug)` BEFORE the backend `run()`. `resolve_path` calls `_paths.resolve_git_root()` which fails outside a real mill-container-layout git repo. Fix applied to Batch 3, Card 11 Requirements: added a `millpy-review-plan.py`-specific argv subsection instructing every plan CLI test case to call `main(["--skip-validate"])`, which bypasses the entire `if not args.skip_validate:` block (validator gate + the offending `resolve_path` call). Dropped the prior "patch `_plan_validate.run -> []`" instruction; `--skip-validate` is the canonical escape and is structurally correct (no helper-module patching needed). The discussion and code CLIs are unaffected; they continue to pass `argv=None` / `[]`.
- `[NIT] Card 14: import errno listed but never used` -- VERIFY accurate: the Card 14 implementation now uses bare `except OSError:` clauses; no `errno.E*` reference remains, and the trailing "Either choice is acceptable" hedge left an unused import. Fix applied to Batch 5, Card 14: dropped `errno` from the Imports line, and replaced the hedging paragraph with the explicit directive "do NOT import errno in the module".

## Pushed Back

(none -- both findings VERIFY-accurate and fixes carry no harm)
