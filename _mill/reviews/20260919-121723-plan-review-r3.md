MILL_REVIEW_BEGIN
# Review: _plan_validate.py context-completeness: path-token exemption list gaps — holistic

```yaml
duration_s: 353.0
verdict: APPROVE
reviewer_model: sonnethigh
reviewer_self_id: claude-sonnet-5 (Sonnet 5)
reviewed_file: plan/
date: 2026-09-19
```

## Findings

None. Verified against source:

- Insertion point for the new `continue` guards (batch 1, cards 1-3) matches
  `_plan_validate.py:2356-2361` exactly (immediately after the contrast-citation guard, immediately
  before `if is_path_shaped:`).
- Docstring numbered list genuinely ends at item `10. Quoted material` (line 2252-2253); items
  11-13 append cleanly before the trailing "Not-shaped-at-all..." prose paragraph.
- `backtick_re` (line 2289) has exactly one other use site (line 2330); card 2's promotion to
  module-level `_BACKTICK_RE` + local rebind is safe and necessary (the new exemption function is
  module-level and cannot see `_check_context_completeness`'s local variable).
- `_OWNERSHIP_VERB_FORMS`/`_OWNERSHIP_RE`, `_OUTPUT_VERB_FORMS`/`_OUTPUT_VERB_RE`,
  `_is_cross_card_ownership_exempt`, `_is_literal_enumeration_exempt`, `_is_illustrative_output_exempt`,
  `_BACKTICK_RE` are all genuinely new names (grepped, zero collisions).
- Verb-form dict style (hand-spelled base/3rd/past/gerund, "rewrite" as 5-form irregular) mirrors
  `_PROHIBITION_VERB_FORMS`'s existing convention (lines 1672-1693) exactly, matching the Shared
  Decision's rationale.
- Traced `_OWNERSHIP_RE` and `_OUTPUT_VERB_RE` regex construction against Python's alternation +
  trailing `\b` backtracking behavior — both correctly match longer inflected forms (e.g. "owning")
  over shorter prefix alternatives ("own") without truncation bugs.
- Traced card 5's `_is_literal_enumeration_exempt` fixture (`#984` mixed-shapes line) token-by-token:
  `#x` is neither path- nor symbol-shaped (fails `_symbol_candidate_shape`'s identifier regex) and
  correctly trips the >=3-backtick-token exemption; the all-path-shaped `a.py`/`b.py`/`c.py` fixture
  correctly does NOT trip it (3 genuine errors expected, matches test assertion).
  Confirmed the function re-scans the line with its own `_BACKTICK_RE.finditer`, independent of the
  outer loop's shape-gate `continue`, so non-path/non-symbol siblings that the outer loop would
  otherwise skip are still counted.
- Card 6's illustrative-output fixtures contain no accidental overlap with existing exemptions
  (`_PROHIBITION_VERB_FORMS` has no "emit"/"render"/"print"/"display" entries; no `_CITATION_MARKERS`
  substring present; "never" alone in the `#985` fixture does not satisfy `_is_prohibition_exempt`,
  which requires a negation AND a prohibition verb).
- Card 7's SKILL.md insertion point verified verbatim against `mill-plan/SKILL.md:674-675` — the
  "A forward reference..." and "A not-involved mention..." anchor sentences match character-for-character.
- Test insertion points (both the new test-function bodies after
  `test_check_context_completeness_dirty_escape_marker_phrase_removed` at line 5257-5293, and the
  `main()` tests-list registration after line 10337) verified against actual file content.
- Batch Index DAG: no cycle, all three `file:` entries present in the manifest and plan directory,
  `depends-on` accurate (batch 2 on batch 1; batch 3 correctly independent since its doc text is
  fully self-contained, not derived from reading the implementation).
- All Files Touched lists exactly the three edited files; no Moves/Creates exist in this plan, so
  no Rename-mechanic section is required and none is present — correct.
- Global card numbering (1-7) is sequential across the three batches with no gaps.
- Every card carries all six required fields (Context/Edits/Creates/Deletes/Moves/Requirements) plus
  Commit; every `Requirements:` field names the exact function/class/constant/verb-table being added,
  with no vague prose.
- `verify:` commands use the `PYTHONPATH=` (literal, empty) prefix per this Python project's
  convention; batch 3's `verify: null` is justified (doc-only edit, no runnable surface).

## Verdict

APPROVE
Every mechanism claim traced to source line numbers checks out; no gaps in exemption-chain wiring, docstring numbering, or doc-sync anchors.
MILL_REVIEW_END
