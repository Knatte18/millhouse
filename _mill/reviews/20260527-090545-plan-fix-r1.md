# Plan-fix report: round 1

```yaml
round: 1
review_file: _mill/reviews/20260527-090323-plan-review-r1.md
date: 2026-05-27
```

## Fixed

- `[BLOCKING] Card 11: _subprocess_util.py missing from Context despite Requirements reference` — added `plugins/mill/scripts/_subprocess_util.py` to Card 11's `Context:` so the implementer can verify the `run()` signature accepts the `env=` kwarg. Also added `plugins/mill/unit_tests/test-fixer-env-isolation.py` to Card 11 Context — implementer reads the test contract before implementing.
- `[NIT] Cards 9, 11, 14 have Context: none -- violates non-empty rule (Card 9)` — set Card 9 `Context:` to `plugins/mill/unit_tests/test-cli-commit-author.py`. NOT to `_subprocess_util.py` (which is in Edits: and `plan-batch.md` rules say "Files listed in Edits: are implicitly read -- do not repeat them in Context:"). The test file is the contract Card 9 must satisfy and is the legitimate reading-context for the implementer.
- `[NIT] Cards 9, 11, 14 have Context: none -- violates non-empty rule (Card 14)` — set Card 14 `Context:` to `plugins/mill/skills/conversation/SKILL.md`. The conversation skill documents the worktree-isolation rules that Card 14's brief-edit must align with — legitimate cross-reference reading.
- `[NIT] Card 7 guard block: wiki daemon exceptions not caught` — wrapped the `_client.get_task(wiki_path, slug)` call in a `try/except (WikiStartupError, WikiProtocolError) as e:` block; the except branch prints `f"_mill/status.md absent and wiki daemon unavailable: {e} -- inspect manually."` to stderr and raises `SystemExit(1)`. Updated Card 7's intro prose to mention the daemon-error path. Updated import path to `from wiki import WikiStartupError, WikiProtocolError` (exceptions live in `wiki/__init__.py`, not `wiki/_client.py` — verified). Added `plugins/mill/scripts/wiki/__init__.py` to Card 7 Context so the implementer can confirm the exception classes' home.

## Pushed Back

_(none — all findings fixed.)_
