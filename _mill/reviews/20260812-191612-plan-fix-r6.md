# Plan fix — round 6

## Fixed

- `20260812-191521-plan-review-r6.md` — "[NIT:consistency, demoted from BLOCKING] Batch 4's dead-parent status.md fixtures lack the required ```yaml fence, so resolve_dead_parent can't parse them": wrapped every fixture status.md/task.md content string in Batch 4 Cards 11(a), 12, 13, and 14 with the required leading `` ```yaml `` / trailing `` ``` `` fence, matching `_setup_trio`'s and `_setup_nested_hub_scenario`'s existing fixture convention in the same file. Without this fix, four of the six new #817 test scenarios would have failed their own assertions as written (`_parse_parent_from_yaml_text` treats unfenced content as if `parent:` were entirely absent).
- `20260812-191521-plan-review-r6.md` — "[NIT:consistency] Card 1 documents `resolve_dead_parent` in the module docstring before Card 2 defines it": Card 1 now adds only the `check_liveness` bullet to the module docstring's Public API list; Card 2 adds its own `resolve_dead_parent` bullet at the commit that actually defines the function.
- `20260812-191521-plan-review-r6.md` — "[NIT:consistency] New parent fast-forward step sits close to an existing \"do not ff-sync\" note without cross-referencing it": added a one-line cross-reference in Batch 2 Card 3's "Why" paragraph clarifying the `merged` PR-state route's separate no-ff-sync note is unaffected because that route skips Step 5 entirely.

## Pushed Back

(none — all three findings were factually accurate; the first was a genuine test-breaking bug despite being demoted to NIT by the stage ceiling, so it was fixed exactly as any BLOCKING finding would be, per `mill-receiving-review`'s "NITs are not optional" rule.)

## Note on round-cap / handoff

Verdict this round was `APPROVE` with 3 NITs (one demoted from BLOCKING). Per the operator's explicit instruction to proceed to Handoff after this round regardless of outcome, this round's fixes are applied and `approved: true` is set without dispatching a round 7 re-review — even though the convergence gate's demoted-predicate would otherwise call for one more round. Commit message for the approve-commit is annotated accordingly.
