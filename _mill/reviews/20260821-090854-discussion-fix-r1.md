```yaml
round: 1
type: discussion-fix
```

## Fixed

- 20260821-090718-discussion-review-r1.md — "#895 default grace window contradicts its own stated purpose": reworded the #895 Decision to state the ~20s default is intentionally conservative (guards only a single-poll coin-flip race, not the ~2m41s window in the reported repro) and that this hub should configure `pipeline.entry_wait_blocked_grace_s` explicitly larger (e.g. 300s) as part of the plan if it wants that repro actually covered.
- 20260821-090718-discussion-review-r1.md — "Technical Context misattributes `_compute_transitive_ancestors`": moved the function from the `_plan_dag.py` bullet to the `_plan_validate.py` bullet (confirmed it's defined at `_plan_validate.py:985`, not in `_plan_dag.py`).
- 20260821-090718-discussion-review-r1.md — "#877 rationale's grep claim is inaccurate": reworded to note the one incidental, unrelated "same commit" hit (Entry-gate wait section, phase-transition commit coupling) rather than asserting a literal zero-hit grep across all three terms.
- 20260821-090718-discussion-review-r1.md — "#877 rationale rests on an unverified 'no-amend policy'": dropped the "no-amend policy" framing (no such named policy is documented in-repo); reworded to cite the harness's standing git-safety protocol (create new commits, don't amend) plus the confirmed one-commit-per-card convention (`mill-go-base/SKILL.md` line 871).
- 20260821-090718-discussion-review-r1.md — "#887's 'general_refs' description over-includes Creates:": replaced the `general_refs` reuse with an explicit `_parse_context_only(path) | _parse_edits_only(path)` computation, noting why reusing `general_refs` verbatim would be wrong (it also includes the batch's own `Creates:` tokens).

## Pushed Back

(none — all 5 findings verified accurate, no harm from fixing any of them)
