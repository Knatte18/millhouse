# Review: CodeGuide support for .ipynb — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-29
```

## Findings

### [NIT] No test case for `raw` cell type
**Location:** Batch 1 / Card 2
**Issue:** `build_digest` specifies handling for `raw` (or unknown) cells with `# [raw cell K]` marker, but the required test cases omit a fixture exercising this path.
**Fix:** Add one raw-cell fixture to the test's in-memory notebook dict and assert the marker appears.

### [NIT] No test for single-string `source` field
**Location:** Batch 1 / Card 2
**Issue:** `cell_source_text` must handle `source` as either `list[str]` or `str`; the required test cases use only list-form, leaving the single-string branch untested.
**Fix:** Add a notebook fixture where at least one cell has `source` as a plain string and verify the digest produces the correct text.

## Verdict

APPROVE
Two minor test-coverage gaps; no blocking issues found.