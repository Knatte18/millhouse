## Fixed

- `20260814-084830-discussion-review-r1.md` — "String-fallback pattern won't match the quoted WinError text": corrected the `baseline-retry-match-on-winerror-not-string` decision's string-fallback pattern from `"directory not empty" in str(exc).lower()` to `"directory is not empty" in str(exc).lower()`, matching the actual Windows `ERROR_DIR_NOT_EMPTY` message text quoted in the Problem section.
- `20260814-084830-discussion-review-r1.md` — "Baseline wrapper's except clause narrower than the 'never raises' contract it targets": broadened the `baseline-teardown-defense-in-depth` decision's layer-(b) wrapper from a `WorktreeError`/`WorktreeLockedError`-scoped catch to a general `except Exception`, so it actually honors `_run_baseline_stage`'s unconditional "never raises" docstring promise, including exception surfaces `remove_safe` doesn't itself guard (e.g. unguarded `_subprocess_util.run` calls, `_junction.strip_all_in_worktree`).

## Pushed Back

(none)
