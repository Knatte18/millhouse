MILL_REVIEW_BEGIN
# Review: _config.py / _treeguard.py: silently-ignored config keys and unguarded str-vs-Path args — holistic

```yaml
duration_s: 95.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-19
```

## Findings

No findings. Both cards are fully realized and match the plan's Shared Decisions exactly.

Verified:
- `_config.py`: `RENAMED_KEY_HINTS` placed immediately after `ENV_REGISTRY`, added to `__all__` alongside it, exact wording matches the card. `warn_unknown_keys` loop refactored to `continue` on `deprecated_keys` then hint-lookup branch, with no change to `deprecated_keys` or `walk_unknown_keys` — matches the card's behavior-preserving-refactor requirement.
- `test-config.py`: both new tests (`test_renamed_key_hints_named_in_warning`, `test_unrelated_unknown_key_no_hint_bleed`) correctly avoid `_setup_plugin_template` and instead write a local template with `pipeline: {}`, patching `resolve_plugin_template_path` directly — mirroring `test_pipeline_autonomous_mode_does_not_trigger_unknown_key_warning`'s pattern as directed. Both names are registered in `main()`'s `tests` list, adjacent to the existing `test_unknown_key_warning_emitted`.
- `_treeguard.py`: two `isinstance` guards added as `check_and_restore`'s first statements, before the `status_porcelain` call, with the exact message format `"{fn}: {arg} must be a pathlib.Path, got {type}"` matching `_status._require_path`'s shape, implemented inline (no import, no new shared module) — matches Shared Decision 2 and the `_rebase_onto_hub` reimplementation-convention precedent.
- `test-treeguard.py`: two new scenarios (11, 12) placed after the last existing scenario and before the final summary print, following the file's `try`/`except TypeError`/`else: raise AssertionError` shape used nowhere else in this file but exactly as specified; scenario 12 uses a real `Path` for `worktree` so only the `git_root` guard fires, as required. Neither uses `_seed_repo`, correctly.
- No out-of-plan files; `_status.py` is read-only context (its `_require_path` message shape is the template, not imported). No shared-helper duplication introduced. Verify command matches `PYTHONPATH=` convention.

## Verdict

APPROVE
Both cards match the plan and shared decisions exactly; no deviations found.
MILL_REVIEW_END
