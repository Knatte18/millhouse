# Plan fix — round 4

```yaml
task: Explore and adopt Claude Code fork-agents in mill orchestration
slug: explore-fork-agent-opportunities
round: 4
source_review: 20260712-142435-plan-review-r4.md
verdict_addressed: APPROVE (0 BLOCKING, 4 NIT)
```

## Fixed

- `20260712-142435-plan-review-r4.md` — **[NIT] "`mill-reviewer.md:9` blanket no-modify claim survives
  card 14"**. Accurate; verified `:9` reads *"you MUST NOT modify any files or run commands that change
  state"*, which flatly contradicts the `Write` grant, and card 22's `agents/` assertions look for
  three specific patterns that would not match this phrasing. Added as card 14(a1): reword to
  "modifies no existing file and runs no commands; writes only its own report".

- `20260712-142435-plan-review-r4.md` — **[NIT] "Card 15's deletion list vs card 18(b)'s assertion"**.
  Accurate, and a genuine internal contradiction: card 18(b) asserts no template contains
  `You are a READ-ONLY reviewer`, but card 15 never named that sentence for deletion — so a literal
  implementer would keep it, fail card 18's test, and be tempted to weaken the test rather than delete
  the sentence. Card 15(a) now names all three deletions explicitly (the opening sentence, the
  `MUST NOT call Edit, Write, Bash` clause, the `MUST NOT make git commits` clause) and states that the
  fix for a card-18 failure is deletion, never weakening the assertion.

- `20260712-142435-plan-review-r4.md` — **[NIT] "'Nothing else imports it' claims in Batch Tests are
  false"**. Accurate on both counts, and the batch-1 half mattered most: card 2's `.out.md` unlink is
  **unconditional**, so it fires on the implementer path too, and the only automated check of the
  descope's "implementer comes out byte-identical" guarantee lives in suites that were **not** in any
  verify set. Batch 1's verify now adds `test-implementer-common.py`, `test-millpy-implement.py`,
  `test-millpy-fix.py`, `test-millpy-merge-in-subagent.py`. Batch 2's verify adds `test-review-cli.py`
  — verified it loads all three review CLIs (`:112-114`, `:170-172`, `:252-254`) and drives the exact
  `--stage prepare` branch cards 9-11 modify. Both rationale paragraphs are rewritten to describe
  scoping rather than to assert a false exclusivity, and the overview's Batch Index verify lines are
  re-synced byte-identically (the same drift that caused round 3's BLOCKING).

- `20260712-142435-plan-review-r4.md` — **[NIT] "Line-number drift in several citations"**. Accurate;
  all four verified against source and corrected even though the reviewer marked it no-action:
  `ThreadPoolExecutor` `:752` -> `:749`; `_MODE_BY_ALLOWED_TOOLS` `_llm_claude.py:79` -> `:80`;
  mill-go's Builder-does-not-act-on-findings bullets `:820` -> `:816`; step 6.5 `:159-165` -> `:157-165`.
  Stale line numbers in a cold-start plan are a real cost — they send the implementer to the wrong
  place — so these are worth the seconds they cost.

## Pushed Back

_None. All four findings verified accurate against source; none met a harm-check exception._
