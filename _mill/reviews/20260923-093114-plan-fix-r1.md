```yaml
round: 1
```

## Fixed

- [BLOCKING:design] "No shared source for the hub-fragment exclusion prefixes" — added a shared `_baseline_exclusion_prefixes(project_root, git_root)` helper in Card 11 that Card 12's dirt-warning callers now use, instead of each caller re-deriving the hub-fragment re-anchoring inline.
- [BLOCKING:design] "Batch 5's dependency rationale is factually wrong about millpy-merge-in-subagent.py" — corrected `05-merge-in-baseline-recompute.md`'s Batch Scope paragraph: the file does call `_forward_output`/`finalize_from_output` (in `_run_conflicts`'s conflicts-mode finalize branch); the no-batch-4-dependency conclusion holds because neither call site passes `batch_name`/`git_name`/`git_email`, not because the file never calls these functions.
- [NIT:consistency] "Card 34 misattributes the quoted claim's location in mill-go-base/SKILL.md" — corrected Card 34: the quoted claim lives inside "0.5. Baseline pre-flight"'s own "First check" sub-bullet, not inside "Entry-gate wait for upstream mill-plan"; the edit is now scoped to "0.5. Baseline pre-flight" only.
- [NIT:consistency] "Preflight guard is called twice per invocation, not once as documented" — reworked Cards 11/13/14/15: `_baseline_preflight_skip_reason` is now called exactly once, by `_run_baseline_stage` (Card 15), which threads the result to both `_run_module_wide_standalone` (Card 13) and `_run_per_batch_baseline_standalone` (Card 14) as a `preflight_skip_reason` parameter instead of either half calling the guard itself. Added a regression-guard test requirement to Card 19 asserting the guard's `git` calls run exactly once per invocation.

## Pushed Back

(none)
