# Discussion fix — round 4

## Fixed

- `_mill/reviews/20260904-100921-discussion-review-r4.md`: "[NIT:consistency] #938 drift-guard halt message promises a recovery the Entry table can't deliver" — rewrote the drift-guard decision's halt/recovery text to (a) match this file's own existing "manual intervention, operator clears `blocked_reason`" pattern for non-max-rounds blocked states instead of implying a bare `/mill-plan` re-run recovers automatically, (b) explicitly state that recovery requires deleting `plan_dir` and starting a fresh Phase: Plan run, since `discussion_sha` is captured exactly once at Phase: Plan's single creation of `00-overview.md`, and (c) address the reviewer's parenthetical sub-issue by discarding written-but-uncommitted plan files (`git clean -fd <plan_dir>`) before halting when the mismatch fires at the pre-plan-commit check, so the halt never leaves a dirty, half-written plan tree behind.

## Pushed Back

(none)
