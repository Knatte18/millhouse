# Plan-fix report — round 2

```yaml
round: 2
source_review: 20260524-172934-plan-review-r2.md
verdict: REQUEST_CHANGES
blocking_count: 3
nit_count: 2
```

## Fixed

- `20260524-172934-plan-review-r2.md` — "[BLOCKING] CAS mechanism never raises WikiConflictError" — removed client-driven CAS from card 6 (no `WikiConflictError` retry; constant `CAS_RETRIES` kept declared so `test-wiki-e2e.py` can `from wiki._client import CAS_RETRIES` instead of duplicating a local literal). Card 5 now explicitly drops `base_hash`/`FIELD_BASE_HASH`/`ERR_CONFLICT` machinery from `_handle_write` and documents that cross-host conflicts surface exclusively at `_sync.commit_push`'s rebase retry, with permanent failure raising `WikiPushError`. Shared decision `no-advisory-lock` in overview rewritten to match. Card 11 e2e test updated: the concurrent-write soak no longer asserts a CAS-conflict retry path (impossible under daemon single-threading); it now asserts both interleaved writes succeed and final TinyDB state reflects them.
- `20260524-172934-plan-review-r2.md` — "[BLOCKING] Batch 1 verify gate omits integration tests modified by cards 11/12" — rewrote batch 1's `verify:` (in both `00-overview.md` Batch Index yaml and `01-wiki-module-refactor.md` frontmatter) into a chained `&&` command that runs `run-all.py` plus `test-wiki-sync.py` (skipped by run-all per its `SKIP` set, but modified by card 12) plus the three integration tests touched by cards 11-12 (`test-wiki-e2e.py`, `test-wiki-daemon-tinydb.py`, `test-wiki-concurrency.py`). Batch 3 verify also extended to chain `test-wiki-sync.py` + `test-wiki-e2e.py` after `run-all.py` for the same coverage reason. Updated batch 1's `## Batch Tests` section to describe the chained verify.
- `20260524-172934-plan-review-r2.md` — "[BLOCKING] Card 13 backup commit step not idempotent on re-run" — split step 5b into a two-stage guard: `git add Home.md.pre-v3.bak` followed by `git diff --cached --quiet`; if returncode 0 (no diff) skip the commit and proceed; otherwise commit. Card 14's re-run idempotency test now explicitly asserts script exit 0, head SHA unchanged, and no additional backup commit.
- `20260524-172934-plan-review-r2.md` — "[NIT] Card 36 scope risks reviewer/implementer fatigue" — split former card 36 into three sequential cards 36/37/38 grouped by rewrite-rule family: card 36 (imports + direct V2 calls), card 37 (mock.patch retargeting), card 38 (dict-fixture conversion + dead-test deletion). Each card has its own Edits list scoped to the files touched by that rule family. Batch 3 card count bumped from 22 to 24; overview decision range 15-36 -> 15-38.
- `20260524-172934-plan-review-r2.md` — "[NIT] Card 22 line-reference to discussion bug fix is dangling" — inlined the constraint in card 22's line `:68` requirement: stated that `_config.load_config` takes `(repo_root: Path, worktree_root: Path)` where `repo_root` is the hub repo root (NOT `wiki_path`), with the explicit rationale that passing `wiki_path` would silently return template defaults. No external reference required to read the constraint.

## Pushed Back

(none — every finding was factually accurate and no fix would cause harm per the `mill-receiving-review` decision tree.)
