# Plan fix — round 1

Source review: `_mill/reviews/20260630-191344-plan-review-r1.md`

## Fixed

- "Card 5 cites mill-fold/SKILL.md but omits it from Context" — added `plugins/mill/skills/mill-fold/SKILL.md` to batch 3 Card 5's Context.
- "'matching /mill-fold's output' for fold-ins is imprecise (brief vs body)" — clarified in the overview's "per-Sources-bullet rendering" and "unclaimed-only fold guard" decisions, and in batch 2 Card 4 Step 5, that `mill-triage-to-tasks` appends fold-in bullets to the task's `body` field (matching today's `mill-ghissues-to-tasks` placement), reusing only `/mill-fold`'s bullet-string format, not its `brief`-field placement.
- "_sandbox_report tests omit the top-level-not-a-dict case" — added test case 8 to batch 1 Card 2: a non-object top-level JSON value must raise `SandboxReportError`, not an uncaught `AttributeError`.
- "_paths.* used in skill cards without a Context entry" — added `plugins/mill/scripts/_paths.py` to Context in batch 2 Card 4, batch 3 Card 5, and batch 4 Card 6.

## Pushed Back

(none)
