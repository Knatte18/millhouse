MILL_REVIEW_BEGIN
# Review: millpy-implement/fix.py: stuck-type false positives and session-hygiene gaps — holistic

```yaml
duration_s: 383.0
verdict: REQUEST_CHANGES
reviewer_model: sonnethigh
reviewer_self_id: claude-sonnet-5
reviewed_file: plan/
date: 2026-09-04
```

## Findings

### [BLOCKING:scope] Batch 3 Card 6 uses `_status.py` symbols not in Context/Edits
**Location:** Batch 3 (forward-verify-baselines-millpy-fix), Card 6. **Issue:** Requirements direct the implementer to call `_status.get_module_verify_baseline(status_path)` and `_status.read_batches(status_path)` (twice — once per scope arm), but Card 6's `Context:` lists only `_plan_dag.py` and `_implementer_common.py`, and `Edits:` is only `millpy-fix.py`; `_status.py` appears in neither. `get_module_verify_baseline` is not referenced anywhere in either listed Context file (verified via grep), so the implementer has no source-grounded way to confirm its signature/return shape without cold-start exploration outside the granted Context. `read_batches` is at least referenced once inside `_plan_dag.py` (line 639), which partially mitigates that one symbol, but not `get_module_verify_baseline`. **Fix:** Add `plugins/mill/scripts/_status.py` to Card 6's `Context:` list.

## Verdict

REQUEST_CHANGES
Batch 3 Card 6 references `_status.py` functions without listing that file in Context or Edits.
MILL_REVIEW_END
