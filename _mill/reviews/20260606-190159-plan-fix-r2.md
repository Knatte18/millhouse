# Plan fix report — round 2

Source review: `20260606-190111-plan-review-r2.md` (holistic, APPROVE, 0 BLOCKING + 2 NIT).

## Fixed

- [NIT] "Race-guard test docstring will be stale after Card 2" — verified accurate: Card 2 collapses the two-read race (EXIT detected inside the single `_probe_liveness` call), so `test_check_bg_status_race_guard_exit_appears`'s original scenario no longer exists. Extended Card 4's mock-migration note to repoint that test to assert a probe yielding the EXIT sentinel returns `("exit", code)` and to update its docstring from "re-read shows EXIT" to "probe returns exit".
- [NIT] "Card 5 omits `_cleanliness` from mock guidance" — verified accurate: `_implementer_common._forward_output` calls `_cleanliness.compute_scope_violations` / `compute_new_dirt` (confirmed in `_cleanliness.py:23,50`). Added `_cleanliness` to Card 5's mock-target guidance and added `plugins/mill/scripts/_cleanliness.py` to Card 5 `Context:`.

## Pushed Back

- none
