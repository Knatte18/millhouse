# Plan fix — round 1

```yaml
review_file: _mill/reviews/20260618-092155-plan-review-r1.md
verdict: APPROVE
fixed: 2
pushed_back: 0
```

## Fixed

- `20260618-092155-plan-review-r1.md` [NIT] "load_config second-arg semantics vs mill-go's pattern" — Card 3 (batch 02) now states explicitly that it must use `_config.load_config(hub_root, worktree_root)` (arg2 = `git_root`), NOT `_review_common.load_config` (arg2 = a `.millhouse` dir), and warns against copying mill-go Entry step 3's argument shape. Prevents the implementer from "correcting" arg2 mid-stream and silently misreading config.
- `20260618-092155-plan-review-r1.md` [NIT] "Card 7 assertion (c) cites a non-existent `_archive_tag` helper" — Card 7 (batch 03) reworded to point at the existing raw `git tag archive/<slug>` approach (`test-merge.py` ~line 291); removed the misleading `_archive_tag` helper reference.

## Pushed Back

(none — both NITs were accurate and the clarifications prevent real implementer mistakes; no harm.)
