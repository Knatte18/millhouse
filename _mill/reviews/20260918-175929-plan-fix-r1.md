## Fixed

- `_mill/reviews/20260918-175845-plan-review-r1.md`: "Card 3 Case 17 is a tautological regression check" — replaced the equality-against-`cmd` assertion (comparing two identical calls to a pure function, which can never discriminate a real regression) with an equality assertion against a hardcoded golden string reproducing `build_wait_command`'s exact current output for the same four positional arguments.

## Pushed Back

(none)
