## Fixed

- `_mill/reviews/20260810-180051-discussion-review-r3.md` — "[NIT:consistency] 810 rationale mischaracterizes 1187/1190/1195's literal call shape": corrected the `810-mutation-sequence` Rejected bullet to describe the sibling lines' actual `set_batch_field`-shaped prose (`set batch state -> blocked, blocked_reason: "..."` + `_status.append_phase(...)`) instead of the incorrect `append_phase`+`update_field` characterization, while keeping the underlying `set_blocked` decision unchanged (holistic scope has no `batch_name` to key a `set_batch_field`-shaped call on).

## Pushed Back

(none)
