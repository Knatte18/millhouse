# Plan fix: round 2

## Fixed

- [source: `20260918-183627-plan-review-r2.md`] "Card 4 directs new baseline-CLI test into a file with no matching fixture convention" — verified accurate: `test-verify-baseline.py` has zero `def test_` functions, zero mentions of `_run_baseline_stage`/`--stage baseline`/`spec_from_file_location`; its `_case_*` functions call `_verify_baseline.compute_baseline`/`compute_batch_baselines` directly with a mocked `_subprocess_util.run`. `test-millpy-implement.py` has the matching convention (`spec_from_file_location`, `self._run_main(["--stage", "baseline"])`, existing tests like `test_baseline_stage_cwd_hub_derives_relative_fragment_for_compute_baseline`). Retargeted Card 4's new test into `test-millpy-implement.py`, updated its Edits: list, the batch's frontmatter `verify:` and Batch Tests, and the mirrored entries in `00-overview.md` (Batch Index `verify:` and All Files Touched, re-sorted alphabetically). Recomputed the batch's context estimate against the new, larger file (120,432 bytes vs. 14,044) — still comfortably under the cap (≈76,602 total).
- [source: `20260918-183627-plan-review-r2.md`] "`clear_baseline_preflight_log` told to mirror a less-direct template than the one already in the file" — verified accurate: `clear_module_verify_baseline` (same file) already has the identical contract (standalone top-level scalar field, remove-if-present, no-op-if-absent, "safe to call unconditionally" per its own docstring) and is the more literal template match. Repointed Card 3's Requirements at it as the primary template.

## Pushed Back

(none)
