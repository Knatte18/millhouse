MILL_REVIEW_BEGIN
# Review: mill-merge/merge-in: squash non-ff rejection, stale-worktree logic, parent-branch detection, conflict resolution — holistic

```yaml
duration_s: 280.0
verdict: REQUEST_CHANGES
reviewer_model: sonnethigh
reviewer_self_id: Claude Sonnet (harness-reported model id: claude-sonnet-5)
reviewed_file: plan/
date: 2026-08-12
```

## Findings

### [BLOCKING:design] Batch 3 Card 7 depends on batch 2 Card 6's not-yet-existing text
**Location:** Batch 3 (mill-merge-in-and-conflict-brief), Card 7.
**Issue:** Card 7's Requirements say "see that step's own 'Liveness check (#817)' paragraph for the exact bash invocation and the JSON shape returned," referring to text batch 2 Card 6 inserts into `mill-merge/SKILL.md`. The overview's Batch Index gives batch 3 `depends-on: [1]` only — not `[1, 2]` — so nothing guarantees batch 2 has landed before batch 3's implementer reads that file.
**Fix:** Add `2` to batch 3's `depends-on` in `00-overview.md`'s Batch Index (or restate the exact bash/JSON shape directly in Card 7 instead of cross-referencing).

### [BLOCKING:scope] Card 2 names `_paths.resolve_task_path`; `_paths.py` missing from Context
**Location:** Batch 1 (parent-liveness-module), Card 2.
**Issue:** step 4 of the Requirements cites "the documented legacy-layout fallback `_paths.resolve_task_path` uses elsewhere" as the rationale for the `_mill/status.md`→`task/status.md` retry order. `_paths.py` is not in Card 2's `Context:` (`_archive_tag.py`, `_marker.py`, `_subprocess_util.py`) or `Edits:`, and `_parent_branch.py` (the Edits target) never itself imports `_paths` — verified by reading the full current file.
**Fix:** Add `plugins/mill/scripts/_paths.py` to Card 2's `Context:`.

### [NIT:consistency] Card 6's rebind snippet re-derives status_path outside the file's own convention
**Demoted-from:** BLOCKING
**Location:** Batch 2 (mill-merge-skill-fixes), Card 6.
**Issue:** the rebind bash block computes `status_path = _paths.resolve_task_path(_paths.resolve_hub_path(), '_mill/status.md')` inside a fresh `python -c` call, instead of reusing `mill-merge/SKILL.md`'s own established pattern already in scope by Entry Step 4 — `worktree_root = _paths.resolve_active_hub(container_path, slug, cfg=cfg, git_root=git_root)` then `status_path = _paths.resolve_task_path(worktree_root, cfg['paths']['status_md'])` (Path Setup 1.5, restated at the top of Step 4). `resolve_hub_path()` walks from cwd rather than the resolved `worktree_root`, and the literal `'_mill/status.md'` bypasses the config-driven `cfg['paths']['status_md']` the rest of the file always reads.
**Fix:** Rewrite the rebind snippet to derive `status_path` the same way the rest of Step 4 does (pass `worktree_root` and `cfg['paths']['status_md']` into the one-liner) instead of a second, divergent resolution path.

## Verdict

REQUEST_CHANGES
Two Context-completeness/dependency gaps and one status_path-derivation inconsistency need fixing before approval.
MILL_REVIEW_END
