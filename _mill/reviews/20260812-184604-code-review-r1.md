MILL_REVIEW_BEGIN
# Review: _plan_validate: context-completeness fires on forbidding/explanatory file mentions — holistic

```yaml
duration_s: 94.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-12
```

## Findings

None. Verified end-to-end:

- `_plan_validate.py:1372-1440` — `_PROHIBITION_MARKERS` fully removed (zero remaining references repo-wide); `_PROHIBITION_NEGATIONS`, `_PROHIBITION_VERB_FORMS`, `_PROHIBITION_NEGATION_RE`, `_PROHIBITION_VERB_RE`, `_is_prohibition_exempt` match Card 1's specified content verbatim, including the docstring's known-limitations note.
- `_plan_validate.py:1610-1613` — call site in `_check_context_completeness` correctly replaced with `_is_prohibition_exempt(lowered_line)`; `lowered_line` assignment and the untouched citation-marker block below it are preserved as required.
- `_PATH_CANDIDATE_EXTENSIONS`, `_extract_requirements_text`, `_card_own_reference_set` untouched, per Card 1's explicit scope guard.
- `mill-plan/SKILL.md:564-567` — new Principles bullet placed exactly where Card 2 specified (after the "Express renames as `Moves:`" bullet, before `## Board discipline`), text matches verbatim.
- `test-plan-validate.py` — all 5 new regression tests (untested-existing markers, new verbs, write-irregular, unrelated-negation-not-exempted, verb-without-negation-not-exempted) present with bodies matching Card 3's spec exactly (fixtures, requirements text, assertions), and registered in the `tests` list in the correct order immediately after `test_check_context_completeness_clean_prohibition_marker_change_modify` and before the `requirements-quote-indent-drift` section comment.
- Pre-existing baseline tests (`..._clean_prohibition_marker` using "forbid"+"touching", `..._change_modify` using "do not change"/"must not modify") still pass under the new predicate — verified by tracing the regex logic against their fixture text.
- No out-of-plan files: the 3 touched files match the plan's "All Files Touched" list exactly; no duplicated helper logic introduced.

## Verdict

APPROVE
Implementation matches both batches' cards and Shared Decisions exactly; no deviations found.
MILL_REVIEW_END
