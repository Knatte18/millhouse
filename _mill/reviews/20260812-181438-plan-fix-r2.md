# Plan fix — round 2

Source review: `20260812-181308-plan-review-r2.md`

## Fixed

- `[BLOCKING:design] New unit-test fixtures break on Windows via Path.resolve() drive semantics` — rewrote all five new Card 2 test bodies to build fixtures from `tempfile.TemporaryDirectory()` (mirroring `test_resolve_plugin_template_path_stale_root`'s existing pattern) instead of hardcoded POSIX string literals, so both sides of each equality assertion resolve identically on every platform.
- `[NIT:consistency] "--only"-scoped language misapplied in Batch Tests rationale` — reworded the Batch Tests paragraph to drop the incorrect `--only`-scoping reference (`test-config.py` has no function-level selection mechanism; `run-all.py --only` scopes by file, not function) and state plainly that this batch runs the whole file.

## Pushed Back

(none)
