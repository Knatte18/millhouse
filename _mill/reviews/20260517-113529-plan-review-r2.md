# Review: 63 (A) — Reviewer tool-sandbox: git snapshot guard + fix --allowedTools

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-17
```

## Findings

### [BLOCKING] review-discussion.md template is corrupted; batch 4 preserves the corruption
**Location:** batch 4, card 8; `plugins/mill/templates/review-discussion.md`
**Issue:** After the line "Omit the `## Findings` section entirely if there are zero findings. Never invent findings to pad the review.", the template file contains a large block of content that does not belong in a discussion review: a second `## Source-grounding rule` section (the bulk-mode plan holistic variant referencing "## Files included manifest"), a complete set of plan holistic review criteria (14 bullets, e.g. "Batch Index DAG integrity", "Context completeness", "Global step numbering"), a filled-in plan holistic output format block (including `reviewed_file: plan/`), and a hardcoded example review verdict for the current task ("# Review: 63 (A) — Reviewer tool-sandbox..." with `reviewer_model: sonnetmax`). Card 8's requirement — "Each template file's existing content is preserved verbatim — no deletion, no reordering, no edit beyond the prepend" — means the identity header will be prepended to this corrupted file as-is. A discussion reviewer that receives this template gets contradictory criteria (discussion GAP/NOTE vocabulary plus plan BLOCKING/NIT vocabulary), wrong output format instructions (plan holistic format), and a task-specific example output hard-coded at the bottom.
**Fix:** Before prepending the identity header, strip the corrupted content from `review-discussion.md`: everything from the second `## Source-grounding rule` heading onward (i.e., all content after the "Omit the `## Findings` section entirely..." closing line) must be removed. The plan must state this as an explicit edit step in card 8, not leave it as "verbatim preservation." Because `verify: null` is set for batch 4, no test would catch this if left unfixed.

### [NIT] `check=False` passed explicitly to `_subprocess_util.run` is redundant
**Location:** batch 1, card 1; `_capture_head_sha` and `_capture_porcelain` implementations
**Issue:** `_subprocess_util.run` already defaults to `check=False` per its signature; passing it explicitly is noise.
**Fix:** Omit the `check=False` keyword argument in both private helper calls; the callers already inspect `result.returncode` manually.

### [NIT] Batch 4 verification step is informal with no clear operator instruction
**Location:** batch 4, card 8, requirement 4
**Issue:** "Verify by reading the top 5 lines of each post-edit; they must be byte-identical" is written as a check the implementer performs during development, but `verify: null` means mill-go will not enforce it and there is no test surface to catch a mis-applied prepend.
**Fix:** Add a simple shell one-liner (e.g., `head -5 plugins/mill/templates/review-*.md`) to the batch's commit checklist so the implementer has a concrete manual gate rather than an informal reminder.

## Verdict

REQUEST_CHANGES
One blocking defect: `review-discussion.md` is corrupted and batch 4 would preserve the corruption unchanged.