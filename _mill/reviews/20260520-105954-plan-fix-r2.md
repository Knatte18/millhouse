# Plan Fix — round 2

```yaml
source_review: 20260520-105113-plan-review-r2.md
round: 2
```

## Fixed

- `20260520-105113-plan-review-r2.md` / "[BLOCKING] repo-named-'worktrees' false positive in resolve_common_dir_parent and list_worktrees": Added `and git_dir.parent.parent.name == ".git"` to the linked-worktree discriminator in both `resolve_common_dir_parent` and `list_worktrees` specs in batch 01 card 2. A coincidentally-named main repo at `<root>/worktrees/` would have `.git` as grandparent only if it IS a linked worktree; a main repo named "worktrees" has a non-`.git` grandparent.

- `20260520-105113-plan-review-r2.md` / "[BLOCKING] Batch 4/5 verify test files not analyzed for stale-mock risk": Investigated all three files. `test-worktree.py`: all `patch("_worktree._subprocess_util.run", ...)` calls target `remove_safe` write operations only — confirmed no mock patches for `list_worktrees`; added explicit confirmation to batch 04 Batch Tests. `test-cleanliness.py`: nine tests patch `_subprocess_util.run` for `capture_snapshot` and `compute_new_dirt` against a plain tempdir — will fail after migration. Added card 12 to batch 05 specifying the fix (replace with `patch("_cleanliness._pygit2_util.status_porcelain", ...)` using `list[str]` return values). `test-review-common-guard.py`: uses `subprocess.run` (stdlib) for real git fixture creation only, no mock patches on `_capture_head_sha` or `_capture_porcelain`; added explicit confirmation to batch 05 Batch Tests.

- `20260520-105113-plan-review-r2.md` / "[NIT] Windows path format divergence in list_worktrees output": Updated `list_worktrees` return spec in batch 01 card 2 to use `resolved_path.as_posix()` for both main and linked worktree paths, producing forward-slash format on all platforms and matching the existing subprocess implementation's output format.

## Pushed Back

(none)
