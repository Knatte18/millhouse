# Plan fix report — round 4

Source review: `_mill/reviews/20260812-091916-plan-review-r4.md`

## Fixed

- r4 "Card 20's single stated exception omits a confirmed same-file collision" (BLOCKING:design) — accurate, and the sharpest finding of the review so far. Confirmed against the source: SKILL.md:332, step 5's sentence beginning "The old behaviour made the orchestrator read the reviewer's entire final message", cites `(see step 3 of "Code Review loop")` — a namespace-3 reference physically inside `## Agent-mode dispatch`. Round 3's fix widened card 20 to a full in-section sweep with exactly one exception (cross-skill citations), which does not cover this; card 21 lists the same reference as a known collision site but is scoped outside the section, so it fell through the gap between the two cards. Card 20 now states two exception classes, names this instance explicitly, and adds the general rule that subsumes both: a reference shifts only when it names a step of the Agent-mode dispatch list itself, and physical location inside the section is not grounds to remap. Card 21's known-collision entry re-scoped to a cross-check ("re-read it here, do not edit it") so ownership is unambiguous.
- r4 "History note's line count is stale by one" (NIT:consistency) — the same claim as r1, which was pushed back on argument. Rather than repeat the argument, card 17 now settles it mechanically: it must run `git show 356da5e5:plugins/mill/skills/mill-go-base/SKILL.md | wc -l` before writing the note and use whatever that returns. The literal stays 1483 because that command was run for this fix pass and returned 1483 (`od -c` on the same content shows it ends with a single `\n` after "do."), so a tool reporting 1484 is counting the empty string after the trailing newline. The card is now correct whichever number is right, which is a better outcome than either side of the dispute winning.

## Pushed Back

None. Both findings drove a real change to the plan.
