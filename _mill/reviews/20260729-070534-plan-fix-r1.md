## Fixed

- `_mill/reviews/20260729-070355-plan-review-r1.md`: "[BLOCKING] Card 5's new test crashes with SystemExit before reaching `is_inplace`" — Card 5 (Batch 1) now patches `mill_cleanup._paths.resolve_main_worktree_root` with `return_value=hub_root` in both Case A and Case B, in addition to the existing `mill_cleanup._inplace.resolve_main_worktree_root` patch, since `_resolve_inplace_mode` reaches `_paths.resolve_worktrees_dir` (which internally calls `_paths.resolve_main_worktree_root`) before ever calling `is_inplace`.
- `_mill/reviews/20260729-070355-plan-review-r1.md`: "[BLOCKING] Card 7's \"Repro\" step cannot reproduce #736 — wrong git command ordering" — Card 7 (Batch 2) now runs `git reset -q HEAD -- "_mill"` immediately before the bare unguarded checkout in the Repro step, matching Step 5's real command ordering. Also added a `feature.txt` file to the squashed diff so the later commit assertion has a real staged change to land once `_mill/` is reset out, and added a commit-content assertion confirming `_mill` is excluded from the resulting commit.

## Pushed Back

(none)
