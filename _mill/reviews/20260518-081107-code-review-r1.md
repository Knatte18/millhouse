# Review: 64 (A) — Small infra fixes batch 9 — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-05-18
```

## Findings

### [BLOCKING] Wrong keyword argument names in `bulk_files_with_diff` delimiter test
**Location:** `plugins/mill/unit_tests/test-review-common.py` (bulk_files_with_diff: END FILE delimiter test block)
**Issue:** The test calls `bulk_files_with_diff([p1, p2], start_sha=None, repo=Path(tmpdir), ratio_threshold=0.25)` but the function signature is `bulk_files_with_diff(file_paths, start_sha, project_root, threshold)`. `repo` and `ratio_threshold` are not valid parameter names — this raises `TypeError: got an unexpected keyword argument 'repo'` and causes batch 3's verify step to fail.
**Fix:** Change to `bulk_files_with_diff([p1, p2], None, Path(tmpdir), 0.25)` (positional) or use the correct keyword names `project_root` and `threshold`.

## Verdict

REQUEST_CHANGES — one test is broken by wrong keyword argument names and will fail on `run-all.py`.