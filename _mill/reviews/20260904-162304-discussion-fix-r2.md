# Discussion fix — round 2

Source review: `_mill/reviews/20260904-162233-discussion-review-r2.md`

## Fixed

- r2 NIT (demoted-from BLOCKING) "Testing §7 misattributes run-holistic site to wrong function" — Testing item 7's parenthetical corrected: the four `_review_plan.py` assembly sites span **three** functions, not two (`_review_one_batch` def 134 -> manifest 232; `prepare()` def 382 -> manifests 505 and 614; `run()` def 750 -> manifest 1045 with `batch_list`/`read_list` at 1048-1049), with an explicit note that site 4 is reached through `run()`'s own holistic dispatch path rather than `prepare()`.
- Same correction propagated into Technical context's four site bullets, which previously carried no function attribution at all — each bullet now names its enclosing function and definition line, so the two near-duplicate per-batch blocks cannot be confused again.

## Pushed Back

(none)
