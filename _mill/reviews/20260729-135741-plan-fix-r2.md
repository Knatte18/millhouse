# Plan fix — round 2

```yaml
task: mill-plan/review validation false-positives, hard-fails, and truncated failure reasons
slug: mill-plan-review-validation-gaps
round: 2
source_review: _mill/reviews/20260729-135631-plan-review-r2.md
verdict: REQUEST_CHANGES (1 BLOCKING, 2 NIT)
```

## Fixed

- "Blanket Context: none conflicts with SKILL.md's own rule" — `_mill/reviews/20260729-135631-plan-review-r2.md`: `[BLOCKING] Blanket "Context: none" conflicts with SKILL.md's own rule`. Verified against `mill-plan/SKILL.md` line 263 ("Card Context: is an allowlist ... An empty or terse Context: is a review-blocker"): the plan's blanket `Context: none` across all 12 cards does read as conflicting with that documented Principle without explanation. Added a note to the overview's "Context: is none on every card" Shared Decision explicitly marking this as a deliberate, reviewed exception — every cross-file helper called from a card has its exact signature/behavior transcribed verbatim into that card's own `Requirements:`, so the cold-start harm the Principle guards against does not apply here.
- "_run_verify_gate docstring goes stale after Card 11's enrichment" (NIT) — `_mill/reviews/20260729-135631-plan-review-r2.md`: `[NIT] \`_run_verify_gate\` docstring goes stale after Card 11's enrichment`. Verified: the current docstring says "reason set to the last 2000 characters," which becomes inaccurate once the enrichment ships. Added a docstring-update requirement to Card 11 describing the new marker-plus-tail behavior.
- "Card 4's rationale for the Creates:-only exclusion is imprecise" (NIT) — `_mill/reviews/20260729-135631-plan-review-r2.md`: `[NIT] Card 4's rationale for the Creates:-only exclusion is imprecise`. Verified: step 2 already excludes `Creates:` tokens before step 3's `resolve_existing_paths` call is ever reached, so attributing scenario (h)'s zero-findings result to `resolve_existing_paths`'s drop behavior was imprecise. Reworded Card 4 step 2 to attribute it correctly to the token-collection step itself.

## Pushed Back

(none)
