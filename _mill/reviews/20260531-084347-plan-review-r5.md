# Review: Replace manual layer letters with depends_on + isolated flags — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-31
```

## Findings

### [BLOCKING] Card 11 Test 13 retains done-task bracket assertions that Card 14 eliminates
**Location:** Batch 3 / Card 11
**Issue:** Card 11 updates Test 13's task dicts (`group: "A"` → `depends_on: []`, `group: "Z"` → `isolated: True`) but does not remove the assertions `"## **#001:** Finished A [A]"` and `"## **#003:** Finished Z [Z]"`; Card 14 explicitly specifies done tasks emit **no** bracket suffix, so those heading assertions fail once Cards 12–14 are implemented.
**Fix:** Card 11's Test 13 instructions must explicitly replace those two assertions with `"## **#001:** Finished A"` and `"## **#003:** Finished Z"` (no layer suffix).

### [BLOCKING] Card 4 migration tests (n–q) do not specify direct TinyDB insert
**Location:** Batch 2 / Card 4 (tests n–q)
**Issue:** Tests (n–q) say "insert `{"slug": "z-task", "group": "Z"}`" without naming `store._db.insert(…)` as the required method; after Card 7 wires `_validate_write`, calling `upsert_task({"slug": "z-task", "group": "Z"})` raises `ValueError` ("stray 'group' key"), which breaks every migration test when the full batch verify runs.
**Fix:** Requirements for tests (n–q) must specify using `store._db.insert({…full record…})` directly to bypass validation when setting up pre-migration fixtures, matching the pattern Card 15 test (e) uses for the daemon test (`wiki_server._store._db.insert({…})`).

## Verdict

REQUEST_CHANGES
Two BLOCKING test-specification gaps produce failing verify runs; no other issues found.