## Fixed

- `20260821-104056-plan-review-r5.md` — "[NIT:consistency] Card 1's step 4.5->3.5 rename leaves a stale cross-file reference": Card 1 now also edits `plugins/mill/skills/mill-go-base/SKILL.md:801`, replacing "step 4.5" with "step 3.5" in the two-pass-cap cross-reference sentence. Added `mill-go-base/SKILL.md` to Card 1's `Edits:` and the overview's All Files Touched.
- `20260821-104056-plan-review-r5.md` — "[NIT:consistency] Card 10 adds real `git` subprocess calls, contradicting the test file's stated no-real-git convention": Card 10 now names `_test_helpers.init_minimal_git_repo` directly (the existing pygit2-based helper) instead of an import-based hedge that didn't resolve to anything, and adds an instruction to update the module docstring's "no real git" claim to note this one documented exception.

## Pushed Back

(none)
