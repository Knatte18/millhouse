# Plan-fix report -- round 5

```yaml
slug: safe-rmtree
round: 5
source_review: _mill/reviews/20260513-084234-plan-review-r5.md
```

## Fixed

- `_mill/reviews/20260513-084234-plan-review-r5.md` [NIT] "Monkeypatch target underspecified in test scenarios" -- added an explicit "Monkeypatch target convention" sentence to Batch 1 Card 2 stipulating `with patch("_safe_rmtree._paths.resolve_container_path", ...)` (the consumer-module-namespaced form). Updated each blacklist-refusal scenario to reference this patch shape directly.
- `_mill/reviews/20260513-084234-plan-review-r5.md` [NIT] "Replacement log string content not specified" -- enumerated the four exact replacement strings in Card 3 (two docstring sentences, one inline comment, one log string), each given with the literal before/after text so the implementer does not have to invent wording.
- `_mill/reviews/20260513-084234-plan-review-r5.md` [NIT] "Inline `import shutil` removal implicit for test-millpy-spawn.py" -- added explicit guidance in Card 4 to remove the function-local `import shutil` lines at `:783`, `:837`, `:899` (inside `test_spawn_standard_layout_regression`, `test_spawn_subfolder_install_destination_layout`, `test_spawn_discovery_round_trip_subfolder` respectively) and to replace each with a function-local `import _safe_rmtree`. Left the unrelated `import shutil as _shutil` at `:647` alone (different scope, aliased, may be referenced elsewhere).

## Pushed Back

(none)
