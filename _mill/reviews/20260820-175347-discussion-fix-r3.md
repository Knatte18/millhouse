```yaml
round: 3
source: _mill/reviews/20260820-175329-discussion-review-r3.md
```

## Fixed

- `20260820-175329-discussion-review-r3.md` — "Duplicate-PR URL lookup failure has no stated
  disposition": added an explicit double-failure disposition to `duplicate-pr-detection` — when
  both the `gh pr view` retry and the REST `GET` fallback fail to yield a URL, report a fixed
  message ("...could not be retrieved — check the repository's Pull Requests tab") and stop,
  explicitly not falling through to the browser-compare fallback (which would attempt to create a
  second PR against a branch already known to have one).

## Pushed Back

(none)
