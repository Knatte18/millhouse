## Fixed

- `20260728-183946-plan-review-r1.md` — "[BLOCKING] Card 7 omits `_implementer_common.py` from Context": added `plugins/mill/scripts/_implementer_common.py` to Card 7's Context in `02-merge-in-marker-verification.md`.
- `20260728-183946-plan-review-r1.md` — "[BLOCKING] Cards 9-11 omit `millpy-merge-in-subagent.py` from Context": added `plugins/mill/scripts/millpy-merge-in-subagent.py` to Context on Cards 9, 10, and 11 in `02-merge-in-marker-verification.md`.
- `20260728-183946-plan-review-r1.md` — "[BLOCKING] Card 10 misdescribes two tests' current mocks; both will break": verified against `test-millpy-merge-in-subagent.py` — `test_15_stage_finalize_conflicts` has no `_subprocess_util.run` mock at all, and `test_19_finalize_conflicts_accepts_parity_flags` mocks `finalize_from_output` directly to avoid real git calls on a non-repo tempdir. Rewrote Card 10's Requirements to add `_subprocess_util.run` `side_effect` mocking to both tests explicitly, rather than assuming it already existed there.
- `20260728-183946-plan-review-r1.md` — "[BLOCKING] Card 16's `_setup_trio` return-tuple is misdescribed": verified against `test-merge.py:89-264` — the helper returns `(hub, wiki_path, worktree, slug)` (one worktree, `slug` literally `"demo-merge"`, branch `f"test/{slug}"`). Corrected Card 16's Requirements in `04-dirty-parent-worktree-preflight.md` to unpack correctly and derive `child_branch` from `slug`, and named the concrete tracked file (`README.md`) `_setup_trio` commits on `hub`.
- `20260728-183946-plan-review-r1.md` — "[NIT] Cards 7-8 don't spell out the no-JSON extraction case": both cards' Requirements now explicitly state that a `None` extraction from `_extract_status_json` falls through to the unchanged path, same as a non-`"success"` status.

## Pushed Back

(none — all findings verified accurate against source and fixed)
