# Discussion Fix Report — round 1

Source review: `_mill/reviews/20260515-090318-discussion-review-r1.md`

## Fixed

- [NOTE] Two `run()` call sites per script, described as one — updated Technical context to state that `millpy-implement.py` and `millpy-merge-in-subagent.py` each have two call sites, and all must be updated.
- [NOTE] Implement tests have no `_test_registry.write_to()` calls to update — corrected Testing section to specify mocking `_reviewers.load` + `_reviewers.resolve` instead of updating nonexistent `_test_registry.write_to()` calls; noted that `sonnethigh` must be present in the mock registry.

## Pushed Back

(none)
