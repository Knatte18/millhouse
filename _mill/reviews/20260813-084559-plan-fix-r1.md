# Plan fix — round 1

## Fixed

- `20260813-084213-plan-review-r1.md` — `[BLOCKING:design] Card 1's new \`_config.load_config(hub_root, worktree_root)\` call swaps the two arguments`. Verified against `_config.py`'s own docstring/params and three established call sites (`mill-go-base/SKILL.md`, `git-commit/SKILL.md`, `mill-quick/SKILL.md`), all of which feed `resolve_hub_path()`'s result as `load_config`'s first argument and `git_root` as the second. Removed the unused `hub_root = _paths.resolve_main_worktree_root(git_root)` binding entirely (nothing else in the batch consumed it, and introducing it caused the argument-order bug); Card 1 now binds only `worktree_root = _paths.resolve_hub_path()` at Entry step 1 and calls `cfg = _config.load_config(worktree_root, git_root)`, matching `mill-quick/SKILL.md`'s exact naming/shape. Card 1's title, commit message, the overview's Shared Decision prose, and the batch's own Batch Tests section were all updated to match (no more `hub_root` variable, no more `_paths.resolve_main_worktree_root(` drift-test reference).
- `20260813-084213-plan-review-r1.md` — `[NIT:consistency] Card 7's "step 2" citation for \`output_path\` doesn't match \`mill-go-base/SKILL.md\`'s numbering`. Card 7's own new sentence (authored fresh, not an edit to `mill-start/SKILL.md`) now cites the pattern's actual step numbers ("extracted at ... step 1 ..., used verbatim at its step 5") instead of reproducing `mill-start/SKILL.md`'s pre-existing "step 2" inaccuracy — fixable within this plan's own scope since it's new text, no `mill-start/SKILL.md` edit required.

## Pushed Back

(none)
