# Discussion fixer report — round 2

```yaml
slug: mill-skill-and-tooling-gaps
round: 2
source_review: _mill/reviews/20260630-043738-discussion-review-r2.md
verdict_addressed: APPROVE (2 NOTEs)
```

## Fixed

- r2 "[NOTE] Explore data flow: re-fetch vs reuse Select output" — added a data-flow
  source-of-truth statement to `millstart-fetch-body-brief`: each bash call is a fresh
  subprocess, so Phase: Explore re-calls `_client.get_task` itself and reads
  `task['body']`/`task['brief']` from that fresh result rather than relying on Select scrollback.
- r2 "[NOTE] Select printout delimiting vs status gate parse" — specified that the Phase:
  Select snippet prints `status` on its own first line (`STATUS:`) and emits `brief`/`body`
  afterward under labeled sentinel delimiters, so the "status must be active" single-line
  gate stays unambiguous even when `body` spans many lines.

## Pushed Back

(none — both NOTEs were factually accurate and harmless to fix)
