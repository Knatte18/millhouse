# Discussion-fix round 1

Source review: `_mill/reviews/20260812-180053-discussion-review-r1.md` (verdict APPROVE, 0 BLOCKING, 2 NIT).

## Fixed

- **[NIT:scope] "Fourth ERROR-only-aggregate site omitted from Scope"** — verified via
  `grep -rn "ERROR-only-aggregate" plugins/mill/skills/`: four files match, not three
  (`mill-go-base/holistic-review.md:112` was missing from Scope's In list and Technical
  context's site enumeration). Added `mill-go-base/holistic-review.md` sub-step 3.5 to Scope's
  In list alongside the other three consumer sites; corrected Technical context's file count and
  citation from three to four; corrected the parallel "three files" claim in Testing for
  consistency.
- **[NIT:consistency] "Full-stage error-kind rationale premise is inaccurate"** — verified via
  `grep -n "stage full" plugins/mill/skills/mill-plan/SKILL.md`: line 370 does document
  `--stage full` via `millpy-bg` as the second-consecutive-Agent-API-error fallback path.
  Corrected the Decisions → "error_kind bucketing" Rejected bullet to drop the false "not used
  by Agent-mode dispatch" claim and rely solely on the multi-cause rationale (the stage wraps a
  multi-round loop with many possible failure origins), which stands independent of how often
  the stage is actually invoked.

## Pushed Back

(none)
