# Discussion fix — round 3

```yaml
source_review: _mill/reviews/20260725-112935-discussion-review-r3.md
round: 3
```

## Fixed

- [NOTE] "Bug-1 field-report narrative not reconciled with confirmed-present
  inner guard" — added a caveat paragraph to Problem item 1 acknowledging
  that the inner guard (`_daemon.py:142-153`) is confirmed present and
  correctly scoped, so #684/#687/#688 (external `loomyard` repo) and even
  #677 itself (per CLAUDE.md, `${CLAUDE_PLUGIN_ROOT}` always resolves to the
  installed plugin cache, even for scripts run from a millhouse dev
  checkout) may reflect a daemon still running pre-guard cache/publish-lag
  code rather than a live gap in the guard as written today — the same
  cache-rollout-lag mechanism bug 2 exists to fix. Noted that this task's
  own fixes face the identical rollout-lag exposure until the plugin cache
  picks them up.

## Pushed Back

(none)
