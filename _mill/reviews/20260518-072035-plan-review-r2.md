# Review: 64 (A) — Small infra fixes batch 9 — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
round: 2
task: "64 (A) — Small infra fixes batch 9"
batches_reviewed: [1, 2, 3, 4]
```

## Findings

**NIT — Pre-existing unreachable success path in `_review_code.py` is untracked**

- In the current source, the `parse_verdict(raw)` success branch (blocking-count, write review file, `return ReviewResult(...)`) is dead code: it sits inside the `except ReviewError:` handler after an unconditional `return`, making it unreachable on any successful review pass
- Card 6 adds a `rounds=0` early-return at the top of `run()`; that guard fires before the buggy region and is safe as written
- The non-zero-rounds success path in `_review_code.py` remains broken in production and is not flagged by this plan for a follow-on fix
- Recommend noting this as a known pre-existing bug in the batch-9 commit message or opening a follow-on task; the structural fix is simple (dedent the success block out of the `except` handler) but out of scope here

**NIT — Card 8's implementer deferral should state the guard's return shape explicitly**

- Card 8 adds `batch_max_rounds == 0` / `holistic_max_rounds == 0` guards inside `_review_plan.run()`'s sub-sections but notes only that "the implementer must confirm early return produces a well-formed outer `ReviewResult`"
- The plan does not specify whether the guard returns from within the batch aggregation loop (leaving `reviews` partially populated) or above it (trivially safe), nor what the return value is (`None`, raise, or synthesized APPROVE `ReviewResult`)
- Cards 6–7 for the simpler reviewers return a synthesized APPROVE `ReviewResult` directly; Card 8 should mirror that pattern and state it explicitly, rather than leaving the shape unspecified for the implementer to decide
- Not BLOCKING because `_review_plan.run()` callers (`millpy-review-plan.py`) wrap the result and would surface an `AttributeError` immediately if the return is malformed, making any mistake fail loudly in testing rather than silently in production