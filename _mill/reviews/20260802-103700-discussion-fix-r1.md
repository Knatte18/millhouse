# Discussion fix report — round 1

## Fixed

- `_mill/reviews/20260802-103520-discussion-review-r1.md`: "[NOTE] Fence-normalization 'guaranteed match' claim slightly overstated" — reworded the `quote-indent-drift fence-body normalization` Decision's rationale bullet to say the `\n[ \t]*\Z` substitution matches whenever the closing fence is on its own line (the conventional form, and the only form #754/#761 hit), rather than claiming it is guaranteed for every `fence_body`; noted the unconventional same-line-closer case is still safe (no-op, never deletes real content).

## Pushed Back

(none)
