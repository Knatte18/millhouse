# Plan Fix Report — round 1

```yaml
review_file: 20260517-052658-plan-review-r1.md
round: 1
verdict: REQUEST_CHANGES
blocking_count: 1
fixes_applied: 3
pushbacks: 0
```

## Fixed

- `20260517-052658-plan-review-r1.md` [BLOCKING] "Card 6: `write_commit_push` call is wrong on three counts" — rewrote card 6 Change B to (a) explicitly write the file via `(wiki_path / "agents.yaml").write_text(new_text, encoding="utf-8")` before staging, (b) pass `["agents.yaml"]` as a list (not a dict), (c) use the positional `commit_msg` argument (not `msg=`). Added a leading note documenting the helper's `(wiki_path, relative_paths, commit_msg, *, slug)` signature so the reader does not need to grep `_wiki.py`.
- `20260517-052658-plan-review-r1.md` [NIT] "Cards 2 and 5: new tests not added to `main()` tests list" — added a "Tests-list registration (mandatory)" requirement block to card 2 (test-config.py, around line 393) and card 5 (test-reviewers.py, around line 369). Each block tells the implementer to append every new test function to the existing `tests = [...]` list in declaration order, with the rationale that unregistered functions produce false-green verifies.
- `20260517-052658-plan-review-r1.md` [NIT] "Card 5 test 9 does not exercise post-merge validation" — restructured `test_required_field_missing_after_merge_raises` from the single-entry `a: {type: single, model: foo}` to the two-entry `base: {type: single, model: foo}` + `child: {extends: base}` form so `_resolve_extends` actually runs the merge path the test name claims to exercise.

## Pushed Back

(none)
