# Plan fix report — round 2

```yaml
slug: review-plan-and-ref-paths
round: 2
source_review: _mill/reviews/20260615-114005-plan-review-r2.md
verdict: APPROVE (2 NIT)
```

## Fixed

- `20260615-114005-plan-review-r2.md` — "[NIT] Card 3(b) regression claim mismatches current resolve_ref_paths": re-scoped Card 3 assertion (b) so the #471 doubling-regression value attaches to `resolve_existing_paths` (no `git_root/root/raw` candidate today); for `resolve_ref_paths` the (b) assertion now verifies the reorder-to-primary (it already had a `git_root/root/raw` fallback at `:656` and never doubled).
- `20260615-114005-plan-review-r2.md` — "[NIT] Card 7 prose names args.skip_check; actual dest is skip_checks": changed Card 7 prose to `args.skip_checks` (parser dest per `millpy-review-plan.py:69`), matching the embedded code snippet.

## Pushed Back

(none)
