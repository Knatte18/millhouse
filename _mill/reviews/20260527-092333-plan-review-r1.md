# Review: Sub-project repo (hub_relative_path) support — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-27
```

## Findings

### [NIT] Card 6 may be no-op — verify and document
**Location:** Batch 1, Card 6
**Issue:** `test-paths.py` already has M1+sub coverage for `resolve_active_hub` with `hub_relative_path != "."`. Card says "no edit needed" path is valid, but the commit message must explicitly document the finding.
**Fix:** Implementer should scan for existing `hub_relative_path != "."` tests and document in commit message whether the card was no-op or additive.

### [NIT] Line numbers in Cards 9/10 must be re-verified at implementation time
**Location:** Batch 3, Cards 9-10
**Issue:** Plan provides specific line numbers (e.g., "line 254 for resolve_ref_paths in _review_code.py") that may drift if Batch 1/2 edits add/remove lines.
**Fix:** Implementer must re-grep counts after Batch 2 lands, before implementing Batch 3. Already called out in plan ("halt if count drifts") — no plan change needed.

## Verdict

APPROVE
Plan correctly and completely addresses hub_relative_path sub-project layout support across all five batches.