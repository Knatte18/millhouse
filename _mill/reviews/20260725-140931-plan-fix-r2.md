```yaml
round: 2
source_review: _mill/reviews/20260725-140834-plan-review-r2.md
```

## Fixed

- `[BLOCKING] Card 3's rewritten find_active_slug drops the single-marker fallback, breaking an existing test and a real recovery path` — Card 3 (`02-on-disk-first-resolution.md`) corrected: restored `if len(matches) == 1: return matches[0].stem` inside the `except _marker.MarkerError` handler, so the pre-existing "daemon fails, trust the lone unconfirmed marker" fallback is preserved exactly as today. This is what the existing, unmodified `test-review-common.py` test ("find_active_slug glob fallback: one .active file -> returns slug") already requires. Updated the docstring and rationale text to describe the corrected three-way behavior (confirmed-marker fast path / unconfirmed-marker-then-daemon / daemon-failure-fallback).

## Pushed Back

(none — the finding was confirmed accurate against source and the existing test suite, and fixed)
