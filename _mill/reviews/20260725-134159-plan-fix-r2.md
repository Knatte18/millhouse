# Plan fix: round 2

## Fixed

- `20260725-134159-plan-review-r2.md` — "[NIT] Card 8's Context: omits `_review_common.py`": added `plugins/mill/scripts/_review_common.py` to Batch 03 Card 8's Context: (Requirements already names `finalize_scope()` and `count_unrecognized_severity_findings()`, both defined there).
- `20260725-134159-plan-review-r2.md` — "[NIT] Card 10 updates only `run()`'s docstring, not the module-level 'Checks performed' list": extended Batch 04 Card 10's Requirements to also append `commit-none-with-content` to `_plan_validate.py`'s module-level top-of-file docstring check list, mirroring cards 1/9's "Public API" docstring sync requirement.
- `20260725-134159-plan-review-r2.md` — "[NIT] Card 15 calls a new parameter 'keyword-only' without adding `*` to the signature": clarified Batch 06 Card 15's Requirements to explicitly insert `*,` before `cards_done` in `_reclassify_verify_failure`'s signature, making both `cards_done` and the new `commit_none_card_ids` genuinely keyword-only. Verified all 4 internal call sites plus the 3 test call sites already pass `cards_done=`/`card_ids=` correctly (either keyword or, for `card_ids`/`session_id`, positional-before-the-new-`*`) so this is a safe, non-breaking signature change.

## Pushed Back

(none)
