# Plan-fix round 3

Review: `_mill/reviews/20260801-162342-plan-review-r3.md`

## Fixed

- [BLOCKING] "Card 9 scope-violations self-resolve can't reach its own halt path, risks destructive delete" — confirmed the binary classify rule left no path to the described halt condition and risked a wrong-guess destructive `git clean`. Added an explicit third "leave untouched when uncertain" branch to Card 9 so unclassifiable paths correctly reappear in the re-run's `blocking_paths` and trigger the existing halt.
- [BLOCKING] "Card 10 (mill-merge stale-worktree self-resolve) omits the status.md audit-trail append" — confirmed Shared Decision `audit-trail-via-status-timeline` lists `mill-merge-self-resolve` under "Applies to" and Card 10 had no append for either resolution outcome. Added `_status.append_phase(status_path, f"self-resolved-stale-worktree-{mode}", ...)` + commit/push to both the `inplace` and `worktree` resolution paths.
- [NIT] "Card 8's 'genuinely ambiguous' halt framing doesn't match its unconditional-commit self-resolve step" — confirmed the self-resolve step commits all in-scope dirt unconditionally with no ambiguity judgment. Reworded the halt parenthetical to name the real cause (commit/re-check failure or concurrent new dirt).
- [NIT] "Card 10's 'entry present/current/branch matches → worktree' branch may be logically unreachable" — confirmed the reviewer's own hedge that this ambiguity is pre-existing in the source (the trigger condition is unchanged by this card, only the post-trigger action is). Added a clarifying note explaining what "the branch matches" compares and why the `worktree` branch may rarely fire, without altering the byte-exact quoted trigger text (which is out of this card's scope — Decision `stale-worktree-self-investigates` only changes the action taken after the ambiguity is detected).

## Pushed Back

(none — all four findings were accurate and fixed)
