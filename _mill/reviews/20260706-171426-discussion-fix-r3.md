# Discussion fix: round 3 NOTE resolution

```yaml
task: Fix mill-merge-in stale ref check, PowerShell 5.1 ConvertTo-Json, and _status str-path crash
slug: mill-script-fixes
round: 3
source_review: _mill/reviews/20260706-171352-discussion-review-r3.md
```

## Fixed

- **`$MERGE_REF` carry-over vs re-derivability** (source: `20260706-171352-discussion-review-r3.md`, "[NOTE] $MERGE_REF carry-over vs re-derivability"): replaced the plain-shell-variable threading mechanism with a deterministic re-derivation from durable git refs (`refs/remotes/origin/<parent-branch>` + `git merge-base --is-ancestor`), re-run identically in both Step 1 and Step 3 instead of relying on cross-block shell-variable persistence.
- **Local parent ahead of origin is now missed** (source: `20260706-171352-discussion-review-r3.md`, "[NOTE] Local parent ahead of origin is now missed"): added an `--is-ancestor` check to the ref-resolution logic so `origin/<parent-branch>` is only chosen when the local branch is behind-or-equal to origin; local-ahead and diverged cases fall back to merging the local branch, preserving today's behavior and never dropping unpushed local commits.

## Pushed Back

(none)
