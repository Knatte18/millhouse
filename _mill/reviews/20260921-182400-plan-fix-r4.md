# Plan fix — round 4

Source review: `20260921-182243-plan-review-r4.md`

## Fixed

- `[BLOCKING:scope] Card 6's TaskOutput-unavailable fallback documents 2 of 4 identical probe sites`
  — extended Card 6 (batch 2) to name and fix all four "or the probe call itself errors" occurrences
  in `mill-go-base/SKILL.md` (step 3(b), step 3(c), and step 5.5's warm-`SendMessage` probe and
  `--resume-incomplete` defensive re-check), not just the two in step 3. Updated batch 2's `## Batch
  Tests` self-check to verify all four sites.
- `[NIT:consistency] renumber_after_collision doesn't shift prose cross-references to card numbers`
  — added a "known limitation, accepted" paragraph to Card 11's Requirements (batch 5), mirroring
  Card 1's established pattern, documenting that only `### Card N:` headings are rewritten, never
  free-text card-number mentions in a card's own prose.
- `[NIT:consistency] Card 3's split-logic citation omits _parse_cards's fence-awareness` — reworded
  Card 3's Requirements (batch 1) to cite `parse_commit_none_card_ids`'s actual (non-fence-aware)
  splitting loop as the pattern to mirror, not `_plan_validate._parse_cards`'s (which is
  fence-aware); added `_plan_validate.py` to Card 3's `Context:` since the corrected wording now
  names `_parse_cards` as a contrast reference.

## Pushed Back

(none — all three findings were accurate and fixed as described above)
