# Plan-fix report — round 2

Source review: `_mill/reviews/20260623-085555-plan-review-r2.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 2 NIT).

## Fixed

- [BLOCKING] "Dirty-tree gate crashes test-millpy-implement.py regression suite" (Card 6 + Card 8) — `_in_scope_dirty_stuck` now wraps the `compute_terminal_dirt` call in `try/except Exception` and returns `None` on any failure; `compute_terminal_dirt` -> `_pygit2_util.status_porcelain` raises `GitOpsError` on the non-git fixture used by `test-millpy-implement.py`. Both gate helpers are now fully self-defensive (no exception escapes), so the new calls in the self-reported-success branch — which sit outside the inferred-path `except Exception` wrapper — cannot crash finalize. The authoritative mill-go 2b gate still runs afterward. Requirements updated in `02-implementer-finalize-contract.md` Card 6.
- [NIT] "Card 11 misstates merge.model as haiku" (Card 11) — removed the specific `roles.fixer.model: haiku` / `merge.model: haiku` value claims (merge.model is haiku in the template but sonnethigh in the hub). Reworded to "change ONLY the implementer block's model: key; do not touch any other model: key in either file", which is correct regardless of the other keys' current values.
- [NIT] "Card 4 attributes the status read to run() not prepare()" (Card 4) — reworded to reference `_review_code.prepare` (the function `run` invokes, ~lines 234-248); batch-scoping logic unchanged.

## Pushed Back

(none)
