# Plan fix — round 2

## Fixed

- [BLOCKING] `verify:` commands only in Batch Index, not batch frontmatter — `_plan_dag.iter_batch_verifies` reads exclusively via `_read_batch_frontmatter(batch_path)` (each batch file's own yaml block), never the overview's Batch Index, so all 4 batches' own `verify: null` frontmatter meant `mill-merge-in`'s replay step and `millpy-fix.py`'s holistic prepare/finalize would silently skip every test suite in this plan. Set each of the 4 batch files' own frontmatter `verify:` to the same command already declared in its Batch Index entry.
- [BLOCKING] Cards 9/10/11/13 miss `_review_common.py` in Context — each card's Requirements names a symbol defined in `_review_common.py` (`resolve_existing_paths`, `finalize_scope`, `parse_verdict`, `count_unrecognized_severity_findings`) but Context: listed only `_review_plan.py` or "none". Added `plugins/mill/scripts/_review_common.py` to Context: for all 4 cards, matching the pattern already correct in Batch 1's Cards 1/2/4/6 and Batch 2's own Card 12 (which cites no `_review_common.py` symbol and correctly has no such Context entry).
- [NIT] Batch 2 Scope's round-1 fix wrongly claimed existing tests 4/16/20/21 "already cover `blocking_count == 0`" on the schema-parity-only sites — verified those tests assert only `verdict == "ERROR"` and error-message content, no `blocking_count` assertion at all. Corrected the rationale to the real justification: the aggregation's `sum(r.get("nit_count", 0) ...)` tolerates a missing/wrong key via `.get(..., 0)`, so a dedicated per-site assertion has low marginal value.

## Pushed Back

(none — all 3 findings verified accurate with no harm from fixing)
