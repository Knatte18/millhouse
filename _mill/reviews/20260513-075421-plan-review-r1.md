# Review: (A) — Add status_md to paths config + refactor 14 callsites — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-13
```

## Findings

### [BLOCKING] Card 8 names non-existent function `_remove_worktree`
**Location:** Batch 2 / Card 8, `millpy-cleanup.py` requirements
**Issue:** The requirements for lines 325 and 353 specify they are "inside `_remove_worktree`" — this function does not exist in `millpy-cleanup.py`. The two calls (`_status.read_parent_branch(_paths.resolve_task_path(record.worktree_path, "_mill/status.md"))` and `_read_phase(_paths.resolve_task_path(record.worktree_path, "_mill/status.md"))`) are inside `_apply_inplace_record`, which has signature `(record, hub_root, task_branch="")` — no `cfg` parameter. The threading instruction ("prefer threading it as a keyword arg") is correct guidance but targets the wrong function, causing the implementer to search for a function that doesn't exist. Additionally, `_apply_inplace_record` is called from two sites (`apply_plan` and `_apply_pr_reap_record`), both of which have `cfg`; both call sites must be updated. The requirements must name `_apply_inplace_record`, specify adding `cfg: dict` as a keyword arg, and list both call site updates.
**Fix:** Replace all three occurrences of `_remove_worktree` with `_apply_inplace_record`. Add explicit threading instruction: add `cfg: dict` keyword arg to `_apply_inplace_record`, update both call sites in `apply_plan` and `_apply_pr_reap_record` to pass `cfg=cfg`. For line 130 in `build_plan` (where `cfg` is also not in scope), confirm the general Card 8 fallback applies: load via `cfg = _load_config(wiki_path, hub_root)` inside `build_plan`.

## Verdict

REQUEST_CHANGES — one blocking: Card 8 cites a non-existent function `_remove_worktree`; fix by replacing with `_apply_inplace_record` and specifying both call-site updates.