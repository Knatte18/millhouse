# Discussion fix — round 1

Source review: `_mill/reviews/20260918-175022-discussion-review-r1.md` (verdict APPROVE, 1 NIT,
1 finding demoted from BLOCKING by the `blocking_classes: [design]` ceiling since its class is
`consistency`).

## Fixed

- **"schema.md line 59 paragraph: delete-whole vs. keep-part conflict"** — confirmed accurate:
  `review-output.schema.md`'s line-59 paragraph is two sentences sharing one line, and the
  Decisions section instructed deleting "the paragraph" while the Technical context section, on
  the same cited line, said the `apply_actual_model_override()` text there "does not need to
  change" — contradictory instructions for the same line. Fixed by rewording both the
  `reviewer-self-id-removed` Decision and the `review-output.schema.md` Technical context bullet
  to state precisely: delete only the first sentence (the `reviewer_self_id` explanation); keep
  and reword the second sentence (the `apply_actual_model_override()` / `reviewer_model`
  explanation) so it stands alone without the now-nonexistent "distinct from `reviewer_self_id`"
  contrast. Added the exact replacement wording for the second sentence to the Decision so
  mill-plan does not have to re-derive it.

## Pushed Back

(none)
