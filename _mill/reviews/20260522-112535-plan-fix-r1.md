# Plan Fix Report — round 1

## Fixed

- `20260522-112201-plan-review-r1.md` — "[BLOCKING] `FIELD_OK` missing": `FIELD_OK = "ok"` was already present in Card 1's constant list (factually wrong finding; pushed back on the factual claim). Fixed the real consistency issue: Cards 5, 6, and 8 now reference `FIELD_OK`, `FIELD_ERROR_TYPE`, `FIELD_ERROR` as Python constants instead of bare strings in dict-construction requirements.
- `20260522-112201-plan-review-r1.md` — "[BLOCKING] `path_guard("")` spec/test contradiction": Added explicit empty-string guard to Card 3: `path_guard` raises `WikiPathError` when `rel_path` is empty (`not rel_path`). Also dropped the underspecified `(base / rel_path).resolve()` escape check; replaced with `Path(rel_path).parts` check for `..` components only (addresses NIT #7 simultaneously).
- `20260522-112201-plan-review-r1.md` — "[BLOCKING] `CREATE_BREAKAWAY_FROM_JOB` absent from Windows spawn": Added `CREATE_BREAKAWAY_FROM_JOB = 0x01000000` to Card 6 `_spawn_server` Windows creationflags, with an explanatory note that it is required to escape the VS Code / CC Job Object.
- `20260522-112201-plan-review-r1.md` — "[BLOCKING] Integration test not covered by `verify:`": Added `verify: "PYTHONPATH=plugins/mill/scripts python plugins/mill/integration_tests/test-wiki-e2e.py"` to Batch 7 frontmatter and updated the overview DAG entry to match.
- `20260522-112201-plan-review-r1.md` — "[NIT] `datetime` not in `_daemon.py` imports list": Added `datetime` to the stdlib imports list in Card 4.
- `20260522-112201-plan-review-r1.md` — "[NIT] `WikiPathError` imported but unused in `_store.py`": Removed the `WikiPathError` import from Card 2 requirements.
- `20260522-112201-plan-review-r1.md` — "[NIT] `path_guard` dummy base unspecified": Resolved by the Card 3 rewrite — no dummy base is used; the check is part-based only.

## Pushed Back

- `20260522-112201-plan-review-r1.md` — "[BLOCKING] `FIELD_OK` missing" (factual claim only): `FIELD_OK = "ok"` appears explicitly in Card 1's constant list (`OP_READ = "read"`, `OP_WRITE = "write_commit_push"`, `FIELD_OK = "ok"`, `FIELD_CONTENT = "content"` …). The finding is factually inaccurate with respect to Card 1. The consistency issue (usage in Cards 5, 6, 8) was fixed separately above.
