# Plan fix — round 2

Source review: `20260921-180614-plan-review-r2.md`

## Fixed

- `[BLOCKING:design] Card 3's full-history-fallback success envelope hardcodes a stale commit_sha` —
  Card 3's Requirements (batch 1) now build the success envelope without a `commit_sha` field and
  call the existing `_attach_commit_sha(_success, project_root)` helper to resolve the actual current
  HEAD, instead of hardcoding the `start_sha` parameter (which is stale whenever the bypassed
  demotion is the `_is_only_start_batch_commit` housekeeping-commit case, where HEAD is one commit
  ahead of `start_sha`).

## Pushed Back

(none — the finding was accurate and fixed as described above)
