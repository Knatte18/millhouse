# Plan fixer report — round 1

```yaml
slug: mill-spawn-and-workflow-integrity
round: 1
verdict_addressed: REQUEST_CHANGES (2 BLOCKING, 2 NIT)
source_review: _mill/reviews/20260625-071322-plan-review-r1.md
```

## Fixed

- `20260625-071322-plan-review-r1.md` — [BLOCKING] "Card 5 names a nonexistent `branch` field on `read_status`": verified `_status.read_status` (line 545) returns a summary dict without `branch`; rewrote Card 5 to resolve the branch via `_status.read_branch(status_path, cfg=cfg, slug=slug)` (line 760) and explicitly warned against `info["branch"]`.
- `20260625-071322-plan-review-r1.md` — [BLOCKING] "Card 11 test asserts a substitution the template lacks until Card 12": scoped Card 11's test to assert the render-token MAP contains `PARENT_BRANCH` (empty-string-safe), not the real-brief substitution; moved the rendered-brief assertion to Card 12 (added `test-millpy-implement.py` to Card 12 Edits) and added an explicit map-first sequencing note.
- `20260625-071322-plan-review-r1.md` — [NIT] "Card 9 verify-gate insertion point underspecified (5 call sites)": reworded Card 9 to factor a single `_run_verify_gates` helper that runs the batch gate then the module-wide gate, and to route all five `_run_verify_gate` call sites (`_forward_output` ~567/~691/~746/~802 + `finalize_from_output`) through it so no success path skips the module-wide check; added a test for each routed path.
- `20260625-071322-plan-review-r1.md` — [NIT] "Card 11/12 omit `<PARENT_BRANCH>` from the brief's token-comment": Card 12 now also adds `<PARENT_BRANCH>` to the brief's leading token doc-comment.

## Pushed Back

(none)
