# Plan fix: round 5

## Fixed

- [source: `20260918-190103-plan-review-r5.md`] "Card numbering has a gap — Card 3 is missing" — verified accurate: `Card 3` no longer exists anywhere, confirmed by grep across `_mill/plan/` (a leftover of round 4's removal of the `_status.py` `baseline_preflight_log` trio). Adopted the finding's own offered alternative (explicit retirement over renumbering, given cards 4-11 are already reviewed and cited by number in prior fixer reports): added a "Card numbering note" to the overview's Shared Decision naming the retirement explicitly (`3` retired, never reused), and a matching note in batch 2's own Scope. Renumbering was rejected as higher-risk this late in review (would touch 8 already-approved-content cards purely for cosmetic sequencing, and would silently invalidate every existing cross-reference to a card by number in this plan's own prior fixer reports).
- [source: `20260918-190103-plan-review-r5.md`] "Batch 5's `verify: null` skips a checkable marker for Card 7" — verified accurate: card 7's `latest=True` is a real, deterministically-greppable Python keyword argument, unused by the batch's `verify: null`. Set batch 5's frontmatter `verify:` (and the mirrored overview entry) to a `python -c` assertion checking `latest=True` landed in `holistic-review.md`, narrowed the Batch Tests prose to state this covers card 7 only (card 8 remains read-through-verified, no marker retracted).
- [source: `20260918-190103-plan-review-r5.md`] "Card 8 overstates how \"identical\" the two edit sites are" — verified accurate: `SKILL.md`'s actual intervening sentence (confirmed by reading the file) is materially longer/different from `holistic-review.md`'s own. Reworded Edit B to quote `SKILL.md`'s real intervening sentence and state explicitly that only the insertion-point anchor sentence is byte-identical across both files, not the intervening sentence.

## Pushed Back

(none)
