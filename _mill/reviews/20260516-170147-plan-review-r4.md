All six findings resolved and committed. Summary of what changed:

- **Card 24** (`05-wiki-helpers-post-migration.md`): `Creates:` → `Edits:`, with explicit instruction to read the existing 426-line `test-wiki.py` and append the five new test functions rather than replace the file.
- **Card 14** (`02-loaders-refactor.md`): Deletion list expanded from two to four machine-layer tests — `test_load_config_machine_overrides_wiki` and `test_load_config_worktree_overrides_machine` added.
- **Card 19** (`03-setup-migration.md`): `_subprocess_util.SubprocessError` replaced with actual convention: check `result.returncode != 0` per call; explains why `check=True` must not be used (bypasses "nothing to commit" tolerance); top-level `except Exception` covers I/O errors.
- **Card 18** (`02-loaders-refactor.md`): All 12 load-calling tests now explicitly named in three groups (A: 11 validation tests → write to `.millhouse/agents.local.yaml`; B: missing-file test → monkeypatch `resolve_wiki_path` to raise `SystemExit`; C: reviewers-yaml fallback test → monkeypatch both `resolve_wiki_path` and `_resolve_plugin_template_path`).
- **Card 13** (`02-loaders-refactor.md`): Misleading "callers of `resolve_path` may also need adjustment" sentence removed; replaced with specific `load_config(wiki_root, ...)` → `load_config(git_root, ...)` instruction.
- **Card 3** (`01-foundation.md`): Scope note added clarifying `_autonomous.py` is scaffolding only and mill-go wiring is deferred to a follow-up task.