MILL_REVIEW_BEGIN
# Review: Merge-in conflict handling: silent marker-verification gaps, mill-config.yaml chicken-and-egg crash, and undocumented dirty-worktree squash failure — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/
date: 2026-07-28
```

## Findings

### [NIT] Card 16's new scratch fixtures aren't added to the cleanup `finally:` block
**Location:** Batch 4 / Card 16
**Issue:** `test-merge.py`'s `finally:` block only cleans up `container`/`container_nested`/`container_verify` by name, but Card 16 introduces further scratch dirs (`dirty_hub`, `untracked_hub`, and a full second `_setup_trio(<distinct container path>)` for the mid-retry case) with no instruction to register any of them there.
**Fix:** Name the new container variable(s) explicitly and add matching `if "<name>" in locals(): _safe_rmtree.safe_rmtree(...)` entries, mirroring the `container_nested`/`container_verify` pattern.

### [NIT] Card 16 case 2's fixture is heavier than its own stated rationale requires
**Location:** Batch 4 / Card 16, sub-scenario 2 (mid-Step-5-retry)
**Issue:** The card justifies lightweight fixtures for cases 1/3 specifically to avoid `_setup_trio`'s wiki/bare-repo/junction overhead, then mandates a full `_setup_trio()` for case 2 solely to obtain "a real child branch to squash" — a plain two-branch single repo (no wiki, no worktree, no junctions) would satisfy that same need.
**Fix:** Either use a lighter two-branch fixture for case 2 consistent with cases 1/3's own reasoning, or state explicitly why the full trio is required there.

## Verdict

APPROVE
Plan is sound and well-grounded in the source files; only two minor NITs on Card 16's new test-fixture hygiene.
MILL_REVIEW_END
