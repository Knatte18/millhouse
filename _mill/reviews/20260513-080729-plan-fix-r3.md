# Plan fix -- round 3

Source review: `_mill/reviews/20260513-080642-plan-review-r3.md`

## Fixed

- [NIT] `Card 3 module-docstring style reference inaccurate` -- card 3's docstring-entry requirement now points the implementer at `resolve_hub_relative_path` only (it is the entry actually present in `_paths.py`'s Public API docstring) and explicitly notes that `resolve_task_path` is in `__all__` but lacks a Public API docstring entry, so the implementer doesn't waste time hunting for absent style guidance.
- [NIT] `No unit-test coverage for bug fixes in Cards 5 and 6` -- chose the reviewer's option (b): added a "Coverage gap" paragraph to batch 2's Batch Tests section explicitly calling out that `run-all.py` does not exercise `millpy-inspect.py:77` or `millpy-status.py:49` (no `test-millpy-inspect.py` / `test-millpy-status.py` files exist), that the bug-fix correctness rests on the substitution being mechanical plus the helper being unit-tested in batch 1, and that adding thin tests for the two scripts is out of scope per the discussion's testing decision. A future reviewer reading the batch tests now sees the gap without surprise.

## Pushed Back

(none)
