## Fixed

- `_mill/reviews/20260730-110841-plan-review-r2.md`: "[BLOCKING] `_requirements_fence_aware_body` boundary loop likely self-terminates on the header line" — the header line itself matches the same stop-condition regex used for boundary detection, so a literal reading of "walk forward from the found index" would immediately stop before collecting anything. Rewrote the spec to explicitly mirror `_extract_requirements_text`'s own shape: unconditionally append the header line to the result first (`collected = [card_lines[start]]`), then start the boundary-scan loop at `start + 1`, never re-testing the header line against the stop condition.
- `_mill/reviews/20260730-110841-plan-review-r2.md`: "[NIT] Module-level fence regex constant left unnamed" — named the fence-body regex `_RE_FENCE_BODY`, following the file's existing `_RE_*` naming convention (`_RE_REFS_HEADER`, `_RE_REFS_SUB`, etc.), and updated both the definition site and the `.findall(...)` call site in Card 1 to reference it by name instead of an inline literal.

## Pushed Back

(none — both findings were factually accurate with no harm-check exemption; both were fixed)
