# Discussion-fix report r2

```yaml
slug: subprocess-fixes
round: 2
source_review: _mill/reviews/20260513-081111-discussion-review-r2.md
```

## Fixed

- `20260513-081111-discussion-review-r2.md` — `[NOTE] Watchdog reader threads undefined for non-PIPE stdout`: added a "Non-PIPE stdout/stderr override" bullet under `Decision: timeout-watchdog-stream-handling` that explicitly conditions reader-thread start on `proc.stdout is not None` / `proc.stderr is not None` and confirms `timeout=` + non-PIPE remains supported.

- `20260513-081111-discussion-review-r2.md` — `[NOTE] START sentinel "fail fast" requires caller updates not in scope`: added a "Scope clarification — sentinel is diagnostic-only in this task" bullet under `Decision: worker-start-sentinel` that marks SKILL.md poll-loop updates explicitly OUT of scope and re-frames the sentinel's value as observability for this task, with fail-fast caller behaviour deferred to a follow-on.

## Pushed Back

_(none — auto mode treats every NOTE as FIX; PUSH BACK unavailable without an operator.)_
