# Plan-fix round 1

## Fixed

- `20260918-180450-plan-review-r1.md` — "[NIT:consistency] Card 3 misdescribes run_gate's current docstring text": corrected batch `02-done-gate-reason-priority.md` Card 3 Requirement 5 to state that "a non-zero exit is reported as `blocked` with the captured output" and "so both call sites ... treat the same `gate_cmd` identically" currently live only in `run_preflight`'s docstring, not `run_gate`'s (which instead says "Mirrors `run_preflight`'s subprocess-invocation shape and never-raise contract exactly"). Also added an instruction to update `run_gate`'s own "2000-character tail truncation on failure" phrase to reflect the new enriched-truncation shape. The originally-mandated edit (updating both `Returns:` sections, which are identically shaped in both docstrings) is unchanged.

## Pushed Back

(none)
