MILL_REVIEW_BEGIN
# Review: Improve diagnosability of plan-validate errors and finalize verify-replay failures — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-4.5
reviewed_file: plan/
date: 2026-08-02
```

## Findings

None. Verified against source: all cited line numbers/function signatures in batches 1-7 match `_plan_validate.py`, `_status.py`, `_implementer_common.py`, `_verify_baseline.py`, `_plan_dag.py`, `millpy-implement.py`, and both SKILL.md files exactly (spot-checked ~25 distinct citations, no drift found). Batch Index DAG has no cycles, no forward deps, no undeclared file refs. `depends-on` accurately reflects each batch's real dependency (batch 5 on 4's helpers; batch 6 on 3/4/5's field/helpers; batch 7 on 6's wired CLI). Card numbering is globally sequential 1-25 with no gaps or duplicates. All `Moves:` are `none` uniformly, so no `## Rename mechanic` section is required and none is missing. `## All Files Touched` matches the exact union of Edits:/Creates: across all batches. Every card has Creates/Edits/Context/Moves/Requirements/Commit. `Requirements:` fields name concrete functions/line ranges/constants throughout, not vague prose. Every function/constant a card's Requirements mentions is present in that card's Context: or Edits:. The fail-safe-to-strict decision (non-vacuous subset waiver) is correctly threaded end-to-end: batch 4 builds it, batch 5 stores raw signatures via union-of-two-runs, batch 6 wires it into finalize/full, batch 7 integration-tests it. Batch 7's `verify:` runs the new integration test file directly, matching its stated deliverable.

## Verdict

APPROVE
Plan is complete, well-sequenced, and every checked source citation is accurate.
MILL_REVIEW_END
