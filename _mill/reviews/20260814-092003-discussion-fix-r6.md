# Discussion fix report — round 6

## Fixed

- `_mill/reviews/20260814-091925-discussion-review-r6.md` — "[BLOCKING:design] `blocked_resume_round` binds `reviews_dir` before it exists": deferred `blocked_resume_round`'s computation from Entry step 4 (where `reviews_dir` is not yet bound, per the file's own Path Setup section) to Phase: Plan Review's own Path Setup, immediately after `reviews_dir` is derived there and before the `revise-{N+1}` namespacing branch. Entry step 4 now only binds the plain boolean `revise_from_blocked`.
- `_mill/reviews/20260814-091925-discussion-review-r6.md` — "[NIT:consistency] `git add <plan_dir>` on the blocked-resume commit is a no-op": dropped `<plan_dir>` from the blocked branch's commit pathspec (this branch never touches `plan_dir`, unlike the sibling planned+approved branch).

## Pushed Back

(none)
