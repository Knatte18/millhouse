# Plan fix — round 2

Source review: `20260517-113529-plan-review-r2.md` (verdict: REQUEST_CHANGES, blocking_count: 1)

## Fixed

- `[NIT] check=False passed explicitly to _subprocess_util.run is redundant` — Removed the `check=False` keyword from both `_capture_head_sha` and `_capture_porcelain` example call sites in card 1's requirement text. `_subprocess_util.run`'s signature confirms `check: bool = False` is the default (verified via `inspect.signature` during Phase: Plan), so the explicit pass was noise.
- `[NIT] Batch 4 verification step is informal with no clear operator instruction` — Replaced the informal "Verify by reading the top 5 lines" sentence in card 8 requirement 4 with an explicit pre-commit verification gate: a concrete `head -5 …` shell one-liner naming all five template files, plus a hard rule "Do not commit until this gate passes." This gives the implementer a single command to run and a clear stop signal if the prepend was mis-applied.

## Pushed Back

- `[BLOCKING] review-discussion.md template is corrupted; batch 4 preserves the corruption` — **Pushed back: factually inaccurate per `mill-receiving-review` decision tree (VERIFY → factually wrong → push back with evidence).** The reviewer claims that after the line `"Omit the ## Findings section entirely..."` the file contains a second `## Source-grounding rule` section, plan holistic review criteria (14 bullets), a plan holistic output format block, and a hardcoded example review verdict. **No such content exists.** Verified by `Path('plugins/mill/templates/review-discussion.md').read_text(encoding='utf-8')`: file is 75 lines (2937 bytes); the final non-empty line at index 75 IS the closing sentence `"Omit the ## Findings section entirely if there are zero findings. Never invent findings to pad the review."` There is no content past that line. The reviewer hallucinated injected content that does not exist on disk. Batch 4 card 8's "verbatim preservation" requirement is correct as-written — there is nothing corrupt to strip. No plan change applied.
