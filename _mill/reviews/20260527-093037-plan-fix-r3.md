# Plan fix report — round 3

Source: `_mill/reviews/20260527-092944-plan-review-r3.md`

## Fixed

- `20260527-092944-plan-review-r3.md` — "[NIT] Card 9 'keep import shutil' instruction is inaccurate" — Verified: all `import shutil` statements in `test-wiki-daemon.py`, `test-wiki-store.py`, `test-wiki-sync.py` are inline imports immediately above each `shutil.rmtree(...)` call (no top-level import). Card 9's instruction rewritten: convert each callsite AND remove the inline `import shutil` lines; verify no `import shutil` remains after editing.
- `20260527-092944-plan-review-r3.md` — "[NIT] Card 2 test setup omits PROTOCOL_VERSION import" — Verified: `PROTOCOL_VERSION` is not currently imported in `test-wiki-daemon.py`. Card 2's import instruction extended to include `from wiki import PROTOCOL_VERSION, WikiStartupError` (combined with the already-required `WikiStartupError` import added in r2).

## Pushed Back

(none)
