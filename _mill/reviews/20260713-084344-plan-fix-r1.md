# Plan fixer report — round 1

Source review: `_mill/reviews/20260713-083936-plan-review-r1.md`
(verdict REQUEST_CHANGES, 2 BLOCKING, 2 NIT).

## Fixed

- `20260713-083936-plan-review-r1.md` — "[BLOCKING] Card 6 Requirements ref a file not in Context" AND "[BLOCKING] Card 6 cannot discover current script surface to adapt to": both resolved at the root by **dropping batch 02 (bootstrap-posix-test) entirely**. Verification during the fix confirmed the finding is deeper than a missing Context entry: `test-bootstrap.ps1` is an obsolete Layer-01 (pre-daemon) test — it drives the removed `millpy-list.py`, the removed `_sidebar.regenerate` seed step, and the old one-arg `_shortcuts.write_all(Path)` signature asserting `.ps1` wrappers. The current wiki is daemon-based (`millpy-add` -> `wiki/_client`), and the equivalent mill-add-against-a-throwaway-wiki coverage already exists POSIX-natively in `plugins/mill/integration_tests/test-wiki-e2e.py` (+ `test-wiki-concurrency.py`, `test-wiki-daemon-tinydb.py`). Porting the `.ps1` would produce a redundant, architecturally-dead shell duplicate, so the correct fix is to remove the batch, not to expand its Context. Recorded as `Decision: bootstrap-test-port-dropped` in `00-overview.md`; deleted `02-bootstrap-posix-test.md`; removed the `no-tmp-use-scratch` shared decision and the `test-bootstrap.sh` entry from All Files Touched (both existed only for the dropped batch). Flagged at handoff so the operator can re-add it as a separate integration-test-modernization task if wanted.
- `20260713-083936-plan-review-r1.md` — "[NIT] Batch 2 scope omits Phase 4.7 divergence from the port": moot — batch 02 dropped, so there is no ported `.sh` to scope.

## Pushed Back

- `20260713-083936-plan-review-r1.md` — "[NIT] All-Files-Touched omits Card 6's created file": **factually inaccurate**. The round-1 `00-overview.md` `## All Files Touched` listed the entry with its full repo-relative path, `plugins/mill/integration_tests/test-bootstrap.sh` (not the un-prefixed `test-bootstrap.sh` the finding claimed). No missing-entry or path-prefix defect existed. Now moot regardless: the entry was removed together with batch 02.
