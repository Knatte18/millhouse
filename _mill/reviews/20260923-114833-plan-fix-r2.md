```yaml
round: 2
source: 20260923-114712-plan-review-r2.md
```

## Fixed

- [BLOCKING:design] "Card 4's call-site simplification breaks an existing cache-count test" —
  restored the caller-side `if search_key in search_cache: ... else: ...` pre-check at the symbol
  branch's call site (only its callee arguments change, per the resolution-scope narrowing), instead
  of an unconditional call. Verified `test_check_context_completeness_symbol_cache_invoked_once_per_key`
  monkey-patches the module-level `_resolve_symbol_files` function object and counts CALLS, not
  filesystem walks — dropping the pre-check would have made it see 2 calls instead of 1. Added a note
  to card 4's Requirements explaining why the pre-check must stay.
- [NIT:consistency] "`path_to_token`'s 'first-seen wins' claim is actually alphabetical-first" —
  reworded the comment to state the actual mechanism: `cited_files_map` is built by iterating
  `sorted(raw_tokens)`, so `path_to_token` keeps whichever citing token sorts alphabetically first on
  a duplicate-path collision, not whichever card cited it first in plan order.

## Pushed Back

(none)
