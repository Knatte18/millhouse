MILL_REVIEW_BEGIN
# Review: mill-plan SKILL.md: Phase Plan Review gate, convergence, and DAG-validation correctness bugs — holistic

```yaml
duration_s: 345.0
verdict: REQUEST_CHANGES
reviewer_model: sonnethigh
reviewer_self_id: Claude Sonnet 5
reviewed_file: plan/
date: 2026-08-21
```

## Findings

### [BLOCKING:scope] Card 10 uses `resolve_ref_paths`/`ReviewError` without `_review_common.py` in Context:
**Location:** Batch 2, Card 10 (#868). **Issue:** Requirements dictates adding `ReviewError` and `resolve_ref_paths` to the `_review_common` import and calling `resolve_ref_paths([t], project_root, root, wiki_root=wiki_root, git_root=git_root, soft_fail_gitignored=True)` — an exact keyword-argument signature for a function neither currently imported nor used anywhere in `_plan_validate.py` today (unlike Card 8's `PlanDAGError`/`extract_batch_index`, which are already imported and used identically elsewhere in-file, explicitly noted as needing no `_plan_dag.py` read). Card 10's `Context:` is `none` and `Edits:` is only `_plan_validate.py`/`test-plan-validate.py`; `plugins/mill/scripts/_review_common.py` (home of both named symbols) is absent from both. **Fix:** Add `plugins/mill/scripts/_review_common.py` to Card 10's `Context:` list.

### [NIT:consistency] Widened/new checks not reflected in their own docstrings
**Location:** Batch 2, Cards 8 and 9. **Issue:** `_plan_validate.py`'s module-level "Checks performed (check keys):" docstring list (top of file) is never updated to add `cross-batch-creates-no-depends-on` (Card 8), and `_check_verify_full_suite`'s own one-line docstring summary ("Flag verify: commands that invoke run-all.py without a scoping filter") is never updated after Card 9 widens it to Go/C#/bare-pytest — both lists have historically been kept in sync with the actual check set (e.g. the existing `commit-none-with-content (#664)` entry). **Fix:** Add one line to the module docstring for the new check, and reword `_check_verify_full_suite`'s summary sentence to cover all four language heuristics.

## Verdict

REQUEST_CHANGES
Card 10 omits `_review_common.py` from Context: despite Requirements citing its exact function signature.
MILL_REVIEW_END
