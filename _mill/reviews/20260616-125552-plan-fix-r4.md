# Plan fixer report — round 4

Source review: `_mill/reviews/20260616-125510-plan-review-r4.md` (holistic, REQUEST_CHANGES, 1 BLOCKING + 1 NIT).

## Fixed

- [BLOCKING] "Batch 4 verify depends on batch 2's arrow fix" — VERIFIED accurate: batch 4's `verify:` runs `test-guards.py`, whose `_check_no_unicode_arrow` scans every `test-*.py` including `test-claude-sub.py` (arrows at lines 775/787 removed only by batch 2). With both at `depends-on: []`, batch 4 could verify before batch 2 lands and fail through no fault of its own. Added `depends-on: [2]` to batch 4 in BOTH the overview Batch Index and `04-implementer-guardrail.md` frontmatter, and documented the rationale in batch 4's Batch Scope. DAG re-validated (deps: 4 -> [2]; acyclic, no parallel-overlap).
- [NIT] "Card 7 fixture batch file has no frontmatter verify" — VERIFIED accurate: `test-millpy-implement.py`'s fixture writes the per-batch `.md` as a bare `# Batch:` line, so `_read_batch_frontmatter(...).get("verify")` would resolve to `None`. Added a note to Card 7 requiring the threading test to write a fenced-yaml `verify:` into the per-batch `.md` frontmatter (not the overview entry) so the resolved `verify_cmd` is the asserted non-null value.

## Pushed Back

- (none)
