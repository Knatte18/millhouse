```yaml
round: 3
type: discussion-fix
```

## Fixed

- 20260821-091822-discussion-review-r3.md — "#881 language-detection precedent omits a real 4th Python marker": confirmed via `_plan_validate.py:1986-1990`'s actual `is_python_project` that it ORs four markers, not three (`pyproject.toml`, `setup.py`, `setup.cfg`, and `plugins/mill/pyproject.toml`). Reworded the #881 Decision to name `is_python_project` explicitly and cite all four markers, calling out that this repo itself is detected as Python solely via the nested-plugin marker — so a shared language-detection helper that drops it would misdetect this very self-hosted repo. Noted CLAUDE.md's own three-marker phrasing has the same drift, flagged as out of scope to fix in this task.

## Pushed Back

(none — finding verified accurate against current `_plan_validate.py` source, no harm from fixing)
