# Review: Fix infrastructure bugs across merge, wiki-daemon, config, plan, and cleanup

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: _mill/plan/
date: 2026-06-06
```

## Findings

### [BLOCKING] Batch 1 verify references a non-existent test file

Batch-1 verify uses `--only test-wiki-client-retry.py`, a Creates file. Ordering must guarantee the file exists before verify runs.

### [BLOCKING] Card 4 directory-skip fix is incomplete: bulk_files_with_diff also catches only FileNotFoundError

`bulk_files_with_diff` has an identical `except FileNotFoundError` block that would also crash on a directory path; the PermissionError guard must extend there too.

### [BLOCKING] Card 3 unfenced fallback contradicts review-output.schema.md

`review-output.schema.md` documents that parse_verdict raises on missing fence; Card 3 must update that doc.

### [BLOCKING] Card 5 resolve_large_prompt_timeout wiring ambiguous (prepare vs run)

The fix applies to `_review_plan.run()` (both the primary and resume-retry `_reviewer_single.run` calls), not `prepare()`.

### [BLOCKING] Card 11 Windows process enumeration API unspecified (wmic deprecated)

Specify `tasklist /FO CSV /V /NH` (present on all Windows incl. 11); wmic is removed on recent Windows.

### [BLOCKING] Card 15 millpy-implement.py absent from Context

The Edits target must appear in Context so the implementer can read it.

### [BLOCKING] Card 16 formatter-drift detection mechanism unspecified

Specify the heuristic (e.g. `git diff -w` shows no diff while `git diff` does => whitespace-only formatter drift).

### [BLOCKING] Card 19 timeout threading path unspecified

State whether timeout already reaches `_build_psmux_argv` or specify the parameter chain.

### [BLOCKING] Card 23 finalize_from_output bypass branching ambiguous

Include millpy-merge-in-subagent.py in Context and describe the three-case branching.

### [NIT] Batch 7 verify references test-preflight.py (not yet existing)

### [NIT] Batch 8 verify references test-merge-in-subagent.py (same pattern)

### [NIT] Card 10 orphan detection "ignore OR report" is non-deterministic

### [NIT] Card 5 DRY: resolve_large_prompt_timeout duplicates maybe_switch_spec_for_large_prompt threshold logic

## Verdict

REQUEST_CHANGES
