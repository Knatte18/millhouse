# Plan-fix round 4

## Fixed

- `20260729-080033-plan-review-r4.md`: "[BLOCKING] Test-treeguard scenario 8 contradicts Card 1's mandatory disk-existence check" — scenario 8 rewritten to use a real nested-hub git fixture (a real `hub/` subdirectory of a real temp git repo, real commit, real deletion, no mocking) instead of mocking `_pygit2_util.status_porcelain`/`_subprocess_util.run` with no actual disk side effect, which was incompatible with Card 1's own disk-existence check.
- `20260729-080033-plan-review-r4.md`: "[BLOCKING] Card 2's Context omits `_treeguard.py`, whose functions/attributes its Requirements depend on" — added `plugins/mill/scripts/_treeguard.py` to Card 2's `Context:` list.
- `20260729-080033-plan-review-r4.md`: "[NIT] Imprecise line-range citation for `revert_out_of_scope_drift`" — corrected `00-overview.md`'s citation from `_cleanliness.py:324-445` to `_cleanliness.py:324-451` (verified the function's actual closing `return` line).

## Pushed Back

(none — all round 4 findings were fixed as described above)
