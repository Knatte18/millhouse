# Discussion-fix report: round 2

```yaml
round: 2
review_file: _mill/reviews/20260527-084824-discussion-review-r2.md
date: 2026-05-27
```

## Fixed

- `[NOTE] _status.read_full raises ValueError, not FileNotFoundError` — updated the Technical-context bullet for `skills/mill-go/SKILL.md` to describe the actual exception (`ValueError`) raised by `_status.read` when `status_path` is absent. Fix-logic semantics unchanged: the proposed `if not status_path.exists():` guard still short-circuits before `_status.read_full` is called, so the original recovery branch is correct regardless of exception type.

## Pushed Back

_(none — `--auto` mode disables push-back; the single NOTE was applied.)_
