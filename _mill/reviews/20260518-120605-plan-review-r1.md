# Review: Keep psmux TUI alive across calls for session continuity — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-18
```

## Findings

### [BLOCKING] Card 9 derives psmux_name unconditionally, contradicting Test K2
**Location:** Batch 3 / Cards 9 and 11
**Issue:** Card 9 requirement (c) says "Immediately above the `argv = _build_psmux_argv(...)` call, derive `psmux_name = f"mill-{session_id[:12]}"` — no conditional. Card 8 then emits `--psmux-session psmux_name` whenever `psmux_session_name is not None`, meaning `--psmux-session` always appears in argv. Test K2 explicitly asserts `--psmux-session` is NOT in argv when `session_id=None`. Following Card 9 literally makes the batch 3 verify fail.
**Fix:** Add a condition in Card 9 requirement (c): `psmux_name = f"mill-{session_id[:12]}" if caller_provided_session_id else None`. This matches the keep-alive gating rule (both flags gate on `caller_provided_session_id`) and makes K2 pass.

### [NIT] Test S5 omits required `_wait_for_marker_in_pane` mock setup
**Location:** Batch 2 / Card 7, Test S5
**Issue:** S5 sets `_wait_for_idle_prompt` → False to trigger the boot-timeout error, but the preceding CLAUDE_READY check (`_wait_for_marker_in_pane`) also needs to return True; otherwise the error fires at Step 7 rather than Step 9 — the assert still passes coincidentally but the test exercises the wrong path.
**Fix:** Add "mock `_wait_for_marker_in_pane` to return True" in the S5 setup description so the test reliably exercises the intended error site.

## Verdict

REQUEST_CHANGES
Card 9's unconditional `psmux_name` derivation directly contradicts Test K2; batch 3 verify fails as written.