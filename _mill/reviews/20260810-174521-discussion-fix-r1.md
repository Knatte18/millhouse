# Discussion-fix report — round 1

Source review: `_mill/reviews/20260810-174458-discussion-review-r1.md`

## Fixed

- "SKILL.md fix-table row is only partially stale, not uniformly" — clarified the
  `moves-source-message-qualifier (#793)` Decision in `_mill/discussion.md` to note that
  `mill-plan/SKILL.md` line ~320's `context-completeness` fix-table row has two clauses: the first
  (`Edits:/Creates:/Deletes:/Moves:`) is stale and needs the `-source` suffix, while a later clause
  (`Deletes:/Moves:-source`) is already correctly qualified. Instructed fixing only the first
  occurrence to avoid a blind find/replace producing `Moves:-source-source`.

## Pushed Back

(none)
