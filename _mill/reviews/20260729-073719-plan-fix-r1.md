# Plan-fix round 1

## Fixed

- `20260729-073400-plan-review-r1.md`: "[BLOCKING] check_and_restore trusts an unchecked git-checkout restore" — Batch 1 Card 1 steps 6-8 rewritten to verify per-path restore outcome via `Path.exists()` after the checkout subprocess call and never return `triggered: True` when nothing was actually restored. Added a new Card 2 test scenario (7) mocking `_subprocess_util.run` to simulate a failed restore and asserting it is never reported as triggered.
- `20260729-073400-plan-review-r1.md`: "[BLOCKING] mill-start's ERROR-only retry dispatch (step 3.5) is not tree-guard-bracketed" — Batch 3 Card 5 now adds pre/post tree-guard checkpoints around step 3.5's Agent-mode re-dispatch.
- `20260729-073400-plan-review-r1.md`: "[BLOCKING] mill-plan's ERROR-only retry dispatch (step 4.5) is not tree-guard-bracketed" — Batch 4 Card 6 now adds pre/post tree-guard checkpoints around step 4.5's Agent-mode re-dispatch.
- `20260729-073400-plan-review-r1.md`: "[BLOCKING] mill-go's ERROR-retry ... Agent-mode dispatches left explicitly unbracketed" — Batch 5 Card 7 (per-batch loop step 4.5) and Card 8 (Holistic loop step 3.5) now add pre/post tree-guard checkpoints around each ERROR-retry re-dispatch of `millpy-review-code.py`.
- `20260729-073400-plan-review-r1.md`: "[NIT] Contradictory ordering guidance in check_and_restore" — Batch 1 Card 1 step 4 no longer claims to preserve unsorted order; the moot "do not re-sort" wording was dropped as part of the step 6-8 rewrite.
- `20260729-073400-plan-review-r1.md`: "[NIT] Vacuous 'record-shape' test assertion" — Batch 1 Card 2 scenario 6 replaced the vacuous claim with a concrete mtime/content-unchanged assertion on `status.md` across the `check_and_restore` call.
- `20260729-073400-plan-review-r1.md`: "[NIT] No test for `_find_recovery_log_block`'s malformed-section ValueError path" — Batch 2 Card 4 adds scenario 5 covering both the missing-fence and unterminated-fence `ValueError` cases.

## Pushed Back

- `20260729-073400-plan-review-r1.md`: "[BLOCKING] mill-go's ERROR-retry and fixer Agent-mode dispatches left explicitly unbracketed" (fixer-dispatch half only) — the finding's suggested fix asks to "reconsider whether fixer dispatches need the same treatment or document why they're categorically safe." Documented instead of fixed: `millpy-fix.py` is not one of the three review CLIs (`millpy-review-discussion.py`/`millpy-review-plan.py`/`millpy-review-code.py`) named in `_mill/discussion.md`'s Scope (In), and Scope (Out) explicitly excludes "any... non-review-loop phase," limiting this task to "the three review loops (discussion/plan/code+holistic)." Bracketing the fixer dispatch (or, by the same logic, Implement/merge-in dispatches sharing the same shared dispatch section) would expand scope beyond what the approved discussion decided. Added an explicit "out of scope — do not bracket" note to Batch 5 Card 7 citing this reasoning, rather than silently leaving it unaddressed.
