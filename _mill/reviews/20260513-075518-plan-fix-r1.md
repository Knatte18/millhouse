# Plan fix -- round 1

Source review: `_mill/reviews/20260513-075421-plan-review-r1.md`

## Fixed

- [BLOCKING] `Card 8 names non-existent function _remove_worktree` -- card 8's `millpy-cleanup.py` requirements rewritten: line-130 entry now correctly identifies `build_plan` as the host function and instructs the implementer to load `cfg` internally via the existing `_load_config` helper (no signature change to `build_plan`); line-325/353 entry now correctly names `_apply_inplace_record` as the host function and prescribes adding `cfg: dict` as a keyword-only argument, with explicit instructions to update both call sites (`_apply_pr_reap_record:520` and `apply_plan:549`). The reviewer's specific fix prescription was applied verbatim.

## Pushed Back

(none)
