# Plan fix — round 1

```yaml
slug: mill-go2-scaffold
round: 1
review: 20260811-121359-plan-review-r1.md
```

## Fixed

- `[BLOCKING:scope]` "Card 10 Requirements cites `_implementer_common.py` without a Context entry" — rephrased card 10's caution in `03-repoint-refs.md` to point at the `script-side-prefixes-unchanged` Shared Decision (which names the parser and the rationale) instead of naming the file inline, so the card no longer references a file outside its own allowlist.
- `[NIT:consistency]` "Card 6 Context lists two files that don't exist at that card's execution time" — dropped `plugins/mill/skills/mill-go/SKILL.md` and `plugins/mill/skills/mill-go2/SKILL.md` from card 6's `Context:` in `02-thin-variants.md`, and added an explicit Requirements note that the test builds its paths as `SKILLS / <name> / "SKILL.md"` and is written against the contract rather than against file content the implementer can inspect.

## Pushed Back

_None._
