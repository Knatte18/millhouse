MILL_REVIEW_BEGIN
# Review: Skill doc/table accuracy gaps across mill-groom, mill-start/mill-plan, and implementer-brief — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/
date: 2026-07-25
```

## Findings

### [BLOCKING] Card 2 Context omits mill-go/SKILL.md, which Requirements/Batch Tests cite by name
**Location:** Batch 1, Card 2 (mill-start `--agent-output`)
**Issue:** Requirements justifies the fix by pointing at "the general Agent-mode dispatch pattern's step 2 in `mill-go/SKILL.md`", and the batch's own Batch Tests verification for Card 2 requires cross-checking phrasing against `mill-go/SKILL.md`'s step 6 ("from the prepare envelope's `output_path` field") — yet Card 2's `Context:` is `none`, and `mill-go/SKILL.md` is not in `Edits:` either.
**Fix:** Add `plugins/mill/skills/mill-go/SKILL.md` to Card 2's `Context:` so the implementer/verifier has an allowlisted path to the file it is instructed to match phrasing against.

### [NIT] Card 1 misattributes a downstream `<WIKI_PATH>` usage site to Step 2
**Location:** Batch 1, Card 1 (mill-groom Entry checks)
**Issue:** Requirements says the new single-step resolution must match "the existing downstream usage of `<WIKI_PATH>` later in the file (`## Step 2 — Read task list` and elsewhere)", but `## Step 2 — Read task list` does not reference `<WIKI_PATH>` at all — it independently re-derives the wiki path inside its own Python one-liner. Actual `<WIKI_PATH>` usages are in Step 4 (line ~131) and Step 6 (lines ~179, ~190).
**Fix:** Correct the citation to "Step 4 and Step 6" (or "elsewhere in the file") so the implementer isn't sent looking for a nonexistent `<WIKI_PATH>` reference in Step 2.

## Verdict

REQUEST_CHANGES
Card 2 lacks Context for a file it explicitly cites for correctness/consistency verification.
MILL_REVIEW_END
