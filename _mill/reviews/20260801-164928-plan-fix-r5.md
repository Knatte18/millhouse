# Plan-fix round 5

Review: `_mill/reviews/20260801-164621-plan-review-r5.md`

## Fixed

- [BLOCKING] "Verify/logic self-resolve plan edits are never committed or logged" — confirmed both Batch 2 Card 4 and Batch 3 Card 6's `verify`/`logic` self-resolve bullets edited the plan and re-fired with no `_status.append_phase`/commit, violating Shared Decision `audit-trail-via-status-timeline`. Added `_status.append_phase(status_path, "self-resolved-verify-logic", ...)` + `git add <plan_dir> <status_path> && git commit` before the re-fire/re-invoke in both cards.
- [BLOCKING] "Scope-violations self-resolve requires reading card bodies, contradicting Lean Builder" — confirmed Batch 4 Card 10's classification step told the Builder to also check "batch cards' Edits:/Creates: fields," which requires opening card bodies mill-go's own "Lean Builder" principle forbids. Dropped that clause; classification now checks solely against the overview's `All Files Touched` list.
- [NIT] "Card 5's rationale cites the wrong step number" — confirmed Card 7 (batch 3) edits step 7 (Rounds exhausted), not step 5. Fixed the citation in Card 5's Requirements prose.
- [NIT] "New self-resolve commits push without updating mill-go's push-policy note" — confirmed `## Board discipline` states Builder-owned state commits (blocked/done/approve/prepare) do not push, and no other Handoff-phase commit in the existing file pushes either. Dropped "and push" from Batch 4 Cards 9 and 10's self-resolve commits to match the documented policy and existing pattern, rather than adding a new card to expand the policy note (smaller, lower-risk fix; the existing prose already covers "Builder's own state commits... do not push" without naming every commit message, so no textual update is needed there).

## Pushed Back

- [NIT] "mill-autofix phase numbering gains gaps after deletions" — the reviewer's own suggested fix explicitly frames this as "Optional cosmetic renumbering." All genuinely broken cross-references to the deleted Phase 4 (the killswitch sentence, the Phase 3 intro sentence, the Principles bullet) were already found and fixed in round 4 — the remaining gap (Phase 0, 1, 1c, 3, 5) is a readability nit, not a correctness defect. Renumbering every remaining phase heading would require re-verifying no other cross-reference in `mill-autofix/SKILL.md` (or elsewhere) points to "Phase 3"/"Phase 5" by number, expanding this batch's edit surface and risk for a cosmetic-only improvement the reviewer itself did not require. Leaving as-is.
