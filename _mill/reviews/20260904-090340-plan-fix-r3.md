# Plan fix — round 3

Source: `_mill/reviews/20260904-090256-plan-review-r3.md`

## Fixed

- [BLOCKING:scope] "Card 9 relies on `_status.py` API not listed in its Context" — confirmed
  `_status.set_batch_field`/`_status.read_batches` were named in Card 9's Requirements without
  `_status.py` in its Context. Added `plugins/mill/scripts/_status.py` to Card 9's Context, and
  replaced the vague "write the minimal status.md text directly" fallback with the concrete,
  already-established `_status.render_initial(...)` + `_status.init_batches(status_path, [...])`
  pattern (verified used identically in `test-millpy-fix.py`), including the exact
  `render_initial` call signature to use.
- [NIT:consistency] "Card 7's 'Add an else:' prose doesn't match the shown elif/else code" —
  confirmed the prose only mentioned an `else:` clause while the code block showed an
  `elif start_sha is None: pass` / `else:` chain. Reworded to name both branches explicitly.

## Pushed Back

(none)
