# Discussion-fix round 1

Source review: `_mill/reviews/20260904-095454-discussion-review-r1.md`

## Fixed

- Source: `20260904-095454-discussion-review-r1.md` — `[NIT:consistency] Named "existing tests assert commit_sha" claim is false`. Verified by reading `test_15_stage_finalize_conflicts`, `test_19_finalize_conflicts_accepts_parity_flags`, and `test_2x_stage_finalize_conflicts_reaches_gate` in `test-millpy-merge-in-subagent.py` directly: every `commit_sha` occurrence is inside an input fixture string; none assert it on output. Corrected Scope, Decisions > rename-conflicts-finalize-field, Technical context, and Testing to state no existing test needs editing — only the new regression test is required.
- Source: `20260904-095454-discussion-review-r1.md` — `[NIT:scope] pre_merge_head doc-update target is unverified`. Verified by grepping `plugins/mill/skills/` for `commit_sha`: no file documents consuming the conflicts-finalize envelope's SHA field. Reworded the Technical context bullet to state plainly there is no confirmed doc-update target, so the plan should not budget a doc-update line item on spec.
- Source: `20260904-095454-discussion-review-r1.md` — `[NIT:consistency] #978 rationale claims an "instruction to restate" that doesn't exist`. Verified by reading `implementer-brief.md`'s `## Report` section in full: it contains no existing instruction to restate `commit_sha` in prose. Reworded the Decisions > no-prose-commit-sha rationale from "removing the instruction to restate it" to "adding a new prohibition" against spontaneous prose restatement.

## Pushed Back

(none — all three findings verified accurate, all three fixed)
