# Plan fix report — round 1

Source review: `_mill/reviews/20260716-111917-plan-review-r1.md`

## Fixed

- `20260716-111917-plan-review-r1.md`: "[BLOCKING] Card 10 'prove the fix' asserts checkout exits 0 but it errors" — Batch 4 Card 10 now seeds `hub`'s `main` branch with a trivial, committed `_mill/status.md` before the squash so the restore commands (both the repro-the-bug old-form and prove-the-fix new-form) have real content to act on; the repro sub-step now asserts both `reset` and `checkout` fail with "outside repository" (not just `reset`); a new closing assertion confirms the seeded `_mill/status.md` survives the squash byte-identical, extending coverage the way the reviewer's alternative-fix suggestion 1 recommended.
- `20260716-111917-plan-review-r1.md`: "[NIT] Card 7 read_slug cannot honor 'absent slug = no-op' semantics" — Batch 3 Card 7 now reads the raw `slug:` field via `_status.read_full(status_path)["yaml"].get("slug")` instead of `_status.read_slug()`, so an absent field is a genuine no-op (matching Card 1's `expected_slug` semantics in `_parent_branch.py`) rather than always resolving to the literal `_mill` fallback. Batch 4 Card 11's mirrored test assertion was updated to match the same raw-field read for consistency.

## Pushed Back

_None._
