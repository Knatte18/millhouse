# Plan fix — round 2

```yaml
slug: mill-go2-scaffold
round: 2
review: 20260811-121949-plan-review-r2.md
```

## Fixed

- `[NIT:decision]` (demoted from BLOCKING) "Line-321 self-reference in the relocated file is never repointed" — added a fourth surgical edit to card 1 in `01-extract-base.md` repointing the `## Agent-mode dispatch` step-6 `--actual-model` paragraph's `mill-go/SKILL.md` citation to `mill-go-base/SKILL.md`, located by grep rather than by line number with a one-match assertion. Amended card 4's exclusion list so the parameterization card does not revisit it.
- `[NIT:consistency]` "\"Step 0a\" is placed before \"Step 0\"" — the inserted block is now unnumbered (`**Variant binding and driver preamble.**`) instead of `Step 0a`, with an explicit instruction to open it with a sentence stating it runs before Step 0 and why. Card 2's title changed to "ahead of Entry step 0" to match.

## Pushed Back

_None._
