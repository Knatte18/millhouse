# Plan Fix — Round 1

```yaml
slug: codeguide-scope-resolution-fixes
round: 1
source_review: _mill/reviews/20260709-132310-plan-review-r1.md
```

## Fixed

- Source: `20260709-132310-plan-review-r1.md` — "[BLOCKING] Card 7 uses `cfg` with no load step / `_config.py` absent": added `_config.py` to Card 7's Context, and added an explicit `cfg = _config.load_config(hub_root, git_root)` step (with the correct signature `load_config(hub_root, worktree_root)`) before the `status_path` resolution line.
- Source: `20260709-132310-plan-review-r1.md` — "[NIT] Card 3 Scenario 14 assertion contradicts `<base>..HEAD` semantics": reworded Scenario 14 so the checkpoint branch marks the base and HEAD is its descendant, and the assertion now correctly expects the files committed *after* the checkpoint, not on it.
- Source: `20260709-132310-plan-review-r1.md` — "[NIT] `enumerate_scope` docstring signature not updated for `parent`": added a requirement to Card 2 updating the module docstring's `Function: enumerate_scope(...)` line to include the new `parent` kwarg.

## Pushed Back

(none — all three findings verified accurate and fixed)
