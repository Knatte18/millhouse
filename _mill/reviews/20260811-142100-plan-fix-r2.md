# Plan fix report — round 2

```yaml
task: 'mill-go2: fork-based implementer dispatch'
slug: mill-go2-fork-implementer
round: 2
review: 20260811-141347-plan-review-r2.md
```

## Fixed

- r2 `[BLOCKING:design]` "Card 4's override text contradicts its own inference disclaimer" — verified: the "Dispatch cold" bullet asserted the fork's `agentId` return shape as settled while "Known limits" three paragraphs later marks the same claim an unspiked inference. Took the reviewer's second option and dropped the sub-clause: the bullet now says step 6.5.1's warm resume "needs no assignment either way — it re-addresses an already-live handle rather than dispatching afresh," which is true regardless of what shape that handle turns out to have. "Known limits" remains the file's single statement about the inference.
- r2 `[NIT:consistency]` "\"cited specifically\" overclaims mill-plan's paraphrase" — verified against `plugins/mill/skills/mill-plan/SKILL.md:119`, which reads "a fork always inherits the parent's **full tool access**" and never contains the literal `parent's tools`. Cards 3 and 5 now say mill-plan paraphrases the claim and points back at the base paragraph, rather than citing it specifically.
- Validator follow-on (no review finding): rewording the card-3 rationale removed the citation-marker phrasing that had exempted its `Requirements:` line from `_plan_validate.py`'s `context-completeness` check, which then fired on `mill-start/SKILL.md` and `mill-plan/SKILL.md`. Both were added to card 3's `Context:` per the check's own fix rule — they are genuine reads for confirming the two inbound citations resolve.

## Pushed Back

_None._
