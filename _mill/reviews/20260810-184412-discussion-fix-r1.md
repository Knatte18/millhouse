# Discussion Fix — Round 1

## Fixed

- `_mill/reviews/20260810-184343-discussion-review-r1.md` — "[NIT:consistency] Technical Context misattributes require_status_path call site": corrected the "Sibling function for context" bullet in Technical context to attribute the `_paths.require_status_path` call at `millpy-implement.py:561` to `main()`, not `_run_baseline_stage` (which only receives an already-resolved `status_path` parameter and never calls `require_status_path` itself).

## Pushed Back

(none)
