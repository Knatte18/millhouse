# Plan fix report — round 1

```yaml
round: 1
source_review: _mill/reviews/20260709-131918-plan-review-r1.md
verdict_addressed: APPROVE (2 NIT)
```

## Fixed

- From `20260709-131918-plan-review-r1.md` — **"[NIT] Card 4 test must use a temp dir for project_root"**: updated Card 4 Requirements to pass `project_root`/`git_root` as paths inside a `tempfile.TemporaryDirectory` (matching `test-worktree.py`), because `compute_baseline` unconditionally runs `scratch_dir.mkdir(...)` on `project_root/.scratch` (`_verify_baseline.py:148-149`); removed the inaccurate "no real filesystem work" phrasing.
- From `20260709-131918-plan-review-r1.md` — **"[NIT] Card 1 Context omits `_paths.py`"**: added `plugins/mill/scripts/_paths.py` to Card 1 `Context:` so the `_paths.resolve_git_root` / `_paths.resolve_wiki_path` identifiers named in Requirements have a listed source.

## Pushed Back

- (none)
