# Plan fix report — round 1

```yaml
slug: review-plan-and-ref-paths
round: 1
source_review: _mill/reviews/20260615-113532-plan-review-r1.md
verdict: REQUEST_CHANGES (1 BLOCKING, 2 NIT)
```

## Fixed

- `20260615-113532-plan-review-r1.md` — "[BLOCKING] Card 8 targets wrong entry point for prepare gate": rewrote Card 8 requirements to invoke the CLI `main(["--stage","prepare","--holistic-only"])` entry point (where the prepare gate lives), not the `plan_run` backend; added the harness note that `main()` calls `resolve_git_root`/`resolve_hub_path`/`resolve_wiki_path`, so the fixture must `git init` or monkeypatch those resolvers. Added `plugins/mill/scripts/_paths.py` to Card 8 Context.
- `20260615-113532-plan-review-r1.md` — "[NIT] Card 9 mis-cites SKILL.md:133 scope of the 'BOTH modes' note": reworded Card 9 to place the prepare-envelope-handling INSIDE the agent-mode dispatch path (step 2) and to clarify that line 133's "BOTH modes" claim is made accurate by this task's CLI `--stage prepare` gate, without conflating Step 1.5's standalone Python gate with the CLI-internal gate.
- `20260615-113532-plan-review-r1.md` — "[NIT] Card 6 import of _load_root_from_overview not reflected in Context completeness": amended Card 6 requirements to append `_load_root_from_overview` to the existing `from _review_common import (...)` block at `millpy-review-plan.py:98` (no second import line).

## Pushed Back

(none)
