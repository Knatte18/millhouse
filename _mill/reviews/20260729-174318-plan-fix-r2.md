## Fixed

- `20260729-174002-plan-review-r2.md` — [NIT] "`_check_context_completeness` resolvability omits `moves_targets`": threaded `deletes_union` and `moves_targets` into `_check_context_completeness`'s resolvability OR-chain (mirroring `_check_non_existent_path`'s existing four-way OR-chain), added the two new parameters to the function signature, and updated the `run()` wiring call to pass the already-computed `deletes_union`/`moves_targets` locals. This also fixes an internal inconsistency: Batch 01 Card 2's `test_check_context_completeness_dirty_moves_target_only` test already expected a Move-target reference to be flagged, which required this exact fix to actually hold.
- `20260729-174002-plan-review-r2.md` — [NIT] "No specified fallback when a card lacks a `Requirements:` header": added an explicit "skip this card (zero findings)" instruction to Batch 01 Card 1 when no `Requirements:` header line is found, mirroring `_card_field_is_none`'s "no header found -> none" precedent.
- `20260729-174002-plan-review-r2.md` — [NIT] "Anti-pause text cites an unloaded skill's rule": dropped the `mill:conversation` numbered-options-rule parenthetical from both Batch 02 Card 4 and Card 5's anti-pause sentences (neither `mill-plan/SKILL.md` nor `mill-go/SKILL.md` loads that skill), replacing it with "any equivalent free-text operator prompt" and adding a one-line note explaining why the citation was dropped (the two named escape hatches already spell out their own numbered-option format inline).

## Pushed Back

(none)
