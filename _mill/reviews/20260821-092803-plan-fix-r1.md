# Plan Fix — round 1

Source review: `_mill/reviews/20260821-092725-plan-review-r1.md` (verdict: REQUEST_CHANGES, 1 BLOCKING, 1 NIT).

## Fixed

- `[BLOCKING:design]` Card 4's hub-config insertion anchor doesn't match the real file's key order — corrected `02-baseline-build-once-step.md` Card 4's hub-config edit instruction to anchor only on `done_gate_baseline_preflight: false` (dropping the false "before `max_cards_per_batch`" claim), and noted this hub's own `pipeline:` block orders keys differently from the template.
- `[NIT:consistency]` Card 6's `check_bg_status` parse instruction drops the tuple-shape clarification the pattern it mirrors states explicitly — added `as (status, pid_or_code)` to both new parse instructions in `03-baseline-dispatch-background-skill.md` (Card 6a and 6d), matching `mill-start/SKILL.md`'s exact wording.

## Pushed Back

(none)
