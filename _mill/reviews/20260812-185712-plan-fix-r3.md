# Plan fix — round 3

## Fixed

- `20260812-185638-plan-review-r3.md` — "[BLOCKING:design] Batch 3 Card 7 depends on batch 2 Card 6's not-yet-existing text": changed batch 3's `depends-on` from `[1]` to `[1, 2]` in both `00-overview.md`'s Batch Index and `03-mill-merge-in-and-conflict-brief.md`'s own frontmatter, and updated that batch's Batch Scope prose to state the dependency and why.
- `20260812-185638-plan-review-r3.md` — "[BLOCKING:scope] Card 2 names `_paths.resolve_task_path`; `_paths.py` missing from Context": added `plugins/mill/scripts/_paths.py` to Batch 1 Card 2's `Context:` list.
- `20260812-185638-plan-review-r3.md` — "[NIT:consistency] Card 6's rebind snippet re-derives status_path outside the file's own convention": rewrote Batch 2 Card 6's rebind bash snippet to derive `status_path` via the same `worktree_root = _paths.resolve_active_hub(...)` then `status_path = _paths.resolve_task_path(worktree_root, cfg['paths']['status_md'])` pattern the rest of Entry Step 4 uses, instead of a divergent `_paths.resolve_hub_path()` + literal `'_mill/status.md'` derivation.

## Pushed Back

(none)
