# Review: 55 (A) — Fix hardcoded _mill/ paths and mill-setup junction/config bugs — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmedium
reviewed_file: plan/ + source
date: 2026-05-15
```

## Findings

### [NIT] `worktree_root` implicit in mill-start and mill-plan Path Setup
**Location:** `plugins/mill/skills/mill-start/SKILL.md` (Path Setup), `plugins/mill/skills/mill-plan/SKILL.md` (Path Setup)
**Issue:** Both Path Setup blocks reference `worktree_root` without assigning it. Entry step 1 computes `_paths.resolve_git_root()` inline but doesn't bind it. Agents following the skill literally would see an implicit dependency; `git_root` usage in step 2 (`_marker.slug_from_branch(git_root, ...)`) shows this is a pre-existing documentation style, but the Path Setup should make the binding explicit.
**Fix:** Add `worktree_root = _paths.resolve_git_root()` as the first line of each Path Setup block (parallel to the explicit form in mill-go step 4.5 and mill-finalize step 3.5).

### [NIT] Residual `_mill/status.md` prose in mill-merge
**Location:** `plugins/mill/skills/mill-merge/SKILL.md:` Entry step 6 header, Board Discipline
**Issue:** Entry step 6 retains "**Try `_mill/status.md` first.**" as a bold header and the Board Discipline section closes with "hand-editing `_mill/status.md` is banned" — both hardcoded while the rest of the file now uses `status_path`.
**Fix:** Update to "Try `status_path` first" and "hand-editing `status_path` is banned" for consistency.

### [NIT] mill-plan step 4c commit unnamed
**Location:** `plugins/mill/skills/mill-plan/SKILL.md` (Phase: Plan Review step 4c)
**Issue:** 4c describes "commit+push (single commit covering plan + reviews + status)" without naming the variables, unlike the parallel 4b and 4d which use `<plan_dir>`, `<reviews_dir>`, `<status_path>`.
**Fix:** Replace with "single commit covering `<plan_dir>` + `<reviews_dir>` + `<status_path>`" for consistency with the other apply-and-commit paths.

## Verdict

APPROVE — all batch requirements are met; three prose-level NITs only.