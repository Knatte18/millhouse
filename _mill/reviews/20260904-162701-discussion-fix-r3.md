# Discussion fix — round 3

Source review: `_mill/reviews/20260904-162552-discussion-review-r3.md`

## Fixed

- r3 BLOCKING:design "`build_reattached_section` call site missing from Scope/Technical context" — verified accurate (`_review_common.py:1357` calls `bulk_files(file_paths)` at 1368 with no roots; live callers `_review_plan.py:298`, `_review_plan.py:1120`, `_review_code.py:770`). Added as an in-scope sixth display site: new Scope bullet, a Technical context entry explaining why it is the easiest site to miss (it builds a separate `retry_prompt`, not an `artefact_section`, and is delivered as a resume turn in the same reviewer session), a note in `_review_plan.py`'s and `_review_code.py`'s Technical context blocks recording that all roots are already in scope at each of the three branches, corrected "five sites" -> "six display sites" in the `display-only-layer` decision and its Q&A entry, new Testing item 8b covering both the helper unit test and a flow-level `retry_prompt` assertion, and a Q&A log entry.

## Pushed Back

(none)
