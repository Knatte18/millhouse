# Plan fix — round 3

Source review: `20260921-181333-plan-review-r3.md`

## Fixed

- `[BLOCKING:design] Card 2's override never fires for merge-in despite claiming it does` — corrected
  Card 2's Requirements (batch 1) to state the guard is fixer-only, confirmed by reading both
  `millpy-merge-in-subagent.py` callers: `verify-fix` mode never calls `finalize_from_output`/
  `_forward_output` (it hand-rolls its own JSON), and `conflicts` mode's `finalize_from_output` call
  hardcodes `start_sha=None`, so `start_sha is not None` can never hold for either merge-in mode.
  Only `millpy-fix.py` (the actual fixer, which threads `start_sha=args.start_sha` and never passes
  `card_ids`) satisfies the new guard — matching #1104's own incident, a holistic fix-round crash in
  `millpy-fix.py`. Did not extend real merge-in coverage (the reviewer's alternative option) since
  that was never part of #1104's scope.

## Pushed Back

(none — the finding was accurate and fixed as described above)
