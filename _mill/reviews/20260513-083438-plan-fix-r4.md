# Plan-fix report -- round 4

```yaml
slug: safe-rmtree
round: 4
source_review: _mill/reviews/20260513-083346-plan-review-r4.md
```

## Fixed

- `_mill/reviews/20260513-083346-plan-review-r4.md` [BLOCKING] "ignore_errors wrapping absent in Card 1; Card 2 test requires it" -- expanded Batch 1 Card 1 step (9) to wrap `shutil.rmtree` in `try: ... except OSError: if not ignore_errors: raise`. The forwarded `ignore_errors=ignore_errors` lets real `shutil.rmtree` swallow internally; the outer try/except adds defense-in-depth and satisfies the Card 2 test scenario `ignore_errors=True swallows OSError from rmtree` (whose mock raises unconditionally). When `ignore_errors=False` the exception is re-raised verbatim, preserving the caller-visible semantic that `_worktree.remove_safe`'s `except PermissionError` catch depends on.
- `_mill/reviews/20260513-083346-plan-review-r4.md` [NIT] "Card 6 CRLF guidance self-contradicts" -- removed the `splitlines()` reference from Card 6's edge-case section; replaced with an explicit note that Python text-mode performs universal-newlines conversion before the `"\n"` split.

## Pushed Back

(none)
