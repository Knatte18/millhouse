## Fixed

- `_mill/reviews/20260725-112751-plan-review-r2.md` — "[BLOCKING] Card 3 introduces a duplicate `discussed` status append + redundant commit": Card 3's Requirements now explicitly instruct that 4b's "Break loop → Handoff" must route around `### Phase: Handoff`'s own `_status.append_phase`/commit steps (already redundant once 4b appends `discussed` itself) and go straight to Handoff's final report line. `### Phase: Handoff` itself stays unconditional and unchanged for the 4a (no-NOTEs) path, which still needs it. Batch Tests' Card 3 verification bullet updated to match.
- `_mill/reviews/20260725-112751-plan-review-r2.md` — "[NIT] Card 4's Batch Tests verification step needs a file not in Card 4's Context": added `plugins/python/skills/python-build/SKILL.md` to Card 4's `Context:`.

## Pushed Back

(none)
