# Plan-fix report -- round 3

```yaml
slug: safe-rmtree
round: 3
source_review: _mill/reviews/20260513-082055-plan-review-r3.md
```

## Fixed

- `_mill/reviews/20260513-082055-plan-review-r3.md` [BLOCKING] "Blacklist ancestor check has inverted direction" -- corrected the blacklist comparison formula in Batch 1 Card 1's `safe_rmtree` requirements from `resolved_path == entry or entry in resolved_path.parents` to `resolved_path == entry or resolved_path in entry.parents`. Added an explanatory paragraph documenting the direction (`Path.parents` returns the ancestors of the receiver; `X in Y.parents` is True iff `X` is an ancestor of `Y`). Applied the same fix to `_mill/discussion.md`'s "Path resolution for blacklist check" paragraph for consistency.

## Pushed Back

(none)
