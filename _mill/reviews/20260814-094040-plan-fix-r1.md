# Plan-fix round 1

Source review: `20260814-093926-plan-review-r1.md` (APPROVE, 0 BLOCKING, 4 NIT).

## Fixed

- [NIT:consistency] "Card 7 script invocation contradicts CLAUDE.md's explicit script-invocation rule" — switched card 7's invocation back to the canonical cache form (`PYTHONPATH="${CLAUDE_PLUGIN_ROOT}/scripts" "$MILL_PYTHON" "${CLAUDE_PLUGIN_ROOT}/scripts/millpy-skills-index.py"`) and dropped the misapplied source-verification-rule justification — CLAUDE.md's `## Script invocation` section states `${CLAUDE_PLUGIN_ROOT}` remains correct for script invocation, narrowing only the source-code-*reading* case, not script execution; the script resolves its target repo via cwd regardless of which copy runs.
- [NIT:consistency] "Batch 1 scope understates its own decision inventory" — added `python-how-it-works-conflict` (card 3) and `csharp-file-header-syntax` (card 4) to batch 1's scope prose as batch-local decisions, alongside the three cross-cutting overview decisions.
- [NIT:consistency] "Card 4's requirements mislabel a C#-only trim as 'Go/C#-specific'" — reworded to "C#-specific" (card 4 only touches `csharp-comments/SKILL.md`).
- [NIT:consistency] "Card 5's verification grep pattern never matches the actual pre-edit text" — corrected the grep string from "must begin with a comment describing" to "must begin with a comment that describes", matching `golang-comments/SKILL.md`'s actual pre-edit wording.

## Pushed Back

(none)
