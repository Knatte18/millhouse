MILL_REVIEW_BEGIN
# Review: Fix millpy-bg EXIT marker missing on wrapper crash -- holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-06-06
```

## Findings

### [NIT] Race-guard test docstring will be stale after Card 2
**Location:** Batch 1 / Card 4
**Issue:** `test_check_bg_status_race_guard_exit_appears` exercises a race between two `read_text` calls; Card 2 eliminates that second read entirely (EXIT is checked inside the single `_probe_liveness` call), so the scenario the test documents no longer exists in the new flow.
**Fix:** Card 4's migration note should also direct the implementer to update the test docstring from "re-read shows EXIT" to "probe returns exit" so it reflects what the migrated mock actually exercises.

### [NIT] Card 5 omits `_cleanliness` from mock guidance
**Location:** Batch 1 / Card 5
**Issue:** `_forward_output` calls `_cleanliness.compute_scope_violations` and `_cleanliness.compute_new_dirt`; Card 5 names `_subprocess_util` as the only mock target, leaving `_cleanliness` implicit.
**Fix:** Add `_cleanliness` to the list of modules to patch so the implementer does not need to discover it independently from `_implementer_common.py`.

## Verdict

APPROVE
Plan is correct and internally consistent; two nits only, no blocking issues.
MILL_REVIEW_END
