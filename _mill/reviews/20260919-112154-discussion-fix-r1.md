## Fixed

- `20260919-112106-discussion-review-r1.md`: "Testing section's \"or true\" verify: option contradicts actual enforcement" — replaced the bare-`true` verify: suggestion with a direct statement that the doc-only batch either omits `verify:` (command=None) or uses `PYTHONPATH= true`, citing `_plan_validate.py`'s `_check_verify_not_isolated` and confirming `plugins/mill/pyproject.toml` triggers the Python-project gate.
- `20260919-112106-discussion-review-r1.md`: "Testing section's grep-count claim is arithmetically self-contradictory" — corrected "+1 occurrence" to "+3 occurrences" and scoped the count to the four kwarg-enumeration sites only, excluding unrelated pre-existing `done_gate` mentions elsewhere in the file.
- `20260919-112106-discussion-review-r1.md`: "Decision's \"verbatim\" claim about line 251 doesn't match line 251's actual text" — dropped the "verbatim" claim; clarified the fix borrows only the kwarg set and `done_gate`'s value expression from line 251/282, while preserving 4b/4c/4d's own existing `skip_checks=plan_skip_checks` naming (their pre-existing, correct reuse of Plan Review's persisted-frontmatter variable).

## Pushed Back

(none — all three findings verified accurate on decision-tree VERIFY, no HARM CHECK category applied)
