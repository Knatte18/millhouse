# Plan fix: round 3

## Fixed

- [source: `20260918-184302-plan-review-r3.md`] "New test-status.py tests are added as dead code, never executed" — verified accurate: `test-status.py` has exactly one top-level `def main() -> int:` spanning nearly the whole file, with every existing test as an inline `assert`/`print("PASS: ...")` block sequenced inside it under `# --- <thing> tests ---` markers; there is no function-per-test / auto-discovery convention (unlike `test-millpy-implement.py`, which genuinely uses `unittest.TestCase`). Cards 1 and 2 (batch 1) and Card 3 (batch 2) previously instructed adding standalone `def test_xxx():` functions that `main()`'s linear body would never call, making batch 1's and batch 2's `verify:` gates pass without ever exercising the new code. Reworded all three cards to instead add inline `assert`/`print("PASS: ...")` blocks directly inside `main()`'s existing sequence, at explicit anchor points (immediately after the existing `# --- phase_entry_timestamp tests ---` section for Card 1's new blocks; a new `# --- resume_batch tests ---` section inserted after `# --- set_batch_fields tests ---` and before `# --- read() tests ---` for Card 2's four blocks; immediately after the existing `# --- module_verify_baseline tests ---` section for Card 3's two blocks) — each ending with the file's existing `print("PASS: ...")` convention so `main()` genuinely reaches and executes every new assertion.

## Pushed Back

(none)
