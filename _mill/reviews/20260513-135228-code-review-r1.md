# Review: (A) — Subprocess handling: timeout + JSON-exit + Windows detach — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmedium
reviewed_file: plan/ + source
date: 2026-05-13
```

## Findings

### [NIT] Integration test uses info-class 9 where plan specifies info-class 2
**Location:** `plugins/mill/integration_tests/test-millpy-bg-detached.py:~90`
**Issue:** Plan card 8 specifies `SetInformationJobObject(job, 2, ...)` (`JobObjectBasicLimitInformation`), but the implementation uses info-class `9` (`JobObjectExtendedLimitInformation`) with `JOBOBJECT_EXTENDED_LIMIT_INFORMATION`. The implementation is technically correct — `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` (0x2000) is an extended flag and would be silently ignored or rejected via info-class 2 on modern Windows — but it deviates from the plan's stated spec.
**Fix:** No code change needed; the comment in the test (`# JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE (0x2000) is an extended flag`) adequately justifies the deviation. Plan could be updated to reflect the correct API, but this is post-merge housekeeping.

## Verdict

APPROVE — All three batches are correctly implemented; all shared decisions are applied; no blocking issues found.