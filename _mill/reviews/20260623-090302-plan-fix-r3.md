# Plan fixer report — round 3

```yaml
slug: mill-review-and-verify-quality
round: 3
source_review: _mill/reviews/20260623-090221-plan-review-r3.md
verdict_addressed: REQUEST_CHANGES (1 BLOCKING, 2 NIT)
```

## Fixed

- `20260623-090221-plan-review-r3.md` — "[BLOCKING] Nit-gate marker/approve ordering is inverted": Card 5 now requires only that a `nits-fixed-<scope>` row exists ANYWHERE in the timeline; dropped the "at or after the approve row" predicate (the marker is written by the NIT-fix dispatch BEFORE the approve row, so the positional check would false-flag every fixed scope). Added an explicit note of the write order.
- `20260623-090221-plan-review-r3.md` — "[NIT] Card 5 depends on marker strings owned by mill-go without naming the contract": Card 5 now states the three phase tokens (`approved-<batch>`, `holistic-approved`, `nits-fixed-<scope>`) are the cross-card contract with card 4 and card 6 and must stay identical across cards 4/5/6.
- `20260623-090221-plan-review-r3.md` — "[NIT] 'Do NOT read reviews/' text lives in tool-rule helper, not templates": Card 13 now clarifies it ONLY adds the `<PRIOR_NONBLOCKING>` token and must not displace `<TOOL_RULE>`; the read-ban string is rendered by `_review_common.build_tool_rule`, not literal in the template.

## Pushed Back

(none)
