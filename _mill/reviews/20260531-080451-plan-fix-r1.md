# Plan Fix Report — Round 1

## Fixed

- `20260531-080335-plan-review-r1.md` "[BLOCKING] Existing test-wiki-store.py test 9 not updated" — Updated Card 10 in batch 02 to explicitly instruct updating existing test 9's key-set assertion from the old set (with `group`) to the new set (with `depends_on`, `isolated`, `deferred`). Removed the separate `(b)` label since test 9 is the in-place replacement.
- `20260531-080335-plan-review-r1.md` "[NIT] Unused import of extended_title in Cards 24 and 25" — Removed the `extended_title` import instruction from Card 24 (millpy-status.py) and Card 25 (millpy-inspect.py). Both cards use inline `f"{title} [{layer}]"` logic which is correct; the import was never called.

## Pushed Back

(none)
