MILL_REVIEW_BEGIN
# Review: plan validator and wiki-guard hook false positives — holistic

```yaml
duration_s: 58.6
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/
date: 2026-09-26
```

## Findings

### [BLOCKING:scope] Card 8 Context omits files its Requirements depend on
**Location:** Batch 3 / Card 8 **Issue:** `Context: none`, yet Requirements say to model on `test-plan-validate-indent-drift-line.py`, call "the validator's top-level run function" (never named; it is `run` in `_plan_validate.py`), and rely on which `.cs` lines `_resolve_symbol_files` treats as declarations for the "member-shaped line" scenario. **Fix:** List `plugins/mill/unit_tests/test-plan-validate-indent-drift-line.py` and `plugins/mill/scripts/_plan_validate.py` in Context, and name `_plan_validate.run` explicitly.

### [NIT:scope] Card 3 Context omits its style model
**Location:** Batch 1 / Card 3 **Issue:** Requirements say to follow `test-plan-validate-indent-drift-line.py` style, which is not in Context. **Fix:** Add it to Context.

### [NIT:design] Card 1 step 5 leaves non-`-c` shell invocations undefined
**Location:** Batch 1 / Card 1 (step 5) **Issue:** Only `bash|sh|zsh -c` and `eval` are specified. It is unstated whether other args (`bash .wiki/x.sh`, or the remaining args after the `-c` string) fall through to the step 8 argument test. **Fix:** State that step 8 applies to all args of those commands other than the recursed `-c` string.

### [NIT:consistency] Card 8 "falls through" scenario is non-assertive
**Location:** Batch 3 / Card 8, two-same-class scenario **Issue:** The expected result is "assert no crash ... or no error when still ambiguous", which pins no behavior. **Fix:** Pick one concrete fixture and assert its exact outcome.

## Verdict

REQUEST_CHANGES
Card 8's Context omits the files its Requirements rely on, so the implementer would have to explore cold.
MILL_REVIEW_END
