# Discussion fix — round 2

Source review: `_mill/reviews/20260812-082919-discussion-review-r2.md`

## Fixed

- r2 `[NIT:consistency]` "Per-batch cleanup block's start line disagrees across two Decisions" — aligned `remove-subprocess-poll-loop-maxwait`'s parenthetical from 402–421 to 403–421, matching `remove-psmux-cleanup-block` and the actual `**Per-batch session cleanup.**` heading position.
- r2 `[NIT:consistency]` "Sibling-SKILL subprocess/psmux reference counts (7, 6, 2) don't match a raw grep" — recounted with an explicit command (`grep -cE 'psmux|subprocess'` -> 10, 9, 2), pinned the counts to commit `356da5e5`, and stated that nothing in the plan depends on them.

## Pushed Back

(none)
