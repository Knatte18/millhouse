# Review: (A) — Benchmark Gemini single-reviewers vs sonnetmax baseline — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-14
```

## Findings

### [BLOCKING] Timeout verdict contradicts Shared Decision
**Location:** batch 02 / card 3, vs Shared Decision `timeout-300s`
**Issue:** `Decision: timeout-300s` states "On timeout the row records TIMEOUT in the Verdict column rather than raising," but Card 3 requirements state "On exception/timeout: verdict 'ERROR', findings 0, format_ok False." An implementer following the card produces ERROR for both timeouts and other failures; an implementer following the decision produces TIMEOUT — two distinct observable behaviors with different benchmark utility.
**Fix:** Update Card 3 requirements to match the decision: distinguish timeout explicitly (`verdict = "TIMEOUT"`) and reserve `"ERROR"` for other exceptions, or update the Decision to say ERROR and remove the TIMEOUT mention.

### [NIT] Card 1 omits `slug` arg for `write_commit_push`
**Location:** batch 01 / card 1
**Issue:** Requirements say "commit via `_wiki.write_commit_push`" but `write_commit_push` has a required keyword-only `slug` argument used for the lock file; the requirements don't state what value to pass.
**Fix:** Add one line: pass `slug="bench-gemini-single-reviewers"` (the task slug), or derive it via `_marker.slug_from_branch(git_root, wiki_path, cfg)`.

### [NIT] Inconsistent artefact variable name in card 3
**Location:** batch 02 / card 3
**Issue:** Requirements say "build `artefact_section` as …" then immediately reference it as `discussion_artefact_section` in the `render_prompt` call; plan-artefact and code-artefact sections are similarly unnamed at definition but named distinctly at use.
**Fix:** Use the intended name (`discussion_artefact_section`, `plan_artefact_section`, `code_artefact_section`) consistently at both the build step and the render call.

### [NIT] Batch 2 verify description overspecifies automated checks
**Location:** batch 02 `verify:`
**Issue:** The description lists three post-exit checks (1 row present, verdict APPROVE, findings 0) but the shell command only verifies exit code 0; those three checks are manual and cannot be automated by the one-liner.
**Fix:** Either drop the three supplementary checks from the description (keep them as prose guidance), or add a small assertion block inside the script's `--smoke` / dry-run path that exits non-zero when the row doesn't match expected output.

## Verdict

REQUEST_CHANGES
One BLOCKING contradiction between the shared decision and card requirements on timeout verdict.