# Plan Fix — round 3

## Fixed

- Review `20260820-181748-plan-review-r3.md`, finding "Card 8's `_is_reparse_point` test skips the `os.lstat` fallback branch" — split the fourth test case into two: one exercising `_is_reparse_point`'s `os.path.isjunction` branch (unchanged from round 2's fix), and a new fifth case exercising the `os.lstat` fallback branch, forcing it via `patch("_safe_rmtree.hasattr", MagicMock(return_value=False))` so the fallback runs regardless of the test runner's actual `os.path.isjunction` availability. Updated Card 8's case count (4 -> 5), its Commit message, and batch 04's "Batch Tests" section accordingly.

## Pushed Back

(none — the finding was accurate and its fix carried no harm.)
