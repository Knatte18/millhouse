# Plan fix — round 1

## Fixed

- [BLOCKING] Card 6 cites `_review_common.py` without listing it in Context — added `plugins/mill/scripts/_review_common.py` to Batch 1 Card 6's `Context:`, matching Cards 2 and 4.
- [NIT] Card 4 preserves a `verdict = parse_verdict(raw)` reassignment that becomes dead code after the refactor — corrected Batch 1 Card 4's Requirements to instruct removing that reassignment instead of keeping it.
- [NIT] Batch 2 Scope claims schema-parity test coverage no card delivers — trimmed the Scope sentence to not claim schema-parity coverage, and added an explicit note that the 6 schema-parity-only sites (Batch 1 Cards 3/5) are intentionally left to their existing (untouched) error-path tests, since those sites have no successful raw response to build a non-vacuous finding-based assertion from.
- [NIT] Card 9's fixture literal breaks its own markdown code span (nested single-backtick pair inside a single-backtick span) — rewrapped with 4-backtick delimiters. Proactively found and fixed the identical bug in 5 more fixture-literal spans across Batch 2 (Cards 7, 8, 10, 11 ×2, 12, 13), each embedding a ` ```yaml...``` ` fence inside a single-backtick span the same way Card 9 did.

## Pushed Back

(none — all 4 findings verified accurate with no harm from fixing)
