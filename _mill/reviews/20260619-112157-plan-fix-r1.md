# Plan-fix report — round 1

```yaml
round: 1
reviewed_dir: _mill/plan/
source_review: _mill/reviews/20260619-112045-plan-review-r1.md
verdict_in: REQUEST_CHANGES (2 BLOCKING, 1 NIT)
date: 2026-06-19T11:21:57Z
```

## Fixed

- r1 "[BLOCKING] Card 2 tests fail against the minimal fixture template" — updated Batch 1 / Card 2 Requirements to **register the `git:` block in the `_setup_plugin_template` fixture** (the minimal template string at test-config.py ~L56-80) as a mandatory first step, since the validator checks the merged config against that fixture, not the real template. Verified directly: `_setup_plugin_template` writes a minimal spawn/roles-only string with no `git:`.
- r1 "[BLOCKING] Negative test asserts a subkey path that cannot be emitted" — same fix resolves it: with `git:` registered in the fixture, `walk_unknown_keys` recurses (both sides have `git` as a dict) and emits `git.bogus-key`. Documented the L104-109 recursion precondition inline in Card 2.
- r1 "[NIT] parent-branch is read from a different file than the warning source" — kept `parent-branch` and **sharpened Card 1's rationale** to state all three subkeys are required: once `git` is a known dict the validator recurses into subkeys, so an unregistered `parent-branch` would itself warn — and `git.parent-branch` is precisely the key loomyard sets that triggered #511. Also corrected Batch 1's Batch Tests note (it wrongly claimed `_setup_plugin_template` copies the real template).

## Pushed Back

- r1 "[NIT]" alternative "drop the net-new `parent-branch` key if minimalism is preferred" — **rejected**. Dropping `parent-branch` would resurface a more specific warning (`unknown key: git.parent-branch`) for loomyard's config once `git` becomes a known dict, defeating the fix for the exact config that filed #511. Registration of all three distinct keys is the intentional decision (discussion Q3). The finding itself marked "None required," so no contradiction with the reviewer.
