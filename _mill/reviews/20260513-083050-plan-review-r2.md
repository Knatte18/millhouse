# Review: (A) — Subprocess handling: timeout + JSON-exit + Windows detach — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-13
```

## Findings

### [NIT] Batch 1 scope prose contradicts verify: field
**Location:** Batch 1 — Batch Scope paragraph
**Issue:** "it is local-dev only and not in `verify:`" directly contradicts the batch header's `verify:` field and the Shared Decision on integration-test chaining.
**Fix:** Delete the "not in `verify:`" clause from the Batch Scope sentence; the `verify:` field and Batch Tests section are correct and authoritative.

### [NIT] Card 3 missing explicit red-state instruction
**Location:** Batch 1, Card 3
**Issue:** The TDD Shared Decision applies to card 3, but the requirements do not contain the "write the test update first, run (red), then implement" instruction that cards 1 and 5 have — making the ordering implicit rather than explicit.
**Fix:** Add one sentence after the test-update requirements: "Run the suite now; test (k)'s new argv assertion MUST fail against unmodified `popen_detached` before proceeding to the production change."

### [NIT] Card 10 context missing `_implementer_common.py`
**Location:** Batch 3, Card 10
**Issue:** Requirements call `_forward_output(..., session_id="abc-uuid-xyz")`, which is defined in `_implementer_common.py`, but that file appears only in card 9's `Edits:` — not in card 10's `Context:` or `Edits:`.
**Fix:** Add `plugins/mill/scripts/_implementer_common.py` to card 10's `Context:`. Sequential same-session execution mitigates in practice, but the field should be self-contained.

### [NIT] WORKER ERROR sentinel from discussion scope absent in plan
**Location:** Batch 2 (all cards)
**Issue:** `discussion.md` In-scope section explicitly includes "On worker-side exception (before child spawn), write `[mill-bg] WORKER ERROR <text>` and exit non-zero." No plan card implements this, and no Shared Decision or batch-scope text explicitly excludes it.
**Fix:** Either add a card implementing the `try/except` around `open(log_path, ...)` with the ERROR sentinel, or add an explicit "WORKER ERROR sentinel: out of scope for this task" line to the Decision: worker-start-sentinel block.

## Verdict

APPROVE — no blocking issues; four minor documentation/completeness gaps that do not affect correctness.