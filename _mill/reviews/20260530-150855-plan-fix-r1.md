# Plan-fix report — round 1

Source review: `20260530-150537-plan-review-r1.md` (holistic, REQUEST_CHANGES, 2 BLOCKING + 2 NIT)

## Fixed

- [BLOCKING] "Attempt count contradicts sleep sequence" (Batch 1, Cards 2/4) —
  restated Card 2 as **4 total attempts (1 initial + 3 retries)** with 3 sleeps
  `[2, 4, 8]` between attempts (no trailing sleep before raise), worst case
  ~26s; updated Card 4 cases (a)/(b) to "4 attempts" and kept the `[2, 4, 8]`
  sleep assertion (now internally consistent). Kept the `[2,4,8]` backoff from
  discussion.md; corrected the attempt count to match it.
- [BLOCKING] "Blocking Bash one-liner will timeout for normal workloads"
  (Batch 3, Cards 8/9) — redesigned the batch-2 helper from a blocking
  `wait_for_bg_terminal` loop into a **single-shot, non-blocking**
  `check_bg_status(log_path) -> ("exit",code)|("running",pid)|("dead",pid)`
  (Card 6 rewritten, Card 7 tests rewritten). Cards 8/9 now keep the existing
  incremental `cat` poll and call `check_bg_status` once per iteration, branching
  running/exit/dead — no blocking call that could exceed the Bash 600s cap.
  Updated Batch 2 scope, Batch 3 scope, and the overview `infrastructure`
  Shared Decision to the new helper name/shape.
- [NIT] "`_EXIT_RE` has no capture group" (Batch 2, Card 6) — Card 6 now
  specifies a new module-level `_EXIT_CODE_RE = re.compile(r"\[mill-bg\] EXIT (\d+)")`
  for code extraction and leaves `_EXIT_RE` unchanged; removed the impossible
  "reuse `_EXIT_RE`'s capture" phrasing.
- [NIT] "`sys.platform` patch cannot alter a module-level constant" (Batch 1,
  Card 4 case g) — replaced with a direct `assert _client.SPAWN_TIMEOUT == 20`
  (valid on the Windows target where the suite runs), noting
  `importlib.reload(_client)` under a `sys.platform` patch as the optional
  cross-platform alternative.

## Pushed Back

(none — all findings accepted and fixed)
