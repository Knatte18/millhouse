# Plan Fix — round 1

Source review: `_mill/reviews/20260531-092858-plan-review-r1.md`

## Fixed

- [NIT] Card 2 describes template implementer_timeout as 1800; actual is 3600 — updated Card 2 Requirements to show the template's correct `llm:` block (`bulk_timeout`, `holistic_timeout`, `tool_use_timeout`, `implementer_timeout: 3600`) and hub block (`implementer_timeout: 1800`) separately with the correct insertion point for each.
- [NIT] test_12 doesn't specify patching `_implementer_claude.run` — updated Card 10 to require `patch.object(millpy_implement._implementer_claude, "run")` in test_12 to prevent real LLM invocation.
- [NIT] No test coverage for millpy-fix.py brief-size guard — added Card 11 to batch 3: creates `test-millpy-fix.py` with two test methods for the guard (fires / disabled). Updated batch 3 frontmatter (cards: 4), verify command, and overview Batch Index + All Files Touched.

## Pushed Back

(none)
