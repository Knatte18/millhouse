MILL_REVIEW_BEGIN
# Review: mill-plan: Phase Plan Review's 4b/4c/4d re-validate gate says "7 kwargs", drops done_gate — holistic

```yaml
duration_s: 174.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-09-19
```

## Findings

### [NIT:consistency] Card 1's own grep self-check claims all 4 sites name `done_gate`, but 4d never enumerates
**Location:** `_mill/plan/01-fix-plan-review-kwarg-drift.md:37` vs `plugins/mill/skills/mill-plan/SKILL.md:597`
**Issue:** The card's Requirements say "grep for `done_gate` scoped to these three sites plus Phase: Plan's own site — all four must now name it," but item 3's literal substitution for step 4d only changes "same 7 kwargs" → "same 8 kwargs" (no parenthetical list) — matching the pre-existing style at that site, which never enumerated kwarg names before or after the fix. The implementer applied item 3 exactly as specified, so 4d correctly has no `done_gate` token, contradicting the card's own verification claim.
**Fix:** None required in code — this is a plan-authoring imprecision, not a functional gap; 4d's sentence explicitly defers to 4b's own enumerated list ("the identical full-validate gate step 4b's own full-validate gate uses"), so no reader confusion results.

## Verdict

APPROVE
All three substitutions match Card 1 exactly; count and `done_gate` are now consistent with Phase: Plan and the verified 8-arg `_plan_validate.run` signature.
MILL_REVIEW_END
