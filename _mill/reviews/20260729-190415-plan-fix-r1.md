MILL_FIX_BEGIN
# Fixer report: plan-fix round 1

Source review: `_mill/reviews/20260729-190038-plan-review-r1.md`

## Fixed

- **[BLOCKING] Card 2's new Step 0b prose mis-cites the holistic-rounds-exhausted prompt's location.** Verified: `## Agent-mode dispatch` spans lines 105-515 of `mill-go/SKILL.md`; the holistic-rounds-exhausted prompt (lines 769-774) falls under `## Holistic code review` (lines 567-775), not `## Agent-mode dispatch`. Corrected Card 2's replacement text to attribute `### Stuck escalation` to `## Agent-mode dispatch` (accurate — Stuck escalation is a subsection at line 486, inside 105-515) and the holistic-rounds-exhausted prompt to `## Holistic code review`.
- **[NIT] Every quoted "find" block in Card 1 and Card 2 carries a 2-space over-indent vs. the actual source lines.** Verified: the plan's own list-nesting had added 2 extra leading spaces of markdown list-continuation indent inside every fenced "find"/"replace" block, so a literal byte-for-byte copy would not match source. Dedented every fenced block's content to match source bytes exactly (0 leading spaces for edits 1/2/3 and Card 2's edit; the source's own 3 leading spaces, unchanged, for edits 4/5/6/7). Verified post-fix via automated substring check: every "find" block's content is now a byte-exact substring of the current `mill-plan/SKILL.md` / `mill-go/SKILL.md`.
- **[NIT] Reformatted max-rounds prompt's follow-up routing uses labels, not the numeric routing discussion.md's Decision specifies.** Verified: the Decision's literal wording says "1 → / 2 → / 3 →" but the card routes by label. Added a sentence to Card 1 edit 7 explaining this is an intentional deviation: option-to-number assignment is recomputed per round (whichever option is recommended becomes `1)`), so a fixed number-to-outcome mapping would misroute; label-based routing stays correct regardless of which option is recommended in a given round.

## Pushed Back

(none — all three findings verified accurate and fixed)
MILL_FIX_END
