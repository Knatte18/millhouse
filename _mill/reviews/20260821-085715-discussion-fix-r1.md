# Discussion-fix round 1

Source review: `_mill/reviews/20260821-085554-discussion-review-r1.md` (verdict APPROVE, 3 NIT findings, all `demoted-from: BLOCKING`, class `consistency`).

## Fixed

- **"Recovery machinery works reliably" contradicts base skill's own caveat** — softened the "warm-resume-then-cold-fallback unchanged" decision's language from "already works reliably"/"resolved 100%" to "resolved every observed case ... a small sample, not a guarantee," cross-referencing `mill-go-base/SKILL.md`'s own "unverified" crash-resume caveat. Added a new "logic/verify self-resolve re-fire dispatches cold" decision closing the actual gap the finding identified: mill-go2's `stuck_type: logic`/`verify` self-resolve re-fire was still forked with no cold escape defined, unlike the transient path's existing "Cold fallback, once per batch." Updated Scope → Out and Technical context to reflect that this one sub-path of Stuck escalation dispatch shape does change, while the base skill's Stuck-escalation mechanism itself remains untouched.
- **Preload decision contradicts the "lean Builder" invariant** — added explicit rationale reconciling the tension: "lean Builder" guards against reading card bodies/diffs/full source, not a bounded one-time skill-doc preload; added a concrete bound (≤2 unconditional + 3-per-language, ≤11 total `Skill` calls for this repo's own Python/Go/C# mix) and net-cost framing (bounded one-time Builder cost vs. N-times-per-task redundant per-fork loads).
- **Preload placement option risks leaking into plain /mill-go** — removed the ambiguous "or `mill-go-base` if the override point is cleaner there — mill-plan decides placement" option from Scope → In and from the "shared-skill preload scope" Decision; pinned placement definitively to `mill-go2/SKILL.md` itself, with the rationale (mill-go-base is shared with /mill-go, which never forks and gains zero benefit) now stated explicitly at both the Scope bullet and the Decision's Rejected line.

## Pushed Back

(none — all three findings were factually accurate design gaps with no harm-check grounds for pushback; fixed per the mill-receiving-review default.)
