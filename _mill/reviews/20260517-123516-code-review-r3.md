```
# Review: 63 (A) — Reviewer tool-sandbox: git snapshot guard + fix --allowedTools

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-05-17
```

## Findings

### [BLOCKING] review-discussion.md corruption not stripped before identity-header prepend
**Location:** `plugins/mill/templates/review-discussion.md` (lines after "Omit the `## Findings` section...")
**Issue:** Card 8 requirement 3a mandates stripping any content past the legitimate closing line before prepending the identity header. The file was NOT cleaned — a second `## Source-grounding rule` block, a `## Criteria (apply to the implementation as a whole)` section (code-holistic criteria verbatim), a second `## Output format — STRICT` block, and a hardcoded `# Review: 63 (A) — Reviewer tool-sandbox…` example are all still present at the bottom of the file. The identity header was prepended on top of this corrupted state.
**Fix:** Truncate the file at `"Omit the \`## Findings\` section entirely if there are zero findings. Never invent findings to pad the review.\n"` (with a single trailing newline), then re-verify `head -5` parity across all five templates per requirement 4 of Card 8.

### [NIT] _review_common.py module-level Public API docstring omits new symbols
**Location:** `plugins/mill/scripts/_review_common.py:1-26` (module docstring)
**Issue:** `ReviewerOverstepError` and `worktree_snapshot_guard` are public API added by this task but not listed in the `Public API:` docstring block; callers consulting the module header won't find them.
**Fix:** Add two lines to the `Public API:` section: `ReviewerOverstepError — raised by worktree_snapshot_guard when a reviewer mutates HEAD or working tree` and `worktree_snapshot_guard() — context manager; snapshot guard wrapping each backend run()`.

### [NIT] _build_argv bulk assertion checks any position, not trailing pair
**Location:** `plugins/mill/unit_tests/test-llm-claude.py` (bulk _build_argv assertion block)
**Issue:** Plan specified `assert argv[-2:] == ["--disallowedTools", "Edit,Write,Bash,NotebookEdit"]` (strict end-of-argv check). Implementation uses `argv.index("--disallowedTools")` which passes even if the flag appears in the middle of the list. Functionally equivalent today but less precise as a regression guard.
**Fix:** Replace `dt_idx = argv.index("--disallowedTools"); assert argv[dt_idx + 1] == ...` with `assert argv[-2:] == ["--disallowedTools", "Edit,Write,Bash,NotebookEdit"]`.

## Verdict

REQUEST_CHANGES
One blocking: review-discussion.md corruption must be stripped before the identity-header prepend can be considered correct.