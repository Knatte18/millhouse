# Discussion Fix — Round 5

Source review: `_mill/reviews/20260527-071204-discussion-review-r5.md`

## Fixed

- "mill-autofix Step 2 instruction ambiguous" — clarified that `millpy-add.py` subprocess stays (already V3); only `_tasks_md.parse` error-path (lines 202–212) + Phase 1b Home.md read need replacing.
- "mill-fold LOCKED_FOLD_PHASES replace instruction inconsistent" — aligned mill-fold change profile to inline the set `{"active", "ready-to-merge", "pr-pending"}` directly, matching the LOCKED_FOLD_PHASES section and the mill-ghissues-to-tasks pattern.

## Pushed Back

(none)
