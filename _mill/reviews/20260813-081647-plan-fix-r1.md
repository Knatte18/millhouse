# Plan-fix round 1

## Fixed

- Source: `20260813-080912-plan-review-r1.md`, `[BLOCKING:design] Batch 3 Card 6 Cases 73/74: HEAD==start_sha trips the no-content-commit gate first`. Verified accurate against `_implementer_common.py:1697-1718` (the no-content-commit gate fires whenever `git rev-parse HEAD == start_sha`, which both original Case 73/74 fixtures triggered by never committing again after capturing `start_sha`). Empirically re-derived the fix beyond the reviewer's literal suggestion ("mirror Case 57"): confirmed via a live `git` experiment that simply adding an unrelated intervening commit is *insufficient* for Case 73's negative assertion — `git diff --name-only <start_sha>` is a plain content comparison against the current working tree, so a prior batch's file re-dirtied after `start_sha` still appears in the diff regardless of an intervening commit elsewhere. Rewrote Case 73 to use the only construction that empirically excludes a currently-dirty file from `git diff --name-only <start_sha>`: revert the file's working-tree copy back to byte-identical content with `start_sha`'s own tree after an intervening commit changed it. Rewrote Case 74 with a same-shaped intervening commit (unrelated file) to bypass the no-content-commit gate while leaving the never-committed dirt on `README.md` intact, matching the reviewer's diagnosis for that case exactly.
- Source: `20260813-080912-plan-review-r1.md`, `[NIT:consistency] Batch 4 Card 10 CTD-6/ROOD-5 omit the mandatory status_porcelain mock`. Verified accurate: `compute_terminal_dirt` and `revert_out_of_scope_drift` both call `_pygit2_util.status_porcelain` before `_parent_diff_names`, and every existing CTD-1..5/ROOD-1..3 case already mocks both. Updated Card 10's CTD-6/ROOD-5 Requirements to mock `_cleanliness._pygit2_util.status_porcelain` as well, and disambiguated ROOD-5's fixture to explicitly use ROOD-1/2/3's fully-mocked-tempdir pattern (not ROOD-4's real-git-repo pattern).

## Pushed Back

(none — both findings were accurate and fixed)
