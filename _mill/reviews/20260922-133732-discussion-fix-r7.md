# Discussion fix — round 7

```yaml
slug: baseline-uses-worktree-not-checkout
round: 7
source_review: 20260922-133654-discussion-review-r7.md
```

## Fixed

- r7 `[NIT:decision]` (demoted from BLOCKING) "compute_baseline's post-change signature unstated" — added a Technical-context bullet naming the surviving signature `compute_baseline(cwd, module_wide_verify_cmd, *, timeout_seconds) -> tuple[str, list[str]]`, with `project_root`/`git_root`/`parent_branch` all dropped as unread once the checkout is gone, and stating that the caller resolves the absolute cwd so the callee has no fallback chain and `cwd` is never `None`.
- r7 `[NIT:scope]` (demoted from BLOCKING) "merge-in's parent-branch resolution left with no disposition" — the scope bullet now states that `millpy-merge-in-subagent.py:257-261`'s parent-branch resolve and its `baseline: "error"` early return are removed, and that `:266` resolves the cwd like every other call site (override, else `git_root`) and passes it as `compute_baseline`'s `cwd`.
- r7 `[NIT:design]` "Guard and dirt-warning disagree across the two-invocation path" — recorded the interaction as accepted in `preflight-dirt-warning`: invocation 1's dirt fails invocation 2's porcelain condition, skipping the per-batch capture and gating strictly, which is the safe direction and is diagnosable from invocation 1's warning plus invocation 2's `result: "skipped"`. Attributing dirt to a process is not possible here, so the alternative was rejected rather than deferred.
- r7 `[NIT:design]` "\"Degrade to []\" claim omits a malformed overview" — extended the degrade-not-raise statement to cover `extract_batch_index`'s `PlanDAGError` on a present-but-malformed overview, which would otherwise break the stage's never-raises contract on the unattended speculative path.
- r7 `[NIT:consistency]` "List serialisation of the new scalar-block field unstated" — `module_verify_baseline_signatures:` now explicitly serialises as a flow-sequence via `yaml.safe_dump(..., default_flow_style=True)`, matching `verify_baseline_failures` (`_status.py:689-696`), not the string-only `quote_scalar` the neighbouring scalar accessors use.

## Pushed Back

_None._
