```yaml
round: 1
source_review: _mill/reviews/20260904-081040-discussion-review-r1.md
```

## Fixed

- `20260904-081040-discussion-review-r1.md` — "[NIT:consistency] New `run_gate` snippet breaks Handoff's own halt-parsing prose": edited Decision `done-gate-classifier-safe-invocation` to make the new inline snippet explicitly `sys.exit(1)` on `result["result"] == "blocked"`, and to require the downstream halt-parsing prose edit (`status: blocked` → `result: blocked`) land in the same batch as the `run_gate` switch.
- `20260904-081040-discussion-review-r1.md` — "[NIT:scope] Builder-lock-release decision doesn't say whether `_notify.notify` is also added": edited Decision `builder-lock-release-all-handoff-halts` to state explicitly that `_notify.notify` parity is in scope alongside the lock release, matching the canonical `### Blocked` shape the decision already cites as its model.

## Pushed Back

(none)
