# Plan-fix report — round 2

Source review: `20260615-130447-plan-review-r2.md` (holistic, APPROVE, 0 BLOCKING + 2 NIT)

## Fixed

- [NIT] "Card 7 mirrors a runner from a file not in Context" — verified (Card 7 Requirements reference the `__main__` runner pattern in `test-agent-dispatch.py`, which was absent from the card's `Context:`/`Edits:`). Added `plugins/mill/unit_tests/test-agent-dispatch.py` to Card 7's `Context:`.

## Pushed Back

- [NIT] "Batch-2 verify omits the other parse_batch_refs caller (`_plan_validate.py`)" — the reviewer's own Fix states "Optional — no action strictly required." The Card 4 `fields=` parameter is purely additive with a default tuple `("Context","Edits","Creates","Deletes")` that reproduces the prior behavior exactly; `_plan_validate.py`'s single-arg call is therefore provably unaffected, and `test-review-common.py` (already in batch-2 `verify:`) guards that default path. Adding the unrelated `_plan_validate` suite to a focused per-batch verify contradicts the per-batch verify-scoping convention (Shared Decision `python-verify-isolation` / mill-plan "Verify command scope") for no behavioral coverage gain.
