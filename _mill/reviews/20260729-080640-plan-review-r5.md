MILL_REVIEW_BEGIN
# Review: mill-start: tracked _mill/ files disappear from the working tree mid-review-loop; existing safeguard covers only status.md — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/
date: 2026-07-29
```

Verified against source: every `_cleanliness.py`/`_status.py` line-number citation in `00-overview.md`'s Shared Decisions and in batches 01/02 (e.g. `_cleanliness.py:62-63,324-451,365-372,374-384,406-407,429,432-435`; `_status.py:521,530,550-596,646,657-660`) matches the actual file content exactly. Every verbatim SKILL.md anchor sentence quoted for insertion (mill-start Discussion Review status-safeguard paragraph and step 2/3.5 dispatch sentences; mill-plan Path-Setup/step-2/step-4.5 sentences; mill-go per-batch step-2/4.5 and Holistic step-2/3/3.5/3.6 sentences) was located byte-for-byte in the current SKILL.md files, including the "before/after" boundary sentences used to anchor each insertion. `worktree_root`/`git_root` are confirmed already-bound locals at every call site in all three skills before the phases this plan edits. `_pygit2_util._flags_to_xy`'s deletion-code semantics (` D` / `D `) match the plan's Decision on status-code matching. Batch Index DAG is acyclic, all `file:` targets exist, `depends-on` has no forward references. All 8 cards have non-empty Creates/Edits/Context/Moves(`none`)/Requirements/Commit; no `Moves:` anywhere so no Rename-mechanic section is required. Global step numbering (cards 1–8) is sequential with no gaps. `verify:` on batches 1/2 uses the required `PYTHONPATH= uv run --project plugins/mill ...` shape; batches 3–5 justify `verify: null` for documentation-only SKILL.md prose edits, consistent with project convention. `test-treeguard.py` needs no registration — `run-all.py` glob-discovers `test-*.py` automatically. Card 2's runner-shape instruction (single top-level try/except, abort-on-first-failure, mirroring `test-status.py` rather than `test-cleanliness.py`'s accumulate-and-report style) is correctly attributed — confirmed against both files. `All Files Touched` in the overview exactly matches the union of every card's Creates/Edits.

No BLOCKING or NIT findings.

## Verdict

APPROVE
Every citation, anchor sentence, and cross-batch dependency verified against source; no gaps found in this round.
MILL_REVIEW_END
