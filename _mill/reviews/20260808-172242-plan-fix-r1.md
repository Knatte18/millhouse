# Plan fix — round 1

## Fixed

- `_mill/reviews/20260808-172152-plan-review-r1.md` — "Card 2's runner-section anchor text
  doesn't byte-match the real comment": reworded the insertion-point anchor in Card 2's
  Requirements from `before the "# --- Runner ---" section` to
  `before the "# Runner" banner comment (the two-line "# ----..." / "# Runner" block)`, matching
  the file's actual two-line banner.
- `_mill/reviews/20260808-172152-plan-review-r1.md` — "No regression test for a composed
  GOOS+custom-tag expression": added `_LINUX_AND_SCOUT_TAGGED_TEST_GO` fixture and test 10
  (`test_verify_excludes_edited_tagged_test_goos_and_custom_composed_dirty`) to Card 2, proving
  the denylist strips only the standard `linux` identifier while still discovering the custom
  `scout` tag from the same `//go:build linux && scout` expression. Bumped the "Add N new test
  functions" / "Register all N" counts from 9 to 10 accordingly.

## Pushed Back

(none)
