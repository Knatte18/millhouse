# Review: 61 (A) — Review pipeline fixes (avhenger av 60) — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnetmax
reviewed_file: _mill/plan/
date: 2026-05-17
```

## Findings

### [NIT] Card 4 claim "exactly one parse_verdict invocation" is misleading
**Location:** Batch 2 / Card 4
**Issue:** `_review_code.run` has two `parse_verdict(raw)` calls — one initial, one inside the NEED_CONTEXT retry `else` branch. The card correctly says to wrap both in one outer `try/except` but then says "there is exactly one `parse_verdict` invocation per code-review flow; one try/except suffices," which contradicts the count and may cause an implementer to wrap only the second call.
**Fix:** Replace the one-invocation claim with: "both calls live in the same execution path; a single outer `try/except ReviewError` enclosing both is sufficient."

### [NIT] Card 2 guard test: positional-arg check described incorrectly
**Location:** Batch 1 / Card 2, `_test_write_review_file_uses_utc_not_local`
**Issue:** The instruction to "patch `_review_common.datetime.now` to raise when called WITHOUT `tz=timezone.utc` (assert call kwargs include `tz`)" fails on correct code — `datetime.now(timezone.utc)` passes the arg positionally, so `call.kwargs` is empty. The primary frozen-clock test already adequately locks in UTC correctness.
**Fix:** Use `mock.assert_called_with(timezone.utc)` (which is position-aware) instead of checking `kwargs`, or drop the guard test entirely and rely on the frozen-clock test.

### [NIT] Card 10 YAML indentation example off by 2 spaces
**Location:** Batch 4 / Card 10
**Issue:** The code block in Requirements shows `fallback_reviewer` at 8-space indent, but the template uses 2-space indent per level; children of `holistic:` (which sits at 4 spaces) belong at 6 spaces. An implementer following the example literally would produce a style inconsistency (still valid YAML, but mismatched).
**Fix:** Adjust the example to 6-space indent (`      fallback_reviewer: null`) to match the template convention.

## Verdict

APPROVE — no blocking defects; three low-risk wording/style nits that a careful implementer will resolve by reading the actual source files.