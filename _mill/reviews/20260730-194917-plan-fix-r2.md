```yaml
round: 2
source_review: _mill/reviews/20260730-194812-plan-review-r2.md
date: 2026-07-30
```

## Fixed

- `20260730-194812-plan-review-r2.md` — "[NIT] Batch 1 'Batch Tests' rationale misstates `_config.py`'s key check": verified `_config.py` exports `walk_unknown_keys`/`warn_unknown_keys` (a template-vs-actual key diff, warning-only, non-load-blocking). Reworded Batch 1's Batch Tests rationale to acknowledge this check exists and explain why it still doesn't fire here (card 3 adds identical keys to both the hub config and the template, so there is no divergence for it to warn about), instead of asserting no such check exists at all.
- `20260730-194812-plan-review-r2.md` — "[NIT] Unit test's CRLF regression case hard-depends on a `bash` binary with no graceful fallback": verified no existing file in `plugins/mill/unit_tests/` shells out to a subprocess (all are pure in-memory Python). Wrapped Card 2's test case 13 `subprocess.run(["bash", ...])` call in a `try`/`except FileNotFoundError` that prints a `SKIP:` line and continues past the case on a machine with no `bash` on `PATH`, instead of letting an uncaught exception abort the whole test file before its final PASS/FAIL summary.

## Pushed Back

(none)
