# Review: 66 (A) — Review sandbox follow-up: guard exceptions + bare-exit + sandbox argv — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-05-17
```

## Findings

### [NIT] `_bg.py` docstring uses compact bullets, not tab-aligned table
**Location:** `plugins/mill/scripts/_bg.py:17-27`
**Issue:** Plan Card 14 specified "use a tab-aligned text representation" for the behaviour matrix. The implementation uses a condensed bullet list that omits the separate EPERM/ESRCH rows and conflates "kill raises ESRCH" and "kill raises EPERM" into a single line.
**Fix:** Expand the docstring to include all seven distinct rows from the plan's matrix, tab-aligned, matching the specification exactly.

### [NIT] `test-review-common-guard.py` / `test-wiki-noop-commit.py` / `test-bg-liveness.py` / `test-review-cli-error-envelope.py` use bare `unittest.main()` without `sys.exit()`
**Location:** All four new test files, `if __name__ == "__main__":` block
**Issue:** `test-llm-claude-argv.py` follows the `main() -> int` + `sys.exit(main())` pattern and the plan cited `test-bg-launcher.py` as the reference convention; the other four files use bare `unittest.main()`. Functionally equivalent but inconsistent across the test suite.
**Fix:** Add an explicit `main()` wrapper returning `0 if result.wasSuccessful() else 1` and call `sys.exit(main())`.

## Verdict

APPROVE
Implementation is correct on all batches; two documentation-level NITs only.