# Review: Smoke-test the psmux implementer end-to-end — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-06-01
```

## Findings

### [NIT] Stale docstring in `_wait_for_idle_stable`
**Location:** `plugins/mill/scripts/millpy-claude-sub.py:137`
**Issue:** Docstring Phase 2 description still reads `"for shortcuts" marker` but the implementation was changed to `"shortcuts"` per the shared decision.
**Fix:** Update docstring line to `"shortcuts" marker appearing in two consecutive polls`.

### [NIT] Test 12 may not exercise U+00A0 path
**Location:** `plugins/mill/unit_tests/test-psmux-capture.py` (Test 12 snapshot)
**Issue:** Tests 12 and 13 render identically in text (`"● First line"`); if the character between `●` and `First` in Test 12 is U+0020 rather than U+00A0, the test is a duplicate of Test 13 and the U+00A0 path goes untested.
**Fix:** Verify the byte at that position is `\xc2\xa0` (UTF-8 for U+00A0); if not, replace it with the explicit escape `"\u00a0"` in the string literal.

## Verdict

APPROVE
Implementation is plan-aligned across all three batches; no blocking issues.