MILL_REVIEW_BEGIN
# Review: mill-plan SKILL.md: entry, phase-gate, finalize, and re-entry path gaps — holistic

```yaml
duration_s: 348.0
verdict: APPROVE
reviewer_model: sonnet
reviewer_self_id: claude-sonnet-4.5
reviewed_file: plan/
date: 2026-08-13
```

## Findings

(none)

Verification performed against source, not just plan prose: `_paths.resolve_hub_path`'s docstring mislabel (Card 2) confirmed against `_paths.py`; `_config.load_config` call-site shape `(hub_root, git_root)` cross-checked against `mill-quick/SKILL.md`, `git-commit/SKILL.md`, and `mill-go-base/SKILL.md` line 514 — all match Card 1's cited convention. `mill-start/SKILL.md`'s Discussion Review step 4b (lines 354-364) confirmed the not-converged branch commits `discussion-fix-r{N}` standalone without a following `discussed` write, validating Card 3's #821 rationale and disproving the prose it replaces. `_status.append_phase`'s auto-clear-`blocked_reason`-on-non-blocked-transition behavior (Card 4) and no-dedup-against-existing-Timeline-row behavior confirmed in `_status.py`. `_review_common.discover_round`'s `max(found)+1`/`1`-if-none contract (Card 4) confirmed. `_review_plan.run()`'s `holistic_max_rounds = max_rounds if max_rounds is not None else cfg[...]` and `round_n > holistic_max_rounds` cap (Card 5) confirmed in `_review_plan.py`; confirmed `prepare()`/`finalize()` take no `max_rounds` param, validating Card 5's claim that Agent-mode is unaffected by the CLI-flag-threading rule. `millpy-review-plan.py --stage finalize`'s `--agent-output` requirement and exact error string (Card 7) confirmed. Card 7's claim that `mill-start/SKILL.md`'s analogous sentence mis-cites "step 2" of the Agent-mode dispatch pattern (should be step 1/step 5) is independently verified against `mill-go-base/SKILL.md`'s actual step numbering — a genuine pre-existing inaccuracy Card 7 correctly avoids reproducing. The four new Step 1.5 fix-table rows (Card 6) name checks (`depends-on-batch-mismatch`, `verify-malformed-cwd`, `verify-mixed-cwd`, `verify-full-suite`) that exist in `_plan_validate.py` and were genuinely absent from the current table.

Batch Index DAG is trivial and valid (single batch, no deps, file present). All Files Touched matches the Edits union. `verify:` carries the required `PYTHONPATH=` prefix. No `Moves:` used anywhere, so no Rename-mechanic requirement applies. Card numbering 1-8 sequential, no gaps. Every card has all required fields. Decision alignment confirmed against both Shared Decisions (single-batch file-position ordering; no script behavior changes beyond the one docstring line).

## Verdict

APPROVE
All eight cards' factual claims verified against live source; DAG, fields, and decisions are consistent.
MILL_REVIEW_END
