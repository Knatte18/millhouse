# Plan-fix round 2

```yaml
round: 2
source_review: 20260516-163623-plan-review-r2.md
date: 2026-05-16
```

## Fixed

- [BLOCKING] "Card 17: wiki_root -> hub_dir rename breaks wiki-path ops in review backends" -- card 17 now scopes the change in `_review_code.py` / `_review_discussion.py` / `_review_plan.py` to a single local `hub_dir = project_root` assignment plus a one-line edit of the `_reviewers.load(wiki_root)` call. `wiki_root` itself is preserved unchanged in the backend signatures and in every other use (`resolve_ref_paths`, `resolve_existing_paths`, `load_task_title`). No upstream callers need updating for this card.
- [BLOCKING] "Card 20: Phase 3.1 runs before Phase 3.2b -- user config overwritten by template" -- renamed the new mill-setup phase from "Phase 3.2b" to "Phase 3.0b" and explicitly directed insertion immediately BEFORE Phase 3.1 (not after Phase 3.2). With the new order: migration copies `wiki/config.yaml` to `mill-config.yaml` first (preserving user content), then Phase 3.1 case-2 upserts only the missing blocks. Updated every reference (Batch Scope, batch-local decisions, card 20 body, commit message, the Decision-block mention in 00-overview.md) from "3.2b" to "3.0b".
- [NIT] "Cards 12/13: strict-check bodies in local wrappers not specified after rename" -- card 12 now contains an explicit Python snippet for the strict-check replacement in `millpy-claim.py` and `millpy-spawn.py`. The new check tests `repo_root / "mill-config.yaml"` AND `_paths.resolve_wiki_path(repo_root) / "config.yaml"`; raises `SystemExit` only when both are absent, accommodating both the migrated state and the legacy state.
- [NIT] "Card 20: integration test does not cover Phase 3.1->3.2b interaction" -- addressed via the reviewer's second alternative (document the constraint in SKILL.md). Card 20's Phase 3.0b body now includes a verbatim "Ordering constraint" callout stating that Phase 3.0b MUST execute before Phase 3.1, with a one-sentence rationale (silent overwrite of user config). No new integration-test case was added -- simulating the full mill-setup phase sequence in `test-migration.py` would expand the test's scope from a single script to a SKILL.md interpreter; the SKILL.md text is the source of truth for phase order.

## Pushed Back

_None._
