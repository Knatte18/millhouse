# Plan fix report — round 2

Source: `_mill/reviews/20260527-092129-plan-review-r2.md`

## Fixed

- `20260527-092129-plan-review-r2.md` — "[BLOCKING] Card 2 tests 1 & 2 will fail: WikiStartupError uncaught, 10s delay" — Card 2 cases 1 (stale-port-reuse) and 2 (non-ok-health-response) updated. Added `patch("wiki._client.SPAWN_TIMEOUT", 0.01)` to both cases, wrapped `_ensure_daemon` call in `try/except wiki.WikiStartupError: pass`, and noted `WikiStartupError` is correctly raised because the mocked no-op `_spawn_server` produces no fresh state file. Case 3 (successful-health) explicitly does not need the patch because it returns before entering the spawn loop. Also added the `WikiStartupError` import requirement to the imports instruction.
- `20260527-092129-plan-review-r2.md` — "[NIT] Card 7 preamble omits `import _safe_rmtree`" — added `import _safe_rmtree` to Card 7's preamble snippet, matching the `test-safe-rmtree.py` style.

## Pushed Back

(none)
