# Plan fixer report — round 1

```yaml
slug: mill-unit-test-and-signal-accuracy
round: 1
source_review: _mill/reviews/20260625-070711-plan-review-r1.md
verdict_handled: APPROVE (2 NIT)
```

## Fixed

- `20260625-070711-plan-review-r1.md` [NIT] "Plan/code prepare tests must bypass the plan validator" — Batch 1 / Card 2 Requirements now instruct the new plan-prepare test to pass `--skip-validate` or mock `_plan_validate.run` (the plan CLI runs the validator before `prepare`); noted the code CLI needs no bypass.
- `20260625-070711-plan-review-r1.md` [NIT] "Card 10 mock mechanism is under-specified for multi-call rev-parse" — Batch 3 / Card 10 Requirements now mandate a callable `side_effect` that inspects the git argv (returns `abc1234` for rev-parse during prepare, `def5678` during finalize) instead of a flat positional SHA list, to avoid order-fragility across the many `_subprocess_util.run` calls.

## Pushed Back

- (none)
