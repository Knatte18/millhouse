# Discussion Fix — round 2

Source review: `_mill/reviews/20260531-091404-discussion-review-r2.md`

## Fixed

- [NOTE] Subprocess vs. pygit2 in compute_scope_violations — clarified that implementation uses `_pygit2_util.status_porcelain(worktree, include_untracked=True)` (module pattern, mock-testable); updated testing section to use mock instead of real git fixture.
- [NOTE] Unknown-key warning rationale is wrong — corrected to: `_validate_and_return` only checks required fields; adding `timeout` to the template passes silently with no warning.
- [NOTE] scope_violations unspecified for fallback path — added that `scope_violations` is merged into the `no structured report` fallback JSON when violations exist.
- [NOTE] mill-config.yaml template not in sync scope — added `plugins/mill/templates/mill-config.yaml` to the in-scope list.

## Pushed Back

(none)
