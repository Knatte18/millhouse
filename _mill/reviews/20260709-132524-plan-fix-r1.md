# Plan-fix report — round 1

```yaml
source_review: _mill/reviews/20260709-132520-plan-review-r1.md
round: 1
```

## Fixed

- **[BLOCKING] Card 11 test never runs — not added to test list** — verified `test-config.py`'s `tests` list (line 1305-1352) is indeed the mechanism by which `main()` executes tests, and card 11's original steps never appended the new function to it. Root-caused further: `plugins/mill/unit_tests/test-config.py:1201` already contains `test_implementer_model_default_is_sonnethigh`, already registered in the `tests` list, already asserting the exact `roles.implementer.model == "sonnethigh"` value against both the plugin template and the hub's own `mill-config.yaml` — stricter and complete coverage of the regression-guard need. Rather than fix card 11's registration bug (which would only add a weaker duplicate), dropped card 11 entirely. Batch 3 is now single-card (`cards: 1`), `verify: null` (implementer-brief.md is a prompt template with no runnable surface, matching the pattern already used for batch 2's SKILL.md prose edits). Updated `00-overview.md`'s Batch Index (batch 3's `verify` now `null`), added a new Shared Decision documenting why no new test is added, and removed `test-config.py` from "All Files Touched" (no longer touched by this task).
- **[NIT] Card 11 duplicates an existing, stricter guard** — same fix as above; resolved by dropping card 11 rather than reference/extend, since the existing test already fully covers the need and there is nothing left for a new test to add.
- **[NIT] Card 2 case placement/number is contradictory** — reworded batch 1 card 2's Requirements to state one unambiguous location: append the new case physically at the END of the file (after Case 63's `except` block, before the `if errors:` summary), numbered Case 64 — consistent with the file's existing convention that case numbers match physical top-to-bottom order. Removed the contradictory "insert immediately after Case 12" instruction.

## Pushed Back

None — all three findings were accurate; no harm-check grounds to reject any.
