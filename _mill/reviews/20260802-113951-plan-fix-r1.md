```yaml
round: 1
slug: mill-config-autonomous-mode-unknown-key
```

## Fixed

- `_mill/reviews/20260802-113846-plan-review-r1.md` — "[BLOCKING] Card 2's new unit test is vacuous — passes with or without Card 1's fix": Card 2's Requirements now write a dedicated inline synthetic template (via `_write_yaml`, not `_setup_plugin_template`) that includes a top-level `pipeline:` dict, so `walk_unknown_keys` recurses into it and actually produces the `"pipeline.autonomous_mode"` path the assertion checks for.

## Pushed Back

(none — the single BLOCKING finding was verified accurate and fixed)
