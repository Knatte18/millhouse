```yaml
round: 4
source: 20260923-121407-plan-review-r4.md
```

## Fixed

- [BLOCKING:design] "Card 6's run-boundary rule still lets a dangling backtick bridge into a fence
  delimiter" — verified by hand-tracing `_BACKTICK_RE.finditer` over a joined run that still included
  the opening fence-delimiter line: a dangling backtick immediately before a fence does find a
  closing partner in one of the delimiter's own three backtick characters, since three consecutive
  backticks provide a valid `` `([^`]+)` `` closer. Changed card 6's step 1 rule so a line whose
  lstripped form starts with the triple-backtick marker is EXCLUDED from run membership on both the
  opening and closing side (a deliberate departure from the existing per-line loop's "current-state"
  toggle judgment, which was only safe when each physical line was scanned in isolation). The
  `in_fence` toggle mechanics are unchanged — only which lines are eligible for run membership
  changes. Strengthened the new regression test to place trailing prose after the dangling backtick
  (not merely at end-of-line) and a genuine dependency both before and after the fence, asserting
  both are independently and correctly flagged.

## Pushed Back

(none)
