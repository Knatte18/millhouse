# Plan-fix round 6

Review: `_mill/reviews/20260801-165643-plan-review-r6.md`

## Fixed

- [NIT] "Nit-enforcement gate self-resolve lacks explicit audit-trail append" — confirmed Card 8 (unlike Cards 9, 10, 12) had no explicit `_status.append_phase` call, though Shared Decision `audit-trail-via-status-timeline` lists `mill-go-handoff-gates` in scope. Took the second offered fix: added a clarifying sentence noting that the dispatched NIT-fix pass's `--stage finalize` call already writes the `nits-fixed-<scope>` marker (per the existing, unedited "Manual recovery note"), so no separate `self-resolved-nits` row is needed at this site — the marker itself is the intended audit trail.

## Pushed Back

(none)
