# Plan Fix — Round 1

Source review: `_mill/reviews/20260604-170758-plan-review-r1.md`

## Fixed

- **[NIT] _last_json_line name implies a return value, not bool** — renamed helper to `_has_valid_json_result(text: str) -> bool` in `01-bg-json-fallback.md` Card 1.
- **[NIT] Time-control approach inconsistent with existing test pattern** — replaced `unittest.mock.patch(time.time)` instruction with `os.utime` backdating approach (matching existing `test_log_dead_pid_no_exit` style) in `01-bg-json-fallback.md` Card 2.
- **[NIT] Card 6 references a non-existent helper function** — replaced reference to `_make_default_run_side_effect` with a concrete `side_effect` routing function keyed on `argv[1]` in `02-implementer-stuck-fields.md` Card 6.

## Pushed Back

(none)
