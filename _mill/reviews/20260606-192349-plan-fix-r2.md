# Plan fix report -- round 2

Source review: `20260606-192255-plan-review-r2.md` (verdict APPROVE, 0 BLOCKING, 5 NIT).

## Fixed

- r2 NIT "Card 5 helper extraction underspecified" -- named the shared private helper `_check_large_prompt(prompt_text, cfg, role, scope) -> tuple[bool, int]` in Card 5 so test and implementation align.
- r2 NIT "Card 11 processes_holding_path must handle None CommandLine" -- Card 11 now requires silently skipping records whose CommandLine is None/empty (CIM returns None for elevated procs) before any substring check, avoiding TypeError.
- r2 NIT "Batch 7 check_helpers fallback test wording ambiguous" -- Card 22 test now specifies: with `CLAUDE_PLUGIN_ROOT` unset, `check_helpers(["_preflight"])` returns 0 (falls back to `_preflight.py`'s own dir).
- r2 NIT "Card 19 response_poll_timeout_s.bulk relationship undocumented" -- added the clarifying comment requirement to the `templates/mill-config.yaml` edit. Placed in Card 5 (review-backend batch) rather than the psmux-dispatch batch because `templates/mill-config.yaml` is owned by the review-backend batch; placing it in psmux-dispatch would create a parallel-edit conflict on the same file across two independent batches.
- r2 NIT "Card 16 formatter-drift heuristic git-failure handling" -- Card 16 now requires treating any `git diff` subprocess failure in the heuristic as "not formatter drift" (skip auto-commit, proceed), so the heuristic never fails the run.

## Pushed Back

(none -- all five NITs fixed)
