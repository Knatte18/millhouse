MILL_REVIEW_BEGIN
# Review: _plan_validate.py: hardcoded tag-name check and fenced-code-block-unaware card parsing — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-08
```

All 5 cards verified against source. Denylist constants (`_GO_BUILD_DENYLIST_GOOS/GOARCH/RESERVED`,
`_RE_GO_RELEASE_VERSION_TAG`) placed exactly as specified, with the required divergence comment vs
`_implementer_common.py`. `_go_file_custom_tags` and `_verify_command_has_any_tag` correctly renamed
with no stale references to the old integration-only names anywhere in the file.
`_check_verify_excludes_edited_tagged_test` now parses `verify:` once per batch (moved ahead of the
per-token loop, preserving malformed-mapping skip-whole-batch behavior), loops every edited
`_test.go` token without `break`, and the finding message uses `sorted(tags)[0]` for determinism —
matches all four Shared Decisions. `_parse_cards` fence-awareness mirrors
`_requirements_fence_aware_body`'s toggle convention exactly, guarding both the card-start and
card-end `### ` matches; all 6 existing call sites are unaffected (same signature).

`mill-plan/SKILL.md`'s step-1.5 fix-table row for `verify-excludes-edited-tagged-test` was updated
to reference the discovered `<tag>` from the message instead of hardcoding `"integration"` — no
stray `"integration"` references remain in that file.

Test coverage: all 10 new tag-discovery/multi-file/multi-composed-tag tests and both fence-guard
tests are present with fixtures matching the plan's exact specification, registered in `main()`'s
`tests` list in the correct position and order. Verified the 10 tag tests' assertions (path,
message content, positive/negative tag membership) and the 2 fence tests' assertions
(`card-missing-field`/`card-numbering` emptiness) all match their stated intent.

No out-of-plan files: the 3 touched files match the plan's "All Files Touched" list and the review
manifest exactly.

## Verdict

APPROVE
Implementation fully matches plan requirements and Shared Decisions across all 5 cards; no issues found.
MILL_REVIEW_END
