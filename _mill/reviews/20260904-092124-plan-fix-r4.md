# Plan fix — round 4

## Fixed

- `_mill/reviews/20260904-092037-plan-review-r4.md` — "Card 7 never specifies how to derive `target_batch_file` from `batch_name`": added the explicit derivation (`batches = _plan_dag.extract_batch_index(...)`; `target_batch_file = Path(next(e["file"] for e in batches if e["name"] == batch_name)).stem`) to both the per-batch `SKILL.md` occurrence and the holistic `holistic-review.md` occurrence in Batch 2 Card 7's Requirements, including the fresh `batches`/`overview_text` load needed for the holistic occurrence (which has no such binding already in scope).
- `_mill/reviews/20260904-092037-plan-review-r4.md` — "`remove_batch_from_index`'s `re.sub` replacement is not backslash-escaped" (NIT): changed Batch 3 Card 9's Requirements to use the function form (`lambda _m: replacement`) instead of a bare string passed to `.sub()`.

## Pushed Back

(none — both findings verified accurate, both fixed)
