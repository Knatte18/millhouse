# Discussion fix round 1

Source review: `_mill/reviews/20260919-112513-discussion-review-r1.md`

## Fixed

- "Ownership exemption's clause-scoping contradicts its own #1022 example" — changed the ownership exemption's mechanism from clause-scoped (reusing `_clause_bounds`) to line-wide (mirroring `_is_prohibition_exempt`), since the real #1022 sentence puts a comma between the token and the ownership phrase, putting them in different clauses under the clause-scoped design. Updated the Decision, the rejected-alternatives list, the Q&A summary of the mechanism, and the Testing section's guard test (replaced `..._dirty_ownership_separate_clause_not_exempted` with `..._dirty_ownership_separate_line_not_exempted`, since the line-wide design no longer has a same-line clause boundary to guard).
- "Threshold rationale direction is backwards" — reworded the Q&A log's threshold rationale from "above the reported example's own list of 7" to "safely below the reported example's own 7-token list".
- "Mixed-shape literal-enumeration heuristic's false-negative risk on genuine dependency lines is unacknowledged" — added an explicit "Accepted tradeoff" paragraph to the literal-enumeration Decision, documenting that a genuine dependency line also naming one non-path/non-symbol literal is wrongly swept in, consistent with how `_is_prohibition_exempt`/`_CITATION_MARKERS` already document their own accepted tradeoffs.

## Pushed Back

(none)
