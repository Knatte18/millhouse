MILL_REVIEW_BEGIN
# Review: millpy-implement --stage baseline: Windows verify-baseline worktree teardown fails (WinError 145 / long paths), leaves orphaned artifacts — holistic

```yaml
duration_s: 105.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-20
```

## Findings

No findings. All four batches were checked end-to-end against the plan and against each other:

- `_long_path.to_extended` (batch 1) matches Card 1's platform-gate/idempotency/UNC/drive-absolute spec exactly (`_long_path.py:21-41`); `test-long-path.py` covers all four required cases with the specified `sys.platform` mocking.
- `_worktree.remove_safe` (batch 2) inserts `-c core.longpaths=true` at the correct position in both the `worktree remove` argv (`_worktree.py:298`) and `worktree prune` argv (`_worktree.py:365`), mirroring `_verify_baseline.py:106`'s creation-side placement per the `core-longpaths-argv-placement` shared decision. The plain `remove()` function's argv is untouched as required (`_worktree.py:211`). `test-worktree.py`'s new case asserts both argv shapes and pair-ordering correctly.
- `_junction.py` (batch 3) routes `_walk`'s `os.scandir`, `_is_junction_or_symlink`'s `lexists`/`isjunction`/`lstat`/`islink`, and `remove()`'s `lexists`/`islink`/`unlink`/`rmdir` through `_long_path.to_extended` proactively, per the `proactive-extended-path-prefix` shared decision; the existing `FileNotFoundError` vanished-entry handling is untouched (`preserve-genuine-vanished-handling`). `remove()` correctly still calls `_is_junction_or_symlink(link_path)` with the raw `Path`, not `p`, as specified. `test-junction.py` cases (i)-(l) verify each call site via marker-return mocks.
- `_safe_rmtree.py` (batch 4) applies the same treatment to `_is_reparse_point`, `_walk_strip_reparse_points`'s scandir, and both `shutil.rmtree` calls in `safe_rmtree`; `_onexc_chmod_retry` is correctly left untouched (inherits long-path safety transitively from the extended-prefixed root, as the card explains). `test-safe-rmtree.py`'s five new cases match Card 8 precisely, including the `hasattr`-patch trick for forcing the `os.lstat` fallback branch.
- No out-of-plan files: the "All Files Touched" union in `00-overview.md` matches exactly the eight files edited/created across all batches; `_verify_baseline.py` and `test-verify-baseline.py` remain context-only and unmodified.
- No cross-batch contract violations or duplicated helpers: batches 3 and 4 each call `_long_path.to_extended` independently (as intended, since it's a stateless pure function) rather than reimplementing it.

## Verdict

APPROVE
All four batches match their plan cards and shared decisions; no cross-batch or integration issues found.
MILL_REVIEW_END
