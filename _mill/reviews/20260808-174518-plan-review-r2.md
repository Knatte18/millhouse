MILL_REVIEW_BEGIN
# Review: Classify review GAPs by kind (design/scope/decision/consistency); scope discussion review to what downstream stages cannot catch — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewer_self_id: Claude Sonnet 4.5 (Sonnet 5)
reviewed_file: plan/
date: 2026-08-08
```

## Findings

### [BLOCKING:design] `_nit_gate.find_unfixed_nit_scopes` does not exist
**Location:** `00-overview.md` Shared Decision `ceiling-applied-once-at-write-time`; batch 4 Batch Scope prose; batch 4 Card 19 requirements.
**Issue:** `plugins/mill/scripts/_nit_gate.py`'s public function is `compute_unfixed_nits(worktree, reviews_dir, status_path)`, not `find_unfixed_nit_scopes`. `test-nit-gate.py` and `mill-go/SKILL.md` already reference the real name. The plan rests this Decision and Card 19's test-assertion instruction on a symbol that isn't in the codebase.
**Fix:** Replace every `find_unfixed_nit_scopes` reference in the overview and batch 4 with `compute_unfixed_nits`.

### [BLOCKING:scope] Card 13 misses one of three ERROR-shaped dicts in `_review_one_batch`
**Location:** batch 3 / Card 13.
**Issue:** `_review_one_batch` (`_review_plan.py`) has three `verdict: "ERROR"` return dicts: the initial `_reviewer_single.run` `LLMError` catch (~line 243), the resume-retry `LLMError` catch (~line 279, "resume retry failed"), and the outer `except ReviewError` handler (~line 307). Card 13 names only "the LLMError resume-retry failure and the outer ReviewError handler" for the `"findings": []` addition, omitting the first (initial-call) `LLMError` dict.
**Fix:** Reword Card 13 to cover all three ERROR-shaped return dicts in `_review_one_batch`, or use the broad "every hand-built dict... elsewhere" phrasing already used correctly in Cards 9 and 17.

### [BLOCKING:scope] Card 14's trigger condition excludes the rounds=0 skip stubs
**Location:** batch 3 / Card 14, `_review_plan.run`.
**Issue:** Card 14 says to add `"findings": []` to "every hand-built zero-count review-entry dict in `run` that currently sets `"blocking_count": 0` and `"nit_count": 0`". The two rounds=0 skip-stub review-entry dicts (`{"scope": b.stem, "verdict": "APPROVE", "file": None, "skipped": True}` for batch-disabled, ~line 752; `{"scope": "holistic", "verdict": "APPROVE", "file": None, "skipped": True}` for holistic-disabled, ~line 856) never set `blocking_count`/`nit_count` at all, so the literal trigger condition never matches them. They will lack a `findings` key, contradicting the card's own stated goal ("the key is present on every entry regardless of path").
**Fix:** Explicitly name the two skip-stub dicts as needing `"findings": []` too, independent of the blocking_count/nit_count trigger.

## Verdict

REQUEST_CHANGES
Two real envelope-shape gaps in batch 3 and one non-existent function name in batch 4/overview.
MILL_REVIEW_END
