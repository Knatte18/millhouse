```yaml
source_review: _mill/reviews/20260802-111907-discussion-review-r9.md
round: 9
```

## Fixed

- 20260802-111907-discussion-review-r9.md: "[NOTE] #755 pointer-target line ranges are stale/inaccurate" — corrected `mill-go/SKILL.md`'s "Entry-gate wait for upstream mill-plan" citation to lines 89-181 (was "~130-181") and `mill-plan/SKILL.md`'s "Entry-gate wait for upstream mill-start" citation to lines 42-124 (was "~42-91"); added a note that line numbers will drift as this task's own plan edits these files, so the heading text is the reliable identifier.
- 20260802-111907-discussion-review-r9.md: "[NOTE] Decision 757's 'six of the seven' count is unreconcilable from the bullet list alone" — reworded the Rejected paragraph in Decision `757-phase-gate-widening` to explicitly enumerate all 7 buckets (the pre-existing `implementing`/`reviewing`/`fixing` exact-match bucket plus the 6 newly-widened patterns), so the "6 of 7" count is self-evident without cross-referencing the `matches_wait_trigger` call separately.

## Pushed Back

(none)
