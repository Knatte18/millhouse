# Review: Replace manual layer letters with depends_on + isolated flags — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnetmax
reviewed_file: plan/
date: 2026-05-31
```

## Findings

### [BLOCKING] Existing test-wiki-store.py test 9 not updated
**Location:** Batch 2, Card 10
**Issue:** Test 9 in `test-wiki-store.py` asserts `key_set == {"id", "slug", "title", "group", "brief", "status", "has_proposal"}` with exact equality. Card 7 changes `list_tasks_brief` to return `depends_on`/`isolated`/`deferred` instead of `group`. After Card 7 the assertion fails: `group` is absent and three new keys appear. Card 10 adds new test case (b) for the new key set but never instructs removing or updating old test 9. Batch 2 verify (`test-wiki-store.py`) will be RED.
**Fix:** Add an explicit instruction in Card 10: "Update existing test 9 to replace the expected set `{"id", "slug", "title", "group", "brief", "status", "has_proposal"}` with `{"id", "slug", "title", "depends_on", "isolated", "deferred", "brief", "status", "has_proposal"}` and remove the old assertion; the new test (b) is the replacement."

### [NIT] Unused import of extended_title in Cards 24 and 25
**Location:** Batch 5, Cards 24–25
**Issue:** Card 24 says "Import `extended_title` from `wiki._render`" but the `_render_table` implementation described uses inline `f"{title} [{layer}]"` logic — not a call to `extended_title()`. The `status` and `deferred` fields are absent from the `rows` dicts, so calling `extended_title(row)` would produce incorrect output for done tasks anyway. Card 25 has the same unused import pattern for `millpy-inspect.py`.
**Fix:** Remove the `extended_title` import from Cards 24 and 25; the inline layer-suffix logic they describe is correct and sufficient.

## Verdict

REQUEST_CHANGES
One blocking issue: batch 2 verify will fail because the plan omits updating the existing `list_tasks_brief` key-set assertion in `test-wiki-store.py`.