# Plan fix: round 1

## Fixed

- [source: `20260918-182931-plan-review-r1.md`] "`--module-wide-only` skip point doesn't reach the shared-checkout path" — verified accurate by reading `millpy-implement.py`'s `_run_baseline_stage`/`_enumerate_batch_verify_triples`: `batches_needing_computation` is populated by reading every batch file's own `verify:` frontmatter directly off disk, independent of `## Batches`'s existence in `status.md`, so skipping only the final per-batch loop body left the expensive Case-B shared-checkout setup (`_checkout_parent_branch`, `_link_dependency_dirs`) running regardless. Rewrote Card 4's Requirements (batch 2) to bypass the enumeration step entirely and bind `batches_needing_computation = []` / `cached_batches = []` directly when `--module-wide-only` is set, so the existing `if not batches_needing_computation:` check naturally takes Case A (no shared checkout at all). Added a new test assertion (test case c) specifically confirming `_checkout_parent_branch` is never invoked under the flag.
- [source: `20260918-182931-plan-review-r1.md`] "Batch 7 verify's second assert is vacuously true today" — verified accurate: `grep` confirms `worktree_root` already appears 6 times in the current, unedited `handoff.md`, all unrelated to step 6, making the `or 'worktree_root' in t` fallback always true. Removed the fallback from both batch 7's per-batch frontmatter `verify:` and the mirrored entry in `00-overview.md`'s Batch Index, leaving only the windowed check. Confirmed by direct execution against the current unedited file that the windowed-only check correctly evaluates `False` today (only passes once Card 11's edit lands).

## Pushed Back

(none)
