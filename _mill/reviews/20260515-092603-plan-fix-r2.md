# Plan Fix Report — round 2

Source review: `_mill/reviews/20260515-092455-plan-review-r2.md`

## Fixed

- [NIT] config.yaml string replacement has no failure signal — added assertion after the `replace()` call in Card 4: raises `RuntimeError` if `"model: sonnethigh"` is not present in the result string before writing.

## Pushed Back

- [NIT] Context: none on cards 2, 3, 5 — the plan-batch.md template explicitly states "Edits: files are implicitly read — do not repeat them in Context:" and "When a field has nothing, write the literal 'none' on the same line as the field label." These cards have no files needed that are not already in Edits:, so `Context: none` is correct per spec. Adding Edits files to Context: would violate the documented no-repeat rule.
