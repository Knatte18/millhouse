# Plan Fix -- round 1

```yaml
review: 20260530-150745-plan-review-r1.md
verdict_in: APPROVE
nits_fixed: 2
nits_pushed_back: 0
```

## Fixed

- `20260530-150745-plan-review-r1.md` -- "[NIT] Phase 8 PYTHONPATH verification
  still shells to PowerShell": added a `get_user_env_var(name) -> str | None`
  read helper to `_winenv` (card 1, plus test scenario 7) and a card 2
  requirement to replace Phase 8's single
  `[System.Environment]::GetEnvironmentVariable('PYTHONPATH', 'User')` invariant
  line with a `_winenv.get_user_env_var('PYTHONPATH')` read -- removing the last
  PowerShell spawn from the `/mill-setup` run path. Scope note: discussion.md's
  Scope->Out listed Phase 8 as untouched, but the operator's emphasized
  `no-powershell-anywhere` requirement and the receiving-review rule (out-of-scope
  is not a valid dismissal) make this a justified, single-line scope expansion.
  Only that one Phase 8 line changes; all other Phase 8 checks stay as-is.

- `20260530-150745-plan-review-r1.md` -- "[NIT] `_do_broadcast` leaves ctypes
  result type and timeout unspecified": specified `result = ctypes.c_ulong(0)`
  and an explicit `500` ms timeout in card 1's `_do_broadcast` requirement,
  removing the `<result>` / `<timeout-ms>` placeholders.

## Pushed Back

_None._
