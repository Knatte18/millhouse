# Review: Fix millpy-bg EXIT marker and implementer reliability — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-06-06
```

## Findings

### [NIT] Tab/space mixing in new functions
**Location:** `_bg.py:19-31`, `test-bg-liveness.py` (TestCheckBgStatusJsonFallback class)
**Issue:** `_has_valid_json_result` and the new test class use hard-tab indentation; the rest of both files use 4-space indentation. Python 3 will run this (separate function scopes), but PEP 8 and most linters flag it.
**Fix:** Re-indent the new code with spaces to match the surrounding file style.

## Verdict

APPROVE
All plan requirements met; one cosmetic indentation inconsistency.