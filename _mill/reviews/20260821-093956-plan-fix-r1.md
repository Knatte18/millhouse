# Plan-fix round 1

Source review: `_mill/reviews/20260821-093737-plan-review-r1.md`

## Fixed

- `[BLOCKING:design] Card 7's stub-misuse warning contradicts Card 9's own new use case` — narrowed Card 7's gate from `worktree_root != hub_root` alone to `worktree_root != hub_root and "hub_relative_path" in stub_data`, so the warning fires only when a stub genuinely declares itself a pointer (`hub_relative_path`) AND also carries other keys — not on every nested-layout stub with extra keys. This exempts Card 9's own legitimate pure-override git-root stub pattern (and the pre-existing `mill-plan`/`mill-merge` git-root-stub usage the reviewer correctly noted would have been equally affected). Added a fourth unit test (`test_load_config_stub_misuse_no_warning_git_root_override_only`) plus a new stderr-silence assertion inside the existing Test 3 (`test_review_common_load_config_git_root_param`) to lock in the distinction.
- `[BLOCKING:scope] Cards 3 and 4 assert external-file facts absent from their own Context:` — added `plugins/mill/scripts/_implementer_common.py` to Card 3's Context; added `plugins/codeguide/scripts/resolve.py` and `plugins/codeguide/skills/codeguide-update/SKILL.md` to Card 4's Context.

## Pushed Back

(none)
