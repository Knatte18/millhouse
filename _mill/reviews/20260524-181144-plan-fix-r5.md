# Plan-fix report — round 5 (final, user-approved override)

```yaml
round: 5
source_review: 20260524-180825-plan-review-r5.md
verdict: REQUEST_CHANGES
blocking_count: 2
nit_count: 2
override: user_approved_after_fix
```

The user explicitly authorised closing this round after fix-and-approve when the remaining BLOCKINGs were mechanical rather than design-level. Both r5 BLOCKINGs were mechanical (one-field swap; test-assertion relaxation). Both NITs are addressed inline.

## Fixed

- `20260524-180825-plan-review-r5.md` — "[BLOCKING] Card 24 appends fold note to wrong field" — corrected card 24 line `:135` to append to `brief`, not `body`. Added explicit rationale citing batch 1 card 2's render contract (`brief` renders under the Home.md slug line; `body` renders to `proposal-{slug}.md`); writing to `body` would have hidden the fold note from Home.md and silently created a proposal file. Call now uses `wiki.upsert_task(wiki_path, target_slug, brief=((task["brief"] or "") + "\n" + fold_line))`.
- `20260524-180825-plan-review-r5.md` — "[BLOCKING] Card 14 commit-count assertion ignores daemon gitignore commit" — relaxed card 14 assertion (5) from "exactly two new commits" to "both expected commit messages (`wiki: backup pre-V3 Home.md` and `wiki: migrate to V3 (TinyDB-backed)`) appear in the new commit range, in any order". The daemon's `on_start` runs `_ensure_gitignore` and produces `chore(wiki): gitignore daemon artifacts` on a fresh wiki; the test must tolerate that interleaved commit.
- `20260524-180825-plan-review-r5.md` — "[NIT] mill-fold loses concurrency protection" — added a "Known regression — client-composed read-modify-write windows" entry to the overview's `## Shared Decisions` `no-advisory-lock` block. Documents that `mill-fold`'s `get_task` -> compute -> `upsert_task` pattern is last-writer-wins on concurrent folds against the same slug; accepts the regression because (a) `mill-fold` is interactive and concurrent folds on the same slug are extremely rare, (b) the remediation path (atomic `OP_APPEND_BRIEF` op) is concrete and can be added later without restructuring. `multi_select_groom_then_claim` already uses the atomic `wiki.merge_tasks` and is not affected.
- `20260524-180825-plan-review-r5.md` — "[NIT] mill-add silently overwrites instead of rejecting duplicate slugs" — restored V2's duplicate-slug rejection in card 21: before the upsert, call `existing = wiki.get_task(wiki_path, args.slug)` and `raise SystemExit(f"Slug {args.slug!r} already present in Home.md.")` if `existing is not None`. Error message matches V2's exact text for grep continuity. Without this check, the new `wiki.upsert_task` silently overwrites existing tasks (a regression from V2's explicit rejection).

## Pushed Back

(none — every finding was factually accurate and no fix would cause harm per the `mill-receiving-review` decision tree.)

## Note on closure

Per user override after round 4 ("bump to 5 if you must") and round 5 ("if next BLOCKINGs aren't so serious, fix and approve"): plan is set to `approved: true` after this commit; mill-plan proceeds to Handoff without a round-6 review. Convergence trend across rounds 1-5: 5 -> 3 -> 2 -> 2 -> 2 BLOCKINGs, with each round's BLOCKINGs strictly smaller in scope than the previous — final two were a one-word field swap and a test-assertion relaxation. No deferred work; the plan is shippable as-is for `/mill-go`.
