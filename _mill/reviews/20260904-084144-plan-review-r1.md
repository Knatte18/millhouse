MILL_REVIEW_BEGIN
# Review: mill-go: done-gate halt path and cleanliness-gate recovery are under-documented — holistic

```yaml
duration_s: 287.0
verdict: APPROVE
reviewer_model: sonnethigh
reviewer_self_id: Claude Sonnet 5 (claude-sonnet-5)
reviewed_file: plan/
date: 2026-09-04
```

## Findings

### [NIT:consistency] "Do NOT set phase: done" sentence has conflicting placement across Cards 3/4
**Demoted-from:** BLOCKING
**Location:** batch 2, Card 3 vs Card 4. **Issue:** Card 3 keeps the sentence as a single occurrence "immediately before the numbered `1.` step." Card 4 inserts its whole new fixer-dispatch numbered list (steps 1-3, ending in two independent `BLOCKED:` halts) "immediately after" the same anchor sentence Card 3 used, i.e. *before* that single "Do NOT set phase: done" sentence — yet Card 4 also requires the sentence be "attached to both halt outcomes in step 2 and step 3," which needs two copies, not the one Card 3 preserved. **Fix:** Have Card 4 explicitly state the sentence is duplicated once into each of its two new halt branches (and that Card 3's original single-occurrence placement is superseded), removing the ambiguity about count/location.

### [NIT:consistency] Card 5's stated "four total call sites" doesn't match its own 1+3+1=5 breakdown
**Location:** batch 2, Card 5. **Issue:** The closing parenthetical says "inserting the two new calls at each of the four total call sites (1 + 3 + 1 ... = 5 insertion points across three gates — Card 4 already handled the sixth...)" — "four" contradicts the arithmetic that follows (five, not four). **Fix:** Change "four total call sites" to "five" (or "six" if counting Card 4's pre-done-gate site too) so the header count matches the enumerated bullets.

### [NIT:consistency] Card 7's "reuse that same insertion point" is ambiguous for a relocated halt
**Location:** batch 3, Card 7. **Issue:** The fallback/cycle halt is inserted earlier in the flow (before `compute_terminal_dirt` is even called) than the pre-existing "`in_scope_dirt is None`" halt whose notify/lock-release pair it's told to "reuse... do not add a second, redundant pair." Since these are two textually and control-flow-distinct halt sites, it's unclear whether the implementer should physically duplicate the two calls at the new (earlier) site or restructure the flow so both converge on one physical block. **Fix:** Clarify explicitly that the notify/lock-release *calls* (same shape) are written again at this new site — only the message-composition pattern is being reused, not a single physical code block shared by both branches.

### [NIT:scope] Card 8's `Context:` lists `handoff.md`, which nothing in its Requirements references
**Location:** batch 4, Card 8. **Issue:** Card 8 edits `SKILL.md` step 5.5 by mirroring step 3(b)/(c) — both already inside `SKILL.md` (an Edit target, implicitly read) — but `Context:` lists `plugins/mill/skills/mill-go-base/handoff.md`, which is never mentioned in the card's Requirements or Batch Scope. **Fix:** Drop the `handoff.md` Context entry (or, if it was meant to reference something else, correct it to the intended file).

## Verdict

APPROVE
Cards 3/4's split "Do NOT set phase: done" sentence placement is genuinely ambiguous; remaining findings are minor wording/typo issues.
_Note: 1 finding(s) demoted from BLOCKING to NIT by the stage's blocking-class ceiling; current blocking_count is 0._
MILL_REVIEW_END
