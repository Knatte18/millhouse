MILL_REVIEW_BEGIN
# Review: Unit test suite: hangs, unmocked-path errors, and stuck/success envelope bug found in piecewise sweep — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-08
```

## Findings

### [NIT] `_run_main_with_mocks` reuses `wiki_mock` instead of a distinct fresh mock for `wiki._client`
**Location:** `plugins/mill/unit_tests/test-millpy-spawn.py:187,202-203,209-210` (and the same pattern at the 4 standalone Card-4 sites, e.g. `:348-349,355-356`)
**Issue:** The `## Shared Decisions` mechanic calls for injecting "a fresh `MagicMock()`" at `sys.modules["wiki._client"]`; the implementation instead reuses the same `wiki_mock` object already assigned to the dead `"_wiki"` stub-map key, rather than a separately-constructed mock dedicated to `wiki._client` (contrast with `test_spawn_rolls_back_when_write_initial_status_fails`, which does use a dedicated `wiki_client_mock`).
**Fix:** No functional impact — none of the affected tests assert on the returned `wiki_mock`'s calls — but a dedicated `MagicMock()` per the letter of the decision would keep the two stub roles (dead `_wiki` key vs. live `wiki._client` key) visually distinct for future maintainers.

## Verdict

APPROVE
All four batches match their cards precisely; wiki-stub, SHA-gating, and idle-mock fixes are correctly and consistently applied.
MILL_REVIEW_END
