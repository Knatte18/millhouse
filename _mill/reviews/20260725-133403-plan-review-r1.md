MILL_REVIEW_BEGIN
# Review: mill-plan review severity counting and validation schema gaps — holistic

```yaml
verdict: REQUEST_CHANGES
reviewer_model: sonnet
reviewed_file: plan/
date: 2026-07-25
```

## Findings

### [BLOCKING] `commit_none_card_ids` not threaded into the `--stage full` dispatch path
**Location:** Batch 06 (commit-none-backend-gate), Card 17
**Issue:** Card 17's Requirements only wire `commit_none_card_ids = _plan_dag.parse_commit_none_card_ids(_batch_text)` into the `--stage finalize` branch's `finalize_from_output(...)` call (`millpy-implement.py` lines 406-421). `millpy-implement.py` has a second, symmetric call site at lines 672-687 (`--stage full`, the synchronous/subprocess dispatch path) that calls `_forward_output(...)` directly with `card_ids=card_ids` but is never mentioned in Card 17's Requirements. Since Card 16 defaults `commit_none_card_ids` to `None` when not passed, and `_cards_done_all_commit_none(..., None)` always returns `False`, a batch composed entirely of `Commit: none` cards run via `--stage full` will still be mechanically misclassified as `stuck_type: logic` — reproducing exactly the bug this task exists to fix, on one of the two production dispatch paths. `card_ids` itself is threaded to both call sites in the existing code (line 415 and line 681), so `commit_none_card_ids` should follow the identical established pattern.
**Fix:** Extend Card 17's Requirements to also add `commit_none_card_ids=commit_none_card_ids` to the `_forward_output(...)` call at (currently) lines 672-687, mirroring the `finalize_from_output` addition, and extend Card 18's test coverage to exercise (or at least note as covered-by-code-identity) the `full`-stage call site.

## Verdict

REQUEST_CHANGES
Batch 06 Card 17 omits the `--stage full` call site, letting the no-content-commit bug reproduce there.
MILL_REVIEW_END
