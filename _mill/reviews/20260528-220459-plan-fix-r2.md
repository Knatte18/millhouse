# Plan fixer report — round 2

```yaml
review_file: _mill/reviews/20260528-220410-plan-review-r2.md
verdict_reviewed: APPROVE
blocking_total: 0
nit_total: 3
fixed: 3
pushed_back: 0
```

## Fixed

- `20260528-220410-plan-review-r2.md` [NIT] "batch-oversized context estimate includes Deletes tokens" -- Card 7 (batch 03) now subtracts `_parse_deletes_only(batch_path)` from the `parse_batch_refs` token set before resolving, so deleted-file bytes do not inflate the context estimate.
- `20260528-220410-plan-review-r2.md` [NIT] "NIT-fix commit timing is ambiguous in mill-go prose" -- Card 10 (batch 05) reworded: the NIT-fix session commits its own source-file changes atomically; the approve commit then stages `status_path` and `review_file_path` as before (unchanged staging).
- `20260528-220410-plan-review-r2.md` [NIT] "Cases B/F update options underspecified" -- Card 2 (batch 01) now prescribes a single deterministic option: update Cases B and F to assert NO raise plus a stderr-capture assertion for the fast-forward warning; do not remove (the new fast-forward-removal test does not duplicate the commit-inside-expected_paths scenario).

## Pushed Back

(none)
