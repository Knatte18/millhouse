```yaml
round: 7
source_review: _mill/reviews/20260730-191055-discussion-review-r7.md
date: 2026-07-30
```

## Fixed

- `20260730-191055-discussion-review-r7.md` — "[NOTE] 'autonomous_mode-style toggle' precedent is mid-deprecation": verified `_autonomous.py`'s docstring ("Replaces the removed `pipeline.autonomous_mode` config key") and confirmed the hub's own `mill-config.yaml` already omits `autonomous_mode` from its `pipeline:` block. Reworded the "Master on/off config switch" Decision's rationale to cite the general `pipeline.*` static-toggle pattern (`auto_merge`, `auto_report`, `done_gate`) instead, and added an explicit note explaining why `autonomous_mode` specifically is not the precedent (stale/in-flight migration to a per-hub flag file, and a materially different shape of state — ephemeral per-task escalation flag vs. static operator-set toggle — even setting the migration aside).

## Pushed Back

(none)
