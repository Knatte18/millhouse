# Plan fix -- round 2

```yaml
review: 20260808-174518-plan-review-r2.md
round: 2
```

## Note on the round-2 envelope

As in round 1, the CLI envelope reported `blocking_count: 0` / `nit_count: 0` against a `REQUEST_CHANGES` verdict.
The reviewer again emitted `### [BLOCKING:design]` / `### [BLOCKING:scope]` headings, which the installed plugin cache's un-widened `parse_blocking_count` pattern cannot match -- the defect batch 1 card 5 fixes.
The review file was treated as authoritative: 3 BLOCKING, 0 NIT, routed as step 4d.

## Fixed

- `20260808-174518-plan-review-r2.md` -- "[BLOCKING:design] `_nit_gate.find_unfixed_nit_scopes` does not exist".
  Accurate and verified against source: `plugins/mill/scripts/_nit_gate.py` defines `compute_unfixed_nits`, and no symbol named `find_unfixed_nit_scopes` exists anywhere in the repo.
  Replaced all four references -- two in the overview's `ceiling-applied-once-at-write-time` Decision, one in batch 4's Batch Scope, one in card 19's requirements.
- `20260808-174518-plan-review-r2.md` -- "[BLOCKING:scope] Card 13 misses one of three ERROR-shaped dicts in `_review_one_batch`".
  Accurate and verified: `_review_one_batch` has three `verdict: "ERROR"` return dicts (initial `_reviewer_single.run` `LLMError` catch, resume-retry `LLMError` catch, outer `except ReviewError`), and card 13 named only the latter two.
  Reworded card 13 to cover every non-success return dict in the function and named all three explicitly, matching the broad phrasing cards 9 and 17 already use.
- `20260808-174518-plan-review-r2.md` -- "[BLOCKING:scope] Card 14's trigger condition excludes the rounds=0 skip stubs".
  Accurate and verified: the two `rounds: 0` skip stubs build `{"scope": ..., "verdict": "APPROVE", "file": None, "skipped": True}` dicts that carry neither `blocking_count` nor `nit_count`, so card 14's key-presence trigger could never match them.
  Reworded card 14 to scope by the dict's position inside a `reviews=[...]` list rather than by the presence of a count key, and named the per-batch stub, its empty-list `{"scope": "batch", ...}` fallback, and the holistic stub explicitly.
  The empty-list fallback is a third stub the finding did not name; including it follows from the same reasoning and would otherwise have been the identical defect one line over.

## Pushed Back

(none)
