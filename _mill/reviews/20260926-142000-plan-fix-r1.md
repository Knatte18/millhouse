# Plan fix report: round 1

## Fixed

- 20260926-141526-plan-review-r1.md: "`halt()`'s `resume=[]` is a mutable default argument" -- `resume` is now a required keyword-only argument of `halt` and `callback` (card 1).
- 20260926-141526-plan-review-r1.md: "Card 9 undercounts the Liveness-check references it must retarget" -- card 9 now says every reference (three today) in that paragraph.

## Pushed Back

- 20260926-141526-plan-review-r1.md: "No test exercises the new script against real git" -- conflicts with the discussion's design: Scope "Out" lists "New integration tests with real git/gh (see Decision `testing-scope`)", and Decision `testing-scope` rejects a real-git integration test (needs gh for the PR-state gate; a fake gh shim is more scaffolding than the task warrants). Recorded as overview Shared Decision `unit-tests-only`.
