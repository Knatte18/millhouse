# Review: Replace powershell subprocess with winreg in mill-setup — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-05-31
```

## Findings

### [NIT] Scenarios 1 and 2 check call count, not args
**Location:** `plugins/mill/unit_tests/test-winenv.py:34,52`
**Issue:** `test_write_when_absent` and `test_write_when_different` only assert `SetValueEx.assert_called_once()`; the plan requires verifying REG_SZ and the value are passed through.
**Fix:** Add `call_args` assertions mirroring scenario 4, or accept that scenario 4 covers args exhaustively (no correctness gap exists).

### [NIT] `get_user_env_var` access-mode not verified
**Location:** `plugins/mill/unit_tests/test-winenv.py:143,161`
**Issue:** Neither `test_get_user_env_var_present` nor `test_get_user_env_var_absent` asserts that `CreateKeyEx` was called with `KEY_READ` only (not `KEY_READ | KEY_WRITE`).
**Fix:** Add `mock_winreg.CreateKeyEx.assert_called_once_with(mock_winreg.HKEY_CURRENT_USER, "Environment", 0, mock_winreg.KEY_READ)` to both get tests.

## Verdict

APPROVE
Both NITs are non-blocking; all plan contracts, shared decisions, and scenarios are correctly implemented.