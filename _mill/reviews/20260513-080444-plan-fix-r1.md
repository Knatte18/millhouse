# Plan-fix report -- round 1

```yaml
slug: safe-rmtree
round: 1
source_review: _mill/reviews/20260513-080331-plan-review-r1.md
```

## Fixed

- `_mill/reviews/20260513-080331-plan-review-r1.md` [BLOCKING] "Junction/symlink detection uses resolved path, not original" -- reordered the operation steps in Batch 1 Card 1's `safe_rmtree` requirements so the path-is-symlink (step 2) and path-is-junction (step 3) checks both run on `original = Path(path)` BEFORE `.resolve()` is called (step 4). The path-is-junction unit-test scenario in Card 2 now also asserts the junction's target survives the (raised) call, as a regression guard against `Path.resolve()` chasing the junction on Windows.
- `_mill/reviews/20260513-080331-plan-review-r1.md` [NIT] "Card 4 misstates the cleanup call form for test-millpy-spawn.py" -- rewrote Card 4 to enumerate two call shapes (Shape A: `addCleanup(shutil.rmtree, ...)` in `test-millpy-implement-holistic.py` / `test-millpy-implement.py` / `test-millpy-merge-in-subagent.py`; Shape B: `shutil.rmtree(str(tmpdir), ignore_errors=True)` in `test-millpy-spawn.py:825,887,1002`'s `finally` blocks) and specified the correct transform for each.

## Pushed Back

(none)
