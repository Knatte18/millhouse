Review: Fix millpy-bg EXIT marker and implementer reliability — holistic

  verdict: APPROVE
  reviewer_model:sonnetmax_tool
  reviewed_file: plan/
date: 2026-06-04

  Findings

  [NIT] _last_json_line name implies a return value, not bool

  Location: Batch 1 / Card 1
  Issue: _last_json_line(text: str) -> bool returns a boolean but the name reads as "get the last json line" — a reader expects a str | None return type.
  Fix: Rename to _has_valid_json_result(text: str) -> bool for clarity.

  [NIT] Time-control approach inconsistent with existing test pattern

  Location: Batch 1 / Card 2
  Issue: Plan instructs using unittest.mock.patch on time.time to control staleness, but every existing test in the file controls staleness by backdating mtime with os.utime — this is a divergence
  from the established style.
  Fix: Follow the os.utime pattern already used in test_log_dead_pid_no_exit; time.time mock is redundant when mtime is backdated far enough.

  [NIT] Card 6 references a non-existent helper function

  Location: Batch 2 / Card 6
  Issue: Requirements say "look at the _make_default_run_side_effect or equivalent helper pattern" — no such helper exists in test-millpy-implement.py. The default mock_subprocess_run uses a single
  return_value="abc1234\n" for all calls; git rev-list --count must return a digit string or int() raises ValueError. The implementer must invent routing logic to make
  test_commits_made_nonzero_on_llm_error and the skip-commit tests work.
  Fix: Drop the reference to the non-existent helper; instead direct the implementer to add a side_effect routing function keyed on argv[1] (e.g. "rev-list" → "3\n", "log" → batch-name string, others
  → existing default).

  Verdict

  APPROVE
  All issues are NITs; plan is internally consistent, sequencing correct, shared decisions faithfully implemented.