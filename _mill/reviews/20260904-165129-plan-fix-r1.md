# Plan fix report — round 1

Source review: `_mill/reviews/20260904-165052-plan-review-r1.md`

## Fixed

- r1 BLOCKING:design — "Out-of-repo exemption's `is_relative_to` test is lexical, defeats its own required test case": verified that `resolve_existing_paths` joins the raw token onto a root without calling `.resolve()`, and that `_check_out_of_worktree_target` already establishes the resolve-before-containment idiom in the same module. Batch 1 card 2's second half now requires `.resolve()` on both the candidate and each root before the `is_relative_to` comparison, with the rationale stated inline. Batch 1 card 3's `source_root` derivation carries the same requirement, per the finding's trailing note about the shared unresolved candidate.
- r1 NIT:scope — "\"Absolute-looking\" shape in Card 2's first half misses UNC/backslash-rooted literals": batch 1 card 2's first half now also treats a doubled-backslash UNC root as absolute-looking, and the module-level regex covers both the drive-letter and UNC shapes.

## Pushed Back

(none)
