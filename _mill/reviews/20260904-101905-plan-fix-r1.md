# Plan fix: round 1

Source review: `_mill/reviews/20260904-101811-plan-review-r1.md` (verdict: APPROVE, 1 NIT, demoted from BLOCKING)

## Fixed

- **[NIT:consistency] Card 1's literal replacement text drops `<status_path>` from the git-add pathspec** (partial) — the finding's second claim was accurate: `discussion.md`'s `gap-fix-commit-pathspec` Decision was never carried into `00-overview.md`'s `## Shared Decisions`. Added a `gap-fix-commit-pathspec` Shared Decision subsection to `00-overview.md`, between `gap-fix-timeline-label` and `orch-review-scratch-location`, so the pathspec ordering is visible cross-batch instead of only in Card 1's own prose.

## Pushed Back

- **[NIT:consistency] Card 1's literal replacement text drops `<status_path>` from the git-add pathspec** (primary claim) — factually wrong. Card 1's "Replace it with:" fenced block (`_mill/plan/01-review-hygiene-fixes.md` line 39, both in the working tree and in the already-pushed commit `ca1affb1`) reads `git -C <worktree> add <discussion_path> <reviews_dir>/ _mill/briefs/ <status_path> && git commit -m "mill-start: discussion-gap-fix round {N} for {slug}"` — `<status_path>` is present, immediately before `&&`. The finding's own "Fix" instruction ("Correct the fenced replacement block to end `... _mill/briefs/ <status_path> && git commit ...`") describes text that already matches the current content verbatim; no textual change was made for this half of the finding.
