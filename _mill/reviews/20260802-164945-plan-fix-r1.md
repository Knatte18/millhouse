# Plan-fix round 1

## Fixed

- `_mill/reviews/20260802-164714-plan-review-r1.md` — "[BLOCKING] Batch 6/Card 20 never implements the single-shared-checkout Decision": rewrote Card 20's Requirements in `06-baseline-stage-wiring.md` so the module-wide command is computed via `_run_module_wide_verify_algorithm` directly against the shared checkout (bypassing `compute_baseline`) whenever both module-wide and per-batch computation are needed in the same invocation; falls back to the unchanged standalone `compute_baseline` path only when no per-batch computation is needed. Also tightened the overview's "one shared parent-branch checkout" Shared Decision text and Card 24's shared-checkout-failure test (d) to match the corrected design (module-wide is only "unaffected" by a shared-checkout failure when it did not need computing this round).
- `_mill/reviews/20260802-164714-plan-review-r1.md` — "[BLOCKING] Batch 3/Card 5's serialization-branch instruction is self-contradictory Python": rewrote Card 5's Requirements in `03-status-batch-baseline-field.md` to describe the correct `if isinstance(value, list): ... elif isinstance(value, str): ... else: ...` three-way chain instead of the impossible "insert `elif` before `if`" instruction.

## Pushed Back

(none — both findings were accurate and fixed)
