# Plan fix report — round 2

## Fixed

- `20260729-075039-plan-review-r2.md` — "[BLOCKING] Cards 3/4 omit explicit `spec` assignment in override branch": confirmed against the actual `_review_discussion.py` code (`prepare()`'s `mode = "tool-use" if spec.get("tooluse") else "bulk"` immediately after resolution; `run()`'s `_reviewer_single.run(spec, prompt_text)` call) — both need `spec` defined in the override branch, which the cards' Requirements text never stated explicitly. Added `AND spec = <the resolved spec>` to both Card 3's and Card 4's override-branch requirements in `02-discussion-review-cli.md`, matching Card 6/7's existing phrasing for the `_review_plan.py` equivalent.
- `20260729-075039-plan-review-r2.md` — "[BLOCKING] Card 7 / overview Decision misdescribes plan-review's rounds:0 override precedent": confirmed against `_review_plan.py::run()`'s step-3 reviewer-loading code — the `if holistic_name is None or cfg["roles"]["plan-review"]["holistic"]["rounds"] == 0: holistic_spec = None` gate reads the raw `cfg[...]["rounds"]` value, not the max_rounds-overridden `holistic_max_rounds` variable computed earlier in the same function, so `--reviewer X --max-rounds N` does not actually force a holistic plan round when cfg rounds is 0 — unlike `_review_discussion.py`'s `prepare()`/`run()`, where the equivalent checks do read the effective max-rounds value. Card 7 itself already correctly avoids claiming this combo works; the inaccuracy was in the overview's `--reviewer bypasses a reviewer: null disablement` Decision, which implied a universal `--max-rounds`-forces-`rounds:0` escape hatch. Reworded that Decision to break the rationale out per-backend, explicitly naming `_review_plan.py::run()`'s pre-existing raw-cfg-read limitation as out of this task's scope (neither sourced issue asks for it, and no `reviewer_override` decision in this task depends on it).
- `20260729-075039-plan-review-r2.md` — "[BLOCKING] Card 12 Requirements reference `_review_common.py` functions not in Context": confirmed Card 12's Requirements cite `parse_verdict()` and `apply_actual_model_override()`, both defined in `_review_common.py`, which was absent from its `Context:` (previously `none`). Added `plugins/mill/scripts/_review_common.py` to Card 12's `Context:` in `04-reviewer-self-id-templates.md`.

## Self-corrected (not reviewer-flagged this round, but found while editing the same files)

- Round 1's card renumbering (1→1, 2-17→3-18) left several cross-references pointing at the pre-renumber card numbers: `02-discussion-review-cli.md`'s and `03-plan-review-cli.md`'s "Batch Tests" footers still cited "Cards 2-3"/"Card 4" and "Cards 5-6"/"Card 7"; `04-reviewer-self-id-templates.md`'s Card 12 (then-Card 11) Requirements cited "Cards 8-10" and Cards 9/10's Requirements cited "Card 8" twice; `03-plan-review-cli.md`'s Card 7 Requirements cited "Card 3's rationale" instead of "Card 4's". Corrected all of these to the current numbering while making the round-2 edits above, so no stale reference survives into round 3.

## Pushed Back

(none)
