# Plan-fix report — round 3

```yaml
round: 3
source_review: 20260524-175021-plan-review-r3.md
verdict: REQUEST_CHANGES
blocking_count: 2
nit_count: 4
```

## Fixed

- `20260524-175021-plan-review-r3.md` — "[BLOCKING] TinyDB refresh after pull undefined" — added explicit `Store.reload()` method to card 1 (re-opens TinyDB from disk, discarding in-memory cache). Card 5's `_render_and_commit_all` helper now performs the pull-with-reload pattern: before the render it calls `_sync.pull(wiki_path)`; on fast-forward it calls `self._store.reload()` so the in-memory DB picks up cross-host writes. Same after `commit_push`'s rebase retry succeeds — reload before returning. Card 7 store test (j) added: cover `reload()` discarding stale in-memory state when on-disk file mutates out of band.
- `20260524-175021-plan-review-r3.md` — "[BLOCKING] Multi-step ops lose atomicity without replacement" — added a new daemon op `OP_MERGE_TASKS` and matching `_handle_merge_tasks` handler in card 5; matching `Store.merge_tasks(remove_slugs, upsert, set_phase)` in card 1 (single TinyDB transaction containing all three sub-ops); matching public `wiki._client.merge_tasks(wiki_path, *, remove_slugs, upsert, set_phase)` in card 6's External interface and Requirements. Card 26's `groom_and_claim_merge` port rewritten to call `wiki.merge_tasks(...)` as a single atomic op — a crash mid-sequence is now impossible because all three sub-ops happen inside one DB transaction before render or commit fires. Card 4 adds the `OP_MERGE_TASKS` constant; card 10 protocol test extended to cover it.
- `20260524-175021-plan-review-r3.md` — "[NIT] CAS_RETRIES kept declared just to be imported by test" — deleted `CAS_RETRIES` from card 6 entirely (no production use, no test imports). Card 11 e2e test correspondingly drops the `from wiki._client import CAS_RETRIES` and any retry-count pinning — there is no client-level retry to assert.
- `20260524-175021-plan-review-r3.md` — "[NIT] `[s]` fast-path becomes dead code after parser change" — added line-level requirements to card 26 covering `_spawn_core.py:264, 292, 302, 421, 431`: drop `"s"` from `phase in (None, "s")` allowlists; delete the `fast = next(... phase == "s" ...)` fast-path lookups; update the docstring to drop the `"s"` mention. Picking now operates on `phase is None` only.
- `20260524-175021-plan-review-r3.md` — "[NIT] test-wiki-noop-commit.py disposition unclear" — rewrote card 12's treatment of `test-wiki-noop-commit.py`. The V3 noop-commit semantic lives in `wiki._sync.commit_push`; the test is ported to import `wiki._sync` and exercise `wiki._sync.commit_push(wiki_path, ["Home.md"], ...)` directly against a synthetic git fixture, preserving the identical-content -> no-commit-no-push assertion. The V2 `_wiki` import is dropped.
- `20260524-175021-plan-review-r3.md` — "[NIT] millpy-wiki-migrate.py claims mill-config.yaml is required but doesn't enforce it" — weakened card 13's preamble per option (b) of the reviewer's fix suggestion. The migration script does NOT consult `mill-config.yaml` (that's `_config.load_config`'s domain, not `_paths.resolve_wiki_path`'s). The preamble now states: "wiki path must be resolvable — either sibling discovery finds `<container>/wiki/`, or `paths.wiki:` is set in `<worktree>/.millhouse/config.local.yaml`". Step 1 no longer claims `mill-config.yaml` is required; it relies on `_paths.resolve_wiki_path`'s own exception for the failure case.

## Pushed Back

(none — every finding was factually accurate and no fix would cause harm per the `mill-receiving-review` decision tree.)
