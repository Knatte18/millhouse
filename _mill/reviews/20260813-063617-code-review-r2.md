MILL_REVIEW_BEGIN
# Review: mill-merge/merge-in: squash non-ff rejection, stale-worktree logic, parent-branch detection, conflict resolution — holistic

```yaml
duration_s: 142.0
verdict: APPROVE
reviewer_model: sonnet
reviewed_file: plan/ + source
date: 2026-08-13
```

## Findings

### [NIT:consistency] Stale/self-referential cross-reference to renamed Rollback heading
**Location:** `plugins/mill/skills/mill-merge/SKILL.md:300` and `:320`
**Issue:** Line 300's pre-squash dirty-parent-worktree "Rollback exemption" note still points readers at a section titled `"Dirty-parent-worktree halt (Step 5)"`, but Card 4 renamed that heading (line 510) to `"Dirty-parent-worktree halt and parent-fast-forward-failure halt (Step 5)"` — the referenced title string no longer exists verbatim. Separately, line 320 permanently bakes plan-authoring meta-commentary ("Card 4 of this batch ... renames it") into shipped, user-facing skill prose even though Card 4 has already landed in this same delivered implementation, describing a since-resolved transitional state as if still pending.
**Fix:** Update line 300's quoted title to match the current heading, and reword line 320 to describe the final state directly (e.g. "see the Rollback section's dirty-parent-worktree-and-fast-forward-failure halt paragraph") rather than referencing the card-authoring sequence.

## Verdict

APPROVE
All four batches match their plan cards; cross-batch contracts, helper signatures, and finally-block registrations are consistent; only a stale doc cross-reference found.
MILL_REVIEW_END
