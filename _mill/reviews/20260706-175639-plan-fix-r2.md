# Plan fix report — round 2

Source review: `_mill/reviews/20260706-175636-plan-review-r2.md` (verdict: REQUEST_CHANGES, 2 BLOCKING, 2 NIT).

## Fixed

- **[BLOCKING] compute_scope_violations crashes on git_root=None** — Batch 1 / Card 1 now types `git_root` as `Path | None` and treats `None` (or `git_root == hub_root`) as flat layout with `hub_prefix = ""`. Card 3 documents that `git_root` at the four call sites can legitimately be `None` (flows from `_forward_output`'s optional param, exercised by `test-millpy-implement.py:1143`). Card 5 gains a test case calling `compute_scope_violations(tmp_path, None)` asserting it behaves identically to the flat-layout case.
- **[BLOCKING] Unbound cwd_override at fixer finalize stage** — Batch 5 / Card 19 now pre-initializes `cwd_override = None` alongside the existing `verify_cmd = None`, before the scope branch, and keeps the `parse_verify_field` assignment inside the existing `if batch_entry is not None:` guard. Card 20 now routes both holistic-scope sites through a shared `_resolve_holistic_verify` helper, preserving the finalize-stage's `if batch_verifies:` guard (pre-initialized `cwd_override`/`verify_cmd` stay `None` when empty) and the full-stage's existing ternary structure (always assigns both names). Card 21 gains two regression cases (batch-not-found, empty holistic `batch_verifies`) asserting no `NameError`.
- **[NIT] Baseline dependency junctions not re-anchored for cwd:hub** — Batch 4 / Card 14 now also re-anchors the `_DEPENDENCY_DIR_CANDIDATES` junction targets to `tmp_path / cwd_override_relative / name` when the parameter is set (unchanged `tmp_path / name` otherwise). Card 18 gains a matching test case.
- **[NIT] Garbled Batch Scope prose in batch 5** — rewrote the paragraph to state all four read sites cleanly with correct line references.

## Pushed Back

(none)
