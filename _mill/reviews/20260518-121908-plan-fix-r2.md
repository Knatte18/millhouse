# Plan-fix report: round 2 (NIT-only APPROVE)

```yaml
slug: psmux-session-keepalive
round: 2
source_review: 20260518-121520-plan-review-r2.md
verdict_received: APPROVE
```

## Fixed

- `20260518-121520-plan-review-r2.md`: "[NIT] cleanup_session placement contradicts its public status" — Card 10 (batch 3) now places `cleanup_session` inside the `# Public API` section, immediately after `run_implementer`, alongside the other public entry points.
- `20260518-121520-plan-review-r2.md`: "[NIT] Holistic session_id capture depends on unverifiable assumption" — Card 13 (batch 4) now lists `_implementer_common.py` and `templates/implementer-holistic-brief.md` in its Context, and the requirements explicitly trace both emission paths (brief-driven happy path + `_forward_output` inferred-fallback). The card also instructs the implementer to add a one-sentence note in the SKILL.md holistic-cleanup paragraph documenting the degraded-to-no-op behaviour when the implementer misbehaves (because `cleanup_session("")` and `cleanup_session("unknown")` are already documented no-ops in batch 3 card 10).
- `20260518-121520-plan-review-r2.md`: "[NIT] psmux resume error message references non-existent `--resume` flag" — Card 9 (batch 3) requirement (e) now uses `f"psmux-claude (session {session_id[:8]}...) exited {result.returncode}: {error_detail}"` on both branches (LLMSessionError and LLMError). The misleading `--resume` text is gone; the `(session <id8>...)` prefix preserves operator debuggability. ASCII-only literal `...` per the project rule.
- `20260518-121520-plan-review-r2.md`: "[NIT] Batch 1 adds no direct test for the nested config read path" — Card 3 (batch 1) is extended with a Test 12 sub-requirement covering three sub-cases: (i) nested-positive returns True, (ii) flat-legacy returns False (hard-cutover regression guard), (iii) empty config returns False. The test is folded into Card 3 rather than added as a separate card to avoid cross-batch card-number renumbering. Batch Tests text updated to mention Test 12.

## Pushed Back

(none)
