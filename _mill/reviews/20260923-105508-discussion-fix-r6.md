```yaml
round: 6
source_review: 20260923-105410-discussion-review-r6.md
```

## Fixed

- `20260923-105410-discussion-review-r6.md`: "[BLOCKING:design] `_compute_declared_symbols_union` doesn't
  need line-join-refactor" — verified against source: the function already calls `_BACKTICK_RE.finditer`
  once over its whole Requirements body with no per-physical-line loop, so it never had the backtick-line-
  wrap corruption bug. Removed it from every `line-join-refactor`-touches-this-function claim (Scope,
  Decision, Technical context, and the Q&A log entry that had carried the same stale claim forward).
- `20260923-105410-discussion-review-r6.md`: "[NIT:consistency] Testing item (d)'s empty-union example
  contradicts whole-plan Decision" — reworded item (d) to describe the real trigger (no card anywhere in
  the plan has any reference-field entry, order-independent) instead of the misleading "first card, before
  accumulation" framing, which implied an incremental/order-dependent union that contradicts the
  whole-plan-union Decision.

## Pushed Back

(none)
