# Discussion fix report — round 7

Source review: `_mill/reviews/20260904-083601-discussion-review-r7.md`

## Fixed

- `[NIT:consistency]` (demoted from BLOCKING) "Resolvability-gate walk names 2 roots, canonicalization assumes 3" — corrected the Resolvability gate's search-implementation clause to union matches across all three roots `resolve_existing_paths` itself tries (`git_root/root`, `project_root/root`-or-bare-`project_root`, and unconditional bare `git_root`), so it now matches exactly what the paired canonicalization decision already assumed.
- `[NIT:consistency]` (demoted from BLOCKING) "Technical Context overclaims reuse of creates/deletes/moves_targets for symbols" — corrected the Technical Context bullet to state precisely that the symbol branch consults only `moves_sources` (for the exemption), not the full plan-wide-set claim it previously made. Added an explicit accepted-limitation note to Scope: symbol resolution never covers not-yet-created files (a not-yet-written batch's future symbol silently never resolves), unlike the path branch's `creates_union`/`deletes_union`/`moves_targets` resolvability leniency.

## Pushed Back

(none)
