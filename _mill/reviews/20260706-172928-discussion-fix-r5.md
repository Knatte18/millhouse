# Discussion fix report — round 5

Source review: `_mill/reviews/20260706-172854-discussion-review-r5.md` (verdict: APPROVE, 2 NOTE findings).

## Fixed

- **[NOTE] parse_verify_field behavior for absent/null verify unspecified** — added the `(None, None)` return contract for absent/`None`/whitespace-only `verify` to the "Verify-cwd explicit field (#604)" decision, tying it explicitly to `_plan_dag.py:320`'s existing pure-docs-batch skip guard; added a matching case to the `parse_verify_field` TDD candidate list in Testing.
- **[NOTE] Who derives cwd_override_relative for compute_baseline is implicit** — clarified in the baseline paragraph of the "Verify-cwd explicit field (#604)" decision that `parse_verify_field` returns a resolved absolute `cwd` (not a relative prefix), and that the baseline call site itself derives `cwd_override_relative` via `hub_root.relative_to(git_root)` from that one resolution — no second resolution call.

## Pushed Back

(none)
