# Review: mill-go / mill-merge / plan-validator follow-up bugs (round 2) — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-05-31
```

## Findings

### [NIT] `_forward_output` no-snapshot branch omits `check=True` on status call
**Location:** `plugins/mill/scripts/_implementer_common.py` — `elif start_sha is not None and snapshot_path is None:` block
**Issue:** The `git status` call in the new `elif` branch omits `check=True`, while the equivalent call in the existing snapshot branch uses `check=True`. Inside `try/except Exception: pass` this is not a bug, but the inconsistency is a maintenance trap.
**Fix:** Add `check=True` (or document why it's intentionally absent).

### [NIT] `test_worktree_template_augments_template_cfg` uses bare `sys.stderr` assignment instead of `patch`
**Location:** `plugins/mill/unit_tests/test-config.py` — `test_worktree_template_augments_template_cfg`
**Issue:** Uses `sys.stderr = stderr_capture` / `sys.stderr = original_stderr` while every other stderr-capturing test in this file uses `with patch("sys.stderr", new=io.StringIO()) as mock_stderr:`.
**Fix:** Replace the bare assignment pattern with `with patch("sys.stderr", new=io.StringIO()) as mock_stderr:` for consistency.

## Verdict

APPROVE
All three batches are correctly implemented against the plan; no blocking defects found.