# Plan fix — round 4

## Fixed

- `20260812-190213-plan-review-r4.md` — "[BLOCKING:design] Card 9's divergence-halt fixture cannot actually diverge — `--ff-only` will succeed, not fail": rewrote Batch 4 Card 9's divergence-halt sub-scenario to also advance `origin_ff_halt/main` independently (via a nested throwaway `advancer_ff_halt` clone, mirroring the success sub-scenario's `advancer_ff` pattern) before the local unpushed commit, so both sides genuinely diverge and `git merge --ff-only` has no fast-forward path in either direction — matching what the test's own assertion (non-zero exit) actually requires.
- `20260812-190213-plan-review-r4.md` — "[NIT:consistency] Batch 2 Card 3's \"Why\" narrative overstates the `--ff-only` failure trigger": clarified Batch 2 Card 3's prose to state the halt only fires on genuine two-sided divergence (local-only commits AND an independently-advanced origin), not merely unpushed local commits alone (which `--ff-only` treats as a no-op success).

## Pushed Back

(none)
