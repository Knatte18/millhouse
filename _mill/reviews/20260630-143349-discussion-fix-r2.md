# Discussion fixer report — round 2

```yaml
slug: mill-merge-pr-state-awareness
round: 2
source_review: _mill/reviews/20260630-143305-discussion-review-r2.md
verdict_in: APPROVE (3 NOTE)
```

## Fixed

- r2 "merge_commit shape left unspecified" — `normalized-state-precedence` now
  states `_pr_state.py` preserves gh's raw `mergeCommit` object (carrying `.oid`),
  so `_apply_pr_reap_record`'s `(merge_commit or {}).get("oid")` tag-target
  fallback (millpy-cleanup.py ~line 635) keeps working after the refactor.
- r2 "'generalizes pr-pending MERGED' undersells a behavior change" —
  `merged-remote-cleanup-only` now records that the existing SKILL.md
  `## PR-path re-entry` MERGED branch (~line 264) skips Step 4 today and tags the
  un-cleaned tip; the plan MUST rewrite it to run Step 4, not assume it identical.
- r2 "CLOSED-proceeds collides with branch-protection fallback" —
  `closed-no-merge-proceeds` now acknowledges that the CLOSED route's local squash
  push can be rejected by branch protection and re-trigger Step 5's auto-PR
  fallback; the plan must not treat CLOSED -> local-squash as always terminal.

## Pushed Back

- None. All three NOTEs were factually accurate, caused no harm to fix, and
  conflicted with no documented design decision.
