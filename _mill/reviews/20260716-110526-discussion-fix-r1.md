```yaml
type: discussion-fix
round: 1
source_review: _mill/reviews/20260716-110438-discussion-review-r1.md
date: 2026-07-16
```

## Fixed

- "Other bare-connect sites hit the same daemon" — added a scope note under the "Remove the
  redundant bare-connect probe in `_ensure_daemon`" decision documenting that
  `wait_for_socket_reachable()` and `_is_stale()` are two additional legitimate (non-redundant)
  bare-connect probes to the same daemon that remain unchanged, and clarified that this is
  precisely why the `_handle_connection` daemon-side fix is required, not merely defense-in-depth.
- "Malformed-nonempty payload no longer gets a response" — added a scope note under the
  "`_handle_connection` empty/malformed-payload handling" decision explicitly stating that
  dropping the reply covers both the zero-byte and malformed-nonempty sub-cases intentionally,
  since no known in-repo sender transmits malformed-but-nonempty JSON to this port, so a plan
  writer must not restore the old `sendall` for this branch.

## Pushed Back

(none)
