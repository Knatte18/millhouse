# Review: Migrate wiki task store to TinyDB — holistic

```yaml
verdict: APPROVE
reviewer_model: sonnethigh
reviewed_file: plan/ + source
date: 2026-05-23
```

## Findings

### [NIT] What-comments in _store.py violate shared decision
**Location:** `plugins/mill/scripts/wiki/_store.py:15-22`
**Issue:** Three inline comments (`# Collect slugs from parsed content`, `# Delete tasks not in the new content`, `# Upsert remaining tasks`) describe WHAT the code does, not WHY — violating the shared "no comments unless WHY is non-obvious" decision.
**Fix:** Remove all three comment lines; the variable names and method calls are self-explanatory.

### [NIT] Dead PYTHONPATH setup in integration test
**Location:** `plugins/mill/integration_tests/test-wiki-daemon-tinydb.py:89-95`
**Issue:** The `env` dict with PYTHONPATH manipulation is constructed but never passed to `_client.read` or `_client.write_commit_push` (they accept no `env` parameter); daemon spawning sets its own PYTHONPATH via `_spawn_server`.
**Fix:** Remove the `env` setup block; it has no effect and misleads readers into thinking it influences the daemon's environment.

### [NIT] What-comment in _server.py
**Location:** `plugins/mill/scripts/wiki/_server.py:58`
**Issue:** `# Set up rotating file logger` describes WHAT, not WHY.
**Fix:** Remove the comment.

## Verdict

APPROVE
Implementation is complete and correct; all plan cards realized with no blocking issues.