# Plan fix — round 3

## Fixed

- `20260813-090212-plan-review-r3.md` — `[BLOCKING:design] Card 3's widened trigger is unreachable — phase table not widened`. Confirmed: the Entry step 4 phase table's `phase: discussing` row was never widened, so `discussion-fix-r{N}` would fall through to the catch-all row before ever reaching the `matches_wait_trigger` call Card 3 already fixed — the #821 fix was dead code. Added a new Requirements paragraph to Card 3 widening the phase table row's left cell to `` `phase: discussing`, or matching `^discussion-fix-r\d+$` `` (mirroring `mill-go-base/SKILL.md`'s own widened-row cell-text pattern), leaving the action column unchanged.
- `20260813-090212-plan-review-r3.md` — `[NIT:consistency] Card 5 cites a nonexistent "Card 4's 'Dispatch-mode scoping'"` (demoted from BLOCKING). Dropped the dangling cross-reference in Card 5's "Subprocess/psmux CLI-flag threading" paragraph and inlined the Agent-mode-unaffected rationale directly (`_review_plan.py`'s `prepare()`/`finalize()` accept no `max_rounds` parameter; `millpy-review-plan.py` never reads `args.max_rounds` outside `--stage full`), per NITs being fixed under the default rule regardless of demotion class.
- `20260813-090212-plan-review-r3.md` — `[NIT:consistency] Card 6's verify-mixed-cwd row overstates a single message's contents`. Reworded per the reviewer's suggested fix: each error dict states only its own batch's cwd plus the conflicting-name list; reading them requires reading every emitted `verify-mixed-cwd` error dict together.

## Pushed Back

(none)
