# Plan-fix round 1

```yaml
round: 1
source_review: 20260516-162642-plan-review-r1.md
date: 2026-05-16
```

## Fixed

- [BLOCKING] "Existing `test-config.py` tests will fail after Card 10 signature change" -- updated card 14 to explicitly rewrite the five existing `load_config` test calls from `(wiki, wt_root)` to `(wt_root, wt_root)`, delete the two machine-layer tests (`test_load_config_machine_layer_present_merged`, `test_load_config_machine_absent_graceful`), and update the module docstring. Also added a parallel update directive in card 15 for `test-review-common.py`.
- [BLOCKING] "`write_commit_push` after `git rm` will always fail" -- rewrote card 19's wiki-delete branch (both config and agents) to use a direct `git rm` -> `git commit` (with "nothing to commit" tolerance) -> `git push` (with one rebase-retry on non-fast-forward) sequence inside the wiki lock, bypassing `_wiki.write_commit_push`. Mirrors `_wiki._write_commit_push_body` retry policy for UX parity.
- [BLOCKING] "CLAUDE.md `wiki-config.yaml mirror` bullet left stale after Batch 4" -- added a CLAUDE.md edit step to card 22 that (a) rewrites the line-109 mirror bullet to point at `mill-config.yaml` and (b) updates the parenthetical reference at line 116 to swap `wiki/config.yaml` -> `mill-config.yaml`. Added `CLAUDE.md` to card 22's Edits: list and renamed the card to reflect the additional scope.
- [NIT] "Card 6 entry count is incorrect" -- changed "all 12 entries" to "all 14 entries" and enumerated the names explicitly.
- [NIT] "Card 18 legacy-wiki fallback test needs `resolve_wiki_path` mocking" -- card 18 now specifies monkeypatching `_paths.resolve_wiki_path` to return a `tmp_path / "wiki"` fixture dir, with an explanatory note. The same monkeypatch directive was added to card 14's fallback and both-files-present tests and to card 15's parallel structure.

## Pushed Back

_None._
