## Fixed

- `20260729-143356-plan-review-r3.md`: "relocate_and_scaffold's hub_root is not hub_relative_path-aware" — verified: `resolve_main_worktree_root` (used to fix round 2's cwd self-reference bug) never consults `hub_relative_path`, unlike `resolve_hub_path`/`resolve_wiki_path`. For a main worktree whose own hub `.millhouse` lives in a git subdirectory (the same M2+sub repo shape #728/Batches 3-4 already fix elsewhere), `hub_root / ".millhouse"` would not exist, and `_worktree.copy_millhouse` silently no-ops (does not raise) when `src` is missing — a silent partial-scaffold failure. Fixed Card 10 Step 4 to resolve `hub_root` in two steps: `main_root = _paths.resolve_main_worktree_root(git_root)` (git-metadata based, immune to the round-2 cwd-self-reference bug) then `hub_root = _paths.resolve_hub_path(cwd=main_root)` (passing the already-correct `main_root` as `resolve_hub_path`'s explicit `cwd` argument runs its normal stub/`hub_relative_path`-aware walk rooted at the true main worktree instead of at the broken worktree's own cwd — verified this composes correctly by reading `resolve_hub_path`'s full implementation, which resolves `git_root = resolve_git_root(cwd)` from whatever `cwd` it's given). Updated Card 8's docstring requirement to state this two-step resolution explicitly and to warn against the single-step `resolve_main_worktree_root`-only version. Extended Card 11's integration test with a new scenario (f): a hub-in-subdirectory main worktree fixture, asserting the two-step resolution finds the subdirectory hub and the copied `.millhouse` is non-empty.

## Pushed Back

(none — the finding verified accurate against source; fixed.)
