# Plan-fix round 1

## Fixed

- `_mill/reviews/20260921-154516-plan-review-r1.md` — "Card 3's `error` field can't be split from
  the existing combined branch" (BLOCKING). Verified: `_pr_state.py` line 79 is one combined
  conditional. Rewrote Card 3's Requirements to explicitly instruct splitting it into two separate
  `if` statements before either carries an `error` value.
- `_mill/reviews/20260921-154516-plan-review-r1.md` — "Card 11 Context omits `_status.py` despite
  naming its functions" (BLOCKING). Added `plugins/mill/scripts/_status.py` to Card 11's `Context:`.
- `_mill/reviews/20260921-154516-plan-review-r1.md` — "Card 12's 'speculative early launch' text
  describes a mechanism that no longer exists post-Card-11" (BLOCKING). Verified against the actual
  `mill-go-base/SKILL.md` "0.5. Baseline pre-flight" text. Added an explicit instruction to Card 12
  to delete the "second, ordinary --stage baseline invocation for the per-batch substage" clause.
- `_mill/reviews/20260921-154516-plan-review-r1.md` — "Card 5's halt message duplicates guidance
  already covered by Card 3's structural gap" (NIT, demoted from BLOCKING). Added a one-line
  dependency note to Card 5 acknowledging the coupling to Card 3's `error` semantics.
- `_mill/reviews/20260921-154516-plan-review-r1.md` — "Card 1's test guidance mocks a function the
  fix removes from the call path" (NIT). Removed the `wait_for_socket_reachable` patch from Card 1's
  Batch Tests description and explained why it would be an inert no-op after the fix.
- `_mill/reviews/20260921-154516-plan-review-r1.md` — "`derive_duration_s`'s module placement
  diverges from `_mill/discussion.md`'s stated Decision without noted rationale" (NIT). Added a
  rationale note to Card 13 explaining the deliberate relocation to `_agent_dispatch.py`.

## Pushed Back

(none)
