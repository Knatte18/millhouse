# Plan fix — round 1

Source review: `_mill/reviews/20260919-113512-plan-review-r1.md`

## Fixed

- "Card 2 recovery paragraph asserts a false commit-count claim" (demoted from BLOCKING) — verified against `mill-merge-in/SKILL.md` Steps 3 and 5.5, which can each add a commit inside `mill-merge`'s own Step 2 call before Step 4's cleanup commit runs. Dropped the false "only commit" justification entirely and replaced it with the actually-sufficient reasoning: `HEAD` at this point is Step 4's own cleanup commit, so `HEAD~1` is unconditionally the commit immediately before that deletion, regardless of how many earlier commits led up to it.
- "`<status-relative-path>` placeholder is not tied to any bound variable" — replaced the undefined placeholder with the literal `_mill/status.md` path (the same fix that resolved the commit-count finding above already made this concrete).
- "Batch Tests' 'no other test reads either file' claim is slightly inaccurate" — verified `test-guards.py`'s `_WIKI_CWD_ALLOWLIST` does list `plugins/mill/skills/mill-merge/SKILL.md`; reworded the claim to "no other test asserts specific content of either file" and named the allowlisted exception explicitly.

## Pushed Back

(none)
