# Discussion: mill-merge/merge-in: nested-layout config resolution, stale locks, and rollback-target bugs

```yaml
task: mill-merge/merge-in: nested-layout config resolution, stale locks, and rollback-target bugs
slug: mill-merge-nested-layout-and-lock-bugs
status: discussing
parent: main
```

## Problem

This task bundles seven GitHub-issue-sourced defects (#904, #900, #899, #880, #879, #863, #862) that all surfaced while running `mill-merge` / `mill-merge-in` — the skills that sync a task's parent branch and squash-merge a completed task back to its parent. They fall into three clusters: (1) the push-failure/rollback/lock-hygiene path around `mill-merge` Step 5 and Step 8 can race, leak state, or reset the wrong ref; (2) `_parent_branch.check_liveness` misclassifies a live, local-only (never-pushed) parent branch as dead; (3) three independent nested-hub-layout bugs (`hub_root != git_root`) — config-override resolution, verify-replay cwd, and codeguide path resolution — silently diverge or fail depending on which entry point or cwd happens to be in play. None of these are one coherent feature; each is a narrow, independently-fixable correctness bug discovered in production use (mostly on the `NORCE-DrillingAndWells/Models` nested-hub repo and this repo's own dogfooding).

**Why now:** all seven were filed against real runs (2026-08-14 through 2026-08-20) and are open correctness/reliability gaps in a skill every task in this repo depends on (mill-merge/mill-merge-in run at the end of every task).

## Scope

**In:**
- `mill-merge/SKILL.md` Step 5 push-failure classification: add a plain-non-fast-forward branch that fetches + rebases + retries the push once (fixes #904, and completes the fix #862 asked for — the rollback-target part of #862 is already fixed, see Decisions).
- `mill-merge/SKILL.md`: move Step 8 (release merge lock) to run immediately after Step 5's squash+push lands, before Steps 5.5/6/7 (fixes #863).
- `plugins/mill/scripts/_parent_branch.py`: widen `check_liveness` to also accept a live local branch ref (fixes #879).
- `plugins/mill/scripts/_review_common.py` (or its `load_config` wrapper used by `millpy-review-plan.py`): pass `git_root` instead of `project_root` as `_config.load_config`'s second argument, aligning it with `mill-plan`/`mill-start`/`mill-merge`'s existing call convention; add a stderr warning when a `config.local.yaml` sets `hub_relative_path` alongside other top-level keys (fixes #900).
- `mill-merge-in/SKILL.md` Step 4 (Verify): change the `cwd is None` resolution from `hub_root` to `git_root`, matching `_run_verify_gate`'s actual live-dispatch behavior (fixes #899).
- `mill-merge-in/SKILL.md` Step 5 (Codeguide update): pin the `codeguide-update` invocation's cwd to `hub_root` explicitly instead of relying on ambient cwd (fixes #880).
- Any unit test additions/updates for `_parent_branch.check_liveness` and the `_config`/`_review_common` caller alignment.

**Out:**
- `codeguide/scripts/resolve.py`'s core inline/sibling walk algorithm — not touched; the nested-layout fix for #880 lives entirely at the `mill-merge-in` call site (see Decisions, "codeguide cwd pinning").
- Any change to `_config.load_config`'s parameter names/signature — the fix is caller alignment only, not an API rename.
- Broader "three-state liveness" modeling for #879 — rejected as unjustified scope (see Decisions).
- `mill-finalize`/PR-path dispatch — out of scope; this task only touches the direct-merge path in `mill-merge`/`mill-merge-in`.
- Any other open issues not in this task's source list.

## Decisions

### push-failure-classification (#904, #862)

- Decision: In `mill-merge/SKILL.md` Step 5's "On push failure" handling, add a third branch after the existing branch-protection-substring check: when the output matches none of the branch-protection substrings AND is a plain non-fast-forward rejection, run `git -C <parent-path> fetch origin <parent_branch>` + `git -C <parent-path> rebase origin/<parent_branch>` to replay the local squash commit, then retry `git -C <parent-path> push` once. On a real conflict during the rebase, abort the rebase (`git -C <parent-path> rebase --abort`) and fall through to the existing Step 1–5 rollback with a message naming the conflicting files. On a second push failure after a clean rebase, also fall through to rollback.
- Rationale: the existing pre-squash `merge --ff-only` guard (already in code, commit `6474a96a`) narrows the race window but cannot close it — `origin/<parent_branch>` can still advance between the ff-only check and the final push (squash+commit happen in between). #904 reproduced exactly this. #862 independently asked for the identical fetch+rebase+retry fix for the same Step 5 push-failure path. User's standing guidance (prior feedback on git races): fix races via rebase+retry, don't halt-and-ask.
- Rejected: leaving the ff-only pre-check as the sole mitigation (does not close the race); a second ff-only re-check immediately before push with no retry (still fails the push and rolls back on legitimate remote advancement, wasting the local squash work for no reason since the fix is trivial).
- Note — already fixed, no action needed: #862's *rollback-target* complaint (`git -C <parent-path> reset --hard mill-checkpoint-<name>` resetting the parent to an unrelated child-branch SHA) was already fixed by commit `6474a96a` (2026-08-13) — the `## Rollback (Steps 1-5 only)` section now resets to `origin/<parent_branch>`, with an explanatory "Why" paragraph already in place. Verify this is still true and, if so, do not re-touch that section beyond what the new rebase-retry branch needs.

### merge-lock-early-release (#863)

- Decision: Move Step 8 (release merge lock) to run immediately after Step 5's squash+push succeeds (including after the new rebase-retry path above lands), before Steps 5.5 (cache-helper preflight), 6 (archive tag), 7 (Home.md flip). Steps 5.5/6/7 operate on the task branch, the plugin cache, and the wiki — none of them touch the parent worktree, so none of them need the lock held. Update the "Failure handling after the squash landed on parent" note (currently under Step 7) to no longer say "release all locks" since the lock is already released by that point.
- Rationale: `mill-merge`'s steps are Claude-agent instructions, not code with real try/finally — if the orchestrating session is interrupted (crash, disconnect, context exhaustion) between Step 5's push and Step 8, the lock leaks regardless of how carefully Step 8's own instructions are written. Releasing the lock as soon as the locked resource (the parent worktree) is no longer being touched eliminates the exposure window structurally rather than relying on every downstream step to remember to release it. This matches the actual reported scenario (push had already landed on origin; lock never got released).
- Rejected: keeping release at Step 8 and adding more explicit "release lock" reminders to intermediate steps (still leaves a real gap if the session dies mid-step); a self-heal check at lock-acquisition time that treats a lock as stale immediately when its recorded branch's commit is already on `origin/<parent_branch>` (useful defense-in-depth, but adds parsing complexity for a case the early-release fix already prevents — can be added later if leaks recur).

### dead-parent-liveness-widen (#879)

- Decision: Widen `_parent_branch.check_liveness(branch, git_root)` to return `True` when *either* `git ls-remote --exit-code origin <branch>` succeeds *or* a local branch ref exists (`git rev-parse --verify --quiet refs/heads/<branch>` against `git_root`, exit 0). Keep the function's existing single-bool return type and both call sites (`mill-merge/SKILL.md` Entry Step 4, `mill-merge-in/SKILL.md` Entry step 2) unchanged — they already branch on a single `alive` bool and already handle the "resolve_dead_parent" flow correctly when `alive` is `False`.
- Rationale: the existing docstring warning ("local remote-tracking refs... deliberately not used, because mill-cleanup's remote-branch deletion never prunes them") is about stale `origin/<branch>` *remote-tracking* refs cached locally — a different, unrelated signal from the local branch ref itself (`refs/heads/<branch>`). Checking the local branch ref directly does not reintroduce the staleness problem that warning describes, since `mill-cleanup` deletes the actual local branch (not just its remote-tracking counterpart) when a task is torn down. The issue itself notes the downstream `MERGE_REF` fallback in `mill-merge-in` Steps 1/3 already treats a local-only parent correctly — only the liveness gate disagreed with it.
- Rejected: introducing a three-state classification (`alive-remote` / `alive-local-only` / `dead`) threaded through both skills — no caller currently needs to distinguish those two "alive" cases, and no caller does anything different for them; this would be scope beyond what the bug requires.

### config-local-yaml-caller-alignment (#900)

- Decision: Change the `load_config` wrapper `_review_common.py` uses on behalf of `millpy-review-plan.py` (currently derives `worktree_root = mill_dir.parent`, i.e. the project/hub root) to instead pass `git_root` as `_config.load_config`'s second positional argument, matching the convention every other entry point already uses (`mill-plan/SKILL.md` Entry step 2, `mill-start/SKILL.md` Entry step 2, `mill-merge/SKILL.md` Step 1 all call `_config.load_config(hub_root_value, git_root_value)`). Additionally, in `_config.load_config`, after loading the stub layer (layer 3, `worktree_root / ".millhouse" / "config.local.yaml"`), if that stub dict has any top-level key other than `hub_relative_path`, print a `[config] warning: ...` line to stderr naming the file and the unexpected keys — mirroring the existing stale-`review:`-key warning precedent already in this module's style.
- Rationale: confirmed via source read that `_config.load_config(hub_root, worktree_root)`'s layer-3 stub path is `worktree_root / ".millhouse" / "config.local.yaml"` — when callers disagree on what `worktree_root` means (hub root vs. git root), they silently read different files for the "local override" layer in a nested-hub layout, where the two only diverge when hub_root != git_root. `mill-plan`/`mill-start`/`mill-merge` already pass `git_root` there (their local `worktree_root`-named variable is actually bound to `resolve_hub_path()` and passed as the function's first arg, `git_root` as the second — this is the pattern issue #900 traced exactly). `_review_common.py`'s wrapper instead collapses both stub and real layers onto the hub itself, so a git-root stub is invisible to it. Aligning on the already-majority convention (3 of 4 entry points already do this) is lower-risk than the reverse.
- Rejected: docstring-only clarification (leaves the actual inconsistency live — the bug is the divergence itself, not just that it's undocumented); aligning callers without the warning (loses cheap, immediate detection of the exact misuse pattern — real-key stub file — that surfaced this bug in the first place).

### merge-in-verify-cwd-default (#899)

- Decision: In `mill-merge-in/SKILL.md` Step 4 (Verify)'s cwd-resolution bullet, change "hub_root when cwd is None (the string-form default)" to "git_root when cwd is None (the string-form default)".
- Rationale: confirmed via `_implementer_common._run_verify_gate`'s docstring and its two call sites in `_run_verify_gates` (`plugins/mill/scripts/_implementer_common.py` ~L1043, ~L1072) that mill-go's own live batch-dispatch always passes `git_root=git_root`, and `_run_verify_gate` resolves a plain-string verify command's cwd to `git_root` whenever `git_root` is not `None` (only falling back to `project_root`/hub when `git_root` itself is `None`, which live dispatch never does). So the plain-string `verify:` form's *actual* originally-exercised cwd is git_root, not hub_root, in every real mill-go run. `mill-merge-in`'s replay must match that to avoid exactly the reported symptom (`MSBUILD : error MSB1009: Project file does not exist` when replayed from hub_root against a git_root-relative command).
- Rejected: leaving the hub_root default and requiring plan authors to always use the explicit `{cwd: ..., command: ...}` mapping form for nested-hub plans — this pushes a correctness fix onto plan-authoring discipline instead of fixing the actual default to match live-dispatch behavior, and does nothing for plans already written with the plain-string form.

### codeguide-cwd-pinning (#880)

- Decision: In `mill-merge-in/SKILL.md` Step 5, when invoking the `codeguide-update` skill, explicitly resolve and use `hub_root = _paths.resolve_hub_path()` as the operative cwd/root for that invocation (however the `codeguide-update` skill accepts a cwd/root — pin it explicitly rather than relying on whatever the ambient shell cwd happens to be at that point in the run).
- Rationale: confirmed via source read of `codeguide/scripts/resolve.py` — `_inline_walk`/`resolve()` only walks from cwd *upward* to the git toplevel (`_walk_levels`); it has no mechanism to find a `_codeguide/` directory that lives in a descendant directory below cwd (i.e., the hub, nested under git_root). Running from git_root in a nested-hub layout can therefore never find a codeguide anchored at hub_root. `resolve.py` is a shared, widely-depended-on resolver used by other (flat-layout) call sites; changing its core upward-only walk algorithm risks unintended behavior changes elsewhere. Pinning the cwd at the one call site that has this specific nested-hub requirement is the minimal, contained fix — and matches the issue's own second suggested fix.
- Rejected: changing `resolve.py`'s `_inline_walk`/`resolve()` to also search descendant directories under cwd — larger blast radius across every codeguide call site for a fix only one nested-hub call site needs.

## Technical context

- `plugins/mill/skills/mill-merge/SKILL.md` — Step 5 "Direct squash" (push-failure classification lives at "On push failure — branch-protection fallback", currently only two branches: branch-protection match -> PR fallback, no match -> fail+rollback). Step 8 "Release merge lock" currently runs last in the Teardown sequence (Steps 4-9), after 5.5/6/7.
- `plugins/mill/skills/mill-merge-in/SKILL.md` — Step 4 "Verify" (cwd-resolution bullet, currently reads `hub_root when cwd is None`); Step 5 "Codeguide update" (currently invokes `codeguide:codeguide-update` skill with `"$CHK..HEAD"` as its only argument, no explicit cwd/root pinning).
- `plugins/mill/scripts/_parent_branch.py` — `check_liveness(branch, git_root)` (currently `git ls-remote --exit-code origin <branch>` only, ~L100-112); both call sites already handle `alive`/`resolve_dead_parent` — no call-site changes needed beyond the widened function.
- `plugins/mill/scripts/_config.py` — `load_config(hub_root, worktree_root)` (~L221); layer-3 stub read at `worktree_root / ".millhouse" / "config.local.yaml"` (~L295); layer-4 real read via `hub_relative_path` (~L303-307). `warn_unknown_keys`/`deprecated_keys` (~L112-126) is the existing precedent style for the new stub-misuse warning.
- `plugins/mill/scripts/_review_common.py` — hosts the `load_config` wrapper `millpy-review-plan.py` calls; confirm the exact call site before editing (issue #900 cites `millpy-review-plan.py:174-178` calling into a wrapper that derives `worktree_root = mill_dir.parent`).
- `plugins/mill/scripts/_implementer_common.py` — `_run_verify_gate` (~L791, docstring documents the `git_root`-wins-over-`project_root` fallback order) and `_run_verify_gates` (~L1043, ~L1072, both pass `git_root=git_root` through) — the grounding evidence for the merge-in-verify-cwd-default decision above.
- `plugins/mill/scripts/_plan_dag.py` — `parse_verify_field` (~L344) already supports the explicit `{cwd: hub|git_root, command: ...}` mapping form; this task does not touch `parse_verify_field` itself, only the plain-string-form default consumer in `mill-merge-in/SKILL.md`.
- `plugins/codeguide/scripts/resolve.py` — `_inline_walk`/`_walk_levels`/`resolve()` (~L28-137); confirmed upward-only walk behavior via source read. Not modified by this task (see Decisions, "codeguide cwd pinning").
- Commit `6474a96a` (2026-08-13, "mill-merge/merge-in: squash non-ff rejection, stale-worktree logic, parent-branch detection, conflict resolution") already added the pre-squash `merge --ff-only` guard and the `origin/<parent_branch>` rollback-target fix — read its diff before touching Step 5/Rollback sections again, to avoid re-deriving already-settled reasoning.

## Constraints

No `CONSTRAINTS.md` present at hub root as of this discussion.

- All `mill-merge`/`mill-merge-in` SKILL.md changes must preserve the existing cross-worktree invariants (never `cd <parent-worktree>`, all parent-branch git ops via `git -C <parent-path>`).
- The rebase-retry addition (push-failure-classification decision) must not weaken the existing branch-protection fallback path — it is a new, distinct branch in the same classification, evaluated after the existing substring check, not a replacement for it.
- Any `_config.py` change must not alter `load_config`'s existing return shape or break the documented merge-order (plugin template -> repo layer -> local stub -> local real -> env overrides) for callers not touched by this task.
- `check_liveness`'s widened local-ref check must use `git_root` (not cwd) for the `git rev-parse --verify` call, matching every other git invocation pattern in `_parent_branch.py`.

## Testing

- `_parent_branch.py`: add/extend unit tests for `check_liveness` covering (a) remote-only alive, (b) local-only alive (new — the #879 regression case), (c) neither (dead), (d) both. Existing `plugins/mill/unit_tests/` fixtures use in-memory/tempfile git repos per project convention (`mill:testing`/`python:python-testing`) — no real network calls.
- `_config.py` / `_review_common.py`: add a unit test asserting both callers (the `mill-plan`-style direct call and the `millpy-review-plan.py` wrapper) resolve the same stub file path for an identical nested-hub fixture (hub_root != git_root) — this is the regression test for #900's divergence. Add a second test for the new stub-misuse warning (a stub file with `hub_relative_path` plus an extra key emits the stderr warning; a stub with only `hub_relative_path` does not).
- `mill-merge/SKILL.md` and `mill-merge-in/SKILL.md` changes are markdown-instruction changes, not directly unit-testable — verify via `plugins/mill/integration_tests/test-merge.py` (extend if it already covers Step 5/Step 8 sequencing; the push-failure-classification and lock-timing decisions are the two highest-value scenarios to add there) plus a manual walkthrough description in the plan for anything integration tests can't exercise (e.g. actually simulating a mid-run session interruption for the lock-early-release fix).
- No TDD candidates beyond the two unit-testable helpers above (`check_liveness`, `_config`/`_review_common` caller alignment) — the rest of the fixes are SKILL.md instruction-sequencing changes with no standalone function to unit test in isolation.

## Q&A log

- **Q:** How should Step 5's push failure be classified when it's a plain non-fast-forward rejection (neither branch-protection nor already-up-to-date)? **A:** [auto-pick] Add a third branch: fetch + rebase the parent's local squash commit onto the new `origin/<parent_branch>` tip and retry the push once; fall through to existing rollback on a real conflict or a second failure. **Why:** closes the #904/#862 race instead of narrowing it; matches standing guidance to fix git races via rebase+retry rather than halting.
- **Q:** Where should the merge lock be released to prevent the #863 stale-lock leak? **A:** [auto-pick] Move release to immediately after Step 5's squash+push succeeds, before Steps 5.5/6/7. **Why:** those later steps don't touch the parent worktree; releasing early eliminates the crash/interrupt exposure window structurally, since SKILL.md instructions can't guarantee true try/finally semantics.
- **Q:** How should `check_liveness` stop misclassifying a local-only parent branch as dead (#879)? **A:** [auto-pick] Widen it to return `True` when either the origin ref exists or a local `refs/heads/<branch>` ref exists. **Why:** minimal, doesn't disturb the existing alive/dead-boolean call sites, and doesn't conflict with the function's existing (unrelated) warning about stale remote-tracking refs.
- **Q:** How should the two divergent `_config.load_config` call conventions be reconciled (#900)? **A:** [auto-pick] Align `_review_common.py`'s wrapper to pass `git_root` (matching the 3-of-4-entry-points-already-do-this convention), plus add a stderr warning when a stub file carries unexpected keys. **Why:** fixes the actual divergence, not just its documentation; the warning gives cheap early detection of the exact misuse pattern that surfaced the bug.
- **Q:** What should `mill-merge-in` Step 4's verify cwd default be when a batch's `verify:` field is a plain string (#899)? **A:** [auto-pick] Change the `cwd is None` resolution from `hub_root` to `git_root`. **Why:** confirmed via `_run_verify_gate`/`_run_verify_gates` source that mill-go's own live batch dispatch always resolves the plain-string form to `git_root`, not `hub_root` — merge-in's replay must match what was actually exercised originally.
- **Q:** How should `mill-merge-in` Step 5 find a nested-hub codeguide when running from git_root (#880)? **A:** [auto-pick] Pin the `codeguide-update` invocation's cwd/root explicitly to `hub_root` at the Step 5 call site, rather than modifying `resolve.py`'s shared upward-only walk algorithm. **Why:** minimal, contained blast radius — `resolve.py` is depended on by other flat-layout call sites that shouldn't change behavior.
