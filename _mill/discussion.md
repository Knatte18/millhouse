# Discussion: Batch review/verify pipeline doesn't account for cross-batch state changes

```yaml
task: Batch review/verify pipeline doesn't account for cross-batch state changes
slug: mill-review-verify-pipeline-state-gaps
status: discussing
parent: hanf/linux-port-more
```

## Problem

Three related bugs (GitHub #686, #689, #698, all consolidated into this task) share one root cause: mill's review/verify machinery treats a plan's batch DAG as if the file layout and implementation state were static, when in reality batches can move files, delete files, and be at different stages of completion relative to each other.

- **#686** — `millpy-review-code.py`'s holistic *prepare* stage hard-ERRORs (`ReviewError` in `resolve_ref_paths`) when a batch card's `Context:` ref points at a path that a *later* batch relocates via `Moves:`. The path was valid when the plan was written and when the referencing batch ran, but by the time holistic review runs, the file has moved away.
- **#689** — `millpy-fix.py`'s holistic *finalize* verify-replay (`_plan_dag.iter_batch_verifies`, joined via `_resolve_holistic_verify`) replays every batch's `verify:` command in DAG order against the *final* tree, with no notion that a later "revert" batch legitimately deleted an earlier batch's build target. This produces a false-positive `stuck_type: verify` that blocks handoff even though the authoritative final-state checks pass.
- **#698** — `mill-merge-in`'s Step 4 calls the same `iter_batch_verifies()` with no phase/implementation gate. If merge-in runs before any batch has been implemented (e.g. an operator-directed sync during the PLAN phase), it replays verify commands against packages/paths that don't exist *yet*, dispatches a vacuous verify-fix sub-agent, and risks a stuck→rollback of an otherwise-good merge.

All three were filed with a documented manual workaround (retarget the stale `Context:` refs; rewrite batch `verify:` commands to the final-state command; run only the verifies whose targets exist) — the point of this task is to make the pipeline handle these cases automatically.

## Scope

**In:**
- `_review_code.py`'s holistic prepare: stop discarding the `sources` half of `compute_moves_union()`'s return value; merge it into `deletes_union` before calling `resolve_ref_paths`, per that helper's own docstring contract.
- `_plan_dag.iter_batch_verifies()`: skip (not fail) a batch's `verify:` command when a strictly-later batch (in topo order) declares — via its own `Deletes:` or `Moves:` — the exact path(s) referenced in that command's tokens.
- `_plan_dag.iter_batch_verifies()`: add an optional `status_path` parameter; when given, filter out any batch whose `_status.read_batches()` state is not `"approved"`.
- Wire the new `status_path` parameter through both existing callers: `millpy-fix.py` (holistic prepare + finalize) and `mill-merge-in` Step 4.
- Visible, counted skip reporting for both new skip reasons, mirroring the existing "allowlisted as known-broken" skip pattern in `mill-merge-in`'s report line, plus an equivalent stderr note from `millpy-fix.py`.
- Unit test coverage for all of the above in `test-review-code-flow.py`, `test-plan-dag.py`, and `test-millpy-fix.py`.

**Out:**
- No new plan-authoring schema fields (e.g. no `verify-superseded-by:` or `verify-target:` frontmatter key). The fix reuses the existing `Deletes:`/`Moves:` bullets that plans already author for other purposes.
- No filesystem-existence heuristic for verify-target detection (rejected — see Decision 2 below; a command that creates its own output path before using it, e.g. `mkdir -p out && go build -o out/app ./cmd/app`, would be falsely flagged as referencing a missing path).
- No `mill-plan`-time validation/warning for revert-style plans (option (c) from #689) — this task is a runtime fix, not an authoring-time lint. A future task may add the lint; not needed once the runtime skip is correct.
- No change to per-batch verify during live `mill-go` dispatch (`millpy-implement.py` / per-batch `parse_verify_field` calls) — `iter_batch_verifies` is only ever used for cross-batch *replay* (holistic prepare/finalize, merge-in), never for a single batch's own live verify, so nothing there needs to change.
- No change to `_review_plan.py`'s two `compute_moves_union()` call sites (lines 354, 668) — those already correctly keep both halves (`moves_sources_union` used directly, not discarded); only `_review_code.py`'s holistic prepare has the bug.
- No handling of flag=value path tokens (e.g. `--dir=foo/bar`) in the verify-command tokenizer — out of scope; see Technical context for the exact tokenization rule and its known limitation.

## Decisions

### 1. `_review_code.py` — stop discarding `compute_moves_union()`'s sources half

- Decision: In `_collect_source_files` (or equivalent) in `plugins/mill/scripts/_review_code.py` around line 279, change `_, moves_targets_union = compute_moves_union(plan_dir)` to capture both halves, and pass `deletes_union | moves_sources_union` (not bare `deletes_union`) into the `resolve_ref_paths(...)` call at line ~280-283.
- Rationale: `compute_moves_union()`'s own docstring (`_review_common.py:714-736`) states "the source set mirrors the semantics of `compute_deletes_union`... Callers that need to suppress non-existent-path errors for [paths the task relocates] should treat [sources] the same way they treat `deletes_union`." `_review_plan.py` already follows this contract correctly at both of its call sites (lines 354 and 668, feeding `moves_sources_union` into its own ref-suppression logic). `_review_code.py`'s holistic prepare is the one place in the codebase that discards the tuple's first element with `_,`.
- Rejected: A new third suppression kwarg on `resolve_ref_paths` (adds API surface for something that already fits the existing `deletes_union` semantics exactly); resolving sources via `resolve_existing_paths` and appending unconditionally to `source_files` (wrong semantics — a moved-away source should be *suppressed*, i.e. not hard-fail, not necessarily *included* in the reviewer's file list, since it no longer exists at its old path by the time holistic review runs).

### 2. Cross-batch verify-target detection uses plan metadata, not disk state

- Decision: A batch's `verify:` command is considered to reference a "removed-later" target when: (a) the command is tokenized with `shlex.split(command)`; (b) a token is a *path candidate* iff it does not start with `-` (excludes flags), contains `/`, and does not contain any of `*`, `?`, or the literal substring `...` (excludes shell globs and Go's `./...` wildcard); (c) each path-candidate token is normalized (strip one leading `./`, strip any trailing `/`); (d) the normalized token exact-matches a normalized entry in the *per-batch* `Deletes:` or `Moves:`-source set of some batch that is strictly later than the verifying batch in `topo_order()`.
- Rationale: Reuses the exact same trusted parsing helpers Decision 1 already wires up correctly (`parse_deletes`, `parse_moves` — called per-batch-file, same as `compute_deletes_union`/`compute_moves_union` already do internally) — no new parsing surface, no new plan-authoring convention. Matching against plan-declared removals (author intent) rather than live disk existence avoids a real false-positive: a verify command that creates its own output path before consuming it (`mkdir -p out && go build -o out/app ./cmd/app`) would see `out/app` as "missing" under a naive existence check before the `mkdir` runs, and get wrongly skipped even though the verify is perfectly valid.
- Rejected: Blind filesystem-existence check on tokenized args (the false-positive above); a new plan-schema field requiring authors to hand-declare which batch supersedes which (adds authoring burden and a new validation surface for a case the existing `Deletes:`/`Moves:` bullets already describe).
- Note: "strictly later in topo order" is essential — it must not suppress a batch's own legitimately-broken verify just because that same batch also deletes something, and must not suppress an *earlier* batch's verify based on something declared even earlier (nonsensical — nothing has been removed yet from that batch's point of view).

### 3. The filter lives inside `_plan_dag.iter_batch_verifies()` itself

- Decision: Decision 2's filtering logic is applied unconditionally inside `_plan_dag.iter_batch_verifies()` (`plugins/mill/scripts/_plan_dag.py:359`), not as an opt-in wrapper. Every existing caller gets the corrected behavior automatically with no call-site changes beyond Decision 4's new kwarg.
- Rationale: `iter_batch_verifies()`'s own docstring already frames its contract from the caller's perspective as "replay exactly the checks that ran during implementation." A verify against a target a later batch legitimately reverted was never really part of that surviving contract — no current or plausible future caller wants the unfiltered, technically-incorrect list. Both current callers (`millpy-fix.py` holistic prepare/finalize, `mill-merge-in` Step 4) need this fix; a wrapper would just be a second name for "the function everyone should actually call."
- Rejected: A separate wrapper function (`iter_batch_verifies_filtered` or similar) that callers opt into — pure indirection with no caller that would ever want the old behavior.

### 4. `#698` gate: per-batch state filter via a new `status_path` kwarg

- Decision: Add `status_path: Path | None = None` as a keyword-only parameter to `iter_batch_verifies(plan_dir, hub_root, git_root, *, status_path=None)`. When provided, call `_status.read_batches(status_path)` once, build a `{name: state}` lookup, and skip any batch whose state is not `"approved"` (this naturally covers "no `## Batches` section yet" — `read_batches` returns `[]`, so every batch is filtered out — and the partial-completion case where merge-in runs mid-`mill-go`). When `status_path` is `None` (not passed), behavior is unchanged from today (no state filtering) — this preserves the parameter as strictly additive.
- Rationale: `_status.read_batches()` / the `state` field (`pending`, `running`, `reviewing`, `fixing`, `approved`, `blocked` — `_status.py:540-547`) is the existing, precise source of truth for per-batch implementation progress, already read elsewhere in `millpy-fix.py`'s per-batch dispatch. It degrades correctly to "skip everything" pre-`mill-go` without a separate phase check, and handles partial-run merge-in more precisely than a single coarse phase gate could (a phase gate can only say "building has started," not "batch 3 specifically is done").
- Rejected: A coarse top-level `phase:` gate (skip entirely if `phase` < `"building"`) — too coarse for the partial-completion case, and redundant once the per-batch filter exists; doing both (belt-and-suspenders) — unnecessary complexity, the per-batch filter is a strict superset of what the phase gate would catch.
- Call-site wiring: `mill-merge-in` Step 4 passes `status_path` (the actual fix for #698 — resolved the same way Step 4 already resolves `hub_root`/`git_root`, i.e. `status_path = _paths.resolve_task_path(hub_root, "_mill/status.md")`). `millpy-fix.py`'s two `iter_batch_verifies(...)` call sites (holistic prepare-time verify_cmd derivation and holistic finalize, both already have `status_path` in scope as a local variable) also pass it — this is a no-op in practice, since `mill-go`'s own linear DAG execution guarantees every batch is already `"approved"` by the time holistic fix runs, but keeps the contract consistent rather than leaving one caller silently relying on an invariant the function itself doesn't enforce.

### 5. Skip visibility: extend the existing `skipped` counter pattern

- Decision: Both new skip reasons (Decision 2's "target removed by a later batch" and Decision 4's "batch not yet approved") must be visibly logged and counted, using the same shape as `mill-merge-in` Step 4's existing "allowlisted as known-broken" skip (`plugins/mill/skills/mill-merge-in/SKILL.md` Step 4). `iter_batch_verifies()` itself doesn't print (it's a pure data function returning triples) — so each *skip reason* becomes visible in the return path: batches filtered out by Decision 2/4 are simply absent from the returned list (same contract as today's `verify: null` skip), and the two callers are responsible for surfacing *why* triples are missing.
  - `mill-merge-in` Step 4: extend the existing `skipped` counter to also count "not yet approved" and "target removed" batches, distinguished in the final report line (e.g. `Verify: <ran> batch tests ran, <N> skipped (allowlisted), <M> skipped (batch not approved), <K> skipped (target removed by later batch).` — only emit the clauses whose count is nonzero, mirroring the existing conditional-clause pattern).
  - `millpy-fix.py` holistic prepare/finalize: print an equivalent `[millpy-fix] skipped <batch_name>: <reason>` line to stderr for each batch `iter_batch_verifies()` silently dropped, so the fixer session's own log carries the same auditability merge-in's report line has. (Compute this by diffing "which batches from `extract_batch_index()`/`topo_order()` had a non-null `verify:` in their own frontmatter" against "which batch names came back from `iter_batch_verifies()`" — the difference is exactly the skipped set, tagged with the specific reason from re-checking Decision 2/4's conditions.)
- Rationale: matches this project's existing convention (the allowlist skip already does this) and general good practice — a verify that silently never ran must never look identical to a verify that ran and passed.
- Rejected: Silent skip with no counter/log change — indistinguishable from "verified" in the report, which is exactly the kind of gap this task exists to close.

## Technical context

- `plugins/mill/scripts/_review_common.py`
  - `compute_moves_union(plan_dir) -> tuple[set[str], set[str]]` (line 714) — returns `(sources, targets)`; iterates `plan_dir.glob("??-*.md")` (skipping `00-overview.md`), calls `parse_moves(batch_path)` per file. Already correct; the bug is entirely in how `_review_code.py` consumes its return value.
  - `compute_deletes_union(plan_dir) -> set[str]` (line 666) and `compute_creates_union(plan_dir) -> set[str]` (line 618) — same per-batch-file iteration pattern; use as the model for Decision 2's per-batch (not unioned) variant.
  - `resolve_ref_paths(...)` (line 755) — the `ReviewError` in #686 originates here; `creates_union`/`deletes_union` kwargs suppress the hard-fail (docstring at lines 781-800 spells out the exact suppression contract).
- `plugins/mill/scripts/_review_code.py` — line 279: `_, moves_targets_union = compute_moves_union(plan_dir)`; lines 280-283: the `resolve_ref_paths(...)` call whose `deletes_union=deletes_union` kwarg needs to become `deletes_union=deletes_union | moves_sources_union`.
- `plugins/mill/scripts/_review_plan.py` — lines 354 and 668 are the reference implementation for "keep both halves of `compute_moves_union()`'s return value"; lines 394, 490, 780-781, 842 show how `moves_sources_union` is consumed downstream (`resolve_existing_paths`, silently skipping a target not yet on disk). Not identical to what `_review_code.py` needs (that's suppression via `deletes_union`, not inclusion via `resolve_existing_paths`), but confirms the sources half is meant to be used, not discarded.
- `plugins/mill/scripts/_plan_dag.py`
  - `iter_batch_verifies(plan_dir, hub_root, git_root) -> list[tuple[str, str, Path | None]]` (line 359) — the function both Decision 2 and Decision 4 modify. Currently: `extract_batch_index()` → `topo_order()` → per-batch `_read_batch_frontmatter()` + `parse_verify_field()`, collecting `(name, command, cwd)` for every batch with a non-null `verify:`.
  - `topo_order(batches) -> list[str]` (line 419) — already used by `iter_batch_verifies`; Decision 2 needs each batch's *index* in this order (not just membership) to determine "strictly later."
  - `parse_verify_field(frontmatter, hub_root, git_root)` (line 285) — unchanged; still the single normalizer for the string-vs-mapping `verify:` shapes.
- `plugins/mill/scripts/millpy-fix.py`
  - Line 332 (`--stage finalize`, `args.scope == "holistic"`) and line 452 (non-finalize holistic branch) — both call `_plan_dag.iter_batch_verifies(plan_base, project_root, git_root)`; both need `status_path=status_path` added (already a local variable in this file — see `status_path` usage elsewhere in the same function).
  - `_resolve_holistic_verify(batch_verifies)` (line 67) — joins the (now-filtered) triples with `" && "`; unchanged, but now sees a filtered list so no longer joins in a dead target's command.
- `plugins/mill/skills/mill-merge-in/SKILL.md` Step 4 (lines 91-103) — the prose orchestration for the CLI-less merge-in skill; must be updated to (a) resolve and pass `status_path` into the `iter_batch_verifies(...)` call, and (b) extend the `skipped`/`ran` counter reporting per Decision 5. Step 4 also already has an existing `skip_list`/`skipped` allowlist mechanism (lines 95, 99) to mirror.
- `plugins/mill/scripts/_status.py`
  - `read_batches(status_path) -> list[dict]` (line 667) and `_BATCH_STATES = {"pending", "running", "reviewing", "fixing", "approved", "blocked"}` (line 540) — the state vocabulary Decision 4 filters on. `init_batches()` (line 945) is called by `mill-go` at its own startup — before that, `## Batches` is entirely absent from `status.md` and `read_batches()` returns `[]`.
- Batch file `Deletes:`/`Moves:` bullets are parsed by `parse_deletes` / `parse_moves` (referenced from `_review_common.py`; same module, used internally by `compute_deletes_union`/`compute_moves_union`) — Decision 2 needs to call these two parsers directly, per batch file, to get *per-batch* (not unioned) sets.

## Testing

- **`test-review-code-flow.py`** (Decision 1, TDD candidate): a plan fixture with batch A whose `Context:` references a path, and batch B (later in the plan) that `Moves:` that exact path elsewhere. Assert holistic prepare no longer raises `ReviewError` and the resolved source-file list behaves as it does for an equivalent `Deletes:`-suppressed path today (i.e. the moved-away path is silently omitted, not hard-failed).
- **`test-plan-dag.py`** (Decisions 2 & 3, TDD candidates):
  - Fixture mirroring #689 exactly: batch 1-3 `verify: go build ./tools/x/`, batch 4 (depends on 1-3) `Deletes: tools/x/` and `verify: go build ./...`. Assert `iter_batch_verifies()` returns only batch 4's triple; batches 1-3 are silently absent (not raising, not present).
  - Negative case: a batch's own `verify:` references a path *it itself* deletes in the same batch card — assert this batch's verify is **not** suppressed (only strictly-later removals count; same-batch or earlier-batch declarations must not affect it).
  - Tokenizer edge cases: `go build ./...` (never suppressed — `...` excluded); `go test ./pkg/...` (same); a flag-form token `--dir=foo/bar` (not treated as a path candidate — starts with `-`); confirm these never spuriously match even when `foo/bar` happens to appear in some other batch's `Deletes:`.
  - `status_path` kwarg (Decision 4): a fixture `status.md` with batches in mixed states (`approved`, `pending`, absent-from-list); assert only `"approved"` batches' triples are returned when `status_path` is passed, and that omitting `status_path` entirely reproduces today's unfiltered behavior byte-for-byte (regression guard for the "strictly additive" claim in Decision 4).
  - No-`## Batches`-section fixture (status.md with no Batches heading at all, i.e. pre-`mill-go`): assert `iter_batch_verifies(..., status_path=status_path)` returns `[]`.
- **`test-millpy-fix.py`** (Decision 4 wiring + Decision 5 stderr logging): assert both holistic call sites now pass `status_path`; assert a skipped batch (either reason) produces the `[millpy-fix] skipped <batch_name>: <reason>` stderr line.
- **`mill-merge-in` skill-level coverage**: this skill has no Python CLI of its own for Step 4's orchestration (it's prose-driven), so its `status_path` wiring and `skipped`/`ran` report-line format are covered by the underlying `iter_batch_verifies()` unit tests above plus a manual verification pass during implementation (run `mill-merge-in` against a fixture task pre- and post-`mill-go`, per the skill's own testing conventions) — not a new automated test file. `test-merge-in-subagent.py` covers the sub-agent dispatch paths (conflicts, verify-fix), which are unaffected by this task; no changes needed there.
- No TDD candidate needed for Decision 1's `_review_plan.py` call sites — those are explicitly out of scope (already correct).

## Q&A log

- **Q:** How should `_review_code.py`'s holistic prepare stop discarding `compute_moves_union()`'s sources half? **A:** [auto-pick] Merge `moves_sources_union` into `deletes_union` before calling `resolve_ref_paths`. **Why:** Matches the helper's own docstring contract and the pattern `_review_plan.py` already uses correctly; smallest diff, zero new API surface.
- **Q:** How should a cross-batch verify-target that a later batch reverted be detected? **A:** [auto-pick] Plan-metadata driven — match normalized verify-command tokens against per-batch `Deletes:`/`Moves:`-source declarations from strictly-later batches. **Why:** Avoids the disk-existence heuristic's false positive on commands that create their own output path before using it (e.g. `mkdir -p out && go build -o out/app ./cmd/app`); reuses the same trusted parsers Decision 1 already wires up correctly.
- **Q:** Where should this filtering logic live? **A:** [auto-pick] Inside `_plan_dag.iter_batch_verifies()` itself, applied unconditionally. **Why:** The function's own docstring already frames its contract as "what actually ran/survives"; no caller wants the unfiltered list, so a wrapper would be pure indirection.
- **Q:** How should `mill-merge-in` avoid replaying unimplemented batches' verifies? **A:** [auto-pick] Per-batch state filter via a new optional `status_path` kwarg on `iter_batch_verifies`, skipping any batch not in state `"approved"`. **Why:** More precise than a coarse phase gate — handles partial-completion mid-`mill-go` merge-in correctly, and degrades to "skip everything" pre-`mill-go` for free since `read_batches()` returns `[]` when no `## Batches` section exists yet.
- **Q:** How should the new skips be surfaced? **A:** [auto-pick] Extend the existing `skipped` counter/report-line pattern in `mill-merge-in` Step 4, plus an equivalent stderr note in `millpy-fix.py`. **Why:** Matches the existing allowlist-skip precedent; a verify that never ran must never look identical to one that ran and passed.
