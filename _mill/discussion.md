# Discussion: Plan review verdict correctness: unverified platform claims and missing nit_count in subprocess dispatch

```yaml
task: Plan review verdict correctness: unverified platform claims and missing nit_count in subprocess dispatch
slug: mill-review-verdict-and-counting-gaps
status: discussing
parent: main
```

## Problem

Plan-review round verdicts and finding counts are unreliable in two independent ways. First, reviewers can approve a plan based on a factual claim about Claude Code's own platform/harness behavior (agent auto-discovery, plugin manifest semantics, etc.) without ever checking that claim against the actual manifest/docs — one such approval (#714) let a plan ship six undispatchable agent definitions because `plugin.json`'s explicit `agents` array silently disables directory-based auto-discovery, and the reviewer never opened that file. Second, the finalize envelope's finding counts are structurally incomplete: `_review_plan.py`'s subprocess/psmux `run()` path computes `blocking_count` at every terminal point but never computes `nit_count` anywhere (#709), and (per #717) the holistic reviewer brief omits a plan-format rule it's expected to know, causing it to raise a false NIT that would itself fail the plan validator. Together these mean an APPROVE verdict from plan review cannot be trusted at face value — `mill-plan` branches on `blocking_count` from the JSON envelope to decide round outcome, and separately re-reads the review file directly for `[NIT]`-prefixed findings (it does not branch on the numeric `nit_count` field itself today). Fixing `nit_count` in the subprocess/psmux path is therefore about restoring schema consistency with the already-correct Agent-mode and code-review `nit_count` flow — not an active mill-plan mis-read — so the envelope is trustworthy for any current or future consumer that does read the field, and so the finding class (unfixed duplication bug) doesn't recur elsewhere.

## Scope

**In:**
- Fix `_review_plan.py`'s `run()` (subprocess/psmux dispatch) to compute and aggregate `nit_count` correctly, matching the Agent-mode `finalize()`/`finalize_scope()` path (#709).
- Add a regression test proving `[MEDIUM]`-only (or any off-vocabulary severity) findings fold into `blocking_count` on both dispatch paths — confirming #720's fold-in behavior (already shipped in `cf075f93`) holds and stays covered.
- Add the "All Files Touched excludes Deletes-only paths and Move sources" rule to `review-plan-holistic.md` and `review-plan-batch.md` (#717).
- Add a platform-behavior-claim verification criterion to `review-plan-holistic.md` and `review-plan-batch.md`, plus a `_plan_validate.py` rule requiring `plugin.json` in a batch's `Context:` or `Edits:` when that batch touches agent-definition files under `plugins/mill/agents/`, plus the corresponding `mill-plan/SKILL.md` Step 1.5 fix-table row (#714).
- Unit test coverage for all of the above in `plugins/mill/unit_tests/`.

**Out:**
- `_review_discussion.py` and `_review_code.py` — confirmed by exploration to already delegate to `finalize_scope()` in their `run()` paths (no duplicated inline counting logic), so they don't share #709's bug. No changes needed there.
- Redesigning the severity vocabulary itself (adding a formal MEDIUM tier, an `other_count` field, etc.) — #720 is being treated as already resolved by the existing fold-into-blocking behavior; not reopening that design.
- Any change to how `mill-plan`/`mill-go` branch on `blocking_count`/`nit_count` (SKILL.md logic) — this task fixes the counts feeding into that logic, not the branching logic itself.
- General plan-validator (`_plan_validate.py`) rule authoring beyond the one new `plugin.json`-in-Context rule for #714.
- The resume-round disk-scan block's pre-existing dead-code discard (`_disk_reviews` built but never merged into `reviews`) — a separate bug, unrelated to #709's counting logic, discovered during review verification. Not fixed here; left for a future task if it warrants one.

## Decisions

### nit-count-fix-mechanism

- Decision: In `_review_plan.py`'s `run()`, refactor the 4 terminal-write call sites that write a NEW review file on a **successful** verdict parse AND whose result reaches the returned `ReviewResult` — per-batch in `_review_one_batch()`; holistic NEED_CONTEXT retry-success; holistic NEED_CONTEXT no-resolve; holistic normal — to call `finalize_scope()` from `_review_common.py` instead of duplicating `parse_verdict` + `write_review_file` + `parse_blocking_count` + `count_unrecognized_severity_findings` inline. Use `finalize_scope()`'s returned `verdict`, `blocking_count`, `nit_count`, `file` for the review entry dict.
- A 5th write-site also reaches `ReviewResult`: the holistic block's outer `except ReviewError` catch (lines 1014-1024), reached when `parse_verdict` itself fails. It is deliberately **not** refactored to `finalize_scope()` — doing so would just re-parse the same already-failed `raw` text and re-raise the identical error — but its literal dict gets `"nit_count": 0` added for schema parity with `finalize()`'s equivalent `except ReviewError` catch, which already sets both counters explicitly. See the dedicated site entry in Technical context below for the full rationale.
- The resume-round disk-scan block (a 5th `blocking_count` computation site, structurally distinct from the 4 above) is **out of scope for this fix**: it builds a local `_disk_reviews` list but never merges it into `reviews` — no `reviews.append`/`reviews.extend(_disk_reviews)` exists anywhere in `run()` (confirmed by grep; `test-review-plan-flow.py` Test 17's own comment records "reviews has 1 entry (holistic only, bug C fix #184)" after resume). `_disk_reviews`'s computed values, including its `blocking_count`, are already discarded today — only `len(_disk_reviews)` reaches a log line. Adding a `nit_count` line to a block whose output never reaches `ReviewResult` would be a no-op patch. This block's own dead-code discard is a separate, pre-existing bug — not part of #709's scope (see Scope > Out) — and is left untouched.
- Also update the final `ReviewResult` construction (end of `run()`) to sum `nit_count` across `reviews[]` the same way `aggregate_blocking` already sums `blocking_count`, and pass it into the returned `ReviewResult(..., nit_count=aggregate_nit)`.
- **6th site — skip-approved carryforward.** `_scan_approved_batches()` (`_review_plan.py:70-119`) builds a carryforward dict for each already-APPROVE'd batch it finds on disk, with `"blocking_count": 0` hardcoded and no `nit_count` key at all. These carryforward dicts merge into `reviews[]` (lines 799-801) exactly like a freshly-reviewed batch's entry does, so once `aggregate_nit` sums `r.get("nit_count", 0)` across `reviews[]`, a carried-forward batch that was APPROVE'd with real `[NIT]` findings silently contributes 0 to the total — and the same is already true of `blocking_count` today: `parse_verdict` returning `"APPROVE"` is never cross-validated against the review's actual finding counts anywhere in this codebase (that's the entire premise of the `count_unrecognized_severity_findings` fold-in elsewhere), so a batch could in principle carry an unrecognized-severity finding and still read `verdict: APPROVE`, and this site would silently reset that to 0 too. Fix this site fully, mirroring every other site's math: `blocking_count = parse_blocking_count(raw, severity="BLOCKING") + count_unrecognized_severity_findings(raw, blocking_severity="BLOCKING", nit_severity="NIT")` and `nit_count = parse_blocking_count(raw, severity="NIT")`, both alongside the existing `parse_verdict(raw)` call, both included in the carryforward dict.
- Rationale: eliminates the exact code duplication that caused #709 for the 4 sites whose output is actually observable (the write-sites become single-source-of-truth via the shared helper, matching how `_review_discussion.py`/`_review_code.py` already work); the resume-disk-scan site's discarded output means any change there is invisible regardless of correctness, so it's excluded rather than patched for appearance's sake.
- Rejected: patching all 5 sites narrowly without refactoring — leaves the duplication (and its bug-recurrence risk) in place for the 4 live write-sites. Also rejected: patching the resume-disk-scan site's `nit_count` alongside its `blocking_count` — would silently suggest the site's output matters when it provably doesn't reach `ReviewResult`.

### medium-severity-regression-test

- Decision: Treat #720 as already resolved by `cf075f93`'s `count_unrecognized_severity_findings` fold-in (confirmed present and exercised in both `finalize_scope()` and `run()`'s existing `blocking_count` computations on this branch's HEAD). Add a regression test asserting a `[MEDIUM]`-only review folds into `blocking_count` (not silently dropped) for both the per-batch and holistic paths of `run()`, and for `finalize()`/`finalize_scope()`. No production code change for #720 itself.
- Rationale: the loomyard repro predates the fix reaching that repo's plugin cache; millhouse's own code already does the right thing. A regression test is cheap insurance without reopening a settled design decision (fold vs. separate counter).
- Rejected: adding a distinct `other_count`/`medium_count` field — would be new API surface for a case the existing fold-in already makes safe (visible, forces attention via `blocking_count`).

### all-files-touched-brief-fix

- Decision: Add one sentence to both `review-plan-holistic.md` and `review-plan-batch.md` (near the existing criteria list) stating: the overview's `## All Files Touched` section is the union of `Edits:`/`Creates:`/Move-target paths across all batches, with `Deletes:` tokens and Move-source paths excluded — a Deletes-only or Move-source-only path missing from that list is correct, not a finding.
- Rationale: both templates bulk the full overview (including `## All Files Touched`) into the reviewer's prompt, so both are equally exposed to raising the same false NIT that #717 reported; `mill-plan/SKILL.md` and `_plan_validate.py` check 8 already encode this exact rule for the plan-writing side — the reviewer brief was the only place missing it.
- Rejected: holistic-only fix (matches the issue's literal title but leaves batch reviewers exposed to the identical false-positive).

### platform-claim-verification

- Decision: Add a criteria bullet to `review-plan-holistic.md` and `review-plan-batch.md`: when a plan or discussion claim describes Claude Code's own platform/harness behavior and a manifest or doc file that could confirm or refute the claim is present in the reviewer's context (bulked or Read-able), the reviewer must check that file before accepting the claim as given — BLOCKING if the claim is checkable from available context, unverified, and the plan's correctness depends on it. Tool-use-mode reviewers may additionally Read `plugin.json`/platform docs directly even when not bulked. The bullet does NOT claim uniform coverage of every possible platform-behavior category (tool-call parameter shapes, subagent resolution, etc. have no corresponding local manifest to check today) — it scopes "must verify" strictly to claims a manifest actually present in context can answer. Concretely, that means: add a new `_plan_validate.py` check requiring any batch whose `Creates:`/`Edits:`/`Deletes:` introduces, modifies, or removes a file under `plugins/mill/agents/` to have `plugin.json` present in that batch's `Context:` **or** `Edits:` (not `Context:` alone) — the primary expected case is a batch that *registers* the new agent, which edits `plugin.json`'s `agents` array directly, and existing convention (`review-plan-batch.md:40`: "Edits: files are implicitly read — do not repeat them in Context:") never duplicates an `Edits:` file into `Context:`; requiring `Context:` only would false-positive on that primary case. `Deletes:` is included alongside `Creates:`/`Edits:` (not deferred like the commands/hooks categories below) because the underlying path-matching mechanism is identical and already available — `compute_deletes_union` is already used elsewhere in `_review_plan.py` — so excluding it would leave the symmetric failure mode (removing an agent file without updating `plugin.json`'s array) silently uncovered for no implementation-cost reason. Scoped to agent-definition files only — `plugin.json` currently declares only an `agents` array (no `commands`/`hooks` key), and no `commands/`/`hooks/` directory exists anywhere in this plugin (skills, under `plugins/mill/skills/`, are the slash-command mechanism here and are not registered in `plugin.json` the way agents are), so "slash commands" and "hook wiring" have no source-grounded path pattern to check against today. Other platform-claim categories remain uncovered by concrete enforcement until a corresponding manifest/validator rule is added in a future task, once such a category actually exists in this plugin.
- The new check's mechanical fix is explicitly **in scope** for this task (not general validator-rule authoring, and not a halt case): add a new row to `mill-plan/SKILL.md`'s Step 1.5 fix table (the mapping every one of the ~20 existing `_plan_validate.py` checks already has, keyed by `check` id, e.g. `all-files-touched-mismatch`), named e.g. `plugin-manifest-context-missing`, with mechanical fix "Add `plugin.json` to the offending batch's `Context:` list (unless the batch's own `Edits:` already includes it, in which case the check should not have fired)." Without this row, `mill-plan`'s autonomous Step 1.5 fix loop has undefined behavior the first time this check's `errors` entry appears, since that loop's `_plan_validate.py`-failure branch requires "using the fix table in Step 1.5 below as the source of truth for all fix semantics" for every error dict it receives.
- Rationale: matches #714's own "Suggested fix direction"; the new `_plan_validate.py` rule specifically closes the gap for bulk-mode reviewers, who cannot fetch files on their own — without it, the criteria bullet alone is a no-op for that dispatch mode, which is exactly the mode #714 occurred in.
- Rejected: a new orchestrator-side mandatory `claude-code-guide` verification dispatch — new orchestration surface for a narrow failure class; the reviewer-prompt + Context-completeness fix is cheaper and addresses the same root cause (the reviewer/plan never had the manifest in view).

## Technical context

- `_review_plan.py`'s `run()` (`plugins/mill/scripts/_review_plan.py:613-1037`) is the subprocess/psmux legacy dispatch path; `prepare()`/`finalize()` (same file) is the Agent-mode path and already delegates to `finalize_scope()` correctly.
- 5 inline `blocking_count` computations (4 refactored to `finalize_scope()`, 1 error-catch parity-only), plus 1 carryforward site and 1 excluded dead-code site, by line (current HEAD):
  - `_review_one_batch()` lines 285-291 (per-batch terminal write) → refactor to `finalize_scope()`.
  - Resume-round disk-scan block, lines ~738-749 (reads an existing file, no write) → **no code change** — its computed values (including `blocking_count`) are discarded before reaching `ReviewResult` (see `nit-count-fix-mechanism`); out of scope for this fix.
  - Holistic NEED_CONTEXT retry-success block, lines ~954-972 → refactor to `finalize_scope()`.
  - Holistic NEED_CONTEXT no-resolve block, lines ~975-993 → refactor to `finalize_scope()`.
  - Holistic normal block, lines ~995-1013 → refactor to `finalize_scope()`.
  - `_scan_approved_batches()`, lines 70-119 (skip-approved carryforward, not itself in `run()`) → compute both `blocking_count` and `nit_count` properly (`parse_blocking_count` + `count_unrecognized_severity_findings` for `blocking_count`; `parse_blocking_count(severity="NIT")` for `nit_count`) instead of the current hardcoded `"blocking_count": 0` with no `nit_count` key.
  - Holistic outer `except ReviewError` catch, lines 1014-1024 (a `parse_verdict` failure after a raw response was already obtained; writes the file and appends an `"ERROR"`-verdict entry) → **not** refactored to `finalize_scope()` (would just re-raise the same error); add `"nit_count": 0` to its literal dict for schema parity with `finalize()`'s equivalent catch (`_review_plan.py:587-600`), which already sets both counters explicitly. See `nit-count-fix-mechanism` for full rationale.
- Final `ReviewResult` construction at line ~1026-1037: add `aggregate_nit = sum(r.get("nit_count", 0) for r in reviews)` alongside the existing `aggregate_blocking` line, pass `nit_count=aggregate_nit`.
- `finalize_scope()` (`plugins/mill/scripts/_review_common.py:1870-1933`) is the canonical helper: `apply_actual_model_override` → `parse_verdict` → `write_review_file` → `parse_blocking_count(severity=blocking_severity)` + `parse_blocking_count(severity=nit_severity)` → fold `count_unrecognized_severity_findings()` into `blocking_count` only. Signature: `finalize_scope(reviews_dir, review_type, round_n, raw_text, *, scope=None, actual_model=None) -> dict` with keys `scope, verdict, file, blocking_count, nit_count`.
- `count_unrecognized_severity_findings()` (`plugins/mill/scripts/_review_common.py:1697-1752`) already exists (added in `cf075f93`, merged 2026-07-25, already an ancestor of this branch) and is exercised by both `finalize_scope()` and `run()`'s existing `blocking_count` math — the mechanism #720 needs already ships.
- Severity vocabulary is closed to `BLOCKING`/`NIT` by prompt instruction already present in `review-plan-holistic.md:85` and `review-plan-batch.md:90` (also `review-discussion.md:73` for `GAP`/`NOTE`, `review-code-holistic.md:82`, `review-code-batch.md:86`). No template needs a MEDIUM tier added — the existing instruction already forbids it; the fold-in is the defensive backstop for when a reviewer ignores the instruction anyway.
- `## All Files Touched` rule is currently documented only in `plugins/mill/skills/mill-plan/SKILL.md:140` (plan-authoring side) and enforced by `_plan_validate.py`'s check 8 (`_check_all_files_touched_mismatch`, lines 1253-1319) — neither `review-plan-holistic.md` nor `review-plan-batch.md` states it; both bulk the full overview into their prompt (`_review_plan.py`'s `all_bulked`/`bulked` construction includes `overview_path` in both the batch and holistic code paths).
- Existing test file to extend: `plugins/mill/unit_tests/test-review-plan-flow.py`. It never asserts `nit_count` anywhere in the file (confirmed by grep) — this is why #709 shipped unnoticed. Coverage per branch, verified by direct read:
  - Per-batch (line 875, Test 14 "aggregate blocking_count") and the `[MAJOR]`-fold-in regression (line 1691, Test 29) both assert `blocking_count` already — extend these two with a parallel `nit_count` assertion.
  - Holistic NEED_CONTEXT retry-success (Test 7, lines ~549-588) exists but asserts only `verdict` and prompt/session plumbing — it does NOT assert `blocking_count` or `nit_count` today. Needs a **new assertion**, not one added "alongside" pre-existing coverage.
  - Holistic NEED_CONTEXT no-resolve (the branch where `missing_paths` resolves to empty and NEED_CONTEXT propagates untouched) has **no existing test at all** (confirmed by grep for `no.resolve`/`missing_paths` — no hits). Needs a **net-new test case**.
  - Holistic normal (non-NEED_CONTEXT) path has existing `blocking_count` coverage elsewhere in the file (e.g. Test 14) — extend with `nit_count`.
- No existing test file targets `_review_plan.py`'s per-batch/holistic `[MEDIUM]`-fold behavior specifically — new test cases needed for the #720 regression coverage.
- `plugin.json` lives at `plugins/mill/.claude-plugin/plugin.json` (per this repo's `${CLAUDE_PLUGIN_ROOT}` convention) and already declares an explicit `agents` array (confirmed relevant per #714's repro).
- `mill-plan/SKILL.md`'s Step 1.5 fix table (lines ~126-147) is the mechanical-fix mapping every `_plan_validate.py` check must have a row in — confirmed by reading the full table (`non-existent-path` through `batch-index-parse`); rows may legitimately say "halt" for structural errors, but every check id needs an entry either way.

## Testing

- `_review_plan.py::run()` — TDD candidate. Extend `test-review-plan-flow.py`: for each of the 4 `finalize_scope()`-refactored sites, assert `nit_count` (per-entry and in the final aggregate `ReviewResult.nit_count`) matches the actual `[NIT]` heading count in mocked reviewer output. **Every one of these assertions must be backed by a mocked review text containing at least one real `[NIT]` heading — never a reused zero-NIT fixture with a bare `nit_count == 0` assertion appended.** Test 14's existing fixtures (`two_blockings`/`one_blocking`/`APPROVE_TEXT`) and Test 29's (`major_only`/`APPROVE_TEXT`) — verified by direct read — carry zero `[NIT]` headings each; today's unfixed `run()` never passes `nit_count=` into its final `ReviewResult` at all, so the dataclass default `0` applies regardless of input, which means a "parallel" assertion reusing those exact fixtures would read `== 0` and pass identically on both fixed and unfixed code — a vacuous test that exercises nothing. Concretely: augment at least one of Test 14's per-batch mocked texts (and ideally Test 29's) with an added `### [NIT] ...` heading so the assertion is `nit_count == <nonzero>`, actually exercising the cross-entry `aggregate_nit = sum(...)` logic that is #709's actual subject. Holistic NEED_CONTEXT retry-success (Test 7) and holistic NEED_CONTEXT no-resolve (no existing test) each need genuinely new assertions/test cases, not additions to pre-existing `blocking_count` checks that don't exist yet for those two branches, and each new/modified fixture must likewise carry a real `[NIT]` finding rather than asserting a trivial zero. Additionally, extend Test 8 (skip-approved happy path) — the fixture that exercises `_scan_approved_batches()`'s carryforward path — with **two** carried-forward-batch review files: one containing a real `[NIT]` finding (assert aggregate `nit_count` reflects it, not silently 0) and one containing a real `[BLOCKING]` or off-vocabulary finding (assert aggregate `blocking_count` reflects it, not the current hardcoded 0) — the 6th site's fix (per `nit-count-fix-mechanism`) touches both counters, and testing only `nit_count` would leave the `blocking_count` half of that same fix exercising nothing, the identical vacuous-test trap already caught for Test 14/29 above.
- `[MEDIUM]`-fold regression (#720) — the per-batch case (one off-vocabulary finding, zero `[BLOCKING]`/`[NIT]`) is already covered once Test 29's `[MAJOR]`-fold-in gets its `nit_count == 0` assertion added above (the fold-in logic treats any off-vocabulary label identically, so `[MEDIUM]` vs `[MAJOR]` is not a distinct case). The remaining #720 coverage is: (1) a holistic-path equivalent of Test 29 (one `[MEDIUM]`-only holistic review, assert `blocking_count == 1, nit_count == 0`) — genuinely new, no existing holistic-path fold-in test; and (2) an isolated (zero-`[BLOCKING]`/`[NIT]`) `finalize_scope()`-direct case in `test-review-common.py` — this **extends/complements** existing coverage rather than being wholly new ground: `test-review-common.py` (~lines 2321-2345) already has a `finalize_scope()` fold-in test asserting `blocking_count == 2, nit_count == 1` via the same unconditional fold-in path, just mixed with a real `[BLOCKING]`/`[NIT]` rather than isolated to zero of each.
- Template changes (#717, #714 criteria bullets) — extend `test-review-templates.py`'s existing kept/deleted-prose assertion pattern (`test_kept_prose_stays_kept`/`test_deleted_prose_stays_deleted`, which already loop over all five templates including `review-plan-holistic.md`/`review-plan-batch.md` asserting exact phrases land in the raw source) with a new assertion that the All-Files-Touched-exclusion sentence and the platform-claim-verification bullet are present in both edited templates' source — not manual read-through only.
- `_plan_validate.py` new `plugin-manifest-context-missing` rule for `plugin.json` (#714) — TDD candidate, mirrors the existing `test-plan-validate.py` style (fixture overview + batch files, assert the new check's error/no-error cases): batch with `Creates:`/`Edits:` touching `plugins/mill/agents/*.md` and `plugin.json` in neither `Context:` nor `Edits:` → error; same batch with `plugin.json` in `Context:` → no error; same batch with `plugin.json` in `Edits:` instead (the primary expected case — registering a new agent) → no error; batch with `Deletes:` (not `Creates:`/`Edits:`) touching `plugins/mill/agents/*.md` and `plugin.json` missing from Context/Edits → error (symmetric removal case); batch not touching files under `plugins/mill/agents/` in any of `Creates:`/`Edits:`/`Deletes:` → no error regardless of Context/Edits.

## Q&A log

- **Q:** #709 fix mechanism — refactor `run()`'s duplicated inline logic to call `finalize_scope()`, or patch each site narrowly? **A:** [auto-pick] Refactor to call `finalize_scope()` at the 4 live write-sites; leave the resume-round disk-scan site untouched. **Why:** removes the exact duplication that caused #709 for every site whose output actually reaches `ReviewResult`; the resume-scan site's own `_disk_reviews` output is discarded before reaching `ReviewResult` (a separate pre-existing dead-code bug caught during discussion-review round 1 — see `nit-count-fix-mechanism`), so patching its `nit_count` would be a no-op.
- **Q:** [round-1 GAP] Does the resume-round disk-scan "5th site" `nit_count` patch actually reach the returned `ReviewResult`? **A:** [auto-fix] No — `_disk_reviews` is built but never merged into `reviews` anywhere in `run()` (confirmed by grep and by `test-review-plan-flow.py` Test 17's own comment). Dropped the patch for that site; documented the discard as a separate out-of-scope bug in Scope > Out and corrected `nit-count-fix-mechanism` to describe only 4 live sites.
- **Q:** [round-1 GAP] Does `test-review-plan-flow.py` line 1691 actually cover holistic NEED_CONTEXT, and does an "alongside existing assertions" framing hold for that branch and the no-resolve branch? **A:** [auto-fix] No — line 1691 is Test 29 (`[MAJOR]` per-batch fold-in), not holistic NEED_CONTEXT. The real holistic-NEED_CONTEXT-retry-success test (Test 7) never asserts `blocking_count`, and the holistic NEED_CONTEXT no-resolve branch has no existing test at all. Corrected Technical context and Testing to name the right tests and to require net-new assertions/test cases for those two branches rather than additions to nonexistent coverage.
- **Q:** #720 disposition — already resolved by `cf075f93`, or needs new production code? **A:** [auto-pick] Already resolved; add a regression test only. **Why:** the fold-in mechanism is confirmed present and already exercised on this branch's HEAD; the loomyard repro predates that fix reaching its plugin cache.
- **Q:** #717 fix scope — holistic template only, or both holistic and batch templates? **A:** [auto-pick] Both. **Why:** batch reviewers bulk the same overview (including `## All Files Touched`) and are equally exposed to the same false-NIT failure mode.
- **Q:** #714 reviewer instruction — prompt-only criteria bullet, or new mandatory orchestrator-side verification dispatch? **A:** [auto-pick] Prompt-only criteria bullet. **Why:** matches #714's own suggested fix direction; a new orchestration surface is unjustified for this failure class (YAGNI).
- **Q:** #714 bulk-mode reachability — leave bulk-mode reviewers dependent on incidental Context inclusion, or add a `_plan_validate.py` rule requiring `plugin.json` in Context for plugin-mechanic-touching batches? **A:** [auto-pick] Add the validator rule. **Why:** without it, the prompt-only fix is a no-op for bulk-mode reviewers — exactly the dispatch mode #714 occurred in.
- **Q:** Testing approach — extend the existing `test-review-plan-flow.py`, or a new test file? **A:** [auto-pick] Extend the existing file. **Why:** it already asserts `blocking_count` at the exact branches needing `nit_count` coverage; fragmenting coverage across files has no benefit.
- **Q:** [round-2 GAP] Does `_scan_approved_batches()`'s skip-approved carryforward path get a `nit_count`, given its dicts merge into `reviews[]` just like a freshly-reviewed batch? **A:** [auto-fix] No — it hardcodes `blocking_count: 0` with no `nit_count` key at all, so a carried-forward APPROVE'd batch with real `[NIT]` findings would silently contribute 0 to the new `aggregate_nit` sum. Added this as a 6th site to `nit-count-fix-mechanism`: parse `nit_count` alongside the existing `parse_verdict` call in that function. Added a Test 8 extension to Testing to cover it.
- **Q:** [round-2 GAP] Does `test-review-templates.py` already cover these two edited templates, contradicting the Testing section's "no dedicated rendering-assertion tests" claim? **A:** [auto-fix] Yes — `test_kept_prose_stays_kept`/`test_deleted_prose_stays_deleted` already loop over all five templates (including both edited ones) asserting exact phrases present/absent in raw source. Corrected Testing to require extending that file's assertions rather than relying on manual read-through.
- **Q:** [round-2 GAP] Does `plugin.json` (or this plugin) have a concrete path pattern for "slash commands" or "hook wiring," the other two categories the `platform-claim-verification` validator rule named? **A:** [auto-fix] No — `plugin.json` declares only an `agents` array, and no `commands/`/`hooks/` directory exists anywhere in the plugin. Narrowed the validator rule's scope to agent-definition files only (the one concrete, testable case); deferred the other two categories to a future task once they actually exist as a plugin mechanic.
- **Q:** [round-2 GAP] Does #720's proposed "new" per-batch `[MEDIUM]`-fold test duplicate #709's Test 29 extension? **A:** [auto-fix] Yes — both describe the identical scenario (one off-vocabulary-severity finding, zero BLOCKING/NIT, `blocking_count == 1, nit_count == 0`); the fold-in logic treats `[MEDIUM]` and `[MAJOR]` identically. Rescoped #720's genuinely new Testing work to the holistic path and an isolated `finalize_scope()` case only, removing the duplicate per-batch test.
- **Q:** [round-3 GAP] Does the 6th-site (`_scan_approved_batches`) fix leave `blocking_count` hardcoded at 0 while fixing only `nit_count`? **A:** [auto-fix] Yes — `parse_verdict` returning `"APPROVE"` is never cross-validated against actual finding counts anywhere in this codebase (that's the whole premise of the unrecognized-severity fold-in elsewhere), so a carried-forward APPROVE'd batch could in principle still carry a real off-vocabulary-severity finding and have it silently zeroed on carryforward. Fixed this site to compute `blocking_count` the same way every other site does (`parse_blocking_count` + `count_unrecognized_severity_findings`), not just `nit_count`.
- **Q:** [round-3 GAP] Does the new `plugin.json`-in-`Context:` validator rule false-positive on the primary expected case — a batch that registers a new agent by editing `plugin.json` directly? **A:** [auto-fix] Yes — existing convention (`review-plan-batch.md:40`) never duplicates an `Edits:` file into `Context:`, so the natural agent-registering batch lists `plugin.json` under `Edits:`, not `Context:`, and a Context:-only check would flag it. Changed the rule to accept `plugin.json` in either `Context:` or `Edits:`.
- **Q:** [round-3 GAP] Does the new validator check have a corresponding `mill-plan/SKILL.md` Step 1.5 fix-table row, given every one of the ~20 existing checks has one? **A:** [auto-fix] No — added an explicit decision that the fix-table row is in scope for this task (not a halt case, not general validator-rule authoring): new row `plugin-manifest-context-missing` with mechanical fix "add `plugin.json` to the batch's `Context:` (unless already in `Edits:`)."
- **Q:** [round-4 GAP] Would the planned "parallel" `nit_count` assertions on Test 14/29 actually exercise anything, given their fixtures carry zero `[NIT]` headings? **A:** [auto-fix] No — both fixtures have zero `[NIT]` headings, and today's unfixed `run()` already defaults `nit_count` to 0 (it's never set), so a reused-fixture assertion would read `== 0` and pass identically before and after the fix — a vacuous test. Corrected Testing to require every new/modified `nit_count` assertion be backed by a fixture with at least one real `[NIT]` heading, mirroring the already-correct "not silently 0" requirement on the Test 8 extension.
- **Q:** [round-5 GAP] Does the Testing section's Test 8 extension cover the 6th site's `blocking_count` fix, given round-3 expanded that site's fix to both counters? **A:** [auto-fix] No — Test 8's extension only specified a `[NIT]`-carrying fixture and `nit_count` assertion, leaving `blocking_count` untested for that same site (the identical vacuous-test trap round-4 caught for `nit_count` on Test 14/29). Added a second carried-forward fixture with a real `[BLOCKING]`/off-vocabulary finding and a `blocking_count` assertion.
- **Q:** [round-5 GAP] Does Technical context's 5-site list match the Decisions section, given the Decision was corrected in round 1 to exclude the resume-disk-scan site and add `_scan_approved_batches()` as a 6th site? **A:** [auto-fix] No — the list still said "narrow patch, add `nit_count` line only" for the resume-disk-scan site (directly contradicting the Decision's "left untouched") and had no bullet at all for `_scan_approved_batches()`. Corrected the list to match the Decision exactly: resume-disk-scan → no code change (out of scope), plus a new 6th bullet for `_scan_approved_batches()`.
- **Q:** [round-5 NOTE] Does `test-review-common.py` already have `finalize_scope()` fold-in coverage, making the #720 Testing item's "genuinely new" framing inaccurate? **A:** [auto-fix] Yes — an existing test (~lines 2321-2345) already asserts `blocking_count == 2, nit_count == 1` via the same fold-in path, mixed with real BLOCKING/NIT rather than isolated to zero of each. Reframed the isolated-case item as extending/complementing that existing test, not wholly new ground.
- **Q:** [round-5 NOTE] Does the new `plugin.json` validator rule cover the symmetric case — a batch that only `Deletes:` an agent-definition file? **A:** [auto-fix] No, originally. Since the same path-matching mechanism (`compute_deletes_union`) is already available and used elsewhere in `_review_plan.py`, extended the rule's trigger to include `Deletes:` alongside `Creates:`/`Edits:` rather than deferring it — this is a cheap, symmetric extension, unlike the commands/hooks categories which have no detection mechanism to extend.
- **Q:** [round-6 GAP] Is there a 5th write-site reaching `ReviewResult` that the site enumeration missed, beyond the 4 `finalize_scope()`-refactored sites and the 2 sites added in rounds 2-3? **A:** [auto-fix] Yes — the holistic block's outer `except ReviewError` catch (lines 1014-1024) writes a review file and appends an `"ERROR"`-verdict entry to `reviews[]`, satisfying the Decision's own inclusion test, but had no explicit treatment and hardcoded `"blocking_count": 0` with no `nit_count` key (unlike `finalize()`'s parallel catch, which sets both explicitly). Added it as an explicitly-excluded-from-refactor, parity-only site: add `"nit_count": 0` to its literal dict, matching `finalize()`'s equivalent. Not a runtime bug (`.get()` already defaults correctly) but closes the Agent-mode/subprocess-mode schema-consistency gap this task exists to fix.
